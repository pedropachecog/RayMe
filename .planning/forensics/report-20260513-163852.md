# Forensic Report

**Generated:** 2026-05-13T16:38:52Z
**Problem:** A debug pass deployed `6faf893` (`fix(call): stop post-end response playback`) for an Android call failure where the product requirement was to keep the call alive and deliver the generated AI response live. The patch suppressed post-end generation instead of preventing premature call failure.

---

## Evidence Summary

### Git Activity

- **Last commit at report time:** 2026-05-13 16:33:53 +0000 - `3800391` (`Revert "fix(call): stop post-end response playback"`)
- **Faulty runtime commit:** 2026-05-13 14:23:37 +0000 - `6faf893` (`fix(call): stop post-end response playback`)
- **Relevant prior runtime commit:** 2026-05-13 13:34:07 +0000 - `2d00461` (`fix(call): finalize stalled reconnect backfill`)
- **Uncommitted changes:** yes; debug/evidence notes only:
  - `.planning/debug/phone-calls-missing-chunks.md`
  - `.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-EVIDENCE.md`
- **Active worktrees:** one; no orphaned worktree evidence.
- **Repeated files:** `web-ui/server/app/api/calls.py`, `web-ui/server/tests/test_calls.py`, `ai-backend/app/api/webrtc.py`, and `ai-backend/tests/test_webrtc_signaling.py` were edited by `6faf893` and immediately reverted by `3800391`.

### Planning State

- **Current milestone:** `v1.0`
- **Current phase:** Phase 03.1 Call MVP Stabilization and Regression Fixes
- **STATE.md status:** `ready_to_execute`
- **Last recorded stop point:** `Completed 03.1-06-PLAN.md`
- **Phase gap:** plan `03.1-07` remains incomplete pending Android product-owner acceptance.

### Debug Evidence

The active debug file recorded the fresh Android failure at `2d00461`:

- SQLite persisted short turns, then long `user_speech`, then `call_end`, then final `ai_speech` after `call_end`.
- Web UI logs showed recovered `user_final` reached `/turns`, `llm.first_token` began, a second `/offer` returned 502, terminal cleanup drained events, posted `/end`, and entered failed state before `llm.done`.
- AI backend logs showed `/speak` later enqueued TTS on a closed session; `ai_audio_started` and `ai_done` were skipped because the data channel was closed.

The debug file correctly stated the upstream boundary:

- "terminal media cleanup racing with in-flight recovered turn generation"
- "the call is ended before the assistant response is ready"

The faulty patch did not fix that boundary. It cancelled generation on `/end` and rejected `/speak` after an ended/failed session.

### Artifact Completeness

| Phase | PLAN | CONTEXT | RESEARCH | SUMMARY | VERIFICATION |
|-------|------|---------|----------|---------|--------------|
| 03 | yes | yes | yes | yes | yes |
| 03.1 | yes | yes | yes | partial | no |
| 07 | yes | yes | yes | yes | yes |
| 08 | yes | yes | yes | yes | yes |

Phase 03.1 is expected to remain partial because Android acceptance has not passed.

## Anomalies Detected

### Objective Drift In Debug Checkpoint - Confidence HIGH

**Evidence:** The user reported that the whole poem and AI response were generated but not shown or heard during the live call. The handoff for `6faf893` asked whether the response was "seen/heard during the live call, or only appears in transcript after failure," but the implemented fix made the post-failure response stop being generated instead of making the call stay live.

**Interpretation:** The acceptance target was diluted from "deliver the response live" into "avoid hidden post-end generation." That reframed a product failure as a cleanup consistency problem.

### Wrong Test Oracle - Confidence HIGH

**Evidence:** `6faf893` added tests named `test_end_cancels_server_generation_before_call_end` and `test_webrtc_speak_rejects_ended_session_without_synthesis`. Those tests verify suppression after `/end`. They do not assert that `/end` is delayed, blocked, or avoided while a recovered long-turn response is in flight.

**Interpretation:** The tests faithfully proved the wrong behavior. They converted the observed failure into a negative side-effect test instead of an upstream recovery/liveness test.

### Downstream Patch Treated As Root Fix - Confidence HIGH

**Evidence:** The debug evidence explicitly identified the first user-visible boundary as terminal cleanup racing with recovered turn generation. The patch changed server/backend behavior after terminal cleanup had already happened.

**Interpretation:** The fix operated one boundary too late. It removed the misleading post-end artifact but left the actual premature failure path intact.

### Human Acceptance Misrepresented As Ready - Confidence HIGH

**Evidence:** The final handoff said the deployed commit was ready for Android retest. The retest checklist still asked for live response delivery, but the code change could only prevent post-end response generation.

**Interpretation:** The deployment was presented as acceptance-ready without a product-goal preservation check.

### Existing Guard Was Not Applied - Confidence MEDIUM

**Evidence:** `.planning/OPERATING-NOTES.md` already says: "When the user points out a sequencing or architecture mistake, correct the underlying approach and update durable docs; do not just patch the symptom." It also says manual testing is product-owner acceptance, not first-line QA.

**Interpretation:** The project already had the right principle, but no concrete debug checklist forced the agent to distinguish symptom suppression from upstream correction before deployment.

## Root Cause Hypothesis

The debug process failed because it optimized for the latest observable artifact (`ai_speech` after `call_end`) instead of preserving the product goal (`do not end the call before the recovered long turn can produce and play/show the response`). The local regression tests encoded the downstream cleanup behavior, so verification passed even though the user-visible failure remained. The main agent then over-trusted the structured debug result and did not challenge whether the patch could possibly make the call not fail.

## Immediate Recovery Already Done

1. Reverted `6faf893` with `3800391`.
2. Pushed `3800391` to `origin/main`.
3. Deployed `3800391` through `scripts/deploy-omen.sh`.
4. OMEN deploy completed at `3800391b9a445963f4d1d2aefefbed5f2a5e482f`.

## Required Guardrails Before Further Product Patching

1. **No symptom-suppression acceptance:** A patch that merely cancels, rejects, hides, drops, or suppresses work after a failure cannot be marked acceptance-ready for a bug whose expected behavior is successful completion.
2. **User-goal preservation check:** Before any debug fix is committed or deployed, write one sentence naming the user-visible behavior that must still succeed. If the patch removes that behavior or only prevents a side effect, stop.
3. **First-boundary test requirement:** The first failing regression must assert the earliest wrong state transition or control-flow edge, not a later cleanup artifact. For this incident, the required RED test must assert that terminal cleanup does not post `/end` or enter failed UI while a recovered `user_final` `/turns` stream is active.
4. **Negative tests are not enough:** Tests that only prove "no late generation" or "reject after ended" cannot satisfy call-liveness acceptance.
5. **Checkpoint wording must not broaden the target:** Human verification prompts must ask the user to confirm the original desired success path, not an alternate absence-of-side-effect path.
6. **Agent-result review gate:** When a subagent returns a fix for a live product failure, the main agent must explicitly compare the code diff to the user's reported expected behavior before deployment.

## Recommended Next Product Fix Direction

Do not reapply `6faf893`. The next implementation should target the upstream race:

1. Reproduce the Android failure shape in browser tests: recovered `user_final` starts `/turns`, a reconnect `/offer` fails, and terminal cleanup attempts to `/end` while the turn stream is still active.
2. Make that test expect no `/end` and no failed blocking panel until the recovered turn response has either delivered `ai_audio_started`/`ai_done` live or exhausted a bounded response-delivery recovery path.
3. Only after that RED test exists, patch the call route/client/backend liveness behavior.

---

*Report generated by `$gsd-forensics`. Paths are relative to the repository root.*
