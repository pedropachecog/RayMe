---
phase: 09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/server/app/domain/refusal_guard.py
  - web-ui/server/app/domain/refusal_activity.py
  - web-ui/server/app/domain/llm_stream.py
  - web-ui/server/app/api/calls.py
  - web-ui/server/tests/fixtures/phase091_refusal_corpus.json
  - web-ui/server/tests/test_refusal_guard.py
  - web-ui/server/tests/test_chat_stream.py
  - web-ui/server/tests/test_calls.py
autonomous: true
requirements: [REQ-39]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-17/D-19/D-27: A refusal-then-accepted live call closes attempt 1 and starts accepted caption and speech while attempt 2 and TTS remain open; the guard never waits for a complete LLM or TTS stream."
    - "D-18/D-26: Case- and whitespace-tolerant refusal matching works across arbitrary Unicode token fragments, while benign in-character negation passes with its original UTF-8 text unchanged."
    - "D-20/D-21: Attempts 2 and 3 use distinct seeds plus exact correction `The prior draft broke character. Respond only with the in-world reply.`, never copy rejected prose, and terminate as sanitized llm_refusal_exhausted with no display, speech, or assistant persistence."
    - "An upstream attempt that completes without usable text produces no SSE token, caption, audio submission, or assistant write and uses the existing sanitized empty/upstream failure contract."
    - "Each concurrent generation owns an immutable attempt state, RNG seam, upstream stream, cancellation path, and bounded metadata record without leaking state across threads or actions."
  prohibitions:
    - statement: "RayMe must not turn refusal recovery into whole-response or whole-TTS generate-then-play behavior."
      status: unresolved
    - statement: "RayMe must not report hidden refusal suppression as success or substitute canned roleplay prose after exhaustion."
      status: unresolved
    - statement: "Rejected prose must not enter render output, speech, history, retry prompts, logs, or evidence."
      status: unresolved
  artifacts:
    - path: "web-ui/server/app/domain/refusal_guard.py"
      provides: "Pure bounded PrefixRefusalGuard state machine and typed decision metadata"
    - path: "web-ui/server/app/domain/refusal_activity.py"
      provides: "Content-free bounded process-local, deliberately non-durable recent refusal activity ring"
    - path: "web-ui/server/tests/fixtures/phase091_refusal_corpus.json"
      provides: "Frozen 10-refusal and 12-benign corpus with eight fragmentation schedules"
    - path: "web-ui/server/app/domain/llm_stream.py"
      provides: "Shared accepted-token iterator with explicit upstream ownership and semantic retries"
  key_links:
    - from: "web-ui/server/app/domain/llm_stream.py"
      to: "web-ui/server/app/domain/refusal_guard.py"
      via: "Every streamed and collected completion feeds original chunks through the pre-release guard before yielding accepted text"
      pattern: "PrefixRefusalGuard|llm_refusal_exhausted"
    - from: "web-ui/server/app/domain/llm_stream.py"
      to: "web-ui/server/app/api/calls.py"
      via: "Call turns consume only accepted tokens before ai_token captions and TTS segmentation"
      pattern: "stream_chat_completion|ai_token"
    - from: "web-ui/server/app/domain/refusal_activity.py"
      to: "web-ui/server/app/domain/llm_stream.py"
      via: "Attempt outcomes append allowlisted metadata only after the attempt stream closes"
      pattern: "attempt|reason_code|prefix"
---

<objective>
Prove the phase's highest-risk path end to end: reject one streamed Qwen-style refusal below every consumer, retry with a fresh seed, and start accepted live-call caption and speech before the accepted generation finishes (D-17 through D-22, D-26, D-27).

Purpose: Establish the accepted-token and cancellation architecture before prompt composition, UI, or evidence layers build on it.
Output: Production refusal guard, shared semantic-attempt iterator, bounded content-free activity, frozen corpus, and text/call regressions.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-AI-SPEC.md
@.planning/LIVE-CALL-INVARIANTS.md
@web-ui/server/app/domain/llm_stream.py
@web-ui/server/app/api/calls.py
@web-ui/server/tests/test_calls.py
</context>

## Artifacts this phase produces

- `web-ui/server/app/domain/refusal_guard.py`: `PrefixRefusalGuard`, immutable guard decision/reason metadata, and typed `LLMRefusalExhausted`/empty-output failures.
- `web-ui/server/app/domain/refusal_activity.py`: `RefusalActivityStore` capped at 20 content-free rows per thread.
- `web-ui/server/tests/fixtures/phase091_refusal_corpus.json` and `web-ui/server/tests/test_refusal_guard.py`: frozen corpus/fragmentation and classifier/lifecycle contracts.
- `web-ui/server/app/domain/llm_stream.py`: the single shared accepted-token iterator used by streamed and collected completions.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one refusal-then-accepted call through the lowest shared iterator</name>
  <read_first>
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md — D-17 through D-22 and D-27 lock guard placement, retry, release, failure, metadata, and live-call behavior.
    - web-ui/server/app/domain/llm_stream.py — current SDK/custom-client token seam, random seed, and broad error flattening
    - web-ui/server/app/api/calls.py — current ai_token, segment submission, completion-generator closure, and turn ownership order
    - web-ui/server/tests/test_calls.py — held-open LLM and early-speech regression pattern
    - .planning/LIVE-CALL-INVARIANTS.md — first-playback and explicit-Interrupt non-negotiables
  </read_first>
  <files>web-ui/server/app/domain/refusal_guard.py, web-ui/server/app/domain/llm_stream.py, web-ui/server/app/api/calls.py, web-ui/server/tests/test_calls.py</files>
  <behavior>
    - Attempt 1 streams the observed generic/guideline refusal in arbitrary chunks, releases zero bytes, and is observed closed before attempt 2 starts.
    - Attempt 2 uses a distinct injected seed plus exact correction `The prior draft broke character. Respond only with the in-world reply.` and contains none of attempt 1's prose.
    - Attempt 2 releases a safe prefix to ai_token and the existing TTS segment path while both accepted LLM generation and TTS remain open.
    - Explicit Interrupt closes the current attempt, rejects late caption/audio/completion, and authorizes no durable assistant row.
  </behavior>
  <action>Start with the held-open call regression, then implement `PrefixRefusalGuard` and an accepted-token attempt loop immediately below the shared raw token iterator per D-17. Use three total semantic attempts with SDK retries disabled, fresh injected seeds, explicit async stream closure before retry, cancellation re-raise, and an irreversible passthrough state after first release per D-19/D-20. Freeze retry input at the shared request-builder seam: attempts 2 and 3 add exactly `The prior draft broke character. Respond only with the in-world reply.` and never append any rejected byte. Keep the existing call segmenter, speech scheduler, first/final metric carriers, and turn ownership intact; the call route may expose typed attempt metadata but must not own a second classifier. Count the provisional guard ceiling deterministically as 384 Unicode code points or 96 versioned estimated tokens, with early safe-sentence release after 24 visible characters; retain original chunks separately so accepted text round-trips unchanged.</action>
  <acceptance_criteria>
    - The new integration test observes `attempt_1_closed < attempt_2_open < first_caption < llm_complete` and `ai_audio_started < llm_complete < tts_stream_complete` where applicable.
    - No rejected byte appears in caption events, speech submissions, normal completion, or persisted assistant rows.
    - Retry-request capture proves the exact frozen correction and recursively finds none of the rejected prefix in attempt 2.
    - Interrupt during attempt 2 closes the active upstream response and permits no late normal terminal or retry.
    - The tracer becomes production code; later plans extend its composer inputs without replacing its stream-ownership architecture.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py web-ui/server/tests/test_call_tts_segments.py -q</automated>
    <fails_when>The command exits non-zero, collects 0 tests, a rejected chunk reaches a call spy, stream-close ordering is absent, or first caption/audio occurs only after accepted completion.</fails_when>
  </verify>
  <done>One production refusal-then-accepted live-call path works end to end without contaminating captions, TTS, persistence, or first-playback timing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze Unicode-fragmented refusal precision and metadata-only activity</name>
  <read_first>
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-AI-SPEC.md — REF-01/REF-02/STREAM-01 corpus and latency thresholds
    - web-ui/server/app/domain/call_tts_segments.py — small deterministic feed/finish state-machine analog
    - web-ui/server/tests/test_call_tts_segments.py — parametrized fragmentation-test style
    - web-ui/server/app/domain/refusal_guard.py — tracer contract from Task 1
  </read_first>
  <files>web-ui/server/tests/fixtures/phase091_refusal_corpus.json, web-ui/server/tests/test_refusal_guard.py, web-ui/server/app/domain/refusal_guard.py, web-ui/server/app/domain/refusal_activity.py</files>
  <behavior>
    - Ten explicit generic/guideline refusals match under one chunk, one-code-point chunks, phrase/punctuation boundaries, and four deterministic irregular fragmentations.
    - Twelve benign prefixes, including in-character negation, quoted refusal language, threats, profanity, and fictional rules/safety discussion, pass exactly once with zero retry.
    - Classification uses casefolded/whitespace-collapsed text without altering retained content; combining characters and multilingual fixtures remain byte-equivalent after UTF-8 transport.
    - Guard CPU remains below 5 ms p95 across at least 100 feeds, and the metadata ring never stores prompt, history, prose, key, URL credential, seed, exception string, or audio.
    - Refusal activity is bounded process-local state only: constructing a fresh store after simulated process restart yields a truthful empty list rather than restoring or inventing rows.
  </behavior>
  <action>Freeze the D-18/D-26 positive and benign corpus before tuning. Implement combined refusal-verb plus secondary-signal scoring with bounded precompiled matching, apology lead handling, deterministic Unicode code-point prefix counts, early safe release, and post-release passthrough. Add a process-local `RefusalActivityStore` capped at 20 rows per thread containing only action, attempt, reason code, prefix character/estimated-token counts, retry count, release/decision milliseconds, terminal outcome, and timestamp per D-22. Deliberately add no database/file persistence, startup hydration, or cross-process replication; after restart the absence of rows is truthful product state. Treat an empty upstream completion as unusable output, never a successful empty assistant turn.</action>
  <acceptance_criteria>
    - REF-01 is 100% recall and 100% specificity under every frozen fragmentation with zero refused emitted bytes and zero false retries.
    - Original benign content round-trips unchanged; normalization exists only in the classifier's comparison view.
    - A held-open safe stream releases from `feed()` before `finish()`/upstream completion.
    - Ring eviction is deterministic at 20 rows and a recursive serialization scan finds none of the forbidden content-bearing fields.
    - A fresh-store/restart fixture exposes zero rows and the preview contract treats that as truthful absence, not an all-clear metric.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_refusal_guard.py -q</automated>
    <fails_when>The command exits non-zero, collects 0 tests, any corpus label is missed, any benign byte changes or retries, p95 guard CPU reaches 5 ms, a content/credential/seed canary survives serialization, or activity survives a simulated process restart.</fails_when>
  </verify>
  <done>The classifier and recent-activity contract are frozen, bounded, precise across Unicode fragmentation, content-free, process-local, and truthfully empty after restart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make streamed and collected text share refusal ownership</name>
  <read_first>
    - web-ui/server/app/domain/llm_stream.py — shared stream/collect APIs and persistence callback
    - web-ui/server/tests/test_chat_stream.py — SSE token/done/error and persistence contracts
    - web-ui/server/tests/test_refusal_guard.py — accepted-token attempt semantics from Task 2
  </read_first>
  <files>web-ui/server/app/domain/llm_stream.py, web-ui/server/tests/test_chat_stream.py, web-ui/server/tests/test_refusal_guard.py</files>
  <behavior>
    - Streaming SSE and collected completion APIs both consume the same accepted-token iterator and never create endpoint-local retry loops.
    - Three refusals yield only typed `llm_refusal_exhausted`; captured requests prove attempts 2 and 3 each carry the exact frozen correction, distinct seeds, and no rejected prose; empty output and pre-release transport failure release/persist nothing.
    - A transport error after accepted release surfaces as a stream failure and never begins an invisible replacement answer.
    - Two concurrent requests cancel and retry independently without sharing seeds, buffers, attempt counts, settings, or stream handles.
  </behavior>
  <action>Route `stream_chat_completion()` and `collect_chat_completion()` through the Task 1 accepted-token iterator. Preserve SSE token/done ordering and final persistence only after accepted nonblank completion, surface stable safe error codes without raw upstream text, and inject stream/RNG/request-capture factories for lifecycle tests. Assert that attempts 2 and 3 receive exactly `The prior draft broke character. Respond only with the in-world reply.`, distinct seeds, and none of the rejected prefix; Plan 05 supplies profile-specific roles through the stable request-builder seam.</action>
  <acceptance_criteria>
    - Streamed and collected variants have identical attempt, close, seed, empty-output, cancellation, and post-release-failure behavior.
    - Successful accepted output persists exactly once; refused, exhausted, empty, failed, and cancelled output persists zero assistant rows.
    - Hidden SDK retries are disabled, so observed request count never exceeds three; requests 2 and 3 match the frozen D-20 correction semantics exactly.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_refusal_guard.py web-ui/server/tests/test_chat_stream.py -q</automated>
    <fails_when>The command exits non-zero, collects 0 tests, stream and collect paths diverge, more than three requests occur, attempts 2/3 omit or alter the frozen correction, rejected prose reaches a retry request, or any unsuccessful attempt releases or persists assistant text.</fails_when>
  </verify>
  <done>Every text-generation consumer now inherits one bounded refusal, empty-output, closure, and cancellation contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| External OpenAI-compatible stream → accepted-token iterator | Untrusted, arbitrarily fragmented model text and transport lifecycle enter RayMe. |
| Accepted-token iterator → SSE/call/TTS/persistence | Only pre-release-approved original bytes may cross into visible or durable product state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09.1-01 | Tampering | Attempt/turn ownership | high | mitigate | One request-scoped owner, explicit close-before-retry, cancellation re-raise, and late-event persistence spies. |
| T-09.1-02 | Information Disclosure | Logs/activity/SSE | high | mitigate | Positive metadata allowlist; no prompt, refusal prose, key, seed, URL credential, audio, or raw exception. |
| T-09.1-03 | Denial of Service | Prefix matcher and upstream slots | high | mitigate | Hard prefix/attempt/ring bounds, bounded regexes, max_retries=0, p95 CPU gate, and explicit stream close. |
| T-09.1-04 | Repudiation | Retry outcome telemetry | medium | mitigate | Stable reason codes, attempt/close ordering, timestamped content-free outcomes, and deterministic tests. |
| T-09.1-SC | Tampering | Package supply chain | low | accept | No npm, pip, or cargo package installation occurs in this plan. |
</threat_model>

<verification>
Run all three task commands, then the established call boundaries: `uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py web-ui/server/tests/test_call_tts_segments.py -q` and `uv run --project ai-backend pytest ai-backend/tests/test_call_session.py ai-backend/tests/test_webrtc_signaling.py ai-backend/tests/test_tts_voxcpm2.py -q`.
</verification>

<success_criteria>
Refused prefixes are rejected before every consumer, safe output releases early, all attempt streams close deterministically, and existing call playback/Interrupt/VoxCPM2 invariants remain green.
</success_criteria>

<output>
Create `.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-01-SUMMARY.md` when done.
</output>
