---
phase: 09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc
plan: "04"
subsystem: database
tags: [alembic, sqlalchemy, thread-snapshots, compatibility, tdd]
status: complete
requires:
  - phase: 09.1-01
    provides: frozen D-12 truthful thread-example snapshot contract
provides:
  - nullable revision-0009 thread example snapshot schema with no legacy backfill
  - exact creation-time example capture for new threads
  - stored-column-only hydration that preserves truthful nulls after later card edits
affects: [09.1-05, 09.1-06, 09.1-07, prompt-composer, prompt-inspector]
tech-stack:
  added: []
  patterns: [nullable-compatibility-migration, immutable-thread-snapshot, stored-column-only-hydration, red-green-tdd]
key-files:
  created:
    - web-ui/server/alembic/versions/0009_thread_example_snapshot.py
  modified:
    - web-ui/server/app/storage/models.py
    - web-ui/server/app/domain/thread_service.py
    - web-ui/server/tests/test_migrations.py
    - web-ui/server/tests/test_threads.py
key-decisions:
  - "Treat a null legacy example snapshot as historical truth, never as permission to read the linked character's current mes_example value."
  - "Keep revision 0009 to one nullable Text column with no UPDATE or backfill so unrelated character, thread, lorebook, and source data remains byte-identical."
patterns-established:
  - "New mutable character-card fields cross into conversation history only through an explicit creation-time Thread snapshot."
  - "Compatibility migrations preserve absence as SQL NULL and prove downgrade/re-upgrade behavior against populated legacy rows."
requirements-completed: [REQ-37, REQ-38]
coverage:
  - id: D1
    description: "Added Alembic revision 0009 as a nullable add/drop-only thread example snapshot migration, leaving legacy rows null and all unrelated persisted data byte-identical through upgrade, downgrade, and re-upgrade."
    requirement: REQ-38
    verification:
      - kind: integration
        ref: "web-ui/server/tests/test_migrations.py#test_thread_example_snapshot_upgrade_keeps_legacy_null_and_preserves_all_other_data"
        status: pass
      - kind: integration
        ref: "uv run --project web-ui/server alembic -c web-ui/server/alembic.ini upgrade head"
        status: pass
    human_judgment: false
  - id: D2
    description: "New threads capture the character's exact mes_example bytes once, including blank, Unicode, and combining-character content, and expose the stored value in character_snapshot."
    requirement: REQ-38
    verification:
      - kind: integration
        ref: "web-ui/server/tests/test_threads.py#test_thread_creation_snapshots_examples_exactly_once"
        status: pass
      - kind: integration
        ref: "uv run --project web-ui/server pytest web-ui/server/tests/test_migrations.py web-ui/server/tests/test_threads.py -q (23 passed)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Existing-thread null snapshots remain null after linked character examples change, preventing mature conversations from silently resnapshotting while the global Roleplay default remains live."
    requirement: REQ-37
    verification:
      - kind: integration
        ref: "web-ui/server/tests/test_threads.py#test_thread_detail_keeps_truthful_null_example_snapshot_after_character_edit"
        status: pass
      - kind: integration
        ref: "uv run --project web-ui/server pytest web-ui/server/tests -q (527 passed)"
        status: pass
    human_judgment: false
actuals:
  tokens: 3537
  tasks: 1
  commits: 2
metrics:
  duration: 9m
  completed: 2026-08-31
---

# Phase 09.1 Plan 04: Truthful Thread Example Snapshots Summary

Nullable revision-0009 snapshots now preserve exact creation-time character examples for new threads while old conversations remain truthfully null and isolated from later card edits.

## Performance

- **Duration:** 9m
- **Started:** 2026-08-31T04:15:03Z
- **Completed:** 2026-08-31T04:23:55Z
- **Tasks:** 1
- **Files modified:** 5

## Accomplishments

- Added Alembic revision 0009 as a nullable add/drop-only thread example snapshot migration, leaving legacy rows null and all unrelated persisted data byte-identical through upgrade, downgrade, and re-upgrade.
- New threads capture the character's exact `mes_example` bytes once, including blank, Unicode, and combining-character content, and expose the stored value in `character_snapshot`.
- Existing-thread null snapshots remain null after linked character examples change, preventing mature conversations from silently resnapshotting while the global Roleplay default remains live.

## Task Commits

1. **Task 1 RED: truthful example snapshot regressions** - `18d733d`
2. **Task 1 GREEN: nullable migration and stored snapshot wiring** - `38aed83`

## Files Created/Modified

- `web-ui/server/alembic/versions/0009_thread_example_snapshot.py` - nullable add/drop-only revision descending from 0008.
- `web-ui/server/app/storage/models.py` - nullable `Thread.character_snapshot_mes_example` ORM field.
- `web-ui/server/app/domain/thread_service.py` - exact creation-time copy and stored-column-only detail hydration.
- `web-ui/server/tests/test_migrations.py` - head schema, populated legacy upgrade, downgrade/re-upgrade, null, and unrelated-data preservation coverage.
- `web-ui/server/tests/test_threads.py` - blank/Unicode exact capture, later-card-edit isolation, and truthful legacy-null hydration coverage.

## Decisions Made

- SQL `NULL` means no example snapshot was captured. Hydration returns that value directly and never joins or falls through to the character's current `mes_example`.
- Revision 0009 performs only `add_column` and `drop_column`; it deliberately does not normalize, copy, or backfill any row.
- Existing global prompt settings remain live and untouched, preserving the Roleplay default shipped in Plan 03.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Reconciled the legacy STATE header after the state SDK could not advance it**
- **Found during:** Plan close-out
- **Issue:** `state.advance-plan` could not parse `Current Plan` / `Total Plans in Phase`, and `state.update-progress` skipped the unscoped legacy STATE shape.
- **Fix:** Kept the SDK-applied metric, decisions, session, and ROADMAP updates, then advanced the existing `progress.completed_plans` and `last_activity_desc` fields directly and removed a duplicate metric produced by the interrupted retry.
- **Files modified:** `.planning/STATE.md`
- **Verification:** STATE reports 104 completed plans, Plan 09.1-04 as the last activity/stopped point, and ROADMAP reports 4/14 plans executed.
- **Committed in:** final sequential metadata commit

---

**Total deviations:** 1 auto-fixed (1 blocking workflow compatibility issue).
**Impact on plan:** No product, migration, prompt-default, or compatibility behavior changed; the adjustment only completed canonical metadata reconciliation for the repository's existing STATE shape.

## Issues Encountered

The state SDK's legacy plan-position parser did not match this repository's durable STATE schema. The close-out fields were reconciled without altering unrelated state history.

## Verification

- Focused migration and thread command: **23 passed**.
- Full Web UI server suite: **527 passed**.
- `alembic upgrade head`: **PASS**, ending at `0009_thread_example_snapshot`.
- Ruff lint: **PASS**.
- `git diff --check`: **PASS**.
- Migration no-backfill source scan: **PASS**; revision 0009 contains no update/execute operation.
- TDD sequence: `18d733d` RED precedes `38aed83` GREEN: **PASS**.

## Known Stubs

None.

## Security and Data Integrity

- The planned migration trust boundary is covered by populated legacy-row tests that compare every pre-existing character and thread column before and after each supported migration path.
- Character lorebook/world-info and raw-source JSON bytes remain unchanged; no new network, authentication, file-access, or unmodeled trust surface was introduced.
- Null legacy history cannot be tampered with by later live character edits because thread detail reads only the stored snapshot column.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 05 can parse and budget examples from the truthful nullable thread snapshot without consulting mutable character-card rows.
- REQ-37 and REQ-38 remain shared across later Phase 09.1 plans; the requirements readiness gate should mark them complete only after every declaring sibling plan has a SUMMARY.
- No blockers.

## Self-Check: PASSED

- All five planned implementation/test files and this SUMMARY exist on disk.
- Both TDD commits exist in git history in RED then GREEN order.
- All three coverage entries validate and classify as fully automated passing coverage.
- The focused plan command, full server suite, Alembic head upgrade, lint, and whitespace checks pass.

---
*Phase: 09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc*
*Completed: 2026-08-31*
