---
phase: 09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc
plan: 03
type: execute
wave: 2
depends_on: [09.1-01]
files_modified:
  - web-ui/server/app/domain/prompt_profiles.py
  - web-ui/server/app/domain/settings_service.py
  - web-ui/server/app/api/settings.py
  - web-ui/server/tests/test_prompt_profiles.py
  - web-ui/server/tests/test_health_settings.py
autonomous: true
requirements: [REQ-37, REQ-38]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "D-01/D-02: Missing legacy settings and every new installation resolve live to one persisted Roleplay prompt profile; Assistant and Custom are explicit global selections and no thread freezes global prompt settings."
    - "D-03 through D-06: Versioned Roleplay and Assistant built-ins plus separate Custom Main/Auxiliary/PHI fields round-trip with exact Unicode text and explicit required/optional validation."
    - "Blank card Main and PHI are represented as inheritance inputs, while blank optional Custom Auxiliary/PHI remain truthfully empty and never fabricate a message."
    - "Nested prompt-profile and sampler values round-trip as typed JSON without erasing endpoint/audio/VAD settings or an omitted configured API key."
  prohibitions:
    - statement: "Prompt mode must not silently switch in response to card content, a refusal, model profile, or endpoint error."
      status: unresolved
    - statement: "The persisted prompt contract must not be split into hidden roleplay booleans alongside obsolete flat prompt fields."
      status: unresolved
  artifacts:
    - path: "web-ui/server/app/domain/prompt_profiles.py"
      provides: "PromptMode, ModelProfile, PromptSet, PromptGenerationSettings, defaults, bounds, and versioned built-ins"
    - path: "web-ui/server/app/api/settings.py"
      provides: "Typed nested prompt_generation PATCH/public response contract"
  key_links:
    - from: "web-ui/server/app/api/settings.py"
      to: "web-ui/server/app/domain/settings_service.py"
      via: "Pydantic nested patch uses exclude_unset and merges one prompt profile without touching omitted private endpoint values"
      pattern: "prompt_generation|exclude_unset"
---

<objective>
Persist the Roleplay/Assistant/Custom prompt profile and generation-settings contract independently of the thread snapshot migration (D-01 through D-06, D-15).

Purpose: Give the composer one typed live global settings snapshot instead of flat coercion, hidden roleplay booleans, or automatic mode switching.
Output: Versioned profile/settings model, Settings API persistence, and focused validation/round-trip tests.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-UI-SPEC.md
@web-ui/server/app/domain/settings_service.py
@web-ui/server/app/api/settings.py
</context>

<assumption_delta_decision>
primary_noun: prompt profile
decision: promote
rationale: Persist one selected Roleplay/Assistant/Custom profile plus its versioned Main, Auxiliary, PHI, model-profile, budget, and sampler fields; do not add roleplay booleans beside the old flat prompt fields. The unrelated TTS fallback signal is no-change and remains prohibited by the live-call invariant.
</assumption_delta_decision>

## Artifacts this phase produces

- `web-ui/server/app/domain/prompt_profiles.py`: `PromptMode`, `ModelProfile`, `PromptSet`, `PromptGenerationSettings`, prompt-contract version, exact built-in Roleplay/Assistant prompt bytes, and validation constants.
- `web-ui/server/app/api/settings.py`: nested public prompt-generation DTO with positive private-field omission.
- `web-ui/server/tests/test_prompt_profiles.py`: focused profile/default/validation contracts; the existing settings API suite is expanded without touching the independent relational snapshot slice.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist one live typed prompt profile with Roleplay as the absent-settings default</name>
  <reversibility rating="costly">The nested settings shape is consumed by server and client surfaces; changing it later requires coordinated API, persistence, and fixture updates.</reversibility>
  <read_first>
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md — D-01 through D-06 and D-15 are the exact locked profile/default/persistence contract.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-PATTERNS.md — copy the mapped prompt-profile analog: frozen slotted dataclass values, Protocol boundaries, immutable snapshots, and positive public DTO shaping.
    - web-ui/server/app/domain/prompt_builder.py — copy its `@dataclass(frozen=True, slots=True)` value style and `Protocol` dependency boundary for the new profile contract without coupling profiles to SQLAlchemy.
    - web-ui/server/app/domain/settings_service.py — immutable EndpointSettings, positive public allowlist, JSON persistence, and numeric-cleaner trap
    - web-ui/server/app/api/settings.py — Pydantic PATCH/public models and exclude_unset merge pattern
    - web-ui/server/tests/test_health_settings.py — API key omission and typed numeric JSON round-trip patterns
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-UI-SPEC.md — exact profile values, defaults, bounds, steps, and validation rules
  </read_first>
  <files>web-ui/server/app/domain/prompt_profiles.py, web-ui/server/app/domain/settings_service.py, web-ui/server/app/api/settings.py, web-ui/server/tests/test_prompt_profiles.py, web-ui/server/tests/test_health_settings.py</files>
  <behavior>
    - An absent `prompt_generation` object resolves to selected mode `roleplay`, profile `auto`, the shipped Roleplay/Assistant/Custom sets, context 16384, max output 512, and exact sampler defaults.
    - Roleplay and Assistant require nonblank Main/Auxiliary/PHI; Custom requires Main while optional Auxiliary/PHI may remain empty strings.
    - Context/output and all seven sampler values enforce the UI-SPEC min/max/step contract as typed int/float values.
    - Partial PATCH preserves omitted prompt sets, endpoint/audio/VAD values, and configured API key; public GET/PATCH never returns the key.
  </behavior>
  <action>Create frozen slotted dataclass profile/settings values in `prompt_profiles.py` with exact D-03/D-04/D-05 semantics and a prompt-contract/schema version, following `prompt_builder.py`'s `@dataclass(frozen=True, slots=True)` and `Protocol` boundary patterns rather than mutable dictionaries or persistence-aware domain types. Extend `EndpointSettings`, SettingsService read/update/save, `SettingsPatch`, and `PublicSettings` with one nested `prompt_generation` object per the assumption-delta decision. Resolve missing legacy JSON to Roleplay without writing during reads, merge nested partial updates deliberately, and validate numbers without the current string fallback. Store exact Unicode strings without trimming/normalizing optional prompt content; use whitespace only to decide required-field validity. Keep mode changes independent from model profile, sampler values, Qwen no-thinking, and endpoint settings.</action>
  <acceptance_criteria>
    - Old rows with no nested object GET as Roleplay defaults; saving and rereading all modes/fields yields identical typed values.
    - Built-in Roleplay Main, Auxiliary, and PHI implement D-03/D-04/D-05 and are versioned/resettable; Assistant is conventional and Custom remains separate.
    - Every invalid required prompt, out-of-range value, and off-step value has a stable field-specific error contract; blank optional Custom Auxiliary/PHI are accepted unchanged.
    - API-key canaries appear only in private storage/runtime objects, never the public response or prompt-generation tests.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_prompt_profiles.py web-ui/server/tests/test_health_settings.py -q</automated>
    <fails_when>The command exits non-zero, collects 0 tests, absent settings do not resolve to Roleplay, numeric values rehydrate as strings/blanks, a partial patch erases unrelated settings/key state, or Unicode prompt bytes change.</fails_when>
  </verify>
  <done>One selected live prompt profile and all bounded generation fields persist and round-trip without legacy coercion or secret disclosure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser Settings PATCH → validated settings service | Untrusted prompt text and numeric configuration enter persistent application state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09.1-09 | Tampering | Nested prompt settings | high | mitigate | Pydantic bounds/steps, extra-forbid DTOs, typed merge, and exact round-trip tests. |
| T-09.1-10 | Information Disclosure | Public settings response | high | mitigate | Positive public allowlist and recursive API-key canaries; private values never serialized then removed. |
| T-09.1-12 | Denial of Service | Prompt size/numeric values | medium | mitigate | Explicit prompt field size limits and bounded context/sampler validation at API/persistence boundaries. |
| T-09.1-SC | Tampering | Package supply chain | low | accept | No npm, pip, or cargo package installation occurs in this plan. |
</threat_model>

<verification>
Run the task command and `git diff --check`. Confirm the settings table remains JSON-backed and no relational migration is introduced by this plan.
</verification>

<success_criteria>
Roleplay is the live persisted default for old and new threads, explicit alternatives retain their own fields, and every nested generation value round-trips without private-key disclosure or numeric coercion.
</success_criteria>

<output>
Create `.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-03-SUMMARY.md` when done.
</output>
