---
phase: 09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc
plan: 07
type: execute
wave: 4
depends_on:
  - 09.1-01
  - 09.1-05
files_modified:
  - web-ui/server/app/api/calls.py
  - web-ui/server/tests/test_calls.py
autonomous: true
requirements:
  - REQ-37
  - REQ-38
  - REQ-39
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-09/D-14, call offers and turns share the composer/adapter; only AI-backend call offers apply the 48-message/20,000-character transport ceilings, while call turns use normal configured context/output budgeting."
    - "Per D-17 through D-21 and D-27, the refusal guard is below the shared token iterator and before captions, segmentation, TTS, persistence, and call evidence; a rejected attempt closes before a fresh-seed retry and no rejected byte reaches any downstream surface."
    - "A slow accepted call and a refusal-then-accepted call both begin accepted captions and audio while the accepted model generation and accepted TTS stream remain open; neither model text nor the full TTS synthesis is buffered to completion."
    - "Explicit Interrupt closes owned generation/TTS work and rejects late chunks; listening recovery and the existing turn-ownership/idempotency state remain correct."
    - "Automatic microphone/VAD barge-in remains disabled and VoxCPM2 whole-synthesis fallback remains rejected by regression tests."
    - "An upstream attempt completing without usable text releases no caption, audio, SSE content, or assistant/call-turn persistence and ends through the existing sanitized empty/upstream failure contract."
    - "Each call offer/turn owns its immutable settings/thread snapshot, seed, upstream stream, and retry state, so concurrent or cancelled turns cannot leak messages or mutate another turn."
  artifacts:
    - "web-ui/server/app/api/calls.py routes offer and turn generations through the shared composer/adapter/guard before every call output boundary."
    - "web-ui/server/tests/test_calls.py proves composer parity, early accepted caption/audio, refusal retry, empty upstream, concurrent ownership, and explicit interruption."
    - "Existing AI-backend WebRTC and VoxCPM2 regressions continue to prove listening recovery and forbid whole-synthesis fallback."
  key_links:
    - "call offer/turn -> immutable prompt build -> model adapter -> guarded iterator -> caption segmentation -> streaming TTS -> call SSE/persistence."
    - "Interrupt/turn ownership -> close generation and TTS -> reject late chunks -> listening recovery."
    - "Refusal or empty upstream -> no segment admission -> no TTS/caption/persistence -> sanitized typed failure."
  prohibitions:
    - statement: "Do not wait for the whole assistant response or whole TTS stream before first accepted call playback."
      status: unresolved
    - statement: "Do not reintroduce automatic microphone/VAD barge-in."
      status: unresolved
    - statement: "Do not add or permit VoxCPM2 whole-synthesis fallback."
      status: unresolved
    - statement: "Do not render, speak, persist, retry with, log, or include rejected prose in evidence."
      status: unresolved
    - statement: "Do not report refusal exhaustion or an empty upstream as a successful call turn or substitute canned prose."
      status: unresolved
    - statement: "Do not apply the AI-backend call-offer message ceiling to ordinary call-turn generation."
      status: unresolved
---

<objective>
Put live-call offers and turns on the same prompt and refusal-resistant streaming boundary without compromising the live phone-call contract.

Purpose: Give calls the selected fictional-roleplay prompt semantics and bounded refusal recovery while keeping early caption/audio, explicit interruption, listening recovery, and streaming VoxCPM2 behavior intact.
Output: Unified call generation and live-call regressions that prove accepted-only early streaming end to end.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/LIVE-CALL-INVARIANTS.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-AI-SPEC.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-01-SUMMARY.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-05-SUMMARY.md
@web-ui/server/app/api/calls.py
@web-ui/server/tests/test_calls.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compose call offers and turns from the same immutable prompt build</name>
  <read_first>
    - .planning/LIVE-CALL-INVARIANTS.md — the live streaming, listening recovery, interruption, VAD, and VoxCPM2 constraints are non-negotiable.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md — D-09, D-14, D-15, and D-16 define shared call composition and call-specific limits.
    - web-ui/server/app/api/calls.py — preserve call session, turn ownership, idempotency, segmentation, and TTS orchestration seams.
    - web-ui/server/app/domain/prompt_builder.py — invoke the single logical composer with explicit call-offer/call-turn actions.
    - web-ui/server/app/domain/generation_profiles.py — use the same configured/effective adapter and sampler serialization as text.
    - web-ui/server/tests/test_calls.py — extend established offer/turn tests before implementation.
  </read_first>
  <files>web-ui/server/app/api/calls.py, web-ui/server/tests/test_calls.py</files>
  <behavior>
    - Call offers and turns use the selected persisted prompt profile, card inheritance/replacement, captured examples, stable call history, locked section order, samplers, and effective adapter.
    - Only Call offer enforces the final AI-backend offer contract of 48 messages and 20,000 characters per message after profile mapping; Call turn has no 48-message ceiling and uses `context_limit - configured_max_output - safety_margin` like text generation.
    - Call offer and turn inputs are immutable snapshots; two barrier-synchronized turns keep settings, messages, seeds, stream handles, and retry state isolated.
    - Qwen late PHI remains consumable in call-shaped requests and prompt metadata contains only bounded profile/adapter identifiers.
    - Existing call ownership, idempotency, and lifecycle transitions remain valid.
  </behavior>
  <action>Write offer/turn parity, budget, and concurrency tests first. Remove call-local prompt concatenation and resolve one frozen call-generation input through the Plan 05 composer and adapter per D-09/D-14 with distinct Call offer and Call turn actions. Apply the 48-message/20,000-character final transport ceilings only to the Call offer payload sent to the AI backend; for Call turn, compute the normal output-aware input budget and do not inherit the offer ceiling. Test non-default configured output values and typed mandatory overflow in both actions. Preserve existing call-session ownership/idempotency checks. Give each turn its own settings/thread snapshot, seed handle, upstream handle, retry state, and bounded metadata projection. Do not change microphone/VAD behavior, call control schemas, or segmentation/TTS release in this task.</action>
  <acceptance_criteria>
    - Tests compare call logical sections, adapter, samplers, snapshot examples, and late-PHI placement with the equivalent shared text build.
    - Offer fixtures fail above 48 final messages or 20,000 characters per message after locked drops, while Call turn fixtures prove no 48-message cap and respect only their configured output-aware context budget.
    - Blank and legacy-null example/profile inputs obey D-01/D-07/D-12 with no live example reread.
    - Concurrent call turns prove settings/message/seed/stream/retry isolation.
    - Existing offer, ownership, idempotency, lifecycle, and accepted call-turn tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py web-ui/server/tests/test_call_tts_segments.py -q</automated>
    <fails_when>The command exits non-zero, reports 0 passed, a call bypasses the shared composer/adapter, the offer ceiling reaches Call turn, a non-default output value is ignored, snapshot state crosses turns, or call lifecycle regressions appear.</fails_when>
  </verify>
  <done>Call offers and turns consume the same exact immutable prompt build as text actions while retaining existing call lifecycle and isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admit only accepted text into live caption and streaming TTS</name>
  <read_first>
    - .planning/LIVE-CALL-INVARIANTS.md — first playback must precede stream completion and explicit Interrupt must reject late chunks.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md — D-17 through D-22 and D-27 lock guard placement, retry count, errors, metadata, and call regression evidence.
    - web-ui/server/app/api/calls.py — place the guard before caption segmentation, TTS, call SSE, and persistence without adding a whole-response collector.
    - web-ui/server/app/domain/refusal_guard.py — consume the shared bounded Unicode-aware prefix decision.
    - web-ui/server/tests/test_calls.py — add slow accepted/refusal/empty/interrupt timing barriers before implementation.
    - ai-backend/tests/test_tts_voxcpm2.py — preserve the existing rejection of whole-synthesis fallback.
  </read_first>
  <files>web-ui/server/app/api/calls.py, web-ui/server/tests/test_calls.py</files>
  <behavior>
    - Slow accepted text crosses caption segmentation and begins TTS/audio while both accepted upstream generation and TTS synthesis remain open.
    - Fragmented refusal text reaches no caption, TTS input, audio, SSE content, persistence, later retry context, log capture, or evidence; its stream closes before fresh-seed attempts 2/3 carrying exactly the frozen correction and no rejected prose.
    - Refusal-then-accepted text starts accepted caption/audio while accepted generation/TTS remain open; three refusals yield a sanitized typed call failure.
    - Empty upstream completion produces no caption/audio/content/persistence and returns the established sanitized empty/upstream failure.
    - Explicit Interrupt closes generation and TTS, rejects barrier-released late chunks, and restores listening state; automatic VAD does not interrupt.
  </behavior>
  <action>Add deterministic async barriers around model chunks, segment admission, TTS first chunk, upstream completion, Interrupt, and late chunks. Move the shared guarded iterator directly ahead of call segmentation so accepted chunks can continue through the existing bounded segmenter immediately after prefix acceptance; never collect the whole response. On rejection, close the upstream before allocating a fresh seed and make attempts 2 and 3 use the adapter-positioned exact correction `The prior draft broke character. Respond only with the in-world reply.`; recursive request/sink canaries prove rejected prose is never copied. Treat empty output and exhaustion as typed failures and stage persistence until usable accepted content exists. Propagate Interrupt cancellation through the owned model/TTS pipeline and guard late events using the existing turn-ownership epoch. Leave automatic microphone/VAD barge-in disabled and retain the VoxCPM2 streaming-only contract.</action>
  <acceptance_criteria>
    - Barrier assertions prove first accepted caption and first accepted audio precede accepted model completion and TTS completion in slow and refusal-then-accepted cases.
    - Negative spies prove zero rejected/empty text at every caption, segment, TTS, audio, SSE, persistence, retry-context, log, and evidence sink.
    - Interrupt tests prove late chunks are ignored and listening recovery occurs without VAD-triggered interruption.
    - VoxCPM2 tests still reject whole-synthesis fallback and AI call-session/WebRTC tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py web-ui/server/tests/test_call_tts_segments.py -q</automated>
    <fails_when>The command exits non-zero, reports 0 passed, first playback waits for completion, rejected/empty text reaches a sink, or Interrupt/listening assertions fail.</fails_when>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_call_session.py ai-backend/tests/test_webrtc_signaling.py ai-backend/tests/test_tts_voxcpm2.py -q</automated>
    <fails_when>The command exits non-zero, reports 0 passed, WebRTC/listening recovery regresses, or VoxCPM2 accepts a whole-synthesis fallback path.</fails_when>
  </verify>
  <done>Accepted call text reaches caption and audio early, rejected or empty text reaches nothing, and explicit interruption remains authoritative through late model/TTS chunks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| call signaling/browser -> call API | Untrusted call/turn IDs, control events, and Interrupt timing cross into owned async work. |
| inference stream -> refusal guard | Arbitrarily fragmented model Unicode is untrusted until the bounded guard accepts it. |
| accepted guard output -> captions/TTS/audio/persistence | Text becomes visible, audible, and durable only after acceptance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09.1-07-01 | Information Disclosure | caption/TTS/audio sinks | high | mitigate | Guard before segmentation, negative sink spies, accepted-only persistence, and sanitized evidence/metadata. |
| T-09.1-07-02 | Tampering | Interrupt and late chunks | high | mitigate | Existing turn ownership/epoch checks, propagated cancellation, owned stream closure, and barrier-based late-event regressions. |
| T-09.1-07-03 | Denial of Service | model/TTS retries | high | mitigate | Three model attempts, bounded prefix/startup buffering, immediate rejected/cancelled stream closure, and no whole-response/TTS buffering. |
| T-09.1-07-04 | Spoofing | concurrent turn ownership | medium | mitigate | Preserve call-session ownership and idempotency validation before generation and at every late output boundary. |
| T-09.1-SC | Tampering | package supply chain | low | accept | No package-manager install occurs in this plan. |
</threat_model>

## Artifacts this phase produces

- Updated `web-ui/server/app/api/calls.py`: shared call-offer/turn composition, guarded pre-segmentation streaming, typed refusal/empty failures, owned cancellation, and bounded metadata.
- Extended `web-ui/server/tests/test_calls.py`: prompt parity, Qwen late-PHI, request isolation, slow early playback, refusal retry, empty output, sink exclusion, and Interrupt timing contracts.
- Preserved `ai-backend/tests/test_call_session.py`, `test_webrtc_signaling.py`, and `test_tts_voxcpm2.py`: listening/transport/streaming-fallback protected regressions.

<verification>
Run both exact proven command lines from Task 2, the Task 1 command, and `git diff --check`. Confirm no production path awaits model completion before segment admission and no new automatic VAD interrupt handler exists.
</verification>

<success_criteria>
Calls use the shared roleplay prompt and refusal recovery, begin accepted caption/audio before generation/TTS completion, emit no rejected or empty content, and retain explicit Interrupt, listening recovery, and VoxCPM2 streaming-only behavior.
</success_criteria>

<output>
Create `.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-07-SUMMARY.md` when done.
</output>
