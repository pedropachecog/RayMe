---
phase: 09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc
plan: 02
type: execute
wave: 2
depends_on: [09.1-01]
files_modified:
  - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-evidence-manifest.json
  - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/test_phase091_evidence.py
  - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-run-omen-evidence.py
  - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-verify-evidence.py
  - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/results/.gitignore
autonomous: true
requirements: [REQ-37, REQ-38, REQ-39]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Before provider serialization or deployed evidence assumes anything about OMEN, a read-only preflight observes exact deployed commit/health, model identity, context capacity, chat-template identity/hash, and assistant-prefill configuration; research-time identity remains explicitly unknown until this passes."
    - "D-26: The evidence contract freezes 12 blank/weak/strong/mature/antagonistic scenarios at three fixed evidence seeds, while ordinary runtime seeds remain fresh and undisclosed."
    - "D-27: Call-shaped evidence distinguishes first caption, first speech, LLM completion, TTS stream completion, final playout, Interrupt, and late-chunk rejection."
    - "D-28: Decision-ready verification fails unless local/origin/deployed commits, health, server-reported model/template, effective request, fixed matrix, and call stream all identify the intended release."
    - "Shared artifacts contain only allowlisted IDs, hashes, counts, scalar timings, versions, and gate outcomes—never prompt/history/rejected prose, credentials, runtime seeds, audio, or private paths."
  prohibitions:
    - statement: "Shared evidence must not expose credentials, authorization headers, runtime seeds, private prompt/history, rejected prose, audio, or absolute private paths."
      status: unresolved
    - statement: "Deployment evidence must not use an ad-hoc OMEN launcher, scheduled-task edit, or deployment mechanism outside scripts/deploy-omen.sh."
      status: unresolved
    - statement: "Evidence must not treat hidden refusal suppression, a mocked browser path, or a model-only request as successful live-call proof."
      status: unresolved
  artifacts:
    - path: ".planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-evidence-manifest.json"
      provides: "Versioned 36-turn matrix, lifecycle scenarios, thresholds, privacy rules, and artifact inventory"
    - path: ".planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-run-omen-evidence.py"
      provides: "Read-only identity preflight plus production-path exact-commit request/call evidence acquisition"
    - path: ".planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-verify-evidence.py"
      provides: "Contracts-only and decision-ready fail-closed verifier"
    - path: ".planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/test_phase091_evidence.py"
      provides: "Hermetic schema, mutation, aggregate, and privacy tests"
    - path: ".planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/results/.gitignore"
      provides: "Allowlist-style exclusion of generated/raw evidence, including omen-preflight.json, by default"
  key_links:
    - from: ".planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/results/omen-preflight.json"
      to: ".planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-evidence-manifest.json"
      via: "Observed sanitized identity/capacity/template/prefill facts bind later expectations; no default fills missing live identity"
      pattern: "model_id|context_capacity|chat_template_sha256|assistant_prefill"
    - from: ".planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-evidence-manifest.json"
      to: ".planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-run-omen-evidence.py"
      via: "Runner loads the immutable scenario, seed, expected-event, threshold, and result-path inventory"
      pattern: "scenario_id|seed|expected_events|thresholds"
    - from: ".planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-run-omen-evidence.py"
      to: ".planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-verify-evidence.py"
      via: "Runner writes allowlisted result artifacts that the verifier independently recomputes"
      pattern: "expected_commit|decision_ready|privacy"
---

<objective>
Observe the live OMEN provider contract read-only, then freeze the Phase 09.1 evidence contract so prompt, refusal, UI, and live-call implementations cannot assume identity or redefine success after seeing model output (D-26 through D-28).

Purpose: Ground provider work in observed model/capacity/template/prefill facts and make exact-commit, refusal-matrix, early-stream, request-parity, and privacy acceptance independently verifiable before canonical deployment.
Output: Sanitized read-only preflight, manifest, hermetic tests, OMEN evidence runner, fail-closed verifier, and result-ignore policy.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-AI-SPEC.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-VALIDATION.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-evidence-manifest.json
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-run-omen-evidence.py
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-verify-evidence.py
@.planning/REFERENCE-AUTHORIZATION-PROHIBITION.md
</context>

## Artifacts this phase produces

- `.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-evidence-manifest.json`: `REF-01`, `REF-02`, `PROMPT-01`, `STREAM-01`, `CALL-01`, `STATE-01`, `QUALITY-01`, `DEPLOY-01`, and `PRIV-01` gate inventory plus exact result paths.
- `.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-run-omen-evidence.py` and `.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-verify-evidence.py`: read-only preflight, `EvidenceRunnerError`, manifest loader, production probes, local result writer, contracts-only/local-release/decision-ready verifier, and sanitized CLI failures.
- `.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/test_phase091_evidence.py`: hermetic runner/verifier and hostile mutation suite.
- `.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/results/.gitignore`: allowlist-style generated/raw evidence exclusion.
- Ignored `results/omen-preflight.json`: sanitized observed commit/health/model/context/template-hash/prefill facts; no raw template, credentials, URL, or guessed values.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Observe the live OMEN model/template contract without mutation</name>
  <precondition>The existing canonical `rayme-pmpg` OMEN SSH target, deployed RayMe health endpoint, and configured OpenAI-compatible LLM endpoint are reachable for read-only probes.</precondition>
  <read_first>
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-RESEARCH.md — the live model ID, context capacity, chat template, and assistant-prefill flag are explicitly unknown in research and must be observed by this preflight.
    - scripts/deploy-omen.sh — reuse canonical OMEN connection/identity conventions without invoking deployment or changing launcher/task state.
    - .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-run-hardware-tracer.py — exact-commit and sanitized remote-probe failure patterns only.
    - web-ui/server/app/domain/settings_service.py — read configured model/base endpoint privately without serializing credentials.
  </read_first>
  <files>.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-evidence-manifest.json, .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/test_phase091_evidence.py, .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-run-omen-evidence.py, .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/results/.gitignore</files>
  <behavior>
    - Preflight starts with no expected model identity, capacity, template, or assistant-prefill value and obtains each from the running OMEN checkout/RayMe/LLM service using read-only operations only.
    - The result records exact deployed commit and health, server-reported model ID, numeric context capacity, chat-template SHA-256 plus byte count/stable template identifier, and whether assistant prefill is enabled or explicitly disabled.
    - Missing, ambiguous, conflicting, or unparseable identity/capacity/template/prefill facts fail closed; no Qwen name, capacity, template hash, or prefill default is substituted from research.
    - Shared output contains only allowlisted IDs, hashes, counts, booleans, UTC freshness, and source endpoint labels—never credentials, credential-bearing URLs, raw template text, prompts, history, generated prose, seeds, or private paths.
    - Tests prove the preflight performs no POST/PATCH/PUT/DELETE, deployment, settings write, database mutation, service restart, launcher/task edit, or runtime seed allocation.
  </behavior>
  <action>Add a tested `--preflight --output` mode to the tracked runner before any provider/evidence implementation. Reuse the canonical OMEN target and existing private settings boundary to issue only GET/read-only SSH probes for deployed commit/health and OpenAI-compatible model plus llama-server `/props`-equivalent data. Hash the exact returned chat-template bytes in memory and retain only SHA-256, byte count, stable server template identifier, and assistant-prefill enabled/disabled fact. Write the sanitized ignored result atomically to `results/omen-preflight.json`; do not deploy, restart, alter settings, query generated completions, or treat `unsloth/Qwen3.5-27B` (or any other research-time identity) as observed. Freeze manifest/test fields that make later Task 2/3 and Plan 05 fail when the result is absent, stale, ambiguous, or mismatched.</action>
  <acceptance_criteria>
    - Hermetic spies prove every probe is read-only and every missing/ambiguous/conflicting model, capacity, template, or prefill response fails.
    - The live invocation writes a fresh sanitized `omen-preflight.json` with all six facts and no assumed fallback.
    - Recursive scans find zero credentials, URLs with userinfo/query secrets, raw template text, prompt/history/generated prose, runtime seeds, tracebacks, or private paths.
    - No deployment, settings/database mutation, service control, scheduled-task, launcher, or completion-generation call occurs.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/test_phase091_evidence.py -q -k 'preflight or model_identity or context_capacity or chat_template or assistant_prefill' &amp;&amp; python3 .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-run-omen-evidence.py --preflight --output .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/results/omen-preflight.json</automated>
    <fails_when>Either command exits non-zero, 0 tests run, a required live fact is missing/assumed/ambiguous, a mutating request or deployment action occurs, or a private/raw canary reaches the result.</fails_when>
  </verify>
  <done>The exact live OMEN model, capacity, template identity, and assistant-prefill configuration are observed read-only and sanitized before any dependent provider or evidence work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze evidence schema, thresholds, and hostile verifier mutations</name>
  <read_first>
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-AI-SPEC.md — canonical dataset, gate IDs, strict thresholds, privacy rules, and QUALITY-01 rubric
    - .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-evidence-manifest.json — tracked versioned manifest analog
    - .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/test_phase09_evidence.py — hermetic import, fixture, mutation, and recomputation analog
    - .planning/REFERENCE-AUTHORIZATION-PROHIBITION.md — fields and fallback behavior that must not be copied from historical evidence
  </read_first>
  <files>.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-evidence-manifest.json, .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/test_phase091_evidence.py, .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-verify-evidence.py, .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/results/.gitignore</files>
  <behavior>
    - Manifest names the fresh `results/omen-preflight.json` prerequisite and forbids any hardcoded expected model/capacity/template/prefill value before Task 1 observes it.
    - Manifest names 12 deployed prompt/refusal scenarios × 3 fixed evidence seeds, six lifecycle traces, eight request-composition gold cases, and eight domain-quality cases with immutable IDs.
    - Manifest names ignored result artifacts `results/local-verification.json`, `results/omen-evidence.json`, and `results/decision-report.json` with strict allowlisted schemas.
    - Verifier independently recomputes every threshold and rejects missing/wrong types, commit/model/template mismatch, stale artifacts, event-order failure, false retries, request diffs, or nonzero secret/private-content canaries.
    - Hostile mutations remove artifacts/fields, alter aggregates/timestamps/commit/model/event order, insert credentials/raw prompt/rejected-prose canaries, and prove each mutation fails.
  </behavior>
  <action>Port only the Phase 09 structural evidence patterns: versioned manifest, explicit artifact and critical-gate inventories, strict type helpers, independent aggregate recomputation, contracts-only/local-release/decision-ready modes, freshness/expected-commit checks, and recursive privacy scanning. Make Task 1's `results/omen-preflight.json` a required prerequisite whose observed fields bind provider expectations; reject absent/stale/ambiguous/mismatched identity and never hardcode a research-time model, capacity, template, or prefill value. Name `results/omen-preflight.json`, `results/local-verification.json`, `results/omen-evidence.json`, and `results/decision-report.json` as the only admitted generated result shapes and keep them ignored by default. Replace voice/TTS scenarios with the D-26/D-27 prompt/refusal/call matrix and omit every historical authorization, steward, consent, provenance-form, use-scope, or generated-person fallback field. Freeze exact thresholds from AI-SPEC rather than storing a trusted overall-pass boolean.</action>
  <acceptance_criteria>
    - The manifest schedules exactly 36 deployed Qwen turns and names every required request, lifecycle, privacy, and quality gate.
    - Every mutation test is fail-first: the pristine synthetic bundle passes contracts-only, and each independently corrupted bundle fails for the intended reason.
    - Recursive canary scans cover nested dictionaries, lists, attachments, and stringified fields without retaining the canary source in admitted results.
    - Result paths are explicit and the ignore file prevents raw/generated evidence from being committed by default.
    - Hostile mutations prove a missing/stale/changed preflight identity, context capacity, template hash, or prefill flag invalidates provider and decision-ready evidence.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/test_phase091_evidence.py -q &amp;&amp; python3 .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-verify-evidence.py --contracts-only</automated>
    <fails_when>Either command exits non-zero, collects 0 tests, pristine contracts fail, any hostile mutation passes, preflight drift is accepted, the scenario count is not 36, or a prohibited authorization/fallback field appears in the manifest schema.</fails_when>
  </verify>
  <done>The evidence schema and verifier failure surface are frozen before production implementation can tune to them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build sanitized production-path OMEN acquisition</name>
  <read_first>
    - .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-run-omen-evidence.py — production API/CLI/protocol and sanitized result-writing analog
    - scripts/deploy-omen.sh — sole deployment mechanism and current exact-commit/health conventions
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-evidence-manifest.json — Task 1 source of scenario truth
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-01-SUMMARY.md — accepted-token event and attempt fields actually implemented
  </read_first>
  <files>.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-run-omen-evidence.py, .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-verify-evidence.py, .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/test_phase091_evidence.py</files>
  <behavior>
    - Runner reloads the fresh read-only preflight, requires current server-reported model ID, context capacity, chat template/prefill properties to equal it, verifies current health independently, and binds RayMe commit identity to the caller-supplied intended release rather than the older preflight commit.
    - The 36-turn quality/refusal matrix uses the configured real Qwen/llama-server endpoint; a separate runner-owned controlled slow OpenAI-compatible fixture on the deployed stack forces refusal attempt 1 then held-open accepted attempt 2 so first-caption/first-speech ordering is observable rather than dependent on Qwen choosing to refuse.
    - Shared result JSON contains scenario IDs, fixed evidence-seed labels, versions, hashes/counts, scalar timing/event facts, gate outcomes, commit/model identity, and freshness only.
  </behavior>
  <action>Implement strict manifest loading and ordered production dispatch through RayMe's existing same-origin APIs. Require the fresh Task 1 preflight and fail if the live model/context/template/prefill facts have drifted; treat the preflight RayMe commit as provenance only, require current health, and bind the current deployed RayMe commit to the caller's explicit `--expected-commit`. Only then run the deployed 36-turn lane against the configured observed Qwen/llama-server identity. For CALL-01/STATE-01, have the tracked runner start a bounded loopback OpenAI-compatible slow fixture on OMEN and exercise the exact deployed Web call route through a runner-scoped dependency/endpoint override held in memory; never save the fixture URL or replace global endpoint/settings/database state, and stop the fixture in `finally`. Keep live inspector/request prose local; persist only parity hashes/counts and classifier/event outcomes to `results/omen-evidence.json`, and emit independent recomputation to `results/decision-report.json`. Provide allowlisted `--record-local-verification` and verifier `--local-release --expected-commit` modes used by Plan 14 for `results/local-verification.json`; that local result must carry the exact key `intended_commit` and the verifier must reject missing or unequal values. Capture immediate and final timing carriers separately, prove rejected upstream closure, verify post-run health/settings equality, and quarantine/refuse publication on any privacy scan failure. The runner must never deploy, edit OMEN tasks, write helper scripts/launchers outside the tracked repo, or manipulate scheduled tasks; Plan 14 invokes it only after `scripts/deploy-omen.sh` succeeds.</action>
  <acceptance_criteria>
    - Hermetic fakes exercise every runner branch without OMEN access, including refusal retry, benign no-retry, empty output, post-release failure, Interrupt, model mismatch, and privacy quarantine.
    - Controlled-call teardown proves the loopback fixture stops and deployed settings, health, database configuration, and scheduled services match their pre-run state.
    - The runner cannot emit decision-ready output when commit/health/model/template/effective request or call-shaped event ordering is absent.
    - A preflight/current-runtime provider-fact mismatch fails before the first generated matrix request; a changed RayMe commit is accepted only when it equals the explicit intended release SHA.
    - No raw request/response prose, full logs, tracebacks, credentials, seed values, audio, or private paths are written.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/test_phase091_evidence.py -q &amp;&amp; python3 .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-verify-evidence.py --contracts-only</automated>
    <fails_when>Either command exits non-zero, 0 tests run, the runner accepts mocked/model-only proof as deployed proof, or any raw/private/credential/runtime-seed canary reaches an admitted artifact.</fails_when>
  </verify>
  <done>The phase has a self-tested, exact-commit, privacy-safe acquisition and independent verification path ready for the final deployment plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Live OMEN/Qwen/RayMe responses → evidence runner | External runtime data, prose, URLs, errors, and model claims are untrusted and private. |
| Runner working data → shared result artifacts | Only explicitly allowlisted scalar/hash/ID evidence may cross into committed/shareable state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09.1-05 | Spoofing | Commit/model/template identity | high | mitigate | Exact local/origin/deployed SHA equality plus server-reported identity and freshness checks. |
| T-09.1-06 | Tampering | Evidence aggregates | high | mitigate | Immutable manifest, raw scalar recomputation, strict types, and hostile mutation tests. |
| T-09.1-07 | Information Disclosure | Shared JSON/attachments | high | mitigate | Positive field allowlist, recursive canary scan, quarantine, and no prose/audio/private paths. |
| T-09.1-08 | Repudiation | Gate outcomes | medium | mitigate | Named gate IDs, scenario/event inventories, timestamps, exact commit/model, and independent verifier messages. |
| T-09.1-SC | Tampering | Package supply chain | low | accept | No npm, pip, or cargo package installation occurs in this plan. |
</threat_model>

<verification>
Run both task commands. Review `git status --short` to confirm only the five named evidence source files changed and generated `results/` payloads remain ignored.
</verification>

<success_criteria>
Evidence contracts fail closed under mutation, production acquisition is privacy-safe and exact-commit-bound, and the final deploy can prove effective request plus live call ordering without sharing private prose.
</success_criteria>

<output>
Create `.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-02-SUMMARY.md` when done.
</output>
