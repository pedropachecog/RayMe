---
phase: 09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc
plan: "06"
subsystem: prompt-generation
tags: [structured-composer, refusal-guard, sse, message-actions, unicode, tdd]
status: complete
requires:
  - phase: 09.1-01
    provides: accepted-token refusal guard, bounded retries, exact correction, typed failures, and owned stream lifecycle
  - phase: 09.1-05
    provides: immutable structured composer and model-aware generation adapter
provides:
  - one structured composer and guarded accepted-token path for Send, Regenerate, Swipe, and Continue
  - early byte-exact accepted SSE with closed rejected attempts and exact correction-only retries
  - atomic accepted-only message-version mutations with sanitized typed failures
  - saved-profile forwarding with legacy injected-client compatibility for unchanged defaults
affects: [09.1-07, 09.1-08, prompt-inspector, text-chat]
actuals:
  tokens: 11513
  tasks: 2
  commits: 5
tech-stack:
  added: []
  patterns: [structured-consumer-boundary, accepted-only-persistence, atomic-message-actions, red-green-tdd]
key-files:
  created: []
  modified:
    - web-ui/server/app/api/chat.py
    - web-ui/server/app/api/messages.py
    - web-ui/server/app/domain/message_actions.py
    - web-ui/server/tests/test_chat_stream.py
    - web-ui/server/tests/test_message_actions.py
key-decisions:
  - "Send, Regenerate, Swipe, and Continue consume section-tagged structured composer output through the same accepted-token iterator; endpoints do not assemble prompts or own retry loops."
  - "Message-action version state changes only after guarded collection returns usable accepted text; exhaustion, empty output, and cancellation leave selection and alternates unchanged."
  - "Injected custom clients retain the historical four-field settings shape for the unchanged default profile, while production and explicitly saved profiles receive the complete structured generation settings."
patterns-established:
  - "Consumer parity: every text action supplies an explicit action and optional Continue prefill to the shared composer before the shared adapter/guard boundary."
  - "Failure atomicity: no message or alternate mutation occurs until accepted completion has succeeded, and client errors expose only stable typed codes and sanitized messages."
requirements-completed: [REQ-37, REQ-38, REQ-39]
coverage:
  - id: D1
    description: "Send now resolves endpoint settings once, builds section-tagged structured prompt candidates, and streams them through the one shared adapter and accepted-token iterator."
    requirement: REQ-38
    verification:
      - kind: integration
        ref: "web-ui/server/tests/test_chat_stream.py#test_send_endpoint_routes_one_saved_profile_through_structured_composer"
        status: pass
      - kind: integration
        ref: "uv run --project web-ui/server pytest web-ui/server/tests/test_chat_stream.py -q (18 passed)"
        status: pass
    human_judgment: false
  - id: D2
    description: "A held-open accepted Send attempt releases combining and multilingual UTF-8 before upstream completion, while a refused attempt closes before an exact correction-only retry and leaks no rejected prose."
    requirement: REQ-39
    verification:
      - kind: integration
        ref: "web-ui/server/tests/test_chat_stream.py#test_refusal_retry_releases_exact_unicode_before_accepted_stream_completion"
        status: pass
      - kind: integration
        ref: "uv run --project web-ui/server pytest web-ui/server/tests -q (562 passed)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Regenerate, Swipe, and Continue use the same structured composer and guarded collector while preserving existing accepted version, selection, and Continue-prefix semantics."
    requirement: REQ-38
    verification:
      - kind: integration
        ref: "web-ui/server/tests/test_message_actions.py#test_generation_actions_share_saved_profile_and_structured_composer"
        status: pass
      - kind: integration
        ref: "uv run --project web-ui/server pytest web-ui/server/tests/test_message_actions.py web-ui/server/tests/test_chat_stream.py web-ui/server/tests/test_generation_profiles.py -q (64 passed)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Refusal exhaustion, empty output, and cancellation persist no rejected or partial assistant text and do not change message-version counts, content, or selected alternate state."
    requirement: REQ-39
    verification:
      - kind: integration
        ref: "web-ui/server/tests/test_message_actions.py#test_generation_action_outcome_matrix_is_accepted_only_and_atomic"
        status: pass
      - kind: integration
        ref: "web-ui/server/tests/test_message_actions.py#test_message_action_routes_return_typed_failures_without_version_mutation"
        status: pass
    human_judgment: false
  - id: D5
    description: "Default Roleplay settings and explicitly saved prompt profiles reach every text consumer without endpoint-local fallback prose, prompt reconstruction, or hidden moderation."
    requirement: REQ-37
    verification:
      - kind: integration
        ref: "web-ui/server/tests/test_chat_stream.py#test_send_endpoint_routes_one_saved_profile_through_structured_composer"
        status: pass
      - kind: integration
        ref: "web-ui/server/tests/test_message_actions.py#test_message_action_routes_forward_qwen_disable_thinking_setting"
        status: pass
      - kind: integration
        ref: "web-ui/server/tests/test_phase1_acceptance.py#test_phase1_import_chat_actions_reload_and_continue"
        status: pass
    human_judgment: false
metrics:
  duration: 19m45s
  completed: 2026-08-31
---

# Phase 09.1 Plan 06: Unified Guarded Text Generation Summary

All four text actions now consume the same structured prompt and accepted-token pipeline, preserving early Unicode streaming, exact refusal recovery, atomic message versions, and sanitized typed failures.

## Performance

- **Duration:** 19m45s
- **Started:** 2026-08-31T05:23:37Z
- **Completed:** 2026-08-31T05:43:22Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Routed Send through the structured composer using one SettingsService snapshot and section-tagged candidates before the shared adapter/guard stream.
- Routed Regenerate, Swipe, and Continue through that same composer and guarded collector, retaining explicit action semantics and Continue prefill.
- Proved early accepted release on a held-open upstream, exact combining/multilingual UTF-8 round trips, close-before-retry, frozen correction text, and complete rejected-prose isolation.
- Made exhausted, empty, and cancelled message actions atomic and exposed stable sanitized failure codes without changing prior message-version state.

## Task Commits

1. **Task 1 RED: require structured Send generation** - `b90707d`
2. **Task 1 GREEN: unify guarded Send generation** - `9202f15`
3. **Task 2 RED: require structured message actions** - `2b61f87`
4. **Task 2 GREEN: unify guarded message actions** - `69b2538`
5. **Task 2 Rule 1 fix: preserve injected-client settings compatibility** - `2b053bc`

## Files Created/Modified

- `web-ui/server/app/api/chat.py` - resolves Send settings once and invokes the structured composer plus guarded SSE path.
- `web-ui/server/app/api/messages.py` - maps guarded action failures to sanitized typed HTTP details and passes saved generation settings.
- `web-ui/server/app/domain/message_actions.py` - gives Regenerate, Swipe, and Continue one structured composer/collector invocation while staging state mutations after success.
- `web-ui/server/tests/test_chat_stream.py` - covers structured Send settings, held-open early Unicode release, refusal isolation, and exact retry correction.
- `web-ui/server/tests/test_message_actions.py` - covers action parity, accepted/refused/exhausted/empty/cancelled outcomes, atomic version state, and typed errors.

## Decisions Made

- Route code owns validation and one settings read; structured prompt construction and accepted-token handling stay in their shared domain seams.
- Rejected attempts never become candidate text for later attempts. Attempts two and three receive only `The prior draft broke character. Respond only with the in-world reply.` from the shared adapter.
- Existing custom-client tests keep their legacy settings projection only for the unchanged default profile. This compatibility path does not alter production requests or explicitly saved profiles.

## Requirements Delivered (Verbatim)

- **REQ-37** `[v1]` — **Default character roleplay contract**: every new and existing character conversation resolves to the Roleplay prompt preset unless the user explicitly selects Assistant or Custom. Blank card-level Main or post-history fields inherit the active preset. The default instructs the LLM to produce the selected character's next in-world reply and must not silently fall back to generic helpful-assistant identity or guideline-refusal boilerplate.
- **REQ-38** `[v1]` — **Shared SillyTavern-class prompt composer**: text and live-call generation use one ordered, token-budgeted composer supporting global Main, Auxiliary, and late post-history prompts; character overrides with `{{original}}`; at least `{{char}}` and `{{user}}` macros; injected example dialogue; model-aware message roles; explicit sampler controls; and an exact effective-request inspector that excludes credentials. Thread snapshots preserve character identity but must not freeze obsolete global prompt defaults.
- **REQ-39** `[v1]` — **Bounded streaming refusal recovery**: RayMe detects an explicit generic/guideline refusal in the initial streamed prefix, cancels that attempt, and retries with corrective prompt context before the refused text is rendered, spoken, or persisted. Normal in-character output must be released after a small bounded prefix guard; neither text nor calls may wait for the full LLM response, and live-call early playback, listening recovery, and explicit interruption remain mandatory.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Preserved legacy injected-client default settings projection**

- **Found during:** Full web-server regression verification after Task 2.
- **Issue:** The Phase 1 end-to-end acceptance client compares the historical four-field `ChatCompletionSettings` projection, while Plan 06 added an explicit default prompt profile to injected-client requests.
- **Fix:** For injected custom clients only, omit the profile field when it equals the unchanged built-in defaults. Production requests and explicitly saved profiles still carry full structured settings; the composer continues to receive the resolved defaults.
- **Files modified:** `web-ui/server/app/api/chat.py`, `web-ui/server/app/api/messages.py`, `web-ui/server/tests/test_chat_stream.py`, `web-ui/server/tests/test_message_actions.py`
- **Verification:** The focused compatibility matrix passed 51 tests and the complete server suite passed 562 tests.
- **Committed in:** `2b053bc`

**2. [Rule 3 - Blocking] Reconciled the legacy sequential STATE header**

- **Found during:** Plan close-out.
- **Issue:** `state.advance-plan` could not parse the legacy STATE document's missing Current Plan / Total Plans fields, and `state.update-progress` correctly declined its unscoped phase shape.
- **Fix:** Retained the SDK metric, session, decision, and ROADMAP writes; advanced only the existing `progress.completed_plans` and `last_activity_desc` fields; and normalized the SDK's duplicated decision phase labels.
- **Files modified:** `.planning/STATE.md`
- **Verification:** STATE records Plan 06 as the latest activity, 107 completed plans, one Plan 06 metric row, two Phase 09.1 decisions, and no duplicate decision text.
- **Committed in:** Final execution-state metadata commit.

---

**Total deviations:** 2 auto-fixed (1 Rule 1 bug, 1 Rule 3 blocking issue)
**Impact on plan:** The narrow compatibility seam keeps prior injected-client behavior without changing production generation, saved profiles, composer inputs, refusal handling, or persistence; the state reconciliation changes only existing GSD bookkeeping fields.

## Verification

- Task 1 Send suite: **18 passed**.
- Complete Plan 06 composer/action/adapter matrix: **80 passed**.
- Legacy Phase 1 plus Plan 06 consumer matrix: **51 passed**.
- Full web-server suite: **562 passed**.
- Ruff lint and formatting: **PASS**.
- `git diff --check`: **PASS**.

## TDD Gate Compliance

- Task 1 RED `b90707d` preceded GREEN `9202f15`.
- Task 2 RED `2b61f87` preceded GREEN `69b2538`.

## Known Stubs

None.

## Security and Privacy

- No prompt, rejected prose, credential, runtime seed, private upstream detail, or canned fallback enters SSE, typed failures, retry context, or persisted assistant content.
- Refusal and empty failures expose only stable public codes and sanitized messages.
- No endpoint, schema, dependency, deployment behavior, TTS buffering, VAD behavior, automatic barge-in, or hidden moderation layer was added.

## Self-Check: PASSED

- All five plan-declared implementation/test files exist.
- All five task and deviation commits exist in git history.
- Requirements metadata is exactly `[REQ-37, REQ-38, REQ-39]`, and all five shipped deliverables have passing coverage records.
- No known stubs, skipped tests, unrun verification commands, or open blockers remain.

---
*Phase: 09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc*
*Completed: 2026-08-31*
