---
phase: 09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc
plan: 04
type: execute
wave: 2
depends_on: [09.1-01]
files_modified:
  - web-ui/server/alembic/versions/0009_thread_example_snapshot.py
  - web-ui/server/app/storage/models.py
  - web-ui/server/app/domain/thread_service.py
  - web-ui/server/tests/test_migrations.py
  - web-ui/server/tests/test_threads.py
autonomous: true
requirements: [REQ-37, REQ-38]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "D-12: New threads snapshot exact `mes_example`; pre-migration rows remain SQL NULL, inject zero examples, and never read a character's later live example text."
    - "Upgrade adds only the nullable snapshot column after revision 0008, performs no backfill, and preserves all unrelated thread, character, and lorebook/world-info data."
    - "Downgrade removes only the new snapshot column; a subsequent upgrade restores the schema without fabricating example values."
  prohibitions:
    - statement: "A null legacy example snapshot must not reread or import the character's later live example text."
      status: unresolved
    - statement: "The migration must not backfill old rows or alter preserved lorebook/world-info data."
      status: unresolved
  artifacts:
    - path: "web-ui/server/alembic/versions/0009_thread_example_snapshot.py"
      provides: "Nullable threads.character_snapshot_mes_example add/drop migration with no backfill"
    - path: "web-ui/server/app/storage/models.py"
      provides: "Thread.character_snapshot_mes_example nullable model field"
    - path: "web-ui/server/app/domain/thread_service.py"
      provides: "Exact new-thread capture and stored-column-only hydration"
    - path: "web-ui/server/tests/test_migrations.py"
      provides: "Clean, legacy, downgrade/upgrade, and unrelated-data preservation coverage"
  key_links:
    - from: "web-ui/server/app/domain/thread_service.py"
      to: "web-ui/server/app/storage/models.py"
      via: "New-thread creation copies Character.mes_example once; hydration returns only the stored snapshot column"
      pattern: "character_snapshot_mes_example"
    - from: "web-ui/server/alembic/versions/0009_thread_example_snapshot.py"
      to: "web-ui/server/tests/test_migrations.py"
      via: "Migration tests prove nullable add/drop, revision linkage, and no UPDATE/backfill"
      pattern: "0008_remove_qwen3_authorization|character_snapshot_mes_example"
---

<objective>
Add the truthful nullable thread example snapshot independently of prompt/settings persistence (D-12).

Purpose: Preserve creation-time example history for new threads without rewriting old conversations from later character edits.
Output: Alembic 0009, Thread model/hydration wiring, and migration/thread regressions.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md
@web-ui/server/alembic/versions/0008_remove_qwen3_authorization.py
@web-ui/server/alembic/versions/0007_call_turn_ownership.py
@web-ui/server/app/storage/models.py
@web-ui/server/app/domain/thread_service.py
@web-ui/server/tests/test_migrations.py
@web-ui/server/tests/test_threads.py
</context>

## Artifacts this phase produces

- `web-ui/server/alembic/versions/0009_thread_example_snapshot.py`: revision descending from `0008_remove_qwen3_authorization`, nullable add/drop only, with no data update.
- `web-ui/server/app/storage/models.py`: nullable `Thread.character_snapshot_mes_example` field.
- `web-ui/server/app/domain/thread_service.py`: exact capture on new-thread creation and stored-column-only hydration.
- Extended migration/thread suites proving clean/legacy upgrades, exact Unicode snapshots, null truth, downgrade/upgrade, and preservation of unrelated data.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate and hydrate truthful example snapshots</name>
  <reversibility rating="costly">D-12 is locked and the nullable column has an explicit Alembic downgrade, but reversing it after data is captured requires a coordinated migration and discards stored snapshots.</reversibility>
  <read_first>
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md — D-12 locks exact new-thread capture and truthful null legacy behavior.
    - web-ui/server/alembic/versions/0008_remove_qwen3_authorization.py — current revision/down_revision source of truth
    - web-ui/server/alembic/versions/0007_call_turn_ownership.py — nullable add/drop migration style
    - web-ui/server/app/storage/models.py — adjacent Character.mes_example, lorebook/world-info, and Thread snapshot declarations
    - web-ui/server/app/domain/thread_service.py — new-thread snapshot assignments and character_snapshot hydration
    - web-ui/server/tests/test_migrations.py — head/column and legacy-row upgrade preservation fixtures
  </read_first>
  <files>web-ui/server/alembic/versions/0009_thread_example_snapshot.py, web-ui/server/app/storage/models.py, web-ui/server/app/domain/thread_service.py, web-ui/server/tests/test_migrations.py, web-ui/server/tests/test_threads.py</files>
  <behavior>
    - Upgrade adds only nullable `threads.character_snapshot_mes_example`; downgrade drops it; no UPDATE/backfill runs.
    - A new thread copies the character's current `mes_example`, including blank/Unicode/combining content, exactly once.
    - Existing upgraded rows remain null and hydrate null even when the linked Character later gains or changes examples.
    - Thread detail exposes the stored field under `character_snapshot` for the shared composer.
    - Existing character lorebook/world-info bytes and every unrelated thread snapshot field remain unchanged through clean and legacy upgrade/downgrade cycles.
  </behavior>
  <action>Create Alembic revision `0009_thread_example_snapshot` with `down_revision = "0008_remove_qwen3_authorization"`, add the nullable Text model field, and wire create/hydrate through `ThreadService` per D-12. Preserve null as “no example snapshot captured”; never fall through to a live Character join and never backfill old rows. Extend migration tests for clean head, legacy upgrade, downgrade/upgrade, unrelated-row and persisted lorebook/world-info preservation, and `alembic upgrade head`, plus thread tests for new exact capture and later-card-edit isolation.</action>
  <acceptance_criteria>
    - Clean and legacy databases upgrade to head; the new column is nullable and old values stay SQL NULL.
    - New thread example snapshots round-trip exact Unicode; null legacy snapshots remain null after later character edits.
    - Downgrade removes only the new column and a subsequent upgrade restores schema without fabricating example values.
    - Lorebook/world-info and all unrelated persisted fields are byte-equivalent before and after every migration path.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_migrations.py web-ui/server/tests/test_threads.py -q &amp;&amp; uv run --project web-ui/server alembic -c web-ui/server/alembic.ini upgrade head</automated>
    <fails_when>Either command exits non-zero, 0 tests run, the migration backfills any old row, new threads omit/normalize examples, null hydration consults live character data, unrelated persisted data changes, or head is not revision 0009.</fails_when>
  </verify>
  <done>Thread examples have a truthful creation-time snapshot, legacy absence remains null, and unrelated character/thread data is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Character row → new Thread snapshot → later composer | Mutable card examples cross into immutable per-thread historical identity. |
| Existing SQLite data → Alembic 0009 | User conversation and card data cross a schema migration boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09.1-04-01 | Tampering | Legacy thread example history | high | mitigate | Nullable no-backfill migration plus stored-column-only hydration and later-card-edit trap tests. |
| T-09.1-04-02 | Tampering | Unrelated character/thread data | high | mitigate | Before/after row fixtures prove only the new nullable column changes and lorebook/world-info remains byte-equivalent. |
| T-09.1-04-03 | Repudiation | Migration lineage | medium | mitigate | Exact down_revision/head assertions and downgrade/upgrade coverage record the canonical schema path. |
| T-09.1-SC | Tampering | Package supply chain | low | accept | No npm, pip, or cargo package installation occurs in this plan. |
</threat_model>

<verification>
Run the task command and `git diff --check`. Confirm 0009 is the sole relational schema change and settings remain JSON-backed.
</verification>

<success_criteria>
New threads capture exact examples once, old rows remain truthfully null, and migration cycles leave every unrelated field untouched.
</success_criteria>

<output>
Create `.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-04-SUMMARY.md` when done.
</output>
