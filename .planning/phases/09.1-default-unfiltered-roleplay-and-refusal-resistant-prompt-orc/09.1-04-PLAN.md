---
phase: 09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc
plan: 04
type: execute
wave: 3
depends_on:
  - 09.1-03
files_modified:
  - web-ui/server/app/domain/prompt_profiles.py
  - web-ui/server/app/domain/prompt_builder.py
  - web-ui/server/app/domain/generation_profiles.py
  - web-ui/server/app/domain/llm_stream.py
  - web-ui/server/tests/test_prompt_profiles.py
  - web-ui/server/tests/test_prompt_builder.py
  - web-ui/server/tests/test_generation_profiles.py
autonomous: true
requirements:
  - REQ-37
  - REQ-38
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-03 through D-06, Roleplay, Assistant, and Custom resolve to their exact persisted Main, Auxiliary, PHI, and sampler semantics; selecting another profile is explicit and never inferred from blank text."
    - "Per D-07 and D-08, blank card Main/PHI inherit the selected profile, nonblank card text replaces it, `{{original}}` inserts the inherited value at the exact marker boundary, character then user macros run one non-recursive pass, and unknown macros remain visible."
    - "Per D-10 through D-13, Main, persona, Auxiliary, complete example groups, stable history, and late PHI remain separately identified in a structured build result and serialize deterministically in that locked order."
    - "Adjacent macro markers and `{{original}}` boundaries neither insert nor delete separators; prompt/card text remains unnormalized Unicode with no HTML interpretation, and multilingual and combining-character fixtures round-trip unchanged into wire messages."
    - "Empty example input produces zero complete groups; optional empty Auxiliary and PHI sections are represented truthfully without fabricating messages; equal-content messages remain distinct and ordered by persisted sequence/id."
    - "Per D-14, budgeting drops oldest history and then oldest complete example groups, never mandatory sections; Continue keeps assistant prefill final and call-shaped requests apply persisted context/output limits plus the 48-message and 20,000-character transport ceilings."
    - "Per D-15 and D-16, one immutable generation profile resolves configured and effective samplers plus auto/Qwen/generic adapter selection, and the labeled versioned token estimate cannot alter request text."
    - "Qwen/llama-server serializes late PHI and retry correction where the pinned chat template consumes them while preserving Continue's final assistant prefill; generic adapters preserve standard message roles."
  artifacts:
    - "web-ui/server/app/domain/prompt_builder.py exports the structured composer and immutable build result used by every consumer."
    - "web-ui/server/app/domain/generation_profiles.py exports model-aware adapter resolution, wire serialization, sampler mapping, and the labeled estimator."
    - "web-ui/server/tests/test_prompt_builder.py proves macros, section identity/order, snapshots, budgets, exact Unicode, and no deduplication."
    - "web-ui/server/tests/test_generation_profiles.py proves Qwen/generic/auto serialization, Continue ordering, samplers, and immutable resolution."
  key_links:
    - "PromptProfileSettings -> build_prompt(...) -> structured sections -> generation adapter -> exact OpenAI-compatible messages."
    - "Character Main/PHI inheritance -> `{{original}}` expansion -> one-pass character/user macros -> separately inspectable sections."
    - "Model identifier plus explicit override -> effective adapter -> Qwen late-PHI/retry wire placement without changing logical section order."
  prohibitions:
    - statement: "Do not normalize, sanitize into HTML, deduplicate, merge by equal content, or silently reorder prompt strings or messages."
      status: unresolved
    - statement: "Do not read a character's later live example text when a legacy thread has a null example snapshot."
      status: unresolved
    - statement: "Do not expose credentials, endpoint secrets, runtime seeds, or private headers in structured composer results or estimates."
      status: unresolved
---

<objective>
Create the single structured, model-aware prompt composer that every text, action, call, and preview path can consume per D-09.

Purpose: Turn persisted prompt profiles and immutable thread snapshots into exact, inspectable logical sections and adapter-safe wire requests without letting a pinned Qwen chat template discard late instructions.
Output: Structured composition, deterministic budgeting, model adapters, sampler resolution, and executable Unicode/order/boundary contracts.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-RESEARCH.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-AI-SPEC.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-03-SUMMARY.md
@web-ui/server/app/domain/prompt_profiles.py
@web-ui/server/app/domain/prompt_builder.py
@web-ui/server/app/domain/llm_stream.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compose exact logical sections from one immutable snapshot</name>
  <read_first>
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md — D-03 through D-15 define profile text, macros, section order, examples, snapshots, budgeting, and samplers.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-AI-SPEC.md — use the approved structured build-result and evaluation contracts.
    - web-ui/server/app/domain/prompt_profiles.py — consume the persisted profile types and validators created by Plan 03.
    - web-ui/server/app/domain/prompt_builder.py — retain compatible public seams only where they do not conflict with D-09's single composer.
    - web-ui/server/app/domain/thread_service.py — use the hydrated immutable thread/example snapshot rather than rereading mutable character examples.
    - web-ui/server/tests/test_prompt_builder.py — preserve existing request-shape assertions and extend them before implementation.
  </read_first>
  <files>web-ui/server/app/domain/prompt_profiles.py, web-ui/server/app/domain/prompt_builder.py, web-ui/server/tests/test_prompt_profiles.py, web-ui/server/tests/test_prompt_builder.py</files>
  <behavior>
    - Roleplay, Assistant, and valid Custom profiles produce exact persisted Main/Auxiliary/PHI/sampler values; required Main validation is explicit while optional empty sections stay empty.
    - Blank card fields inherit; nonblank fields replace; each `{{original}}` occurrence expands exactly in place before one non-recursive character/user macro pass, preserving adjacent delimiters, unknown tokens, Unicode code points, and combining sequences.
    - Logical output retains separately named Main, persona, Auxiliary, complete `<START>` example groups, stable history, and late PHI; `{{user}}:`/`{{char}}:` delimit example turns, continuation lines attach to their preceding turn, and equal strings remain separate entries.
    - Null legacy example snapshots produce zero examples and never consult later character data; empty examples also produce zero groups.
    - Budget pressure removes oldest history first and oldest complete example groups second, while Main/persona/Auxiliary/late-PHI, the newest user turn, stable sequence/id ordering, Continue's final assistant prefill, configured call context/output limits, and the 48-message/20,000-character ceilings remain intact.
  </behavior>
  <action>Write the failing boundary/encoding/order tests first, then replace divergent prompt assembly with one pure structured composer per D-07 through D-14. Give each build its own frozen settings, character, thread, history, snapshot, call-mode, and generation-action inputs. Each immutable `PromptSection` carries stable section ID, logical role, exact content, source, override state, mandatory flag, estimated tokens, and atomic group ID; `PromptBuildResult` carries action/variant/mode/contract version, ordered retained sections, transmitted-message candidates, budget/drop accounting, warnings, and target constraints. Resolve `{{original}}` before a single character-then-user macro pass; use the literal `User` for `{{user}}` because RayMe has no persisted user display-name source. Preserve raw Python Unicode strings without normalization or HTML parsing. Parse only complete `<START>` example groups: `{{user}}:` and `{{char}}:` begin turns, subsequent non-speaker lines append exactly to the preceding turn, and no partial/orphan turn is injected. Apply deterministic stable ordering and never content-deduplicate; cap call-shaped selected messages at 48 and each transported content string at 20,000 characters, using the same oldest-history/complete-example drop policy and a typed mandatory-overflow failure rather than rewriting mandatory text. Use a documented estimator version of `ceil(UTF-8 bytes / 3) + 6 per message` plus fixed adapter overhead and reserve `max(256, ceil(context_window * 0.05))`; estimates guide selection only and never truncate or rewrite text. Keep mandatory overflow explicit as a typed build failure.</action>
  <acceptance_criteria>
    - Tests prove every D-07/D-08 macro boundary, unknown-marker, Unicode, and non-recursion behavior with byte-equivalent UTF-8 round trips.
    - Build results separately identify all D-10 sections and distinguish empty optional sections from absent/fabricated messages.
    - Tests prove deterministic order, stable sequence/id selection, no equal-content deduplication, complete-group example dropping, mandatory retention, and Continue/call constraints.
    - A legacy null snapshot test fails if any live character example accessor is invoked.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_prompt_profiles.py web-ui/server/tests/test_prompt_builder.py -q</automated>
    <fails_when>The command exits non-zero, reports 0 passed, any null-snapshot trap is touched, or exact Unicode/order/budget assertions differ.</fails_when>
  </verify>
  <done>The composer returns one immutable, inspectable, exact logical prompt for every action shape without losing separators, Unicode, duplicates, snapshot history, or mandatory sections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serialize model-aware requests without losing late instructions</name>
  <read_first>
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md — D-15 and D-16 lock sampler and auto/Qwen/generic behavior.
    - .planning/notes/2026-08-30-sillytavern-prompt-composition-and-rayme-refusal-diagnosis.md — use the verified Qwen pinned-template failure mechanism.
    - web-ui/server/app/domain/prompt_builder.py — consume logical sections without rebuilding prompt text.
    - web-ui/server/app/domain/llm_stream.py — connect typed wire requests at the existing OpenAI-compatible boundary.
    - web-ui/server/tests/test_generation_profiles.py — create the adapter matrix before implementation.
  </read_first>
  <files>web-ui/server/app/domain/generation_profiles.py, web-ui/server/app/domain/prompt_builder.py, web-ui/server/app/domain/llm_stream.py, web-ui/server/tests/test_generation_profiles.py, web-ui/server/tests/test_prompt_builder.py</files>
  <behavior>
    - Explicit adapter selection wins; auto resolves Qwen for model identifiers containing `qwen` case-insensitively and generic otherwise, with configured and effective values both observable.
    - Profile sampler fields map exactly once to the OpenAI-compatible payload and Qwen no-thinking/template extras plus the existing `/no_think` suffix behavior merge without erasing explicit profile values.
    - Qwen coalesces leading Main/persona/Auxiliary system content deterministically, represents late PHI and retry correction as final user-consumed instructions, and leaves Continue assistant prefill as the final message.
    - Generic serialization preserves standard logical roles and exact strings.
    - Repeated and concurrent resolutions return isolated immutable request objects with no shared seed, settings, or message mutation.
  </behavior>
  <action>Create `generation_profiles.py` as the sole logical-to-wire adapter per D-15/D-16. Represent configured adapter, effective adapter, estimator version, sampler payload, and exact wire messages as typed immutable values. Preserve the composer's logical section identity for preview while adapting only transport roles/order. For Qwen/llama-server, deterministically coalesce leading system material and position late PHI/retry correction where the pinned template consumes it; when Continue has assistant prefill, keep that prefill final and put correction immediately before it. Deep-merge the existing no-thinking controls, chat-template extras, `/no_think` suffix behavior, and explicit sampler settings without replacing any side of the contract. Route `llm_stream.py` through this adapter seam, but leave consumer migration to Plans 05 and 06. Do not introduce provider-specific dependencies or a second composer.</action>
  <acceptance_criteria>
    - The fixed adapter matrix covers explicit Qwen, explicit generic, Qwen auto detection, non-Qwen auto detection, late PHI, retry correction, and Continue assistant-prefill finality.
    - Wire payload tests prove exact Unicode content and sampler values; the labeled estimator is informational and cannot mutate wire strings.
    - Concurrent-build tests prove separate settings/message/seed ownership and cancellation-safe immutable request data.
    - Existing OpenAI-compatible streaming tests still receive the same supported transport shape through the new adapter boundary.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_generation_profiles.py web-ui/server/tests/test_prompt_builder.py web-ui/server/tests/test_chat_stream.py -q</automated>
    <fails_when>The command exits non-zero, reports 0 passed, Qwen drops late instructions, Continue prefill is not final, or any exact wire/sampler/concurrency assertion fails.</fails_when>
  </verify>
  <done>One typed adapter turns the exact logical prompt into a model-consumable request for Qwen or generic servers while preserving late instructions, Continue semantics, samplers, and request isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted settings/card/thread -> composer | User-authored Unicode and macro syntax enter deterministic server-side composition. |
| composer -> adapter -> OpenAI-compatible server | Private character/history text and sampler data cross into the configured local inference boundary. |
| structured result -> later preview API | Inspectable content must remain exact text without becoming executable HTML or leaking transport secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09.1-04-01 | Tampering | macro expansion and section ordering | high | mitigate | Pure one-pass expansion, immutable snapshots, stable sequence/id ordering, exact Unicode fixtures, and no content deduplication. |
| T-09.1-04-02 | Information Disclosure | structured build result | high | mitigate | Include prompt sections and public generation metadata only; omit credentials, endpoint headers, and runtime seed values by construction. |
| T-09.1-04-03 | Denial of Service | prompt budgeting | medium | mitigate | Validate bounds, reserve a documented margin, drop only bounded optional groups in deterministic order, and return typed mandatory-overflow errors. |
| T-09.1-04-04 | Elevation of Privilege | preview HTML interpretation | medium | mitigate | Preserve all text as data and require downstream escaped text rendering; never generate trusted HTML from card/prompt strings. |
| T-09.1-SC | Tampering | package supply chain | low | accept | No package-manager install occurs in this plan. |
</threat_model>

## Artifacts this phase produces

- `web-ui/server/app/domain/prompt_builder.py`: `PromptBuildInput`, `PromptSection`, `PromptBuildResult`, example-group parsing, exact macro expansion, deterministic budgeting, and the one shared composer.
- `web-ui/server/app/domain/generation_profiles.py`: configured/effective adapter types, sampler resolution, estimator label/version, Qwen and generic serializers, and immutable wire request type.
- `web-ui/server/tests/test_generation_profiles.py`: fixed Qwen/generic/auto/Continue/concurrency serialization matrix.
- Extended `web-ui/server/tests/test_prompt_builder.py` and `test_prompt_profiles.py`: adjacency, empty, Unicode, ordering, snapshot, and budget regressions.

<verification>
Run both task test commands, then `uv run --project web-ui/server pytest web-ui/server/tests/test_health_settings.py web-ui/server/tests/test_threads.py -q` and `git diff --check`. Inspect one test failure snapshot to confirm prompt content is compared as text/bytes rather than normalized HTML.
</verification>

<success_criteria>
Every logical section is exact and inspectable, budgets are deterministic, and both generic and pinned-Qwen wire requests preserve the selected profile, late instructions, Continue ordering, samplers, Unicode, and immutable request ownership.
</success_criteria>

<output>
Create `.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-04-SUMMARY.md` when done.
</output>
