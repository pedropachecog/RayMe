---
phase: 09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc
plan: 14
type: execute
wave: 7
depends_on:
  - 09.1-02
  - 09.1-07
  - 09.1-10
  - 09.1-11
  - 09.1-13
files_modified: []
autonomous: true
requirements:
  - REQ-37
  - REQ-38
  - REQ-39
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "After every pre-deployment implementation/evidence/plan-summary commit exists, one intended SHA is frozen and the complete server, AI-backend, client, browser, migration, evidence, live-call, and privacy gate runs against that clean exact tree."
    - "Per D-28, the exact locally tested commit equals the configured origin branch commit before OMEN deployment, and OMEN is deployed only by `scripts/deploy-omen.sh`."
    - "No commit or tracked implementation/phase-evidence-source change occurs after the full local gate and before canonical deployment; `09.1-14-SUMMARY.md` is written and committed only after deployed evidence and human verification finish."
    - "The clean/SHA gate covers all tracked `web-ui`, `ai-backend`, `scripts`, and Phase 09.1 source artifacts—including manifest, runner, verifier, evidence tests, and plans—so every executed gate comes from the intended commit."
    - "The post-deploy runner and independent decision-ready verifier receive `--expected-commit` with that same immutable SHA and prove OMEN checkout/health plus equality with the earlier read-only model/context/template/prefill preflight."
    - "Per D-26, the fixed 12-scenario by 3-seed refusal matrix passes exact attempt, fresh-seed, correction, close, classifier precision/recall, and accepted-content integrity gates."
    - "Per D-27, a controlled slow call-shaped refusal-then-accepted stream proves first accepted caption/audio before accepted generation/TTS completion, rejected upstream closure, explicit Interrupt late-chunk rejection, listening recovery, and no whole-response/TTS fallback."
    - "Shared evidence contains only allowlisted IDs, hashes, counts, scalar timings, versions, commit/model identity, and gate outcomes; credentials, runtime seeds, raw prompts/history/generated/rejected prose, audio, private paths, and tracebacks are absent."
    - "The deployed Prompt Inspector effective request equals the captured model request across the fixed Qwen matrix while remaining same-origin, non-mutating, escaped, and credential-free."
  artifacts:
    - "The Plan 02 evidence runner writes its manifest-named ignored local and OMEN result JSON beneath the phase `results/` directory."
    - "The Plan 02 verifier emits a sanitized decision-ready pass/fail report tied to the exact intended commit."
    - "The plan summary records exact local/origin/OMEN SHA equality, command outcomes, health/model identity, matrix aggregate, call timing order, privacy scan, and human-check result without private prose."
  key_links:
    - "tested local HEAD -> exact origin branch HEAD -> deploy-omen expected HEAD -> OMEN checkout -> evidence expected-commit identity."
    - "manifest -> real Qwen scenario dispatch -> allowlisted result JSON -> independent recomputation/verifier -> decision-ready gate."
    - "controlled call barriers -> caption/audio/generation/TTS/Interrupt events -> strict ordering and sink-exclusion gates."
  prohibitions:
    - statement: "Do not deploy through any ad-hoc OMEN script, launcher, manual scheduled-task edit, hidden process, or mechanism other than scripts/deploy-omen.sh."
      status: unresolved
    - statement: "Do not deploy an unpushed, dirty, untested, or SHA-mismatched commit."
      status: unresolved
    - statement: "Do not create a commit or write the execution summary between full local verification and deployment/evidence completion."
      status: unresolved
    - statement: "Do not buffer a whole model response or whole TTS synthesis to obtain production evidence."
      status: unresolved
    - statement: "Do not reintroduce automatic microphone/VAD barge-in during deployment or evidence acquisition."
      status: unresolved
    - statement: "Do not share credentials, runtime seeds, raw prompts/history/generated/rejected prose, audio, private paths, tracebacks, or legacy voice authorization/steward/consent/source/use-scope fields in evidence."
      status: unresolved
---

<objective>
Prove the entire phase locally, deploy the exact tested commit through the canonical OMEN path, and collect independently verified production-path evidence.

Purpose: Turn implementation and focused tests into a release decision tied to one immutable commit, the real configured Qwen runtime, and a live-call-shaped early-streaming retry.
Output: Complete local gate, exact canonical deployment, sanitized real-runtime evidence, independent decision-ready verification, and final human-visible confirmation.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/LIVE-CALL-INVARIANTS.md
@.planning/REFERENCE-AUTHORIZATION-PROHIBITION.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-AI-SPEC.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-02-SUMMARY.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-07-SUMMARY.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-10-SUMMARY.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-11-SUMMARY.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-13-SUMMARY.md
@scripts/deploy-omen.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the intended SHA after all commits and verify that exact tree</name>
  <read_first>
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-AI-SPEC.md — run its complete CI/CD and evidence-contract gate, not a focused subset.
    - .planning/LIVE-CALL-INVARIANTS.md — protected early playback, interruption, listening recovery, VAD, and VoxCPM2 regressions must be present in the local gate.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-VALIDATION.md — every finalized task/requirement behavior must have its named automated owner and pass.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-evidence-manifest.json — use the frozen artifacts, thresholds, and privacy allowlist.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-verify-evidence.py — run contracts-only and hostile verifier self-tests before any deployment.
  </read_first>
  <files>.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/results/local-verification.json</files>
  <action>Require every prior implementation, phase evidence source, and Plan 01–13 summary commit to be complete before this task starts; do not write `09.1-14-SUMMARY.md` yet. Set `intended_sha` once from clean HEAD, scope `release_paths` to tracked `web-ui`, `ai-backend`, `scripts`, and the entire Phase 09.1 directory, and prove those paths match `intended_sha` before running anything while preserving unrelated user STATE/debug artifacts outside that scope. Run Alembic head plus the complete Web server, AI backend, protected call/VoxCPM2, Settings, client unit, hermetic desktop Playwright, live-call inventory, evidence self-test, contracts-only verifier, build, and diff gates from AI-SPEC in one shell without commits. Execute the manifest, runner, verifier, and evidence tests directly from the clean working tree that equals `intended_sha`. After the final gate, reassert HEAD and every tracked release path still equal `intended_sha`, then use Plan 02's allowlisted recorder to write ignored `local-verification.json` and verify it with `--local-release --expected-commit`. Record only command IDs, exit statuses, nonzero test counts, SHA, UTC freshness, and gate booleans. Stop on first failure; do not push, deploy, commit, or create the plan summary.</action>
  <acceptance_criteria>
    - Alembic upgrades through `0009` and migration/thread/profile/composer/adapter/consumer/preview/refusal/call/UI tests all pass.
    - Slow accepted/refusal-accepted call tests prove first caption/audio before completion; Interrupt/listening/VoxCPM2 streaming-only regressions pass.
    - Client unit, prompt-inspector E2E, full server/AI suites, build, evidence hostile mutations, contracts-only verifier, and `git diff --check` pass with nonzero test counts.
    - The sanitized local result references one candidate SHA and passes the recursive privacy allowlist.
    - HEAD and all tracked implementation/phase-evidence source paths are unchanged before and after the full gate, and no Plan 14 summary or task commit exists.
  </acceptance_criteria>
  <verify>
    <automated>phase_evidence_dir=".planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc"; release_paths=(web-ui ai-backend scripts "$phase_evidence_dir"); intended_sha="$(git rev-parse HEAD)" &amp;&amp; test ! -e "$phase_evidence_dir/09.1-14-SUMMARY.md" &amp;&amp; test -z "$(git status --porcelain --untracked-files=no -- "${release_paths[@]}")" &amp;&amp; git diff --quiet "$intended_sha" -- "${release_paths[@]}" &amp;&amp; uv run --project web-ui/server alembic -c web-ui/server/alembic.ini upgrade head &amp;&amp; uv run --project web-ui/server pytest web-ui/server/tests -q &amp;&amp; uv run --project ai-backend pytest ai-backend/tests -q &amp;&amp; uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py web-ui/server/tests/test_call_tts_segments.py -q &amp;&amp; uv run --project ai-backend pytest ai-backend/tests/test_call_session.py ai-backend/tests/test_webrtc_signaling.py ai-backend/tests/test_tts_voxcpm2.py -q &amp;&amp; npm --prefix web-ui/client exec vitest -- run tests/unit/settings.test.ts &amp;&amp; npm --prefix web-ui/client run test:unit -- --run &amp;&amp; npm --prefix web-ui/client run test:e2e -- tests/e2e/chat-stream.spec.ts tests/e2e/call-start.spec.ts tests/e2e/settings-connection.spec.ts tests/e2e/prompt-inspector.spec.ts --project=desktop-chromium &amp;&amp; npm --prefix web-ui/client exec playwright -- test --list live-call.spec.ts &amp;&amp; npm --prefix web-ui/client run build &amp;&amp; python3 -m pytest "$phase_evidence_dir/test_phase091_evidence.py" -q &amp;&amp; python3 "$phase_evidence_dir/09.1-verify-evidence.py" --contracts-only &amp;&amp; git diff --check &amp;&amp; test "$intended_sha" = "$(git rev-parse HEAD)" &amp;&amp; test -z "$(git status --porcelain --untracked-files=no -- "${release_paths[@]}")" &amp;&amp; git diff --quiet "$intended_sha" -- "${release_paths[@]}" &amp;&amp; python3 "$phase_evidence_dir/09.1-run-omen-evidence.py" --record-local-verification --expected-commit "$intended_sha" &amp;&amp; python3 "$phase_evidence_dir/09.1-verify-evidence.py" --local-release --expected-commit "$intended_sha" &amp;&amp; test "$intended_sha" = "$(git rev-parse HEAD)" &amp;&amp; test ! -e "$phase_evidence_dir/09.1-14-SUMMARY.md"</automated>
    <fails_when>The command exits non-zero, any suite reports 0 tests, migration head is not 0009, evidence sources differ from intended SHA, a hostile/privacy mutation passes, HEAD or a tracked release path changes during the gate, local-release verification does not bind the exact SHA, or the Plan 14 summary exists before deployment.</fails_when>
  </verify>
  <done>One immutable intended SHA has passed the entire local gate from a clean matching tree, with no intervening commit, tracked release-path change, deployment, or Plan 14 summary.</done>
</task>

<task type="auto">
  <name>Task 2: Push, canonically deploy, and independently verify the exact commit</name>
  <precondition>`results/local-verification.json` passes `--local-release` for the current clean HEAD, no `09.1-14-SUMMARY.md` exists, an upstream branch is configured, and canonical `rayme-pmpg` OMEN deployment prerequisites are available.</precondition>
  <reversibility rating="costly">Deployment replaces the running OMEN application/database schema at the exact commit; recovery must use repository history and the same canonical deployment script.</reversibility>
  <read_first>
    - AGENTS.md — `scripts/deploy-omen.sh` is the only authorized OMEN deployment mechanism and owns launcher/scheduled-task state.
    - .planning/LIVE-CALL-INVARIANTS.md — production evidence must preserve early playback and explicit interruption.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md — D-26 through D-28 lock matrix, live-call, exact-commit, health, and deployment evidence.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-evidence-manifest.json — dispatch exactly the frozen scenarios/seeds/artifacts/thresholds.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-run-omen-evidence.py — acquire through production paths and emit only allowlisted fields.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-verify-evidence.py — independently recompute decision readiness for the intended SHA.
    - scripts/deploy-omen.sh — invoke directly; fix this canonical script in a separately reviewed change if it cannot perform required deployment behavior.
  </read_first>
  <files>.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/results/omen-evidence.json, .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/results/decision-report.json</files>
  <action>Read `intended_sha` from Task 1's verified local result and require it to equal clean HEAD across all tracked `web-ui`, `ai-backend`, `scripts`, and Phase 09.1 source paths. Do not create a task commit or write `09.1-14-SUMMARY.md`. Verify local-release, push the unchanged current branch, fetch it, and require local HEAD and the upstream branch to equal that full SHA. Reassert clean release paths, then invoke exactly `scripts/deploy-omen.sh`; do not create another OMEN helper, edit launchers, or manipulate scheduled tasks. Run the Plan 02 OMEN evidence runner with `--expected-commit "$intended_sha"`, then the independent verifier with `--decision-ready --expected-commit "$intended_sha"`. Require exact OMEN commit/health and current model/context/template/prefill equality with the early read-only preflight, plus 12 real-Qwen scenarios × 3 fixed evidence seeds. Separately require the tracked controlled slow fixture to prove preview/wire parity, classifier thresholds, fresh seeds, exact attempt-2/3 correction with rejected-prose exclusion, stream closure, three-attempt cap, empty-output exclusion, accepted-content integrity, early call audio, Interrupt, teardown, and post-run settings/health equality. Reassert local HEAD and every tracked release path are still clean at `intended_sha`. Quarantine/fail any private canary. Only after this task's automated evidence and the end-of-phase human check pass may the executor write and commit `09.1-14-SUMMARY.md`; that summary commit is post-deployment evidence and is never part of the deployed SHA.</action>
  <acceptance_criteria>
    - Local HEAD, configured origin branch, OMEN checkout, runner expected commit, and verifier expected commit are the same full SHA.
    - No commit or tracked release-source change occurs from Task 1's final local gate through deployment and decision-ready evidence; the only later tracked write is the completed Plan 14 summary.
    - `scripts/deploy-omen.sh` is the sole deployment command and reports healthy canonical services after Alembic/client deployment.
    - All 36 fixed Qwen turns and controlled call-shaped scenario satisfy manifest thresholds, exact effective-request parity, accepted-text integrity, rejected/empty sink exclusion, early caption/audio, and Interrupt/listening recovery.
    - Decision-ready verification passes independent recomputation, freshness/model/template/commit identity, artifact completeness, and recursive privacy scans.
  </acceptance_criteria>
  <verify>
    <automated>phase_evidence_dir=".planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc"; release_paths=(web-ui ai-backend scripts "$phase_evidence_dir"); intended_sha="$(python3 -c 'import json, sys; print(json.load(open(sys.argv[1], encoding="utf-8"))["intended_commit"])' "$phase_evidence_dir/results/local-verification.json")" &amp;&amp; test -n "$intended_sha" &amp;&amp; test "$intended_sha" = "$(git rev-parse HEAD)" &amp;&amp; test ! -e "$phase_evidence_dir/09.1-14-SUMMARY.md" &amp;&amp; test -z "$(git status --porcelain --untracked-files=no -- "${release_paths[@]}")" &amp;&amp; git diff --quiet "$intended_sha" -- "${release_paths[@]}" &amp;&amp; python3 "$phase_evidence_dir/09.1-verify-evidence.py" --local-release --expected-commit "$intended_sha" &amp;&amp; git push &amp;&amp; git fetch &amp;&amp; test "$intended_sha" = "$(git rev-parse HEAD)" &amp;&amp; test "$intended_sha" = "$(git rev-parse @{upstream})" &amp;&amp; test -z "$(git status --porcelain --untracked-files=no -- "${release_paths[@]}")" &amp;&amp; scripts/deploy-omen.sh &amp;&amp; test "$intended_sha" = "$(git rev-parse HEAD)" &amp;&amp; python3 "$phase_evidence_dir/09.1-run-omen-evidence.py" --expected-commit "$intended_sha" &amp;&amp; python3 "$phase_evidence_dir/09.1-verify-evidence.py" --decision-ready --expected-commit "$intended_sha" &amp;&amp; test "$intended_sha" = "$(git rev-parse HEAD)" &amp;&amp; test -z "$(git status --porcelain --untracked-files=no -- "${release_paths[@]}")" &amp;&amp; git diff --quiet "$intended_sha" -- "${release_paths[@]}" &amp;&amp; test ! -e "$phase_evidence_dir/09.1-14-SUMMARY.md"</automated>
    <fails_when>The command exits non-zero, local-release is not tied to clean current HEAD, any tracked implementation/phase-evidence source differs, a commit or summary appears between local verification and deployed evidence, local/upstream/OMEN SHA differs, canonical deploy fails, preflight provider facts drift, any matrix/call/privacy gate fails, or decision readiness is rejected.</fails_when>
  </verify>
  <done>The unchanged locally verified SHA is pushed, canonically deployed, and decision-ready against preflight-grounded real-Qwen/live-call/privacy evidence; summary creation remains post-evidence only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repository -> origin -> OMEN | Release identity crosses git and remote deployment state. |
| evidence runner -> configured Qwen/web/call runtime | Controlled fixtures cross real production paths and receive private runtime content/events. |
| raw runner observations -> shared result JSON/report | Only sanitized allowlisted facts may become durable/shareable evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09.1-14-01 | Spoofing | commit/model identity | high | mitigate | Full-SHA local/upstream/OMEN equality plus runner/verifier expected-commit and preflight-observed model/template identity checks. |
| T-09.1-14-02 | Tampering | deployment path | high | mitigate | Invoke only tracked `scripts/deploy-omen.sh`; no manual launchers/tasks; fail if canonical script cannot complete. |
| T-09.1-14-03 | Information Disclosure | evidence outputs | high | mitigate | Positive field allowlist, hashes/counts only, recursive canary scan, quarantine, ignored raw results, and sanitized failures. |
| T-09.1-14-04 | Repudiation | verified-SHA continuity | high | mitigate | Freeze one intended SHA after all predeployment commits, run the whole local gate from its clean tree, allow no later commit before deploy, and independently recompute every evidence gate. |
| T-09.1-14-05 | Denial of Service | live matrix/call evidence | medium | mitigate | Fixed finite 36-turn schedule, three-attempt caps, owned stream closure, bounded timeouts, and health recovery checks. |
| T-09.1-SC | Tampering | package supply chain | low | accept | No package install occurs; deployment uses existing lockfiles and the repository's canonical script. |
</threat_model>

## Artifacts this phase produces

- Ignored `results/local-verification.json`: allowlisted local command/test/build/migration/evidence gate facts tied to the candidate SHA.
- Manifest-named ignored `results/omen-evidence.json`: real-Qwen matrix, exact request parity, call event/timing, commit/model/health, and privacy-safe gate facts.
- Ignored `results/decision-report.json`: independent verifier result and recomputed aggregate for the exact expected commit.
- `09.1-14-SUMMARY.md`: sanitized release record with exact SHA equality, command counts, deployed health/model identity, matrix/call gates, privacy result, and end-of-phase human check.

<verification>
Run both task verification blocks without substituting commands.
<human-check>After decision-ready passes, verify Settings first-ready Roleplay and exact controls, Character macro help, desktop and 320px Prompt Inspector focus/escaping/full-message behavior, one text refusal-recovery path, one live call whose accepted caption/audio begins before completion, and explicit Interrupt/listening recovery. Record observations only as sanitized booleans/counts—no private prompt, generated, rejected, credential, seed, or audio content.</human-check>
</verification>

<success_criteria>
One exact clean commit passes all local gates, equals origin and OMEN, runs through the sole canonical deploy path, and has independently verified real-Qwen and live-call evidence with zero private/rejected content leakage.
</success_criteria>

<output>
Create `.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-14-SUMMARY.md` only after deployed evidence and the end-of-phase human check are complete.
</output>
