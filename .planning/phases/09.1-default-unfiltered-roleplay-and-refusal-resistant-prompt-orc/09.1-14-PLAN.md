---
phase: 09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc
plan: 14
type: execute
wave: 7
depends_on:
  - 09.1-02
  - 09.1-07
  - 09.1-10
  - 09.1-11
  - 09.1-13
files_modified:
  - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-14-DEPLOYMENT-RECEIPT.md
autonomous: false
requirements:
  - REQ-37
  - REQ-38
  - REQ-39
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "After every pre-deployment implementation/evidence/Plan 01–13 summary commit exists, one auto task freezes an intended SHA and completes the local gate, exact-SHA push, canonical deploy, decision-ready evidence, post-evidence clean-tree check, and sanitized receipt before its mandatory task-end commit."
    - "Per D-28, the exact locally tested commit equals the configured origin branch commit before OMEN deployment, and OMEN is deployed only by `scripts/deploy-omen.sh`."
    - "No commit or tracked implementation/phase-evidence-source change occurs after the full local gate and before canonical deployment/evidence; the receipt is the first tracked write and its task-end commit occurs only afterward, while `09.1-14-SUMMARY.md` is written only after the blocking human verification."
    - "The clean/SHA gate covers tracked dirt and every non-ignored untracked file beneath `web-ui`, `ai-backend`, `scripts`, and the whole Phase 09.1 directory—including manifest, runner, verifier, evidence tests, plans, and summaries—before local verification, before push/deploy, and after evidence."
    - "The post-deploy runner and independent decision-ready verifier receive `--expected-commit` with that same immutable SHA and prove OMEN checkout/health plus equality with the earlier read-only model/context/template/prefill preflight."
    - "Per D-26, the fixed 12-scenario by 3-seed refusal matrix passes exact attempt, fresh-seed, correction, close, classifier precision/recall, and accepted-content integrity gates."
    - "Per D-27, a controlled slow call-shaped refusal-then-accepted stream proves first accepted caption/audio before accepted generation/TTS completion, rejected upstream closure, explicit Interrupt late-chunk rejection, listening recovery, and no whole-response/TTS fallback."
    - "Shared evidence contains only allowlisted IDs, hashes, counts, scalar timings, versions, commit/model identity, and gate outcomes; credentials, runtime seeds, raw prompts/history/generated/rejected prose, audio, private paths, and tracebacks are absent."
    - "The deployed Prompt Inspector effective request equals the captured model request across the fixed Qwen matrix while remaining same-origin, non-mutating, escaped, and credential-free."
  artifacts:
    - "The Plan 02 evidence runner writes its manifest-named ignored local and OMEN result JSON beneath the phase `results/` directory."
    - "The Plan 02 verifier emits a sanitized decision-ready pass/fail report tied to the exact intended commit."
    - "A tracked sanitized deployment receipt records the deployed/tested `intended_sha`, local/origin/OMEN equality, command outcomes, evidence paths, and the fact that its later commit and the later summary commit are not the deployed code SHA."
    - "The plan summary records exact local/origin/OMEN SHA equality, command outcomes, health/model identity, matrix aggregate, call timing order, privacy scan, and blocking-human result without private prose."
  key_links:
    - "tested local HEAD -> exact origin branch HEAD -> deploy-omen expected HEAD -> OMEN checkout -> evidence expected-commit identity."
    - "manifest -> real Qwen scenario dispatch -> allowlisted result JSON -> independent recomputation/verifier -> decision-ready gate."
    - "controlled call barriers -> caption/audio/generation/TTS/Interrupt events -> strict ordering and sink-exclusion gates."
    - "post-evidence clean-tree assertion -> first tracked deployment receipt -> task-end receipt commit -> blocking-human review -> later plan summary commit."
  prohibitions:
    - statement: "Do not deploy through any ad-hoc OMEN script, launcher, manual scheduled-task edit, hidden process, or mechanism other than scripts/deploy-omen.sh."
      status: unresolved
    - statement: "Do not deploy an unpushed, dirty, untested, or SHA-mismatched commit."
      status: unresolved
    - statement: "Do not create a commit or write the execution summary between full local verification and deployment/evidence completion."
      status: unresolved
    - statement: "Do not buffer a whole model response or whole TTS synthesis to obtain production evidence."
      status: unresolved
    - statement: "Do not reintroduce automatic microphone/VAD barge-in during deployment or evidence acquisition."
      status: unresolved
    - statement: "Do not share credentials, runtime seeds, raw prompts/history/generated/rejected prose, audio, private paths, tracebacks, or legacy voice authorization/steward/consent/source/use-scope fields in evidence."
      status: unresolved
---

<objective>
Prove the entire phase locally, deploy the exact tested commit through the canonical OMEN path, and collect independently verified production-path evidence.

Purpose: Turn implementation and focused tests into a release decision tied to one immutable commit, the real configured Qwen runtime, and a live-call-shaped early-streaming retry.
Output: Complete local gate, exact canonical deployment, sanitized real-runtime evidence, independent decision-ready verification, and final human-visible confirmation.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/LIVE-CALL-INVARIANTS.md
@.planning/REFERENCE-AUTHORIZATION-PROHIBITION.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-AI-SPEC.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-02-SUMMARY.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-07-SUMMARY.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-10-SUMMARY.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-11-SUMMARY.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-13-SUMMARY.md
@scripts/deploy-omen.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify, push, canonically deploy, and evidence one exact SHA before the task commit</name>
  <precondition>Every Plan 01–13 implementation, phase-evidence source, and summary commit is complete; an upstream branch is configured; and canonical `rayme-pmpg` OMEN deployment prerequisites are available.</precondition>
  <reversibility rating="costly">Deployment replaces the running OMEN application/database schema at the exact commit; recovery must use repository history and the same canonical deployment script.</reversibility>
  <read_first>
    - AGENTS.md — `scripts/deploy-omen.sh` is the only authorized OMEN deployment mechanism and owns launcher/scheduled-task state.
    - .planning/LIVE-CALL-INVARIANTS.md — production evidence must preserve early playback and explicit interruption.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-AI-SPEC.md — run its complete CI/CD and evidence-contract gate, not a focused subset.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md — D-26 through D-28 lock matrix, live-call, exact-commit, health, and deployment evidence.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-VALIDATION.md — every finalized task/requirement behavior must have its named automated owner and pass.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-evidence-manifest.json — dispatch exactly the frozen scenarios/seeds/artifacts/thresholds.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-run-omen-evidence.py — acquire through production paths and emit only allowlisted fields.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-verify-evidence.py — independently recompute decision readiness for the intended SHA.
    - scripts/deploy-omen.sh — invoke directly; fix this canonical script in a separately reviewed change if it cannot perform required deployment behavior.
  </read_first>
  <files>.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-14-DEPLOYMENT-RECEIPT.md</files>
  <action>Perform D-26/D-27/D-28 as one uninterrupted release transaction inside this auto task; the normal executor task commit remains mandatory and occurs only after the transaction creates its receipt. Before any command, require both `09.1-14-DEPLOYMENT-RECEIPT.md` and `09.1-14-SUMMARY.md` to be absent, set `intended_sha` once from HEAD, and set `release_paths` to `web-ui`, `ai-backend`, `scripts`, and the entire Phase 09.1 directory. At the initial gate, again after local verification immediately before push/deploy, and again after decision-ready evidence, require HEAD to equal `intended_sha`, `git diff --quiet "$intended_sha" -- "${release_paths[@]}"`, zero tracked dirt from `git status --porcelain --untracked-files=no -- "${release_paths[@]}"`, and zero non-ignored untracked inputs from `git ls-files --others --exclude-standard -- "${release_paths[@]}"`; ignored `results/` artifacts may remain. Stop if any tracked or non-ignored untracked source exists, because no file outside `intended_sha` may affect local tests, deployment, or evidence.

Run Alembic head plus the complete Web server, AI backend, protected call/VoxCPM2, Settings, client unit, hermetic desktop Playwright, live-call inventory, evidence self-test, contracts-only verifier, client build, and `git diff --check` gates named in AI-SPEC, with nonzero collected-test assertions. From the still-clean exact tree, write ignored `results/local-verification.json` through Plan 02's `--record-local-verification --expected-commit "$intended_sha"` mode and pass `--local-release`. Repeat both tracked and non-ignored-untracked checks, push the unchanged branch, fetch it, require `HEAD`, `@{upstream}`, and `intended_sha` to be identical, and repeat the clean-tree checks immediately before invoking only `scripts/deploy-omen.sh`. Do not create an OMEN helper, edit launchers, manipulate scheduled tasks, or create a commit at any point in this sequence.

After canonical deployment, run `09.1-run-omen-evidence.py --expected-commit "$intended_sha"` and `09.1-verify-evidence.py --decision-ready --expected-commit "$intended_sha"`. Require exact OMEN commit/health and current model/context/template/prefill equality with Plan 02's preflight, 12 real-Qwen scenarios × 3 seeds, effective-request parity, classifier precision/recall, fresh retry seeds, the exact attempt-2/3 correction, rejected/empty sink exclusion, accepted-content integrity, stream closure, three-attempt cap, early accepted caption/audio, explicit Interrupt/listening recovery, teardown, post-run settings/health equality, and the recursive privacy allowlist. Before writing any tracked file, repeat the HEAD, tracked-dirt, non-ignored-untracked, and `git diff` checks for every release path. Only after all evidence passes, create sanitized `09.1-14-DEPLOYMENT-RECEIPT.md` as this task's first tracked write. The receipt must state the full deployed/tested `intended_sha`, local/upstream/OMEN equality, canonical deploy command, UTC freshness, allowlisted command/gate outcomes, and exact paths to `results/local-verification.json`, `results/omen-evidence.json`, and `results/decision-report.json`; it must also state that the task-end receipt commit and later `09.1-14-SUMMARY.md` commit are intentionally later and are not the deployed code SHA. Leave the human result pending, leave the summary absent, and let the normal executor commit this receipt at task end.</action>
  <acceptance_criteria>
    - Alembic upgrades through `0009`; full server/AI/client/browser/build/evidence suites pass with nonzero test counts; protected slow-stream, Interrupt/listening, and VoxCPM2 streaming-only regressions remain green.
    - Before the local gate, before push/deploy, and after evidence, all four release paths have zero tracked dirt and zero non-ignored untracked files, while only ignored generated result artifacts may remain.
    - Local HEAD, configured origin branch, OMEN checkout, runner expected commit, and verifier expected commit are the same full SHA.
    - No task boundary, commit, or tracked release-source change occurs from the full local gate through deployment and decision-ready evidence.
    - `scripts/deploy-omen.sh` is the sole deployment command and reports healthy canonical services after Alembic/client deployment.
    - All 36 fixed Qwen turns and controlled call-shaped scenario satisfy manifest thresholds, exact effective-request parity, accepted-text integrity, rejected/empty sink exclusion, early caption/audio, and Interrupt/listening recovery.
    - Decision-ready verification passes independent recomputation, freshness/model/template/commit identity, artifact completeness, and recursive privacy scans.
    - The deployment receipt is the first tracked post-evidence write, names the exact deployed/tested `intended_sha`, explains that its task-end commit and the future summary commit are not deployed, and `09.1-14-SUMMARY.md` remains absent pending Task 2.
  </acceptance_criteria>
  <verify>
    <automated>phase_evidence_dir=".planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc"; receipt_path="$phase_evidence_dir/09.1-14-DEPLOYMENT-RECEIPT.md"; release_paths=(web-ui ai-backend scripts "$phase_evidence_dir"); intended_sha="$(python3 -c 'import json, sys; print(json.load(open(sys.argv[1], encoding="utf-8"))["intended_commit"])' "$phase_evidence_dir/results/local-verification.json")" &amp;&amp; test -n "$intended_sha" &amp;&amp; test "$intended_sha" = "$(git rev-parse HEAD)" &amp;&amp; test "$intended_sha" = "$(git rev-parse @{upstream})" &amp;&amp; python3 "$phase_evidence_dir/09.1-verify-evidence.py" --local-release --expected-commit "$intended_sha" &amp;&amp; python3 "$phase_evidence_dir/09.1-verify-evidence.py" --decision-ready --expected-commit "$intended_sha" &amp;&amp; test -f "$receipt_path" &amp;&amp; git diff --quiet "$intended_sha" -- "${release_paths[@]}" &amp;&amp; test -z "$(git status --porcelain --untracked-files=no -- "${release_paths[@]}")" &amp;&amp; test "$(git ls-files --others --exclude-standard -- "${release_paths[@]}")" = "$receipt_path" &amp;&amp; rg -F --quiet "$intended_sha" "$receipt_path" &amp;&amp; rg -F --quiet 'receipt commit' "$receipt_path" &amp;&amp; rg -F --quiet 'not the deployed code SHA' "$receipt_path" &amp;&amp; rg -F --quiet '09.1-14-SUMMARY.md' "$receipt_path" &amp;&amp; test ! -e "$phase_evidence_dir/09.1-14-SUMMARY.md"</automated>
    <fails_when>The command exits non-zero, local/decision-ready evidence is not tied to the current intended SHA, upstream differs, tracked dirt remains, a non-ignored untracked release input exists besides the new receipt, the receipt omits the deployed/tested SHA or later-commit distinction, or the Plan 14 summary exists before human approval.</fails_when>
  </verify>
  <done>The unchanged locally verified SHA is pushed, canonically deployed, and decision-ready in one commit-free transaction; a sanitized receipt is ready for the mandatory task-end commit and the summary remains blocked on human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Confirm deployed visual and live-call behavior before summary creation</name>
  <read_first>
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-14-DEPLOYMENT-RECEIPT.md — confirm the deployed/tested `intended_sha` and post-deployment receipt-commit distinction.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/results/local-verification.json — sanitized exact-SHA local gate result.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/results/omen-evidence.json — sanitized deployed matrix and live-call event evidence.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/results/decision-report.json — independent decision-ready verdict for the same intended SHA.
  </read_first>
  <what-built>The exact SHA recorded in `09.1-14-DEPLOYMENT-RECEIPT.md` has passed the full local gate, equals origin and OMEN, was deployed only through `scripts/deploy-omen.sh`, and has passing decision-ready Qwen/refusal/live-call/privacy evidence. The receipt's later commit is intentionally not the deployed code SHA.</what-built>
  <how-to-verify>
    Use the canonical deployed RayMe UI at `https://192.168.1.199:8443` and approve only when every item below passes:
    1. Confirm `09.1-14-DEPLOYMENT-RECEIPT.md`, `results/local-verification.json`, `results/omen-evidence.json`, and `results/decision-report.json` name/pass the same full `intended_sha`, with no private prompt, generated/rejected prose, credential, seed, path, audio, or traceback content.
    2. Open Settings: Roleplay is selected on first ready render; Roleplay/Assistant/Custom fields, context/output, samplers, reset, save, loading, and error states are visible and usable.
    3. Open Character Editor: `{{char}}`, `{{user}}`, `{{original}}`, `<START>` examples, inheritance, and late-PHI help is readable and rendered as escaped text.
    4. Open Prompt Inspector on desktop and at 320px: ordered content remains exact/selectable/escaped, no page-level horizontal scroll appears, keyboard focus enters/traps/returns correctly, and Close/Escape work.
    5. Run one text roleplay turn through the refusal-recovery path: no rejected generic/guideline refusal appears or persists, and the accepted in-character result remains usable.
    6. Start one live call: accepted caption/audio begins while later LLM/TTS work is still active; press explicit Interrupt after first audio; late caption/audio does not continue as a normal completion; the call returns to Listening. Do not enable or test automatic microphone/VAD barge-in.
  </how-to-verify>
  <acceptance_criteria>
    - Every listed evidence artifact passes for the same receipt-recorded `intended_sha`.
    - Settings, Character help, and desktop/320px Prompt Inspector satisfy the visible, escaped, responsive, and focus criteria.
    - Text refusal recovery exposes/persists no rejected prose, and the live call preserves early playback, explicit Interrupt, late-chunk rejection, and listening recovery.
    - Approval authorizes creation of `09.1-14-SUMMARY.md`; any reported failure stops completion and records no summary.
  </acceptance_criteria>
  <resume-signal>Reply `approved` only after all six checks pass, or describe the exact failed check and observed behavior.</resume-signal>
  <done>Blocking human verification approves the exact deployed `intended_sha`; only then may the executor create `09.1-14-SUMMARY.md` as a later non-deployed commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repository -> origin -> OMEN | Release identity crosses git and remote deployment state. |
| evidence runner -> configured Qwen/web/call runtime | Controlled fixtures cross real production paths and receive private runtime content/events. |
| raw runner observations -> shared result JSON/report | Only sanitized allowlisted facts may become durable/shareable evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09.1-14-01 | Spoofing | commit/model identity | high | mitigate | Full-SHA local/upstream/OMEN equality plus runner/verifier expected-commit and preflight-observed model/template identity checks. |
| T-09.1-14-02 | Tampering | deployment path | high | mitigate | Invoke only tracked `scripts/deploy-omen.sh`; no manual launchers/tasks; fail if canonical script cannot complete. |
| T-09.1-14-03 | Information Disclosure | evidence outputs | high | mitigate | Positive field allowlist, hashes/counts only, recursive canary scan, quarantine, ignored raw results, and sanitized failures. |
| T-09.1-14-04 | Repudiation | verified-SHA continuity | high | mitigate | Freeze one intended SHA after all predeployment commits; reject tracked dirt and non-ignored untracked release inputs at every boundary; complete local gate, push, deploy, and evidence in one auto task; then record the intended SHA in the first tracked receipt before the mandatory task-end commit. |
| T-09.1-14-05 | Denial of Service | live matrix/call evidence | medium | mitigate | Fixed finite 36-turn schedule, three-attempt caps, owned stream closure, bounded timeouts, and health recovery checks. |
| T-09.1-SC | Tampering | package supply chain | low | accept | No package install occurs; deployment uses existing lockfiles and the repository's canonical script. |
</threat_model>

## Artifacts this phase produces

- Ignored `results/local-verification.json`: allowlisted local command/test/build/migration/evidence gate facts tied to the candidate SHA.
- Manifest-named ignored `results/omen-evidence.json`: real-Qwen matrix, exact request parity, call event/timing, commit/model/health, and privacy-safe gate facts.
- Ignored `results/decision-report.json`: independent verifier result and recomputed aggregate for the exact expected commit.
- `09.1-14-DEPLOYMENT-RECEIPT.md`: first tracked post-evidence write, recording the deployed/tested `intended_sha`, exact release/evidence outcomes, and the later receipt/summary commit distinction.
- `09.1-14-SUMMARY.md`: post-checkpoint sanitized release record with exact SHA equality, command counts, deployed health/model identity, matrix/call gates, privacy result, and blocking-human result.

<verification>
Run Task 1's automated postflight without substituting commands, let the executor commit only the completed deployment receipt, then stop at Task 2's `gate="blocking-human"` review. Create the plan summary only after the user returns the checkpoint's `approved` resume signal.
</verification>

<success_criteria>
One exact clean commit passes all local gates, equals origin and OMEN, runs through the sole canonical deploy path, and has independently verified real-Qwen/live-call evidence plus blocking human acceptance with zero private/rejected content leakage; later receipt/summary commits remain explicitly distinct from the deployed SHA.
</success_criteria>

<output>
Create `.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-14-SUMMARY.md` only after Task 1's receipt commit and Task 2's blocking-human approval are complete; record the deployed/tested `intended_sha`, not the later receipt/summary commit as deployment identity.
</output>
