---
phase: 09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc
plan: 11
type: execute
wave: 4
depends_on: [09.1-09]
files_modified:
  - web-ui/client/src/lib/components/PromptMacroHelp.svelte
  - web-ui/client/src/routes/characters/[id]/+page.svelte
  - web-ui/client/tests/unit/character-prompt-help.test.ts
autonomous: true
requirements: [REQ-37, REQ-38]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "D-07/D-08/D-25: Character Editor persistently explains blank inheritance, nonblank replacement, `{{original}}`, and one nonrecursive `{{char}}`/`{{user}}` expansion with exact escaped tokens."
    - "D-11: Example help names exact `<START>`, `{{user}}:`, and `{{char}}:` syntax and explains that only complete scenes are retained under budget pressure."
    - "System and late-PHI guidance reacts among blank inheritance, nonblank replacement, and includes-original states without mutating card fields or reading later live examples for legacy threads."
    - "Help stays visible in create/review/edit modes, preserves existing editor flows, and wraps without hiding content at 320px."
  prohibitions:
    - statement: "Do not render prompt/card/help text through raw HTML, unsafe Markdown, or an HTML parser."
      status: unresolved
    - statement: "Do not mutate character fields, global settings, or thread snapshots from explanatory help."
      status: unresolved
    - statement: "Do not live-reread later character example text for a legacy null snapshot."
      status: unresolved
  artifacts:
    - path: "web-ui/client/src/lib/components/PromptMacroHelp.svelte"
      provides: "Persistent escaped macro/example/inheritance/PHI guidance"
    - path: "web-ui/client/src/routes/characters/[id]/+page.svelte"
      provides: "Create/review/edit integration without changing existing editor actions"
    - path: "web-ui/client/tests/unit/character-prompt-help.test.ts"
      provides: "Exact copy, state, escaping, mutation, mode, long-text, and 320px contracts"
  key_links:
    - from: "web-ui/client/src/routes/characters/[id]/+page.svelte"
      to: "web-ui/client/src/lib/components/PromptMacroHelp.svelte"
      via: "Current local System/PHI/profile values feed read-only reactive guidance"
      pattern: "PromptMacroHelp"
    - from: "web-ui/client/src/lib/components/PromptMacroHelp.svelte"
      to: "web-ui/client/tests/unit/character-prompt-help.test.ts"
      via: "Escaped token and hostile-markup fixtures prove exact text-only rendering"
      pattern: "original|START|post-history"
---

<objective>
Explain prompt examples, macros, inheritance, and late PHI directly in Character Editor as an independently executable UI slice.

Purpose: Make card-authoring behavior visible without mutating card data, consulting mutable legacy sources, or interpreting markup.
Output: Reusable PromptMacroHelp, Character Editor integration, and focused exact-copy/accessibility/responsive tests.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-UI-SPEC.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-PATTERNS.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-09-SUMMARY.md
@web-ui/client/src/routes/characters/[id]/+page.svelte
@web-ui/client/src/lib/components/CharacterFormSection.svelte
</context>

## Artifacts this phase produces

- `web-ui/client/src/lib/components/PromptMacroHelp.svelte`: escaped macro, example, inheritance, and late-PHI guidance.
- Updated Character Editor integration preserving create/review/edit, Discard/Save, portrait, and voice behavior.
- `character-prompt-help.test.ts`: exact-copy, read-only reactivity, escaping, modes, long content, and 320px coverage.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Explain examples, macros, inheritance, and late PHI in Character Editor</name>
  <read_first>
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md — D-07, D-08, D-10 through D-12, and D-25 define visible macro/inheritance/example truth.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-UI-SPEC.md — use exact Character Editor helper, macro definition, inheritance, PHI copy, modes, escaping, and 320px behavior.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-09-SUMMARY.md — consume the typed selected global profile from the completed client settings contract.
    - web-ui/client/src/routes/characters/[id]/+page.svelte — preserve the current two-column create/review/edit form and save/discard/portrait/voice flows.
    - web-ui/client/src/lib/components/CharacterFormSection.svelte — match established section rhythm.
  </read_first>
  <files>web-ui/client/src/lib/components/PromptMacroHelp.svelte, web-ui/client/src/routes/characters/[id]/+page.svelte, web-ui/client/tests/unit/character-prompt-help.test.ts</files>
  <behavior>
    - Example messages always show the exact `<START>`, `{{user}}:`, `{{char}}:`, and complete-scene trimming help.
    - The macro callout shows exact `{{char}}`, `{{user}}`, and `{{original}}` definitions plus the one-pass/unknown-macro sentence using code/text nodes only; `{{user}}` visibly resolves to `User` for this phase.
    - System prompt and PHI guidance reacts exactly among blank inheritance, nonblank override, and nonblank includes-original states without mutating field values.
    - PHI always carries the exact after-history/jailbreak explanatory sentence.
    - Help remains visible and wraps at 320px in create/review/edit modes; hostile markup is selectable escaped characters and existing editor actions remain unchanged.
  </behavior>
  <action>Build `PromptMacroHelp` as a compact reusable tonal callout rendered only through ordinary Svelte interpolation. Integrate the exact UI-SPEC example helper, macro definition rows, one-pass sentence, phase-local `{{user}} = User` explanation, reactive System/PHI source guidance, and PHI explanation into existing Character Editor sections per D-07/D-08/D-11/D-25. Derive guidance from current local field values and selected global profile information without writing either or consulting a thread's mutable/live example source. Add create/review/edit, blank/override/original, long Unicode, 320px wrapping, hostile `<img onerror=...>` escaping, and existing-action preservation tests.</action>
  <acceptance_criteria>
    - Every exact Character Editor sentence/token from UI-SPEC is asserted in all relevant editor modes, including the literal `User` macro value.
    - Reactive guidance changes only when local field/profile inputs change and never mutates saved or unsaved card content.
    - Hostile card/help strings render as text; no raw HTML API appears.
    - 320px and long-text fixtures wrap without hiding guidance, and Discard/Save/portrait/voice tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix web-ui/client exec vitest -- run tests/unit/character-prompt-help.test.ts</automated>
    <fails_when>The command exits non-zero, reports 0 passed, exact help/inheritance copy differs, the user macro value is not `User`, markup is interpreted, fields mutate, modes lose help, or 320px content overflows.</fails_when>
  </verify>
  <done>Character authors can see exactly how examples, macros, inheritance, and PHI compose in every editor state without hidden mutation or unsafe rendering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Card/prompt text → Svelte DOM | Arbitrary Unicode and markup-like text enters Character Editor guidance. |
| Mutable editor/profile state → read-only help | Explanatory reactivity must not write card, settings, or thread data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09.1-11-01 | Elevation of Privilege | Card/help rendering | high | mitigate | Ordinary interpolation only, hostile-markup fixtures, and no HTML/Markdown parser. |
| T-09.1-11-02 | Tampering | Reactive inheritance help | medium | mitigate | Derived read-only props/state plus mutation spies across blank/override/original transitions. |
| T-09.1-11-03 | Information Disclosure | Legacy null example state | medium | mitigate | Help consumes current form/profile data only and never queries a later live character source for a thread snapshot. |
| T-09.1-SC | Tampering | Package supply chain | low | accept | No npm, pip, or cargo package installation occurs in this plan. |
</threat_model>

<verification>
Run the task command and `git diff --check`. Confirm exact help copy remains present in all editor modes and all markup-like input is rendered as text.
</verification>

<success_criteria>
Character authors understand complete examples, exact macro values, inheritance, and late PHI in every editor mode without hidden mutation, live legacy rereads, or unsafe rendering.
</success_criteria>

<output>
Create `.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-11-SUMMARY.md` when done.
</output>
