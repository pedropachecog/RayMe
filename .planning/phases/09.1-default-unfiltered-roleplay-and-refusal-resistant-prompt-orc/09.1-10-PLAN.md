---
phase: 09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc
plan: 10
type: execute
wave: 4
depends_on: [09.1-09]
files_modified:
  - web-ui/client/src/app.css
  - web-ui/client/static/fonts/Manrope-SemiBold.woff2
  - web-ui/client/static/fonts/OFL.txt
  - web-ui/client/src/lib/components/settings/PromptGenerationSettingsPanel.svelte
  - web-ui/client/tests/unit/prompt-generation-settings.test.ts
autonomous: true
requirements: [REQ-37, REQ-38]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "D-23: The complete Prompt & Generation surface remains truthful across empty, loading, error, populated, partial, mandatory-overflow, and long-text states while preserving entered content."
    - "Local Manrope SemiBold owns all app-level Heading/Display tokens while Inter remains Body/Label/control/prompt prose; no runtime font CDN or third-party component registry is used."
    - "Settings stays one column below 720px, mode cards stack below 640px, controls keep 44px targets, and no phase-owned state overflows a 320px viewport."
    - "Long prompt text preserves line breaks, wraps, and remains vertically resizable within the approved 144–320px bounds; mandatory overflow never trims or rewrites text."
  prohibitions:
    - statement: "Do not fetch Manrope or any UI component/style asset from a runtime CDN or add a third-party component registry."
      status: unresolved
    - statement: "Do not render prompt text through raw HTML, unsafe Markdown, or an HTML parser."
      status: unresolved
    - statement: "Do not hide mandatory budget overflow by trimming or permanently ellipsizing the user's prompt text."
      status: unresolved
  artifacts:
    - path: "web-ui/client/static/fonts/Manrope-SemiBold.woff2"
      provides: "Reviewed locally served Manrope SemiBold font asset"
    - path: "web-ui/client/static/fonts/OFL.txt"
      provides: "Bundled upstream license"
    - path: "web-ui/client/src/app.css"
      provides: "App-level Heading/Display typography tokens and responsive foundations"
    - path: "web-ui/client/tests/unit/prompt-generation-settings.test.ts"
      provides: "Settings state, asset, escaping, breakpoint, target-size, and 320px contracts"
  key_links:
    - from: "web-ui/client/src/app.css"
      to: "web-ui/client/static/fonts/Manrope-SemiBold.woff2"
      via: "Local @font-face and app-level Heading/Display tokens"
      pattern: "Manrope|font-face"
    - from: "web-ui/client/src/lib/components/settings/PromptGenerationSettingsPanel.svelte"
      to: "web-ui/client/tests/unit/prompt-generation-settings.test.ts"
      via: "Explicit responsive fixtures cover every approved state and breakpoint"
      pattern: "320|640|720|overflow"
---

<objective>
Finish the Settings surface's app-level local typography and responsive/state behavior after its typed controls are independently working.

Purpose: Make the visible prompt controls stable, readable, escaped, and operable from 320px through desktop without changing their persistence semantics.
Output: Local Manrope/OFL assets, app typography tokens, responsive component styling, and state/geometry tests.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-CONTEXT.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-UI-SPEC.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-PATTERNS.md
@.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-09-SUMMARY.md
@web-ui/client/src/app.css
@web-ui/client/src/lib/components/settings/PromptGenerationSettingsPanel.svelte
</context>

## Artifacts this phase produces

- `web-ui/client/static/fonts/Manrope-SemiBold.woff2` and `OFL.txt`: reviewed locally served heading font and license.
- `web-ui/client/src/app.css`: Manrope Heading/Display and Inter Body/Label token assignment with no runtime font request.
- Responsive/state styling in `PromptGenerationSettingsPanel.svelte`.
- `prompt-generation-settings.test.ts`: asset, escaping, state, breakpoint, target-size, long-content, and 320px contracts.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finish responsive prompt states and app-level local typography</name>
  <read_first>
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-UI-SPEC.md — apply its tokens, typography, Settings empty/loading/error/populated/overflow/long-text behavior, and 320px/640px/720px breakpoints.
    - .planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-09-SUMMARY.md — consume the independently completed typed Settings panel without changing its DTO/persistence contract.
    - web-ui/client/src/app.css — extend existing tokens and responsive conventions at app level.
    - web-ui/client/src/lib/components/settings/PromptGenerationSettingsPanel.svelte — style semantic controls without changing Plan 09 behavior.
    - web-ui/client/tests/unit/settings.test.ts — retain exact route state assertions.
  </read_first>
  <files>web-ui/client/src/app.css, web-ui/client/static/fonts/Manrope-SemiBold.woff2, web-ui/client/static/fonts/OFL.txt, web-ui/client/src/lib/components/settings/PromptGenerationSettingsPanel.svelte, web-ui/client/tests/unit/prompt-generation-settings.test.ts</files>
  <behavior>
    - Manrope SemiBold is loaded locally with its license and applied once to Heading/Display tokens; Inter remains Body/Label/control/prompt text and no runtime font request leaves RayMe.
    - Settings is one column below 720px, numeric controls are one column below 720px, mode cards stack below 640px, and the panel does not overflow a 320px viewport.
    - Mode cards have at least 44px targets/56px height; selected Roleplay has the 4px pulse-gradient strip, checked radio, and Default chip without relying on color alone.
    - Textareas remain 100% width, 144–320px high and vertically resizable; long prompts preserve line breaks, wrap, and never permanently ellipsize.
    - Skeleton, populated preset/source/Modified states, intentional blank Custom fields, typed errors, and mandatory budget overflow preserve stable geometry and entered content.
  </behavior>
  <action>Acquire the official Manrope SemiBold WOFF2 and OFL license from the upstream Google Fonts Manrope family at an immutable revision, verify the downloaded file is a valid WOFF2, and commit both as local assets. Add app-level `@font-face` and Heading/Display token assignment in `app.css`, retaining Inter everywhere else and no runtime URL. Implement UI-SPEC spacing, surface, accent, focus, target, textarea, grid, and breakpoint rules in the phase-owned component using source-owned CSS. Add DOM/style-contract fixtures at 320px, 639px, 720px, and desktop for Custom empty, skeleton/loading, save error, populated/Modified, invalid/partial, mandatory budget overflow, and long Unicode prompt states.</action>
  <acceptance_criteria>
    - Font validation and tests prove a local WOFF2/OFL pair, no remote font URL, and app-level Manrope/Inter role assignment.
    - Responsive fixtures show no horizontal overflow at 320px and exact stack/grid changes at 640px/720px with 44px targets.
    - Every Settings consideration pair—empty, loading, error, populated, partial, overflow, and long text—has an explicit assertion and stable preserved content.
    - Hostile prompt markup remains escaped text and no raw-HTML rendering path exists in the component.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix web-ui/client exec vitest -- run tests/unit/prompt-generation-settings.test.ts tests/unit/settings.test.ts</automated>
    <fails_when>The command exits non-zero, reports 0 passed, the font is remote/invalid/unlicensed, 320px overflows, breakpoints/targets differ, or a UI state lacks its explicit assertion.</fails_when>
  </verify>
  <done>The prompt settings surface is locally branded, readable and operable at 320px through desktop, and truthful across all approved state categories and long content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Prompt text → responsive Svelte DOM | Arbitrary Unicode and markup-like content enters styled visible controls. |
| Local font asset → app typography | A vendored binary and license enter the shipped frontend asset graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09.1-10-01 | Elevation of Privilege | Prompt rendering | high | mitigate | Ordinary Svelte interpolation, hostile-markup fixtures, and no HTML/Markdown parser. |
| T-09.1-10-02 | Denial of Service | Long prompt UI | medium | mitigate | Bounded resizable controls, wrapping, responsive containment, and explicit overflow fixtures. |
| T-09.1-10-03 | Tampering | Local font asset | medium | mitigate | Immutable official upstream source, WOFF2 validation, license inclusion, tracked local loading, and no runtime CDN. |
| T-09.1-SC | Tampering | Package supply chain | low | accept | No npm, pip, or cargo package install occurs; the font is a reviewed licensed static asset with validation. |
</threat_model>

<verification>
Run the task command and `git diff --check`. Confirm no runtime font URL, raw-HTML rendering path, or unresolved Settings state pair remains.
</verification>

<success_criteria>
The independently functional Settings panel is locally branded, responsive, accessible, exact-text safe, and truthful across all required UI states.
</success_criteria>

<output>
Create `.planning/phases/09.1-default-unfiltered-roleplay-and-refusal-resistant-prompt-orc/09.1-10-SUMMARY.md` when done.
</output>
