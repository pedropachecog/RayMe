---
phase: 08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - ai-backend/app/api/webrtc.py
  - ai-backend/tests/test_webrtc_signaling.py
  - web-ui/server/app/api/calls.py
  - web-ui/server/tests/test_calls.py
autonomous: true
requirements:
  - P8-R1
  - P8-R3
  - P8-R4
must_haves:
  truths:
    - "The existing `/webrtc/sessions/{session_id}/speak` route exposes streaming timing without a new VoxCPM2 public route."
    - "Web UI call SSE forwards the first-audio event and keeps one durable `ai_speech` row."
    - "Sanitized `call_tts_failed` behavior is unchanged for streaming failures."
  artifacts:
    - path: "ai-backend/app/api/webrtc.py"
      provides: "Stable speak route response wrapping streaming metrics"
    - path: "web-ui/server/app/api/calls.py"
      provides: "SSE keepalive and nested audio-started forwarding"
    - path: "ai-backend/tests/test_webrtc_signaling.py"
      provides: "Route tests for bounded inputs and sanitized streaming failures"
    - path: "web-ui/server/tests/test_calls.py"
      provides: "Server tests for single durable AI speech row and forwarded metrics"
  key_links:
    - from: "ai-backend/app/api/webrtc.py"
      to: "ai-backend/app/call/session.py"
      via: "session.speak_text(...) return payload"
      pattern: "speak_text"
    - from: "web-ui/server/app/api/calls.py"
      to: "ai-backend/app/api/webrtc.py"
      via: "AiBackendClient.speak_call return dict"
      pattern: "_extract_ai_audio_started_event"
---

<objective>
Preserve RayMe's public call surfaces while carrying immediate first-audio streaming metrics and final playback metrics through the existing backend and Web UI call path.

Purpose: satisfy D-01, D-02, D-06, D-07, D-08, and D-09 without adding browser-visible VoxCPM2 runtime APIs.
Output: route/server tests and any small response-shape adjustments needed for immediate `ai_audio_started_event.tts_playback`, final `event.tts_playback_final`, and one durable AI turn.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback/08-SPEC.md
@.planning/phases/08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback/08-CONTEXT.md
@.planning/phases/08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback/08-PATTERNS.md
@.planning/phases/08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback/08-02-SUMMARY.md
@ai-backend/app/api/webrtc.py
@ai-backend/tests/test_webrtc_signaling.py
@web-ui/server/app/api/calls.py
@web-ui/server/app/domain/ai_backend_client.py
@web-ui/server/tests/test_calls.py

<interfaces>
Existing route/server contracts:
```python
@router.post("/sessions/{session_id}/speak")
async def speak_session(request: Request, session_id: str, payload: SpeakRequest) -> dict[str, Any]:
    event = await session.speak_text(...)
    return {"session_id": session.session_id, "turn_id": payload.turn_id, "state": session.state, "event": event}

def _extract_ai_audio_started_event(speak_result: Mapping[str, Any] | None) -> dict[str, Any] | None:
    candidates = [
        speak_result.get("ai_audio_started_event"),
        event.get("ai_audio_started_event") if isinstance(event, Mapping) else None,
    ]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock `/webrtc/speak` streaming metrics and sanitized failures</name>
  <files>ai-backend/app/api/webrtc.py, ai-backend/tests/test_webrtc_signaling.py</files>
  <read_first>
    ai-backend/app/api/webrtc.py
    ai-backend/app/call/session.py
    ai-backend/tests/test_webrtc_signaling.py
    .planning/phases/08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback/08-SPEC.md
    .planning/phases/08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback/08-CONTEXT.md
    .planning/phases/08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback/08-02-SUMMARY.md
  </read_first>
  <behavior>
    - `/webrtc/sessions/{session_id}/speak` still returns HTTP 200 for successful speech.
    - The route response contains nested `event.ai_audio_started_event.tts_playback` for immediate first-audio metrics.
    - The route response contains `event.tts_playback_final` for metrics only known after generation/playback completion.
    - Raw streaming failures still return fixed `call_tts_failed` details.
  </behavior>
  <action>In `test_webrtc_signaling.py`, add tests named `test_webrtc_speak_returns_streaming_tts_playback_metrics_for_voxcpm2` and `test_webrtc_speak_streaming_failure_keeps_fixed_public_error`. Use a scripted VoxCPM2 adapter that implements `stream(...)` and one that raises `RuntimeError("Traceback /home/pmpg/.cache openbmb/VoxCPM2")` from `stream(...)`. Assert the successful response still uses `/webrtc/sessions/{session_id}/speak`, status `200`, `payload["event"]["type"] == "ai_done"`, `payload["event"]["ai_audio_started_event"]["type"] == "ai_audio_started"`, `payload["event"]["ai_audio_started_event"]["tts_playback"]["streaming_used"] is True`, `payload["event"]["ai_audio_started_event"]["tts_playback"]["chunk_count_at_start"] == 1`, and `payload["event"]["ai_audio_started_event"]["tts_playback"]` does not contain `total_generation_ms` or `total_playback_ms`. Assert the same successful response has `payload["event"]["tts_playback_final"]["streaming_used"] is True`, `chunk_count >= 1`, finite `total_generation_ms`, finite `total_playback_ms`, and list `inter_chunk_gaps_ms`. Assert the failure response is status `502` with exact detail `{"code": "call_tts_failed", "message": "Speech playback failed", "engine_id": "voxcpm2"}` and response text does not contain `Traceback`, `/home/`, `.cache`, or `openbmb/VoxCPM2`; for failures after first audio, assert returned event shape preserves `ai_audio_started_event.tts_playback` plus `tts_playback_final` when available. Update `webrtc.py` only if the current wrapper drops `ai_audio_started_event`, drops `tts_playback_final`, or leaks raw errors; do not add any route containing `voxcpm2` in its path.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_webrtc_signaling.py -q</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "test_webrtc_speak_returns_streaming_tts_playback_metrics_for_voxcpm2|test_webrtc_speak_streaming_failure_keeps_fixed_public_error|tts_playback_final|chunk_count_at_start" ai-backend/tests/test_webrtc_signaling.py` returns all four patterns.
    `rg -n "voxcpm2" ai-backend/app/api/webrtc.py` returns only payload option handling, not a `@router` path containing `voxcpm2`.
    `uv run --project ai-backend pytest ai-backend/tests/test_webrtc_signaling.py -q` passes.
  </acceptance_criteria>
  <done>The AI backend speak route carries streaming proof fields while preserving bounded inputs and sanitized public errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve Web UI SSE keepalive and single durable AI speech row</name>
  <files>web-ui/server/app/api/calls.py, web-ui/server/tests/test_calls.py</files>
  <read_first>
    web-ui/server/app/api/calls.py
    web-ui/server/app/domain/ai_backend_client.py
    web-ui/server/tests/test_calls.py
    .planning/phases/08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback/08-SPEC.md
    .planning/phases/08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback/08-CONTEXT.md
    .planning/phases/08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback/08-PATTERNS.md
  </read_first>
  <behavior>
    - Web UI call turn response includes one `ai_audio_started` SSE event with immediate `tts_playback`.
    - Web UI preserves final backend `tts_playback_final` if the call facade records or exposes the returned final speak payload.
    - Web UI server writes one `ai_speech` row for the visible LLM response.
    - No VoxCPM2-specific Web UI or browser route is created.
  </behavior>
  <action>Add `test_turn_forwards_streaming_audio_started_metrics_without_extra_speech_rows` to `web-ui/server/tests/test_calls.py`. Use a scripted backend `speak_call(...)` result containing `{"event": {"type": "ai_done", "ai_audio_started_event": {"type": "ai_audio_started", "tts_playback": {"streaming_used": True, "fallback_used": False, "whole_wav_fallback_used": False, "chunk_count_at_start": 1, "first_chunk_generated_ms": 390.0, "first_chunk_enqueued_ms": 410.0, "ai_audio_started_ms": 412.0, "inter_chunk_gaps_ms": []}}, "tts_playback_final": {"streaming_used": True, "fallback_used": False, "whole_wav_fallback_used": False, "chunk_count": 3, "total_generation_ms": 1800.0, "total_playback_ms": 1700.0, "inter_chunk_gaps_ms": [22.0, 31.0]}}}`. Assert `_sse_events(response.text)` contains exactly one `ai_audio_started` event, that event preserves `tts_playback.chunk_count_at_start == 1`, that immediate `tts_playback` does not contain `total_generation_ms` or `total_playback_ms`, and database message rows contain exactly one `ai_speech` row for the accumulated visible text. Keep `_extract_ai_audio_started_event(...)` returning `dict(candidate)` so nested immediate metrics survive. If the helper currently fails the test, update only that helper or the SSE yield site; do not add per-chunk persistence and do not add a route with `voxcpm2` in the path.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py -q</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "test_turn_forwards_streaming_audio_started_metrics_without_extra_speech_rows|tts_playback|tts_playback_final|chunk_count_at_start|whole_wav_fallback_used" web-ui/server/tests/test_calls.py` returns all five patterns.
    `rg -n "voxcpm2" web-ui/server/app/api/calls.py` does not return a FastAPI route path containing `voxcpm2`.
    `uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py -q` passes.
  </acceptance_criteria>
  <done>Web UI call SSE forwards first-audio metrics and keeps one durable AI speech turn per LLM response.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/Web UI -> AI backend `/webrtc` | Browser-originated call control reaches AI backend session routes through the Web UI facade. |
| AI backend -> Web UI SSE | Backend speak result is forwarded to browser-visible call SSE. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Information Disclosure | `/webrtc/sessions/{session_id}/speak` and Web UI SSE | mitigate | Assert fixed `call_tts_failed` detail and forbid `Traceback`, `/home/`, `.cache`, and `openbmb/VoxCPM2` in responses. |
| T-08-03-02 | Denial of Service | `SpeakRequest` | mitigate | Keep `extra="forbid"`, reference-audio max length, text max length `5000`, transcript max length `10000`, and VoxCPM2 numeric bounds. |
| T-08-03-03 | Elevation of Privilege | route surface | mitigate | Preserve existing `/webrtc` and `/api/calls` surfaces; add no browser-visible VoxCPM2 runtime endpoint. |
| T-08-03-04 | Tampering | interrupt and late chunk enqueue evidence | mitigate | Surface `tts_playback` fields from the call session so tests can prove cancellation prevents later chunks. |
| T-08-03-05 | Repudiation | false streaming success | mitigate | Forward `streaming_used`, `fallback_used`, and `whole_wav_fallback_used` through route/server tests for evidence tooling. |
</threat_model>

<verification>
Run `uv run --project ai-backend pytest ai-backend/tests/test_webrtc_signaling.py -q`, `uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py -q`, then `git diff --check`.
</verification>

<success_criteria>
Existing public call APIs remain intact while immediate first-audio timing reaches Web UI SSE and final fallback/playback proof fields reach the returned backend speak payload for evidence code.
</success_criteria>

<output>
After completion, create `.planning/phases/08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback/08-03-SUMMARY.md`.
</output>
