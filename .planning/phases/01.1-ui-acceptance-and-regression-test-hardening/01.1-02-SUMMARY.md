---
phase: 01.1-ui-acceptance-and-regression-test-hardening
plan: "02"
subsystem: testing
tags: [playwright, e2e, portrait-persistence, sveltekit, ui-regression]

requires:
  - phase: 01.1-ui-acceptance-and-regression-test-hardening
    provides: Shared Playwright acceptance helpers, fixture builders, and PNG portrait image fulfillment from plan 01.1-01
provides:
  - Browser-visible PNG import portrait persistence coverage across Editor, Gallery, Home, Chat, and reload
  - Browser-visible direct upload, replacement, removal, stale-asset absence, and initials fallback coverage
  - Consistent two-initial removed portrait fallback in Editor, Home picker, and Chat header
affects: [settings-connection, phase1-full-path, live-lan-smoke, android-acceptance]

tech-stack:
  added: []
  patterns:
    - Playwright route mocks paired with actual image/png fulfillment and browser request tracking
    - Surface-level portrait assertions across Editor, Gallery, Home picker, Home thread row, and Chat header

key-files:
  created:
    - web-ui/client/tests/e2e/portrait-persistence.spec.ts
  modified:
    - web-ui/client/src/lib/components/PortraitDropzone.svelte
    - web-ui/client/src/routes/characters/[id]/+page.svelte
    - web-ui/client/src/routes/+page.svelte
    - web-ui/client/src/routes/chat/[threadId]/+page.svelte

key-decisions:
  - "Normalized removed portrait fallback to two initials in Editor, Home picker, and Chat header so all required surfaces match Gallery and thread-row behavior."
  - "Kept portrait tests route-mocked and deterministic while fulfilling actual image/png requests through the shared helper."

patterns-established:
  - "Portrait regression tests assert DOM image visibility, browser image requests, stale image absence, and fallback text instead of JSON-only portrait_url presence."
  - "TDD task commits use RED Playwright failures followed by GREEN browser/API verification commits."

requirements-completed: [REQ-10, REQ-11, REQ-12, REQ-13, REQ-70, REQ-72, REQ-90, REQ-A0, REQ-A1]

duration: 23 min
completed: 2026-04-24
---

# Phase 01.1 Plan 02: Portrait Persistence Regression Summary

**Playwright portrait persistence coverage with real image requests, reload checks, upload replacement/removal assertions, and consistent removed-portrait initials fallback**

## Performance

- **Duration:** 23 min
- **Started:** 2026-04-24T15:31:01Z
- **Completed:** 2026-04-24T15:53:48Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added `portrait-persistence.spec.ts` covering PNG card import portrait persistence across Editor preview, Gallery card, Home picker, Home thread row, Chat header, and reload.
- Added direct portrait upload coverage for first upload, save/reload persistence, replacement, stale asset absence, removal, and fallback rendering.
- Fixed removed-portrait initials consistency in Editor, Home picker, and Chat header so every covered surface returns to `PU` after removal.

## Task Commits

Each task used RED/GREEN commits because both tasks were marked `tdd="true"`:

1. **Task 1: Cover PNG import portrait persistence across all required surfaces**
   - `26b5f51` test: add failing test for PNG portrait import persistence
   - `58fcbd9` feat: cover PNG portrait import persistence
2. **Task 2: Cover direct portrait upload replacement and removal**
   - `8b84d33` test: add failing test for direct portrait upload
   - `67978d9` feat: cover direct portrait upload persistence

## Files Created/Modified

- `web-ui/client/tests/e2e/portrait-persistence.spec.ts` - New Playwright spec for PNG import, direct upload, replacement, removal, reload, stale asset, and fallback coverage.
- `web-ui/client/src/lib/components/PortraitDropzone.svelte` - Shows character initials in the empty/removed portrait state when initials are available.
- `web-ui/client/src/routes/characters/[id]/+page.svelte` - Passes editor character initials into `PortraitDropzone`.
- `web-ui/client/src/routes/+page.svelte` - Uses two-initial fallback in the Home start-chat picker.
- `web-ui/client/src/routes/chat/[threadId]/+page.svelte` - Uses two-initial fallback in the Chat header.

## Verification

- `npm --prefix web-ui/client run test:e2e -- portrait-persistence.spec.ts` - PASS, 4 Playwright tests across desktop and mobile projects.
- `uv run --project web-ui/server pytest web-ui/server/tests/test_characters.py web-ui/server/tests/test_threads.py -q` - PASS, 16 backend/API tests.
- `npm --prefix web-ui/client run test:unit -- --run` - PASS, 63 unit tests across 11 files.
- Acceptance greps for required test titles, helper usage, fixture IDs, upload/delete asset IDs, stale-image assertions, and `PU` fallback - PASS.
- `git diff --check` - PASS.

## Decisions Made

- Direct upload tests now treat browser-rendered images as the contract: image URLs are fulfilled as `image/png`, visible `<img>` elements are asserted, and actual browser requests are tracked.
- Removed portrait fallback uses two initials wherever a character avatar is shown. This keeps the user-visible removal state consistent with Gallery cards and Home thread rows.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - UI Bug] Normalized removed portrait fallback initials across all required surfaces**
- **Found during:** Task 2 (Cover direct portrait upload replacement and removal)
- **Issue:** Gallery cards and Home thread rows rendered two-initial fallbacks, but Editor showed only an upload icon and Home picker/Chat header rendered one initial. The plan required `PU` after removal on every covered surface.
- **Fix:** Added an initials fallback prop to `PortraitDropzone`, passed Editor initials from the character form, and updated Home picker and Chat header fallback logic to use two initials.
- **Files modified:** `PortraitDropzone.svelte`, `characters/[id]/+page.svelte`, `+page.svelte`, `chat/[threadId]/+page.svelte`
- **Verification:** `npm --prefix web-ui/client run test:e2e -- portrait-persistence.spec.ts`, `npm --prefix web-ui/client run test:unit -- --run`
- **Committed in:** `67978d9`

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** No scope expansion. The UI fix was required for the planned removal assertions to match every required surface.

## Issues Encountered

None remaining. The RED Playwright failures were expected TDD gates before the GREEN commits.

## Known Stubs

None. The stub scan found only intentional empty `alt=""` attributes on decorative avatar images.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for the remaining Phase 01.1 browser hardening plans to build on the shared acceptance helpers and portrait persistence coverage. No changes were made to `.planning/STATE.md` or `.planning/ROADMAP.md`.

## Self-Check: PASSED

- Summary file exists at `.planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-02-SUMMARY.md`.
- Created spec exists at `web-ui/client/tests/e2e/portrait-persistence.spec.ts`.
- Task commits are present: `26b5f51`, `58fcbd9`, `8b84d33`, and `67978d9`.
- `.planning/STATE.md` and `.planning/ROADMAP.md` were not modified.

---
*Phase: 01.1-ui-acceptance-and-regression-test-hardening*
*Completed: 2026-04-24*
