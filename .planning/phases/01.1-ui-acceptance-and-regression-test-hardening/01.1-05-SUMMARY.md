---
phase: 01.1-ui-acceptance-and-regression-test-hardening
plan: "05"
subsystem: live-acceptance
tags: [playwright, live-lan, android, settings, portraits, chat-actions]

requires:
  - phase: 01.1-ui-acceptance-and-regression-test-hardening
    provides: Guarded local Phase 1 browser acceptance path from plan 01.1-04
provides:
  - Gated live LAN Playwright spec excluded from default local E2E runs
  - Deployed OMEN-PC browser verification for Settings save-before-test, portrait persistence, and full text flow
  - Android Chrome product-owner checkpoint evidence after agent-run backend, browser, live, and deployed-runtime checks
affects: [phase1-summary-gate, android-acceptance, live-lan-runtime, chat-actions]

tech-stack:
  added: []
  patterns:
    - Live specs are gated by RAYME_ENABLE_LIVE_E2E=1 and explicit live URL environment variables
    - Android checkpoints include what the agent already verified, what remains for the human, and why the human signal is required

key-files:
  created:
    - web-ui/client/tests/e2e/live-lan-functional.spec.ts
  modified:
    - web-ui/client/playwright.config.ts
    - web-ui/client/tests/e2e/helpers/images.ts
    - web-ui/client/tests/e2e/mobile-text.spec.ts
    - web-ui/client/src/lib/api/chat.ts
    - web-ui/client/src/lib/components/ChatMessageBubble.svelte
    - web-ui/client/src/lib/components/MessageActionMenu.svelte
    - web-ui/client/src/lib/components/SwipeStepper.svelte
    - web-ui/client/src/routes/chat/[threadId]/+page.svelte
    - web-ui/server/app/domain/llm_stream.py
    - web-ui/server/app/domain/prompt_builder.py
    - .planning/OPERATING-NOTES.md
    - .planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-VALIDATION.md

key-decisions:
  - "Live LAN acceptance runs only when `RAYME_ENABLE_LIVE_E2E=1` and live Web UI/AI health URLs are supplied."
  - "Regenerate-like UI labels were changed to the clearer user model: `Redo` creates another alternate; `Redo and Replace` replaces the selected AI turn."
  - "Continue avoids assistant response prefill so the local Qwen/OpenAI-compatible server with thinking enabled accepts the request."

requirements-completed: [REQ-03, REQ-10, REQ-11, REQ-12, REQ-13, REQ-14, REQ-16, REQ-17, REQ-30, REQ-31, REQ-32, REQ-33, REQ-34, REQ-35, REQ-36, REQ-60, REQ-70, REQ-71, REQ-72, REQ-90, REQ-A0, REQ-A1]

duration: 3h 15min
completed: 2026-04-24
---

# Phase 01.1 Plan 05: Live LAN And Android Acceptance Summary

**Functional live OMEN-PC browser verification and Android Chrome product-owner acceptance completed after hardening the reported message-action defects.**

## Accomplishments

- Added `web-ui/client/tests/e2e/live-lan-functional.spec.ts` with the title `live OMEN-PC Settings portraits and text flow pass before Android handoff`.
- Gated the live spec behind `RAYME_ENABLE_LIVE_E2E=1` so default `npm --prefix web-ui/client run test:e2e` remains deterministic and local.
- Verified Settings save-before-test against the live Web UI, AI backend, and local no-key LLM Settings path.
- Verified live portrait persistence across import/upload, Gallery, Home, Chat header, and reload surfaces.
- Verified the live full text flow: import/save/start chat/alternate greeting/stream/Redo and Replace/Redo/Continue/reload/continue-after-reload.
- Restored and reused the documented SSH/runtime path for `OMEN-PC`, rebuilt the client, restarted the hidden runtime, and verified both HTTPS health endpoints.
- Fixed the Android-reported message-action blockers: random per-request seed, Continue prompt incompatibility with thinking-enabled Qwen, visible touch controls, Pointer Events swipe feedback, and clearer Redo labels.

## Product-Owner Android Checkpoint

The Android Chrome product-owner checkpoint is approved.

- Approved date: 2026-04-24.
- Web UI URL: `https://192.168.1.199:8443`.
- AI backend health URL: `https://192.168.1.199:9443/health`.
- User-reported result: `it works`.
- Remaining UX clarification was resolved by renaming the actions:
  - `Redo` creates a new alternate and selects it.
  - `Redo and Replace` replaces the selected AI turn.

## Verification

- `uv run --project web-ui/server pytest web-ui/server/tests -q` - PASS during Phase 01.1 wave verification.
- `npm --prefix web-ui/client run test:unit -- --run` - PASS during Phase 01.1 wave verification.
- `npm --prefix web-ui/client run test:e2e` - PASS during Phase 01.1 wave verification.
- `uv run --project web-ui/server pytest web-ui/server/tests/test_prompt_builder.py web-ui/server/tests/test_chat_stream.py web-ui/server/tests/test_message_actions.py -q` - PASS, 24 tests.
- `uv run --project web-ui/server pytest web-ui/server/tests/test_phase1_acceptance.py -q` - PASS, 1 test.
- `npm --prefix web-ui/client run test:unit -- --run tests/unit/chat.test.ts` - PASS, 14 tests.
- `npm --prefix web-ui/client run test:e2e -- mobile-text.spec.ts --project=mobile-chromium` - PASS with touch-pointer alternate swiping.
- `npm --prefix web-ui/client run build` - PASS.
- `curl -k --fail https://192.168.1.199:8443 >/dev/null && curl -k --fail https://192.168.1.199:9443/health >/dev/null` - PASS after deployed runtime restart.
- `RAYME_ENABLE_LIVE_E2E=1 RAYME_LIVE_WEB_URL=https://192.168.1.199:8443 RAYME_LIVE_AI_HEALTH_URL=https://192.168.1.199:9443/health npm --prefix web-ui/client run test:e2e -- live-lan-functional.spec.ts --project=desktop-chromium` - PASS against deployed OMEN-PC runtime.

## Evidence Strings For Phase 1 Gate

- `Settings save-before-test`
- `portrait`
- `full text flow`
- `live-lan-functional.spec.ts`
- `RAYME_ENABLE_LIVE_E2E=1`
- `live OMEN-PC Settings portraits and text flow pass before Android handoff`
- `https://192.168.1.199:8443`
- `https://192.168.1.199:9443/health`
- `approved`

## Deployed Runtime

- Final deployed commit for this checkpoint: `6f687e9`.
- Runtime host: `OMEN-PC`.
- Remote checkout: `C:\Users\pmpg\rayme\RayMe`.
- Restart path: `.local/phase1-tls/reconfigure-hidden-runtime.ps1`.
- The runtime was rebuilt and restarted after the Redo label change, and the live LAN Playwright spec passed against the deployed UI.

## Deviations

- The first Android manual run found real defects after the desktop live guard passed. These were treated as blockers, fixed, deployed, and verified before the checkpoint was considered approved.
- The original user-facing terms `Regenerate` and `Generate alternate` were confusing. The final UI uses `Redo and Replace` and `Redo`.

## Next Phase Readiness

Plan 01.1-05 is complete. Phase 1 plan `01-24-SUMMARY.md` may now be created by plan 01.1-06 because the hardened local suites, live LAN browser verification, and Android Chrome product-owner checkpoint are complete.

