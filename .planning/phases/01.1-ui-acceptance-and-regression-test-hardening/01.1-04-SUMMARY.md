---
phase: 01.1-ui-acceptance-and-regression-test-hardening
plan: "04"
subsystem: testing
tags: [playwright, e2e, browser-errors, acceptance, regression]

requires:
  - phase: 01.1-ui-acceptance-and-regression-test-hardening
    provides: Shared Playwright browser error guard, acceptance helpers, portrait persistence coverage, and Settings connection regressions from plans 01.1-01 through 01.1-03
provides:
  - Guarded full Phase 1 browser acceptance path covering import, edit/save, alternate greeting, stream, regenerate, swipe, continue, reload, export, delete, rename, and delete-thread flows
  - Browser console/page-error guard integration across core Phase 1 import, mobile, home, stream, virtualization, HTTPS, and UI contract specs
  - Full local backend, unit, and Playwright suite evidence before live LAN and Android handoff
affects: [phase1-full-path, live-lan-smoke, android-acceptance, ui-regression]

tech-stack:
  added: []
  patterns:
    - Playwright specs install installBrowserErrorGuard(page) per test and assert expectNoBrowserErrors() after final UI assertions
    - Full-path route state asserts backend payloads while checking rendered DOM state, downloads, destructive confirmations, and reload continuity

key-files:
  created:
    - web-ui/client/tests/e2e/phase1-full-path.spec.ts
  modified:
    - web-ui/client/tests/e2e/import-chat-reload.spec.ts
    - web-ui/client/tests/e2e/mobile-text.spec.ts
    - web-ui/client/tests/e2e/home-start-chat.spec.ts
    - web-ui/client/tests/e2e/chat-stream.spec.ts
    - web-ui/client/tests/e2e/chat-virtualization.spec.ts
    - web-ui/client/tests/e2e/https-status.spec.ts
    - web-ui/client/tests/e2e/ui-contract.spec.ts

key-decisions:
  - "Used the current Home thread action labels (Actions for title, Rename, Delete) while preserving required PATCH and DELETE route assertions."
  - "Seeded stale-row visibility through initial thread hydration so the UI renders the stale flag deterministically before action testing."

patterns-established:
  - "Guarded E2E pattern: install the guard before route setup or navigation and assert it after the final UI/layout assertion."
  - "Full-path acceptance pattern: combine route payload assertions with DOM continuity checks for selected alternates, stale flags, downloads, destructive confirmations, and reload."

requirements-completed: [REQ-03, REQ-10, REQ-11, REQ-12, REQ-13, REQ-14, REQ-16, REQ-17, REQ-30, REQ-31, REQ-32, REQ-33, REQ-34, REQ-35, REQ-36, REQ-60, REQ-70, REQ-71, REQ-72, REQ-90, REQ-A0, REQ-A1]

duration: 18 min
completed: 2026-04-24
---

# Phase 01.1 Plan 04: Full Path Browser Guard Summary

**Guarded Phase 1 browser acceptance coverage with full text-chat continuity and console/page-error capture across the local E2E suite**

## Performance

- **Duration:** 18 min
- **Started:** 2026-04-24T16:38:58Z
- **Completed:** 2026-04-24T16:56:10Z
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments

- Added `phase1-full-path.spec.ts` covering import, review/edit/save, Home start chat, alternate greeting, streaming send, regenerate, swipe, continue with composer text, reload, and continue again.
- Extended that full-path spec to cover v2 JSON export, Gallery character delete, Home thread rename/delete, stale flag rendering, selected alternate IDs, lorebook warning preservation, and malicious HTML sanitization.
- Installed `installBrowserErrorGuard(page)` across the Phase 1 core, mobile, stream, virtualization, HTTPS, and UI contract E2E specs.
- Verified the full local backend, unit, and Playwright suites after guard integration.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add a guarded full Phase 1 browser acceptance spec** - `2ccd02d` (test)
2. **Task 2: Install the shared guard in core import chat mobile home and stream specs** - `1ecac46` (test)
3. **Task 3: Install the shared guard in virtualization HTTPS and UI contract specs** - `f6bff90` (test)

## Files Created/Modified

- `web-ui/client/tests/e2e/phase1-full-path.spec.ts` - New guarded full Phase 1 browser path with route payload assertions, export/download checks, destructive confirmations, selected alternate checks, stale flag visibility, and reload continuity.
- `web-ui/client/tests/e2e/import-chat-reload.spec.ts` - Installs browser error guard around the existing import/chat/action/reload path.
- `web-ui/client/tests/e2e/mobile-text.spec.ts` - Installs browser error guard around Android-like mobile text flow and layout assertions.
- `web-ui/client/tests/e2e/home-start-chat.spec.ts` - Installs browser error guard around Home/Gallery start-chat, export, and character delete coverage.
- `web-ui/client/tests/e2e/chat-stream.spec.ts` - Installs browser error guard around stream hydration and stream-error recovery tests.
- `web-ui/client/tests/e2e/chat-virtualization.spec.ts` - Installs browser error guard around desktop/mobile virtualization and scroll stability tests.
- `web-ui/client/tests/e2e/https-status.spec.ts` - Installs browser error guard around secure-context, media readiness, status, and key masking checks.
- `web-ui/client/tests/e2e/ui-contract.spec.ts` - Installs browser error guard around future-scope copy exclusion and navigation inventory checks.

## Verification

- `npm --prefix web-ui/client run test:e2e -- phase1-full-path.spec.ts` - PASS, 2 Playwright tests across desktop and mobile.
- `uv run --project web-ui/server pytest web-ui/server/tests/test_phase1_acceptance.py -q` - PASS, 1 backend acceptance test.
- Task 1 acceptance greps for title, guard, route IDs, actions, stale/alternate selectors, export, delete, rename, and thread delete literals - PASS.
- `npm --prefix web-ui/client run test:e2e -- import-chat-reload.spec.ts mobile-text.spec.ts home-start-chat.spec.ts chat-stream.spec.ts` - PASS, 10 Playwright tests.
- Task 2 guard greps for imports/usages and final `expectNoBrowserErrors()` calls - PASS.
- `npm --prefix web-ui/client run test:e2e -- chat-virtualization.spec.ts https-status.spec.ts ui-contract.spec.ts` - PASS, 10 Playwright tests.
- Task 3 guard/preserved-coverage greps for `LONG_THREAD_SIZE = 520`, `Secure context`, `Media devices available`, `Voice Lab`, and `VAD sensitivity` - PASS.
- `uv run --project web-ui/server pytest web-ui/server/tests -q` - PASS, 86 tests.
- `npm --prefix web-ui/client run test:unit -- --run` - PASS, 63 tests.
- `npm --prefix web-ui/client run test:e2e` - PASS, 28 Playwright tests.

## Decisions Made

- Used the current Home thread action controls (`Actions for ...`, `Rename`, `Delete`) because those are the actual accessible labels in `ThreadListItem`; the test still asserts the required `PATCH /api/threads/phase1-thread-rename-delete` and `DELETE /api/threads/phase1-thread-rename-delete` route behavior.
- Kept all guard integration test-only. No production UI code changes were needed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Test Harness] Hydrated stale-row state before stream action testing**
- **Found during:** Task 1 (Add a guarded full Phase 1 browser acceptance spec)
- **Issue:** The first Playwright run expected `[data-stale-after-edit="true"]` after the first streamed send, but the UI only receives the streamed `done` message from that SSE response, so the extra stale mock row was not rendered.
- **Fix:** Seeded the stale row through initial thread hydration and updated message action route mocks to find `phase1-ai-1` by ID rather than array position.
- **Files modified:** `web-ui/client/tests/e2e/phase1-full-path.spec.ts`
- **Verification:** `npm --prefix web-ui/client run test:e2e -- phase1-full-path.spec.ts` passed.
- **Committed in:** `2ccd02d`

**2. [Rule 1 - Acceptance Criteria] Added exact export route literal**
- **Found during:** Task 1 acceptance grep
- **Issue:** The export route was built from `characterId`, so the file did not contain the exact `/api/characters/phase1-full-character/export-v2` literal required by the plan grep.
- **Fix:** Added an explicit `exportV2Path` constant and reused it in the route and request wait.
- **Files modified:** `web-ui/client/tests/e2e/phase1-full-path.spec.ts`
- **Verification:** Fixed-string export route grep and targeted Playwright rerun passed.
- **Committed in:** `2ccd02d`

---

**Total deviations:** 2 auto-fixed (2 bugs)
**Impact on plan:** No scope change. Both fixes made the planned test evidence deterministic and aligned with the acceptance criteria.

## Issues Encountered

- The initial Task 1 Playwright run failed on stale-row visibility because mocked route state exceeded what the streaming UI receives from the SSE `done` event. The route state was corrected before commit.
- The Task 1 literal export-route grep failed once because the route string was variable-composed. The fixed literal now exists in the spec.

## Known Stubs

None. Stub scanning found no placeholder/TODO/FIXME patterns in the created or modified files.

## Threat Flags

None. This plan modified browser tests only and introduced no new runtime endpoints, auth paths, file access patterns, or schema trust boundaries.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for the remaining Phase 01.1 plans. The full local backend, client unit, and Playwright suites are green with browser console/page-error capture installed across the covered Phase 1 E2E specs. `.planning/STATE.md` and `.planning/ROADMAP.md` were intentionally not modified because the orchestrator owns shared tracking updates.

## Self-Check: PASSED

- Summary file exists at `.planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-04-SUMMARY.md`.
- Created spec exists at `web-ui/client/tests/e2e/phase1-full-path.spec.ts`.
- Task commits are present: `2ccd02d`, `1ecac46`, and `f6bff90`.
- `.planning/STATE.md` and `.planning/ROADMAP.md` were not modified.

---
*Phase: 01.1-ui-acceptance-and-regression-test-hardening*
*Completed: 2026-04-24*
