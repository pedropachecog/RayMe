---
phase: 01.1-ui-acceptance-and-regression-test-hardening
plan: "01"
subsystem: testing
tags: [playwright, vitest, e2e, acceptance, sveltekit]

requires:
  - phase: 01-foundations-text-chat-end-to-end
    provides: Existing SvelteKit UI, Playwright E2E suite, and Phase 1 chat/character API contracts
provides:
  - Shared Playwright browser error guard for console and page exceptions
  - Shared JSON/SSE route helpers for deterministic browser acceptance specs
  - Phase 01.1 character, thread, message, and local LLM fixture builders
  - Deterministic PNG portrait bytes and route fulfillment helpers
affects: [portrait-persistence, settings-connection, phase1-full-path, live-lan-smoke]

tech-stack:
  added: []
  patterns:
    - Shared E2E helpers under web-ui/client/tests/e2e/helpers
    - Helper behavior covered by focused Vitest tests before reuse in Playwright specs

key-files:
  created:
    - web-ui/client/tests/e2e/helpers/acceptance.ts
    - web-ui/client/tests/e2e/helpers/fixtures.ts
    - web-ui/client/tests/e2e/helpers/images.ts
    - web-ui/client/tests/unit/e2e-helpers.test.ts
  modified: []

key-decisions:
  - "Kept local LLM URL and model values as test fixture constants only, not production code."
  - "Mocked Playwright expect in Vitest helper tests so unit verification does not load Playwright runtime behavior."

patterns-established:
  - "Browser guard pattern: installBrowserErrorGuard(page) captures console.error and pageerror, then asserts no recorded browser failures."
  - "Route fixture pattern: fulfillJson, fulfillSse, and fulfillPortraitImage centralize response headers and body formatting."

requirements-completed: [REQ-03, REQ-10, REQ-11, REQ-12, REQ-13, REQ-14, REQ-16, REQ-17, REQ-30, REQ-31, REQ-32, REQ-33, REQ-34, REQ-35, REQ-36, REQ-60, REQ-70, REQ-71, REQ-72, REQ-90, REQ-A0, REQ-A1]

duration: 17 min
completed: 2026-04-24
---

# Phase 01.1 Plan 01: Shared Acceptance Foundation Summary

**Reusable Playwright acceptance helpers with browser error capture, RayMe API request guards, deterministic fixtures, and PNG portrait route support**

## Performance

- **Duration:** 17 min
- **Started:** 2026-04-24T15:08:55Z
- **Completed:** 2026-04-24T15:25:50Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- Added `acceptance.ts` with shared console/page-error capture, JSON/SSE route helpers, and browser direct-provider/key-leak rejection.
- Added `fixtures.ts` with Phase 01.1 character, thread, AI/user message, local LLM URL, and local LLM model fixtures.
- Added `images.ts` with deterministic PNG buffers and an exact Playwright route helper for portrait image rendering tests.
- Added focused helper tests covering the exported helper behavior before future E2E specs consume them.

## Task Commits

Each task used RED/GREEN commits because the plan marked the tasks `tdd="true"`:

1. **Task 1: Add shared browser error and route helpers**
   - `e6c01ff` test: add failing test for acceptance helpers
   - `2a54cf6` feat: add shared acceptance helpers
2. **Task 2: Add shared Phase 01.1 fixture builders**
   - `12f93dc` test: add failing test for fixture builders
   - `96dd6bf` feat: add Phase 01.1 fixture builders
3. **Task 3: Add shared portrait image helpers**
   - `4ff0fb4` test: add failing test for portrait image helpers
   - `9e47f0d` feat: add portrait image helpers

**Auto-fix:** `c0e2d88` fix: isolate helper tests from Playwright runtime

## Files Created/Modified

- `web-ui/client/tests/e2e/helpers/acceptance.ts` - Shared browser error guard, JSON/SSE route fulfillment, and RayMe API request safety helper.
- `web-ui/client/tests/e2e/helpers/fixtures.ts` - Character, thread, AI message, user message, and local LLM fixture builders.
- `web-ui/client/tests/e2e/helpers/images.ts` - Deterministic PNG buffers and portrait image route fulfillment helper.
- `web-ui/client/tests/unit/e2e-helpers.test.ts` - Focused tests for all helper behavior.

## Verification

- `npm --prefix web-ui/client run test:e2e -- --list` - PASS, listed 20 tests in 7 files.
- `npm --prefix web-ui/client run test:unit -- --run` - PASS, 63 tests across 11 files.
- `rg "installBrowserErrorGuard|fulfillJson|fulfillSse|expectRayMeApiRequest|makeCharacter|makeThreadDetail|fulfillPortraitImage" web-ui/client/tests/e2e/helpers` - PASS, all required exports found.

## Decisions Made

- Local LLM endpoint values remain test fixture constants, preserving the product rule that production code does not hardcode the LAN LLM URL.
- Vitest mocks Playwright's `expect` only inside helper tests, allowing `acceptance.ts` to keep the required Playwright import while unit verification stays deterministic.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Test Harness] Isolated helper unit tests from Playwright runtime**
- **Found during:** Task 3 verification
- **Issue:** Importing the Playwright runtime into Vitest helper tests printed a noisy `127.0.0.1:3000` connection error even though tests exited 0.
- **Fix:** Mocked Playwright's `expect` in `e2e-helpers.test.ts` and dynamically imported `acceptance.ts` after the mock.
- **Files modified:** `web-ui/client/tests/unit/e2e-helpers.test.ts`
- **Verification:** `npm --prefix web-ui/client run test:unit -- --run` passed cleanly.
- **Committed in:** `c0e2d88`

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** No scope change. The auto-fix keeps the added helper tests reliable for future verification.

## Issues Encountered

None remaining.

## Known Stubs

None. Empty arrays and null values found during stub scanning are deterministic test fixture defaults, not UI-rendered placeholders.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for Plan 01.1-02 to build portrait persistence E2E specs on top of the shared helper modules.

## Self-Check: PASSED

- Created helper files exist: `acceptance.ts`, `fixtures.ts`, `images.ts`, and `e2e-helpers.test.ts`.
- Summary file exists at `.planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-01-SUMMARY.md`.
- All task commits are present in `git log --all --grep "01.1-01"`.
- `.planning/STATE.md` and `.planning/ROADMAP.md` were not modified.

---
*Phase: 01.1-ui-acceptance-and-regression-test-hardening*
*Completed: 2026-04-24*
