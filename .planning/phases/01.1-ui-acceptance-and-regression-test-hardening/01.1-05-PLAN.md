---
phase: 01.1-ui-acceptance-and-regression-test-hardening
plan: "05"
type: execute
wave: 3
depends_on: ["04"]
files_modified:
  - web-ui/client/tests/e2e/live-lan-smoke.spec.ts
  - .planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-VALIDATION.md
autonomous: false
requirements: [REQ-03, REQ-10, REQ-11, REQ-13, REQ-30, REQ-31, REQ-32, REQ-34, REQ-35, REQ-70, REQ-72, REQ-90, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "The agent runs local backend/unit/E2E checks and live `OMEN-PC` browser smoke before asking for Android product-owner acceptance."
    - "Live browser smoke uses `https://192.168.1.199:8443` and checks rendered Settings content with console/page-error capture."
    - "The Android handoff states exactly what the agent already verified and asks only for the remaining physical-device signal."
  artifacts:
    - path: "web-ui/client/tests/e2e/live-lan-smoke.spec.ts"
      provides: "Live OMEN-PC browser smoke for Settings readiness"
      contains: "RAYME_LIVE_WEB_URL"
    - path: ".planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-VALIDATION.md"
      provides: "Execution-updated validation status for live LAN and Android checkpoint"
  key_links:
    - from: "web-ui/client/tests/e2e/live-lan-smoke.spec.ts"
      to: "https://192.168.1.199:8443/settings"
      via: "absolute live URL navigation"
      pattern: "192\\.168\\.1\\.199:8443"
    - from: ".planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-VALIDATION.md"
      to: "Android Chrome checkpoint"
      via: "manual-only verification row status"
      pattern: "Android Chrome product-owner checkpoint"
---

<objective>
Run the live LAN browser gate and Android product-owner checkpoint.

Purpose: Ensure local deterministic tests are not the final signal for LAN/HTTPS behavior, and preserve the rule that manual Android testing happens only after backend/API/browser/live verification passes.
Output: A live-LAN Playwright smoke spec plus an updated Phase 01.1 validation record after product-owner approval.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/OPERATING-NOTES.md
@.planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-CONTEXT.md
@.planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-RESEARCH.md
@.planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-UI-SPEC.md
@.planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-VALIDATION.md
@web-ui/server/docs/HTTPS-LAN.md
@web-ui/server/docs/PHASE1-ACCEPTANCE.md
@web-ui/client/tests/e2e/helpers/acceptance.ts

<interfaces>
Live environment values from context:
```text
Web UI: https://192.168.1.199:8443
AI backend health: https://192.168.1.199:9443/health
Local LLM Settings value: http://192.168.1.190:8001/v1
LLM model: unsloth/Qwen3.5-27B
Required handoff stem: Agent verification completed: backend/API checks, Playwright browser checks with console and page-error capture, and live LAN browser verification have passed. Please confirm the remaining Android Chrome product-owner signal at https://192.168.1.199:8443.
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add and run live OMEN-PC browser smoke</name>
  <read_first>
    - `web-ui/client/tests/e2e/live-lan-smoke.spec.ts` (new file).
    - `web-ui/client/tests/e2e/helpers/acceptance.ts` for browser guard.
    - `web-ui/client/tests/e2e/https-status.spec.ts` for existing secure/media readiness assertions.
    - `.planning/OPERATING-NOTES.md` Backend Host And TLS and Runbook Execution.
    - `.planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-CONTEXT.md` Current Live Environment and Non-Negotiable Operating Rule.
    - `.planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-UI-SPEC.md` Live LAN And Android Checkpoint.
    - `web-ui/server/docs/HTTPS-LAN.md` for scheduled task/runtime URL details.
  </read_first>
  <files>web-ui/client/tests/e2e/live-lan-smoke.spec.ts</files>
  <behavior>
    - Live smoke defaults to `RAYME_LIVE_WEB_URL || 'https://192.168.1.199:8443'`.
    - Live smoke defaults to `RAYME_LIVE_AI_HEALTH_URL || 'https://192.168.1.199:9443/health'`.
    - The test uses `test.use({ ignoreHTTPSErrors: true })` so browser rendering can be checked even when the local automation trust store lacks the Android root CA.
    - The test navigates to `${liveWebUrl}/settings` and asserts heading `Endpoint Settings`, text `HTTPS secure-context status`, and status test IDs `web-ui-status`, `ai-backend-status`, and `llm-status` are attached.
    - The guard reports no console/page errors.
  </behavior>
  <action>Create `web-ui/client/tests/e2e/live-lan-smoke.spec.ts` with a test named exactly `live OMEN-PC Settings page is browser ready before Android handoff`. Import `expect` and `test` from `@playwright/test` and `installBrowserErrorGuard` from `./helpers/acceptance`. Define constants `LIVE_WEB_URL = process.env.RAYME_LIVE_WEB_URL ?? 'https://192.168.1.199:8443'` and `LIVE_AI_HEALTH_URL = process.env.RAYME_LIVE_AI_HEALTH_URL ?? 'https://192.168.1.199:9443/health'`. Use `test.use({ ignoreHTTPSErrors: true })`. In the test, install the guard, navigate to `${LIVE_WEB_URL.replace(/\\/$/, '')}/settings`, assert heading `Endpoint Settings`, text `HTTPS secure-context status`, and locators `[data-testid="web-ui-status"]`, `[data-testid="ai-backend-status"]`, and `[data-testid="llm-status"]` are attached. Also navigate a separate page or use `page.goto(LIVE_AI_HEALTH_URL)` only if it can return browser-renderable JSON in this environment; otherwise rely on the automated `curl -k --fail` command in verify for AI health and keep the Playwright spec focused on Web UI rendering. Do not add product code, do not create TLS certs, and do not hardcode the local LLM URL in production files.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests -q</automated>
    <automated>npm --prefix web-ui/client run test:unit -- --run</automated>
    <automated>npm --prefix web-ui/client run test:e2e</automated>
    <automated>curl -k --fail https://192.168.1.199:8443 >/dev/null && curl -k --fail https://192.168.1.199:9443/health >/dev/null</automated>
    <automated>RAYME_LIVE_WEB_URL=https://192.168.1.199:8443 RAYME_LIVE_AI_HEALTH_URL=https://192.168.1.199:9443/health npm --prefix web-ui/client run test:e2e -- live-lan-smoke.spec.ts --project=desktop-chromium</automated>
  </verify>
  <acceptance_criteria>
    - `rg "live OMEN-PC Settings page is browser ready before Android handoff|RAYME_LIVE_WEB_URL|RAYME_LIVE_AI_HEALTH_URL|192\\.168\\.1\\.199:8443|192\\.168\\.1\\.199:9443/health|ignoreHTTPSErrors|installBrowserErrorGuard|Endpoint Settings|HTTPS secure-context status" web-ui/client/tests/e2e/live-lan-smoke.spec.ts` returns the live smoke contract.
    - `uv run --project web-ui/server pytest web-ui/server/tests -q` exits 0.
    - `npm --prefix web-ui/client run test:unit -- --run` exits 0.
    - `npm --prefix web-ui/client run test:e2e` exits 0.
    - `curl -k --fail https://192.168.1.199:8443 >/dev/null && curl -k --fail https://192.168.1.199:9443/health >/dev/null` exits 0.
    - The live Playwright smoke command with `RAYME_LIVE_WEB_URL=https://192.168.1.199:8443` exits 0 before the Android checkpoint is requested.
  </acceptance_criteria>
  <done>Local suites and live OMEN-PC browser smoke have passed before the product-owner Android checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Product-owner Android Chrome acceptance after agent verification</name>
  <read_first>
    - `.planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-VALIDATION.md`.
    - `.planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-UI-SPEC.md` Agent-To-User Handoff Contract.
    - `.planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-CONTEXT.md` Non-Negotiable Operating Rule.
    - `.planning/OPERATING-NOTES.md` Backend Host And TLS and Android trust setup.
    - `web-ui/server/docs/HTTPS-LAN.md`.
    - `web-ui/server/docs/PHASE1-ACCEPTANCE.md`.
  </read_first>
  <files>.planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-VALIDATION.md</files>
  <action>Only after Task 1 automated commands pass, request the product-owner Android Chrome signal using this exact stem: `Agent verification completed: backend/API checks, Playwright browser checks with console and page-error capture, and live LAN browser verification have passed. Please confirm the remaining Android Chrome product-owner signal at https://192.168.1.199:8443.` Then ask the user to verify exactly these items on Android Chrome: open `https://192.168.1.199:9443/health` and confirm health JSON with no certificate warning; open `https://192.168.1.199:8443` and confirm no certificate warning; run Settings Test Connection for Web UI, AI backend, and local LLM configured as `http://192.168.1.190:8001/v1` with model `unsloth/Qwen3.5-27B` and blank API key if the local no-key LLM is active; confirm imported PNG portrait remains visible after reload in Gallery, Home, and Chat; confirm full text flow import/save/start-chat/alternate-greeting/stream/regenerate/swipe/continue/reload/continue is usable. After approval, update `01.1-VALIDATION.md` Manual-Only Verifications row for `Android Chrome product-owner checkpoint` to status `green` with the date `2026-04-24`, Web UI URL `https://192.168.1.199:8443`, and AI backend URL `https://192.168.1.199:9443/health`. If the user reports an issue, leave the row pending or red and record the exact blocker; do not create Phase 1 `01-24-SUMMARY.md`.</action>
  <verify>
    <automated>RAYME_LIVE_WEB_URL=https://192.168.1.199:8443 RAYME_LIVE_AI_HEALTH_URL=https://192.168.1.199:9443/health npm --prefix web-ui/client run test:e2e -- live-lan-smoke.spec.ts --project=desktop-chromium</automated>
  </verify>
  <how-to-verify>
    1. Android Chrome opens `https://192.168.1.199:9443/health` with no certificate warning and visible health JSON.
    2. Android Chrome opens `https://192.168.1.199:8443` with no certificate warning.
    3. Settings Test Connection shows `Connected` for Web UI and AI backend.
    4. Settings Test Connection shows `Connected` for the configured LLM path, including blank API key for local no-key LLM when active.
    5. PNG-imported portrait is visible after reload in Gallery, Home, and Chat.
    6. Full text path import/save/start chat/alternate greeting/stream/regenerate/swipe/continue/reload/continue is usable.
  </how-to-verify>
  <resume-signal>Type `approved` after the Android Chrome product-owner signal passes, or describe the issue.</resume-signal>
  <acceptance_criteria>
    - The Android handoff message contains the exact required stem from the UI-SPEC.
    - The checkpoint is requested only after backend pytest, client unit, full Playwright, curl live endpoints, and live Playwright smoke pass.
    - If approved, `rg "Android Chrome product-owner checkpoint.*green|2026-04-24|https://192\\.168\\.1\\.199:8443|https://192\\.168\\.1\\.199:9443/health" .planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-VALIDATION.md` returns the updated validation evidence.
    - If not approved, `01.1-VALIDATION.md` records the blocker and no `.planning/phases/01-foundations-text-chat-end-to-end/01-24-SUMMARY.md` file exists.
  </acceptance_criteria>
  <done>Android Chrome product-owner acceptance is approved and recorded, or a concrete blocker is recorded with Phase 1 summary still blocked.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local source tests -> deployed OMEN-PC runtime | Local green tests can be invalid if the LAN runtime is stale or broken. |
| Playwright automation -> Android physical device | Browser automation cannot prove physical Android trust-store and touch behavior. |
| Agent handoff -> product-owner testing | Manual testing must not be first-line QA or vague debugging work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-05-01 | Spoofing | Live LAN runtime | mitigate | Live smoke uses absolute `https://192.168.1.199:8443` and AI health `https://192.168.1.199:9443/health`, not localhost. |
| T-01.1-05-02 | Denial of Service | Browser readiness | mitigate | Live Playwright smoke asserts rendered Settings content with console/page-error guard before Android handoff. |
| T-01.1-05-03 | Repudiation | Product-owner checkpoint | mitigate | Validation file records green approval or exact blocker with concrete URLs and date. |
</threat_model>

<verification>
Run:
`uv run --project web-ui/server pytest web-ui/server/tests -q`
`npm --prefix web-ui/client run test:unit -- --run`
`npm --prefix web-ui/client run test:e2e`
`curl -k --fail https://192.168.1.199:8443 >/dev/null && curl -k --fail https://192.168.1.199:9443/health >/dev/null`
`RAYME_LIVE_WEB_URL=https://192.168.1.199:8443 RAYME_LIVE_AI_HEALTH_URL=https://192.168.1.199:9443/health npm --prefix web-ui/client run test:e2e -- live-lan-smoke.spec.ts --project=desktop-chromium`
Then complete the blocking Android Chrome checkpoint.
</verification>

<success_criteria>
Live acceptance is complete when local suites pass, live LAN browser smoke passes, and Android Chrome product-owner acceptance is either approved and recorded green or blocked with exact evidence while Phase 1 summary remains absent.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-05-SUMMARY.md`.
</output>
