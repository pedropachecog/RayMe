---
phase: 01.1-ui-acceptance-and-regression-test-hardening
plan: "03"
subsystem: testing
tags: [playwright, e2e, settings, llm, regression]

requires:
  - phase: 01.1-ui-acceptance-and-regression-test-hardening
    provides: Shared Playwright acceptance helpers and local LLM fixture constants from plan 01.1-01
provides:
  - Browser regression coverage proving Settings Test Connection saves current form values before endpoint probes
  - Browser regression coverage proving a blank-key local OpenAI-compatible LLM path remains RayMe-proxied and key-safe
  - Source-scope guard evidence that local LLM endpoint/model strings stay out of production code
affects: [settings-connection, phase1-full-path, live-lan-smoke, android-acceptance]

tech-stack:
  added: []
  patterns:
    - Settings E2E records PATCH/POST events to prove save-before-test ordering
    - Browser request URL capture pairs with expectRayMeApiRequest to prove provider access stays behind RayMe /api routes

key-files:
  created:
    - web-ui/client/tests/e2e/settings-connection.spec.ts
  modified:
    - web-ui/client/tests/e2e/helpers/acceptance.ts
    - web-ui/client/tests/unit/e2e-helpers.test.ts

key-decisions:
  - "Kept local LLM endpoint/model values as test fixture/spec data only; production source remains free of the LAN endpoint and model strings."
  - "Preserved Settings connection tests as route-mocked browser regressions so request order, payload shape, browser requests, and visible status copy are deterministic."

patterns-established:
  - "Settings connection regression pattern: record PATCH /api/settings and POST /api/settings/test/* events in one ordered array and assert the immediately preceding PATCH payload."
  - "No-key LLM secrecy pattern: assert omitted llm_api_key payload property, no direct provider browser requests, and no key-like visible text."

requirements-completed: [REQ-03, REQ-90, REQ-A0, REQ-A1]

duration: 20 min
completed: 2026-04-24
---

# Phase 01.1 Plan 03: Settings Connection Regression Summary

**Playwright Settings coverage for save-before-test ordering, local no-key LLM configuration, browser provider isolation, and API-key secrecy**

## Performance

- **Duration:** 20 min
- **Started:** 2026-04-24T15:56:43Z
- **Completed:** 2026-04-24T16:16:38Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Added `settings-connection.spec.ts` with browser coverage for Web UI, AI backend, and LLM Test Connection buttons.
- Asserted every Test Connection click sends `PATCH /api/settings` with current form values immediately before its endpoint-specific `POST`.
- Covered the LAN local LLM URL and model as Settings-entered test data with blank API key, `Connected` status, no direct browser request to the provider, and no visible raw-key leaks.
- Adjusted the shared request guard and helper unit test so the source-scope hardcoding check lists only fixture/spec files.

## Task Commits

Each task was committed atomically:

1. **Task 1: Prove save-before-test request order for all Settings endpoints** - `126e0d3` (test)
2. **Task 2: Prove local no-key LLM path does not leak keys or hardcode endpoints** - `dc74650` (test)

## Files Created/Modified

- `web-ui/client/tests/e2e/settings-connection.spec.ts` - New Playwright spec for Settings save-before-test order, local no-key LLM payload, status, request, and leak assertions.
- `web-ui/client/tests/e2e/helpers/acceptance.ts` - Keeps direct local LLM provider detection without storing the full LAN endpoint literal.
- `web-ui/client/tests/unit/e2e-helpers.test.ts` - Keeps helper coverage while avoiding full local endpoint/model literals outside fixture/spec scope.

## Verification

- `npm --prefix web-ui/client run test:e2e -- settings-connection.spec.ts` - PASS, 2 Playwright tests across desktop and mobile projects.
- `uv run --project web-ui/server pytest web-ui/server/tests/test_health_settings.py -q` - PASS, 8 backend Settings tests.
- `npm --prefix web-ui/client run test:unit -- --run` - PASS, 63 unit tests across 11 files.
- `rg -l "192\\.168\\.1\\.190:8001|unsloth/Qwen3\\.5-27B" web-ui/client/src web-ui/server/app web-ui/client/tests/e2e web-ui/client/tests/unit web-ui/server/tests | sort` - PASS, lists only `web-ui/client/tests/e2e/helpers/fixtures.ts` and `web-ui/client/tests/e2e/settings-connection.spec.ts`.
- Acceptance greps for the exact test title, PATCH/POST/status assertions, endpoint values, no-key assertion, and key-leak strings - PASS.
- `git diff --check` - PASS.

## Decisions Made

- The Settings E2E test uses route mocks rather than a live backend so request ordering and payload inspection remain deterministic.
- The local LLM URL and model remain fixture/spec data for acceptance only. Production client/server source does not contain the LAN endpoint or model literal.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Verification Blocker] Removed full local LLM literals from shared helper test scope**
- **Found during:** Task 2 (Prove local no-key LLM path does not leak keys or hardcode endpoints)
- **Issue:** The plan's source-scope guard expected the full local endpoint/model strings to appear only in `fixtures.ts` and `settings-connection.spec.ts`, but the prior shared acceptance helper and its unit test also contained the full endpoint literal.
- **Fix:** Reworked `expectRayMeApiRequest` to detect the known local provider by parsed hostname/port/path and rewrote the unit assertions to derive expected fixture values without embedding the full literals.
- **Files modified:** `web-ui/client/tests/e2e/helpers/acceptance.ts`, `web-ui/client/tests/unit/e2e-helpers.test.ts`
- **Verification:** `npm --prefix web-ui/client run test:unit -- --run`; source-scope guard command passed exactly.
- **Committed in:** `dc74650`

---

**Total deviations:** 1 auto-fixed (1 Rule 3 blocker)
**Impact on plan:** No product scope change. The helper cleanup was required for the planned hardcoded-endpoint guard to pass while preserving browser direct-provider rejection.

## Issues Encountered

- The first RED Playwright run failed on an ambiguous API-key label selector because the reveal button also had `API key` in its accessible name. The selector was tightened to the textbox before committing the test.
- After the selector fix, the save-before-test behavior already passed because the Settings page already contained the prior bug fix from Phase 01.1 context. This plan therefore shipped durable regression coverage rather than production Settings changes.

## Known Stubs

None. Empty arrays found during scan are request/event collection test state, not UI-rendered placeholders. The local LLM endpoint/model literals are intentional acceptance fixtures.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for the remaining Phase 01.1 browser hardening plans to build on Settings save-before-test and local no-key LLM regression coverage. `.planning/STATE.md` and `.planning/ROADMAP.md` were intentionally not modified because the orchestrator owns shared tracking updates.

## Self-Check: PASSED

- Created spec exists at `web-ui/client/tests/e2e/settings-connection.spec.ts`.
- Summary file exists at `.planning/phases/01.1-ui-acceptance-and-regression-test-hardening/01.1-03-SUMMARY.md`.
- Task commits are present: `126e0d3` and `dc74650`.
- `.planning/STATE.md` and `.planning/ROADMAP.md` were not modified.

---
*Phase: 01.1-ui-acceptance-and-regression-test-hardening*
*Completed: 2026-04-24*
