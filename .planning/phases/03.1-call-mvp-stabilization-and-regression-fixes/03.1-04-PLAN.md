---
phase: 03.1-call-mvp-stabilization-and-regression-fixes
plan: 04
type: execute
wave: 1
depends_on:
  - 03.1-01
files_modified:
  - web-ui/client/src/lib/api/calls.ts
  - web-ui/client/src/lib/call/audio.ts
  - web-ui/client/src/lib/call/reconnectBackfill.ts
  - web-ui/client/src/lib/call/store.svelte.ts
  - web-ui/client/tests/unit/call-audio.test.ts
  - web-ui/client/tests/unit/call-state.test.ts
  - web-ui/client/tests/unit/reconnect-backfill.test.ts
autonomous: true
requirements:
  - REQ-40
  - REQ-47
  - REQ-48
  - REQ-50
  - REQ-A0
  - REQ-A1
must_haves:
  truths:
    - "Client call helpers keep browser call APIs same-origin and typed."
    - "Microphone startup uses Android-safe browser media constraints and gesture unlock."
    - "Reconnect backfill remains bounded and contiguous for recovery attempts, including the missing-chunks delayed-tail debug case."
    - "Client call state keeps server mute independent from local microphone track state."
  artifacts:
    - path: "web-ui/client/src/lib/api/calls.ts"
      provides: "Typed same-origin call API wrappers"
    - path: "web-ui/client/src/lib/call/audio.ts"
      provides: "AudioContext unlock and microphone capture helpers"
    - path: "web-ui/client/src/lib/call/reconnectBackfill.ts"
      provides: "Bounded local mic PCM backfill selection"
    - path: "web-ui/client/src/lib/call/store.svelte.ts"
      provides: "Client call state helper"
    - path: "web-ui/client/tests/unit/reconnect-backfill.test.ts"
      provides: "Reconnect helper regression coverage"
  key_links:
    - from: "web-ui/client/src/lib/api/calls.ts"
      to: "web-ui/server/app/api/calls.py"
      via: "same-origin `/calls/...` wrapper paths"
      pattern: "reconnect-audio|events/recover"
    - from: "web-ui/client/src/lib/call/audio.ts"
      to: "browser microphone permission"
      via: "navigator.mediaDevices.getUserMedia"
      pattern: "getUserMedia"
    - from: "web-ui/client/src/lib/call/reconnectBackfill.ts"
      to: "web-ui/client/src/routes/call/[threadId]/+page.svelte"
      via: "selectReconnectAudioBackfill contract consumed by browser route"
      pattern: "maxDurationMs"
---

<objective>
Stabilize client call helper contracts and unit coverage before route/browser work consumes them.

Purpose: isolate helper-level fixes for mic permission, AudioContext unlock, same-origin call wrappers, bounded reconnect backfill, mute state, and interrupt cleanup so browser route work can build on stable contracts.
Output: client helper fixes plus focused Vitest coverage.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/OPERATING-NOTES.md
@.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-CONTEXT.md
@.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-RESEARCH.md
@.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-PATTERNS.md
@.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/results/03.1-defect-triage.json
@.planning/debug/phone-calls-missing-chunks.md
@.planning/debug/phone-call-repro-dff6545-2026-04-30.md
@web-ui/client/src/lib/api/calls.ts
@web-ui/client/src/lib/call/audio.ts
@web-ui/client/src/lib/call/reconnectBackfill.ts
@web-ui/client/src/lib/call/store.svelte.ts
@web-ui/client/tests/unit/call-audio.test.ts
@web-ui/client/tests/unit/call-state.test.ts
@web-ui/client/tests/unit/reconnect-backfill.test.ts

<interfaces>
Existing client helper contracts:
```ts
export function backfillCallReconnectAudio(callId: string, payload: CallReconnectAudioBackfillRequest)
export function recoverCallEvents(callId: string, sessionId: string)
export async function unlockCallAudioContext(providedAudioContext?: UnlockableAudioContext)
export async function requestCallMicrophone(): Promise<MediaStream>
export function selectReconnectAudioBackfill(chunks: LocalMicPcmChunk[], options: LocalMicPcmSelectionOptions)
export function createCallStore(options?: CallStoreOptions): CallStore
```

Required helper constants:
```ts
const MIC_BACKFILL_MAX_MS = 30000;
const MIC_BACKFILL_BATCH_MAX_MS = 10000;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add client helper RED regressions</name>
  <files>web-ui/client/tests/unit/call-audio.test.ts, web-ui/client/tests/unit/call-state.test.ts, web-ui/client/tests/unit/reconnect-backfill.test.ts</files>
  <read_first>
    web-ui/client/tests/unit/call-audio.test.ts
    web-ui/client/tests/unit/call-state.test.ts
    web-ui/client/tests/unit/reconnect-backfill.test.ts
    web-ui/client/src/lib/api/calls.ts
    web-ui/client/src/lib/call/audio.ts
    web-ui/client/src/lib/call/reconnectBackfill.ts
    web-ui/client/src/lib/call/store.svelte.ts
    .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/results/03.1-defect-triage.json
    .planning/debug/phone-calls-missing-chunks.md
    .planning/debug/phone-call-repro-dff6545-2026-04-30.md
  </read_first>
  <behavior>
    - `requestCallMicrophone()` calls `navigator.mediaDevices.getUserMedia({ audio: { echoCancellation: true, noiseSuppression: true, autoGainControl: true } })`.
    - `unlockCallAudioContext()` resumes the context and plays a one-sample silent buffer from the user gesture path.
    - `backfillCallReconnectAudio(callId, payload)` posts to `/calls/${encodeURIComponent(callId)}/reconnect-audio`.
    - `recoverCallEvents(callId, sessionId)` posts to `/calls/${encodeURIComponent(callId)}/events/recover`.
    - `selectReconnectAudioBackfill` caps initial reconnect selection to 30000 ms and delayed tail selection stays contiguous with the previous end.
    - The missing-chunks regression from `.planning/debug/phone-calls-missing-chunks.md` is covered: if previous backfill ended at `35256ms`, delayed tail selection must resume at `35256ms`, not skip to `69467ms`.
    - `createCallStore` keeps server muted state independent from local microphone track enabled state and button interrupt aborts active generation/playback before returning to listening.
  </behavior>
  <action>Add or strengthen Vitest coverage for the behavior block using existing helper test style. Keep these tests helper-scoped; do not add browser route or Playwright assertions in this plan. Ensure the tests fail if wrapper paths become direct AI backend/provider URLs, if microphone constraints drop Android-safe echo/noise/auto-gain controls, if reconnect selection exceeds the bounded window, if delayed tail selection skips over previously unsent PCM, or if server mute is conflated with local track enabled state. Add an explicit regression based on `.planning/debug/phone-calls-missing-chunks.md`: chunks covering `5226-35256ms`, later chunks through `99412ms`, previous end `35256`, expected next selection `startMs === 35256`, and forbidden skipped start `69467`. Do not add VAD barge-in, echo-loop mitigation, TTFA optimization, advanced caption behavior, or visualizer polish expectations per D-21 through D-24.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run tests/unit/call-audio.test.ts tests/unit/call-state.test.ts tests/unit/reconnect-backfill.test.ts</automated>
    <automated>rg -n "getUserMedia|echoCancellation|noiseSuppression|autoGainControl|reconnect-audio|events/recover|maxDurationMs|serverMuted|30000|35256|69467" web-ui/client/tests/unit</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "getUserMedia|echoCancellation|noiseSuppression|autoGainControl|reconnect-audio|events/recover|maxDurationMs|serverMuted|30000|35256|69467" web-ui/client/tests/unit` returns matches for every term.
    The client unit command exits 0 after production fixes.
    No Playwright/browser route test files are modified by this plan.
  </acceptance_criteria>
  <done>Client unit tests lock the helper contracts that browser route stabilization depends on.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stabilize client call helpers and typed API wrappers</name>
  <files>web-ui/client/src/lib/api/calls.ts, web-ui/client/src/lib/call/audio.ts, web-ui/client/src/lib/call/reconnectBackfill.ts, web-ui/client/src/lib/call/store.svelte.ts</files>
  <read_first>
    web-ui/client/src/lib/api/calls.ts
    web-ui/client/src/lib/call/audio.ts
    web-ui/client/src/lib/call/reconnectBackfill.ts
    web-ui/client/src/lib/call/store.svelte.ts
    web-ui/client/tests/unit/call-audio.test.ts
    web-ui/client/tests/unit/call-state.test.ts
    web-ui/client/tests/unit/reconnect-backfill.test.ts
  </read_first>
  <action>Apply helper-level fixes required by failing tests. Keep `backfillCallReconnectAudio(callId, payload)` posting to `/calls/${encodeURIComponent(callId)}/reconnect-audio` and `recoverCallEvents(callId, sessionId)` posting to `/calls/${encodeURIComponent(callId)}/events/recover`; do not add provider URLs or direct AI backend URLs. Keep `unlockCallAudioContext` resuming the context and playing a one-sample silent buffer. Keep `requestCallMicrophone()` calling `navigator.mediaDevices.getUserMedia({ audio: { echoCancellation: true, noiseSuppression: true, autoGainControl: true } })`. Keep `selectReconnectAudioBackfill` bounded by `maxDurationMs` and contiguous when `startMs` is provided; for the active missing-chunks case, previous end `35256ms` must select from `35256ms` even when the current latest-window start would be `69467ms`. Keep `createCallStore` server muted state independent from local microphone track enabled state, and ensure `interrupt({ source: "button" })` aborts active generation/playback before returning to listening.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run tests/unit/call-audio.test.ts tests/unit/call-state.test.ts tests/unit/reconnect-backfill.test.ts</automated>
    <automated>rg -n "getUserMedia|echoCancellation|noiseSuppression|autoGainControl|reconnect-audio|events/recover|maxDurationMs|serverMuted" web-ui/client/src/lib</automated>
  </verify>
  <acceptance_criteria>
    `web-ui/client/src/lib/api/calls.ts` contains `/reconnect-audio` and `/events/recover`.
    `web-ui/client/src/lib/call/audio.ts` contains `echoCancellation: true`, `noiseSuppression: true`, and `autoGainControl: true`.
    `web-ui/client/src/lib/call/reconnectBackfill.ts` contains `maxDurationMs`.
    Client unit command exits 0.
  </acceptance_criteria>
  <done>Client helpers preserve same-origin call APIs, Android-safe audio unlock, bounded backfill selection, and split server/local mute state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser media APIs -> helper layer | Microphone permission and audio context behavior enter RayMe through browser APIs. |
| Client helper layer -> Web UI `/api/calls` | Typed wrappers send control and reconnect payloads to same-origin routes. |
| Local mic PCM buffer -> reconnect backfill payload | Captured PCM chunks are selected and serialized for recovery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-04-01 | Spoofing | microphone permission/startup helpers | mitigate | Keep `getUserMedia()` as the only mic capture path and preserve Android-safe audio constraints. |
| T-03.1-04-02 | Tampering | call API wrappers | mitigate | Preserve `/calls/...` wrapper paths through `apiFetch`; tests reject direct AI backend/provider URLs. |
| T-03.1-04-03 | Denial of Service | reconnect backfill selection | mitigate | Keep selection bounded by `maxDurationMs`, with route-level batching handled by the browser plan. |
| T-03.1-04-04 | Repudiation | server/local mute state | mitigate | Unit tests keep server mute state distinct from local track enabled state. |
| T-03.1-04-05 | Information Disclosure | helper diagnostics/errors | mitigate | This plan does not add committed raw logs, secrets, tracebacks, or model-cache details. |
</threat_model>

<verification>
Run:
- `npm --prefix web-ui/client run test:unit -- --run tests/unit/call-audio.test.ts tests/unit/call-state.test.ts tests/unit/reconnect-backfill.test.ts`
- `rg -n "getUserMedia|echoCancellation|noiseSuppression|autoGainControl|reconnect-audio|events/recover|maxDurationMs|serverMuted" web-ui/client/src/lib web-ui/client/tests/unit`
- `git diff --check`
</verification>

<success_criteria>
Client helper stabilization is complete when the focused Vitest suite proves same-origin wrappers, browser media helpers, bounded reconnect backfill selection, and split server/local mute state without adding Phase 4 call-feel features.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-04-SUMMARY.md`.
</output>
