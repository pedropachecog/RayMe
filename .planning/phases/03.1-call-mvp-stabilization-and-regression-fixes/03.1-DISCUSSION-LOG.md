# Phase 03.1: Call MVP Stabilization and Regression Fixes - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md - this log preserves the alternatives considered.

**Date:** 2026-05-12T03:11:50Z
**Phase:** 03.1-call-mvp-stabilization-and-regression-fixes
**Areas discussed:** Regression Scope, Acceptance Gate, TTS Engine Coverage, Reconnect And Failure Behavior, Diagnostics And Evidence, Scope Boundary

---

## Regression Scope

### Phase scope

| Option | Description | Selected |
|--------|-------------|----------|
| User-observed and evidence-backed call defects only | Fix defects already seen in evidence, Android/manual acceptance, logs, or failing/flaky tests. Recommended. | |
| Known defects plus nearby brittle edges | Also harden adjacent call startup/teardown/reconnect paths even if not yet user-observed. | yes |
| Broad call MVP hardening sweep | Inspect the whole call stack and fix any suspicious weakness, even without a concrete symptom. | |
| Other | User-provided rule. | |

**User's choice:** Known defects plus nearby brittle edges.
**Notes:** This makes Phase 03.1 tighter than a rewrite, but broader than reproduced failures only.

### Brittle-edge discovery

| Option | Description | Selected |
|--------|-------------|----------|
| Evidence-first plus adjacent code paths | Start from Phase 3/8 evidence, logs, flaky tests, and acceptance issues, then inspect startup, teardown, reconnect, mute, interrupt, and writeback paths. Recommended. | yes |
| Only failures already reproduced | Require a failing test, log, or user-reported repro before including work. | |
| Planner discretion after code audit | Let planner inspect the call stack and decide what is close enough. | |
| Other | User-provided rule. | |

**User's choice:** Evidence-first plus adjacent code paths.

### Priority order

| Option | Description | Selected |
|--------|-------------|----------|
| Lifecycle correctness first | Startup, negotiation, reconnect, teardown, and durable writeback bugs outrank cosmetic/UI polish. Recommended. | |
| Android/mobile blockers first | Physical Android or mobile Chrome defects outrank desktop/server-only issues. | yes |
| Runtime/audio correctness first | TTS playback, mic capture, mute, interrupt, and server-side frame handling outrank navigation/writeback issues. | |
| Other | User-provided order. | |

**User's choice:** Android/mobile blockers first.

### Refactor allowance

| Option | Description | Selected |
|--------|-------------|----------|
| Patch the smallest durable invariant and test it | Focused behavior fix with regression coverage; defer broad rewrites. Recommended. | |
| Allow targeted refactors when cleaner | Permit localized refactors in the affected call path if they reduce fragility. | yes |
| Defer anything that smells like refactor | Patch symptoms only; leave structural cleanup for later. | |
| Other | User-provided rule. | |

**User's choice:** Allow targeted refactors when cleaner.

### Additional scope rule

| Option | Description | Selected |
|--------|-------------|----------|
| No new user-facing call features | Fix and harden existing MVP behaviors only. Recommended. | |
| Allow tiny affordances for recovery | Small UI/copy tweaks are okay if necessary for recovery. | |
| Let implementation decide | Planner/implementation decides case by case. | yes |
| Other | User-provided rule. | |

**User's choice:** Let implementation decide.
**Notes:** This remains bounded by the phase boundary captured in CONTEXT.md.

---

## Acceptance Gate

### Completion verification level

| Option | Description | Selected |
|--------|-------------|----------|
| Local automated plus OMEN live desktop plus Android when touched | Local API/browser tests and OMEN live verification; physical Android acceptance when mobile Chrome, mic permission, call startup, audio playback, or layout is touched. Recommended. | yes |
| Always require physical Android approval | Every Phase 03.1 completion needs Android product-owner acceptance. | |
| Local automated and OMEN live only | No Android handoff unless explicitly requested. | |
| Other | User-provided gate. | |

**User's choice:** Local automated plus OMEN live desktop plus Android when touched.

### Local automated baseline

| Option | Description | Selected |
|--------|-------------|----------|
| Focused call regression suite plus impacted tests | AI backend call/WebRTC, Web UI call/prompt, client call unit, relevant Playwright call specs, and impacted tests. Recommended. | yes |
| Full repo test suite | Run everything regardless of touched area. | |
| Only tests around changed files | Keep it minimal and targeted. | |
| Other | User-provided baseline. | |

**User's choice:** Focused call regression suite plus impacted tests.

### OMEN live verification

| Option | Description | Selected |
|--------|-------------|----------|
| Canonical deploy then live non-mocked call evidence | Deploy only through `scripts/deploy-omen.sh`, run non-mocked live call check, and record evidence. Recommended. | yes |
| Reuse current deployed server if already running | Skip redeploy when code seems current. | |
| Manual smoke is enough | Use browser/manual observation without scripted live call check. | |
| Other | User-provided live gate. | |

**User's choice:** Canonical deploy then live non-mocked call evidence.

### Evidence artifact style

| Option | Description | Selected |
|--------|-------------|----------|
| Dedicated Phase 03.1 evidence doc plus machine-readable logs where useful | Human-readable summary for local/OMEN/Android gates with JSON/log snippets for runtime-sensitive checks. Recommended. | yes |
| Append to Phase 3 evidence files | Keep all call MVP evidence together. | |
| Only plan summaries | Each plan records evidence; no standalone evidence doc. | |
| Other | User-provided artifact style. | |

**User's choice:** Dedicated Phase 03.1 evidence doc plus machine-readable logs where useful.

---

## TTS Engine Coverage

### Engine set

| Option | Description | Selected |
|--------|-------------|----------|
| Both `voxcpm2` default and `f5` fallback for affected paths | Do not regress the current preferred engine or the known-good Phase 3 fallback. Recommended. | yes |
| Only current default `voxcpm2` | Do not require F5 evidence unless fallback logic is touched. | |
| Only F5 for MVP stability | Anchor Phase 03.1 to the engine used during Phase 3 Android approval. | |
| Other | User-provided engine rule. | |

**User's choice:** Both `voxcpm2` default and `f5` fallback for affected paths.

### Depth of proof

| Option | Description | Selected |
|--------|-------------|----------|
| Behavioral smoke per engine, deeper only where touched | Prove each engine starts playback in live call path; add timing/chunk/cancel assertions only where touched. Recommended. | yes |
| Full repeated warm timing comparison again | Re-run Phase 8-style repeated median comparison. | |
| Automated mocks only for the non-touched engine | Live evidence for main touched engine, mocked/contracts for the other. | |
| Other | User-provided depth. | |

**User's choice:** Behavioral smoke per engine, deeper only where touched.

### Engine failure handling

| Option | Description | Selected |
|--------|-------------|----------|
| Block unless proven unrelated | If either `voxcpm2` or `f5` fails live verification, fix it before completion unless clearly unrelated and documented. Recommended. | yes |
| Allow one engine to pass | If one engine works, complete the phase and defer the broken engine. | |
| Ignore engine failures unless this phase touched TTS code | Only care when stabilization changed TTS/playback code. | |
| Other | User-provided rule. | |

**User's choice:** Block unless proven unrelated.
**Notes:** User asked for clarification. The explanation used an OMEN setup/runtime problem versus a real call playback regression as the example.

---

## Reconnect And Failure Behavior

### Mid-call media/WebRTC drop

| Option | Description | Selected |
|--------|-------------|----------|
| Try bounded recovery, then end truthfully if recovery fails | Keep current recovery/backfill direction, but make it bounded, tested, and honest. Recommended. | yes |
| End immediately and preserve transcript | No reconnect attempt in this stabilization phase. | |
| Keep the call UI alive as long as possible | Preserve the session even if state is uncertain. | |
| Other | User-provided behavior. | |

**User's choice:** Try bounded recovery, then end truthfully if recovery fails.

### User-facing failure rule

| Option | Description | Selected |
|--------|-------------|----------|
| Honest state over optimistic continuation | If RayMe cannot prove the call is usable, show failed/ended state and preserve what was captured. Recommended. | yes |
| Keep trying silently | Avoid interrupting the user unless definitely dead. | |
| Prompt the user to choose | Ask whether to retry, stay, or end whenever recovery is uncertain. | |
| Other | User-provided rule. | |

**User's choice:** Honest state over optimistic continuation.

### Failure-path coverage

| Option | Description | Selected |
|--------|-------------|----------|
| Startup, reconnect/drop, mute, interrupt, end-call, and writeback | Covers lifecycle paths most likely to regress stabilization. Recommended. | yes |
| Only paths changed by implementation | Let the eventual fix decide coverage. | |
| All browser/media failure permutations | Exhaustively cover permissions, ICE, data channel, track endings, backend outages, LLM failures, and TTS failures. | |
| Other | User-provided coverage list. | |

**User's choice:** Startup, reconnect/drop, mute, interrupt, end-call, and writeback.

### Reconnect/backfill boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Stabilize existing recovery/backfill behavior only | Make current reconnect/backfill reliable and bounded; do not design a new reconnection feature. Recommended. | yes |
| Simplify by removing backfill | Prefer clean failure over backfill complexity. | |
| Upgrade reconnect into a real user-visible feature | Add visible reconnect states and stronger recovery UX. | |
| Other | User-provided boundary. | |

**User's choice:** Stabilize existing recovery/backfill behavior only.

---

## Diagnostics And Evidence

### Diagnostic logging

| Option | Description | Selected |
|--------|-------------|----------|
| Keep useful diagnostics, gate noisy ones | Keep logs/events that help diagnose live Android/OMEN failures; make noisy debug routes/log spam test-only or bounded. Recommended. | yes |
| Remove debug plumbing from product code | Strip diagnostic routes/events unless strictly user-facing. | |
| Keep all diagnostics verbose | Prefer maximum observability during stabilization. | |
| Other | User-provided rule. | |

**User's choice:** Keep useful diagnostics, gate noisy ones.

### Diagnostic data boundary

| Option | Description | Selected |
|--------|-------------|----------|
| State/timing/counts only, no sensitive audio/text payloads | Record call IDs, states, timings, frame counts, error codes, sanitized device/browser facts; no mic audio, raw transcripts beyond persisted messages, API keys, local secrets, or full model paths. Recommended. | |
| Anything needed to debug | Allow broader payloads during stabilization. | yes |
| Only high-level pass/fail evidence | Avoid detailed diagnostics entirely. | |
| Other | User-provided boundary. | |

**User's choice:** Anything needed to debug.
**Notes:** The hard safety boundary still applies: no API keys, passwords, tokens, or other secrets in planning docs, commits, or evidence.

### Verbose diagnostic location

| Option | Description | Selected |
|--------|-------------|----------|
| Temporary/runtime artifacts with sanitized summaries committed | Keep raw-ish debug logs local or gitignored; commit useful sanitized summary/evidence. Recommended. | yes |
| Committed evidence files | Put detailed diagnostics directly in Phase 03.1 evidence artifacts. | |
| Runtime logs only | Do not create evidence artifacts from diagnostics. | |
| Other | User-provided location. | |

**User's choice:** Temporary/runtime artifacts with sanitized summaries committed.

### Machine-readable evidence depth

| Option | Description | Selected |
|--------|-------------|----------|
| Use JSON/log snippets for runtime-sensitive checks only | Example: engine smoke, mute frame drop, reconnect outcome, call writeback counts. Recommended. | yes |
| Machine-readable artifact for every acceptance item | Make every gate scriptable and auditable. | |
| Human-readable summaries only | Keep evidence docs light. | |
| Other | User-provided depth. | |

**User's choice:** Use JSON/log snippets for runtime-sensitive checks only.
**Notes:** Auto-selected after user asked to accept remaining recommended values.

---

## Scope Boundary

### Explicit out-of-scope work

| Option | Description | Selected |
|--------|-------------|----------|
| Defer Phase 4 call-feel capabilities | Keep VAD barge-in, echo mitigation, TTFA targets, full visualizer polish, advanced call-feel tuning, and new in-call features out of scope. Recommended. | yes |
| Allow any call-feel fix that appears necessary | Let Phase 03.1 absorb call-feel work if discovered. | |
| Planner decides case by case | No explicit boundary beyond roadmap. | |
| Other | User-provided boundary. | |

**User's choice:** Defer Phase 4 call-feel capabilities.
**Notes:** Auto-selected after user asked to accept remaining recommended values.

### Outside issue handling

| Option | Description | Selected |
|--------|-------------|----------|
| Document and defer unless it blocks current MVP correctness | Capture the issue without expanding scope unless current MVP correctness depends on it. Recommended. | yes |
| Expand Phase 03.1 to fix it | Grow scope to include the issue. | |
| Ignore it for this phase | Do not record or act on it. | |
| Other | User-provided rule. | |

**User's choice:** Document and defer unless it blocks current MVP correctness.
**Notes:** Auto-selected after user asked to accept remaining recommended values.

### UI change allowance

| Option | Description | Selected |
|--------|-------------|----------|
| Small recovery/status/copy changes only | Support truthful recovery or stabilization without redesigning the call surface. Recommended. | yes |
| No user-facing UI changes | Keep all changes non-UI. | |
| Allow broader call UI cleanup | Use stabilization as a chance to clean up call UI. | |
| Other | User-provided UI rule. | |

**User's choice:** Small recovery/status/copy changes only.
**Notes:** Auto-selected after user asked to accept remaining recommended values.

### Deferred ideas handling

| Option | Description | Selected |
|--------|-------------|----------|
| Record Phase 4/backlog candidates in context, not implementation tasks | Preserve ideas without silently expanding the phase. Recommended. | yes |
| Create new scope immediately | Add new work to this phase. | |
| Do not record deferred ideas | Drop out-of-scope ideas. | |
| Other | User-provided handling. | |

**User's choice:** Record Phase 4/backlog candidates in context, not implementation tasks.
**Notes:** Auto-selected after user asked to accept remaining recommended values.

---

## the agent's Discretion

- Exact plan split across backend, server, client, and evidence work.
- Exact localized refactor shape inside affected call paths.
- Exact test command grouping beyond the required focused call baseline.
- Exact diagnostic gating mechanism and artifact names.
- Exact machine-readable fields for runtime-sensitive evidence.

## Deferred Ideas

- VAD-driven barge-in and automatic speech interruption remain Phase 4.
- Echo-loop mitigation, TTFA target optimization, and advanced call-feel tuning remain Phase 4.
- Full call visualizer polish and richer call-state presentation remain Phase 4.
- A redesigned user-visible reconnect feature is out of scope; Phase 03.1 only stabilizes existing recovery/backfill behavior.
- Broad call UI redesign is out of scope.
