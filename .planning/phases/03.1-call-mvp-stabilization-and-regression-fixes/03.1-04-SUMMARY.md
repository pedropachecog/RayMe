---
phase: 03.1-call-mvp-stabilization-and-regression-fixes
plan: 04
subsystem: client-call
tags: [svelte, vitest, webrtc, microphone, reconnect-backfill]

requires:
  - phase: 03.1-call-mvp-stabilization-and-regression-fixes
    provides: Evidence-backed Phase 03.1 defect triage and stabilization scope
provides:
  - Client helper regressions for microphone constraints, same-origin call wrappers, reconnect backfill, mute state, and button interrupt cleanup
  - Contiguous delayed reconnect backfill selection from the previous sent PCM boundary
affects: [phase-03.1, browser-call, reconnect-backfill, Android microphone startup]

tech-stack:
  added: []
  patterns:
    - Focused Vitest helper contracts before browser route stabilization
    - Initial reconnect pre-roll remains capped while delayed tail recovery resumes from the previous end offset

key-files:
  created:
    - .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-04-SUMMARY.md
  modified:
    - web-ui/client/src/lib/call/reconnectBackfill.ts
    - web-ui/client/tests/unit/call-audio.test.ts
    - web-ui/client/tests/unit/call-state.test.ts
    - web-ui/client/tests/unit/reconnect-backfill.test.ts

key-decisions:
  - "Delayed reconnect backfill selections with a prior startMs are treated as contiguous tail recovery and do not reapply the latest-window cap."

patterns-established:
  - "Call helper regressions stay unit-scoped before route/browser plans consume the contracts."

requirements-completed: [REQ-40, REQ-47, REQ-48, REQ-50, REQ-A0, REQ-A1]

duration: 9min
completed: 2026-05-12
---

# Phase 03.1 Plan 04: Client Call Helper Stabilization Summary

**Vitest-locked client call helper contracts with contiguous reconnect PCM tail recovery for the missing-chunks case**

## Performance

- **Duration:** 9 min
- **Started:** 2026-05-12T13:53:54Z
- **Completed:** 2026-05-12T14:03:17Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Added RED helper coverage for Android-safe `getUserMedia` constraints, one-sample AudioContext unlock, same-origin reconnect/event-recovery wrapper paths, server/local mute split, and button interrupt cleanup.
- Reproduced the missing-chunks regression where delayed tail recovery skipped the previous `35256ms` boundary.
- Fixed reconnect selection so initial recovery stays capped to the latest 30s window, while delayed tail selection remains contiguous from the previous end.

## Task Commits

1. **Task 1: Add client helper RED regressions** - `1e4142a` (test)
2. **Task 2: Stabilize client call helpers and typed API wrappers** - `09341c4` (fix)

**Plan metadata:** committed separately after state and roadmap updates.

## Files Created/Modified

- `web-ui/client/tests/unit/call-audio.test.ts` - Added microphone constraint coverage for Android-safe browser capture.
- `web-ui/client/tests/unit/call-state.test.ts` - Added same-origin reconnect/event-recovery wrapper coverage alongside existing mute and interrupt state tests.
- `web-ui/client/tests/unit/reconnect-backfill.test.ts` - Added exact `35256ms` / `69467ms` missing-chunks regression coverage.
- `web-ui/client/src/lib/call/reconnectBackfill.ts` - Preserves delayed tail selection from `startMs` while retaining initial max-duration pre-roll capping.

## Decisions Made

- Delayed reconnect backfill with a nonzero prior `startMs` is tail recovery, not a fresh initial selection, so it must resume from that boundary even when the latest-window cap would skip ahead.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

The existing helper implementation already satisfied most planned contracts. The RED failure was therefore focused on the uncovered reconnect-tail default behavior: before the fix, the selector returned `65256ms` instead of the required `35256ms`.

## Verification

- RED gate: `npm --prefix web-ui/client run test:unit -- --run tests/unit/call-audio.test.ts tests/unit/call-state.test.ts tests/unit/reconnect-backfill.test.ts` - FAIL as expected before the fix, 1 failed / 14 passed; delayed tail selected `65256ms` instead of `35256ms`.
- GREEN gate: same focused unit command - PASS, 3 files and 15 tests passed.
- `rg -n "getUserMedia|echoCancellation|noiseSuppression|autoGainControl|reconnect-audio|events/recover|maxDurationMs|serverMuted" web-ui/client/src/lib web-ui/client/tests/unit` - PASS.
- `git diff --check` - PASS.

## Known Stubs

None. Stub-pattern scan only found existing user-facing unavailable-browser copy and intentional empty/default helper state.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for plan `03.1-05`: browser call-route stabilization can consume the locked helper contracts for microphone startup, same-origin call wrappers, and reconnect backfill continuity.

## Self-Check: PASSED

- Summary file exists at `.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-04-SUMMARY.md`.
- Task commits exist in git history: `1e4142a` and `09341c4`.

---
*Phase: 03.1-call-mvp-stabilization-and-regression-fixes*
*Completed: 2026-05-12*
