# Phase 03.1: Call MVP Stabilization and Regression Fixes - Pattern Map

**Mapped:** 2026-05-12
**Files analyzed:** 32 candidate new/modified files
**Analogs found:** 32 / 32

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---:|---|---|
| `ai-backend/app/call/session.py` | service | event-driven, streaming | `ai-backend/app/call/session.py` | exact |
| `ai-backend/app/api/webrtc.py` | controller/route | request-response, event-driven | `ai-backend/app/api/webrtc.py` | exact |
| `ai-backend/app/call/tracks.py` | utility | streaming, file-I/O | `ai-backend/app/call/tracks.py` | exact |
| `web-ui/server/app/api/calls.py` | controller/route | request-response, streaming | `web-ui/server/app/api/calls.py` | exact |
| `web-ui/server/app/domain/call_service.py` | service | CRUD, request-response | `web-ui/server/app/domain/call_service.py` | exact |
| `web-ui/server/app/domain/prompt_builder.py` | utility/service | transform | `web-ui/server/app/domain/prompt_builder.py` | exact |
| `web-ui/server/app/domain/voice_service.py` | service | CRUD, transform | `web-ui/server/app/domain/voice_service.py` | exact |
| `web-ui/client/src/routes/call/[threadId]/+page.svelte` | route/component | event-driven, streaming | `web-ui/client/src/routes/call/[threadId]/+page.svelte` | exact |
| `web-ui/client/src/lib/api/calls.ts` | utility/API client | request-response, streaming | `web-ui/client/src/lib/api/calls.ts` | exact |
| `web-ui/client/src/lib/call/audio.ts` | utility | browser media file-I/O, streaming | `web-ui/client/src/lib/call/audio.ts` | exact |
| `web-ui/client/src/lib/call/reconnectBackfill.ts` | utility | transform | `web-ui/client/src/lib/call/reconnectBackfill.ts` | exact |
| `web-ui/client/src/lib/call/store.svelte.ts` | store | event-driven | `web-ui/client/src/lib/call/store.svelte.ts` | exact |
| `web-ui/client/src/lib/components/call/CallToolbar.svelte` | component | event-driven | `web-ui/client/src/lib/components/call/CallToolbar.svelte` | exact |
| `web-ui/client/src/lib/components/call/CallTranscript.svelte` | component | transform | `web-ui/client/src/lib/components/call/CallTranscript.svelte` | exact |
| `web-ui/client/src/lib/components/call/VoiceVisualizer.svelte` | component | event-driven | `web-ui/client/src/lib/components/call/VoiceVisualizer.svelte` | exact |
| `ai-backend/tests/test_call_session.py` | test | event-driven, streaming | `ai-backend/tests/test_call_session.py` | exact |
| `ai-backend/tests/test_webrtc_signaling.py` | test | request-response | `ai-backend/tests/test_webrtc_signaling.py` | exact |
| `web-ui/server/tests/test_calls.py` | test | request-response, streaming, CRUD | `web-ui/server/tests/test_calls.py` | exact |
| `web-ui/server/tests/test_prompt_builder.py` | test | transform | `web-ui/server/tests/test_prompt_builder.py` | exact |
| `web-ui/client/tests/unit/call-audio.test.ts` | test | browser media utility | `web-ui/client/tests/unit/call-audio.test.ts` | exact |
| `web-ui/client/tests/unit/call-state.test.ts` | test | event-driven store | `web-ui/client/tests/unit/call-state.test.ts` | exact |
| `web-ui/client/tests/unit/reconnect-backfill.test.ts` | test | transform | `web-ui/client/tests/unit/reconnect-backfill.test.ts` | exact |
| `web-ui/client/tests/e2e/call-start.spec.ts` | test | event-driven, request-response | `web-ui/client/tests/e2e/call-start.spec.ts` | exact |
| `web-ui/client/tests/e2e/call-toolbar.spec.ts` | test | event-driven | `web-ui/client/tests/e2e/call-toolbar.spec.ts` | exact |
| `web-ui/client/tests/e2e/call-permissions.spec.ts` | test | browser permission | `web-ui/client/tests/e2e/call-permissions.spec.ts` | exact |
| `web-ui/client/tests/e2e/call-summary.spec.ts` | test | CRUD, request-response | `web-ui/client/tests/e2e/call-summary.spec.ts` | exact |
| `web-ui/client/tests/e2e/call-mobile.spec.ts` | test | mobile layout, event-driven | `web-ui/client/tests/e2e/call-mobile.spec.ts` | exact |
| `web-ui/client/tests/e2e/call-visualizer.spec.ts` | test | visual state, event-driven | `web-ui/client/tests/e2e/call-visualizer.spec.ts` | exact |
| `web-ui/client/tests/e2e/live-call.spec.ts` | test/evidence | live request-response, browser media | `web-ui/client/tests/e2e/live-call.spec.ts` | exact |
| `.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-EVIDENCE.md` | evidence doc | batch | `.planning/phases/03-first-working-call-mvp/OMEN-PC-LIVE-EVIDENCE.md` | role-match |
| `.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/results/*.json` | evidence data | batch | `.planning/phases/08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback/results/voxcpm2-decision.json` | role-match |
| `scripts/deploy-omen.sh` | config/deployment | batch | `scripts/deploy-omen.sh` | exact |

## Pattern Assignments

### `ai-backend/app/call/session.py` (service, event-driven/streaming)

**Analog:** `ai-backend/app/call/session.py`

**Imports and constants pattern** (lines 1-56):
```python
import asyncio
import base64
import inspect
import json
import logging
import time
from collections.abc import Awaitable, Callable
from datetime import datetime, timezone
from typing import Any

import numpy as np

logger = logging.getLogger(__name__)

from app.call.events import (
    AI_AUDIO_STARTED_EVENT,
    AI_DONE_EVENT,
    ENDED_EVENT,
    FAILED_EVENT,
    INTERRUPTED_EVENT,
    MUTED_EVENT,
    failed_event,
    simple_event,
    user_final_event,
    utc_timestamp,
)
```

**State and inbound audio drop pattern** (lines 132-188):
```python
self.state = "listening"
self.muted = False
self.incoming_audio_frames = 0
self.dropped_audio_frames = 0
self.active_turn_task: Any | None = None
self._undelivered_events: list[dict[str, Any]] = []
self._media_reconnect_grace_pending = False
self._reconnect_audio_backfill_ids: set[str] = set()

async def handle_inbound_audio_frame(self, frame: Any) -> dict[str, Any] | bool | None:
    self.incoming_audio_frames += 1
    was_raw_bytes = isinstance(frame, bytes)
    if self.muted or self.state in {"ended", "failed", "speaking"}:
        self.dropped_audio_frames += 1
        if self.incoming_audio_frames % 100 == 0:
            logger.info(
                "[rayme-call] inbound.dropped session=%s total=%d dropped=%d "
                "muted=%s state=%s",
                self.session_id,
                self.incoming_audio_frames,
                self.dropped_audio_frames,
                self.muted,
                self.state,
            )
        return False if was_raw_bytes else None
```

**Reconnect/backfill pattern** (lines 237-318, 399-450):
```python
def mark_media_reconnect_pending(self) -> None:
    if self.state == "listening" and self._speech_seen and self._turn_frames:
        self._media_reconnect_grace_pending = True
        logger.info(
            "[rayme-call] vad.reconnect_grace.pending session=%s "
            "turn_frames=%d silence_ms=%d",
            self.session_id,
            len(self._turn_frames),
            self._silence_ms,
        )

async def backfill_reconnect_audio(..., final: bool = True) -> dict[str, Any]:
    if backfill_id and backfill_id in self._reconnect_audio_backfill_ids:
        return {"status": "duplicate", "frames": 0, "duration_ms": 0, "state": self.state}
    if self.muted or self.state in {"ended", "speaking", "understanding", "thinking"}:
        return {"status": "skipped", "frames": 0, "duration_ms": 0, "state": self.state}
    ...
    if backfill_id:
        self._reconnect_audio_backfill_ids.add(backfill_id)
    was_pending = self._media_reconnect_grace_pending
    for frame in frames:
        vad_result = self._append_turn_frame(frame, source="reconnect_backfill")
    self._media_reconnect_grace_pending = was_pending or self._media_reconnect_grace_pending
    if final:
        released_end = self._release_reconnect_live_frames(reason="final_backfill")
```

**Control and TTS pattern** (lines 750-822):
```python
async def set_muted(self, muted: bool) -> dict[str, Any]:
    self.muted = muted
    self.state = "muted" if muted else "listening"
    return await self.emit_event(simple_event(MUTED_EVENT, session_id=self.session_id, muted=muted))

async def interrupt(self) -> dict[str, Any]:
    self.interrupted = True
    await self.cancel_ai_turn()
    self.state = "interrupted"
    event = await self.emit_event(simple_event(INTERRUPTED_EVENT, session_id=self.session_id))
    self.state = "listening"
    return event

if _adapter_supports_streaming(adapter, engine_id):
    return await self._speak_streaming_speech(
        turn_id=turn_id,
        text=text,
        voice_id=voice_id,
        engine_id=engine_id,
        final_chunk=final_chunk,
        adapter=adapter,
        reference_audio_b64=reference_audio_b64,
        reference_transcript=reference_transcript,
        reference_audio_content_type=reference_audio_content_type,
        voxcpm2_options=voxcpm2_options,
    )
```

**Manager recovery-retention pattern** (lines 1945-2040):
```python
existing = self._sessions.get(session_id)
if existing is not None:
    if peer_connection is not None and peer_connection is not existing.peer_connection:
        previous_peer = existing.peer_connection
        existing.peer_connection = peer_connection
        if existing.state == "failed" and existing.end_reason == "connection_failed":
            existing.state = "listening"
            existing.end_reason = None
            existing.ended_at = None
        existing.mark_media_reconnect_pending()
    ...
    return existing

async def end_session(..., recovery_grace_seconds: float = CALL_ENDED_EVENT_RECOVERY_GRACE_SECONDS):
    session = self._sessions.get(session_id)
    if session is None:
        return None
    await session.end(reason=reason)
    if recovery_grace_seconds > 0:
        self._ended_session_recovery_deadlines[session_id] = time.monotonic() + recovery_grace_seconds
```

**Testing pattern:** extend `ai-backend/tests/test_call_session.py`. Use existing focused assertions for mute, reconnect, and dual-engine TTS:
```python
# lines 433-443
_run(session.set_muted(True))
accepted = _run(session.handle_inbound_audio_frame(b"pcm-frame-1"))
assert accepted is False
assert session.stats()["dropped_audio_frames"] == 1

# lines 663-702
backfill = _run(session.backfill_reconnect_audio(..., backfill_id="gap-1", final=False))
final = _run(session.handle_inbound_audio_frame(ScriptedInboundAudioFrame(post_pcm)))
assert backfill["status"] == "accepted"
assert final["type"] == "user_final"

# lines 1065-1115
speech = asyncio.create_task(session.speak_text(..., "voxcpm2", final_chunk=True, ...))
await _wait_for_async_event_or_task(audio_started, speech, label="VoxCPM2 first streamed audio event")
assert playback["streaming_used"] is True
assert playback["whole_wav_fallback_used"] is False
```

---

### `ai-backend/app/api/webrtc.py` (controller/route, request-response/event-driven)

**Analog:** `ai-backend/app/api/webrtc.py`

**Request model pattern** (lines 50-107):
```python
class CallOfferRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")

    session_id: str = Field(min_length=1, max_length=128)
    thread_id: str = Field(min_length=1, max_length=128)
    voice_id: str = Field(min_length=1, max_length=128)
    engine_id: str = Field(min_length=1, max_length=64)
    prompt_messages: list[PromptMessage] = Field(default_factory=list, max_length=48)
    offer: SessionDescription

class ReconnectAudioBackfillRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")

    pcm_b64: str = Field(default="", max_length=4_000_000)
    sample_rate: int = Field(default=16000, ge=8000, le=48000)
    channels: int = Field(default=1, ge=1, le=2)
    backfill_id: str | None = Field(default=None, max_length=160)
    attempt: int | None = Field(default=None, ge=0, le=10)
    batch_index: int | None = Field(default=None, ge=0, le=20)
    final: bool = True
```

**Offer/re-offer pattern** (lines 131-184):
```python
@router.post("/offer")
async def create_webrtc_offer_answer(request: Request, payload: CallOfferRequest) -> dict[str, Any]:
    manager = _manager_from_app(request)
    existing_session = manager.get_session(payload.session_id)
    previous_peer_connection = existing_session.peer_connection if existing_session is not None else None
    ...
    peer_connection = _create_peer_connection(payload.offer)
    outbound_audio_track = _attach_outbound_audio_track(peer_connection)
    try:
        session = await manager.create_session(
            session_id=payload.session_id,
            thread_id=payload.thread_id,
            voice_id=payload.voice_id,
            engine_id=payload.engine_id,
            prompt_messages=[message.model_dump() for message in payload.prompt_messages],
            peer_connection=peer_connection,
            vad_adapter=_vad_adapter(request),
            stt_adapter=_stt_adapter(request),
            outbound_audio_track=outbound_audio_track,
            close_previous_peer=False,
        )
        _attach_peer_handlers(peer_connection, session)
        answer = await _negotiate_answer(peer_connection, payload.offer)
```

**Control/speak/reconnect/event routes** (lines 251-407):
```python
@router.post("/sessions/{session_id}/mute", response_model=CallControlResponse)
async def mute_session(..., payload: MuteControlRequest) -> CallControlResponse:
    session = _session_or_404(request, session_id)
    try:
        await session.set_muted(payload.muted)
        return CallControlResponse(session_id=session.session_id, state=session.state, muted=session.muted)
    except Exception as exc:
        raise _control_error() from exc

@router.post("/sessions/{session_id}/speak")
async def speak_session(..., payload: SpeakRequest) -> dict[str, Any]:
    session = _session_or_404(request, session_id)
    try:
        _reject_oversized_reference_audio(payload.reference_audio_b64)
        adapter = _tts_adapter(request, payload.engine_id)
        event = await session.speak_text(..., voxcpm2_denoise=payload.voxcpm2_denoise)
    except Exception as exc:
        raise HTTPException(status_code=status.HTTP_502_BAD_GATEWAY, detail={
            "code": "call_tts_failed",
            "message": "Speech playback failed",
            "engine_id": payload.engine_id,
        }) from exc

@router.post("/sessions/{session_id}/events/drain")
async def drain_session_events(request: Request, session_id: str) -> dict[str, Any]:
    session = _session_or_404(request, session_id)
    return {"session_id": session.session_id, "events": session.drain_undelivered_events()}
```

**Track receive failure pattern** (lines 720-811):
```python
except Exception as exc:
    ice_state = getattr(pc, "iceConnectionState", "?")
    conn_state = getattr(pc, "connectionState", "?")
    if session.state in {"ended", "failed"}:
        break
    if ice_state in {"failed", "closed"} or conn_state in {"failed", "closed"}:
        await session.fail(reason="connection_failed")
        return
    if ice_state == "disconnected" or conn_state == "disconnected":
        reconnected = False
        for attempt in range(10):
            await asyncio.sleep(0.5)
            ...
        if not reconnected:
            await session.fail(reason="connection_failed")
            return
        continue
    consecutive_live_recv_errors += 1
    await asyncio.sleep(min(1.0, 0.1 * consecutive_live_recv_errors))
    continue
```

**Testing pattern:** extend `ai-backend/tests/test_webrtc_signaling.py`:
```python
# lines 289-303
response = client.post(MUTE_ROUTE_TEMPLATE.format(session_id=session_id), json={"muted": True})
assert response.status_code == 200
assert response.json()["muted"] is True

# lines 616-640
response = client.post(RECONNECT_AUDIO_ROUTE_TEMPLATE.format(session_id=session_id), json={...})
assert response.status_code == 200
assert payload["status"] == "accepted"

# lines 729-750
assert response.status_code in {400, 422}
assert "Traceback" not in response.text
assert "File " not in response.text
```

---

### `ai-backend/app/call/tracks.py` (utility, streaming/file-I/O)

**Analog:** `ai-backend/app/call/tracks.py`

**Audio primitive pattern** (lines 20-51):
```python
@dataclass(frozen=True)
class PcmAudioFrame:
    pcm: bytes
    sample_rate: int = 16000
    channels: int = 1

@dataclass
class InboundAudioFrameNormalizer:
    target_sample_rate: int = 16000
    incoming_frames: int = 0

    def normalize(self, frame: Any) -> PcmAudioFrame:
        self.incoming_frames += 1
        return normalize_inbound_audio_frame(frame, target_sample_rate=self.target_sample_rate)
```

**Queued output track pattern** (lines 54-119, 151-196):
```python
class QueuedAudioOutputTrack(MediaStreamTrack):
    kind = "audio"

    async def enqueue(self, wav_bytes: bytes, *, preroll_seconds: float = 0.0) -> float:
        samples = _wav_bytes_to_int16(wav_bytes, target_sample_rate=self.sample_rate)
        preroll_samples = int(self.sample_rate * max(preroll_seconds, 0.0))
        if preroll_samples > 0:
            samples = np.concatenate([np.zeros(preroll_samples, dtype=np.int16), samples])
        self.last_enqueue_stats = audio_stats_for_int16_samples(samples, sample_rate=self.sample_rate)
        if samples.size:
            await self._queue.put(samples)
        return float(self.last_enqueue_stats["duration_ms"]) / 1000.0

async def _next_samples(self) -> np.ndarray:
    ...
    else:
        # Emit silence while no AI audio is queued so recv() continues to
        # produce RTP frames during STT/LLM/TTS gaps.
        self._idle_frame_count += 1
    return samples
```

**Stats pattern** (lines 301-323):
```python
def audio_stats_for_int16_samples(samples: np.ndarray, *, sample_rate: int) -> dict[str, float | int]:
    float_samples = samples.astype(np.float32)
    return {
        "duration_ms": int(samples.size * 1000 / sample_rate) if samples.size else 0,
        "samples": int(samples.size),
        "rms": float(np.sqrt(np.mean(np.square(float_samples)))) if samples.size else 0.0,
        "peak": float(np.max(np.abs(float_samples))) if samples.size else 0.0,
    }
```

---

### `web-ui/server/app/api/calls.py` (controller/route, request-response/streaming)

**Analog:** `web-ui/server/app/api/calls.py`

**Imports and bounded model pattern** (lines 1-15, 49-120):
```python
from fastapi import APIRouter, Depends, HTTPException, Request, status
from fastapi.responses import StreamingResponse
from pydantic import BaseModel, ConfigDict, Field, model_validator

class CallStartRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    thread_id: str | None = Field(default=None, min_length=1, max_length=128)
    character_id: str | None = Field(default=None, min_length=1, max_length=128)

class CallReconnectAudioRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(min_length=1, max_length=128)
    pcm_b64: str = Field(default="", max_length=4_000_000)
    sample_rate: int = Field(default=16000, ge=8000, le=48000)
    channels: int = Field(default=1, ge=1, le=2)
    final: bool = True
```

**Same-origin guard pattern** (lines 170-190):
```python
async def enforce_same_origin_for_calls(request: Request, runtime_settings: Settings = Depends(get_call_runtime_settings)) -> None:
    origin = request.headers.get("origin")
    if not origin:
        return
    allowed_origins = {
        _origin_from_url(runtime_settings.web_public_url),
        *(_origin_from_url(origin) for origin in runtime_settings.allowed_origins),
        _origin_from_url(str(request.base_url)),
    }
    if _origin_from_url(origin) not in allowed_origins:
        raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail={
            "code": CALL_ORIGIN_NOT_ALLOWED,
            "message": "Call controls must come from the RayMe Web UI origin.",
        })
```

**Lifecycle facade pattern** (lines 193-230, 256-344):
```python
@router.post("/start", status_code=status.HTTP_201_CREATED, dependencies=[Depends(enforce_same_origin_for_calls)])
async def start_call(...):
    endpoint_settings = await SettingsService(session, runtime_settings).read()
    await _ensure_backend_ready(backend, endpoint_settings.ai_backend_url)
    try:
        return await CallService(session).start_call(thread_id=payload.thread_id, character_id=payload.character_id)
    except CallServiceError as exc:
        raise _call_error(exc) from exc

@router.post("/{call_id}/mute", dependencies=[Depends(enforce_same_origin_for_calls)])
async def mute_call(...):
    session_id = service.session_for_call(call_id)
    _reject_mismatched_session(session_id, payload.session_id)
    await _mute_call(backend, endpoint_settings.ai_backend_url, session_id, payload.muted)
    state = service.set_muted(call_id, payload.muted)
    return {"call_id": call_id, "session_id": session_id, "muted": state["muted"]}
```

**Reconnect/recover/end/debug pattern** (lines 507-624):
```python
@router.post("/{call_id}/reconnect-audio", dependencies=[Depends(enforce_same_origin_for_calls)])
async def backfill_call_reconnect_audio(...):
    result = await _backfill_call_audio(backend, endpoint_settings.ai_backend_url, session_id, {
        "pcm_b64": payload.pcm_b64,
        "sample_rate": payload.sample_rate,
        "channels": payload.channels,
        "backfill_id": payload.backfill_id,
        "reason": payload.reason,
        "attempt": payload.attempt,
        "duration_ms": payload.duration_ms,
        "batch_index": payload.batch_index,
        "final": payload.final,
    })
    return {"call_id": call_id, "session_id": session_id, **result}

@router.post("/{call_id}/end", dependencies=[Depends(enforce_same_origin_for_calls)])
async def end_call(...):
    try:
        await _end_call(backend, endpoint_settings.ai_backend_url, session_id, reason)
    except AiBackendClientError as exc:
        if exc.code == CALL_BACKEND_CLIENT_MISCONFIGURED:
            raise
    ended = await service.end_call(call_id, reason=reason)
    return {"call_id": call_id, "session_id": session_id, "reason": ended["reason"]}

if len(detail_serialized) > 800:
    detail_serialized = detail_serialized[:800] + "...<truncated>"
```

**Error/SSE helper pattern** (lines 772-790, 805-816):
```python
def _call_error(exc: CallServiceError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.to_public_dict())

def _backend_error(exc: AiBackendClientError) -> HTTPException:
    return HTTPException(status_code=502, detail=exc.to_public_dict())

def _sse(event: Mapping[str, Any]) -> str:
    return f"{SSE_DATA_PREFIX}{json.dumps(dict(event), separators=(',', ':'))}\n\n"

def _extract_ai_audio_started_event(speak_result: Mapping[str, Any] | None) -> dict[str, Any] | None:
    event = speak_result.get("event")
    candidates = [speak_result.get("ai_audio_started_event"), event.get("ai_audio_started_event") if isinstance(event, Mapping) else None]
```

**Testing pattern:** extend `web-ui/server/tests/test_calls.py`:
```python
# lines 249-290
response = call_fixture.client.post(f"/api/calls/{started['call_id']}/reconnect-audio", json={...})
assert response.status_code == 200
assert response.json()["status"] == "accepted"
assert not any(row[0] in {"user_speech", "ai_speech"} for row in rows)

# lines 530-589
assert [event["type"] for event in first_events] == ["ai_token", "ai_token", "ai_done"]
assert speech_rows == [
    ("user_speech", "user", "First user turn."),
    ("ai_speech", "assistant", "First AI answer."),
]

# lines 875-893
response = call_fixture.client.post(f"/api/calls/{started['call_id']}/interrupt", json={"session_id": started["session_id"]})
assert scripted_task.cancel_calls == 1
assert call_fixture.backend.interrupt_calls == [{"base_url": "https://127.0.0.1:9443", "session_id": started["session_id"]}]
```

---

### `web-ui/server/app/domain/call_service.py` (service, CRUD/writeback)

**Analog:** `web-ui/server/app/domain/call_service.py`

**Public-safe service error pattern** (lines 38-49):
```python
class CallServiceError(Exception):
    """Base public-safe call service error."""
    status_code = 400

    def __init__(self, *, code: str, message: str) -> None:
        super().__init__(message)
        self.code = code
        self.message = message

    def to_public_dict(self) -> dict[str, str]:
        return {"code": self.code, "message": self.message}
```

**Active call state and writeback pattern** (lines 82-108, 189-205, 277-289, 326-357):
```python
@dataclass(slots=True)
class ActiveCall:
    call_id: str
    session_id: str
    thread_id: str
    voice_id: str
    engine_id: str
    ended_at: datetime | None = None
    muted: bool = False

def set_muted(self, call_id: str, muted: bool) -> dict[str, Any]:
    call = self._active_call(call_id)
    call.muted = muted
    return call.to_public_dict()

async def end_call(self, call_id: str, reason: str = "hangup") -> dict[str, Any]:
    call = self._active_call(call_id)
    if call.ended_at is None:
        call.ended_at = utc_now()
        await self._append_message(call.thread_id, "Call ended", message_kind="call_end", role="event")
    result = call.to_public_dict()
    result["reason"] = reason
    return result
```

**Voice reference and VoxCPM2 metadata pattern** (lines 218-275, 373-384):
```python
async def voice_reference_for_call(self, call_id: str, voice_blob_dir: Path) -> dict[str, Any]:
    call = self._active_call(call_id)
    voice = await self._required_available_voice(call.voice_id)
    ...
    voice_reference = {
        "reference_audio_base64": base64.b64encode(sample_path.read_bytes()).decode("ascii"),
        "reference_audio_content_type": asset.content_type,
        "reference_transcript": voice.reference_transcript,
    }
    if call.engine_id == VOXCPM2_ENGINE_ID:
        try:
            voice_reference.update(_voxcpm2_call_fields(voice.metadata_json))
        except VoiceMetadataValidationError:
            raise CallVoiceUnavailableError() from None
    return voice_reference

def _voxcpm2_call_fields(metadata: Any) -> dict[str, Any]:
    settings = normalize_voxcpm2_engine_settings(raw_settings)
    return {
        "voxcpm2_cloning_mode": settings["cloning_mode"],
        "voxcpm2_style_prompt": settings["style_prompt"],
        "voxcpm2_cfg_value": settings["cfg_value"],
        "voxcpm2_inference_timesteps": settings["inference_timesteps"],
        "voxcpm2_normalize": settings["normalize"],
        "voxcpm2_denoise": settings["denoise"],
    }
```

---

### `web-ui/server/app/domain/voice_service.py` and `prompt_builder.py` (service/utility, transform)

**Analogs:** `web-ui/server/app/domain/voice_service.py`, `web-ui/server/app/domain/prompt_builder.py`

**Voice metadata normalization pattern** (voice_service.py lines 20-36, 332-355, 376-426):
```python
VOXCPM2_ENGINE_ID = "voxcpm2"
VOXCPM2_DEFAULT_SETTINGS = {
    "cloning_mode": "reference_only",
    "style_prompt": "",
    "cfg_value": 2.0,
    "inference_timesteps": 10,
    "normalize": False,
    "denoise": False,
}

def normalize_voice_metadata(raw_metadata: Any) -> dict[str, Any]:
    if raw_metadata is None:
        return {}
    if not isinstance(raw_metadata, dict):
        raise VoiceMetadataValidationError("Voice metadata must be an object")
    ...
    if VOXCPM2_ENGINE_ID in normalized_engine_settings:
        normalized_engine_settings[VOXCPM2_ENGINE_ID] = normalize_voxcpm2_engine_settings(
            normalized_engine_settings[VOXCPM2_ENGINE_ID]
        )

def normalize_voxcpm2_engine_settings(raw_settings: Any) -> dict[str, Any]:
    settings = {**VOXCPM2_DEFAULT_SETTINGS, **dict(raw_settings)}
    if cloning_mode not in VOXCPM2_CLONING_MODES:
        raise VoiceMetadataValidationError(...)
    if not VOXCPM2_CFG_VALUE_MIN <= cfg_value <= VOXCPM2_CFG_VALUE_MAX:
        raise VoiceMetadataValidationError(...)
    return {...}
```

**Call prompt context pattern** (prompt_builder.py lines 90-130):
```python
async def build_call_prompt_context(
    thread_id: str,
    *,
    repository: PromptContextRepository | None = None,
    max_turns: int = 24,
) -> list[PromptContextMessage]:
    """Build call prompt context from recent selected text and speech rows."""
    ...
    for message in _messages(prompt_thread):
        if _is_stale(message):
            continue
        if _message_kind(message) not in {"user_text", "ai_text", "user_speech", "ai_speech"}:
            continue
        role = _message_role(message)
        if role not in {"user", "assistant"}:
            continue
        content = _selected_content(message)
        if content:
            turn_messages.append({"role": role, "content": content})
    if max_turns > 0:
        turn_messages = turn_messages[-max_turns:]
    context.extend(turn_messages)
    return context
```

---

### `web-ui/client/src/routes/call/[threadId]/+page.svelte` (route/component, event-driven/streaming)

**Analog:** `web-ui/client/src/routes/call/[threadId]/+page.svelte`

**Imports/state/startup pattern** (lines 7-33, 122-131, 187-200):
```svelte
import {
  CallApiError,
  backfillCallReconnectAudio,
  endCall,
  interruptCall,
  recoverCallEvents,
  sendCallOffer,
  setCallMuted,
  startCall,
  submitCallTurn
} from '$lib/api/calls';
import { requestCallMicrophone, unlockCallAudioContext } from '$lib/call/audio';
import { selectReconnectAudioBackfill as selectReconnectAudioBackfillFromChunks } from '$lib/call/reconnectBackfill';

const MEDIA_RECONNECT_DISCONNECTED_GRACE_MS = 2500;
const MEDIA_RECONNECT_MAX_ATTEMPTS = 2;
const MIC_BACKFILL_SAMPLE_RATE = 16000;
const MIC_BACKFILL_RECONNECT_PREROLL_MS = 30000;

localMediaStream = await requestCallMicrophone();
startLocalMicMeter(localMediaStream);
await unlockAudioForCall();
const started = await startCall({ thread_id: threadId });
await connectBrowserMedia(started);
```

**WebRTC state and bounded reconnect pattern** (lines 294-457, 459-585):
```svelte
connection.addEventListener('iceconnectionstatechange', () => {
  const iceConnectionState = connection.iceConnectionState;
  emitDebugEvent(debugCallId, 'pc.iceconnectionstatechange', { iceConnectionState });
  if (iceConnectionState === 'failed' || iceConnectionState === 'disconnected') {
    scheduleBrowserMediaReconnect(connection, debugCallId, iceConnectionState === 'failed' ? 'failed' : 'disconnected');
  }
  if ((iceConnectionState === 'connected' || iceConnectionState === 'completed') && isBrowserMediaConnected(connection)) {
    clearMediaReconnectTimer();
    mediaReconnectAttempts = 0;
    clearReconnectAudioBackfill();
  }
});

if (mediaReconnectAttempts >= MEDIA_RECONNECT_MAX_ATTEMPTS) {
  emitDebugEvent(debugCallId, 'pc.media_reconnect.give_up', { reason, attempts: mediaReconnectAttempts });
  void failTerminalMediaReconnect(debugCallId, 'media_reconnect_give_up');
  return;
}

await connectBrowserMedia({ call_id: callId, session_id: sessionId }, {
  beforeRemoteDescription: () => flushReconnectAudioBackfill(debugCallId, reason, reconnectAttempt)
});
...
await recoverMissedCallEvents(debugCallId, reason);
try {
  await endCall(requestCallId, requestSessionId, 'connection_failed');
} catch {
  // The failed panel remains visible; cleanup failures are diagnostic-only here.
}
```

**Reconnect backfill pattern** (lines 665-960):
```svelte
function startReconnectAudioBackfill(debugCallId: string, reason: MediaReconnectReason) {
  if (reconnectAudioBackfillStartMs >= 0) {
    return;
  }
  const nowMs = performance.now();
  reconnectAudioBackfillStartMs = Math.max(0, nowMs - MIC_BACKFILL_RECONNECT_PREROLL_MS);
  reconnectAudioBackfillId = `${debugCallId || 'call'}-${Date.now()}-${Math.random().toString(36).slice(2, 10)}`;
  reconnectAudioBackfillReason = reason;
}

const response = await backfillCallReconnectAudio(requestCallId, {
  session_id: requestSessionId,
  pcm_b64: selection ? int16SamplesToBase64(selection.samples) : '',
  sample_rate: MIC_BACKFILL_SAMPLE_RATE,
  channels: 1,
  backfill_id: batchBackfillId,
  reason,
  attempt,
  duration_ms: selection?.durationMs ?? 0,
  batch_index: batchIndex,
  final
});
if (response.event) {
  await handleCallDataEvent(response.event);
}
```

**State/SSE/control pattern** (lines 1428-1810):
```svelte
function applyCallState(nextState: string) {
  const normalized = nextState.toLowerCase();
  if (normalized === 'listening' || normalized === 'understanding' || normalized === 'thinking' ||
      normalized === 'speaking' || normalized === 'interrupted' || normalized === 'ended' ||
      normalized === 'failed' || normalized === 'connecting') {
    const prevState = callState;
    callState = normalized;
    keepMicrophoneSenderLive(prevState, normalized);
    syncRemoteAudioAudibility();
  }
}

async function hangup() {
  ending = true;
  clearEventTimers();
  cancelActiveTurnStream();
  try {
    if (callId && sessionId) {
      await drainReconnectAudioBackfillBeforeHangup();
      await recoverMissedCallEvents(callId, 'hangup');
      await endCall(callId, sessionId);
    }
    stopBrowserMedia();
    callState = 'ended';
  } catch {
    callState = 'failed';
    blockingPanel = { body: 'The call ended because the connection dropped. Your transcript so far was saved.', action: 'Return to Thread', tone: 'danger' };
  } finally {
    ending = false;
  }
}
```

**Remote audio and component composition pattern** (lines 1923-1985, 2160-2178):
```svelte
function attachRemoteAudio(stream: MediaStream, debugCallId = '') {
  detachRemoteAudio();
  const element = new Audio();
  element.autoplay = true;
  element.playsInline = true;
  element.controls = false;
  element.muted = false;
  element.srcObject = stream;
  remoteAudioElement = element;
  void element.play().then(...);
}

<CallToolbar
  muted={serverMuted}
  stateLabel={callControlStateLabel}
  ready={callState === 'listening' && canUseToolbar}
  interruptEnabled={callState === 'understanding' || callState === 'thinking' || callState === 'speaking'}
  onMuteToggle={toggleMute}
  onInterrupt={interrupt}
  onEnd={hangup}
/>
<VoiceVisualizer state={visualState} {listeningRms} {speakingRms} />
<CallTranscript turns={transcript} {activeAiText} interrupted={callState === 'interrupted'} />
```

**Testing pattern:** extend `web-ui/client/tests/e2e/call-start.spec.ts`:
```ts
// lines 127-190
await setCurrentMockPeerState(page, 'failed', 'disconnected');
await expect.poll(() => counters.offerCount).toBe(2);
expect(counters.endCount).toBe(0);
expect(micReconnectDiagPhases).toContain('scheduled');
expect(micReconnectDiagPhases).toContain('start');
expect(micReconnectDiagPhases).toContain('ok');

// lines 377-442
await setCurrentMockPeerState(page, 'failed', 'disconnected');
await expect(page.getByRole('alert').getByText('The call ended because the connection dropped.')).toBeVisible();
await expect.poll(() => counters.recoverCount).toBeGreaterThanOrEqual(1);
await expect.poll(() => counters.endCount).toBe(1);
expect(counters.requestOrder.indexOf('recover')).toBeLessThan(counters.requestOrder.indexOf('end'));
```

---

### `web-ui/client/src/lib/api/calls.ts` (utility/API client, request-response)

**Analog:** `web-ui/client/src/lib/api/calls.ts`

**Typed API wrapper and public error pattern** (lines 11-67, 111-172):
```ts
export interface CallReconnectAudioBackfillRequest {
  session_id: string;
  pcm_b64: string;
  sample_rate: number;
  channels: number;
  backfill_id?: string;
  reason?: string;
  attempt?: number;
  duration_ms?: number;
  batch_index?: number;
  final?: boolean;
}

export class CallApiError extends Error {
  code?: CallErrorCode;
  status: number;
  constructor(message: string, status: number, code?: CallErrorCode) {
    super(message);
    this.name = 'CallApiError';
    this.status = status;
    this.code = code;
  }
}

export function backfillCallReconnectAudio(callId: string, payload: CallReconnectAudioBackfillRequest) {
  return apiFetch(`/calls/${encodeURIComponent(callId)}/reconnect-audio`, {
    method: 'POST',
    body: JSON.stringify(payload)
  });
}

export function recoverCallEvents(callId: string, sessionId: string) {
  return apiFetch(`/calls/${encodeURIComponent(callId)}/events/recover`, {
    method: 'POST',
    body: JSON.stringify({ session_id: sessionId })
  });
}
```

---

### Client call utilities and components

**Analogs:** `audio.ts`, `reconnectBackfill.ts`, `store.svelte.ts`, `CallToolbar.svelte`, `CallTranscript.svelte`, `VoiceVisualizer.svelte`

**Audio helper pattern** (`audio.ts` lines 47-104):
```ts
export async function unlockCallAudioContext(providedAudioContext?: UnlockableAudioContext) {
  const audioContext = providedAudioContext ?? new AudioContextCtor!();
  if (audioContext.state === 'suspended') {
    await audioContext.resume();
  }
  const silentBuffer = audioContext.createBuffer(1, 1, audioContext.sampleRate ?? 48000);
  const source = audioContext.createBufferSource();
  source.buffer = silentBuffer;
  source.connect(audioContext.destination as AudioNode);
  source.start();
  return { state: audioContext.state };
}

export async function requestCallMicrophone(): Promise<MediaStream> {
  return navigator.mediaDevices.getUserMedia({
    audio: { echoCancellation: true, noiseSuppression: true, autoGainControl: true }
  });
}

export function keepCallMicrophoneTracksLive(stream: Pick<MediaStream, 'getAudioTracks'>): number {
  ...
}
```

**Backfill selection pattern** (`reconnectBackfill.ts` lines 16-63):
```ts
export function selectReconnectAudioBackfill(chunks: LocalMicPcmChunk[], {...}): LocalMicPcmSelection | null {
  const boundedStartMs = limitToMaxWindow === false ? startMs : Math.max(startMs, endMs - maxDurationMs);
  const selectedChunks = chunks.filter((chunk) => chunk.endMs > boundedStartMs && chunk.startMs < endMs);
  const sampleCount = selectedChunks.reduce((total, chunk) => total + chunk.samples.length, 0);
  if (sampleCount <= 0) {
    return null;
  }
  ...
  return { startMs, endMs, samples, durationMs, rms: Math.sqrt(sumSquares / samples.length), peak };
}
```

**Store pattern** (`store.svelte.ts` lines 67-134, 171-203):
```ts
export function createCallStore(options: CallStoreOptions = {}): CallStore {
  let state: CallStateName = options.callId || options.threadId ? 'connecting' : 'idle';
  let transcript: CallTranscriptTurn[] = [];
  let serverMuted = false;

  const cleanupActiveWork = () => {
    options.generation?.abort?.();
    options.playback?.stop?.();
  };

  const store: CallStore = {
    markListening() { state = 'listening'; errorMessage = null; activeAiText = ''; activeAiTurn = null; },
    interruptAiTurn(options?: { source?: 'button' | 'server' }) {
      if (options?.source === 'button') { cleanupActiveWork(); }
      state = 'interrupted';
    },
    setServerMuted(muted: boolean) { serverMuted = muted; },
    failCall(failure?: CallFailure) { cleanupActiveWork(); errorMessage = failure?.publicMessage ?? failure?.code ?? null; state = 'failed'; },
    async flushTransientState() { if (state === 'interrupted') { await Promise.resolve(); store.markListening(); } }
  };
}
```

**Toolbar pattern** (`CallToolbar.svelte` lines 1-17, 24-44, 71-122, 342-357):
```svelte
import { ChevronDown, Mic, MicOff, MoreHorizontal, PhoneOff, RadioTower, Volume2, Zap } from 'lucide-svelte';
import { INPUT_PICKER_UNAVAILABLE_COPY, OUTPUT_PICKER_UNAVAILABLE_COPY } from '$lib/call/audio';

export let muted = false;
export let stateLabel = 'Connecting';
export let ready = false;
export let interruptEnabled = true;
export let onMuteToggle: () => void = () => {};
...
<button aria-label={muteLabel} aria-pressed={muted} title={muteLabel} on:click={onMuteToggle}>
  {#if muted}<MicOff ... />{:else}<Mic ... />{/if}
</button>

@media (max-width: 520px) {
  .icon-control {
    min-width: 44px;
    min-height: 44px;
  }
}
```

**Transcript/visualizer pattern** (`CallTranscript.svelte` lines 1-39, 42-86; `VoiceVisualizer.svelte` lines 4-16, 59-69):
```svelte
$: visibleTurns = turns.filter((turn) => turn.text.trim().length > 0);
$: streamingHtml = renderTrustedMarkdown(activeAiText);

function chipFor(turn: CallTranscriptTurn): 'Final' | 'Streaming' | 'Interrupted' | 'Notice' {
  if (turn.role === 'event') return 'Notice';
  if ((interrupted && turn.role === 'assistant') || turn.interrupted) return 'Interrupted';
  return 'Final';
}

$: stateLabel = labelForState(state);
$: listeningEnergy = normalizeRms(listeningRms);
$: speakingEnergy = normalizeRms(speakingRms);
$: activeEnergy = state === 'speaking' ? speakingEnergy : state === 'listening' ? listeningEnergy : 0;
```

**Testing pattern:** extend existing unit/e2e tests:
```ts
// call-audio.test.ts lines 43-60
expect(ensureRemoteCallAudioAudible(audio)).toBe(true);
expect(audio.muted).toBe(false);
expect(keepCallMicrophoneTracksLive(stream)).toBe(1);

// reconnect-backfill.test.ts lines 112-143
const selection = selectReconnectAudioBackfill(chunks, { endMs, startMs, maxDurationMs, sampleRate });
expect(selection?.startMs).toBe(5000);
expect(selection?.durationMs).toBe(31000);

// call-toolbar.spec.ts lines 17-41
await page.getByRole('button', { name: 'Mute' }).click();
await expect(page.getByRole('button', { name: 'Unmute' })).toBeVisible();
await page.getByRole('button', { name: 'Interrupt' }).click();
await expect(page.getByRole('button', { name: 'Return to Thread' })).toBeVisible();

// call-mobile.spec.ts lines 26-54
const toolbar = page.getByTestId('call-toolbar');
expect(toolbarBox!.y + toolbarBox!.height).toBeLessThanOrEqual(navBox!.y);
expect(box!.height).toBeGreaterThanOrEqual(44);
```

---

### `web-ui/client/tests/e2e/live-call.spec.ts` (test/evidence, live browser)

**Analog:** `web-ui/client/tests/e2e/live-call.spec.ts`

**Live gate setup pattern** (lines 8-42):
```ts
const canonicalLiveWebUrl = 'https://192.168.1.199:8443';
const canonicalLiveAiHealthUrl = 'https://192.168.1.199:9443/health';

test.skip(
  !liveEnabled || !liveWebUrl || !liveAiHealthUrl || !liveReferenceAudioFile || !liveFakeAudioFile,
  'Set RAYME_ENABLE_LIVE_E2E=1, RAYME_LIVE_WEB_URL, RAYME_LIVE_AI_HEALTH_URL, RAYME_LIVE_REFERENCE_AUDIO_FILE, and RAYME_LIVE_FAKE_AUDIO_FILE to run live call acceptance.'
);

test.use({
  ignoreHTTPSErrors: true,
  permissions: ['microphone'],
  launchOptions: { args: ['--use-fake-device-for-media-stream', '--use-fake-ui-for-media-stream', `--use-file-for-fake-audio-capture=${liveFakeAudioFile ?? ''}`] }
});
```

**Live acceptance flow pattern** (lines 44-68, 90-140):
```ts
test('live OMEN-PC browser call completes two user to AI cycles without mocked call media', async ({ page, request: apiRequest }) => {
  test.setTimeout(600_000 + liveStabilityMs);
  expect(liveWebUrl).toBe(canonicalLiveWebUrl);
  expect(liveAiHealthUrl).toBe(canonicalLiveAiHealthUrl);

  const assertNoBrowserErrors = installBrowserErrorGuard(page);
  const liveEvents: string[] = [];
  page.on('request', (request) => {
    expectRayMeApiRequest(request);
    recordLiveCallRequest(request, liveEvents);
  });
  ...
  await expect.poll(() => transcriptTurnCount(page, 'user_speech'), { timeout: 240_000 }).toBeGreaterThanOrEqual(2);
  await expect.poll(() => transcriptTurnCount(page, 'ai_speech'), { timeout: 300_000 }).toBeGreaterThanOrEqual(2);
  ...
  expect(liveEvents).toEqual(expect.arrayContaining([
    'POST /api/calls/start',
    expect.stringMatching(/^POST \/api\/calls\/.+\/offer$/),
    expect.stringMatching(/^POST \/api\/calls\/.+\/mute$/),
    expect.stringMatching(/^POST \/api\/calls\/.+\/turns$/),
    expect.stringMatching(/^POST \/api\/calls\/.+\/end$/)
  ]));
});
```

**Dual-engine gap to fix:** current analog is F5-only. Copy the fixture shape, but parameterize or duplicate it for `voxcpm2` and `f5`:
```ts
// lines 153-162
await apiRequest.patch(`${canonicalLiveWebUrl}/api/settings`, {
  data: {
    web_url: canonicalLiveWebUrl,
    ai_backend_url: 'https://192.168.1.199:9443',
    llm_base_url: localLlmUrl,
    llm_model: localLlmModel,
    llm_api_key: '',
    tts_default_engine: 'f5'
  }
});

// lines 185-191
data: {
  asset_id: asset.asset_id,
  name: `Live Call Voice ${timestamp}`,
  default_engine: 'f5',
  reference_transcript: liveReferenceTranscript,
  metadata: {}
}
```

---

### Evidence artifacts (new `03.1-EVIDENCE.md` and optional `results/*.json`)

**Analogs:** `.planning/phases/03-first-working-call-mvp/OMEN-PC-LIVE-EVIDENCE.md`, `.planning/phases/08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback/08-VERIFICATION.md`, `.planning/phases/08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback/results/voxcpm2-decision.json`

**Human evidence doc pattern** (Phase 3 evidence lines 20-48, 341-358):
````markdown
## Deployment Result

Command:

```text
scripts/deploy-omen.sh
```

Result:

- Deployment used the canonical `scripts/deploy-omen.sh` path.
- Web client production build had already passed on `OMEN-PC`.
- AI backend and Web UI listeners were verified on `192.168.1.199`.

## Current Acceptance Snapshot

- Live desktop browser acceptance: passed.
- Browser secure context: `window.isSecureContext === true`.
- Non-mocked live call spec target: `desktop-chromium` against `https://192.168.1.199:8443`.
- Call writeback: thread returned with `call_start`, at least two `user_speech`, at least two `ai_speech`, and `call_end` rows.

## Android Product-Owner Acceptance

- Android Chrome product-owner acceptance: approved
- Manual target URL: `https://192.168.1.199:8443`
- Microphone permission prompt/grant: passed by product-owner approval.
- AudioContext/audio playback starts from tap: passed by product-owner approval.
````

**Verification report pattern** (Phase 8 verification lines 1-18, 22-44, 100-109):
````markdown
---
phase: 08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback
verified: 2026-05-11T19:34:11Z
status: passed
score: 19/19 must-haves verified
---

## Goal Achievement

The Phase 8 goal is achieved. ...

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 15 | Same-run warm VoxCPM2 median first-audio is lower than same-run warm F5 median first-audio. | VERIFIED | ... |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| AI backend streaming/call/WebRTC contracts pass | `uv run --project ai-backend pytest ... -q` | 77 passed, 3 warnings | PASS |
````

**Machine-readable decision/evidence pattern** (decision JSON lines 1-12):
```json
{
  "final_outcome": "promoted_for_live_call_default",
  "preferred_call_tts_engine": "voxcpm2",
  "fallback_call_tts_engine": "f5",
  "evidence_path": "results/voxcpm2-live-streaming-call-flow.json",
  "voxcpm2_warm_call_ttfa_ms": 762.7,
  "f5_warm_call_ttfa_ms": 948.0,
  "voxcpm2_beats_f5": true,
  "streaming_used": true,
  "whole_wav_fallback_used": false
}
```

For Phase 03.1, use JSON only for runtime-sensitive proof called out by context: engine smoke, mute frame drops, reconnect outcome, and call writeback counts. Keep raw logs temporary and committed evidence sanitized.

---

### `scripts/deploy-omen.sh` (config/deployment, batch)

**Analog:** `scripts/deploy-omen.sh`

**Canonical environment/deploy pattern** (lines 1-9, 20-29):
```bash
#!/usr/bin/env bash
set -euo pipefail

OMEN_SSH_ALIAS="${OMEN_SSH_ALIAS:-rayme-pmpg}"
OMEN_REPO="${OMEN_REPO:-C:\\Users\\pmpg\\rayme\\RayMe}"
OMEN_BRANCH="${OMEN_BRANCH:-main}"
RAYME_OMEN_VERIFY_VOXCPM2="${RAYME_OMEN_VERIFY_VOXCPM2:-0}"

local_head="$(git rev-parse HEAD)"

RAYME_SSH_ALIAS="${OMEN_SSH_ALIAS}" RAYME_SSH_USER="${OMEN_SSH_USER:-pmpg}" \
  "$REPO_ROOT/scripts/bootstrap-rayme-ssh.sh" restore >/dev/null
```

**Health/evidence marker pattern** (lines 336-362):
```powershell
Write-Host "== Verifying health"
$aiHealth = curl.exe -k -sS https://192.168.1.199:9443/health
$webHealth = curl.exe -k -sS https://192.168.1.199:8443/api/settings
$aiStatus = $aiHealth | ConvertFrom-Json
if (-not $aiStatus.stt_ready -or -not $aiStatus.vad_ready -or $aiStatus.resident_tts_engine -ne "f5") {
  throw "AI backend is not ready for live calls"
}
$webStatus = ($webHealth | ConvertFrom-Json).ai_backend_status
if ($webStatus.endpoint_status -match "unreachable" -or $webStatus.resident_tts_engine -ne "f5") {
  throw "Web UI cannot reach the resident F5 AI backend"
}

if ($verifyVoxCpm2) {
  Write-Host "__RAYME_VOXCPM2_RUNTIME_SMOKE_JSON__$($script:VoxCpm2RuntimeSmoke | ConvertTo-Json -Depth 8 -Compress)"
  Write-Host "__RAYME_VOXCPM2_VRAM_SOAK_JSON__$($script:VoxCpm2VramSoak | ConvertTo-Json -Depth 8 -Compress)"
}
```

If Phase 03.1 needs different OMEN health semantics for VoxCPM2 proof, modify this script directly. Do not create alternate deploy scripts, manual scheduled-task edits, or hidden process workarounds.

## Shared Patterns

### Same-Origin Controls
**Source:** `web-ui/server/app/api/calls.py` lines 170-190  
**Apply to:** all Web UI call routes and any new debug/evidence endpoint.

```python
@router.post("/...", dependencies=[Depends(enforce_same_origin_for_calls)])
async def route(...):
    ...
```

### Public Error Sanitization
**Sources:** `web-ui/server/app/domain/call_service.py` lines 38-49, `web-ui/server/app/api/calls.py` lines 772-777, `ai-backend/app/api/webrtc.py` lines 878-889  
**Apply to:** all call server/backend changes.

```python
def _backend_error(exc: AiBackendClientError) -> HTTPException:
    return HTTPException(status_code=502, detail=exc.to_public_dict())

def _offer_error(*, code: str = WEBRTC_OFFER_FAILED, message: str = WEBRTC_OFFER_FAILED_MESSAGE) -> HTTPException:
    return HTTPException(status_code=status.HTTP_502_BAD_GATEWAY, detail={"code": code, "message": message})
```

### Bounded Validation
**Sources:** `web-ui/server/app/api/calls.py` lines 108-120, `ai-backend/app/api/webrtc.py` lines 96-107  
**Apply to:** reconnect, control, diagnostic, and evidence-facing payloads.

```python
model_config = ConfigDict(extra="forbid")
pcm_b64: str = Field(default="", max_length=4_000_000)
sample_rate: int = Field(default=16000, ge=8000, le=48000)
attempt: int | None = Field(default=None, ge=0, le=10)
final: bool = True
```

### Reconnect Cleanup Order
**Sources:** `web-ui/client/src/routes/call/[threadId]/+page.svelte` lines 554-585, 1771-1810; `web-ui/server/app/api/calls.py` lines 573-593; `ai-backend/app/call/session.py` lines 2021-2040  
**Apply to:** all reconnect/drop/end-call fixes.

Required order:
1. Flush reconnect audio backfill.
2. Recover missed backend events.
3. End the call truthfully.
4. Preserve durable `call_end` writeback unless backend misconfiguration is proven.

### Diagnostics
**Sources:** `web-ui/server/app/api/calls.py` lines 600-624, `web-ui/client/src/routes/call/[threadId]/+page.svelte` lines 871-933  
**Apply to:** browser/WebRTC diagnostics.

Keep diagnostics behavior-neutral and bounded. Server debug route logs only, does not write DB rows, does not call backend, and truncates serialized detail to 800 characters.

### Dual TTS Engine Proof
**Sources:** `web-ui/client/tests/e2e/live-call.spec.ts` lines 153-191, Phase 8 decision lines 1-21, decision JSON lines 1-12  
**Apply to:** any touched speak/playback/interrupt/end/writeback path.

The current live spec is F5-only. Phase 03.1 must add live behavioral smoke for both `voxcpm2` and `f5`, or document a proven unrelated blocker.

### OMEN Deployment
**Sources:** `AGENTS.md`, `scripts/deploy-omen.sh` lines 1-60 and 336-362  
**Apply to:** all live verification.

Use only:
```text
scripts/deploy-omen.sh
```

Do not create ad-hoc OMEN launchers/scripts, manually edit scheduled tasks, or use hidden process launch workarounds.

## No Analog Found

None. Every candidate file has an existing exact or role-match analog. For optional Phase 03.1 machine-readable result files, copy the Phase 8 JSON artifact shape and keep fields limited to runtime-sensitive acceptance proof.

## Metadata

**Analog search scope:** `ai-backend/app`, `ai-backend/tests`, `web-ui/server/app`, `web-ui/server/tests`, `web-ui/client/src`, `web-ui/client/tests`, `.planning/phases/03-*`, `.planning/phases/08-*`, `scripts/deploy-omen.sh`  
**Files scanned:** 38 source/test/evidence files by direct read or targeted grep  
**Pattern extraction date:** 2026-05-12  
**Project instructions applied:** `AGENTS.md` read; root `CLAUDE.md` and project `.claude/skills/` / `.agents/skills/` `SKILL.md` files were not found by `rg --files`.
