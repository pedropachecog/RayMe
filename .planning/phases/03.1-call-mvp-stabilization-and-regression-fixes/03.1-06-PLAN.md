---
phase: 03.1-call-mvp-stabilization-and-regression-fixes
plan: 06
type: execute
wave: 3
depends_on:
  - 03.1-05
files_modified:
  - scripts/deploy-omen.sh
  - .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-EVIDENCE.md
  - .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/results/03.1-live-dual-engine.json
autonomous: false
requirements:
  - REQ-40
  - REQ-47
  - REQ-48
  - REQ-49
  - REQ-50
  - REQ-63
  - REQ-A0
  - REQ-A1
  - P8-DUAL-ENGINE
must_haves:
  truths:
    - "OMEN live desktop verification uses only `scripts/deploy-omen.sh`."
    - "Live desktop evidence behaviorally smokes both `voxcpm2` preferred/default and `f5` fallback."
    - "Android Chrome product-owner acceptance is requested only after local and OMEN desktop checks pass."
    - "Committed live evidence is sanitized and contains no secrets, raw paths, or tracebacks."
  artifacts:
    - path: "scripts/deploy-omen.sh"
      provides: "Canonical OMEN deployment and health verification path"
    - path: ".planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/results/03.1-live-dual-engine.json"
      provides: "Machine-readable dual-engine live smoke result"
    - path: ".planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-EVIDENCE.md"
      provides: "Human-readable OMEN and Android evidence"
  key_links:
    - from: "scripts/deploy-omen.sh"
      to: "OMEN scheduled tasks"
      via: "canonical launchers start-ai-backend.cmd and start-web-ui.cmd"
      pattern: "start-ai-backend\\.cmd|start-web-ui\\.cmd"
    - from: "web-ui/client/tests/e2e/live-call.spec.ts"
      to: "results/03.1-live-dual-engine.json"
      via: "RAYME_LIVE_TTS_ENGINES=voxcpm2,f5"
      pattern: "voxcpm2|f5"
---

<objective>
Deploy through the canonical OMEN flow, run dual-engine live desktop evidence, and collect required Android product-owner acceptance.

Purpose: close Phase 03.1 with live LAN/GPU proof and physical Android acceptance for mobile-sensitive call paths.
Output: `03.1-live-dual-engine.json`, updated `03.1-EVIDENCE.md`, and an Android checkpoint result.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@scripts/deploy-omen.sh
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/OPERATING-NOTES.md
@.planning/LEARNINGS.md
@.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-CONTEXT.md
@.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-VALIDATION.md
@.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-EVIDENCE.md
@.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/results/03.1-local-regression.json
@web-ui/client/tests/e2e/live-call.spec.ts
@.planning/phases/08-wire-voxcpm2-streaming-chunks-into-live-rayme-call-playback/results/voxcpm2-decision.json

<interfaces>
Canonical live command:
```text
RAYME_ENABLE_LIVE_E2E=1
RAYME_LIVE_WEB_URL=https://192.168.1.199:8443
RAYME_LIVE_AI_HEALTH_URL=https://192.168.1.199:9443/health
RAYME_LIVE_TTS_ENGINES=voxcpm2,f5
npm --prefix web-ui/client run test:e2e -- tests/e2e/live-call.spec.ts --project=desktop-chromium --reporter=line
```

OMEN deployment rule:
```text
scripts/deploy-omen.sh
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify canonical OMEN deploy supports Phase 03.1 live proof</name>
  <files>scripts/deploy-omen.sh</files>
  <read_first>
    scripts/deploy-omen.sh
    AGENTS.md
    .planning/OPERATING-NOTES.md
    .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-RESEARCH.md
    .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/results/03.1-defect-triage.json
  </read_first>
  <action>Inspect the current `scripts/deploy-omen.sh` health guard before deploying. If the existing F5 resident-engine guard allows dual-engine proof by keeping F5 resident while live calls create a VoxCPM2 voice/default for the `voxcpm2` smoke, do not modify the script; record that decision in `03.1-EVIDENCE.md`. If the guard blocks correct Phase 8 default proof, modify `scripts/deploy-omen.sh` itself, not an ad-hoc script, to add `RAYME_OMEN_EXPECT_RESIDENT_TTS_ENGINE="${RAYME_OMEN_EXPECT_RESIDENT_TTS_ENGINE:-f5}"` with allowed values exactly `f5` and `voxcpm2`, and compare both AI health and Web UI bridge `resident_tts_engine` to that variable. Keep canonical launcher generation for `start-ai-backend.cmd` and `start-web-ui.cmd` inside this script only. Do not create `.ps1`, `.cmd`, or `.sh` deployment files outside the repo; do not run `schtasks /Create` or `schtasks /Delete` manually; do not use `Start-Process -WindowStyle Hidden` as a workaround.</action>
  <verify>
    <automated>bash -n scripts/deploy-omen.sh</automated>
    <automated>rg -n "start-ai-backend\\.cmd|start-web-ui\\.cmd|resident_tts_engine|RAYME_OMEN_EXPECT_RESIDENT_TTS_ENGINE|f5|voxcpm2" scripts/deploy-omen.sh</automated>
  </verify>
  <acceptance_criteria>
    `bash -n scripts/deploy-omen.sh` exits 0.
    `scripts/deploy-omen.sh` contains `start-ai-backend.cmd`, `start-web-ui.cmd`, and `resident_tts_engine`.
    If `RAYME_OMEN_EXPECT_RESIDENT_TTS_ENGINE` is added, `scripts/deploy-omen.sh` contains both `f5` and `voxcpm2` allowed value handling.
    No new deployment script or launcher file outside `scripts/deploy-omen.sh` is created.
  </acceptance_criteria>
  <done>The canonical OMEN deploy path is confirmed or fixed in-place for Phase 03.1 live evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Run OMEN live desktop dual-engine evidence</name>
  <files>.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/results/03.1-live-dual-engine.json, .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-EVIDENCE.md</files>
  <read_first>
    .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-EVIDENCE.md
    .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/results/03.1-local-regression.json
    web-ui/client/tests/e2e/live-call.spec.ts
    scripts/deploy-omen.sh
    .planning/OPERATING-NOTES.md
  </read_first>
  <action>Deploy to OMEN only by running `scripts/deploy-omen.sh` from the repo root after the target commit is reachable to OMEN through the normal Git path. Then run the live desktop Playwright command against `https://192.168.1.199:8443` and `https://192.168.1.199:9443/health` with `RAYME_ENABLE_LIVE_E2E=1`, `RAYME_LIVE_TTS_ENGINES=voxcpm2,f5`, the existing live reference/fake audio file env vars, and `--project=desktop-chromium --reporter=line`. Create `results/03.1-live-dual-engine.json` with exact top-level keys `phase`, `commit`, `deployed_commit`, `generated_at`, `web_url`, `ai_health_url`, `engines`, `overall_status`, and `sanitization`. The `engines` object must contain keys `voxcpm2` and `f5`; each must contain `status`, `call_started`, `user_speech_count`, `ai_speech_count`, `call_end_written`, `mute_exercised`, `route_surface`, and `notes`. Set `route_surface` to `existing /api/calls route` for both engines. Set `overall_status` to `passed` only when both engines pass. Update `03.1-EVIDENCE.md` under `## OMEN Live Desktop Evidence` and `## Dual-Engine Live Smoke` with the deploy command `scripts/deploy-omen.sh`, deployed commit, target URLs, live command, per-engine pass/fail, and sanitized notes. If either engine fails, stop phase completion unless the failure is proven unrelated to Phase 03.1 and documented with `overall_status: "blocked_unrelated"` plus a concise OMEN setup/runtime follow-up per D-12.</action>
  <verify>
    <automated>python3 -m json.tool .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/results/03.1-live-dual-engine.json</automated>
    <automated>rg -n "\"overall_status\": \"passed\"|\"voxcpm2\"|\"f5\"|\"route_surface\": \"existing /api/calls route\"|scripts/deploy-omen.sh|https://192.168.1.199:8443" .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/results/03.1-live-dual-engine.json .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    `results/03.1-live-dual-engine.json` is valid JSON and contains `"overall_status": "passed"`.
    `results/03.1-live-dual-engine.json` contains engine keys `"voxcpm2"` and `"f5"`, and both contain `"call_end_written": true`.
    `03.1-EVIDENCE.md` contains `scripts/deploy-omen.sh`, `RAYME_LIVE_TTS_ENGINES=voxcpm2,f5`, `https://192.168.1.199:8443`, `voxcpm2`, and `f5`.
    No committed evidence contains `Traceback`, `BEGIN PRIVATE KEY`, `api_key`, `token=`, `C:\\Users\\pmpg\\rayme\\phase1-tls\\*.key`, `/home/`, or `model-cache`.
  </acceptance_criteria>
  <done>OMEN live desktop evidence proves both `voxcpm2` and `f5` through the existing RayMe call flow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Android Chrome product-owner acceptance</name>
  <files>.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-EVIDENCE.md</files>
  <read_first>
    .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-EVIDENCE.md
    .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/results/03.1-local-regression.json
    .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/results/03.1-live-dual-engine.json
    .planning/OPERATING-NOTES.md
  </read_first>
  <what-built>Local focused call tests passed, OMEN desktop live verification passed, and mobile-sensitive Phase 03.1 call paths were changed: mic permission, call startup, reconnect/drop handling, audio playback path, and mobile controls.</what-built>
  <action>Ask the product owner to test physical Android Chrome only after Task 2 passes. The verification script is: open `https://192.168.1.199:8443` on the already trusted Android Chrome device; start a call from an existing chat thread; grant microphone permission if prompted; confirm the call reaches listening; speak at least two turns; confirm two audible AI responses; toggle Mute then Unmute; end the call; confirm the thread shows `call_start`, at least two `user_speech`, at least two `ai_speech`, and `call_end`; confirm controls are not blocked by the mobile bottom nav. Record the response in `03.1-EVIDENCE.md` under `## Android Product-Owner Acceptance` with exact status `approved` or `failed`, timestamp, target URL, and any issue text. Do not ask the user to install a new CA unless the reusable Phase 1 root CA actually changed.</action>
  <verify>
    <automated>rg -n "Android Chrome product-owner acceptance: (approved|failed)|https://192.168.1.199:8443|call_start|user_speech|ai_speech|call_end" .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    `03.1-EVIDENCE.md` contains `Android Chrome product-owner acceptance: approved` before the phase is called complete.
    `03.1-EVIDENCE.md` contains `https://192.168.1.199:8443`, `call_start`, `user_speech`, `ai_speech`, and `call_end` in the Android section.
    If the user reports failure, `03.1-EVIDENCE.md` contains `Android Chrome product-owner acceptance: failed` and this plan stops for follow-up fixes instead of marking success.
  </acceptance_criteria>
  <resume-signal>Type `approved` after the Android checklist passes, or describe the exact failure observed.</resume-signal>
  <done>Physical Android Chrome acceptance is recorded after agent-run local and OMEN desktop verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo -> OMEN-PC runtime | Committed code is deployed to Windows services and scheduled tasks. |
| OMEN browser -> Web UI/AI backend | Live non-mocked Playwright exercises LAN HTTPS call behavior. |
| Android Chrome -> Web UI/AI backend | Physical device verifies secure-context microphone and playback behavior. |
| Live logs/results -> committed evidence | Runtime details are summarized in markdown and JSON. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-06-01 | Tampering | OMEN deployment | mitigate | Use only `scripts/deploy-omen.sh`; if behavior is missing, fix that script and run `bash -n`. |
| T-03.1-06-02 | Elevation of Privilege | scheduled tasks/launchers | mitigate | Do not manually run `schtasks /Create` or `/Delete`; canonical script owns `start-ai-backend.cmd` and `start-web-ui.cmd`. |
| T-03.1-06-03 | Information Disclosure | live evidence | mitigate | Evidence JSON/markdown forbids private keys, API keys, tokens, raw tracebacks, local secret paths, and model-cache internals. |
| T-03.1-06-04 | Denial of Service | runtime mismatch/CPU fallback | mitigate | Canonical deploy script CUDA/runtime guards are preserved; live evidence records AI health and engine status. |
| T-03.1-06-05 | Spoofing | browser mic on insecure context | mitigate | Live and Android verification target `https://192.168.1.199:8443`; Android uses the existing trusted certificate workflow. |
| T-03.1-06-06 | Repudiation | Android acceptance | mitigate | Product-owner result is recorded with exact status, target URL, checklist evidence, and timestamp after automated checks pass. |
</threat_model>

<verification>
Run:
- `bash -n scripts/deploy-omen.sh`
- `scripts/deploy-omen.sh`
- Live Playwright with `RAYME_ENABLE_LIVE_E2E=1`, `RAYME_LIVE_WEB_URL=https://192.168.1.199:8443`, `RAYME_LIVE_AI_HEALTH_URL=https://192.168.1.199:9443/health`, `RAYME_LIVE_TTS_ENGINES=voxcpm2,f5`, reference/fake audio env vars, and `npm --prefix web-ui/client run test:e2e -- tests/e2e/live-call.spec.ts --project=desktop-chromium --reporter=line`
- `python3 -m json.tool .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/results/03.1-live-dual-engine.json`
- `rg -n "Android Chrome product-owner acceptance: approved|overall_status.*passed|voxcpm2|f5" .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes`
- `git diff --check`
</verification>

<success_criteria>
Phase 03.1 live acceptance is complete when canonical OMEN deployment succeeds, both `voxcpm2` and `f5` pass non-mocked desktop live call smoke, sanitized evidence is committed, and Android Chrome product-owner approval is recorded for the touched mobile-sensitive call paths.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-06-SUMMARY.md`.
</output>
