# Phase 03.1 Evidence

Committed Phase 03.1 evidence is intentionally sanitized. Raw logs, API keys,
tokens, passwords, TLS private key material, absolute local reference-audio
paths, model cache paths, and tracebacks must stay out of this file and out of
the `results/` JSON artifacts.

## Source Triage

Wave 0 selected the stabilization defects from Phase 3 live evidence, Phase 8
VoxCPM2 promotion evidence, the Phase 03.1 discussion and validation artifacts,
and every listed open phone-call debug session.

Primary machine-readable triage:

- `results/03.1-defect-triage.json`

Selected defects:

- `CALL-STAB-001`: dual-engine live smoke for `voxcpm2` preferred/default and
  `f5` fallback.
- `CALL-STAB-002`: Phase 03.1 evidence artifact and runtime-sensitive result
  map.
- `CALL-STAB-003`: canonical deploy-script F5 resident-engine guard review.
- `CALL-STAB-004`: reconnect/drop terminal-state proof.
- `CALL-STAB-005`: phone-call missing speech chunks across long turns and
  pauses.
- `CALL-STAB-006`: reconnect PCM gap/tail selection, including no omitted
  browser-captured reconnect span like `35256-69467ms`.
- `CALL-STAB-007`: false browser-facing 502/proxy-timeout behavior after
  backend reconnect backfill was actually applied.
- `CALL-STAB-008`: late STT `user_final` recovery after `/end` or closed data
  channel.
- `CALL-STAB-009`: post-snapshot do-not-retry guard.

## Open Debug Session Coverage

The triage file includes all required phone-call debug inputs:

- `.planning/debug/phone-calls-missing-chunks.md`
- `.planning/debug/phone-call-transcript-comparison.md`
- `.planning/debug/phone-call-repro-2026-04-30.md`
- `.planning/debug/phone-call-repro-1239588-2026-04-30.md`
- `.planning/debug/phone-call-repro-2360feb-2026-04-30.md`
- `.planning/debug/phone-call-repro-dff6545-2026-04-30.md`
- `.planning/debug/phone-call-repro-e4b93d9-2026-04-30.md`
- `.planning/debug/phone-call-expected-poem-2026-05-02.md`
- `.planning/debug/phone-calls-post-snapshot-audit.md`

These sources show that the current urgent issue is not ordinary accent-based
word substitutions. The target failure class is phone-call missing speech chunks
where complete spoken spans disappear before or during reconnect/backfill/STT
recovery.

## Local Regression Baseline

Baseline commands run before creating the Wave 0 evidence files:

| Command | Result |
|---|---|
| `uv run --project ai-backend pytest ai-backend/tests/test_call_session.py ai-backend/tests/test_webrtc_signaling.py -q` | PASS: 66 passed, 3 warnings |
| `uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py web-ui/server/tests/test_prompt_builder.py -q` | PASS: 40 passed |
| `npm --prefix web-ui/client run test:unit -- --run tests/unit/call-audio.test.ts tests/unit/call-state.test.ts tests/unit/reconnect-backfill.test.ts` | PASS: 3 files passed, 12 tests passed |

## Runtime-Sensitive JSON Artifacts

Planned runtime-sensitive artifacts are listed in `results/README.md`:

- `03.1-local-regression.json`
- `03.1-live-dual-engine.json`
- `03.1-mute-frame-drops.json`
- `03.1-reconnect-outcome.json`
- `03.1-missing-chunks.json`
- `03.1-writeback-counts.json`

These should record structured pass/fail fields, relevant commit IDs, sanitized
engine IDs, counts, and measured behavior. They should not store raw logs or
sensitive runtime paths.

## Phone-Call Missing Chunks Evidence

The missing-chunks acceptance target is that no browser-captured reconnect span
is omitted, including the historical gap `35256-69467ms`.

Evidence summary:

- Expected-versus-actual transcript comparison shows long turns losing whole
  spans, especially around long passages and pauses.
- Later reconnect diagnostics showed the browser had live microphone audio
  during reconnect while backend STT received only partial audio.
- The most concrete selected gap showed reconnect backfill covering
  `5226-35256ms`, then `69467-99412ms`, with no request covering
  `35256-69467ms`.
- Phase 03.1 must prove the beginning, middle, and ending of long spoken turns
  remain available to STT and durable writeback after reconnect/backfill paths.

## OMEN Live Desktop Evidence

Phase 3 live desktop evidence passed on OMEN with F5:

- Deployed commit: `e48e2ce57cc31a30e7df97c1f0ea9215c136dc45`
- Web URL: `https://192.168.1.199:8443`
- AI health URL: `https://192.168.1.199:9443/health`
- Live spec result: `1 passed (6.1m)`
- Five-minute stability line recorded at two or more user and AI turns before
  and after the hold.
- Runtime setting during that acceptance: `tts_default_engine=f5`.

Phase 03.1 live evidence must use the canonical OMEN deployment script and must
not create alternate launchers, scheduled-task edits, or ad-hoc deployment
scripts.

## Dual-Engine Live Smoke

Phase 8 promoted `voxcpm2` as the preferred/default live-call TTS engine and
kept `f5` as fallback/comparator. Wave 0 records that the existing live browser
spec must smoke both engines through the same RayMe call route:

- default engine set to `voxcpm2` and `f5` per run;
- VoxCPM2 voice metadata set under `metadata.engine_settings.voxcpm2`;
- same-origin routes preserved: `/api/calls/start`, `/offer`, `/mute`,
  `/turns`, and `/end`;
- no browser-visible VoxCPM2-specific route.

The planned result artifact is `results/03.1-live-dual-engine.json`.

## Android Product-Owner Acceptance

Android Chrome product-owner acceptance is required after agent-run local and
OMEN desktop gates when changes touch mobile Chrome, mic permission, call
startup, audio playback, or layout.

For the missing-chunks target, the product-owner acceptance must confirm a long
spoken passage with a pause preserves beginning, middle, and ending transcript
content after the automated `03.1-missing-chunks.json` and OMEN desktop gates
pass.

## Deferred Out Of Scope

Phase 4 and broader call-feel scope remain deferred:

- VAD-driven barge-in
- echo-loop mitigation
- TTFA target optimization
- advanced call-feel tuning
- full visualizer polish
- new in-call features
- redesigned user-visible reconnect feature
- broad call UI redesign

small recovery/status/copy fixes are allowed only when needed for truthful current MVP stabilization.

Selected defects may only use these stabilization capabilities:

- startup
- negotiation
- reconnect_drop_backfill
- missing_chunks
- reconnect_pcm_gap
- late_user_final_recovery
- proxy_timeout
- mute
- interrupt
- end_call
- tts_playback_smoke
- durable_writeback
- diagnostics
- local_regression
- omen_live_desktop
- android_acceptance
- dual_engine_smoke

## Security Hygiene

Evidence and diagnostics in committed files must stay sanitized:

- no API keys, tokens, passwords, or TLS private key material;
- no raw logs or tracebacks;
- no absolute local reference-audio paths;
- no model cache paths;
- no broad diagnostic dumping that creates unbounded log spam.

Every selected triage item carries `requirements`, `context_decisions`,
`evidence_source`, and `phase4_scope: false` so future plans can trace the
stabilization reason without adding Phase 4 scope.
