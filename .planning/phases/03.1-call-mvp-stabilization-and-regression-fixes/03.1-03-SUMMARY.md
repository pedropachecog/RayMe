---
phase: 03.1-call-mvp-stabilization-and-regression-fixes
plan: 03
subsystem: api
tags: [fastapi, calls, webrtc, reconnect, diagnostics, pytest]

requires:
  - phase: 03.1-call-mvp-stabilization-and-regression-fixes
    provides: Wave 0 defect triage and AI backend call lifecycle regressions
provides:
  - Web UI server call facade regressions for same-origin route ownership, reconnect backfill proxy behavior, debug diagnostics, and dual-engine route ownership
  - Bounded browser call diagnostic logging with the truncation marker inside the 800-character cap
  - Reconfirmed call prompt context, reconnect-audio no-persistence behavior, and VoxCPM2/F5 turn routing through existing `/api/calls/{call_id}/turns`
affects: [phase-03.1, web-ui-server, call-facade, reconnect, prompt-context]

tech-stack:
  added: []
  patterns:
    - RED pytest regression before bounded diagnostics fix
    - Same-origin route matrix for unsafe call controls
    - Behavior-neutral server diagnostic route assertions

key-files:
  created:
    - .planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-03-SUMMARY.md
  modified:
    - web-ui/server/tests/test_calls.py
    - web-ui/server/app/api/calls.py

key-decisions:
  - "Browser call diagnostics remain behavior-neutral and logged only, with serialized detail capped to 800 characters including the truncation marker."
  - "VoxCPM2 and F5 call turns continue using the existing same-origin `/api/calls/{call_id}/turns` surface; no engine-specific call route was added."

patterns-established:
  - "Unsafe call route same-origin checks are covered as a parametrized route suffix matrix."
  - "Reconnect-audio proxy timeout behavior is locked by Web UI call facade tests against the AI backend client's `_webrtc_timeout` path."

requirements-completed: [REQ-40, REQ-47, REQ-50, REQ-63, P8-DUAL-ENGINE]

duration: 12min
completed: 2026-05-12
---

# Phase 03.1 Plan 03: Web UI Server Call Facade Stabilization Summary

**Same-origin Web UI call facade regressions with bounded behavior-neutral diagnostics and reconnect backfill timeout coverage**

## Performance

- **Duration:** 12 min
- **Started:** 2026-05-12T13:32:36Z
- **Completed:** 2026-05-12T13:44:06Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added server regressions proving unsafe call POST routes reject foreign origins with `call_origin_not_allowed`.
- Locked reconnect-audio behavior so backfill forwards to the AI backend without creating `user_speech` or `ai_speech` rows, and the backend client uses `_webrtc_timeout`.
- Added behavior-neutral debug event coverage: no DB rows, no backend calls, fixed success payload, and serialized detail capped to 800 characters.
- Reconfirmed VoxCPM2 and F5 call turns use the existing `/turns` route and no `voxcpm2` route path exists in the call facade.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add server RED regressions for facade ownership and sanitization** - `66d9a6d` (test)
2. **Task 2: Implement server call facade stabilization** - `544aa1c` (fix)

**Plan metadata:** recorded in the final plan closeout commit.

_Note: Task 1 followed the RED gate. The focused server suite failed on `test_debug_event_truncates_detail_and_is_behavior_neutral` because logged debug detail was 814 characters, then passed after Task 2._

## Files Created/Modified

- `web-ui/server/tests/test_calls.py` - Added unsafe route origin matrix, debug route behavior-neutral assertions, reconnect timeout coverage, and F5 route ownership coverage.
- `web-ui/server/app/api/calls.py` - Keeps the debug truncation marker inside the 800-character serialized-detail cap.
- `.planning/phases/03.1-call-mvp-stabilization-and-regression-fixes/03.1-03-SUMMARY.md` - Plan execution summary.

## Decisions Made

- Kept the existing browser-facing call route surface under `/api/calls`; no engine-specific `voxcpm2` route was introduced.
- Treated debug event logging as useful live-call observability only: bounded, logged, behavior-neutral, and not persisted.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

The RED suite failed as intended on the diagnostic bound: `_debug/event` truncated to 800 characters and then appended `...<truncated>`, producing an 814-character logged detail. The production fix keeps the marker inside the 800-character cap.

`gsd-sdk query state.advance-plan`, `state.update-progress`, `state.record-metric`, and `state.add-decision` could not update this project's current STATE.md format. `state.record-session` and `roadmap.update-plan-progress` worked; I updated the missing STATE.md phase-status and decision lines directly. `requirements.mark-complete` reported the plan requirement IDs as not found because this project requirements file is plain requirement text rather than a checkbox list.

## Verification

- `uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py web-ui/server/tests/test_prompt_builder.py -q` - PASS, 52 passed.
- `rg -n "enforce_same_origin_for_calls|call_origin_not_allowed|_debug/event|Call ended|build_call_prompt_context|max_turns=24|_webrtc_timeout" web-ui/server/app web-ui/server/tests` - PASS.
- `rg -n "voxcpm2" web-ui/server/app/api/calls.py` - PASS, no matches, confirming no `voxcpm2` route path in the facade.
- `git diff --check` - PASS.

## Known Stubs

None. Stub scan found only intentional empty test call-list accumulators, Pydantic defaults, and local string accumulators.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for plan `03.1-04`: the Web UI server facade now has focused local regressions for same-origin ownership, reconnect timeout behavior, durable writeback boundaries, prompt context, and bounded diagnostics.

## Self-Check: PASSED

- Created files exist: `03.1-03-SUMMARY.md`.
- Modified source/test files exist: `web-ui/server/app/api/calls.py` and `web-ui/server/tests/test_calls.py`.
- Task commits exist in git history: `66d9a6d` and `544aa1c`.

---
*Phase: 03.1-call-mvp-stabilization-and-regression-fixes*
*Completed: 2026-05-12*
