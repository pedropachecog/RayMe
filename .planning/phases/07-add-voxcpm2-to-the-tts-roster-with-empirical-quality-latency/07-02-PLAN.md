---
phase: 07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - web-ui/server/tests/test_voices.py
  - web-ui/client/tests/unit/voice-lab.test.ts
  - web-ui/client/tests/e2e/voice-lab.spec.ts
autonomous: true
requirements:
  - REQ-20
  - REQ-21
  - REQ-22
  - REQ-23
  - REQ-24
  - REQ-80
must_haves:
  truths:
    - "A VoxCPM2 voice stores mode/style metadata and reuses it for preview and test-play."
    - "VoxCPM2 controls are conditional and do not change non-VoxCPM2 Voice Lab UX."
    - "Disconnected client fallback rosters still render VoxCPM2."
  artifacts:
    - path: "web-ui/server/tests/test_voices.py"
      provides: "Server-side voice metadata and payload RED tests"
    - path: "web-ui/client/tests/unit/voice-lab.test.ts"
      provides: "Client Voice Lab conditional-control RED tests"
    - path: "web-ui/client/tests/e2e/voice-lab.spec.ts"
      provides: "Browser-level Voice Lab RED coverage"
  key_links:
    - from: "web-ui/client/src/routes/voice-lab/+page.svelte"
      to: "web-ui/server/app/api/voices.py"
      via: "save/preview payload metadata"
      pattern: "engine_settings.*voxcpm2"
---

<objective>
Create the RED Web UI server and client contracts for durable VoxCPM2 voice metadata and conditional controls.

Purpose: lock D-05 through D-10 and D-22 before server/client implementation.
Output: failing tests for `metadata_json.engine_settings.voxcpm2`, preview/test-play payloads, fallback roster labels, missing-transcript warning, and engine-switch preservation.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/OPERATING-NOTES.md
@.planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-CONTEXT.md
@.planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-VALIDATION.md
@.planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-PATTERNS.md
@web-ui/server/app/api/voices.py
@web-ui/server/app/domain/voice_service.py
@web-ui/client/src/routes/voice-lab/+page.svelte
@web-ui/client/src/lib/components/voice/TtsEnginePicker.svelte
@web-ui/client/src/lib/components/voice/VoiceAssignmentSelect.svelte
@web-ui/client/src/lib/api/types.ts
@web-ui/client/src/lib/api/voices.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add server voice metadata RED tests</name>
  <files>web-ui/server/tests/test_voices.py</files>
  <read_first>
    web-ui/server/tests/test_voices.py
    web-ui/server/app/api/voices.py
    web-ui/server/app/domain/voice_service.py
    web-ui/server/app/storage/models.py
    .planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-CONTEXT.md
  </read_first>
  <action>Add RED tests requiring save/update of `metadata.engine_settings.voxcpm2` with exact fields: `cloning_mode` enum values `reference_only` and `transcript_guided`, `style_prompt` max 300 chars, `cfg_value` bounded `1.0..3.0`, `inference_timesteps` bounded `4..30`, `normalize` boolean, `denoise` boolean. Add preview and test-play assertions that payloads sent to the AI backend include these fields for `engine_id="voxcpm2"`, include warning code `voxcpm2_reference_only_without_transcript` when transcript is blank, and omit/ignore VoxCPM2 fields when the selected engine is not `voxcpm2` per D-06/D-07/D-08/D-10.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_voices.py -q</automated>
  </verify>
  <acceptance_criteria>
    Test names include `test_voxcpm2_voice_metadata_persists_mode_and_style`, `test_voxcpm2_preview_payload_uses_reference_only_warning_without_transcript`, and `test_voxcpm2_settings_are_ignored_for_non_voxcpm2_engine`.
    Assertions grep for exact keys `engine_settings`, `voxcpm2`, `cloning_mode`, `style_prompt`, `cfg_value`, and `inference_timesteps`.
  </acceptance_criteria>
  <done>Server-side Voice Lab RED tests describe durable VoxCPM2 metadata and bounded payload behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add client conditional-control RED tests</name>
  <files>web-ui/client/tests/unit/voice-lab.test.ts, web-ui/client/tests/e2e/voice-lab.spec.ts</files>
  <read_first>
    web-ui/client/tests/unit/voice-lab.test.ts
    web-ui/client/tests/e2e/voice-lab.spec.ts
    web-ui/client/src/routes/voice-lab/+page.svelte
    web-ui/client/src/lib/components/voice/TtsEnginePicker.svelte
    web-ui/client/src/lib/components/voice/VoiceAssignmentSelect.svelte
    web-ui/client/src/lib/api/types.ts
    web-ui/client/src/lib/api/voices.ts
  </read_first>
  <action>Add RED client tests requiring fallback roster entry `voxcpm2` with label `VoxCPM2`, caveats `Candidate`, `48 kHz`, `RTX 3060 gate pending`, and conditional controls that render only when `selectedEngine === "voxcpm2"`. Cover radio labels `Reference only` and `Transcript guided`, missing-transcript warning text `Transcript-guided mode may improve VoxCPM2 results`, style input max length 300, numeric bounds for `cfg_value` and `inference_timesteps`, and preserving `engine_settings.voxcpm2` after switching away from VoxCPM2 and back per D-09/D-10/D-22.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- voice-lab && npm --prefix web-ui/client run test:e2e -- voice-lab</automated>
  </verify>
  <acceptance_criteria>
    Unit test names include `renders VoxCPM2 controls only for VoxCPM2` and `saves VoxCPM2 engine_settings while preserving non-VoxCPM2 UX`.
    E2E evidence asserts no visible VoxCPM2 controls when `F5-TTS` is selected and visible controls when `VoxCPM2` is selected.
    Recorded save payload includes `metadata.engine_settings.voxcpm2.cloning_mode`.
  </acceptance_criteria>
  <done>Client RED tests lock conditional controls, fallback metadata, and engine-switch preservation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Web UI server voice routes | User-provided transcript, style text, and numeric controls cross into durable storage. |
| Web UI server -> AI backend synthesis payload | Stored metadata becomes runtime input for model synthesis. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering | uploaded sample metadata | mitigate | Tests require existing asset-id blob storage and no original filename path use. |
| T-07-04 | Denial of Service | style/control payloads | mitigate | RED tests require max lengths and numeric bounds before server/client implementation. |
| T-07-06 | Repudiation | manual voice evidence linkage | mitigate | Tests require saved metadata to be deterministic so generated samples can be traced to voice settings. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests/test_voices.py -q`, `npm --prefix web-ui/client run test:unit -- voice-lab`, and `npm --prefix web-ui/client run test:e2e -- voice-lab`; record expected RED failures in the summary.
</verification>

<success_criteria>
Voice Lab server and browser behavior for VoxCPM2 is locked by executable failing tests before implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-02-SUMMARY.md`.
</output>
