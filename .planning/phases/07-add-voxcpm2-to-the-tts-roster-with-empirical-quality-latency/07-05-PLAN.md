---
phase: 07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency
plan: 05
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - ai-backend/pyproject.toml
  - ai-backend/app/models/tts_registry.py
  - ai-backend/app/models/engine_metadata.py
  - ai-backend/app/models/tts_voxcpm2.py
  - ai-backend/app/api/tts.py
  - ai-backend/tests/test_tts_voxcpm2.py
  - ai-backend/tests/test_tts_registry.py
  - ai-backend/tests/test_model_manager.py
autonomous: true
requirements:
  - REQ-02
  - REQ-22
  - REQ-45
  - REQ-80
  - REQ-A3
must_haves:
  truths:
    - "VoxCPM2 appears in AI backend metadata even when its optional package is absent."
    - "VoxCPM2 loads only through CUDA and never through CPU fallback."
    - "The transient synthesis API accepts bounded VoxCPM2 options and returns sanitized errors."
  artifacts:
    - path: "ai-backend/app/models/tts_voxcpm2.py"
      provides: "VoxCPM2 import-gated adapter"
    - path: "ai-backend/app/models/tts_registry.py"
      provides: "Roster metadata and synthesis input contract"
    - path: "ai-backend/app/api/tts.py"
      provides: "Bounded `/tts/synthesize` VoxCPM2 option bridge"
  key_links:
    - from: "ai-backend/app/api/tts.py"
      to: "ai-backend/app/models/tts_voxcpm2.py"
      via: "TtsSynthesisInput fields"
      pattern: "voxcpm2_cloning_mode|voxcpm2_style_prompt"
    - from: "ai-backend/app/models/model_manager.py"
      to: "ai-backend/app/models/tts_registry.py"
      via: "build_default_tts_adapters and EngineStatus"
      pattern: "voxcpm2"
---

<objective>
Implement the AI backend VoxCPM2 optional runtime, metadata, and synthesis API surface.

Purpose: make VoxCPM2 a first-class visible TTS candidate while preserving one-hot residency, CUDA-only runtime acceptance, and engine-scoped failures.
Output: `voxcpm==2.0.2` behind the optional TTS extra, metadata-visible `voxcpm2`, import-gated adapter, bounded API fields, and passing backend contract tests from Plan 07-01.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/OPERATING-NOTES.md
@.planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-CONTEXT.md
@.planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-RESEARCH.md
@.planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-PATTERNS.md
@.planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-01-SUMMARY.md
@ai-backend/docs/RUNTIME-EVIDENCE.md
@ai-backend/docs/STT-GPU-RUNTIME.md
@ai-backend/pyproject.toml
@ai-backend/app/models/tts_registry.py
@ai-backend/app/models/engine_metadata.py
@ai-backend/app/models/tts_f5.py
@ai-backend/app/models/gpu_runtime.py
@ai-backend/app/api/tts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add metadata-visible optional VoxCPM2 roster entry</name>
  <files>ai-backend/pyproject.toml, ai-backend/app/models/tts_registry.py, ai-backend/app/models/engine_metadata.py, ai-backend/tests/test_tts_registry.py, ai-backend/tests/test_model_manager.py</files>
  <read_first>
    ai-backend/pyproject.toml
    ai-backend/app/models/tts_registry.py
    ai-backend/app/models/engine_metadata.py
    ai-backend/app/models/model_manager.py
    ai-backend/tests/test_tts_registry.py
    ai-backend/tests/test_model_manager.py
    .planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-RESEARCH.md
  </read_first>
  <behavior>
    - `TTS_ENGINE_METADATA` contains `id="voxcpm2"` and `label="VoxCPM2"`.
    - `EXPECTED_ENGINE_IDS` includes `voxcpm2`; no existing engine id is removed.
    - Missing `voxcpm` marks only VoxCPM2 unavailable.
  </behavior>
  <action>Add `voxcpm==2.0.2` to `[project.optional-dependencies].tts` only. Add `voxcpm2` metadata with caveat chips `Candidate`, `48 kHz`, and `RTX 3060 gate pending`; `code_license="Apache-2.0"`; `model_license="Apache-2.0"`; `requires_transcript=False`; `supports_streaming=True`; runtime evidence text naming Phase 7 required evidence. Mirror the engine in `engine_metadata.py` with caveat `candidate`. Update roster validation and adapter builder imports without changing F5 as the default per D-01/D-03/D-04/D-21/D-22.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_tts_registry.py ai-backend/tests/test_model_manager.py -q</automated>
  </verify>
  <acceptance_criteria>
    `rg -n '"voxcpm2"|VoxCPM2|voxcpm==2.0.2' ai-backend/pyproject.toml ai-backend/app/models/tts_registry.py ai-backend/app/models/engine_metadata.py` returns matches.
    Tests from Plan 07-01 for roster and engine-scoped unavailable state pass.
  </acceptance_criteria>
  <done>VoxCPM2 is metadata-visible and optional-runtime-gated without changing the current default engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CUDA-forced VoxCPM2 adapter</name>
  <files>ai-backend/app/models/tts_voxcpm2.py, ai-backend/app/models/tts_registry.py, ai-backend/tests/test_tts_voxcpm2.py</files>
  <read_first>
    ai-backend/app/models/tts_voxcpm2.py
    ai-backend/app/models/tts_registry.py
    ai-backend/app/models/tts_f5.py
    ai-backend/app/models/gpu_runtime.py
    ai-backend/tests/test_tts_voxcpm2.py
    .planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-PATTERNS.md
  </read_first>
  <behavior>
    - Adapter calls `require_torch_cuda_runtime("VoxCPM2")` before building the runtime.
    - Adapter calls `VoxCPM.from_pretrained("openbmb/VoxCPM2", device="cuda")`.
    - Adapter returns the runtime sample rate, including `48000`.
  </behavior>
  <action>Create `VoxCpm2TtsAdapter` as an `ImportGatedTtsAdapter` with `engine_id="voxcpm2"`, `required_modules=("voxcpm",)`, and `synthesis_enabled=True`. Extend `TtsSynthesisInput` and `TtsSynthesisOutput` with bounded VoxCPM2 fields and `warnings: list[str] = Field(default_factory=list)`. Map `cloning_mode="transcript_guided"` plus nonblank transcript to `prompt_wav_path` and `prompt_text`; otherwise use reference-only `reference_wav_path` and append warning code `voxcpm2_reference_only_without_transcript`. Apply `style_prompt` by prefixing or otherwise passing the style text through the documented VoxCPM style/control API from research. Use temp files with RayMe-owned prefixes, `soundfile`, and the runtime `tts_model.sample_rate`; never hard-code 16000 or 24000 for VoxCPM2 per D-05/D-07/D-08/D-20/D-21.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_tts_voxcpm2.py -q</automated>
  </verify>
  <acceptance_criteria>
    `ai-backend/app/models/tts_voxcpm2.py` contains exact strings `openbmb/VoxCPM2`, `device=\"cuda\"`, `require_torch_cuda_runtime(\"VoxCPM2\")`, `reference_wav_path`, and `prompt_text`.
    `test_voxcpm2_returns_runtime_sample_rate_48000` passes.
  </acceptance_criteria>
  <done>The adapter implements reference-only and transcript-guided VoxCPM2 synthesis through a CUDA-only optional runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire bounded VoxCPM2 options through `/tts/synthesize`</name>
  <files>ai-backend/app/api/tts.py, ai-backend/app/models/tts_registry.py, ai-backend/tests/test_tts_registry.py</files>
  <read_first>
    ai-backend/app/api/tts.py
    ai-backend/app/models/tts_registry.py
    ai-backend/tests/test_tts_registry.py
    .planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-VALIDATION.md
  </read_first>
  <behavior>
    - `/tts/synthesize` validates bounded VoxCPM2 option fields.
    - Successful synthesis response includes warning codes when the adapter returns them.
    - Synthesis failures return fixed `tts_failed` detail without raw exception text.
  </behavior>
  <action>Extend `TtsSynthesizeRequest` with `voxcpm2_cloning_mode`, `voxcpm2_style_prompt`, `voxcpm2_cfg_value`, `voxcpm2_inference_timesteps`, `voxcpm2_normalize`, and `voxcpm2_denoise` using the same bounds as Plan 07-02. Pass these fields into `TtsSynthesisInput` for all engines, but only the VoxCPM2 adapter consumes them. Include `warnings` in the transient JSON response. Preserve fixed public error payload `{code:"tts_failed", message:"Synthesis failed", engine_id: target_engine}` and mark only the target engine unavailable on failure per D-18/D-20.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_tts_registry.py ai-backend/tests/test_tts_voxcpm2.py -q</automated>
  </verify>
  <acceptance_criteria>
    `/tts/synthesize` rejects `voxcpm2_style_prompt` longer than 300 chars and invalid `voxcpm2_cloning_mode` with 422.
    `/tts/synthesize` response JSON contains `warnings` when the adapter returns `voxcpm2_reference_only_without_transcript`.
    Existing F5 synthesis tests still pass.
  </acceptance_criteria>
  <done>The AI backend public synthesis route supports bounded VoxCPM2 settings without exposing runtime internals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI backend package import -> VoxCPM2 runtime | Optional dependency code and model weights enter runtime. |
| `/tts/synthesize` request -> adapter | User text, style, transcript, and audio bytes cross into model inference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `ai-backend/pyproject.toml`, model id | mitigate | Pin `voxcpm==2.0.2`, use `openbmb/VoxCPM2`, and record model/cache evidence in later OMEN plan. |
| T-07-03 | Information Disclosure | synthesis failure paths | mitigate | Keep fixed public `tts_failed` detail and tests rejecting traceback/path strings. |
| T-07-04 | Denial of Service | synthesis option fields | mitigate | Pydantic bounds on text, transcript, style, cfg, and timesteps. |
| T-07-05 | Spoofing | CUDA device acceptance | mitigate | `require_torch_cuda_runtime("VoxCPM2")` plus explicit `device="cuda"`. |
</threat_model>

<verification>
Run `uv run --project ai-backend pytest ai-backend/tests/test_tts_voxcpm2.py ai-backend/tests/test_tts_registry.py ai-backend/tests/test_model_manager.py -q` and `git diff --check`.
</verification>

<success_criteria>
VoxCPM2 is a normal metadata-visible engine with optional runtime loading, CUDA-only adapter behavior, bounded synthesis options, and sanitized failures.
</success_criteria>

<output>
After completion, create `.planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-05-SUMMARY.md`.
</output>
