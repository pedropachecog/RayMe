---
phase: 07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency
plan: 06
type: execute
wave: 1
depends_on:
  - 07-02
files_modified:
  - web-ui/server/app/api/voices.py
  - web-ui/server/app/domain/voice_service.py
  - web-ui/server/tests/test_voices.py
autonomous: true
requirements:
  - REQ-20
  - REQ-21
  - REQ-22
  - REQ-23
  - REQ-24
  - REQ-80
must_haves:
  truths:
    - "The Web UI server persists VoxCPM2 mode/style settings under voice metadata."
    - "Preview and test-play reuse the same saved VoxCPM2 settings."
    - "Non-VoxCPM2 engine behavior ignores VoxCPM2-specific settings."
  artifacts:
    - path: "web-ui/server/app/domain/voice_service.py"
      provides: "Durable voice metadata normalization"
    - path: "web-ui/server/app/api/voices.py"
      provides: "Preview/test-play synthesis payload bridge"
    - path: "web-ui/server/tests/test_voices.py"
      provides: "Server verification for D-05 through D-10"
  key_links:
    - from: "web-ui/server/app/domain/voice_service.py"
      to: "web-ui/server/app/api/voices.py"
      via: "metadata_json.engine_settings.voxcpm2"
      pattern: "engine_settings.*voxcpm2"
---

<objective>
Implement durable Web UI server support for VoxCPM2 voice metadata and synthesis payloads.

Purpose: keep the Web UI server as the source of truth for voice settings while preserving the existing AI backend API boundary.
Output: bounded `metadata_json.engine_settings.voxcpm2` persistence and preview/test-play payload forwarding.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/OPERATING-NOTES.md
@.planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-CONTEXT.md
@.planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-PATTERNS.md
@.planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-02-SUMMARY.md
@web-ui/server/app/api/voices.py
@web-ui/server/app/domain/voice_service.py
@web-ui/server/app/storage/models.py
@web-ui/server/tests/test_voices.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize and persist VoxCPM2 engine settings</name>
  <files>web-ui/server/app/domain/voice_service.py, web-ui/server/tests/test_voices.py</files>
  <read_first>
    web-ui/server/app/domain/voice_service.py
    web-ui/server/app/storage/models.py
    web-ui/server/tests/test_voices.py
    .planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-CONTEXT.md
  </read_first>
  <behavior>
    - Save preserves `metadata_json.engine_settings.voxcpm2` even when `default_engine` changes.
    - Invalid VoxCPM2 metadata is bounded or rejected before persistence.
    - Non-VoxCPM2 synthesis ignores VoxCPM2 settings.
  </behavior>
  <action>Add a local normalization helper for `engine_settings.voxcpm2` in `voice_service.py`. Canonical fields are `cloning_mode`, `style_prompt`, `cfg_value`, `inference_timesteps`, `normalize`, and `denoise`; defaults are `reference_only`, empty string, `2.0`, `10`, `false`, and `false`. Preserve the `voxcpm2` object inside `metadata_json.engine_settings` when the selected/default engine changes. Do not add a new database table; use the existing `Voice.metadata_json` JSON column per D-06/D-08/D-10.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_voices.py -q</automated>
  </verify>
  <acceptance_criteria>
    A saved voice response includes `metadata.engine_settings.voxcpm2.cloning_mode`.
    Overlong `style_prompt` and out-of-range numeric controls are rejected or clamped exactly as tests require.
    Existing tests for full engine roster and save-without-preview still pass.
  </acceptance_criteria>
  <done>VoxCPM2 voice settings persist durably without changing storage ownership boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Forward VoxCPM2 settings for preview and test-play</name>
  <files>web-ui/server/app/api/voices.py, web-ui/server/app/domain/voice_service.py, web-ui/server/tests/test_voices.py</files>
  <read_first>
    web-ui/server/app/api/voices.py
    web-ui/server/app/domain/voice_service.py
    web-ui/server/app/domain/ai_backend_client.py
    web-ui/server/tests/test_voices.py
  </read_first>
  <behavior>
    - Preview payload includes VoxCPM2 option fields when selected engine is `voxcpm2`.
    - Test-play payload uses the saved voice's VoxCPM2 settings.
    - Missing transcript produces warning code while allowing synthesis.
  </behavior>
  <action>Extend `VoiceSave`, `VoicePreview`, and `VoiceTestPlay` handling so the AI backend synthesis payload includes `voxcpm2_cloning_mode`, `voxcpm2_style_prompt`, `voxcpm2_cfg_value`, `voxcpm2_inference_timesteps`, `voxcpm2_normalize`, and `voxcpm2_denoise` only when the target engine is `voxcpm2`. For blank `reference_transcript`, keep preview/test-play allowed and include warning code `voxcpm2_reference_only_without_transcript` in the Web UI response if the backend returns it or if the server can determine the warning before the backend call per D-07. Do not change rename/delete semantics.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_voices.py -q</automated>
  </verify>
  <acceptance_criteria>
    `_synthesis_payload()` output for VoxCPM2 contains the six exact option keys.
    `_synthesis_payload()` output for `f5` does not contain keys prefixed with `voxcpm2_`.
    Voice Library test-play uses saved `metadata.engine_settings.voxcpm2`, not request-local defaults.
  </acceptance_criteria>
  <done>Preview and test-play consistently reuse durable VoxCPM2 metadata through the existing synthesis bridge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Web UI voice API | User-controlled metadata enters durable storage and synthesis payloads. |
| Web UI server -> AI backend | Bounded metadata is forwarded to model runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering | voice sample path lookup | mitigate | Reuse asset-id blob lookup and never original filenames for synthesis paths. |
| T-07-04 | Denial of Service | `metadata_json.engine_settings.voxcpm2` | mitigate | Normalize enum, text length, booleans, and numeric ranges server-side. |
| T-07-03 | Information Disclosure | preview/test-play errors | mitigate | Preserve existing public `synthesis_failed` mapping in `AiBackendClient`. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests/test_voices.py -q` and `git diff --check`.
</verification>

<success_criteria>
The Web UI server saves and reuses VoxCPM2 mode/style settings without exposing runtime internals or altering other engine behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/07-add-voxcpm2-to-the-tts-roster-with-empirical-quality-latency/07-06-SUMMARY.md`.
</output>
