---
phase: 01-foundations-text-chat-end-to-end
plan: "09"
type: execute
wave: 3
depends_on: ["01"]
files_modified:
  - web-ui/server/app/storage/blob_store.py
  - web-ui/server/app/storage/reaper.py
  - web-ui/server/tests/test_blob_store.py
autonomous: true
requirements: [REQ-12, REQ-13]
must_haves:
  truths:
    - "Portrait/blob writes use temp-file fsync plus atomic rename."
    - "Orphan cleanup cannot delete outside the configured blob directory."
  artifacts:
    - path: "web-ui/server/app/storage/blob_store.py"
      provides: "Atomic filesystem blob write helpers"
    - path: "web-ui/server/app/storage/reaper.py"
      provides: "Orphan cleanup helper"
  key_links:
    - from: "web-ui/server/app/storage/blob_store.py"
      to: "web-ui/server/tests/test_blob_store.py"
      via: "atomic write tests"
      pattern: "os.replace|os.fsync"
---

<objective>
Implement filesystem blob storage and orphan cleanup.

Purpose: Establish D-15 for character portraits now and future audio blobs later without mixing blob work into schema migration.
Output: Blob store, reaper, and storage safety tests.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement atomic blob writes and orphan reaping</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decision D-15.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md` blob storage pattern and Pitfall 7.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` File Blob Writes.
    - Target files if present.
  </read_first>
  <files>web-ui/server/app/storage/blob_store.py, web-ui/server/app/storage/reaper.py, web-ui/server/tests/test_blob_store.py</files>
  <action>Implement `atomic_write_blob(blob_dir: Path, final_name: str, data: bytes) -> Path` using a temp file in the target directory, byte write, flush, `os.fsync`, and `os.replace`. Reject absolute paths and names with path separators such as `../evil.png`. Implement `reap_orphan_blobs(blob_dir: Path, referenced_paths: set[Path]) -> list[Path]` that deletes stale `.tmp` files and unreferenced files only under `blob_dir`. Tests must cover traversal rejection, no direct final-name write before replace, temp cleanup on exception, and orphan deletion limited to the blob directory.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_blob_store.py -q</automated>
    <automated>rg "os\\.replace|os\\.fsync|\\.tmp|\\.\\./evil\\.png" web-ui/server/app/storage web-ui/server/tests/test_blob_store.py</automated>
  </verify>
  <acceptance_criteria>
    - `atomic_write_blob` uses both `os.fsync` and `os.replace`.
    - Tests include `../evil.png`.
    - Tests prove traversal input is rejected.
    - Tests prove orphan cleanup cannot delete outside `blob_dir`.
  </acceptance_criteria>
  <done>Binary asset storage uses the required atomic-temp-rename pattern with traversal-safe cleanup.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upload bytes -> filesystem | Untrusted uploaded assets become local files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09-TRAVERSAL | T/I | `blob_store.py` | mitigate | Reject absolute paths and path separators; tests include traversal strings. |
| T-01-09-ORPHAN | D | `reaper.py` | mitigate | Reaper is constrained to the configured blob directory and tested. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests/test_blob_store.py -q`.
</verification>

<success_criteria>
Blob storage is complete when atomic write and orphan cleanup tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-09-SUMMARY.md`.
</output>
