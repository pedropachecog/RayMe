---
phase: 01-foundations-text-chat-end-to-end
plan: "18"
type: execute
wave: 6
depends_on: ["12", "13", "16", "17"]
files_modified:
  - web-ui/client/src/routes/+page.svelte
  - web-ui/client/src/lib/api/threads.ts
  - web-ui/client/src/lib/components/ThreadListItem.svelte
  - web-ui/client/tests/unit/home.test.ts
autonomous: true
requirements: [REQ-70, REQ-71, REQ-72, REQ-90, REQ-A0]
must_haves:
  truths:
    - "Home is threads-first and can start chat through real character selection."
    - "Thread rename/delete call backend routes and use destructive confirmations."
  artifacts:
    - path: "web-ui/client/src/routes/+page.svelte"
      provides: "Home route"
    - path: "web-ui/client/src/lib/components/ThreadListItem.svelte"
      provides: "Recent thread row"
  key_links:
    - from: "web-ui/client/src/routes/+page.svelte"
      to: "web-ui/client/src/lib/api/threads.ts"
      via: "GET/PATCH/DELETE thread calls"
      pattern: "/api/threads"
---

<objective>
Implement the Home threads dashboard.

Purpose: Deliver D-35 and D-37 as a focused UI slice after thread APIs and shared shell exist.
Output: Home route, thread API wrapper, thread row component, and tests.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@docs/stitch/html/home-true-dark.html
@.planning/phases/01-foundations-text-chat-end-to-end/01-12-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-13-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-16-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-17-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement Home threads list and start-chat entry</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` Home contract.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-35 and D-37.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` Home/thread pattern.
    - Existing route/API/component files if present.
  </read_first>
  <files>web-ui/client/src/routes/+page.svelte, web-ui/client/src/lib/api/threads.ts, web-ui/client/src/lib/components/ThreadListItem.svelte, web-ui/client/tests/unit/home.test.ts</files>
  <action>Implement Home as the first screen. Fetch `GET /api/threads` and render rows with character portrait, character name, thread title or fallback character name, last-message snippet, relative timestamp, and contextual menu actions `Rename` and `Delete`. Add primary `Start Chat`; when no characters exist, route to `/gallery` and focus Gallery empty import/create state. Add secondary `Import Character`, `Create Character`, and `Settings`. Implement rename through `PATCH /api/threads/{thread_id}` and delete through `DELETE /api/threads/{thread_id}` with confirmation text exactly `Delete this thread? This removes the conversation history.` Do not include usage stats, quick-start personas, notifications, account controls, logout, Voice Lab, or Call.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run home</automated>
    <automated>rg "Start Chat|Import Character|Create Character|Delete this thread\\? This removes the conversation history\\.|/api/threads" web-ui/client/src web-ui/client/tests/unit/home.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Home tests prove thread row renders portrait/name/title/snippet/timestamp.
    - Home tests prove `Start Chat` routes to `/gallery` when no characters exist.
    - Home tests prove rename/delete call exact thread routes.
    - `rg "Voice Lab|Call|Account|Billing|Logout|usage stats|quick-start" web-ui/client/src/routes/+page.svelte web-ui/client/src/lib/components` returns no matches.
  </acceptance_criteria>
  <done>Home is a threads-first operational dashboard with real Phase 1 thread actions.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Home UI -> thread API | User thread actions mutate persistent conversations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-18-DELETE | D | Home delete action | mitigate | Destructive confirmation copy before thread deletion. |
| T-01-18-FUTURE | T | Home route | mitigate | Tests/grep block fake future controls and mock stats. |
</threat_model>

<verification>
Run `npm --prefix web-ui/client run test:unit -- --run home`.
</verification>

<success_criteria>
Home UI is complete when unit tests pass and only real Phase 1 Home controls are visible.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-18-SUMMARY.md`.
</output>
