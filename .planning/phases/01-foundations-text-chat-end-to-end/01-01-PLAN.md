---
phase: 01-foundations-text-chat-end-to-end
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/server/pyproject.toml
  - web-ui/server/app/__init__.py
  - web-ui/server/app/main.py
  - web-ui/server/app/config.py
  - web-ui/server/app/api/__init__.py
  - web-ui/server/app/domain/__init__.py
  - web-ui/server/app/storage/__init__.py
  - web-ui/server/tests/conftest.py
autonomous: true
requirements: [REQ-01, REQ-03, REQ-04, REQ-60, REQ-A1]
must_haves:
  truths:
    - "Backend pytest collection works before backend feature tests are filled in."
    - "The FastAPI app factory exists without unsafe wildcard bind or wildcard CORS defaults."
  artifacts:
    - path: "web-ui/server/pyproject.toml"
      provides: "Pinned uv backend project and pytest config"
    - path: "web-ui/server/app/main.py"
      provides: "FastAPI create_app boundary"
  key_links:
    - from: "web-ui/server/app/main.py"
      to: "web-ui/server/app/config.py"
      via: "settings dependency"
      pattern: "create_app"
---

<objective>
Create the backend project harness that all Phase 1 server plans build on.

Purpose: Establish the `web-ui/server` package, dependency pins, app factory, and pytest collection boundary without introducing service behavior ahead of contracts.
Output: Backend package scaffold and test harness.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create backend package and app factory</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-06, D-07, D-09, D-12, D-13, and D-41.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md` Standard Stack and Validation Architecture.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` `web-ui/server` FastAPI app and Validation Gates.
    - Target files if present.
  </read_first>
  <files>web-ui/server/pyproject.toml, web-ui/server/app/__init__.py, web-ui/server/app/main.py, web-ui/server/app/config.py, web-ui/server/app/api/__init__.py, web-ui/server/app/domain/__init__.py, web-ui/server/app/storage/__init__.py, web-ui/server/tests/conftest.py</files>
  <action>Create a uv-managed Python project named `rayme-web-server` with Python `>=3.12`. Pin `fastapi==0.136.1`, `uvicorn==0.46.0`, `sqlalchemy==2.0.49`, `aiosqlite==0.22.1`, `alembic==1.18.4`, `pydantic==2.13.3`, `openai==2.32.0`, `pillow==12.2.0`, `python-multipart==0.0.26`, `httpx==0.28.1`, plus dev dependencies `pytest==9.0.3`, `pytest-asyncio==1.3.0`, and `ruff==0.15.11`. Add pytest config with `asyncio_mode = "auto"` and test path `tests`. Create package `__init__` files and `create_app()` in `app/main.py` returning a FastAPI app titled `RayMe Web UI API`. Keep `config.py` limited to a typed settings shell; do not add wildcard CORS, browser-visible LLM key handling, or a `0.0.0.0` default.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests --collect-only -q</automated>
    <automated>uv run --project web-ui/server ruff check web-ui/server/app web-ui/server/tests</automated>
  </verify>
  <acceptance_criteria>
    - `web-ui/server/pyproject.toml` contains every pinned package name listed in this task.
    - `web-ui/server/app/main.py` exports `create_app`.
    - `rg "0\\.0\\.0\\.0|allow_origins=\\[\"\\*\"\\]" web-ui/server/app web-ui/server/tests` returns no matches.
    - Pytest collection exits 0.
  </acceptance_criteria>
  <done>The backend package exists, dependencies are pinned, pytest collection works, and no unsafe network default is introduced.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN browser -> backend scaffold | Future unauthenticated browser requests will enter this FastAPI app. |
| backend config -> network | Bind and origin defaults determine LAN exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-BIND | I/T | `config.py` | mitigate | Do not define `0.0.0.0`; later config plan adds explicit rejection tests. |
| T-01-01-CORS | S/T | `main.py` | mitigate | Do not add wildcard CORS in the scaffold; later web-host plan adds allowlist middleware. |
| T-01-01-KEY | I | backend scaffold | mitigate | No browser-visible LLM settings or API-key handling in this scaffold. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests --collect-only -q` and `uv run --project web-ui/server ruff check web-ui/server/app web-ui/server/tests`.
</verification>

<success_criteria>
Backend harness is complete when the package installs, app factory exists, test collection passes, and unsafe bind/origin defaults are absent.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-01-SUMMARY.md`.
</output>
