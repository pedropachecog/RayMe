---
phase: 01-foundations-text-chat-end-to-end
plan: "11"
type: execute
wave: 6
depends_on: ["03", "05", "06", "07", "09", "10"]
files_modified:
  - web-ui/client/tests/e2e/import-chat-reload.spec.ts
  - web-ui/client/tests/e2e/home-start-chat.spec.ts
  - web-ui/client/tests/e2e/https-status.spec.ts
  - web-ui/client/tests/e2e/mobile-text.spec.ts
  - web-ui/client/tests/e2e/ui-contract.spec.ts
  - web-ui/server/tests/test_phase1_acceptance.py
  - web-ui/server/docs/HTTPS-LAN.md
  - web-ui/server/docs/PHASE1-ACCEPTANCE.md
  - .planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md
autonomous: false
requirements: [REQ-01, REQ-03, REQ-04, REQ-10, REQ-11, REQ-12, REQ-13, REQ-14, REQ-16, REQ-17, REQ-30, REQ-31, REQ-32, REQ-33, REQ-34, REQ-35, REQ-36, REQ-60, REQ-70, REQ-71, REQ-72, REQ-90, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "The builder can import a v2/v3 card, save it, start a chat, stream a reply, reload, and continue."
    - "Desktop and Android-like mobile viewport tests pass for Phase 1 text surfaces."
    - "HTTPS direct LAN IP workflow is documented and Settings/Home surface secure-context and media-device status."
    - "Manual Android Chrome mkcert verification has exact steps and a blocking approval checkpoint."
  artifacts:
    - path: "web-ui/client/tests/e2e/import-chat-reload.spec.ts"
      provides: "Full imported-character chat browser acceptance"
    - path: "web-ui/client/tests/e2e/mobile-text.spec.ts"
      provides: "Android-like viewport acceptance"
    - path: "web-ui/server/docs/HTTPS-LAN.md"
      provides: "Phase 1 mkcert direct-IP HTTPS workflow"
    - path: ".planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md"
      provides: "Execution-updated validation status after full automated pass"
  key_links:
    - from: "web-ui/client/tests/e2e/import-chat-reload.spec.ts"
      to: "web-ui/server API and Svelte client"
      via: "browser import/chat/reload flow"
      pattern: "imported character chat reloads and continues"
    - from: "web-ui/client/tests/e2e/https-status.spec.ts"
      to: "browser readiness UI"
      via: "secure context and mediaDevices assertions"
      pattern: "isSecureContext"
---

<objective>
Close Phase 1 with automated end-to-end acceptance, HTTPS/mobile validation docs, and a human Android Chrome checkpoint.

Purpose: Prove the balanced vertical slice from D-03 and D-04 works end-to-end over the Phase 0 mkcert LAN path without adding deferred features.
Output: Passing full test suite, acceptance docs, validation status update, and manual Android verification checkpoint.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/00-measurement-gate/HTTPS-SETUP.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-03-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-05-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-06-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-07-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-09-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-10-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Complete automated E2E acceptance specs</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md` Per-Task Verification Map.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` Screen Contracts and Copywriting Contract.
    - Existing E2E spec files from Plan 02.
    - Backend and frontend summaries from dependent plans.
  </read_first>
  <files>web-ui/client/tests/e2e/import-chat-reload.spec.ts, web-ui/client/tests/e2e/home-start-chat.spec.ts, web-ui/client/tests/e2e/https-status.spec.ts, web-ui/client/tests/e2e/mobile-text.spec.ts, web-ui/client/tests/e2e/ui-contract.spec.ts, web-ui/server/tests/test_phase1_acceptance.py</files>
  <action>Finish all Playwright specs so they run against the built Svelte client served by FastAPI. `import-chat-reload.spec.ts` must seed or upload a fixture card containing alternate greetings and malicious description text, import it through Gallery, save in Editor, start a chat, assert the opening AI message, send a user message, assert token streaming, reload the page, and send another message in the same thread. `home-start-chat.spec.ts` must verify Home's `Start Chat` path routes through real character selection. `https-status.spec.ts` must assert Home and Settings show secure-context and media-device status text. `mobile-text.spec.ts` must use a Playwright Android/Pixel-like viewport and repeat import/chat/reload/continue without overlapping nav/composer. `ui-contract.spec.ts` must assert no `Voice Lab`, `Call`, `Account`, `Billing`, `Logout`, fake disabled future controls, search/filtering, or voice controls appear in Phase 1 screens. Add backend acceptance test `test_phase1_acceptance.py` that runs the API-level import -> thread -> stream -> reload/query flow without a browser.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests -q</automated>
    <automated>npm --prefix web-ui/client run test:unit -- --run</automated>
    <automated>npm --prefix web-ui/client run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `rg "test\\.(skip|fixme|todo)" web-ui/client/tests web-ui/server/tests` returns no matches.
    - E2E tests assert `Lorebook present - not used in v1` and malicious HTML does not execute/render as active HTML.
    - E2E tests assert stream tokens appear before final persisted message.
    - Backend acceptance test proves the same thread can continue after reload/query.
  </acceptance_criteria>
  <done>Automated backend, unit, and browser acceptance suites cover the full Phase 1 vertical slice.</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 1 HTTPS and acceptance runbooks</name>
  <read_first>
    - `.planning/phases/00-measurement-gate/HTTPS-SETUP.md`.
    - `docs/phase1-https-lan.md`.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md` Manual-Only Verifications.
    - Existing target docs if present.
  </read_first>
  <files>web-ui/server/docs/HTTPS-LAN.md, web-ui/server/docs/PHASE1-ACCEPTANCE.md</files>
  <action>Create `web-ui/server/docs/HTTPS-LAN.md` as the in-service runbook for Phase 1 HTTPS. It must include exact commands or env keys for `RAYME_WEB_BIND_HOST=192.168.1.199`, `RAYME_WEB_PORT=8443`, mkcert cert/key paths, and browser URL `https://192.168.1.199:8443`; state that direct LAN IP HTTPS is sufficient for Phase 1 and `rayme.local` is optional unless local DNS/mDNS is configured. Create `web-ui/server/docs/PHASE1-ACCEPTANCE.md` with exact automated commands: `uv run --project web-ui/server pytest web-ui/server/tests -q`, `npm --prefix web-ui/client run test:unit -- --run`, and `npm --prefix web-ui/client run test:e2e`; include manual Android steps from Task 3 and the expected Settings/Home text `Secure`, `Media ready`, and `Connected` for Web UI, AI backend, and LLM when configured.</action>
  <verify>
    <automated>rg "RAYME_WEB_BIND_HOST=192\\.168\\.1\\.199|https://192\\.168\\.1\\.199:8443|direct LAN IP HTTPS is sufficient|npm --prefix web-ui/client run test:e2e|uv run --project web-ui/server pytest web-ui/server/tests -q" web-ui/server/docs</automated>
  </verify>
  <acceptance_criteria>
    - `HTTPS-LAN.md` contains `https://192.168.1.199:8443`.
    - `HTTPS-LAN.md` states `rayme.local` is optional unless DNS/mDNS is configured.
    - `PHASE1-ACCEPTANCE.md` contains all three automated commands from the action.
    - Neither doc instructs users to bind `0.0.0.0`.
  </acceptance_criteria>
  <done>Phase 1 has reproducible HTTPS and acceptance docs tied to the implemented commands.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Android Chrome direct-IP HTTPS acceptance</name>
  <read_first>
    - `web-ui/server/docs/HTTPS-LAN.md`.
    - `web-ui/server/docs/PHASE1-ACCEPTANCE.md`.
    - `.planning/phases/00-measurement-gate/HTTPS-SETUP.md`.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md`.
  </read_first>
  <files>.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md</files>
  <action>After automated tests pass and the HTTPS server is running on the configured LAN IP, ask the user to verify the app on physical Android Chrome using `https://192.168.1.199:8443` or the configured direct LAN IP from the runbook. Present the recommended direct-IP path first and use plain numbered verification steps per D-01 and D-02. Do not substitute Playwright for this physical-device trust-store check. After the user approves, update `01-VALIDATION.md` manual Android HTTPS row status to green with the concrete URL used and date. If the user reports issues, leave the row pending/red and summarize the blocker in the plan summary.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:e2e -- https-status.spec.ts mobile-text.spec.ts</automated>
  </verify>
  <how-to-verify>
    1. On the server machine, run the Phase 1 HTTPS command from `web-ui/server/docs/HTTPS-LAN.md`.
    2. On Android Chrome, open `https://192.168.1.199:8443` unless the runbook has a different configured LAN IP.
    3. Confirm there is no certificate warning.
    4. Confirm Home or Settings shows `Secure` and `Media ready`.
    5. Import a fixture character, start a chat, stream a reply, reload, and continue the same thread.
  </how-to-verify>
  <resume-signal>Type `approved` after the Android Chrome direct-IP HTTPS flow works, or describe the issue.</resume-signal>
  <acceptance_criteria>
    - Automated `https-status.spec.ts` and `mobile-text.spec.ts` pass before the checkpoint.
    - User confirms no certificate warning on Android Chrome.
    - User confirms Settings/Home shows `Secure` and `Media ready`.
    - User confirms import/chat/reload/continue works on the physical Android device.
    - `01-VALIDATION.md` is updated only after user approval.
  </acceptance_criteria>
  <done>Physical Android Chrome direct-IP HTTPS acceptance is approved or a concrete blocker is recorded.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser E2E -> app APIs | Full app workflow exercises unauthenticated LAN endpoints. |
| Android device -> HTTPS server | Physical trust-store and LAN route validate mobile secure context. |
| acceptance docs -> operator | Runbooks can accidentally instruct unsafe binding or key exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11-XSS | E/T | E2E malicious card flow | mitigate | Import-chat E2E verifies malicious card content does not execute/render as active HTML. |
| T-01-11-TRAVERSAL | T | Backend acceptance coverage | mitigate | Full backend suite includes upload/path traversal tests from Plan 06. |
| T-01-11-KEY | I | Settings/E2E docs | mitigate | E2E and docs verify masked key and no key echo in status. |
| T-01-11-BIND | I/T | HTTPS runbook | mitigate | Docs use direct LAN IP and explicitly avoid `0.0.0.0`; physical checkpoint confirms exact URL. |
| T-01-11-CORS | S/T | Full backend suite | mitigate | Full tests include explicit origin policy from Plan 05. |
| T-01-11-PNGDOS | D | Full backend suite | mitigate | Full tests include malformed/large PNG metadata rejection from Plan 06. |
| T-01-11-PROMPT | T | E2E/API acceptance | mitigate | Acceptance verifies lorebook indicator and backend prompt tests exclude lorebook. |
| T-01-11-DATA | D | E2E/API acceptance | mitigate | Acceptance verifies character delete preservation and message branch/reload continuity through full suites. |
</threat_model>

<verification>
Run:
`uv run --project web-ui/server pytest web-ui/server/tests -q`
`npm --prefix web-ui/client run test:unit -- --run`
`npm --prefix web-ui/client run test:e2e`
Manual checkpoint: Android Chrome at `https://192.168.1.199:8443` reports secure context/media ready and completes import/chat/reload/continue.
</verification>

<success_criteria>
Phase 1 acceptance is complete when all automated suites pass, HTTPS/mobile runbooks exist, no skipped tests remain, and the user approves the physical Android Chrome direct-IP HTTPS flow.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-11-SUMMARY.md`.
</output>
