---
phase: 01-foundations-text-chat-end-to-end
plan: "11"
type: execute
wave: 5
depends_on: ["08", "10"]
files_modified:
  - web-ui/server/app/api/health.py
  - web-ui/server/app/api/settings.py
  - web-ui/server/app/domain/settings_service.py
  - web-ui/server/app/domain/llm_probe.py
  - web-ui/server/tests/test_health_settings.py
autonomous: true
requirements: [REQ-01, REQ-03, REQ-04, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "Web UI exposes `/health` and Settings APIs for Web UI, AI backend, and LLM tests."
    - "LLM base URL, key, and model remain server-side; browser payloads never echo the raw key."
  artifacts:
    - path: "web-ui/server/app/api/settings.py"
      provides: "Settings read/update/test endpoints"
    - path: "web-ui/server/app/domain/llm_probe.py"
      provides: "OpenAI-compatible endpoint probe"
  key_links:
    - from: "web-ui/server/app/api/settings.py"
      to: "web-ui/server/app/domain/llm_probe.py"
      via: "`POST /api/settings/test/llm`"
      pattern: "test/llm"
---

<objective>
Implement Web UI health and Settings connection-test APIs.

Purpose: Deliver the endpoint/status behavior from D-08, D-09, D-11, and D-39 after network safety is in place.
Output: Health route, persisted settings routes, AI-backend probe, LLM probe, and tests.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-08-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-10-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement health and Settings APIs</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-08, D-09, D-11, and D-39.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` Settings contract.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md` Pitfall 8 and Security Domain.
    - Existing config/storage files from plans 08 and 10.
  </read_first>
  <files>web-ui/server/app/api/health.py, web-ui/server/app/api/settings.py, web-ui/server/app/domain/settings_service.py, web-ui/server/app/domain/llm_probe.py, web-ui/server/tests/test_health_settings.py</files>
  <action>Implement `GET /health` returning `{"service":"rayme-web-ui","status":"ok","phase":"01"}`. Implement `GET /api/settings` and `PATCH /api/settings` for persisted `web_url`, `ai_backend_url`, `llm_base_url`, `llm_model`, and masked `llm_api_key_configured`; never return raw `llm_api_key`. Implement `POST /api/settings/test/web`, `/api/settings/test/ai-backend`, and `/api/settings/test/llm`, each returning exactly `Connected`, `Unreachable`, `Unauthorized`, or `Not configured`. `test/ai-backend` probes configured `/health` with httpx. `test/llm` uses server-side configured base URL/key/model through `AsyncOpenAI` or an OpenAI-compatible minimal request; 401/403 map to `Unauthorized`, missing config to `Not configured`, network failure to `Unreachable`, success to `Connected`.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_health_settings.py -q</automated>
    <automated>rg "Connected|Unreachable|Unauthorized|Not configured|/api/settings/test/llm|llm_api_key_configured" web-ui/server/app web-ui/server/tests/test_health_settings.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert all four status strings can be returned.
    - Tests assert a stored API key is absent from `GET /api/settings` and test responses.
    - Tests assert LLM health is `/api/settings/test/llm`, not a local `llm` service.
    - Health/settings tests exit 0.
  </acceptance_criteria>
  <done>Settings APIs provide web, AI-backend, and LLM connection tests with server-side secret handling.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Settings API | Browser reads and updates endpoint configuration. |
| FastAPI app -> AI backend | Web host probes a separate LAN service. |
| FastAPI app -> LLM endpoint | Server uses sensitive endpoint/key/model values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11-KEY | I | `settings.py`, `llm_probe.py` | mitigate | Return only key-configured boolean and status strings; never raw keys. |
| T-01-11-LLMHEALTH | S/T | `llm_probe.py` | mitigate | Probe configured OpenAI-compatible endpoint server-side. |
| T-01-11-STATUS | R/I | `settings.py` | mitigate | Use exact status enum values; no provider secrets in error text. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests/test_health_settings.py -q`.
</verification>

<success_criteria>
Health/settings APIs are complete when status tests pass and raw LLM keys never leave the server boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-11-SUMMARY.md`.
</output>
