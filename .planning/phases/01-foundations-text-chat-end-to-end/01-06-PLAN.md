---
phase: 01-foundations-text-chat-end-to-end
plan: "06"
type: execute
wave: 2
depends_on: ["05"]
files_modified:
  - web-ui/client/src/lib/sanitizer/renderMarkdown.ts
  - web-ui/client/tests/unit/sanitizer.test.ts
  - web-ui/client/tests/e2e/import-chat-reload.spec.ts
  - web-ui/client/tests/e2e/home-start-chat.spec.ts
  - web-ui/client/tests/e2e/https-status.spec.ts
  - web-ui/client/tests/e2e/mobile-text.spec.ts
  - web-ui/client/tests/e2e/ui-contract.spec.ts
autonomous: true
requirements: [REQ-13, REQ-16, REQ-17, REQ-30, REQ-32, REQ-34, REQ-35, REQ-36, REQ-90, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "Imported character prose has a tested sanitized Markdown boundary."
    - "E2E spec shells name the Phase 1 acceptance flows, including generated backend responses for regenerate, swipe, and continue."
  artifacts:
    - path: "web-ui/client/src/lib/sanitizer/renderMarkdown.ts"
      provides: "Untrusted Markdown to sanitized HTML boundary"
    - path: "web-ui/client/tests/e2e/import-chat-reload.spec.ts"
      provides: "Imported-character chat acceptance shell"
  key_links:
    - from: "web-ui/client/tests/unit/sanitizer.test.ts"
      to: "web-ui/client/src/lib/sanitizer/renderMarkdown.ts"
      via: "renderTrustedMarkdown"
      pattern: "onerror"
---

<objective>
Implement the sanitizer boundary and create Playwright acceptance spec shells.

Purpose: Prevent raw card HTML patterns from entering UI code and ensure the browser acceptance flows are named before screen plans fill them in.
Output: Sanitizer module, unit test, and unskipped E2E shell specs.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-05-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement sanitized Markdown rendering</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decision D-24.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` Accessibility And Safety.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` Sanitizer pattern.
    - Target files if present.
  </read_first>
  <files>web-ui/client/src/lib/sanitizer/renderMarkdown.ts, web-ui/client/tests/unit/sanitizer.test.ts</files>
  <action>Create `renderTrustedMarkdown(untrustedMarkdown: string): string` using `marked.parse(..., { async: false })` followed by `DOMPurify.sanitize`. Allow only `p`, `br`, `strong`, `em`, `code`, `pre`, `blockquote`, `ul`, `ol`, `li`, and `a`; allow only `href` and `title`; set `ALLOW_DATA_ATTR: false`; strip dangerous URL protocols. Tests must prove `<img src=x onerror=alert(1)>`, `<script>alert(1)</script>`, `javascript:alert(1)`, `data-*`, and inline styles are removed while normal Markdown emphasis and safe links survive.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run sanitizer</automated>
  </verify>
  <acceptance_criteria>
    - `renderMarkdown.ts` exports `renderTrustedMarkdown`.
    - Tests include literals `onerror=alert(1)`, `<script>alert(1)</script>`, and `javascript:alert(1)`.
    - Sanitizer test exits 0.
  </acceptance_criteria>
  <done>The frontend has a passing sanitizer boundary for imported character/card prose.</done>
</task>

<task type="auto">
  <name>Task 2: Add unskipped Playwright acceptance shells</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md` Per-Task Verification Map.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` Screen Contracts and Responsive Contract.
    - Target spec files if present.
  </read_first>
  <files>web-ui/client/tests/e2e/import-chat-reload.spec.ts, web-ui/client/tests/e2e/home-start-chat.spec.ts, web-ui/client/tests/e2e/https-status.spec.ts, web-ui/client/tests/e2e/mobile-text.spec.ts, web-ui/client/tests/e2e/ui-contract.spec.ts</files>
  <action>Create Playwright spec shells using routes `/`, `/gallery`, `/characters/:id`, `/chat/:threadId`, and `/settings`. Include test titles exactly `imported character chat reloads and continues`, `home starts chat through real character selection`, `secure context and media device status are visible`, `mobile viewport can import chat reload and continue`, and `phase 1 ui omits future controls`. Also include pending assertions in spec text, without skip/fixme/todo, for generated backend responses: regenerate consumes a returned generated AI alternate, swipe consumes a backend-generated alternate, and continue consumes a backend-generated selected alternate. These flows must not assert local-only state.</action>
  <verify>
    <automated>rg "imported character chat reloads and continues|home starts chat through real character selection|secure context and media device status are visible|mobile viewport can import chat reload and continue|phase 1 ui omits future controls|backend-generated|regenerate|swipe|continue" web-ui/client/tests/e2e</automated>
    <automated>test "$(rg "test\\.(skip|fixme|todo)" web-ui/client/tests/e2e -c | awk -F: '{s+=$2} END {print s+0}')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - All five spec files listed in this task exist.
    - Each exact test title appears once.
    - No E2E spec contains `test.skip`, `test.fixme`, or `test.todo`.
    - Spec text references backend-generated regenerate, swipe, and continue responses.
  </acceptance_criteria>
  <done>The frontend has unskipped Playwright acceptance specs ready for implementation plans to satisfy.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted Markdown -> Svelte DOM | Imported card text crosses into rendered HTML. |
| E2E route shell -> implementation | Test contracts prevent local-only chat action behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-XSS | E/T | `renderMarkdown.ts` | mitigate | Marked output is sanitized by DOMPurify with malicious literal tests. |
| T-01-06-LOCALONLY | T | E2E shell specs | mitigate | Regenerate/swipe/continue assertions require backend-generated responses. |
| T-01-06-FUTURE | T | `ui-contract.spec.ts` | mitigate | UI contract spec blocks future controls outside Phase 1. |
</threat_model>

<verification>
Run `npm --prefix web-ui/client run test:unit -- --run sanitizer` and grep for skipped/fixme/todo E2E specs.
</verification>

<success_criteria>
This plan is complete when sanitizer tests pass and all acceptance shell specs exist without skipped placeholders.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-06-SUMMARY.md`.
</output>
