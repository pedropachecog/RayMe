---
phase: 01-foundations-text-chat-end-to-end
plan: "13"
type: execute
wave: 5
depends_on: ["08", "12"]
files_modified:
  - web-ui/server/app/domain/thread_service.py
  - web-ui/server/app/api/threads.py
  - web-ui/server/tests/test_threads.py
autonomous: true
requirements: [REQ-17, REQ-30, REQ-31, REQ-60, REQ-70, REQ-71, REQ-72]
must_haves:
  truths:
    - "Threads start with first message or selected alternate greeting as opening `ai_text`."
    - "`GET /api/threads/{thread_id}` hydrates metadata, chronological messages, selected alternates, alternate lists, and stale flags."
  artifacts:
    - path: "web-ui/server/app/api/threads.py"
      provides: "Thread list/detail/create/rename/delete routes"
    - path: "web-ui/server/app/domain/thread_service.py"
      provides: "Thread creation and hydration logic"
  key_links:
    - from: "web-ui/server/app/api/threads.py"
      to: "web-ui/server/app/storage/models.py"
      via: "thread/message query"
      pattern: "selected_alternate_id|stale_after_edit"
---

<objective>
Implement backend thread management and full thread hydration.

Purpose: Provide Home and Chat consumers a single durable thread contract, including alternate-greeting opening turns and selected branch state.
Output: Thread service, routes, and passing tests.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-08-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-12-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement thread APIs and opening greetings</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` REQ-17, REQ-31, REQ-70, REQ-71, and REQ-72.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-27 and D-37.
    - `web-ui/server/app/storage/models.py`.
    - Target files if present.
  </read_first>
  <files>web-ui/server/app/domain/thread_service.py, web-ui/server/app/api/threads.py, web-ui/server/tests/test_threads.py</files>
  <action>Implement `GET /api/threads` sorted by `updated_at` descending with character portrait/name snapshot and last-message snippet; `GET /api/threads/{thread_id}` returning thread metadata plus chronological messages; `POST /api/threads` with `character_id`, optional `title`, optional `alternate_greeting_index`; `PATCH /api/threads/{thread_id}` rename; and `DELETE /api/threads/{thread_id}` for deleting that thread's conversation history. The detail response must include each message's `id`, `message_kind`, `role`, `sequence`, resolved selected content, `selected_alternate_id`, ordered `alternates`, and `stale_after_edit`. On creation, insert an opening `ai_text` assistant message at sequence 0 from `first_mes` or selected alternate greeting and persist it as a selected `message_alternates` row with `source_action='first_mes'`.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_threads.py -q</automated>
    <automated>rg "/api/threads|alternate_greeting_index|source_action=.*first_mes|updated_at|selected_alternate_id|stale_after_edit|alternates" web-ui/server/app web-ui/server/tests/test_threads.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove a new thread inserts the opening AI message before any user message.
    - Tests prove detail hydration returns metadata, chronological messages, alternates, selected IDs, and stale flags.
    - Tests prove alternate greeting selection persists as the opening turn.
    - Tests prove list sorting, rename, and delete behavior.
  </acceptance_criteria>
  <done>Thread management and opening greeting behavior are implemented for Home and Chat consumers.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> thread API | User creates, renames, deletes, and hydrates conversation state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13-DATA | D | `threads.py` | mitigate | Tests scope thread delete to selected thread and require full hydration for reload. |
| T-01-13-BRANCH | T | `thread_service.py` | mitigate | Detail response includes selected alternate IDs, alternates, and stale flags. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests/test_threads.py -q`.
</verification>

<success_criteria>
Thread backend is complete when list/detail/create/rename/delete and opening-greeting tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-13-SUMMARY.md`.
</output>
