---
phase: 01-foundations-text-chat-end-to-end
plan: "22"
type: execute
wave: 8
depends_on: ["15", "21"]
files_modified:
  - web-ui/client/src/routes/chat/[threadId]/+page.svelte
  - web-ui/client/src/lib/api/chat.ts
  - web-ui/client/src/lib/components/ChatMessageBubble.svelte
  - web-ui/client/src/lib/components/MessageActionMenu.svelte
  - web-ui/client/src/lib/components/SwipeStepper.svelte
  - web-ui/client/tests/unit/chat.test.ts
autonomous: true
requirements: [REQ-32, REQ-33, REQ-34, REQ-35, REQ-90, REQ-A0]
must_haves:
  truths:
    - "AI turns expose Regenerate, Swipe, Edit, and Continue; user turns expose Edit."
    - "Regenerate, generated swipe, and continue consume backend-generated returned messages/alternates."
    - "Editing marks downstream stale and presents truncate-or-keep choice."
  artifacts:
    - path: "web-ui/client/src/lib/components/MessageActionMenu.svelte"
      provides: "Per-message action menu"
    - path: "web-ui/client/src/lib/components/SwipeStepper.svelte"
      provides: "Alternate response selector"
  key_links:
    - from: "web-ui/client/src/routes/chat/[threadId]/+page.svelte"
      to: "web-ui/client/src/lib/api/chat.ts"
      via: "regenerate/swipe/edit/continue calls"
      pattern: "regenerate|swipe|continue"
---

<objective>
Implement chat message actions, swipes, stale-state UI, and continue.

Purpose: Wire REQ-32 through REQ-35 in the browser against the backend-generated message-action responses from Plan 15.
Output: Action menu, swipe stepper, chat action API wrappers, stale UI, and tests.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-15-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-21-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement backend-backed message action UI</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` REQ-32 through REQ-35.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-29 through D-33.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` Chat interaction contract and Copywriting Contract.
    - Plan 15 summary for generated message action response shapes.
  </read_first>
  <files>web-ui/client/src/routes/chat/[threadId]/+page.svelte, web-ui/client/src/lib/api/chat.ts, web-ui/client/src/lib/components/ChatMessageBubble.svelte, web-ui/client/src/lib/components/MessageActionMenu.svelte, web-ui/client/src/lib/components/SwipeStepper.svelte, web-ui/client/tests/unit/chat.test.ts</files>
  <action>Implement per-message actions. AI turns expose `Regenerate`, `Swipe`, `Edit`, and `Continue`; user turns expose `Edit`. Desktop reveals on hover/focus; mobile uses tap overflow. `Regenerate` calls backend regenerate and replaces the selected AI response using the returned generated ThreadMessageShape; it must not append a second canonical bubble. `SwipeStepper` shows previous, current index like `2 / 3`, next, and generate alternate; generate calls backend swipe, consumes the returned generated alternate/message, and selecting a swipe updates the canonical visible response. Editing opens inline edit; save marks downstream stale from backend response and stale turns show subtle left rail plus `Stale` chip. Continuing after edit opens truncate-or-keep confirmation with exact copy `Remove stale turns after this edit? The selected message stays and later turns are removed from this branch.` Continue sends current composer text to backend and consumes the returned generated continue alternate/message.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run chat</automated>
    <automated>rg "Regenerate|Swipe|Edit|Continue|Stale|Remove stale turns after this edit\\?|generated alternate|selected_alternate_id" web-ui/client/src web-ui/client/tests/unit/chat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove AI menu has Regenerate/Swipe/Edit/Continue and user menu has Edit only.
    - Tests prove regenerate consumes backend response and does not append a second canonical bubble.
    - Tests prove generated swipe consumes backend returned alternate/message and selected alternate becomes canonical.
    - Tests prove continue sends composer text and consumes backend returned continue alternate/message.
    - Tests prove stale chip appears on downstream messages after edit.
  </acceptance_criteria>
  <done>Chat exposes the full Phase 1 text action surface with backend-generated responses.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| message action UI -> backend | User action controls mutate durable branch state and trigger generation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-22-BRANCH | T/D | Message action UI | mitigate | Tests prove regenerate, generated swipes, stale flags, truncate/keep, and continue map to backend APIs. |
| T-01-22-STREAM | D/T | action responses | mitigate | Returned generated messages replace/select existing visible turns without duplicates. |
| T-01-22-MOBILE | D | action menus | mitigate | Touch overflow path required for mobile. |
</threat_model>

<verification>
Run `npm --prefix web-ui/client run test:unit -- --run chat`.
</verification>

<success_criteria>
Chat actions are complete when unit tests prove generated backend responses drive regenerate, swipes, and continue.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-22-SUMMARY.md`.
</output>
