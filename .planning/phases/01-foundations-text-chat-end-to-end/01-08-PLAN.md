---
phase: 01-foundations-text-chat-end-to-end
plan: "08"
type: execute
wave: 3
depends_on: ["01", "02", "03", "04", "05", "06", "07"]
files_modified:
  - .planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md
  - web-ui/server/app/storage/models.py
  - web-ui/server/app/storage/session.py
  - web-ui/server/alembic.ini
  - web-ui/server/alembic/env.py
  - web-ui/server/alembic/versions/0001_initial_schema.py
  - web-ui/server/tests/test_migrations.py
autonomous: true
requirements: [REQ-12, REQ-16, REQ-17, REQ-30, REQ-31, REQ-32, REQ-33, REQ-34, REQ-35, REQ-60, REQ-70, REQ-71]
must_haves:
  truths:
    - "Wave 0 validation is marked complete only after scaffold artifacts exist."
    - "SQLite schema supports characters, threads, unified messages, and alternates from the first migration."
  artifacts:
    - path: "web-ui/server/alembic/versions/0001_initial_schema.py"
      provides: "Initial SQLite schema migration"
      contains: "message_kind"
    - path: "web-ui/server/app/storage/models.py"
      provides: "SQLAlchemy storage models"
  key_links:
    - from: "web-ui/server/tests/test_migrations.py"
      to: "web-ui/server/alembic/versions/0001_initial_schema.py"
      via: "Alembic upgrade"
      pattern: "message_kind"
---

<objective>
Implement the initial database schema and mark Wave 0 validation complete after the scaffold exists.

Purpose: Deliver D-14 through D-18 and commit the unified message schema before APIs and UI depend on it.
Output: SQLAlchemy models/session, Alembic initial migration, migration tests, and validation marker update.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-01-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-02-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-03-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-04-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-05-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-06-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-07-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Mark Wave 0 validation complete</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md` frontmatter and Wave 0 Requirements.
    - Summaries for plans 01 through 07.
    - Target file `.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md`.
  </read_first>
  <files>.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md</files>
  <action>Before editing the validation file, verify that core Wave 0 artifacts exist: backend pyproject, client package, AI-backend pyproject, LLM README, backend contract tests, sanitizer test, E2E shell specs, and card fixtures. Only after checks pass, update YAML frontmatter from `wave_0_complete: false` to `wave_0_complete: true`. Do not mark individual validation rows green in this task.</action>
  <verify>
    <automated>test -f web-ui/server/pyproject.toml && test -f web-ui/client/package.json && test -f ai-backend/pyproject.toml && test -f llm/README.md && rg "wave_0_complete: true" .planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `01-VALIDATION.md` contains `wave_0_complete: true`.
    - No `Status | green` rows are introduced by this task.
    - The artifact checks pass before the frontmatter edit.
  </acceptance_criteria>
  <done>Wave 0 completion is recorded only after scaffold artifacts exist.</done>
</task>

<task type="auto">
  <name>Task 2: Implement schema models and migration</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-14 through D-18.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` Message schema/migration pattern.
    - Existing contract tests from Plan 02.
    - Target files if present.
  </read_first>
  <files>web-ui/server/app/storage/models.py, web-ui/server/app/storage/session.py, web-ui/server/alembic.ini, web-ui/server/alembic/env.py, web-ui/server/alembic/versions/0001_initial_schema.py, web-ui/server/tests/test_migrations.py</files>
  <action>Implement async SQLAlchemy storage with `sqlite+aiosqlite` and Alembic migration. Create tables `app_settings`, `characters`, `character_assets`, `threads`, `messages`, and `message_alternates`. Per D-16, `messages.message_kind` must allow exactly `user_text`, `ai_text`, `user_speech`, `ai_speech`, `call_start`, and `call_end`. Per D-17, `message_alternates.source_action` must allow exactly `first_mes`, `regenerate`, `swipe`, and `continue`, with selected alternate, edit lineage, stale flag, branch root, sequence, and timestamps. Include full character prompt fields, `raw_source_json`, `lorebook_json`, `deleted_at`, and thread character snapshot fields. Keep SQLite journal mode conservative; do not enable WAL in this phase.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_migrations.py -q</automated>
    <automated>rg "user_text|ai_text|user_speech|ai_speech|call_start|call_end|message_alternates|stale_after_edit|selected_alternate_id" web-ui/server/app/storage/models.py web-ui/server/alembic/versions/0001_initial_schema.py</automated>
  </verify>
  <acceptance_criteria>
    - Migration creates all six tables listed in the action.
    - Tests prove all six message kinds insert and an unknown kind fails.
    - Tests prove selected alternate, stale flag, and branch root columns exist.
    - `rg "PRAGMA journal_mode=WAL|journal_mode.*WAL" web-ui/server` returns no matches.
  </acceptance_criteria>
  <done>The first migration is reproducible and schema-complete for Phase 1 text chat plus future call message kinds.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration -> SQLite file | Schema choices create durable state. |
| message branch actions -> storage | Regenerate/swipe/edit/continue depend on branch columns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08-SCHEMA | T/D | `0001_initial_schema.py` | mitigate | Migration tests require message kinds, alternates, stale flags, and snapshots. |
| T-01-08-DELETE | D | `characters`, `threads` tables | mitigate | Soft delete and thread snapshots preserve existing history. |
| T-01-08-BRANCH | T/D | `messages`, `message_alternates` | mitigate | Durable selected alternate and stale columns support message actions. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests/test_migrations.py -q`.
</verification>

<success_criteria>
Storage schema is complete when Wave 0 is marked complete, Alembic creates the initial schema, and migration tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-08-SUMMARY.md`.
</output>
