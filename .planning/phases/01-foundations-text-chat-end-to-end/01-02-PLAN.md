---
phase: 01-foundations-text-chat-end-to-end
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - web-ui/server/app/domain/llm_stream.py
  - web-ui/server/app/domain/message_actions.py
  - web-ui/server/app/domain/prompt_builder.py
  - web-ui/server/app/storage/models.py
  - web-ui/server/tests/test_migrations.py
  - web-ui/server/tests/test_chat_stream.py
  - web-ui/server/tests/test_message_actions.py
  - web-ui/server/tests/test_prompt_builder.py
  - web-ui/server/tests/test_threads.py
autonomous: true
requirements: [REQ-03, REQ-17, REQ-30, REQ-31, REQ-32, REQ-33, REQ-34, REQ-35, REQ-60, REQ-70, REQ-71, REQ-72]
must_haves:
  truths:
    - "Backend contracts define unified messages, thread hydration, streaming chat, selected alternates, stale flags, regenerate, swipe, edit, and continue before implementation."
    - "Message-action tests require real LLM invocation for regenerate, swipe generation, and continue."
  artifacts:
    - path: "web-ui/server/tests/test_message_actions.py"
      provides: "Contracts for LLM-backed regenerate, swipe, edit, and continue"
    - path: "web-ui/server/tests/test_chat_stream.py"
      provides: "SSE token and done-message contract"
    - path: "web-ui/server/app/domain/prompt_builder.py"
      provides: "Prompt-context interface"
  key_links:
    - from: "web-ui/server/tests/test_message_actions.py"
      to: "web-ui/server/app/domain/message_actions.py"
      via: "domain function imports"
      pattern: "regenerate_ai_turn|create_swipe_alternate|continue_ai_turn"
    - from: "web-ui/server/tests/test_chat_stream.py"
      to: "web-ui/server/app/domain/llm_stream.py"
      via: "LLM streaming helper"
      pattern: "data:.*done"
---

<objective>
Create backend chat, schema, and message-action contract tests with interface modules.

Purpose: Lock the text-chat behavior from D-26 through D-34, including the checker-required LLM-backed paths for regenerate, swipes, and continue.
Output: Collectable tests and importable interfaces for later backend implementation plans.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Write schema, thread, prompt, and stream contracts</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md` rows for REQ-03, REQ-17, REQ-30, REQ-31, REQ-60, and REQ-70 through REQ-72.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` Message Branching Semantics and Text Streaming Contract.
    - Existing target files if present.
  </read_first>
  <files>web-ui/server/app/domain/llm_stream.py, web-ui/server/app/domain/prompt_builder.py, web-ui/server/app/storage/models.py, web-ui/server/tests/test_migrations.py, web-ui/server/tests/test_chat_stream.py, web-ui/server/tests/test_prompt_builder.py, web-ui/server/tests/test_threads.py</files>
  <action>Create interface modules exposing `stream_chat_completion`, `collect_chat_completion`, `build_prompt_context`, and model names for characters, threads, messages, and message alternates. Stub implementations may raise `NotImplementedError`, but imports and test collection must pass. Write non-skipped tests named `test_initial_migration_creates_message_kinds`, `test_messages_support_selected_alternate_and_stale_flags`, `test_thread_creation_inserts_first_message_or_selected_alternate`, `test_thread_detail_hydrates_selected_alternates_and_stale_flags`, `test_lorebook_preserved_but_not_injected`, `test_swipe_selected_branch_only_enters_prompt`, `test_llm_stream_yields_tokens_then_persists_final_once`, and `test_done_event_contains_full_thread_message_shape`. The stream contract must require token events shaped `data: {"type":"token","text":"..."}` and a final line shaped `data: {"type":"done","message":{...ThreadMessageShape...}}`.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests --collect-only -q</automated>
    <automated>rg "test_done_event_contains_full_thread_message_shape|build_prompt_context|collect_chat_completion|ThreadMessageShape" web-ui/server/app web-ui/server/tests</automated>
  </verify>
  <acceptance_criteria>
    - Every exact test name listed in the action appears under `web-ui/server/tests`.
    - No backend test is marked `skip`, `xfail`, or `todo`.
    - Tests assert the SSE done event includes a full message object, not only an ID.
    - Pytest collection exits 0.
  </acceptance_criteria>
  <done>Core chat/schema contracts are present and collectable.</done>
</task>

<task type="auto">
  <name>Task 2: Write LLM-backed message-action contracts</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` REQ-32 through REQ-35.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-29 through D-33.
    - Checker issue requiring real LLM-backed regenerate, swipes, and continue.
    - Existing target files if present.
  </read_first>
  <files>web-ui/server/app/domain/message_actions.py, web-ui/server/tests/test_message_actions.py</files>
  <action>Create importable functions `regenerate_ai_turn`, `create_swipe_alternate`, `edit_message_and_mark_stale`, `truncate_stale_after_message`, `keep_stale_after_message`, and `continue_ai_turn`. Write non-skipped tests named `test_regenerate_calls_llm_and_replaces_selected_ai_response`, `test_swipe_calls_llm_persists_alternate_and_excludes_unselected_from_future_context`, `test_continue_calls_llm_with_composer_text_and_selects_continue_alternate`, `test_edit_marks_downstream_turns_stale`, `test_truncate_stale_removes_downstream_rows`, and `test_keep_stale_records_user_choice`. The regenerate/swipe/continue tests must monkeypatch or fake the LLM client/helper and prove it is called with server-side settings and `build_prompt_context`; local-only alternate mutation is not acceptable.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests --collect-only -q</automated>
    <automated>rg "test_regenerate_calls_llm|test_swipe_calls_llm|test_continue_calls_llm|source_action.*swipe|source_action.*continue" web-ui/server/tests/test_message_actions.py web-ui/server/app/domain/message_actions.py</automated>
  </verify>
  <acceptance_criteria>
    - Regenerate test asserts one canonical visible AI turn after replacement.
    - Swipe test asserts `message_alternates.source_action='swipe'` and incremented `alternate_index`.
    - Continue test asserts `source_action='continue'` and composer text enters the LLM request.
    - A prompt-context assertion proves unselected alternates are excluded from future context.
  </acceptance_criteria>
  <done>Message-action contracts require the real server-side LLM generation path for regenerate, swipes, and continue.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> message APIs | User edits and actions mutate durable branch state. |
| server -> LLM endpoint | Regenerate, swipe, continue, and send use sensitive server settings. |
| lorebook -> prompt builder | Preserved imported world-info must not enter Phase 1 prompt context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-KEY | I | `test_chat_stream.py`, `test_message_actions.py` | mitigate | Contracts require server-side settings and reject browser overrides/key echo. |
| T-01-02-BRANCH | T/D | `test_message_actions.py` | mitigate | Tests cover selected alternates, stale flags, regenerate replacement, and unselected-alternate exclusion. |
| T-01-02-PROMPT | T | `test_prompt_builder.py` | mitigate | Tests require lorebook/world-info exclusion from prompt context. |
| T-01-02-STREAM | D/T | `test_chat_stream.py` | mitigate | Tests require exactly one final persisted AI message and full done-message shape. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests --collect-only -q`.
</verification>

<success_criteria>
Backend chat contracts are complete when all specified tests and interfaces exist, collect, and encode LLM-backed regenerate/swipe/continue semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-02-SUMMARY.md`.
</output>
