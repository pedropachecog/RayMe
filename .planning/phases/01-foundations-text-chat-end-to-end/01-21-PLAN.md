---
phase: 01-foundations-text-chat-end-to-end
plan: "21"
type: execute
wave: 7
depends_on: ["14", "16", "17"]
files_modified:
  - web-ui/client/src/routes/chat/[threadId]/+page.svelte
  - web-ui/client/src/lib/api/chat.ts
  - web-ui/client/src/lib/components/ChatMessageBubble.svelte
  - web-ui/client/src/lib/components/Composer.svelte
  - web-ui/client/tests/unit/chat.test.ts
  - web-ui/client/tests/e2e/chat-stream.spec.ts
autonomous: true
requirements: [REQ-17, REQ-30, REQ-31, REQ-60, REQ-90, REQ-A0]
must_haves:
  truths:
    - "Chat hydrates from `GET /api/threads/{thread_id}` and renders selected alternates, alternate lists, and stale flags."
    - "Opening alternate-greeting display reflects the already persisted pre-create selection; Chat does not mutate the opening alternate with a post-create switch endpoint."
    - "Sending a message renders one streaming AI bubble and replaces it with the returned `done.message` ThreadMessage object."
  artifacts:
    - path: "web-ui/client/src/routes/chat/[threadId]/+page.svelte"
      provides: "Text chat route"
    - path: "web-ui/client/src/lib/api/chat.ts"
      provides: "Thread load and send API wrapper"
    - path: "web-ui/client/src/lib/components/Composer.svelte"
      provides: "Multiline chat composer"
  key_links:
    - from: "web-ui/client/src/lib/api/chat.ts"
      to: "web-ui/client/src/lib/api/stream.ts"
      via: "readChatStream"
      pattern: "readChatStream"
    - from: "web-ui/client/src/routes/chat/[threadId]/+page.svelte"
      to: "web-ui/server/app/api/threads.py"
      via: "`GET /api/threads/{thread_id}` persisted opening alternate hydration"
      pattern: "alternate_greeting|selected_alternate_id"
---

<objective>
Implement the chat route, composer, initial hydration, and streaming send UI.

Purpose: Deliver the base messenger experience before adding message actions and virtualization.
Output: Chat route, chat API wrapper, message bubble, composer, unit tests, and stream E2E.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@docs/stitch/html/voice-call-true-dark.html
@.planning/phases/01-foundations-text-chat-end-to-end/01-14-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-16-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-17-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement chat route and streaming send</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` Chat contract.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-27, D-28, and D-38.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` Chat visual pattern and Text Streaming Contract.
    - Existing API wrappers and backend summary from Plan 14.
  </read_first>
  <files>web-ui/client/src/routes/chat/[threadId]/+page.svelte, web-ui/client/src/lib/api/chat.ts, web-ui/client/src/lib/components/ChatMessageBubble.svelte, web-ui/client/src/lib/components/Composer.svelte, web-ui/client/tests/unit/chat.test.ts, web-ui/client/tests/e2e/chat-stream.spec.ts</files>
  <action>Implement `/chat/[threadId]` with header showing character portrait, character name, and thread title. Do not show a call button. Implement `loadThread(threadId)` calling exactly `GET /api/threads/{thread_id}` and rendering metadata, chronological messages, selected alternates, alternate lists, and stale flags from that response. User text aligns end/right; AI text aligns start/left with portrait context. The alternate-greeting choice is made before thread creation in Home/Gallery via `alternate_greeting_index`; in Chat, the opening AI message must display the already persisted selected opening alternate from hydration and may label it as the selected greeting until the first user message is sent, but it must not call or imply a post-create switch endpoint. If the UI offers changing the greeting after creation, it must clearly start a new thread through the existing pre-create flow instead of mutating the current thread. `Composer` is multiline; Enter sends and Shift+Enter inserts newline. On send, keep the user message, create one streaming AI bubble, append token text through `readChatStream`, and on `onDone(message: ThreadMessage)` replace the streaming bubble with the full returned message object. Preserve `id`, `message_kind`, `role`, `sequence`, resolved selected content, `selected_alternate_id`, ordered `alternates`, and `stale_after_edit`; do not do message-id-only hydration. On stream error, keep the user message and render exact copy `RayMe cannot reach the LLM endpoint. Check Settings, run Test Connection, and try again.` plus retry/regenerate affordance.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run chat</automated>
    <automated>npm --prefix web-ui/client run test:e2e -- chat-stream.spec.ts</automated>
    <automated>rg "loadThread|/api/threads/|selected_alternate_id|stale_after_edit|alternates|onDone" web-ui/client/src/lib/api/chat.ts web-ui/client/src/routes/chat/[threadId]/+page.svelte web-ui/client/tests/unit/chat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Chat tests prove no `Call` button renders.
    - Tests prove initial load calls `GET /api/threads/{thread_id}` and renders selected alternates, alternate lists, and stale flags.
    - Tests prove the opening alternate greeting rendered in Chat comes from thread hydration and no alternate-greeting switch endpoint is called after thread creation.
    - Tests prove token chunks append into one AI bubble.
    - Chat-stream E2E proves full `done.message` replaces the streaming bubble and preserves ThreadMessage fields.
    - Error copy exactly matches the UI-SPEC error state.
  </acceptance_criteria>
  <done>Chat can send typed messages, render token streaming, and recover from LLM stream failures.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend stream -> chat DOM | LLM output text becomes user-visible UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-21-XSS | E | `ChatMessageBubble.svelte` | mitigate | Use sanitized Markdown renderer for AI/card-derived prose. |
| T-01-21-STREAM | D/T | streaming bubble | mitigate | One streaming bubble receives tokens and final persisted message replaces it. |
| T-01-21-ALT | T/D | opening alternate display | mitigate | Tests prove Chat displays persisted selected opening alternate and does not call a post-create switch endpoint. |
| T-01-21-FUTURE | T | chat header | mitigate | Tests prove no nonfunctional Call button. |
</threat_model>

<verification>
Run `npm --prefix web-ui/client run test:unit -- --run chat` and `npm --prefix web-ui/client run test:e2e -- chat-stream.spec.ts`.
</verification>

<success_criteria>
Base Chat UI is complete when hydration, streaming, done-message replacement, and error-state tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-21-SUMMARY.md`.
</output>
