---
phase: 01-foundations-text-chat-end-to-end
plan: "14"
type: execute
wave: 6
depends_on: ["11", "13"]
files_modified:
  - web-ui/server/app/domain/prompt_builder.py
  - web-ui/server/app/domain/llm_stream.py
  - web-ui/server/app/api/chat.py
  - web-ui/server/tests/test_prompt_builder.py
  - web-ui/server/tests/test_chat_stream.py
autonomous: true
requirements: [REQ-03, REQ-16, REQ-30, REQ-34, REQ-35, REQ-60]
must_haves:
  truths:
    - "Prompt context uses chronological, non-stale, selected-branch messages only."
    - "Streaming chat uses server-side settings and emits token events plus full `done.message` ThreadMessageShape."
  artifacts:
    - path: "web-ui/server/app/api/chat.py"
      provides: "SSE chat send endpoint"
    - path: "web-ui/server/app/domain/llm_stream.py"
      provides: "Streaming and collect helpers over OpenAI-compatible client"
    - path: "web-ui/server/app/domain/prompt_builder.py"
      provides: "Selected-branch prompt context"
  key_links:
    - from: "web-ui/server/app/api/chat.py"
      to: "web-ui/server/app/domain/llm_stream.py"
      via: "StreamingResponse token generator"
      pattern: "text/event-stream"
---

<objective>
Implement prompt building and the server-side streaming LLM proxy for normal send.

Purpose: Deliver D-09, D-23, D-28, D-30, and the preserved SSE done-message contract before message actions reuse the same LLM path.
Output: Prompt builder, LLM stream helper, chat endpoint, and tests.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-11-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-13-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement selected-branch prompt builder</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` REQ-16, REQ-34, REQ-35, and REQ-60.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-23, D-30, and D-32.
    - Existing thread hydration contract from Plan 13.
    - Target files if present.
  </read_first>
  <files>web-ui/server/app/domain/prompt_builder.py, web-ui/server/tests/test_prompt_builder.py</files>
  <action>Implement `build_prompt_context(thread_id, *, until_message_id=None, action=None, composer_text=None)` so normal send, regenerate, swipe, and continue can all use the same selected-branch context builder. Include character persona fields and chronological non-stale messages using resolved selected content. Exclude `lorebook_json`, world-info payloads, stale downstream rows, and unselected `message_alternates`. For continue, include composer text as extension instruction/prefix without mutating storage. For regenerate/swipe, allow the caller to build context up to the previous user input or selected branch position.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_prompt_builder.py -q</automated>
    <automated>rg "lorebook_json|selected_alternate_id|composer_text|until_message_id" web-ui/server/app/domain/prompt_builder.py web-ui/server/tests/test_prompt_builder.py</automated>
  </verify>
  <acceptance_criteria>
    - Prompt tests prove lorebook/world-info is absent from LLM messages.
    - Prompt tests prove unselected alternates do not enter context.
    - Prompt tests prove composer text appears in continue context.
    - Prompt tests prove stale rows are excluded when truncate/selected branch requires it.
  </acceptance_criteria>
  <done>Prompt context is deterministic and safe for send, regenerate, swipe, and continue.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SSE chat stream and LLM helpers</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` REQ-03 and REQ-30.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-09 and D-28.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` Text Streaming Contract.
    - Settings implementation from Plan 11.
  </read_first>
  <files>web-ui/server/app/domain/llm_stream.py, web-ui/server/app/api/chat.py, web-ui/server/tests/test_chat_stream.py</files>
  <action>Implement `stream_chat_completion(settings, messages)` and `collect_chat_completion(settings, messages)` over the server-side OpenAI-compatible client. Implement `POST /api/chat/{thread_id}/send` accepting `{content: string}`, writing a `user_text` message, building prompt context, streaming token events as `data: {"type":"token","text":"..."}`, persisting the final AI message exactly once on upstream completion, and emitting final `data: {"type":"done","message":{...ThreadMessageShape...}}`. The done `message` must match `GET /api/threads/{thread_id}` message shape including `id`, `message_kind`, `role`, `sequence`, resolved selected content, `selected_alternate_id`, ordered `alternates`, and `stale_after_edit`. On upstream failure, keep the user message and emit an error event without storing partial AI text as final. Request body cannot override base URL, API key, or model.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_chat_stream.py -q</automated>
    <automated>rg "text/event-stream|AsyncOpenAI|collect_chat_completion|data: |selected_alternate_id|stale_after_edit" web-ui/server/app web-ui/server/tests/test_chat_stream.py</automated>
  </verify>
  <acceptance_criteria>
    - Streaming tests prove token events arrive before done.
    - Tests prove final AI text is inserted exactly once.
    - Tests prove done event includes the full ThreadMessageShape.
    - Tests prove browser request body cannot override server LLM settings.
  </acceptance_criteria>
  <done>Text chat streams through the server-side OpenAI-compatible path and persists clean final messages.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> chat API | User text enters storage and LLM context. |
| server -> LLM endpoint | Sensitive server settings are used for completions. |
| lorebook -> prompt builder | Preserved card data must not become prompt input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14-KEY | I | `chat.py`, `llm_stream.py` | mitigate | Use persisted server settings only; tests reject browser override and key echo. |
| T-01-14-PROMPT | T | `prompt_builder.py` | mitigate | Tests exclude lorebook/world-info and unselected alternates. |
| T-01-14-STREAM | D/T | `llm_stream.py` | mitigate | Persist final AI once; error event does not store partial final text. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests/test_prompt_builder.py web-ui/server/tests/test_chat_stream.py -q`.
</verification>

<success_criteria>
LLM send path is complete when prompt and stream tests pass and the SSE done-message shape is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-14-SUMMARY.md`.
</output>
