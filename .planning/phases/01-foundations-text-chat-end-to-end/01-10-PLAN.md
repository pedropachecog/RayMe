---
phase: 01-foundations-text-chat-end-to-end
plan: "10"
type: execute
wave: 4
depends_on: ["01", "07", "08"]
files_modified:
  - web-ui/server/app/config.py
  - web-ui/server/app/main.py
  - web-ui/server/app/security.py
  - web-ui/server/scripts/run_dev_https.py
  - web-ui/server/tests/test_config.py
autonomous: true
requirements: [REQ-01, REQ-03, REQ-04, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "Server startup rejects wildcard binds and wildcard CORS origins."
    - "The web host can serve the built static client when present and still run API tests without it."
    - "HTTPS dev runner uses mkcert direct-IP workflow."
  artifacts:
    - path: "web-ui/server/app/security.py"
      provides: "CORS allowlist and CSP middleware"
    - path: "web-ui/server/scripts/run_dev_https.py"
      provides: "Explicit LAN HTTPS runner"
  key_links:
    - from: "web-ui/server/app/main.py"
      to: "web-ui/server/app/security.py"
      via: "security middleware setup"
      pattern: "configure_cors"
---

<objective>
Implement the web host configuration, origin policy, CSP headers, static serving, and HTTPS runner.

Purpose: Deliver D-10 through D-13 without mixing endpoint behavior into network/bootstrap safety work.
Output: Config validation, security middleware, static host wiring, HTTPS runner, and tests.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/00-measurement-gate/HTTPS-SETUP.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-07-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-08-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement explicit LAN config, CORS, CSP, and static serving</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-10 through D-13.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` Explicit Origin Policy and HTTPS And LAN Bind.
    - `web-ui/server/config.example.env`.
    - Target files if present.
  </read_first>
  <files>web-ui/server/app/config.py, web-ui/server/app/main.py, web-ui/server/app/security.py, web-ui/server/tests/test_config.py</files>
  <action>Implement Pydantic settings for `RAYME_WEB_BIND_HOST`, `RAYME_WEB_PORT`, TLS cert/key, `RAYME_ALLOWED_ORIGINS`, AI backend URL, and LLM base URL/key/model. The runtime default host must be `127.0.0.1`; docs/config examples use `192.168.1.199`. Reject `0.0.0.0`; parse allowed origins as an explicit comma-separated list; reject `*`. Add `configure_cors(app, allowed_origins)` with FastAPI CORSMiddleware and `allow_credentials=False`. Add CSP/security headers with `default-src 'self'`, `connect-src 'self'`, `img-src 'self' data: blob:`, and `style-src 'self' 'unsafe-inline'`. Mount `web-ui/client/build` when present with SPA fallback to `200.html`; leave API tests usable when the build directory is absent.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_config.py -q</automated>
    <automated>rg "RAYME_WEB_BIND_HOST|RAYME_ALLOWED_ORIGINS|configure_cors|default-src 'self'" web-ui/server/app web-ui/server/tests/test_config.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert `RAYME_WEB_BIND_HOST=0.0.0.0` raises a config error.
    - Tests assert `RAYME_ALLOWED_ORIGINS=*` raises a config error.
    - CSP contains `default-src 'self'`.
    - Static client mount is conditional on build output existing.
  </acceptance_criteria>
  <done>The web host has explicit bind/origin controls, CSP headers, and safe static serving.</done>
</task>

<task type="auto">
  <name>Task 2: Add web HTTPS dev runner</name>
  <read_first>
    - `.planning/phases/00-measurement-gate/HTTPS-SETUP.md`.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` HTTPS/mkcert guidance.
    - `web-ui/server/config.example.env`.
    - Target file if present.
  </read_first>
  <files>web-ui/server/scripts/run_dev_https.py, web-ui/server/tests/test_config.py</files>
  <action>Create a CLI that reads config keys, refuses `--host 0.0.0.0`, requires cert/key paths for HTTPS, and starts uvicorn with `ssl_certfile` and `ssl_keyfile`. Add `--check-config` mode. Help text must show `RAYME_WEB_BIND_HOST=192.168.1.199`, `RAYME_WEB_PORT=8443`, and browser URL `https://192.168.1.199:8443`. Extend config tests to validate a safe check-config run and wildcard-host rejection.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_config.py -q</automated>
    <automated>uv run --project web-ui/server python web-ui/server/scripts/run_dev_https.py --check-config --host 127.0.0.1 --port 8443 --cert web-ui/server/config.example.env --key web-ui/server/config.example.env</automated>
  </verify>
  <acceptance_criteria>
    - Runner help contains `https://192.168.1.199:8443`.
    - `--check-config --host 0.0.0.0` exits nonzero in tests.
    - Runner passes cert/key values to uvicorn as `ssl_certfile` and `ssl_keyfile`.
  </acceptance_criteria>
  <done>The web server can start over mkcert HTTPS on a specific LAN IP and refuses wildcard binds.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN browser -> FastAPI app | Unauthenticated LAN clients call static assets and APIs. |
| HTTPS config -> network interface | Runtime bind/TLS settings determine LAN exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10-BIND | I/T | `config.py`, `run_dev_https.py` | mitigate | Reject `0.0.0.0` and require explicit host/IP. |
| T-01-10-CORS | S/T | `security.py`, `main.py` | mitigate | Reject wildcard origins and configure explicit CORS allowlist. |
| T-01-10-XSS | E | CSP middleware | mitigate | Add CSP headers; sanitizer remains the content-render boundary. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests/test_config.py -q`.
</verification>

<success_criteria>
Web host network safety is complete when explicit bind/origin tests pass and the HTTPS runner validates direct-IP startup.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-10-SUMMARY.md`.
</output>
