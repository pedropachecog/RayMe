---
phase: 01-foundations-text-chat-end-to-end
plan: "10"
type: execute
wave: 5
depends_on: ["07", "08"]
files_modified:
  - web-ui/client/src/routes/chat/[threadId]/+page.svelte
  - web-ui/client/src/lib/api/chat.ts
  - web-ui/client/src/lib/components/ChatMessageBubble.svelte
  - web-ui/client/src/lib/components/Composer.svelte
  - web-ui/client/src/lib/components/MessageActionMenu.svelte
  - web-ui/client/src/lib/components/SwipeStepper.svelte
  - web-ui/client/src/lib/components/ConfirmDialog.svelte
  - web-ui/client/tests/unit/chat.test.ts
  - web-ui/client/tests/e2e/chat-stream.spec.ts
  - web-ui/client/tests/e2e/chat-virtualization.spec.ts
autonomous: true
requirements: [REQ-17, REQ-30, REQ-31, REQ-32, REQ-33, REQ-34, REQ-35, REQ-36, REQ-60, REQ-90, REQ-A0]
must_haves:
  truths:
    - "Chat renders a chronological message list with opening greeting and token-by-token streaming output."
    - "AI turns expose Regenerate, Swipe, Edit, and Continue; user turns expose Edit."
    - "Editing marks downstream turns stale and presents truncate-or-keep choice."
    - "Long threads virtualize around 500 messages and show jump-to-latest when scrolled away."
  artifacts:
    - path: "web-ui/client/src/routes/chat/[threadId]/+page.svelte"
      provides: "Phase 1 text-chat route"
    - path: "web-ui/client/src/lib/components/ChatMessageBubble.svelte"
      provides: "User/AI message bubble, streaming, stale, and action anchor"
    - path: "web-ui/client/src/lib/components/SwipeStepper.svelte"
      provides: "AI alternate response selector"
    - path: "web-ui/client/src/lib/components/Composer.svelte"
      provides: "Multiline send/continue composer"
  key_links:
    - from: "web-ui/client/src/lib/api/chat.ts"
      to: "web-ui/client/src/lib/api/stream.ts"
      via: "readChatStream"
      pattern: "readChatStream"
    - from: "web-ui/client/src/routes/chat/[threadId]/+page.svelte"
      to: "web-ui/client/src/lib/api/chat.ts"
      via: "send/regenerate/swipe/edit/continue API calls"
      pattern: "regenerate"
---

<objective>
Implement the Phase 1 text-chat route and message interaction UI.

Purpose: Complete D-26 through D-34 in the browser with a polished RayMe messenger surface backed by the server contracts.
Output: Chat route, streaming integration, message action controls, swipe stepper, composer, virtualization, unit tests, and chat E2E specs.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@docs/stitch/DESIGN.md
@docs/stitch/html/voice-call-true-dark.html
@.planning/phases/01-foundations-text-chat-end-to-end/01-07-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-08-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement chat route, composer, and streaming render</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` Chat contract.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-27 and D-28.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` Chat visual pattern and Text Streaming Contract.
    - Existing API wrappers and backend summary from Plan 07.
  </read_first>
  <files>web-ui/client/src/routes/chat/[threadId]/+page.svelte, web-ui/client/src/lib/api/chat.ts, web-ui/client/src/lib/components/ChatMessageBubble.svelte, web-ui/client/src/lib/components/Composer.svelte, web-ui/client/tests/unit/chat.test.ts, web-ui/client/tests/e2e/chat-stream.spec.ts</files>
  <action>Implement `/chat/[threadId]` with a spacious modern messenger layout per D-38: header showing character portrait, character name, and current thread title. Do not show a call button. Fetch thread messages and render chronological bubbles: user text aligned end/right, AI text aligned start/left with character portrait context. On new chat with alternate greetings, show an alternate-greeting control attached to the opening AI message until the first user message is sent. Implement `Composer` as a multiline input with `Send`; Enter sends and Shift+Enter inserts newline. On send, optimistically keep the user message, create one streaming AI bubble, use `readChatStream` to append token text with an accent caret, and replace the streaming buffer with final persisted message on done without layout jump. On stream error, keep the user message and render inline copy `RayMe cannot reach the LLM endpoint. Check Settings, run Test Connection, and try again.` plus retry/regenerate affordance.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run chat</automated>
    <automated>npm --prefix web-ui/client run test:e2e -- chat-stream.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Chat tests prove no `Call` button renders.
    - Chat tests prove token chunks append into one AI bubble.
    - Chat-stream E2E proves final persisted message replaces streaming text after done.
    - Error copy exactly matches the UI-SPEC error state.
  </acceptance_criteria>
  <done>Chat can send typed messages, render token streaming, and recover from LLM stream failures.</done>
</task>

<task type="auto">
  <name>Task 2: Implement contextual message actions, stale flags, and swipes</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` REQ-32 through REQ-35.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-29 through D-33.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` Chat interaction contract and Copywriting Contract.
    - Existing chat components from Task 1.
  </read_first>
  <files>web-ui/client/src/routes/chat/[threadId]/+page.svelte, web-ui/client/src/lib/api/chat.ts, web-ui/client/src/lib/components/ChatMessageBubble.svelte, web-ui/client/src/lib/components/MessageActionMenu.svelte, web-ui/client/src/lib/components/SwipeStepper.svelte, web-ui/client/src/lib/components/ConfirmDialog.svelte, web-ui/client/tests/unit/chat.test.ts</files>
  <action>Implement per-message contextual actions. AI turns expose `Regenerate`, `Swipe`, `Edit`, and `Continue`; user turns expose `Edit`. Desktop shows actions on hover/focus; mobile uses tap overflow. `Regenerate` calls backend regenerate and replaces the current selected AI response in place. `SwipeStepper` shows previous button, current index text like `2 / 3`, next button, and generate alternate action; selecting a swipe updates the canonical visible AI response. Editing any prior turn opens inline edit; on save, downstream stale turns show a subtle left rail and `Stale` chip. Continuing after an edit opens the truncate-or-keep confirmation with copy exactly `Remove stale turns after this edit? The selected message stays and later turns are removed from this branch.` Continue uses current composer text, when present, as the instruction/prefix for extending the previous AI turn.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run chat</automated>
    <automated>rg "Regenerate|Swipe|Edit|Continue|Stale|Remove stale turns after this edit\\?|selected alternate" web-ui/client/src web-ui/client/tests/unit/chat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove AI menu has Regenerate/Swipe/Edit/Continue and user menu has Edit only.
    - Tests prove regenerate does not append a second canonical AI bubble.
    - Tests prove stale chip appears on downstream messages after edit.
    - Tests prove continue sends composer text to the continue API.
  </acceptance_criteria>
  <done>Chat exposes the full Phase 1 SillyTavern text action surface with durable backend calls.</done>
</task>

<task type="auto">
  <name>Task 3: Add virtualization, jump-to-latest, and mobile layout coverage</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` REQ-36 and REQ-A0.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` Chat and Responsive Contract.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md` Don't Hand-Roll Long chat virtualization.
    - Existing chat route/components from Tasks 1 and 2.
  </read_first>
  <files>web-ui/client/src/routes/chat/[threadId]/+page.svelte, web-ui/client/src/lib/components/ChatMessageBubble.svelte, web-ui/client/src/lib/components/Composer.svelte, web-ui/client/tests/unit/chat.test.ts, web-ui/client/tests/e2e/chat-virtualization.spec.ts</files>
  <action>Use `@tanstack/svelte-virtual` for message virtualization once a thread has at least 500 messages. Preserve stable message heights during streaming and stale/edit state changes. Add a floating glass `Jump to latest` control above the composer when the user scrolls away from the bottom; it must not cover composer text or overlap mobile bottom navigation. Mobile layout must keep the composer sticky, use 44px minimum controls, and maintain readable bubble widths. Do not implement custom scroll-window math unless TanStack integration fails and the failure is documented in the summary with the replacement tests kept equivalent.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run chat</automated>
    <automated>npm --prefix web-ui/client run test:e2e -- chat-virtualization.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Virtualization E2E seeds at least 520 messages.
    - Virtualization E2E proves scroll position remains stable while a streaming token is appended.
    - E2E proves `Jump to latest` appears when scrolled away and returns to the bottom.
    - Mobile viewport checks prove composer and jump control do not overlap bottom navigation.
  </acceptance_criteria>
  <done>Long chat threads remain usable on desktop and mobile with maintained scroll position and jump-to-latest behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend stream -> chat DOM | LLM output text becomes user-visible UI. |
| message actions -> backend | User action controls mutate durable branches. |
| mobile viewport -> interaction controls | Touch UI can hide destructive/stale choices if layout overlaps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10-XSS | E | `ChatMessageBubble.svelte` | mitigate | Use sanitized Markdown renderer for AI/card-derived prose; never raw `{@html}` without sanitizer. |
| T-01-10-BRANCHDATA | T/D | Message action UI | mitigate | Tests prove regenerate, swipes, stale flags, truncate/keep, and continue map to backend APIs. |
| T-01-10-STREAM | D/T | Streaming bubble | mitigate | One streaming bubble receives tokens and final persisted message replaces it without duplicate canonical AI turn. |
| T-01-10-MOBILE | D | Composer/jump controls | mitigate | Mobile E2E checks prevent overlapping controls and hidden composer text. |
| T-01-10-FUTURE | T | Chat header | mitigate | Tests prove no nonfunctional Call button in Phase 1. |
</threat_model>

<verification>
Run:
`npm --prefix web-ui/client run test:unit -- --run chat`
`npm --prefix web-ui/client run test:e2e -- chat-stream.spec.ts chat-virtualization.spec.ts`
`npm --prefix web-ui/client run build`
</verification>

<success_criteria>
Chat UI is complete when streaming, message actions, stale handling, swipes, continue, virtualization, jump-to-latest, and mobile layout tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-10-SUMMARY.md`.
</output>
