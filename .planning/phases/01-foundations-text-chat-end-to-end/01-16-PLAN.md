---
phase: 01-foundations-text-chat-end-to-end
plan: "16"
type: execute
wave: 2
depends_on: ["05"]
files_modified:
  - web-ui/client/src/app.css
  - web-ui/client/src/routes/+layout.svelte
  - web-ui/client/src/routes/+page.svelte
  - web-ui/client/src/lib/components/AppShell.svelte
  - web-ui/client/src/lib/components/StatusChip.svelte
  - web-ui/client/src/lib/components/GlassPanel.svelte
  - web-ui/client/src/lib/components/ConfirmDialog.svelte
  - web-ui/client/src/lib/components/ToastStack.svelte
  - web-ui/client/tests/unit/app-shell.test.ts
autonomous: true
requirements: [REQ-70, REQ-90, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "The first loaded screen is the real Home app shell, not a landing page."
    - "Top-level navigation contains only Home, Gallery, and Settings."
  artifacts:
    - path: "web-ui/client/src/lib/components/AppShell.svelte"
      provides: "Desktop rail, mobile bottom nav, status area"
    - path: "web-ui/client/src/app.css"
      provides: "True Dark tokens"
  key_links:
    - from: "web-ui/client/src/routes/+layout.svelte"
      to: "web-ui/client/src/lib/components/AppShell.svelte"
      via: "layout wrapper"
      pattern: "AppShell"
---

<objective>
Implement the True Dark client app shell and shared UI primitives.

Purpose: Establish D-35 through D-40 and reusable shell/dialog/toast/status components before screen-specific plans modify routes.
Output: CSS tokens, app shell, status/glass/dialog/toast components, and tests.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@docs/stitch/DESIGN.md
@docs/stitch/html/home-true-dark.html
@.planning/phases/01-foundations-text-chat-end-to-end/01-05-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement shell tokens and shared primitives</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` Design System, App Shell, Color, Typography, and Responsive Contract.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-35 through D-40.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` App shell pattern, Visual System, and Scope Trimming.
    - Target files if present.
  </read_first>
  <files>web-ui/client/src/app.css, web-ui/client/src/routes/+layout.svelte, web-ui/client/src/routes/+page.svelte, web-ui/client/src/lib/components/AppShell.svelte, web-ui/client/src/lib/components/StatusChip.svelte, web-ui/client/src/lib/components/GlassPanel.svelte, web-ui/client/src/lib/components/ConfirmDialog.svelte, web-ui/client/src/lib/components/ToastStack.svelte, web-ui/client/tests/unit/app-shell.test.ts</files>
  <action>Use the approved UI-SPEC per D-05. Define CSS custom properties using UI-SPEC values: `#060e20`, `#091328`, `#141f38`, `#192540`, `#b6a0ff`, `#00e3fd`, `#dee5ff`, `#9eaad5`, and `#ff716c`; spacing `4px, 8px, 16px, 24px, 32px, 48px, 64px`; type sizes `12px, 14px, 20px, 28px`. Build `AppShell` with desktop left rail and mobile 64px bottom nav. Nav labels and routes are exactly `Home` -> `/`, `Gallery` -> `/gallery`, and `Settings` -> `/settings`. Build `StatusChip`, `GlassPanel`, `ConfirmDialog`, and `ToastStack` with accessible labels/focus. Update `+layout.svelte` to wrap routes in `AppShell` and `+page.svelte` to render Home shell content, not marketing copy.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run app-shell</automated>
    <automated>npm --prefix web-ui/client run build</automated>
  </verify>
  <acceptance_criteria>
    - `app.css` contains every hex value listed in the action.
    - `AppShell.svelte` contains only top-level labels `Home`, `Gallery`, and `Settings`.
    - App shell tests prove mobile bottom nav contains exactly three items.
    - `rg "Voice Lab|Call|Account|Billing|Logout|Subscribe" web-ui/client/src` returns no matches.
  </acceptance_criteria>
  <done>The client app shell matches Phase 1 scope and True Dark tokens without future-feature controls.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser viewport -> app shell | Navigation defines accessible app surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16-FUTURE | T | `AppShell.svelte` | mitigate | Tests and grep prevent fake future controls. |
| T-01-16-A11Y | D | shared primitives | mitigate | Accessible labels and focus states on buttons/dialogs/status chips. |
</threat_model>

<verification>
Run `npm --prefix web-ui/client run test:unit -- --run app-shell` and `npm --prefix web-ui/client run build`.
</verification>

<success_criteria>
Client shell is complete when tests/build pass and shared primitives match the approved UI scope.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-16-SUMMARY.md`.
</output>
