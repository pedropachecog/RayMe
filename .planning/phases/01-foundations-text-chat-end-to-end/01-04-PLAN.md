---
phase: 01-foundations-text-chat-end-to-end
plan: "04"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - web-ui/server/tests/fixtures/make_card_fixtures.py
  - web-ui/server/tests/fixtures/cards/v2_card.json
  - web-ui/server/tests/fixtures/cards/v3_card.json
  - web-ui/server/tests/fixtures/cards/malicious_card.json
  - web-ui/server/tests/fixtures/cards/v2_card.png
  - web-ui/server/tests/fixtures/cards/v3_card.png
  - web-ui/server/tests/fixtures/cards/dual_chunk_prefers_ccv3.png
autonomous: true
requirements: [REQ-13, REQ-16]
must_haves:
  truths:
    - "Card tests have deterministic v2 JSON, v3 JSON, PNG, dual-chunk, lorebook, and malicious payload fixtures."
    - "The dual PNG fixture proves `ccv3` precedence over `chara`."
  artifacts:
    - path: "web-ui/server/tests/fixtures/make_card_fixtures.py"
      provides: "Deterministic PNG text chunk fixture generator"
    - path: "web-ui/server/tests/fixtures/cards/malicious_card.json"
      provides: "XSS payload fixture"
  key_links:
    - from: "web-ui/server/tests/fixtures/cards/dual_chunk_prefers_ccv3.png"
      to: "web-ui/server/tests/test_card_import.py"
      via: "ccv3/chara precedence test"
      pattern: "ccv3|chara"
---

<objective>
Create the deterministic card fixture corpus used by parser, sanitizer, and acceptance tests.

Purpose: Give every card-import plan stable evidence for v2/v3 JSON, PNG chunk parsing, lorebook preservation, and malicious content handling.
Output: Fixture generator and card files.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build JSON and PNG card fixtures</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-19 through D-24.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md` Character-card parsing notes.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` Character Card Safety.
    - Target fixture files if present.
  </read_first>
  <files>web-ui/server/tests/fixtures/make_card_fixtures.py, web-ui/server/tests/fixtures/cards/v2_card.json, web-ui/server/tests/fixtures/cards/v3_card.json, web-ui/server/tests/fixtures/cards/malicious_card.json, web-ui/server/tests/fixtures/cards/v2_card.png, web-ui/server/tests/fixtures/cards/v3_card.png, web-ui/server/tests/fixtures/cards/dual_chunk_prefers_ccv3.png</files>
  <action>Create JSON fixtures containing `name`, `description`, `personality`, `scenario`, `first_mes`, `mes_example`, `creator_notes`, `character_version`, `alternate_greetings`, and lorebook/world-info fields. `malicious_card.json` must contain literal `<img src=x onerror=alert(1)>` and `javascript:alert(1)`. Add `make_card_fixtures.py` to generate tiny valid PNG files with base64-encoded `chara` and `ccv3` tEXt chunks using Pillow; `dual_chunk_prefers_ccv3.png` must contain both keys with different `name` values.</action>
  <verify>
    <automated>uv run --project web-ui/server python web-ui/server/tests/fixtures/make_card_fixtures.py --verify</automated>
    <automated>rg "onerror=alert\\(1\\)|javascript:alert\\(1\\)|ccv3|chara|alternate_greetings" web-ui/server/tests/fixtures</automated>
  </verify>
  <acceptance_criteria>
    - All fixture files listed in this task exist.
    - The malicious fixture contains both XSS payload literals.
    - The dual PNG fixture contains both `ccv3` and `chara` metadata.
    - The generator `--verify` command exits 0.
  </acceptance_criteria>
  <done>The backend has deterministic card fixtures covering normal, precedence, lorebook, and malicious payload cases.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture corpus -> parser tests | Test files model untrusted user uploads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-XSS | E/T | `malicious_card.json` | mitigate | Include concrete HTML/event and javascript URL payloads for sanitizer/parser tests. |
| T-01-04-PARSE | D | PNG fixtures | mitigate | Include `ccv3`, `chara`, and dual-key PNGs generated through Pillow. |
</threat_model>

<verification>
Run `uv run --project web-ui/server python web-ui/server/tests/fixtures/make_card_fixtures.py --verify`.
</verification>

<success_criteria>
Fixture work is complete when the generator verifies all card fixtures and the malicious/dual-key payloads are present.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-04-SUMMARY.md`.
</output>
