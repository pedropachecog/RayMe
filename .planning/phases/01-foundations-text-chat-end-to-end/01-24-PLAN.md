---
phase: 01-foundations-text-chat-end-to-end
plan: "24"
type: execute
wave: 10
depends_on: ["07", "10", "11", "12", "13", "14", "15", "18", "19", "20", "23"]
files_modified:
  - web-ui/client/tests/e2e/import-chat-reload.spec.ts
  - web-ui/client/tests/e2e/home-start-chat.spec.ts
  - web-ui/client/tests/e2e/https-status.spec.ts
  - web-ui/client/tests/e2e/mobile-text.spec.ts
  - web-ui/client/tests/e2e/ui-contract.spec.ts
  - web-ui/server/tests/test_phase1_acceptance.py
  - web-ui/server/docs/HTTPS-LAN.md
  - web-ui/server/docs/PHASE1-ACCEPTANCE.md
  - .planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md
autonomous: false
requirements: [REQ-01, REQ-03, REQ-04, REQ-10, REQ-11, REQ-12, REQ-13, REQ-14, REQ-16, REQ-17, REQ-30, REQ-31, REQ-32, REQ-33, REQ-34, REQ-35, REQ-36, REQ-60, REQ-70, REQ-71, REQ-72, REQ-90, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "The builder can import a v2/v3 card, save it, start a chat, stream a reply, regenerate, swipe, continue, reload, and continue."
    - "Desktop and Android-like mobile viewport tests pass for Phase 1 text surfaces."
    - "Web UI and AI-backend direct LAN HTTPS workflows are documented and physically checked on Android Chrome."
  artifacts:
    - path: "web-ui/client/tests/e2e/import-chat-reload.spec.ts"
      provides: "Full imported-character chat browser acceptance"
    - path: "web-ui/server/docs/HTTPS-LAN.md"
      provides: "mkcert direct-IP runbook for Web UI and AI-backend health"
    - path: ".planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md"
      provides: "Execution-updated validation status"
  key_links:
    - from: "web-ui/client/tests/e2e/import-chat-reload.spec.ts"
      to: "web-ui/server API and Svelte client"
      via: "browser import/chat/action/reload flow"
      pattern: "imported character chat reloads and continues"
---

<objective>
Close Phase 1 with automated end-to-end acceptance, HTTPS/mobile runbooks, and a physical Android Chrome checkpoint.

Purpose: Prove the balanced vertical slice from D-03 and D-04 over the Phase 0 mkcert LAN path, including generated backend responses for Regenerate, Swipes, and Continue.
Output: Passing full suites, runbooks, validation update, and human checkpoint.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/00-measurement-gate/HTTPS-SETUP.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-07-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-10-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-11-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-12-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-13-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-14-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-15-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-18-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-19-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-20-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-23-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Complete automated E2E and API acceptance</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md` Per-Task Verification Map.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` Screen Contracts and Copywriting Contract.
    - Existing E2E spec files from Plan 06.
    - Backend and frontend summaries from dependent plans.
  </read_first>
  <files>web-ui/client/tests/e2e/import-chat-reload.spec.ts, web-ui/client/tests/e2e/home-start-chat.spec.ts, web-ui/client/tests/e2e/https-status.spec.ts, web-ui/client/tests/e2e/mobile-text.spec.ts, web-ui/client/tests/e2e/ui-contract.spec.ts, web-ui/server/tests/test_phase1_acceptance.py</files>
  <action>Finish Playwright specs against the built Svelte client served by FastAPI. `import-chat-reload.spec.ts` must import a fixture card with alternate greetings and malicious description text, save in Editor, start chat, assert opening AI message, send a user message, assert token streaming, invoke Regenerate and assert a backend-generated replacement response, generate a Swipe and assert a backend-generated alternate is selected/retrievable, invoke Continue with composer text and assert a backend-generated continue alternate is selected, reload, and send another message in the same thread. `home-start-chat.spec.ts` verifies Home routes through real character selection. `https-status.spec.ts` asserts secure-context and media-device status text. `mobile-text.spec.ts` repeats import/chat/regenerate/swipe/continue/reload/continue on Android-like viewport without overlapping nav/composer. `ui-contract.spec.ts` asserts no Voice Lab, Call, Account, Billing, Logout, fake disabled future controls, search/filtering, or voice controls appear. Add `test_phase1_acceptance.py` covering API-level import -> thread -> stream -> regenerate -> swipe -> continue -> reload/query flow with fake LLM responses.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests -q</automated>
    <automated>npm --prefix web-ui/client run test:unit -- --run</automated>
    <automated>npm --prefix web-ui/client run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `rg "test\\.(skip|fixme|todo)" web-ui/client/tests web-ui/server/tests` returns no matches.
    - E2E tests assert `Lorebook present - not used in v1` and malicious HTML does not execute/render as active HTML.
    - E2E/API acceptance tests prove regenerate, swipe, and continue consume generated backend responses.
    - Backend acceptance proves same thread can continue after reload/query.
  </acceptance_criteria>
  <done>Automated backend, unit, and browser acceptance suites cover the full Phase 1 vertical slice.</done>
</task>

<task type="auto">
  <name>Task 2: Write HTTPS and acceptance runbooks</name>
  <read_first>
    - `.planning/phases/00-measurement-gate/HTTPS-SETUP.md`.
    - `docs/phase1-https-lan.md`.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md` Manual-Only Verifications.
    - Target docs if present.
  </read_first>
  <files>web-ui/server/docs/HTTPS-LAN.md, web-ui/server/docs/PHASE1-ACCEPTANCE.md</files>
  <action>Create `web-ui/server/docs/HTTPS-LAN.md` with exact env keys/commands for `RAYME_WEB_BIND_HOST=192.168.1.199`, `RAYME_WEB_PORT=8443`, mkcert cert/key paths, browser URL `https://192.168.1.199:8443`, AI-backend startup `uv run --project ai-backend python ai-backend/scripts/run_https.py --host 192.168.1.199 --port 9443 --cert <mkcert-cert.pem> --key <mkcert-key.pem>`, wildcard `0.0.0.0` rejection, and verification URL `https://192.168.1.199:9443/health`. State direct LAN IP HTTPS is sufficient and `rayme.local` is optional unless DNS/mDNS is configured. Create `PHASE1-ACCEPTANCE.md` with commands `uv run --project web-ui/server pytest web-ui/server/tests -q`, `npm --prefix web-ui/client run test:unit -- --run`, and `npm --prefix web-ui/client run test:e2e`; include manual Android steps and expected statuses `Secure`, `Media ready`, and `Connected` for Web UI, AI backend, and LLM when configured.</action>
  <verify>
    <automated>rg "RAYME_WEB_BIND_HOST=192\\.168\\.1\\.199|https://192\\.168\\.1\\.199:8443|https://192\\.168\\.1\\.199:9443/health|ai-backend/scripts/run_https\\.py|--cert|--key|direct LAN IP HTTPS is sufficient|npm --prefix web-ui/client run test:e2e|uv run --project web-ui/server pytest web-ui/server/tests -q" web-ui/server/docs</automated>
  </verify>
  <acceptance_criteria>
    - HTTPS runbook contains both Web UI and AI-backend direct-IP URLs.
    - Acceptance runbook contains all three automated commands.
    - Docs state `rayme.local` is optional unless DNS/mDNS is configured.
    - Neither doc instructs binding `0.0.0.0`.
  </acceptance_criteria>
  <done>Phase 1 has reproducible HTTPS and acceptance docs tied to implemented commands.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Android Chrome direct-IP HTTPS acceptance</name>
  <read_first>
    - `web-ui/server/docs/HTTPS-LAN.md`.
    - `web-ui/server/docs/PHASE1-ACCEPTANCE.md`.
    - `.planning/phases/00-measurement-gate/HTTPS-SETUP.md`.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md`.
  </read_first>
  <files>.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md</files>
  <action>After automated tests pass and both HTTPS services run on the configured LAN IP, ask the user to verify Android Chrome using `https://192.168.1.199:8443` and AI-backend health at `https://192.168.1.199:9443/health`, or the configured direct LAN IP from the runbook. Present the recommended direct-IP path first and use plain numbered verification steps per D-01 and D-02. Do not substitute Playwright for this physical-device trust-store check. After approval, update `01-VALIDATION.md` manual Android HTTPS row status to green with concrete Web UI and AI-backend URLs and date. If the user reports issues, leave the row pending/red and summarize the blocker in the plan summary.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:e2e -- https-status.spec.ts mobile-text.spec.ts</automated>
  </verify>
  <how-to-verify>
    1. Run the Web UI HTTPS command from `web-ui/server/docs/HTTPS-LAN.md`.
    2. Run the AI-backend HTTPS command from `web-ui/server/docs/HTTPS-LAN.md`.
    3. On Android Chrome, open `https://192.168.1.199:9443/health` unless the runbook has a different LAN IP; confirm health JSON loads with no certificate warning.
    4. On Android Chrome, open `https://192.168.1.199:8443` unless the runbook has a different LAN IP.
    5. Confirm no certificate warning.
    6. Confirm Home or Settings shows `Secure` and `Media ready`.
    7. Confirm Settings shows AI backend `Connected`.
    8. Import a fixture character, start a chat, stream a reply, regenerate, generate/select a swipe, continue with composer text, reload, and continue the same thread.
  </how-to-verify>
  <resume-signal>Type `approved` after the Android Chrome direct-IP HTTPS flow works, or describe the issue.</resume-signal>
  <acceptance_criteria>
    - Automated `https-status.spec.ts` and `mobile-text.spec.ts` pass before the checkpoint.
    - User confirms AI-backend health JSON over HTTPS with no certificate warning.
    - User confirms Web UI has no certificate warning.
    - User confirms Settings/Home shows `Secure`, `Media ready`, and AI backend `Connected`.
    - User confirms import/chat/regenerate/swipe/continue/reload works on physical Android.
    - `01-VALIDATION.md` is updated only after approval.
  </acceptance_criteria>
  <done>Physical Android Chrome direct-IP HTTPS acceptance is approved or a concrete blocker is recorded.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser E2E -> app APIs | Full workflow exercises unauthenticated LAN endpoints. |
| Android device -> HTTPS server | Physical trust store and LAN route validate mobile secure context. |
| acceptance docs -> operator | Runbooks can accidentally instruct unsafe binding or key exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-24-XSS | E/T | E2E malicious card flow | mitigate | E2E verifies malicious card content does not execute/render as active HTML. |
| T-01-24-KEY | I | Settings/E2E docs | mitigate | E2E and docs verify masked key and no key echo. |
| T-01-24-BIND | I/T | HTTPS runbook | mitigate | Docs use direct LAN IP and avoid `0.0.0.0`; checkpoint confirms exact URL. |
| T-01-24-BRANCH | T/D | E2E/API acceptance | mitigate | Acceptance covers regenerate, swipe, continue, selected alternates, and reload continuity. |
</threat_model>

<verification>
Run:
`uv run --project web-ui/server pytest web-ui/server/tests -q`
`npm --prefix web-ui/client run test:unit -- --run`
`npm --prefix web-ui/client run test:e2e`
Manual checkpoint: Android Chrome at `https://192.168.1.199:8443` reports secure context/media ready and completes import/chat/regenerate/swipe/continue/reload.
Manual checkpoint: Android Chrome at `https://192.168.1.199:9443/health` reports AI-backend health JSON over HTTPS with no certificate warning.
</verification>

<success_criteria>
Phase 1 acceptance is complete when all automated suites pass, runbooks exist, no skipped tests remain, and the user approves the physical Android Chrome direct-IP HTTPS flow for both Web UI and AI-backend health.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-24-SUMMARY.md`.
</output>
