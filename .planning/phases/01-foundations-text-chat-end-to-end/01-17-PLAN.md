---
phase: 01-foundations-text-chat-end-to-end
plan: "17"
type: execute
wave: 2
depends_on: ["05"]
files_modified:
  - web-ui/client/src/lib/api/client.ts
  - web-ui/client/src/lib/api/types.ts
  - web-ui/client/src/lib/api/characters.ts
  - web-ui/client/src/lib/api/threads.ts
  - web-ui/client/src/lib/api/settings.ts
  - web-ui/client/src/lib/api/stream.ts
  - web-ui/client/src/lib/browser/environment.ts
  - web-ui/client/tests/unit/api.test.ts
  - web-ui/client/tests/unit/environment.test.ts
autonomous: true
requirements: [REQ-01, REQ-03, REQ-04, REQ-10, REQ-11, REQ-12, REQ-13, REQ-14, REQ-17, REQ-30, REQ-31, REQ-60, REQ-70, REQ-72, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "Client API wrappers call RayMe backend routes only and reject direct provider URLs."
    - "Stream reader consumes token/done/error SSE events where done passes `onDone(message: ThreadMessage)`."
    - "Client wrappers create threads with selected character/alternate greeting and export v2 JSON through exact backend routes."
    - "Client character wrappers cover list, read, create, update, delete, import, portrait upload/remove, and v2 export with REQ-11 payload fields."
    - "Browser readiness helper exposes secure-context and media-device checks."
  artifacts:
    - path: "web-ui/client/src/lib/api/stream.ts"
      provides: "Typed SSE reader for chat streams"
    - path: "web-ui/client/src/lib/api/types.ts"
      provides: "ThreadMessage and endpoint status contracts"
    - path: "web-ui/client/src/lib/api/threads.ts"
      provides: "`createThread` wrapper for `POST /api/threads`"
    - path: "web-ui/client/src/lib/api/characters.ts"
      provides: "`listCharacters`, `getCharacter`, `createCharacter`, `updateCharacter`, `deleteCharacter`, `importCharacterCard`, `uploadPortrait`, `removePortrait`, and `exportCharacterV2` wrappers"
    - path: "web-ui/client/src/lib/browser/environment.ts"
      provides: "Secure-context/media-device browser checks"
  key_links:
    - from: "web-ui/client/src/lib/api/stream.ts"
      to: "web-ui/server/app/api/chat.py"
      via: "SSE `data:` JSON events"
      pattern: "onDone"
    - from: "web-ui/client/src/lib/api/threads.ts"
      to: "web-ui/server/app/api/threads.py"
      via: "`createThread({ character_id, title?, alternate_greeting_index? })`"
      pattern: "POST.*\\/api\\/threads|alternate_greeting_index"
    - from: "web-ui/client/src/lib/api/characters.ts"
      to: "web-ui/server/app/api/characters.py"
      via: "`exportCharacterV2(characterId)`"
      pattern: "export-v2"
    - from: "web-ui/client/src/lib/api/characters.ts"
      to: "web-ui/server/app/api/characters.py"
      via: "CRUD/import/portrait wrappers"
      pattern: "createCharacter|updateCharacter|deleteCharacter|importCharacterCard|uploadPortrait|removePortrait"
---

<objective>
Implement typed client API wrappers, stream reader, and browser readiness helpers.

Purpose: Preserve the server-side LLM boundary, full ThreadMessageShape contract, exact thread-creation route, and exact v2 export route before screens consume API data.
Output: Client networking/types, settings/character/thread wrappers, stream reader, environment helper, and tests.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-05-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement API types, wrappers, and SSE reader</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-09, D-28, and D-39.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` Client stream utility pattern and Text Streaming Contract.
    - Plan 14 contract for SSE `done.message`.
    - Target files if present.
  </read_first>
  <files>web-ui/client/src/lib/api/client.ts, web-ui/client/src/lib/api/types.ts, web-ui/client/src/lib/api/characters.ts, web-ui/client/src/lib/api/threads.ts, web-ui/client/src/lib/api/settings.ts, web-ui/client/src/lib/api/stream.ts, web-ui/client/tests/unit/api.test.ts</files>
  <action>Create `apiFetch(path, options)` that prepends `/api` for API paths and rejects absolute `http://` or `https://` URLs. Define types `CharacterSummary`, `CharacterDetail`, `CharacterEditorPayload`, `ThreadSummary`, `CreateThreadRequest`, `CreateThreadResponse`, `MessageAlternate`, `ThreadMessage`, `EndpointStatus`, and settings payloads. `CharacterSummary` or `CharacterDetail` must expose enough alternate-greeting data for Home/Gallery pre-create selection. `CharacterEditorPayload` must use the exact REQ-11 field names consumed by the backend: `name`, `description`, `personality`, `scenario`, `first_mes`, `mes_example`, `system_prompt`, `creator_notes`, `character_notes`, `tags`, `alternate_greetings`, `post_history_instructions`, `creator`, `character_version`, and portrait metadata returned by the portrait endpoint. `ThreadMessage` must include `id`, `message_kind`, `role`, `sequence`, resolved selected content, `selected_alternate_id`, ordered `alternates`, and `stale_after_edit`, with all six message kinds. Implement `createThread({ character_id, title?, alternate_greeting_index? })` in `threads.ts`; it must issue exactly `POST /api/threads`, send the selected `character_id` and optional `alternate_greeting_index`, and return the backend `thread_id` used for navigation to `/chat/{thread_id}`. Implement explicit character wrappers in `characters.ts`: `listCharacters() -> GET /api/characters`, `getCharacter(characterId) -> GET /api/characters/{character_id}`, `createCharacter(payload) -> POST /api/characters`, `updateCharacter(characterId, payload) -> PATCH /api/characters/{character_id}`, `deleteCharacter(characterId) -> DELETE /api/characters/{character_id}`, `importCharacterCard(file) -> POST /api/characters/import` with `FormData`, `uploadPortrait(characterId, file) -> PUT /api/characters/{character_id}/portrait` with `FormData`, `removePortrait(characterId) -> DELETE /api/characters/{character_id}/portrait`, and `exportCharacterV2(characterId) -> GET /api/characters/{character_id}/export-v2`; the returned export JSON must retain Phase 1 v2 export fields including `spec`, `spec_version`, and `data.name` or the equivalent locked backend v2 shape. Implement `readChatStream(response, handlers)` parsing `data: ` SSE JSON events with `type` values `token`, `done`, and `error`; call `onToken(text)`, `onDone(message: ThreadMessage)`, and `onError(message)`. Implement settings wrappers for `/api/settings`, `/api/settings/test/web`, `/api/settings/test/ai-backend`, and `/api/settings/test/llm`.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run api</automated>
    <automated>rg "apiFetch|createThread|POST.*/api/threads|alternate_greeting_index|listCharacters|GET.*/api/characters|getCharacter|createCharacter|POST.*/api/characters|updateCharacter|PATCH.*/api/characters|deleteCharacter|DELETE.*/api/characters|importCharacterCard|characters/import|uploadPortrait|removePortrait|/portrait|exportCharacterV2|export-v2|first_mes|mes_example|post_history_instructions|readChatStream|onDone\\(message|Connected|Unreachable|Unauthorized|Not configured|test/llm" web-ui/client/src/lib/api web-ui/client/tests/unit/api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - API tests prove absolute LLM/provider URLs are rejected.
    - API tests prove `createThread` calls exactly `POST /api/threads`, sends `character_id` and selected `alternate_greeting_index`, and returns `thread_id`.
    - API tests prove character wrappers call exact routes and methods: `GET /api/characters`, `GET /api/characters/{character_id}`, `POST /api/characters`, `PATCH /api/characters/{character_id}`, `DELETE /api/characters/{character_id}`, `POST /api/characters/import`, `PUT /api/characters/{character_id}/portrait`, `DELETE /api/characters/{character_id}/portrait`, and `GET /api/characters/{character_id}/export-v2`.
    - API tests prove `createCharacter` and `updateCharacter` send REQ-11 payload fields including `name`, `description`, `personality`, `scenario`, `first_mes`, `mes_example`, `system_prompt`, `creator_notes`, `character_notes`, `tags`, `alternate_greetings`, `post_history_instructions`, `creator`, and `character_version`.
    - API tests prove `exportCharacterV2(characterId)` calls exactly `GET /api/characters/{character_id}/export-v2` and returns JSON containing `spec`, `spec_version`, and `data.name` or the equivalent locked v2 shape.
    - Stream tests prove token, done, and error events dispatch to handlers.
    - Stream tests prove done calls `onDone(message: ThreadMessage)` with a full object.
    - No client source contains `RAYME_LLM_API_KEY` or `sk-`.
  </acceptance_criteria>
  <done>Client networking goes through RayMe APIs and can consume token-by-token chat streams.</done>
</task>

<task type="auto">
  <name>Task 2: Implement browser readiness helper</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decision D-11.
    - `.planning/phases/00-measurement-gate/HTTPS-SETUP.md`.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` App Shell and Settings contracts.
    - Target files if present.
  </read_first>
  <files>web-ui/client/src/lib/browser/environment.ts, web-ui/client/tests/unit/environment.test.ts</files>
  <action>Create `getBrowserReadiness()` returning `{ secureContext: boolean, mediaDevicesAvailable: boolean, protocol: string, href: string }` using `window.isSecureContext`, `navigator.mediaDevices`, `location.protocol`, and `location.href`, with browser guards. Tests must mock secure/insecure and media available/unavailable states and assert returned booleans and text consumers can distinguish state without color.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run environment</automated>
    <automated>rg "isSecureContext|navigator\\.mediaDevices|secureContext|mediaDevicesAvailable" web-ui/client/src/lib/browser/environment.ts web-ui/client/tests/unit/environment.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `environment.ts` references `window.isSecureContext` and `navigator.mediaDevices`.
    - Tests cover secure/insecure and media ready/unavailable states.
    - Environment tests exit 0.
  </acceptance_criteria>
  <done>Home and Settings have a reusable browser HTTPS/mobile readiness contract.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client API wrapper -> backend | Browser code chooses network targets. |
| backend stream -> chat UI | LLM output becomes user-visible UI. |
| browser capability checks -> UI | Secure-context status affects mobile readiness messaging. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17-KEY | I | `client.ts`, `settings.ts` | mitigate | Reject external URLs and never store API key values in client code. |
| T-01-17-THREAD | T/D | `threads.ts` | mitigate | Tests require exact `POST /api/threads` body with selected character and alternate greeting index. |
| T-01-17-EXPORT | I/T | `characters.ts` | mitigate | Tests require exact `GET /api/characters/{character_id}/export-v2` and v2 JSON shape. |
| T-01-17-STREAM | T/D | `stream.ts` | mitigate | Parse typed SSE JSON events and full done message object. |
| T-01-17-MOBILE | D | `environment.ts` | mitigate | Text-capable readiness state exposes mobile HTTPS blockers. |
</threat_model>

<verification>
Run `npm --prefix web-ui/client run test:unit -- --run "api|environment"`.
</verification>

<success_criteria>
Client API foundation is complete when unit tests pass, external URLs are rejected, and stream done uses the full ThreadMessage object.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-17-SUMMARY.md`.
</output>
