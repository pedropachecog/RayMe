---
phase: 01-foundations-text-chat-end-to-end
plan: "17"
type: execute
wave: 2
depends_on: ["05"]
files_modified:
  - web-ui/client/src/lib/api/client.ts
  - web-ui/client/src/lib/api/types.ts
  - web-ui/client/src/lib/api/settings.ts
  - web-ui/client/src/lib/api/stream.ts
  - web-ui/client/src/lib/browser/environment.ts
  - web-ui/client/tests/unit/api.test.ts
  - web-ui/client/tests/unit/environment.test.ts
autonomous: true
requirements: [REQ-01, REQ-03, REQ-04, REQ-30, REQ-60, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "Client API wrappers call RayMe backend routes only and reject direct provider URLs."
    - "Stream reader consumes token/done/error SSE events where done passes `onDone(message: ThreadMessage)`."
    - "Browser readiness helper exposes secure-context and media-device checks."
  artifacts:
    - path: "web-ui/client/src/lib/api/stream.ts"
      provides: "Typed SSE reader for chat streams"
    - path: "web-ui/client/src/lib/api/types.ts"
      provides: "ThreadMessage and endpoint status contracts"
    - path: "web-ui/client/src/lib/browser/environment.ts"
      provides: "Secure-context/media-device browser checks"
  key_links:
    - from: "web-ui/client/src/lib/api/stream.ts"
      to: "web-ui/server/app/api/chat.py"
      via: "SSE `data:` JSON events"
      pattern: "onDone"
---

<objective>
Implement typed client API wrappers, stream reader, and browser readiness helpers.

Purpose: Preserve the server-side LLM boundary and the full ThreadMessageShape contract before screens consume API data.
Output: Client networking/types, settings wrappers, stream reader, environment helper, and tests.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-05-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement API types, wrappers, and SSE reader</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-09, D-28, and D-39.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` Client stream utility pattern and Text Streaming Contract.
    - Plan 14 contract for SSE `done.message`.
    - Target files if present.
  </read_first>
  <files>web-ui/client/src/lib/api/client.ts, web-ui/client/src/lib/api/types.ts, web-ui/client/src/lib/api/settings.ts, web-ui/client/src/lib/api/stream.ts, web-ui/client/tests/unit/api.test.ts</files>
  <action>Create `apiFetch(path, options)` that prepends `/api` for API paths and rejects absolute `http://` or `https://` URLs. Define types `CharacterSummary`, `CharacterDetail`, `ThreadSummary`, `MessageAlternate`, `ThreadMessage`, `EndpointStatus`, and settings payloads. `ThreadMessage` must include `id`, `message_kind`, `role`, `sequence`, resolved selected content, `selected_alternate_id`, ordered `alternates`, and `stale_after_edit`, with all six message kinds. Implement `readChatStream(response, handlers)` parsing `data: ` SSE JSON events with `type` values `token`, `done`, and `error`; call `onToken(text)`, `onDone(message: ThreadMessage)`, and `onError(message)`. Implement settings wrappers for `/api/settings`, `/api/settings/test/web`, `/api/settings/test/ai-backend`, and `/api/settings/test/llm`.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run api</automated>
    <automated>rg "apiFetch|readChatStream|onDone\\(message|Connected|Unreachable|Unauthorized|Not configured|test/llm" web-ui/client/src/lib/api web-ui/client/tests/unit/api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - API tests prove absolute LLM/provider URLs are rejected.
    - Stream tests prove token, done, and error events dispatch to handlers.
    - Stream tests prove done calls `onDone(message: ThreadMessage)` with a full object.
    - No client source contains `RAYME_LLM_API_KEY` or `sk-`.
  </acceptance_criteria>
  <done>Client networking goes through RayMe APIs and can consume token-by-token chat streams.</done>
</task>

<task type="auto">
  <name>Task 2: Implement browser readiness helper</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decision D-11.
    - `.planning/phases/00-measurement-gate/HTTPS-SETUP.md`.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` App Shell and Settings contracts.
    - Target files if present.
  </read_first>
  <files>web-ui/client/src/lib/browser/environment.ts, web-ui/client/tests/unit/environment.test.ts</files>
  <action>Create `getBrowserReadiness()` returning `{ secureContext: boolean, mediaDevicesAvailable: boolean, protocol: string, href: string }` using `window.isSecureContext`, `navigator.mediaDevices`, `location.protocol`, and `location.href`, with browser guards. Tests must mock secure/insecure and media available/unavailable states and assert returned booleans and text consumers can distinguish state without color.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run environment</automated>
    <automated>rg "isSecureContext|navigator\\.mediaDevices|secureContext|mediaDevicesAvailable" web-ui/client/src/lib/browser/environment.ts web-ui/client/tests/unit/environment.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `environment.ts` references `window.isSecureContext` and `navigator.mediaDevices`.
    - Tests cover secure/insecure and media ready/unavailable states.
    - Environment tests exit 0.
  </acceptance_criteria>
  <done>Home and Settings have a reusable browser HTTPS/mobile readiness contract.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client API wrapper -> backend | Browser code chooses network targets. |
| backend stream -> chat UI | LLM output becomes user-visible UI. |
| browser capability checks -> UI | Secure-context status affects mobile readiness messaging. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17-KEY | I | `client.ts`, `settings.ts` | mitigate | Reject external URLs and never store API key values in client code. |
| T-01-17-STREAM | T/D | `stream.ts` | mitigate | Parse typed SSE JSON events and full done message object. |
| T-01-17-MOBILE | D | `environment.ts` | mitigate | Text-capable readiness state exposes mobile HTTPS blockers. |
</threat_model>

<verification>
Run `npm --prefix web-ui/client run test:unit -- --run "api|environment"`.
</verification>

<success_criteria>
Client API foundation is complete when unit tests pass, external URLs are rejected, and stream done uses the full ThreadMessage object.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-17-SUMMARY.md`.
</output>
