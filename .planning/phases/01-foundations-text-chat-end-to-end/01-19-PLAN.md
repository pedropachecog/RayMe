---
phase: 01-foundations-text-chat-end-to-end
plan: "19"
type: execute
wave: 6
depends_on: ["06", "12", "13", "16", "17"]
files_modified:
  - web-ui/client/src/routes/gallery/+page.svelte
  - web-ui/client/src/lib/api/characters.ts
  - web-ui/client/src/lib/components/CharacterCard.svelte
  - web-ui/client/src/lib/components/ImportCardDialog.svelte
  - web-ui/client/tests/unit/gallery.test.ts
autonomous: true
requirements: [REQ-10, REQ-12, REQ-13, REQ-14, REQ-16, REQ-17, REQ-31, REQ-72, REQ-90, REQ-A0]
must_haves:
  truths:
    - "Gallery imports JSON/PNG cards, shows sanitized character cards, and routes imported cards to Editor review/save."
    - "Gallery Start Chat calls `POST /api/threads` through `createThread`, passes selected character/alternate greeting, and routes to `/chat/{thread_id}`."
    - "Gallery Export JSON downloads v2 JSON returned by `GET /api/characters/{character_id}/export-v2`."
    - "Gallery exposes only Phase 1 actions and no search/filter/voice controls."
  artifacts:
    - path: "web-ui/client/src/routes/gallery/+page.svelte"
      provides: "Character Gallery route"
    - path: "web-ui/client/src/lib/components/ImportCardDialog.svelte"
      provides: "JSON/PNG import UI"
  key_links:
    - from: "web-ui/client/src/lib/components/CharacterCard.svelte"
      to: "web-ui/client/src/lib/sanitizer/renderMarkdown.ts"
      via: "sanitized snippet"
      pattern: "renderTrustedMarkdown"
    - from: "web-ui/client/src/routes/gallery/+page.svelte"
      to: "web-ui/client/src/lib/api/threads.ts"
      via: "`Start Chat` -> `createThread`"
      pattern: "createThread|alternate_greeting_index|/chat/"
    - from: "web-ui/client/src/routes/gallery/+page.svelte"
      to: "web-ui/client/src/lib/api/characters.ts"
      via: "`Export JSON` -> `exportCharacterV2`"
      pattern: "exportCharacterV2|export-v2"
---

<objective>
Implement the Character Gallery import and card grid UI.

Purpose: Deliver the Gallery side of character management separately from Home and Editor/Settings.
Output: Gallery route, character API wrapper, card/import components, and tests.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@docs/stitch/html/character-gallery-true-dark.html
@.planning/phases/01-foundations-text-chat-end-to-end/01-06-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-12-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-16-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-17-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement Gallery cards and import flow</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` Character Gallery contract.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-20, D-24, and D-25.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` Gallery/card pattern and Character Card Safety.
    - Existing sanitizer and character API contracts.
  </read_first>
  <files>web-ui/client/src/routes/gallery/+page.svelte, web-ui/client/src/lib/api/characters.ts, web-ui/client/src/lib/components/CharacterCard.svelte, web-ui/client/src/lib/components/ImportCardDialog.svelte, web-ui/client/tests/unit/gallery.test.ts</files>
  <action>Implement `/gallery` with `Import Character` and `Create Character`. Fetch `GET /api/characters` and render a responsive portrait grid. `CharacterCard` shows portrait, name, sanitized description snippet using `renderTrustedMarkdown`, tag count or first two tags, and actions `Start Chat`, `Edit`, `Export JSON`, and `Delete`. `Start Chat` must use the selected card's character ID, show a pre-create alternate-greeting picker when that character has alternate greetings per D-27/REQ-17, call `createThread({ character_id, title?, alternate_greeting_index? })` so the network call is exactly `POST /api/threads`, and navigate to `/chat/{thread_id}` using the returned `thread_id`. `Export JSON` must call `exportCharacterV2(characterId)`, which calls exactly `GET /api/characters/{character_id}/export-v2`, and download the returned JSON as a `.json` file with a stable v2-oriented filename such as `{safe-character-name}-v2.json`; tests must inspect the downloaded object for `spec`, `spec_version`, and `data.name` or the equivalent Phase 1 v2 export shape. `ImportCardDialog` accepts `.json` and `.png`, submits `POST /api/characters/import`, shows progress, warning chips `Lorebook present - not used in v1`, `Fields preserved`, and `Unsupported fields ignored`, and on success routes to `/characters/{id}?mode=review`. Import failures distinguish `Unsupported file`, `Malformed JSON`, `Unreadable PNG metadata`, and `Unsafe content`. Delete confirmation copy is exactly `Delete this character? Existing chats stay in history, but the character leaves the gallery.` No search, tag filters, voice badges, or disabled future controls.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run gallery</automated>
    <automated>rg "Import Character|Create Character|Start Chat|createThread|alternate_greeting_index|/chat/|Export JSON|exportCharacterV2|export-v2|Lorebook present - not used in v1|Malformed JSON|Unreadable PNG metadata|renderTrustedMarkdown" web-ui/client/src web-ui/client/tests/unit/gallery.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Gallery tests prove malicious snippets use `renderTrustedMarkdown`.
    - Import success routes to `/characters/{id}?mode=review`.
    - Gallery tests prove `Start Chat` calls `POST /api/threads` through `createThread` with the selected `character_id` and navigates to `/chat/{thread_id}` from the response.
    - Gallery tests prove selecting an alternate greeting before creation passes `alternate_greeting_index` into `createThread`.
    - Gallery tests prove `Export JSON` calls `GET /api/characters/{character_id}/export-v2` through `exportCharacterV2`, downloads a JSON file, and the downloaded object contains v2 fields such as `spec`, `spec_version`, and `data.name`.
    - Delete copy says existing chats stay in history.
    - `rg "Search|Filter|Voice|Call" web-ui/client/src/routes/gallery web-ui/client/src/lib/components/CharacterCard.svelte` returns no matches.
  </acceptance_criteria>
  <done>Gallery supports import/create/start/edit/export/delete with sanitized card rendering and scoped UI.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported card text -> Gallery DOM | Untrusted card fields render in browser. |
| file upload -> character API | JSON/PNG uploads cross from browser to backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-19-XSS | E/T | `CharacterCard.svelte` | mitigate | Route snippets through sanitizer with malicious tests. |
| T-01-19-UPLOAD | T/D | `ImportCardDialog.svelte` | mitigate | Restrict extensions and surface backend malformed/unsafe errors. |
| T-01-19-START | T/D | Gallery Start Chat | mitigate | Tests require selected character ID, optional alternate greeting index, exact `POST /api/threads`, and `/chat/{thread_id}` navigation. |
| T-01-19-EXPORT | I/T | Gallery Export JSON | mitigate | Tests require exact export-v2 endpoint and inspect downloaded v2 JSON shape. |
| T-01-19-FUTURE | T | Gallery route | mitigate | Tests block search/filter/voice/call controls. |
</threat_model>

<verification>
Run `npm --prefix web-ui/client run test:unit -- --run gallery`.
</verification>

<success_criteria>
Gallery UI is complete when unit tests pass and import/card actions use backend data and sanitizer boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-19-SUMMARY.md`.
</output>
