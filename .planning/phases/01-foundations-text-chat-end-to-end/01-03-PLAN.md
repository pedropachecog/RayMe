---
phase: 01-foundations-text-chat-end-to-end
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-backend/pyproject.toml
  - ai-backend/app/__init__.py
  - ai-backend/app/main.py
  - ai-backend/tests/test_health.py
  - llm/README.md
  - llm/openai-compatible.example.env
  - web-ui/server/config.example.env
  - docs/phase1-https-lan.md
autonomous: true
requirements: [REQ-01, REQ-03, REQ-04, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "The Phase 1 AI backend is a real FastAPI health service only, with no STT, TTS, VAD, or WebRTC implementation."
    - "The `llm/` directory contains docs/config for an external OpenAI-compatible endpoint, not a local RayMe LLM service."
    - "Phase 1 LAN HTTPS documentation uses the Phase 0 direct-IP mkcert path and rejects silent wildcard binding."
  artifacts:
    - path: "ai-backend/app/main.py"
      provides: "AI backend `/health` FastAPI stub for Settings connection tests"
      exports: ["create_app"]
    - path: "llm/README.md"
      provides: "External OpenAI-compatible LLM setup and Settings probe contract"
    - path: "web-ui/server/config.example.env"
      provides: "Concrete LAN/TLS/origin env keys for the web host"
  key_links:
    - from: "ai-backend/tests/test_health.py"
      to: "ai-backend/app/main.py"
      via: "FastAPI test client"
      pattern: "/health"
    - from: "llm/README.md"
      to: "web-ui/server Settings LLM probe"
      via: "documents `/api/settings/test/llm` expectation"
      pattern: "RayMe does not ship local inference"
---

<objective>
Create the non-chat service foundations: Phase 1 AI backend health stub, external LLM docs/config, and LAN HTTPS configuration guidance.

Purpose: Satisfy D-06, D-08, D-10, D-12, and the planner decision that LLM `/health` means a Settings connection probe, not a local service.
Output: `ai-backend` FastAPI health project, `llm` docs/config, and concrete HTTPS/LAN config examples.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/00-measurement-gate/KEY_DECISIONS.md
@.planning/phases/00-measurement-gate/HTTPS-SETUP.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement AI backend health-only FastAPI stub</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-06 and D-08.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md` Architectural Responsibility Map and Recommended Project Structure.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` `ai-backend` health stub and tests.
    - Target files if present.
  </read_first>
  <files>ai-backend/pyproject.toml, ai-backend/app/__init__.py, ai-backend/app/main.py, ai-backend/tests/test_health.py</files>
  <action>Create a uv-managed `ai-backend` project named `rayme-ai-backend` with dependencies `fastapi==0.136.1`, `uvicorn==0.46.0`, `httpx==0.28.1`, and dev dependency `pytest==9.0.3`. Implement `create_app()` and `app = create_app()` in `ai-backend/app/main.py`. Add `GET /health` returning JSON exactly shaped with keys `service`, `status`, `phase`, and `capabilities`: `service` is `rayme-ai-backend`, `status` is `ok`, `phase` is `01`, and `capabilities` is `["health"]`. Do not add model load, STT, TTS, VAD, WebRTC, GPU, or voice fields in Phase 1.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_health.py -q</automated>
    <automated>! rg "STT|TTS|VAD|WebRTC|model" ai-backend/app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `ai-backend/tests/test_health.py` asserts `/health` status code 200 and the exact JSON values from the action.
    - `uv run --project ai-backend pytest ai-backend/tests/test_health.py -q` exits 0.
    - `rg "STT|TTS|VAD|WebRTC|gpu|voice" ai-backend/app/main.py` returns no matches.
  </acceptance_criteria>
  <done>The AI backend is a tested, health-only Phase 1 service suitable for Settings connection tests.</done>
</task>

<task type="auto">
  <name>Task 2: Document external OpenAI-compatible LLM boundary</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-06 and D-09.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md` Pitfall 8 and Open Questions.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` `llm` docs/config.
    - Target files if present.
  </read_first>
  <files>llm/README.md, llm/openai-compatible.example.env</files>
  <action>Create `llm/README.md` explaining that RayMe does not ship local inference, that `llm/` is docs/config only, and that Phase 1 LLM status is checked by `web-ui/server` through `POST /api/settings/test/llm` against the configured OpenAI-compatible Chat Completions endpoint. Document concrete setting names `RAYME_LLM_BASE_URL`, `RAYME_LLM_API_KEY`, and `RAYME_LLM_MODEL`, with example base URLs `https://api.openai.com/v1` and `http://192.168.1.199:8080/v1`. Create `openai-compatible.example.env` with those keys and example values only. Do not create `llm/app.py`, `llm/server.py`, or any local `/health` server.</action>
  <verify>
    <automated>test -f llm/README.md && test -f llm/openai-compatible.example.env && test ! -f llm/app.py && test ! -f llm/server.py</automated>
    <automated>rg "RayMe does not ship local inference|/api/settings/test/llm|RAYME_LLM_BASE_URL|RAYME_LLM_API_KEY|RAYME_LLM_MODEL" llm</automated>
  </verify>
  <acceptance_criteria>
    - `llm/README.md` contains `RayMe does not ship local inference`.
    - `llm/README.md` contains `/api/settings/test/llm`.
    - `llm/openai-compatible.example.env` contains `RAYME_LLM_BASE_URL`, `RAYME_LLM_API_KEY`, and `RAYME_LLM_MODEL`.
    - No `llm/app.py` or `llm/server.py` file exists.
  </acceptance_criteria>
  <done>The LLM boundary is documented as external and OpenAI-compatible, with no local service planned or scaffolded.</done>
</task>

<task type="auto">
  <name>Task 3: Create LAN HTTPS config example and setup doc</name>
  <read_first>
    - `.planning/phases/00-measurement-gate/HTTPS-SETUP.md` entire file.
    - `.planning/phases/00-measurement-gate/KEY_DECISIONS.md` HTTPS on Android and Phase 1+ implications.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-10 through D-13.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` HTTPS/mkcert guidance and secure-context probe.
    - Target files if present.
  </read_first>
  <files>web-ui/server/config.example.env, docs/phase1-https-lan.md</files>
  <action>Create `web-ui/server/config.example.env` with concrete keys: `RAYME_WEB_BIND_HOST=192.168.1.199`, `RAYME_WEB_PORT=8443`, `RAYME_TLS_CERT_FILE=./certs/rayme.local+1.pem`, `RAYME_TLS_KEY_FILE=./certs/rayme.local+1-key.pem`, `RAYME_ALLOWED_ORIGINS=https://192.168.1.199:8443`, `RAYME_AI_BACKEND_URL=https://192.168.1.199:9443`, `RAYME_LLM_BASE_URL=http://192.168.1.199:8080/v1`, `RAYME_LLM_MODEL=local-model`, and `RAYME_LLM_API_KEY=`. Create `docs/phase1-https-lan.md` adapting the Phase 0 mkcert workflow and explicitly documenting the known passing Android URL `https://192.168.1.199:8443`, direct LAN IP as sufficient for Phase 1, optional `rayme.local` only if local DNS/mDNS is configured, and a warning that Phase 1 server code must reject `0.0.0.0` binds and wildcard CORS.</action>
  <verify>
    <automated>rg "192\\.168\\.1\\.199|RAYME_WEB_BIND_HOST|RAYME_ALLOWED_ORIGINS|https://192\\.168\\.1\\.199:8443|0\\.0\\.0\\.0|wildcard CORS" web-ui/server/config.example.env docs/phase1-https-lan.md</automated>
    <automated>test "$(rg "RAYME_WEB_BIND_HOST=0\\.0\\.0\\.0|Access-Control-Allow-Origin: \\*" web-ui/server/config.example.env docs/phase1-https-lan.md -c | awk -F: '{s+=$2} END {print s+0}')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `web-ui/server/config.example.env` contains `RAYME_WEB_BIND_HOST=192.168.1.199` and does not contain `RAYME_WEB_BIND_HOST=0.0.0.0`.
    - `docs/phase1-https-lan.md` contains `https://192.168.1.199:8443`.
    - `docs/phase1-https-lan.md` states direct LAN IP HTTPS is sufficient for Phase 1 acceptance.
    - `docs/phase1-https-lan.md` states `rayme.local` is optional unless local DNS/mDNS is configured.
  </acceptance_criteria>
  <done>Phase 1 has concrete LAN/TLS/origin setup guidance without requiring a local hostname or wildcard bind.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN browser -> AI backend health | Unauthenticated clients can probe the health-only stub. |
| web-ui/server -> external LLM | Sensitive API key and endpoint config leave RayMe only from server-side code. |
| LAN network -> web host | Direct IP HTTPS exposes the app on a configured interface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-BIND | I/T | `config.example.env`, HTTPS doc | mitigate | Document explicit `RAYME_WEB_BIND_HOST=192.168.1.199` and warn against `0.0.0.0`. |
| T-01-03-CORS | S/T | HTTPS doc | mitigate | Document `RAYME_ALLOWED_ORIGINS` and no wildcard CORS before web implementation. |
| T-01-03-KEY | I | `llm/README.md`, env example | mitigate | Keep API key in server env/settings only; use a blank example value and Settings probe contract. |
| T-01-03-SCOPE | T | `ai-backend/app/main.py`, `llm/README.md` | mitigate | Health-only AI backend and docs-only LLM prevent fake local model services from contaminating topology. |
| T-01-03-TLS | S/I | `docs/phase1-https-lan.md` | mitigate | Reuse mkcert trusted direct-IP workflow and secure-context checks from Phase 0. |
</threat_model>

<verification>
Run:
`uv run --project ai-backend pytest ai-backend/tests/test_health.py -q`
`test ! -f llm/app.py && test ! -f llm/server.py`
`rg "RayMe does not ship local inference|https://192\\.168\\.1\\.199:8443|RAYME_WEB_BIND_HOST=192\\.168\\.1\\.199" llm docs web-ui/server/config.example.env`
</verification>

<success_criteria>
The service foundation is complete when AI backend `/health` passes, `llm/` is docs/config only, and LAN HTTPS configuration is concrete, direct-IP compatible, and non-wildcard.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-03-SUMMARY.md`.
</output>
