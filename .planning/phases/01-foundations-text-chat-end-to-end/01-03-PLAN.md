---
phase: 01-foundations-text-chat-end-to-end
plan: "03"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - web-ui/server/app/domain/cards.py
  - web-ui/server/app/domain/card_export.py
  - web-ui/server/tests/test_card_import.py
  - web-ui/server/tests/test_card_export.py
  - web-ui/server/tests/test_characters.py
  - web-ui/server/tests/test_health_settings.py
autonomous: true
requirements: [REQ-01, REQ-10, REQ-11, REQ-12, REQ-13, REQ-14, REQ-16]
must_haves:
  truths:
    - "Backend card, character, export, and settings tests define safe untrusted input handling before implementation."
    - "The LLM status contract is a web-server Settings probe, not a local LLM service."
  artifacts:
    - path: "web-ui/server/tests/test_card_import.py"
      provides: "Card parser and malicious payload contracts"
    - path: "web-ui/server/tests/test_characters.py"
      provides: "Character CRUD and safe delete contracts"
    - path: "web-ui/server/tests/test_health_settings.py"
      provides: "Health/settings status contracts"
  key_links:
    - from: "web-ui/server/tests/test_card_import.py"
      to: "web-ui/server/app/domain/cards.py"
      via: "parse_character_card_bytes"
      pattern: "ccv3|chara"
---

<objective>
Create backend contracts for character-card import/export, character CRUD, and health/settings status.

Purpose: Keep untrusted card parsing, character delete preservation, v2 JSON export, and LLM Settings probe behavior executable before feature code lands.
Output: Collectable tests and card/export interface modules.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Write card and character contracts</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-18 through D-25.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` Character Card Safety.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-VALIDATION.md` card and character rows.
    - Target files if present.
  </read_first>
  <files>web-ui/server/app/domain/cards.py, web-ui/server/app/domain/card_export.py, web-ui/server/tests/test_card_import.py, web-ui/server/tests/test_card_export.py, web-ui/server/tests/test_characters.py</files>
  <action>Create importable interfaces `parse_character_card_bytes` and `export_character_v2_json`. Write non-skipped tests named `test_png_prefers_ccv3_over_chara`, `test_malformed_png_metadata_rejected_without_traceback`, `test_malicious_card_html_is_preserved_as_data_not_executed`, `test_v2_export_omits_v3_png_export`, `test_character_delete_preserves_existing_thread_snapshot`, and `test_import_response_reports_lorebook_present_not_used`. Tests must require v2 JSON, v3 JSON, v2 PNG, v3 PNG, strict malformed rejection, preserved raw source JSON, lorebook/world-info preservation, and no prompt injection from lorebook fields.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests --collect-only -q</automated>
    <automated>rg "test_png_prefers_ccv3_over_chara|test_character_delete_preserves_existing_thread_snapshot|test_v2_export_omits_v3_png_export" web-ui/server/tests</automated>
  </verify>
  <acceptance_criteria>
    - Every exact test name listed in the action appears under `web-ui/server/tests`.
    - No backend test is marked `skip`, `xfail`, or `todo`.
    - Tests distinguish stored untrusted HTML from rendered/sanitized output.
    - Pytest collection exits 0.
  </acceptance_criteria>
  <done>Card and character contracts are collectable and cover import, export, safe delete, and lorebook preservation.</done>
</task>

<task type="auto">
  <name>Task 2: Write health and settings contracts</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-08, D-09, D-11, D-12, D-13, and D-39.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md` Pitfall 8 and Security Domain.
    - Existing target files if present.
  </read_first>
  <files>web-ui/server/tests/test_health_settings.py</files>
  <action>Write non-skipped health/settings tests named `test_health_settings_status_values_are_connected_unreachable_unauthorized_not_configured`, `test_settings_never_return_raw_llm_api_key`, and `test_llm_status_is_server_side_settings_probe_not_local_service`. The tests must require Web UI `/health`, AI-backend `/health` probe support, and `POST /api/settings/test/llm` against the configured OpenAI-compatible endpoint. Do not require or create a local `llm` app or local LLM `/health` server.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests --collect-only -q</automated>
    <automated>rg "Connected|Unreachable|Unauthorized|Not configured|/api/settings/test/llm|raw_llm_api_key" web-ui/server/tests/test_health_settings.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests use exactly the four status strings `Connected`, `Unreachable`, `Unauthorized`, and `Not configured`.
    - Tests assert raw LLM API keys are absent from browser-facing payloads.
    - Tests assert LLM status is `/api/settings/test/llm`.
  </acceptance_criteria>
  <done>Health/settings contracts are collectable and preserve the external LLM boundary.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| card upload -> parser | Untrusted JSON/PNG bytes enter backend parsing. |
| browser -> settings APIs | Browser receives endpoint status and masked configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-XSS | E/T | `test_card_import.py` | mitigate | Contracts store malicious HTML as data and require client sanitizer later. |
| T-01-03-PNGDOS | D | `test_card_import.py` | mitigate | Contracts require safe malformed PNG/base64 rejection. |
| T-01-03-KEY | I | `test_health_settings.py` | mitigate | Contracts require masked key behavior and server-side LLM probe. |
| T-01-03-DATA | D | `test_characters.py` | mitigate | Contracts require character delete to preserve thread snapshots. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests --collect-only -q`.
</verification>

<success_criteria>
Card, character, and settings contracts are complete when all listed tests exist, collect, and enforce the Phase 1 boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-03-SUMMARY.md`.
</output>
