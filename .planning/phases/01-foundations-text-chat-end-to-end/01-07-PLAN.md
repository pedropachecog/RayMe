---
phase: 01-foundations-text-chat-end-to-end
plan: "07"
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-backend/pyproject.toml
  - ai-backend/app/__init__.py
  - ai-backend/app/main.py
  - ai-backend/scripts/run_https.py
  - ai-backend/tests/test_health.py
  - llm/README.md
  - llm/openai-compatible.example.env
  - web-ui/server/config.example.env
  - docs/phase1-https-lan.md
autonomous: true
requirements: [REQ-01, REQ-03, REQ-04, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "The Phase 1 AI backend is a health-only FastAPI service."
    - "`llm/` contains docs/config for an external OpenAI-compatible endpoint, not a local RayMe inference server."
    - "The AI-backend HTTPS runner/check path remains planned."
  artifacts:
    - path: "ai-backend/app/main.py"
      provides: "AI backend `/health` FastAPI stub"
      exports: ["create_app"]
    - path: "ai-backend/scripts/run_https.py"
      provides: "AI backend HTTPS runner for port 9443"
    - path: "llm/README.md"
      provides: "External LLM setup contract"
  key_links:
    - from: "llm/README.md"
      to: "web-ui/server Settings LLM probe"
      via: "`/api/settings/test/llm`"
      pattern: "RayMe does not ship local inference"
---

<objective>
Create the AI-backend health stub, external LLM docs/config, and LAN HTTPS configuration examples.

Purpose: Preserve D-06, D-08, D-10, D-12, and the resolved LLM health interpretation while keeping AI model work out of Phase 1.
Output: Health-only `ai-backend`, docs-only `llm`, and direct-IP HTTPS setup notes.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/00-measurement-gate/KEY_DECISIONS.md
@.planning/phases/00-measurement-gate/HTTPS-SETUP.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement AI backend health and HTTPS runner</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-06 and D-08.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` `ai-backend` health stub and tests.
    - Target files if present.
  </read_first>
  <files>ai-backend/pyproject.toml, ai-backend/app/__init__.py, ai-backend/app/main.py, ai-backend/scripts/run_https.py, ai-backend/tests/test_health.py</files>
  <action>Create uv project `rayme-ai-backend` with dependencies `fastapi==0.136.1`, `uvicorn==0.46.0`, `httpx==0.28.1`, and dev dependency `pytest==9.0.3`. Implement `GET /health` returning keys `service`, `status`, `phase`, and `capabilities` with values `rayme-ai-backend`, `ok`, `01`, and `["health"]`. Add `scripts/run_https.py` accepting `--host`, `--port`, `--cert`, `--key`, and `--check-config`; default/document port `9443`; reject `0.0.0.0`; require cert/key paths; pass `ssl_certfile`/`ssl_keyfile` to uvicorn; include `https://192.168.1.199:9443/health` in help text. Do not add STT, TTS, VAD, WebRTC, GPU, or voice fields.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_health.py -q</automated>
    <automated>uv run --project ai-backend python ai-backend/scripts/run_https.py --check-config --host 127.0.0.1 --port 9443 --cert ai-backend/pyproject.toml --key ai-backend/pyproject.toml</automated>
    <automated>! rg "STT|TTS|VAD|WebRTC|gpu|voice" ai-backend/app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - Health tests assert exact JSON values.
    - Tests assert `--host 0.0.0.0` exits nonzero in check mode.
    - Runner help contains `https://192.168.1.199:9443/health`, `--cert`, `--key`, and `9443`.
  </acceptance_criteria>
  <done>The AI backend is a tested health-only service with a direct-IP HTTPS runner.</done>
</task>

<task type="auto">
  <name>Task 2: Document external LLM and LAN HTTPS config</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-06, D-09, D-10, D-12, and D-13.
    - `.planning/phases/00-measurement-gate/HTTPS-SETUP.md`.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md` Pitfall 8.
    - Target files if present.
  </read_first>
  <files>llm/README.md, llm/openai-compatible.example.env, web-ui/server/config.example.env, docs/phase1-https-lan.md</files>
  <action>Create `llm/README.md` stating `RayMe does not ship local inference`; Phase 1 LLM status is `POST /api/settings/test/llm`; settings are `RAYME_LLM_BASE_URL`, `RAYME_LLM_API_KEY`, and `RAYME_LLM_MODEL`. Create `openai-compatible.example.env` with those keys and example values only. Create `web-ui/server/config.example.env` with direct LAN examples for Web UI `192.168.1.199:8443`, AI backend `https://192.168.1.199:9443`, allowed origins, TLS cert/key, and LLM URL/model/key. Create `docs/phase1-https-lan.md` adapting Phase 0 mkcert instructions and stating direct LAN IP HTTPS is sufficient, `rayme.local` is optional without DNS/mDNS, wildcard `0.0.0.0` is rejected, wildcard CORS is not allowed, and AI-backend health is verified at `https://192.168.1.199:9443/health`.</action>
  <verify>
    <automated>test -f llm/README.md && test -f llm/openai-compatible.example.env && test ! -f llm/app.py && test ! -f llm/server.py</automated>
    <automated>rg "RayMe does not ship local inference|/api/settings/test/llm|RAYME_LLM_BASE_URL|https://192\\.168\\.1\\.199:8443|https://192\\.168\\.1\\.199:9443/health|direct LAN IP HTTPS is sufficient" llm web-ui/server/config.example.env docs/phase1-https-lan.md</automated>
  </verify>
  <acceptance_criteria>
    - No `llm/app.py` or `llm/server.py` exists.
    - Docs include Web UI and AI-backend direct-IP HTTPS URLs.
    - Config example does not set `RAYME_WEB_BIND_HOST=0.0.0.0`.
    - Docs state `rayme.local` is optional unless DNS/mDNS is configured.
  </acceptance_criteria>
  <done>The external LLM and direct-IP HTTPS boundaries are documented without creating a local inference service.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN browser -> AI backend health | Unauthenticated clients can probe the health-only service. |
| web server -> external LLM | Sensitive LLM settings leave RayMe only server-side. |
| HTTPS docs -> operator | Runbooks determine LAN exposure choices. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-AIHTTPS | S/I | `ai-backend/scripts/run_https.py` | mitigate | Reject wildcard bind, require cert/key, and document port 9443 health check. |
| T-01-07-KEY | I | `llm/README.md` | mitigate | Keep API key server-side and blank in examples. |
| T-01-07-SCOPE | T | `ai-backend`, `llm` | mitigate | Health-only AI backend and docs-only LLM prevent fake model services. |
</threat_model>

<verification>
Run `uv run --project ai-backend pytest ai-backend/tests/test_health.py -q` and grep docs/config for direct-IP HTTPS URLs.
</verification>

<success_criteria>
Service foundation is complete when AI backend health passes, `llm/` is docs/config only, and LAN HTTPS guidance covers both Web UI and AI-backend health.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-07-SUMMARY.md`.
</output>
