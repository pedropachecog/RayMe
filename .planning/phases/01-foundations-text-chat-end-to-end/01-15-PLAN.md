---
phase: 01-foundations-text-chat-end-to-end
plan: "15"
type: execute
wave: 7
depends_on: ["14"]
files_modified:
  - web-ui/server/app/domain/message_actions.py
  - web-ui/server/app/api/messages.py
  - web-ui/server/tests/test_message_actions.py
autonomous: true
requirements: [REQ-32, REQ-33, REQ-34, REQ-35, REQ-60]
must_haves:
  truths:
    - "Regenerate, swipes, and continue invoke the same server-side OpenAI-compatible path as normal send."
    - "Regenerate replaces/selects the generated AI alternate rather than appending a second canonical turn."
    - "Only selected alternates contribute to future prompt context."
  artifacts:
    - path: "web-ui/server/app/domain/message_actions.py"
      provides: "LLM-backed regenerate, swipe, edit, and continue operations"
    - path: "web-ui/server/app/api/messages.py"
      provides: "Message action routes"
  key_links:
    - from: "web-ui/server/app/domain/message_actions.py"
      to: "web-ui/server/app/domain/prompt_builder.py"
      via: "build_prompt_context"
      pattern: "build_prompt_context"
    - from: "web-ui/server/app/domain/message_actions.py"
      to: "web-ui/server/app/domain/llm_stream.py"
      via: "collect_chat_completion"
      pattern: "collect_chat_completion"
---

<objective>
Implement durable LLM-backed message actions: Regenerate, Edit, Swipes, and Continue.

Purpose: Close the checker blocker for REQ-32, REQ-34, and REQ-35 by making generated actions use server-side settings and prompt context instead of local-only state.
Output: Message action domain functions, routes, and tests.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-14-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement LLM-backed regenerate, swipe, edit, and continue routes</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` REQ-32 through REQ-35.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-29 through D-33.
    - Checker issue requiring real LLM-backed regenerate, swipes, and continue.
    - Plan 14 summary for `build_prompt_context` and `collect_chat_completion`.
  </read_first>
  <files>web-ui/server/app/domain/message_actions.py, web-ui/server/app/api/messages.py, web-ui/server/tests/test_message_actions.py</files>
  <action>Implement routes `POST /api/messages/{message_id}/regenerate`, `POST /api/messages/{message_id}/swipes`, `PATCH /api/messages/{message_id}`, `POST /api/messages/{message_id}/truncate-stale`, `POST /api/messages/{message_id}/keep-stale`, and `POST /api/messages/{message_id}/continue`. Regenerate must rebuild prompt context from the previous user input/selected branch, use persisted server LLM settings, call the OpenAI-compatible helper, and replace/select the generated AI alternate without appending a second canonical turn. Swipe generation must build context from the selected prior branch, call the LLM, persist `message_alternates.source_action='swipe'`, increment `alternate_index`, and allow selecting it; unselected alternates must not appear in future `build_prompt_context`. Continue must use composer text as instruction/prefix to extend the previous AI turn, call the same server-side LLM path, persist/select an alternate with `source_action='continue'`, and return the same ThreadMessageShape as thread hydration. Per D-31, edit marks downstream messages stale and truncate/keep records the selected branch behavior.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_message_actions.py -q</automated>
    <automated>rg "collect_chat_completion|build_prompt_context|source_action=.*swipe|source_action=.*continue|alternate_index|truncate-stale|keep-stale" web-ui/server/app web-ui/server/tests/test_message_actions.py</automated>
  </verify>
  <acceptance_criteria>
    - Regenerate tests prove the LLM helper is called and visible canonical AI response count stays one.
    - Swipe tests prove LLM helper is called, `source_action='swipe'`, incremented `alternate_index`, selection, and unselected-alternate exclusion from future context.
    - Continue tests prove LLM helper is called with composer text and returns ThreadMessageShape with `source_action='continue'`.
    - Edit tests prove downstream rows are flagged stale and truncate/keep behavior works.
  </acceptance_criteria>
  <done>All Phase 1 message actions are durable backend operations backed by the server-side LLM path where generation is required.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> message action APIs | User actions mutate durable branch state and trigger LLM calls. |
| server -> LLM endpoint | Message actions use sensitive server settings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15-KEY | I | `message_actions.py` | mitigate | Use persisted server LLM settings; tests prove helper is called without browser overrides. |
| T-01-15-BRANCH | T/D | `message_actions.py` | mitigate | Tests cover selected branch, alternate index, regenerate replacement, and stale handling. |
| T-01-15-PROMPT | T | `build_prompt_context` use | mitigate | Tests prove unselected alternates are excluded from future prompts. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests/test_message_actions.py -q`.
</verification>

<success_criteria>
Message actions are complete when regenerate, swipe, edit, and continue tests pass and generated actions call the shared server-side LLM helper.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-15-SUMMARY.md`.
</output>
