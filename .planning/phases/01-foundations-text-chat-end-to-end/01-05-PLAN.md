---
phase: 01-foundations-text-chat-end-to-end
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/client/package.json
  - web-ui/client/svelte.config.js
  - web-ui/client/vite.config.ts
  - web-ui/client/vitest.config.ts
  - web-ui/client/playwright.config.ts
  - web-ui/client/tsconfig.json
  - web-ui/client/src/routes/+layout.ts
  - web-ui/client/src/routes/+layout.svelte
  - web-ui/client/src/routes/+page.svelte
autonomous: true
requirements: [REQ-10, REQ-30, REQ-70, REQ-90, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "The SvelteKit static SPA scaffold exists before feature UI plans run."
    - "The first route is an app shell entry, not marketing copy."
  artifacts:
    - path: "web-ui/client/package.json"
      provides: "Pinned SvelteKit/Svelte/Vite/Vitest/Playwright project"
    - path: "web-ui/client/svelte.config.js"
      provides: "adapter-static SPA fallback config"
    - path: "web-ui/client/src/routes/+layout.ts"
      provides: "SSR disabled for static client"
  key_links:
    - from: "web-ui/client/src/routes/+layout.ts"
      to: "web-ui/client/svelte.config.js"
      via: "static SPA build"
      pattern: "ssr = false|fallback"
---

<objective>
Create the SvelteKit static client scaffold and test configuration.

Purpose: Establish the Phase 1 browser project without mixing in sanitizer, E2E, or screen implementation work.
Output: Client package/config files and a minimal app entry route.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create SvelteKit static app scaffold</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-07, D-35 through D-41.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` Design System and App Shell.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-RESEARCH.md` Standard Stack.
    - Target files if present.
  </read_first>
  <files>web-ui/client/package.json, web-ui/client/svelte.config.js, web-ui/client/vite.config.ts, web-ui/client/vitest.config.ts, web-ui/client/playwright.config.ts, web-ui/client/tsconfig.json, web-ui/client/src/routes/+layout.ts, web-ui/client/src/routes/+layout.svelte, web-ui/client/src/routes/+page.svelte</files>
  <action>Create a SvelteKit 2 / Svelte 5 static SPA. Pin dependencies: `@sveltejs/kit@2.58.0`, `svelte@5.55.5`, `@sveltejs/adapter-static@3.0.10`, `vite@8.0.10`, `lucide-svelte@1.0.1`, `marked@18.0.2`, `dompurify@3.4.1`, `@tanstack/svelte-virtual@3.13.24`; dev dependencies `vitest@4.1.5`, `happy-dom@20.9.0`, and `@playwright/test@1.59.1`. Configure `adapter-static` with `fallback: '200.html'`; set `export const ssr = false`. Add scripts `test:unit`, `test:e2e`, `build`, and `check`. Create minimal layout/page files that show `RayMe` as an app entry and include no Voice Lab, Call, account, billing, or logout labels.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run</automated>
    <automated>npm --prefix web-ui/client run build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains the exact dependency names and versions listed in the action.
    - `svelte.config.js` contains `adapter-static` and `fallback: '200.html'`.
    - `+layout.ts` contains `ssr = false`.
    - `rg "Voice Lab|Call|Account|Billing|Logout" web-ui/client/src` returns no matches.
  </acceptance_criteria>
  <done>The client scaffold builds as a static SPA and contains only Phase 1 app-entry content.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> static app | The first app route defines visible Phase 1 scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-FUTURE | T | client scaffold | mitigate | Grep blocks future Voice Lab/Call/account/billing/logout controls. |
| T-01-05-SSR | I | `+layout.ts` | mitigate | Disable SSR so API keys/settings remain server-side in FastAPI, not SvelteKit server code. |
</threat_model>

<verification>
Run `npm --prefix web-ui/client run test:unit -- --run` and `npm --prefix web-ui/client run build`.
</verification>

<success_criteria>
Client scaffold is complete when the static SPA config exists, builds, and contains no out-of-scope controls.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-05-SUMMARY.md`.
</output>
