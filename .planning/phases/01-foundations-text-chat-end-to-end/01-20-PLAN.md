---
phase: 01-foundations-text-chat-end-to-end
plan: "20"
type: execute
wave: 6
depends_on: ["11", "12", "16", "17"]
files_modified:
  - web-ui/client/src/routes/characters/[id]/+page.svelte
  - web-ui/client/src/routes/settings/+page.svelte
  - web-ui/client/src/lib/components/CharacterFormSection.svelte
  - web-ui/client/src/lib/components/PortraitDropzone.svelte
  - web-ui/client/src/lib/components/EndpointSettingsPanel.svelte
  - web-ui/client/tests/unit/character-editor.test.ts
  - web-ui/client/tests/unit/settings.test.ts
autonomous: true
requirements: [REQ-11, REQ-12, REQ-16, REQ-17, REQ-80, REQ-90, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "Editor exposes the full Phase 1 SillyTavern field set and portrait controls."
    - "Editor create, review, and edit modes load/save through real character API wrappers with REQ-11 payload fields."
    - "Settings exposes only real endpoint and HTTPS/mobile readiness controls."
  artifacts:
    - path: "web-ui/client/src/routes/characters/[id]/+page.svelte"
      provides: "Character Editor route"
    - path: "web-ui/client/src/routes/settings/+page.svelte"
      provides: "Endpoint Settings route"
  key_links:
    - from: "web-ui/client/src/routes/characters/[id]/+page.svelte"
      to: "web-ui/client/src/lib/api/characters.ts"
      via: "Editor load/save/portrait wrappers"
      pattern: "getCharacter|createCharacter|updateCharacter|uploadPortrait|removePortrait"
    - from: "web-ui/client/src/routes/settings/+page.svelte"
      to: "web-ui/client/src/lib/api/settings.ts"
      via: "connection test wrappers"
      pattern: "testLlm"
---

<objective>
Implement Character Editor and Settings screens.

Purpose: Split the oversized non-chat UI work into Editor/Settings after backend character/settings APIs and shared shell exist.
Output: Editor route, Settings route, form/dropzone/endpoint components, and tests.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md
@docs/stitch/html/character-editor-true-dark.html
@docs/stitch/html/settings-true-dark.html
@.planning/phases/01-foundations-text-chat-end-to-end/01-11-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-12-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-16-SUMMARY.md
@.planning/phases/01-foundations-text-chat-end-to-end/01-17-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement Editor and Settings surfaces</name>
  <read_first>
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-UI-SPEC.md` Character Editor and Settings contracts.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-CONTEXT.md` decisions D-23, D-36, D-39, and D-40.
    - `.planning/phases/01-foundations-text-chat-end-to-end/01-PATTERNS.md` Editor/form pattern and Settings endpoint pattern.
    - Existing API wrappers and app shell components.
  </read_first>
  <files>web-ui/client/src/routes/characters/[id]/+page.svelte, web-ui/client/src/routes/settings/+page.svelte, web-ui/client/src/lib/components/CharacterFormSection.svelte, web-ui/client/src/lib/components/PortraitDropzone.svelte, web-ui/client/src/lib/components/EndpointSettingsPanel.svelte, web-ui/client/tests/unit/character-editor.test.ts, web-ui/client/tests/unit/settings.test.ts</files>
  <action>Implement Character Editor with desktop two-column/single-column mobile form and visible fields mapped to REQ-11 payload names: `name`, `description`, `personality`, `scenario`, `first_mes`, `mes_example`, `system_prompt`, `creator_notes`, `character_notes`, `tags`, `alternate_greetings`, `post_history_instructions`, `creator`, `character_version`, and portrait upload/preview. Support create mode at `/characters/new?mode=create`, review mode at `/characters/{id}?mode=review`, and edit mode at `/characters/{id}`. Edit/review mode must load existing data through `getCharacter(characterId) -> GET /api/characters/{character_id}`; create mode must not call `getCharacter("new")`. `Save Character` in create mode builds a `CharacterEditorPayload` containing all REQ-11 fields and calls `createCharacter(payload) -> POST /api/characters`; if a portrait file was selected before save, call `uploadPortrait(createdCharacterId, file) -> PUT /api/characters/{character_id}/portrait` after create succeeds. `Save Character` in edit or review mode builds the same complete payload and calls `updateCharacter(characterId, payload) -> PATCH /api/characters/{character_id}`. Portrait replace calls `uploadPortrait(characterId, file)` for existing characters; portrait remove calls `removePortrait(characterId) -> DELETE /api/characters/{character_id}/portrait` and updates the preview state. Alternate greetings support add/edit/remove/reorder icon buttons with accessible labels. Show read-only `Lorebook present - not used in v1` when present. Header actions are `Discard Edits` and `Save Character`; discard confirmation copy is `Discard unsaved changes? Your last saved character version will remain.` Implement Settings with only Web UI status, AI backend URL/status, LLM URL, masked API key with reveal icon, model, LLM status, HTTPS secure-context status, and media-device availability status. Each endpoint panel has `Test Connection` and status text `Connected`, `Unreachable`, `Unauthorized`, or `Not configured`. Do not show account, subscription, billing, wake word, VAD sensitivity, audio device defaults, save-audio toggles, clear-data, PWA install, Voice Lab, or Call controls.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run "character-editor|settings"</automated>
    <automated>rg "Save Character|Discard Edits|Lorebook present - not used in v1|getCharacter|createCharacter|updateCharacter|uploadPortrait|removePortrait|GET.*/api/characters|POST.*/api/characters|PATCH.*/api/characters|PUT.*/api/characters.*/portrait|DELETE.*/api/characters.*/portrait|first_mes|mes_example|post_history_instructions|Test Connection|Connected|Unreachable|Unauthorized|Not configured" web-ui/client/src web-ui/client/tests/unit</automated>
  </verify>
  <acceptance_criteria>
    - Editor tests prove every required field label is rendered.
    - Editor tests prove edit/review mode loads with `getCharacter(characterId)` and does not call `getCharacter("new")` in create mode.
    - Editor tests prove create mode calls `createCharacter(payload)` / `POST /api/characters` with all REQ-11 field payload names.
    - Editor tests prove edit and review saves call `updateCharacter(characterId, payload)` / `PATCH /api/characters/{character_id}` with all REQ-11 field payload names.
    - Editor tests prove portrait upload/replace calls `uploadPortrait(characterId, file)` / `PUT /api/characters/{character_id}/portrait`, and portrait removal calls `removePortrait(characterId)` / `DELETE /api/characters/{character_id}/portrait`.
    - Editor tests prove alternate greetings support add/edit/remove/reorder actions.
    - Settings tests prove API key input is masked by default and key value is not rendered in status text.
    - `rg "Billing|Subscription|Wake word|VAD|save-audio|clear all data|PWA|Voice Lab|Call" web-ui/client/src/routes/settings web-ui/client/src/routes/characters` returns no matches.
  </acceptance_criteria>
  <done>Editor and Settings deliver the full Phase 1 field/status surfaces and omit future-scope controls.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| character fields -> Editor DOM | Untrusted card fields appear in editable UI. |
| Settings API key input -> browser UI | Sensitive LLM key is typed into the UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-20-KEY | I | `EndpointSettingsPanel.svelte` | mitigate | Mask key by default and never render key in statuses/toasts. |
| T-01-20-FUTURE | T | Editor/Settings routes | mitigate | Grep/tests block out-of-scope voice/call/billing/PWA/VAD controls. |
| T-01-20-UPLOAD | T/D | `PortraitDropzone.svelte` | mitigate | Restrict accepted formats and surface backend validation errors. |
</threat_model>

<verification>
Run `npm --prefix web-ui/client run test:unit -- --run "character-editor|settings"`.
</verification>

<success_criteria>
Editor/Settings UI is complete when tests pass, key masking works, and only real Phase 1 controls are present.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-text-chat-end-to-end/01-20-SUMMARY.md`.
</output>
