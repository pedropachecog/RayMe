---
phase: 00-measurement-gate
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-04-22T18:58:08Z
---

# BLOCKING CONSTRAINTS - Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [x] CONSTRAINT: Execute Phase 0 on the real backend only - all host commands for this phase must run on `OMEN-PC` (`192.168.1.199`) over SSH - if SSH is not working, fix SSH or stop; do not run fallback commands on this local Codex workstation or its WSL shell.
- [x] CONSTRAINT: Before claiming backend SSH works, run a real remote command that proves machine, user, Python, and GPU - minimum proof is `hostname`, `whoami`, `python --version`, and `nvidia-smi` on `OMEN-PC`.
- [x] CONSTRAINT: Inspect the staged set before every commit on this dirty branch - the branch already contains unrelated `.planning` edits and a prior commit picked up pre-staged plan hunks - run `git diff --cached --name-only` and `git diff --cached --stat` before committing.
- [x] CONSTRAINT: Never store persistent SSH keys or recovery material in `/tmp` or `/home/agent/.ssh` - both are disposable under fresh `containme` sessions - keep the canonical key on the repo bind mount at `.local/phase0-ssh/` and rebuild `~/.ssh` with `./scripts/bootstrap-rayme-ssh.sh restore`.
- [x] CONSTRAINT: If git reports dubious ownership for `/d/Pedro/Repos/Program/RayMe`, immediately run `git config --global --add safe.directory /d/Pedro/Repos/Program/RayMe` and continue - do not stop the session on that housekeeping fix.
- [x] CONSTRAINT: Under user frustration or time pressure, choose the shortest validated recovery path - prefer one small remote fix plus one local test over long speculative multi-step recovery scripts.

Keep these checked on every resume. Re-run the cached-diff inspection again immediately before committing.

## Incident Severity

- Severity: SEV-1 process failure / trust-impacting workflow failure
- User impact: multiple days of repeated SSH recovery, repeated manual copy/paste, wrong-machine execution, lost persistence, and justified extreme frustration
- Status: technically recovered, must remain process-blocking until the prevention rules above are honored on every resume

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| local-machine phase execution | Phase 0 execution was started from the local Codex workstation/WSL environment instead of the real backend `OMEN-PC` (`192.168.1.199`). This shell does not expose the backend's Windows `py`, `cmd.exe`, `powershell.exe`, or GPU tooling, so backend setup commands produce misleading results here. | blocking | Before any Phase 0 plan runs, verify SSH access to `192.168.1.199` and execute backend commands there. If SSH fails, stop and repair SSH or ask the user for the exact SSH invocation. Never substitute the local workstation. |
| disposable ssh material | Earlier SSH setup put key material in `/tmp`, which is disposable and was later lost. A later assumption that `/home/agent/.ssh` was durable under `containme` was also wrong. Both mistakes destroyed the prepared Phase 0 access path and forced rework. | blocking | Persist the canonical private key on the repo bind mount under `.local/phase0-ssh/`, keep the recovery docs in repo state, and rebuild `~/.ssh` with `./scripts/bootstrap-rayme-ssh.sh restore` at the start of a fresh session. Before relying on SSH, verify the repo-local key exists. |
| unverified remote success claims | SSH was treated as "working" before enough remote evidence had been run and reported. That forced the user to challenge and re-validate something that should have been proven before any claim. | blocking | Before claiming the backend path works, run and report a real remote command that prints machine, user, Python, and GPU facts. Do not infer success from a partial login or a plan to test later. |
| speculative high-friction recovery | Recovery instructions were too long, included avoidable syntax issues, and asked the user to do more manual work than necessary while already frustrated. | blocking | Minimize user actions: perform all local work locally first, send the user the smallest remote command block possible, and keep each recovery loop to one remote action followed by one local verification. |
| dirty-index commit leakage | The branch is dirty and a prior task commit captured already-staged plan-file hunks in addition to the intended scaffold files. | advisory | On this branch, always inspect the cached diff before each commit and stage only the intended files for the current task. |

<current_state>
Phase 00 SSH recovery was previously validated, but the current shell is still the local Codex checkout and fresh `containme` sessions do not keep `/home/agent/.ssh`. Backend execution must therefore be re-bootstrapped through `./scripts/bootstrap-rayme-ssh.sh restore` from the canonical repo-local path `.local/phase0-ssh/` before using `rayme-ssh@192.168.1.199`. The current backend staging area remains `%USERPROFILE%\\phase0-probes` on `OMEN-PC`, and the Phase 0 venv remains `%USERPROFILE%\\.venv-phase0`. The builder recordings now exist and the real voice benchmarks have been run; the remaining blockers are TTS-engine-specific, not missing input audio.
</current_state>

<completed_work>

Completed Tasks:
- Phase 00 context review: confirmed `00-RESEARCH.md` already identifies `OMEN-PC` (`192.168.1.199`) as the real backend and cites live SSH research.
- Plan 00-01 Task 1: scaffold files were created and committed in `e42c349`.
- Guardrail update: added explicit backend-execution notes to the host-sensitive Phase 0 plans, the Phase 0 SSH docs, and this handoff file.
- Durable SSH recovery was originally validated with `/home/agent/.ssh/rayme_omen_phase0_ed25519`, re-adding the matching public key on `OMEN-PC`, and real remote verification. That runtime path must no longer be treated as canonical; future sessions must restore it from `.local/phase0-ssh/`.
- Verified backend command output:
  - `OMEN-PC`
  - `omen-pc\\rayme-ssh`
  - `Python 3.10.8`
  - `NVIDIA GeForce RTX 3060, 12288 MiB, 560.94`
- Plan 00-01 Task 2: backend environment created successfully under `%USERPROFILE%\\.venv-phase0` on `OMEN-PC`.
- Python 3.11 bootstrap path: Miniforge installed per-user, then `py311-bootstrap` created and used to build the final venv.
- Final backend environment facts:
  - `Python 3.11.15`
  - `torch==2.5.1+cu118`
  - `torchvision==0.20.1+cu118`
  - `torchaudio==2.5.1+cu118`
  - `torch.cuda.is_available() == True`
- Pinned Phase 0 packages installed successfully in the backend venv:
  - `faster-whisper==1.2.1`
  - `f5-tts==1.1.17`
  - `coqui-tts==0.27.5`
  - `qwen-tts==0.1.1`
  - `jiwer==4.0.0`
  - `soundfile==0.13.1`
  - `librosa==0.11.0`
  - `pynvml==13.0.1`
  - `httpx==0.28.1`
- Whisper weights cached successfully on `OMEN-PC`:
  - `models--Systran--faster-distil-whisper-large-v3`
  - `models--mobiuslabsgmbh--faster-whisper-large-v3-turbo`
  - `models--Systran--faster-whisper-large-v3`
- Plan 00-01 Task 3 validation: backend smoke test passed on `OMEN-PC` using `%USERPROFILE%\\phase0-probes` and `%USERPROFILE%\\.venv-phase0`.
- Smoke test result:
  - command: `%USERPROFILE%\\.venv-phase0\\Scripts\\python.exe -m pytest test_smoke.py -v -c pytest.ini`
  - outcome: `5 passed, 2 warnings in 3.53s`
- Plan 00-07: FA2 install attempt completed on `OMEN-PC`.
- FA2 result:
  - installed: `false`
  - failure_reason: `windows_build_compile_error`
  - build_duration_s: `56.1`
  - qwen17b_recommended: `false`
  - concrete failure: source build reaches `cl.exe` and then fails inside CUTLASS headers (`error C2065` / `error C2275`), so this is not a missing-toolchain case.
- Plan 00-02 Task 1: HTTPS probe server and builder-facing setup doc skeleton created locally in the repo.
- Plan 00-06 Task 1: LLM cancel probe + parser/idleness unit tests created locally and validated on `OMEN-PC`.
- LLM cancel unit test result:
  - command: `%USERPROFILE%\\.venv-phase0\\Scripts\\python.exe -m pytest test_llm_cancel.py -v -c pytest.ini`
  - outcome: `8 passed in 0.05s`
- Plan 00-03 Task 1: Whisper benchmark rig, reference transcript, and unit tests created locally and validated on `OMEN-PC`.
- Whisper Task 1 artifacts:
  - `.planning/phases/00-measurement-gate/probes/whisper_bench.py`
  - `.planning/phases/00-measurement-gate/probes/test_whisper_bench.py`
  - `.planning/phases/00-measurement-gate/probes/fixtures/reference_transcript.txt`
  - commit: `f207093`
- Whisper Task 1 validation:
  - command: `%USERPROFILE%\\.venv-phase0\\Scripts\\python.exe -m pytest test_whisper_bench.py -v -c pytest.ini`
  - outcome: `8 passed`
  - transcript body length: `1660 words`
- Plan 00-04 Task 1: TTS TTFA rig, short reference transcript, and unit tests created locally and validated on `OMEN-PC`.
- TTS Task 1 artifacts:
  - `.planning/phases/00-measurement-gate/probes/tts_ttfa.py`
  - `.planning/phases/00-measurement-gate/probes/test_tts_ttfa.py`
  - `.planning/phases/00-measurement-gate/probes/fixtures/short_ref_transcript.txt`
  - commit: `f207093`
- TTS Task 1 validation:
  - command: `%USERPROFILE%\\.venv-phase0\\Scripts\\python.exe -m pytest test_tts_ttfa.py -v -c pytest.ini`
  - outcome: `9 passed`
- Combined backend validation for the new voice probe helpers:
  - command: `%USERPROFILE%\\.venv-phase0\\Scripts\\python.exe -m pytest test_whisper_bench.py test_tts_ttfa.py -v -c pytest.ini`
  - outcome: `17 passed in 0.38s`
- Plan 00-03 Task 2: real Whisper benchmark completed on `OMEN-PC` against the builder's recorded voice.
- Whisper runtime fix required before the live run:
  - downgraded `ctranslate2` from `4.7.1` to `3.24.0` because the backend is on CUDA `11.8` and upstream `faster-whisper` / `CTranslate2` docs require that downgrade for CUDA 11 GPU use on Windows
  - downgraded `setuptools` from `82.0.1` to `75.8.0` because `ctranslate2==3.24.0` imports `pkg_resources`
- Whisper probe fix required after the first live run:
  - `whisper_bench.py` now tracks VRAM with NVML delta instead of Torch allocator stats because `ctranslate2` allocates outside Torch and the original `peak_vram_mb` values were bogus
- Whisper result summary (`results/whisper.json`):
  - `distil-large-v3` `int8_float16`: `WER 0.0627`, `p50 18.24 s`, `peak_vram 1731.4 MB`, `default: true`
  - `large-v3-turbo` `int8_float16`: `WER 0.0524`, `p50 23.08 s`, `peak_vram 1074.0 MB`
  - `large-v3` `float16`: `WER 0.0452`, `p50 66.40 s`, `peak_vram 3538.0 MB`
  - default rung: `distil-large-v3` because it stays within the 2pp WER rule and is much faster
- Plan 00-04 Task 2: objective TTS TTFA run completed on `OMEN-PC`; not all engines produced usable samples.
- TTS probe hardening required before the live run:
  - `tts_ttfa.py` now runs each engine in its own subprocess so a fatal engine crash is recorded instead of aborting the whole benchmark
  - subprocess environment now seeds `TTS_HOME` for the non-admin `rayme-ssh` account
- TTS result summary (`results/tts_ttfa.json`):
  - `f5`: Windows crash fixed in-probe with runtime shims; current measured path is `simulated_streaming` with `TTFA 530.3 ms`, `RTF 0.398`, `peak_vram 1990.2 MB`
  - `xtts`: explicit user-approved Coqui CPML acceptance completed on `OMEN-PC`; native `inference_stream` now measures `TTFA 595.2 ms`, `RTF 0.657`, `peak_vram 1897.6 MB`
  - `qwen3`: `non_streaming_mode=False` still only simulates streaming text input and measures `TTFA 7527.3 ms`, `RTF 3.035`, `peak_vram 2520.1 MB`
  - current `v1_default`: `f5` as best-TTFA engine because no engine hit the budget
  - `qwen_gate.accepted: false` because TTFA is above `400 ms`, RTF is above `1.0`, and accent has not been judged
- Production-style F5 follow-up:
  - `results/f5_production_chunked.json`: short ack plus chunked remainder at speed `1.0` showed `570.7 ms` ack TTFA, `1.888 s` ack duration, and `0 ms` dead air after the ack
  - `results/f5_production_chunked_speed15.json`: same shape at speed `1.5` improved to `528.4 ms` ack TTFA, `1.259 s` ack duration, `0 ms` dead air after the ack, `182 ms` of remainder hidden by ack playback, and a `69.36 s` combined long-form render
  - local persisted audio artifacts now include `f5_production_chunked.wav`, `f5_production_chunked_speed15.wav`, `f5_ack*.wav`, and `f5_remainder_chunked*.wav`
</completed_work>

<remaining_work>

- Plan 00-02 Task 2 is builder-blocked on actual Android browser verification and cert trust.
- Plan 00-04 still has unresolved engine blockers:
  - `f5`: no longer crashes, but only exposes simulated chunk streaming and still misses the `400 ms` TTFA target
  - `xtts`: native streaming is now confirmed, but it still misses the `400 ms` TTFA target at `595.2 ms`
  - `qwen3`: objective run is complete, still only exposes simulated streaming semantics, and currently fails the v1 gate badly
- Plan 00-04 still needs the builder's subjective accent check if Qwen output is ever close enough to matter; current objective metrics already miss the gate badly.
- Plan 00-06 Task 2 is builder-blocked on a local LLM server install and model pull.
- Plans 00-02 through 00-08: still unexecuted and must run against the backend, not the local workstation.
</remaining_work>

<decisions_made>

- The canonical execution host for Phase 0 is `OMEN-PC` at `192.168.1.199`.
- A blocking `.continue-here.md` guard is required because plain research notes were not enough to stop the next executor from using the wrong machine.
- The canonical SSH identity now lives on the repo bind mount at `.local/phase0-ssh/rayme_omen_phase0_ed25519`; do not rotate or regenerate it unless the user explicitly asks.
- `/home/agent/.ssh/rayme_omen_phase0_ed25519` is runtime-only and must be recreated with `./scripts/bootstrap-rayme-ssh.sh restore`.
- The preferred operator path is `ssh rayme-ssh` after the bootstrap step, backed by repo docs in `BACKEND-SSH-199.md`.
- The user explicitly approved the XTTS CPML on `2026-04-22`; that accepted state now lives on `OMEN-PC` at `%LOCALAPPDATA%\\rayme-tts-home\\tts\\tts_models--multilingual--multi-dataset--xtts_v2\\tos_agreed.txt`.
- Treat XTTS as the only native-streaming engine in the current benchmark. F5 and Qwen3 are both measured as simulated-streaming paths only.
- Prefer F5 as the primary cloned-voice path. The user rated XTTS clone quality very poor; keep XTTS only as a compatibility fallback unless its quality improves materially.
- The current production-style F5 baseline is short ack plus chunked remainder at speed `1.5`; persisted metrics live in `results/f5_production_chunked_speed15.json`.
- Treat the current `OMEN-PC` Qwen3-TTS result as a no-FlashAttention baseline, not an optimized ceiling. The probe JSON records Qwen3 with eager attention, `qwen_tts` warns that `flash-attn` is not installed on `OMEN-PC`, and the prior FA2 install task failed on that host.
- This incident is severe enough that prevention rules belong in persistent repo artifacts, not just chat memory.
- For this mixed-host phase, repo artifacts remain in git on the local workstation; backend-only validation runs happen over SSH and their results are written back into repo JSON artifacts.
</decisions_made>

<blockers>
- Android browser verification still pending for plan `00-02` Task 2.
- No measured TTS engine clears the v1 budget on `OMEN-PC`: `f5` is `530.3 ms` via simulated chunk streaming, `xtts` native streaming is `595.2 ms`, and `qwen3` simulated streaming is `7527.3 ms`.
- The current `OMEN-PC` Qwen3-TTS numbers are not FA2-optimized. They are eager-attention baseline measurements only, so any later Qwen3 retry must be labeled separately if `flash-attn` ever becomes available on `OMEN-PC`.
- Phase 00 still needs a policy call on whether simulated streaming is acceptable for the fallback engine; `f5` is fastest, but it is not a true incremental streamer.
- Local LLM server install still pending for plan `00-06` Task 2.
</blockers>

## Required Reading (in order)
1. `.planning/phases/00-measurement-gate/.continue-here.md` - contains the blocking anti-patterns and the exact next-step guard.
1. `.planning/phases/00-measurement-gate/BACKEND-SSH-199.md` - canonical persistent SSH key path, public key, fingerprint, and Windows recovery block.
1. `.planning/phases/00-measurement-gate/RERESEARCH-HANDOFF-ADDENDUM-2026-04-22.md` - executor override that binds Phase 0 to `OMEN-PC` and the verified SSH path.
1. `.planning/phases/00-measurement-gate/00-RESEARCH.md` - canonical backend facts for `OMEN-PC`.
1. `.planning/phases/00-measurement-gate/00-01-wave0-setup-PLAN.md` - first executable plan, now with an explicit execution-host note.

## Infrastructure State
- Local shell: Linux/WSL-style environment, not the backend machine.
- Backend target: Windows host `OMEN-PC` at `192.168.1.199`.
- Canonical persisted private key: `.local/phase0-ssh/rayme_omen_phase0_ed25519`
- Canonical persisted public key: `.local/phase0-ssh/rayme_omen_phase0_ed25519.pub`
- Runtime private key: `/home/agent/.ssh/rayme_omen_phase0_ed25519`
- Runtime public key: `/home/agent/.ssh/rayme_omen_phase0_ed25519.pub`
- Preferred SSH alias: `rayme-ssh`
- Local SSH alias config: `/home/agent/.ssh/config`
- Bootstrap command: `./scripts/bootstrap-rayme-ssh.sh restore`
- Git safe-directory recovery command: `git config --global --add safe.directory /d/Pedro/Repos/Program/RayMe`
- Backend host key: recreated in `~/.ssh/known_hosts` by the bootstrap script
- Verified raw SSH command:
  `./scripts/bootstrap-rayme-ssh.sh restore && ssh rayme-ssh "cmd /c \"hostname & whoami & python --version & nvidia-smi --query-gpu=name,memory.total,driver_version --format=csv,noheader\""`
- Expected stable output:
  `OMEN-PC`
  `omen-pc\\rayme-ssh`
  `Python 3.10.8`
  `NVIDIA GeForce RTX 3060, 12288 MiB, 560.94`
- Backend Phase 0 venv:
  `%USERPROFILE%\\.venv-phase0`
- Backend installed package summary:
  `Python 3.11.15`, `torch 2.5.1+cu118`, `cuda 11.8`, `RTX 3060 12 GB`, Whisper models cached locally

## Backend Warnings To Remember
- `qwen_tts` imports successfully, but warns that SoX is not installed as a Windows CLI tool.
- `qwen_tts` also warns that `flash-attn` is not installed; that is expected because plan `00-07` owns FA2 installation separately.
- Because `flash-attn` is absent on `OMEN-PC`, treat the recorded Qwen3 TTFA/RTF numbers as eager-attention baseline data. Do not discuss them later as if they represent an FA2-enabled backend.
- `huggingface_hub` warns about degraded cache behavior because Windows symlinks are unavailable to this non-admin account; downloads still complete successfully.
- A transient `pip install -r requirements-phase0.txt` failure occurred with `WinError 32` on `proto\\__init__.py`; the fix was to stop leftover Python processes and rerun the install cleanly.
- `warmup_cuda()` warns that `expandable_segments` is not supported on this Windows torch build; keep the env var for parity with the research notes, but do not rely on allocator behavior changing on Windows.
- `torch` emits a `pynvml` deprecation warning, but NVML reads still work in the current environment.
- `flash-attn==2.8.3` does not build cleanly on this backend under the current Windows toolchain + torch stack; the failure happens during CUTLASS compilation after `cl.exe` is invoked successfully.

<context>
This was not a minor stumble. It was a workflow failure that burned hours, pushed repeated manual recovery onto the user, and damaged trust. The next executor must treat this as a SEV-1 process incident: use the exact backend, use the repo-persisted canonical key, rebuild `~/.ssh` before claiming SSH exists, run a real verification command before claiming success, and keep the user-facing recovery path brutally short if anything regresses.
</context>

<next_action>
Before final Phase 00 writeback, plan and execute `00-07.1-attention-backend-matrix-PLAN.md` so every TTS measurement is labeled with the backend actually used (`eager`, `sdpa`, `flash_attention_2`, or `not_applicable`). Keep iterating on the F5 production path in parallel, XTTS remains compatibility-only unless its quality improves materially, and any Qwen3 retry on `OMEN-PC` must be compared against the current eager-attention no-FA2 baseline explicitly rather than treating the old numbers as optimized. Accent listening remains optional unless `qwen3` becomes objectively competitive.
</next_action>
