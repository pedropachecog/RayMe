---
phase: 00-measurement-gate
task: 3
total_tasks: 3
status: paused
last_updated: 2026-04-23T22:56:38Z
---

## Latest State - 2026-04-23

Phase 0 is frozen for v1, and the TTS engine-extension follow-up has enough chunked and manual-quality evidence to close as `PASS_WITH_CAVEATS`. The shared TTS chunk planner has been implemented in `.planning/phases/00-measurement-gate/probes/tts_scenario_matrix.py`, and the Windows plus WSL scenario matrix has been rerun on `OMEN-PC`.

Read first:
- `.planning/spikes/003-tts-engine-extension-luxtts-chatterbox-tada/README.md`
- `.planning/spikes/003-tts-engine-extension-luxtts-chatterbox-tada/.continue-here.md`
- `.planning/phases/00-measurement-gate/results/tts_runtime_matrix_v2.json`
- `.planning/spikes/003-tts-engine-extension-luxtts-chatterbox-tada/MANUAL-QUALITY.csv`

Current TTS state:
- `LuxTTS` is still the leading new experimental latency candidate, but current user-sample listening found sample-level quality failures on all six optimized rows: reference/sample tail repetition, metallic noise, over-fast delivery, sample phrase leakage, poor speaker match, and accent loss. Keep the engine in future implementation and retest with better-padded reference audio and other speakers/accents.
- `XTTS` optimized long-form is now validly measured through shared chunking: WSL `427.4 ms` TTFA, `23525.3 ms` total generation, `43606.1 ms` stitched playback, 3 chunks, no fallback. The listened WSL optimized long sample is metallic/hiccupy with incomplete voice likeness; XTTS remains sample-sensitive.
- `F5` remains the safest incumbent from Phase 0, but it no longer wins the scenario-matrix optimized TTFA rows. The listened Windows optimized long sample sounds stretched/slow, hiccupy, and can degrade into mumbling/silence; the 1.5 stretch/duration multiplier is likely too much.
- `Chatterbox Turbo` baseline long-form is gibberish on both runtimes, while normal optimized long-form and `optimized_seed_1337` long-form are fine on both runtimes. This points to the raw/baseline long-form path rather than seed sensitivity.
- `TADA 1B` produced measurable Windows and WSL rows in the rerun. Windows optimized long sounds acceptable with some hiccups; WSL optimized long is less steady, morphs toward another voice, and has hiccups. TADA did not use an explicit fixed seed in this harness.
- `Qwen3 0.6B-Base` stays opt-in/play-only because latency is far outside the useful range.
- User policy: do not leave any TTS engine out of future implementation. Use evaluations to choose defaults, labels, warnings, and retesting priorities.

Recorded listening results live in:

```text
.planning/spikes/003-tts-engine-extension-luxtts-chatterbox-tada/MANUAL-QUALITY.csv
```

That CSV has already been refreshed with 66 measured non-Qwen rows. Sixteen rows now have user listening scores: five accepts, eight rejects, and three caution rows.

Move-on policy:
- Keep every measured TTS engine in future implementation.
- Use the evaluation notes for defaults, labels, warnings, and retesting priorities.
- Tune F5 long-form speed/duration handling before relying on the optimized long path.
- Retest LuxTTS with better-padded reference audio and other speakers/accents.
- Keep Chatterbox optimized chunked long-form available; avoid baseline/raw long-form.

# BLOCKING CONSTRAINTS - Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [x] CONSTRAINT: Execute Phase 0 backend work on the real backend only - all host commands for this phase must run on `OMEN-PC` (`192.168.1.199`) over SSH - if SSH is not working, fix SSH or stop; do not run fallback commands on this local Codex workstation or its WSL shell.
- [x] CONSTRAINT: Before claiming backend SSH works, run a real remote command that proves machine, user, Python, and GPU - minimum proof is `hostname`, `whoami`, `python --version`, and `nvidia-smi` on `OMEN-PC`.
- [x] CONSTRAINT: Inspect the staged set before every commit - this branch previously had dirty-index leakage where pre-staged plan hunks were captured with unrelated work - run `git diff --cached --name-only` and `git diff --cached --stat` before committing.
- [x] CONSTRAINT: Never store persistent SSH keys or recovery material in `/tmp` or `/home/agent/.ssh` - both are disposable under fresh `containme` sessions - keep the canonical key on the repo bind mount at `.local/phase0-ssh/` and rebuild `~/.ssh` with `./scripts/bootstrap-rayme-ssh.sh restore`.
- [x] CONSTRAINT: If git reports dubious ownership for `/d/Pedro/Repos/Program/RayMe`, immediately run `git config --global --add safe.directory /d/Pedro/Repos/Program/RayMe` and continue - do not stop the session on that housekeeping fix.
- [x] CONSTRAINT: Under user frustration or time pressure, choose the shortest validated recovery path - prefer one small remote fix plus one local test over long speculative multi-step recovery scripts.
- [x] CONSTRAINT: Hard safety rule: do not use `rm -rf` and do not delete files or directories through variable-expanded paths on any backend or WSL host.

## Incident Severity

- Severity: SEV-1 process failure / trust-impacting workflow failure
- User impact: multiple days of repeated SSH recovery, repeated manual copy/paste, wrong-machine execution, lost persistence, and justified extreme frustration
- Status: technically recovered, must remain process-blocking until the prevention rules above are honored on every resume

## Infrastructure State

- Local shell: Linux/WSL-style environment, not the backend machine.
- Backend target: Windows host `OMEN-PC` at `192.168.1.199`.
- Canonical persisted private key: `.local/phase0-ssh/rayme_omen_phase0_ed25519`
- Bootstrap command: `./scripts/bootstrap-rayme-ssh.sh restore`
- Native Windows SSH alias: `rayme-ssh`
- WSL-capable SSH alias: `rayme-pmpg`
- Native verification command:
  `./scripts/bootstrap-rayme-ssh.sh restore && ssh rayme-ssh "cmd /c \"hostname & whoami & python --version & nvidia-smi --query-gpu=name,memory.total,driver_version --format=csv,noheader\""`
- WSL verification command:
  `RAYME_SSH_ALIAS=rayme-pmpg RAYME_SSH_USER=pmpg ./scripts/bootstrap-rayme-ssh.sh restore && ssh rayme-pmpg 'wsl -d Ubuntu -e sh -c "hostname; whoami; python3 --version; nvidia-smi --query-gpu=name,memory.total,driver_version --format=csv,noheader"'`

<next_action>
Spike 003 can close and work can move on. Do not remove TTS engines from future implementation; use the scores to choose default suitability and caveats.
</next_action>
