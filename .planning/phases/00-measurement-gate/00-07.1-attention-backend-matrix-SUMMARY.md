---
phase: 00-measurement-gate
plan: "07.1"
subsystem: infra
tags: [tts, qwen3, flash-attention, xtts, f5-tts]
requires:
  - phase: "00"
    provides: "00-04 baseline TTS TTFA results for F5, XTTS, and Qwen3"
  - phase: "00"
    provides: "00-07 FlashAttention 2 install verdict on OMEN-PC"
provides:
  - "Explicit optimization backend labels in results/tts_ttfa.json"
  - "Dedicated results/tts_attention_matrix.json artifact"
  - "Fresh OMEN-PC rerun that distinguishes Qwen eager baseline from unavailable FlashAttention 2"
affects: [phase-00-writeback, phase-02-voice-lab, tts-engine-selection]
tech-stack:
  added: []
  patterns: [backend-labeled benchmark artifacts, OMEN-PC rerun over SSH]
key-files:
  created:
    - .planning/phases/00-measurement-gate/results/tts_attention_matrix.json
  modified:
    - .planning/phases/00-measurement-gate/probes/tts_ttfa.py
    - .planning/phases/00-measurement-gate/probes/f5_production_chunking.py
    - .planning/phases/00-measurement-gate/probes/test_tts_ttfa.py
    - .planning/phases/00-measurement-gate/results/tts_ttfa.json
    - .planning/HANDOFF.json
key-decisions:
  - "F5 and XTTS are labeled not_applicable for attention backend selection in the current probe."
  - "Qwen3 remains an eager-attention baseline until flash_attn imports successfully on OMEN-PC."
patterns-established:
  - "Every persisted TTS benchmark must carry explicit optimization backend metadata."
  - "Qwen eager baseline results and future FlashAttention runs must never be conflated."
requirements-completed: []
duration: 11 min
completed: 2026-04-22
---

# Phase 00 Plan 07.1: Attention Backend Matrix Summary

**Explicit backend labeling for the Phase 0 TTS benchmarks, separating Qwen eager baseline results from unavailable FlashAttention 2 paths**

## Performance

- **Duration:** 11 min
- **Started:** 2026-04-22T22:25:32Z
- **Completed:** 2026-04-22T22:36:07Z
- **Tasks:** 4
- **Files modified:** 6

## Accomplishments
- Added explicit optimization backend metadata to the Phase 0 TTS probes and production-shaped F5 probe output.
- Re-ran the short TTFA benchmark on `OMEN-PC` and persisted a fresh backend-labeled `results/tts_ttfa.json`.
- Created `results/tts_attention_matrix.json` so Phase 00 writeback has a single source of truth for backend support and measured backend per engine.

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend the TTS probes with backend-label metadata and helper tests** - `e48eae3` (feat)
2. **Task 2: Re-run OMEN-PC TTS measurements and add the attention matrix artifact** - `55a3d59` (feat)

**Plan metadata:** included in the docs completion commit for this summary and refreshed handoff.

## Files Created/Modified
- `.planning/phases/00-measurement-gate/probes/tts_ttfa.py` - Adds backend-detection metadata, Qwen eager/FA2 labeling, and matrix generation.
- `.planning/phases/00-measurement-gate/probes/f5_production_chunking.py` - Marks production-shaped F5 results as `not_applicable` for optimization backend selection.
- `.planning/phases/00-measurement-gate/probes/test_tts_ttfa.py` - Covers eager vs FA2 labeling and the matrix builder.
- `.planning/phases/00-measurement-gate/results/tts_ttfa.json` - Fresh OMEN-PC rerun with explicit backend labels and updated TTFA numbers.
- `.planning/phases/00-measurement-gate/results/tts_attention_matrix.json` - Dedicated matrix artifact for backend support and measured backend per engine.
- `.planning/HANDOFF.json` - Updated resume state pointing Phase 00 at the remaining blockers and next autonomous step.

## Decisions Made
- Treat F5 and XTTS as `not_applicable` for attention-backend labeling because the current probe paths do not expose eager, SDPA, or FlashAttention selection.
- Treat Qwen3 as an eager baseline until `flash_attn` imports successfully in the backend runtime; the current FA2 verdict remains a Windows build failure.
- Use `tts_attention_matrix.json` as the source-of-truth artifact for backend labeling in Phase 00 writeback.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- The local container does not have `pytest` installed, so validation ran in the authoritative Phase 0 backend venv on `OMEN-PC` instead.
- The first attention-matrix generation call used the wrong relative FA2 path; the artifact was immediately regenerated so the final matrix includes the FA2 failure summary.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Phase 00 writeback inputs are now backend-labeled and safe to cite in `00-08`.
- `00-05-vram-soak` is the next autonomous plan if execution continues without waiting on the builder.
- `00-02` still needs Android HTTPS verification and `00-06` still needs a local LLM server on `OMEN-PC`, so Phase 00 cannot close until those gaps are resolved.

---
*Phase: 00-measurement-gate*
*Completed: 2026-04-22*
