---
phase: 03-first-working-call-mvp
plan: "03"
type: execute
wave: 0
depends_on: []
files_modified:
  - web-ui/client/tests/unit/call-state.test.ts
  - web-ui/client/tests/unit/call-audio.test.ts
  - web-ui/client/tests/e2e/call-start.spec.ts
  - web-ui/client/tests/e2e/call-toolbar.spec.ts
  - web-ui/client/tests/e2e/call-permissions.spec.ts
  - web-ui/client/tests/e2e/call-visualizer.spec.ts
  - web-ui/client/tests/e2e/call-summary.spec.ts
  - web-ui/client/tests/e2e/call-mobile.spec.ts
  - web-ui/client/tests/e2e/live-call.spec.ts
  - .planning/phases/03-first-working-call-mvp/03-03-SUMMARY.md
autonomous: true
requirements: [REQ-40, REQ-47, REQ-48, REQ-49, REQ-50, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "Client call FSM, media helpers, UI copy, and browser acceptance paths are specified before implementation."
    - "Android/mobile parity is part of Phase 3, not a later cleanup task."
    - "Live OMEN-PC acceptance has a non-mocked Playwright spec that is skipped unless explicitly enabled."
  artifacts:
    - path: "web-ui/client/tests/unit/call-state.test.ts"
      provides: "RED call FSM contract"
    - path: "web-ui/client/tests/e2e/call-mobile.spec.ts"
      provides: "RED mobile-emulation call path"
---

<objective>
Add client Wave 0 tests for the browser call state machine, media helpers, call UI, permissions, toolbar, transcript, summary, and mobile path.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-first-working-call-mvp/03-UI-SPEC.md
@.planning/phases/03-first-working-call-mvp/03-VALIDATION.md
@.planning/phases/03-first-working-call-mvp/03-PATTERNS.md
@web-ui/client/src/routes/chat/[threadId]/+page.svelte
@web-ui/client/src/lib/components/CharacterCard.svelte
@web-ui/client/src/lib/components/ChatMessageBubble.svelte
@web-ui/client/playwright.config.ts
@web-ui/client/vitest.config.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add RED client unit tests for call FSM and audio helpers</name>
  <read_first>
    web-ui/client/src/lib/browser/environment.ts
    web-ui/client/src/lib/components/voice/SynthPreviewPanel.svelte
    web-ui/client/src/lib/components/StatusChip.svelte
    .planning/phases/03-first-working-call-mvp/03-UI-SPEC.md
  </read_first>
  <files>web-ui/client/tests/unit/call-state.test.ts, web-ui/client/tests/unit/call-audio.test.ts</files>
  <action>Create unit tests for future `src/lib/call/store.svelte.ts` and `src/lib/call/audio.ts`. The FSM tests must assert transitions for `connecting`, `listening`, `thinking`, `speaking`, `interrupted`, `ended`, and `failed`; button interrupt cancels AI generation/playback and returns to `listening`; end call stops work and moves to `ended`; mute toggles `serverMuted` independently of local track enabled state. The audio tests must assert `unlockCallAudioContext()` calls `resume()`, plays a one-sample silent buffer, reports `state === \"running\"`, and exposes unsupported output picker copy `Output selection is not available in this browser. RayMe will use the browser default output.` Add RMS-meter tests asserting a non-zero mic sample raises `listeningRms`, silence returns near 0, and AI output samples drive `speakingRms` separately from mic RMS.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run tests/unit/call-state.test.ts tests/unit/call-audio.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `call-state.test.ts` contains all state strings `connecting`, `listening`, `thinking`, `speaking`, `interrupted`, `ended`, and `failed`.
    - `call-audio.test.ts` contains `unlockCallAudioContext`, `resume`, and the exact unsupported output picker copy from the UI-SPEC.
    - `call-audio.test.ts` contains `listeningRms` and `speakingRms` assertions.
    - The unit command fails because implementation files are missing, not because the tests cannot parse.
  </acceptance_criteria>
  <done>Client FSM and audio helper behavior is captured as RED tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add RED Playwright call acceptance specs</name>
  <read_first>
    web-ui/client/tests/e2e/chat-stream.spec.ts
    web-ui/client/tests/e2e/phase1-full-path.spec.ts
    web-ui/client/tests/e2e/live-voice-lab.spec.ts
    web-ui/client/playwright.config.ts
    .planning/phases/03-first-working-call-mvp/03-UI-SPEC.md
  </read_first>
  <files>web-ui/client/tests/e2e/call-start.spec.ts, web-ui/client/tests/e2e/call-toolbar.spec.ts, web-ui/client/tests/e2e/call-permissions.spec.ts, web-ui/client/tests/e2e/call-visualizer.spec.ts, web-ui/client/tests/e2e/call-summary.spec.ts, web-ui/client/tests/e2e/call-mobile.spec.ts, web-ui/client/tests/e2e/live-call.spec.ts</files>
  <action>Create Playwright specs with mocked same-origin `/api/calls/*` and `/webrtc/*` responses where needed for local deterministic specs. Cover exact UI copy: `Start Call`, `Microphone access is blocked. Allow microphone access in Chrome, then retry.`, `Retry Microphone`, `Mute`, `Unmute`, `Interrupt`, `End Call`, `Listening`, `Thinking`, `Speaking`, `Return to Thread`, `Input selection is not available in this browser. RayMe will use the current microphone.`, and `Output selection is not available in this browser. RayMe will use the browser default output.` `call-start.spec.ts` must cover thread-header start and character-card start. `call-summary.spec.ts` must assert chronological thread rows for `call_start`, two `user_speech` rows, two `ai_speech` rows, and `call_end`. `call-mobile.spec.ts` must run on `mobile-chromium` and assert controls are not overlapped by bottom navigation. Add `live-call.spec.ts` following `live-voice-lab.spec.ts`: it must skip unless `RAYME_ENABLE_LIVE_E2E=1`, use `RAYME_LIVE_WEB_URL` and `RAYME_LIVE_AI_HEALTH_URL`, must not mock `/api/calls/*`, `/webrtc/*`, or media responses, and must prove a real LAN call path with two user→AI cycles when the live environment is enabled.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:e2e -- tests/e2e/call-start.spec.ts tests/e2e/call-toolbar.spec.ts tests/e2e/call-permissions.spec.ts tests/e2e/call-visualizer.spec.ts tests/e2e/call-summary.spec.ts --project=desktop-chromium && npm --prefix web-ui/client run test:e2e -- tests/e2e/call-mobile.spec.ts --project=mobile-chromium</automated>
  </verify>
  <acceptance_criteria>
    - Each listed `call-*.spec.ts` file exists and `live-call.spec.ts` exists.
    - The specs contain exact UI copy strings listed in the action.
    - `call-mobile.spec.ts` contains `mobile-chromium`.
    - `live-call.spec.ts` contains `RAYME_ENABLE_LIVE_E2E`, `RAYME_LIVE_WEB_URL`, and no `page.route('**/api/calls` route mock.
    - The Playwright command reaches implementation failures, not TypeScript parse failures.
  </acceptance_criteria>
  <done>Client browser acceptance behavior is captured as RED specs.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Denial of Service | Browser media start | mitigate | Tests require mic denial/retry and secure-context-visible failure states rather than half-connected calls. |
| T-03-03-02 | Tampering | Client call state | mitigate | Tests require server-muted state to be represented separately from local silencing. |
| T-03-03-03 | Information Disclosure | Browser UI errors | mitigate | Specs assert public recovery copy, not raw backend exception text. |
</threat_model>

<verification>
Run the new Vitest and Playwright targets. This plan is complete when client contracts are RED for missing implementation only.
</verification>

<success_criteria>
Client implementation plans have explicit tests for call entry, browser media, MVP visualizer states, toolbar controls, transcript, summary, and mobile behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-working-call-mvp/03-03-SUMMARY.md`.
</output>
