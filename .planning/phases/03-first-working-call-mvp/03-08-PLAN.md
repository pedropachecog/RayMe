---
phase: 03-first-working-call-mvp
plan: "08"
type: execute
wave: 3
depends_on: ["03-03", "03-05", "03-07"]
files_modified:
  - web-ui/client/src/lib/components/call/VoiceVisualizer.svelte
  - web-ui/client/src/lib/components/call/CallToolbar.svelte
  - web-ui/client/src/lib/components/call/CallTranscript.svelte
  - web-ui/client/src/routes/call/[threadId]/+page.svelte
  - web-ui/client/src/routes/chat/[threadId]/+page.svelte
  - web-ui/client/src/lib/components/CharacterCard.svelte
  - web-ui/client/src/lib/components/ChatMessageBubble.svelte
  - web-ui/client/src/app.css
  - web-ui/client/tests/e2e/call-start.spec.ts
  - web-ui/client/tests/e2e/call-toolbar.spec.ts
  - web-ui/client/tests/e2e/call-permissions.spec.ts
  - web-ui/client/tests/e2e/call-visualizer.spec.ts
  - web-ui/client/tests/e2e/call-summary.spec.ts
  - web-ui/client/tests/e2e/call-mobile.spec.ts
  - .planning/phases/03-first-working-call-mvp/03-08-SUMMARY.md
autonomous: true
requirements: [REQ-40, REQ-47, REQ-48, REQ-49, REQ-50, REQ-A0]
must_haves:
  truths:
    - "The first screen for calling is the usable call surface, not a marketing or explanatory page."
    - "The call UI follows the approved UI-SPEC copy, colors, spacing, and mobile non-overlap constraints."
    - "Listening and speaking visualizer energy is driven by separate RMS inputs, with deterministic fallback for unsupported metering."
  artifacts:
    - path: "web-ui/client/src/routes/call/[threadId]/+page.svelte"
      provides: "Operational call screen"
    - path: "web-ui/client/src/lib/components/call/CallToolbar.svelte"
      provides: "Mute, interrupt, device picker, and end controls"
  key_links:
    - from: "createRmsMeter mic source"
      to: "VoiceVisualizer listening state"
      via: "listeningRms"
      pattern: "mic RMS drives listening pulse"
    - from: "remote audio output source"
      to: "VoiceVisualizer speaking state"
      via: "speakingRms"
      pattern: "AI output RMS drives speaking waveform"
---

<objective>
Implement the Phase 3 call UI surface, entry points, toolbar, visualizer, live transcript, and thread row rendering.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-first-working-call-mvp/03-UI-SPEC.md
@.planning/phases/03-first-working-call-mvp/03-PATTERNS.md
@docs/stitch/DESIGN.md
@docs/stitch/screens/voice-call-true-dark.md
@web-ui/client/src/routes/chat/[threadId]/+page.svelte
@web-ui/client/src/lib/components/CharacterCard.svelte
@web-ui/client/src/lib/components/ChatMessageBubble.svelte
@web-ui/client/src/app.css
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add call components and visual states</name>
  <read_first>
    .planning/phases/03-first-working-call-mvp/03-UI-SPEC.md
    docs/stitch/DESIGN.md
    docs/stitch/screens/voice-call-true-dark.md
    web-ui/client/src/lib/components/StatusChip.svelte
    web-ui/client/src/lib/components/GlassPanel.svelte
    web-ui/client/src/lib/components/ConfirmDialog.svelte
    web-ui/client/src/app.css
  </read_first>
  <files>web-ui/client/src/lib/components/call/VoiceVisualizer.svelte, web-ui/client/src/lib/components/call/CallToolbar.svelte, web-ui/client/src/lib/components/call/CallTranscript.svelte, web-ui/client/src/app.css</files>
  <action>Create call components using lucide-svelte icons where available. `VoiceVisualizer.svelte` must render states `Listening`, `Thinking`, and `Speaking`, accept numeric props or store values `listeningRms` and `speakingRms`, drive listening pulse from mic RMS, drive speaking waveform from AI output RMS, use deterministic animated fallback when RMS is unavailable, use cyan communication color `#00e3fd` for listening/speaking energy, use accent `#b6a0ff` / `#70aaff` for thinking shimmer, and respect `prefers-reduced-motion: reduce`. `CallToolbar.svelte` must render controls `Mute`/`Unmute`, `Interrupt`, input picker, output picker, and `End Call`; end call uses destructive `#ff716c` and a `PhoneOff` icon. `CallTranscript.svelte` must render finalized user turns, forward-stable streaming AI text with a gradient caret, and compact chips `Final`, `Streaming`, or `Interrupted`. Maintain 44px minimum touch targets and 56px active primary controls.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:e2e -- tests/e2e/call-toolbar.spec.ts tests/e2e/call-visualizer.spec.ts --project=desktop-chromium</automated>
  </verify>
  <acceptance_criteria>
    - `CallToolbar.svelte` contains `Mute`, `Unmute`, `Interrupt`, `End Call`, and `PhoneOff`.
    - `VoiceVisualizer.svelte` contains `Listening`, `Thinking`, `Speaking`, `listeningRms`, `speakingRms`, `#00e3fd`, and `prefers-reduced-motion`.
    - `CallTranscript.svelte` contains `Final`, `Streaming`, and `Interrupted`.
  </acceptance_criteria>
  <done>Reusable call UI components satisfy visual and toolbar contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Add call route and entry points</name>
  <read_first>
    web-ui/client/src/routes/chat/[threadId]/+page.svelte
    web-ui/client/src/lib/components/CharacterCard.svelte
    web-ui/client/src/lib/api/calls.ts
    web-ui/client/src/lib/call/store.svelte.ts
    web-ui/client/src/lib/call/audio.ts
    .planning/phases/03-first-working-call-mvp/03-UI-SPEC.md
  </read_first>
  <files>web-ui/client/src/routes/call/[threadId]/+page.svelte, web-ui/client/src/routes/chat/[threadId]/+page.svelte, web-ui/client/src/lib/components/CharacterCard.svelte</files>
  <action>Add a route `src/routes/call/[threadId]/+page.svelte` that starts from the active thread, runs mic permission and AudioContext unlock on the Start Call gesture, negotiates the call, shows the operational call screen, hides typed text while active, and returns to `/chat/{threadId}` on end. Add a thread header icon button with accessible label `Start call`. Add character-card `Start Call` action that calls `/api/calls/start` with `character_id` and navigates to the returned thread call route. Blocking preflight panels must use exact UI-SPEC copy for mic denied, no voice, unavailable voice, backend readiness, and peer/network failure.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:e2e -- tests/e2e/call-start.spec.ts tests/e2e/call-permissions.spec.ts --project=desktop-chromium</automated>
  </verify>
  <acceptance_criteria>
    - `chat/[threadId]/+page.svelte` contains accessible label `Start call`.
    - `CharacterCard.svelte` contains `Start Call`.
    - `call/[threadId]/+page.svelte` contains `Retry Microphone`, `Open Character`, `Choose Voice`, `Open Settings`, and `Return to Thread`.
    - The verification command exits 0.
  </acceptance_criteria>
  <done>Users can enter the call route from a thread or character card and see correct blocking states.</done>
</task>

<task type="auto">
  <name>Task 3: Render call rows in thread scrollback and preserve mobile layout</name>
  <read_first>
    web-ui/client/src/lib/components/ChatMessageBubble.svelte
    web-ui/client/src/routes/chat/[threadId]/+page.svelte
    web-ui/client/tests/e2e/call-summary.spec.ts
    web-ui/client/tests/e2e/call-mobile.spec.ts
    .planning/phases/03-first-working-call-mvp/03-UI-SPEC.md
  </read_first>
  <files>web-ui/client/src/lib/components/ChatMessageBubble.svelte, web-ui/client/src/routes/call/[threadId]/+page.svelte, web-ui/client/tests/e2e/call-summary.spec.ts, web-ui/client/tests/e2e/call-mobile.spec.ts</files>
  <action>Extend message rendering so `call_start` and `call_end` rows render as compact event rows, `user_speech` renders as a user speech bubble, and `ai_speech` renders as an assistant speech bubble. After End Call, ensure the thread route shows chronological rows for `call_start`, `user_speech`, `ai_speech`, and `call_end` and the text composer is visible again. On mobile, hide or reserve AppShell bottom navigation while the call route is active so it never overlaps Mute, Interrupt, End Call, or device controls; reserve at least `80px + env(safe-area-inset-bottom)` below sticky controls.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:e2e -- tests/e2e/call-summary.spec.ts --project=desktop-chromium && npm --prefix web-ui/client run test:e2e -- tests/e2e/call-mobile.spec.ts --project=mobile-chromium</automated>
  </verify>
  <acceptance_criteria>
    - `ChatMessageBubble.svelte` branches on all four message kinds `call_start`, `user_speech`, `ai_speech`, and `call_end`.
    - `call-mobile.spec.ts` asserts Mute, Interrupt, End Call, and device controls have non-overlapping bounding boxes or visible touch targets.
    - The verification command exits 0.
  </acceptance_criteria>
  <done>Thread scrollback and mobile call surface satisfy UI-SPEC acceptance.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Information Disclosure | Error panels | mitigate | UI uses fixed recovery copy and never renders raw backend exception text. |
| T-03-08-02 | Tampering | Call row rendering | mitigate | Known `message_kind` values map to fixed render paths; transcript text uses existing sanitized render boundaries. |
| T-03-08-03 | Denial of Service | Mobile layout | mitigate | Playwright mobile spec asserts sticky controls are visible and non-overlapped. |
</threat_model>

<verification>
Run `npm --prefix web-ui/client run test:unit -- --run` and the six Phase 3 call Playwright specs across desktop and mobile projects.
</verification>

<success_criteria>
The Phase 3 call UI is operational, follows the approved UI-SPEC, and supports thread/character entry, live transcript, toolbar controls, and return-to-thread scrollback.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-working-call-mvp/03-08-SUMMARY.md`.
</output>
