---
phase: 03-first-working-call-mvp
plan: "04"
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - ai-backend/app/call/__init__.py
  - ai-backend/app/call/events.py
  - ai-backend/app/call/session.py
  - ai-backend/app/call/tracks.py
  - ai-backend/app/api/webrtc.py
  - ai-backend/app/main.py
  - ai-backend/tests/test_call_session.py
  - ai-backend/tests/test_webrtc_signaling.py
  - .planning/phases/03-first-working-call-mvp/03-04-SUMMARY.md
autonomous: true
requirements: [REQ-47, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "The Phase 2 WebRTC skeleton is replaced by a live session manager without exposing raw exceptions."
    - "Mute stops server-side audio consumption and interrupt cancels the active AI turn."
    - "Inbound WebRTC audio is converted into `user_final` through VAD/end-of-turn and STT before any AI turn begins."
    - "The `rayme-events` data channel carries AI-backend-originated call events to the browser."
  artifacts:
    - path: "ai-backend/app/call/session.py"
      provides: "Call session lifecycle and controls"
    - path: "ai-backend/app/api/webrtc.py"
      provides: "AI backend WebRTC signaling and control routes"
  key_links:
    - from: "Inbound WebRTC audio track"
      to: "CallSession.handle_inbound_audio_frame"
      via: "aiortc frame receiver"
      pattern: "RTP audio -> VAD -> STT -> user_final"
    - from: "CallSession user_final event"
      to: "Browser call route"
      via: "rayme-events RTCDataChannel"
      pattern: "typed JSON event with session_id and turn_id"
---

<objective>
Implement the AI backend live call session manager and `/webrtc` signaling/control routes required by Phase 3.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-first-working-call-mvp/03-CONTEXT.md
@.planning/phases/03-first-working-call-mvp/03-RESEARCH.md
@.planning/phases/03-first-working-call-mvp/03-PATTERNS.md
@ai-backend/app/api/webrtc.py
@ai-backend/app/api/stt.py
@ai-backend/app/api/tts.py
@ai-backend/app/models/model_manager.py
@ai-backend/tests/test_call_session.py
@ai-backend/tests/test_webrtc_signaling.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create typed call events and session manager</name>
  <read_first>
    ai-backend/app/api/webrtc.py
    ai-backend/app/api/stt.py
    ai-backend/app/api/tts.py
    ai-backend/app/models/model_manager.py
    ai-backend/tests/test_call_session.py
    .planning/phases/03-first-working-call-mvp/03-PATTERNS.md
  </read_first>
  <files>ai-backend/app/call/__init__.py, ai-backend/app/call/events.py, ai-backend/app/call/session.py, ai-backend/app/call/tracks.py</files>
  <action>Add `app.call` package. In `events.py`, define typed event dictionaries or Pydantic models for exact event types `user_final`, `ai_audio_started`, `ai_done`, `muted`, `interrupted`, `ended`, and `failed`, each carrying `session_id` and `turn_id` where applicable. In `session.py`, implement `CallSession` and `CallSessionManager` with methods `create_session`, `get_session`, `remove_session`, `handle_inbound_audio_frame`, `finalize_user_turn`, `emit_event`, `set_muted`, `interrupt`, `end`, and `stats`. Track fields `session_id`, `thread_id`, `voice_id`, `engine_id`, `state`, `muted`, `incoming_audio_frames`, `dropped_audio_frames`, `active_turn_task`, `started_at`, and `ended_at`. In `tracks.py`, add real Phase 3 inbound/outbound track helpers: inbound helpers accept aiortc audio frames or decoded PCM frames, normalize them to the STT/VAD input format used by `app.api.stt`, and expose frame counters; outbound helpers accept synthesized WAV/PCM chunks for playback. Do not use production stubs for inbound audio. Ensure `set_muted(True)` causes inbound frame handling to increment `dropped_audio_frames` and skip VAD/STT consumption.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_call_session.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `ai-backend/app/call/session.py` contains `class CallSession` and `class CallSessionManager`.
    - `ai-backend/app/call/events.py` contains all event type strings `user_final`, `ai_audio_started`, `ai_done`, `muted`, `interrupted`, `ended`, and `failed`.
    - `ai-backend/app/call/session.py` contains `handle_inbound_audio_frame` and `finalize_user_turn`.
    - `uv run --project ai-backend pytest ai-backend/tests/test_call_session.py -q` exits 0.
  </acceptance_criteria>
  <done>AI backend call session lifecycle and control model passes RED tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire inbound audio to VAD/STT and user_final data-channel events</name>
  <read_first>
    ai-backend/app/api/stt.py
    ai-backend/app/models/stt.py
    ai-backend/app/models/vad.py
    ai-backend/app/call/session.py
    ai-backend/app/call/tracks.py
    ai-backend/tests/test_call_session.py
  </read_first>
  <files>ai-backend/app/call/session.py, ai-backend/app/call/tracks.py, ai-backend/app/call/events.py, ai-backend/tests/test_call_session.py</files>
  <action>Implement the real MVP finalization path: each inbound audio frame from aiortc is counted, ignored when muted, otherwise appended to a per-session turn buffer. Use the existing VAD adapter semantics and configured `vad_threshold` / `vad_end_silence_ms` to decide end-of-turn. On end-of-turn, write the buffered audio to a temporary WAV using existing audio utilities, call the existing STT adapter with English settings, and emit one `user_final` event through the session event sink / `rayme-events` data channel with fields `type`, `session_id`, `turn_id`, `text`, `started_at`, and `ended_at`. If STT fails, emit `failed` with public code `call_stt_failed` and retry guidance; do not include tracebacks or local temp paths. Clean up temp audio after transcription.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_call_session.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `session.py` emits `user_final` only after VAD end-of-turn.
    - `session.py` contains public code `call_stt_failed`.
    - Tests assert muted frames do not call STT and unmuted finalized frames emit `user_final`.
    - The verification command exits 0.
  </acceptance_criteria>
  <done>AI backend produces real finalized user speech events from inbound WebRTC audio.</done>
</task>

<task type="auto">
  <name>Task 3: Replace WebRTC skeleton routes with live signaling and controls</name>
  <read_first>
    ai-backend/app/api/webrtc.py
    ai-backend/app/main.py
    ai-backend/app/call/session.py
    ai-backend/tests/test_webrtc_signaling.py
    ai-backend/tests/test_health.py
  </read_first>
  <files>ai-backend/app/api/webrtc.py, ai-backend/app/main.py</files>
  <action>Update `ai-backend/app/api/webrtc.py` so `/webrtc/status` returns `status`, `phase: \"03\"`, `live_call_ready`, `media_transport_ready`, `active_sessions`, and `message`. Add Pydantic request models for offer/control payloads. Implement `POST /webrtc/offer` to accept server-owned `session_id`, `thread_id`, `voice_id`, `engine_id`, `prompt_messages`, and `offer`, create/get a `CallSession`, attach inbound audio receiver, create/accept data channel label `rayme-events`, and return `{\"session_id\": ..., \"answer\": {\"type\": \"answer\", \"sdp\": ...}, \"event_channel\": \"rayme-events\"}`. For unit tests, allow a deterministic fake answer when real aiortc peer negotiation is not injected, but do not bypass the session/event-channel contract. Implement `POST /webrtc/sessions/{session_id}/mute`, `/interrupt`, and `/end` returning `session_id`, `state`, and control result fields. Store one `CallSessionManager` on `app.state.call_session_manager` in `main.py`. All unexpected errors must map to fixed public codes such as `webrtc_offer_failed`, `call_session_not_found`, and `call_control_failed` without tracebacks.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_webrtc_signaling.py ai-backend/tests/test_health.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `ai-backend/app/api/webrtc.py` contains `CallOfferRequest`, `CallControlResponse`, `rayme-events`, `/webrtc/sessions/{session_id}/mute`, `/interrupt`, and `/end`.
    - `/webrtc/status` test sees `phase` equal to `03`.
    - `uv run --project ai-backend pytest ai-backend/tests/test_webrtc_signaling.py ai-backend/tests/test_health.py -q` exits 0.
  </acceptance_criteria>
  <done>AI backend `/webrtc` signaling and controls satisfy Phase 3 tests.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | Offer/control payloads | mitigate | Pydantic models validate required IDs and offer shape before session operations. |
| T-03-04-02 | Denial of Service | Orphaned sessions | mitigate | `end()` and failed connection state close peer/session resources and remove manager entries. |
| T-03-04-03 | Information Disclosure | Runtime exceptions | mitigate | Routes return fixed public error codes and never include tracebacks or local paths. |
| T-03-04-04 | Denial of Service | Mute semantics | mitigate | Muted sessions stop server-side audio consumption rather than only toggling local playback. |
| T-03-04-05 | Information Disclosure | STT failures | mitigate | `call_stt_failed` events expose only public retry guidance and never temp paths or tracebacks. |
</threat_model>

<verification>
Run `uv run --project ai-backend pytest ai-backend/tests/test_call_session.py ai-backend/tests/test_webrtc_signaling.py ai-backend/tests/test_health.py -q`.
</verification>

<success_criteria>
The AI backend has a tested Phase 3 WebRTC/session surface that the Web UI server can call for offer/answer, mute, interrupt, and teardown.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-working-call-mvp/03-04-SUMMARY.md`.
</output>
