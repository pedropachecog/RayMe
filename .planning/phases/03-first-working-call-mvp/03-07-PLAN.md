---
phase: 03-first-working-call-mvp
plan: "07"
type: execute
wave: 2
depends_on: ["03-03", "03-05"]
files_modified:
  - web-ui/client/src/lib/api/calls.ts
  - web-ui/client/src/lib/api/types.ts
  - web-ui/client/src/lib/call/client.ts
  - web-ui/client/src/lib/call/store.svelte.ts
  - web-ui/client/src/lib/call/audio.ts
  - web-ui/client/tests/unit/call-state.test.ts
  - web-ui/client/tests/unit/call-audio.test.ts
  - .planning/phases/03-first-working-call-mvp/03-07-SUMMARY.md
autonomous: true
requirements: [REQ-47, REQ-48, REQ-49, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "Browser call state is represented by a dedicated FSM, not route-local ad hoc booleans."
    - "AudioContext unlock runs from the Start Call gesture and reports `state === 'running'`."
    - "Mute tracks server-side consumption state separately from local audio track state."
    - "The browser receives AI-backend-originated `user_final` events over the `rayme-events` data channel."
  artifacts:
    - path: "web-ui/client/src/lib/call/store.svelte.ts"
      provides: "Call FSM and transcript state"
    - path: "web-ui/client/src/lib/call/audio.ts"
      provides: "AudioContext unlock, device picker, and metering helpers"
  key_links:
    - from: "RTCPeerConnection rayme-events data channel"
      to: "createCallStore.appendUserFinal"
      via: "onDataEvent"
      pattern: "AI backend user_final -> browser -> /api/calls/{call_id}/turns"
---

<objective>
Implement client call API wrappers, browser media helpers, and the call state machine.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-first-working-call-mvp/03-UI-SPEC.md
@.planning/phases/03-first-working-call-mvp/03-PATTERNS.md
@web-ui/client/src/lib/api/client.ts
@web-ui/client/src/lib/api/chat.ts
@web-ui/client/src/lib/api/types.ts
@web-ui/client/src/lib/browser/environment.ts
@web-ui/client/tests/unit/call-state.test.ts
@web-ui/client/tests/unit/call-audio.test.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add typed call API and WebRTC client helper</name>
  <read_first>
    web-ui/client/src/lib/api/client.ts
    web-ui/client/src/lib/api/chat.ts
    web-ui/client/src/lib/api/types.ts
    web-ui/client/tests/unit/call-state.test.ts
  </read_first>
  <files>web-ui/client/src/lib/api/calls.ts, web-ui/client/src/lib/api/types.ts, web-ui/client/src/lib/call/client.ts</files>
  <action>Add typed client functions `startCall`, `sendCallOffer`, `submitCallTurn`, `setCallMuted`, `interruptCall`, and `endCall` in `src/lib/api/calls.ts`. Add TypeScript types for `CallStartRequest`, `CallStartResponse`, `CallOfferResponse`, `CallTurnRequest`, `CallEvent`, `CallTranscriptTurn`, `CallStateName`, and `CallErrorCode` in `api/types.ts`. Add `src/lib/call/client.ts` with `createCallPeer({ localStream, onRemoteStream, onDataEvent })`, `attachMicrophoneTracks(peer, stream)`, `negotiateCall(peer, callId)`, and `closeCallPeer(peer)`. Use `RTCPeerConnection.addTrack()` and `pc.ontrack` only; do not use legacy `addStream()`. Create or accept an RTCDataChannel labeled `rayme-events`; parse JSON messages with event types `user_final`, `ai_audio_started`, `muted`, `interrupted`, `ended`, and `failed`, and pass them to `onDataEvent` without throwing on malformed JSON.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run tests/unit/call-state.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `web-ui/client/src/lib/api/calls.ts` exports all six API function names from the action, including `submitCallTurn`.
    - `web-ui/client/src/lib/call/client.ts` contains `RTCPeerConnection`, `addTrack`, `ontrack`, and `rayme-events`.
    - No file contains `addStream(`.
  </acceptance_criteria>
  <done>Typed browser call transport helpers are available.</done>
</task>

<task type="auto">
  <name>Task 2: Implement call FSM and AudioContext/device helpers</name>
  <read_first>
    web-ui/client/src/lib/browser/environment.ts
    web-ui/client/src/lib/components/voice/SynthPreviewPanel.svelte
    .planning/phases/03-first-working-call-mvp/03-UI-SPEC.md
    web-ui/client/tests/unit/call-state.test.ts
    web-ui/client/tests/unit/call-audio.test.ts
  </read_first>
  <files>web-ui/client/src/lib/call/store.svelte.ts, web-ui/client/src/lib/call/audio.ts, web-ui/client/tests/unit/call-state.test.ts, web-ui/client/tests/unit/call-audio.test.ts</files>
  <action>Implement `createCallStore()` with exact states `idle`, `connecting`, `listening`, `thinking`, `speaking`, `interrupted`, `ended`, and `failed`. The store must expose `startConnecting`, `markListening`, `markThinking`, `markSpeaking`, `interruptAiTurn`, `setServerMuted`, `appendUserFinal`, `appendAiToken`, `finishAiTurn`, `failCall`, and `endCall`. `appendUserFinal` must add finalized user text received from the AI-backend data channel before posting it to the server. `appendAiToken` must only append to the active AI text and never rewrite existing visible text. Implement `unlockCallAudioContext()` in `audio.ts` to create/resume an `AudioContext`, play a one-sample silent buffer, and return `{ state: audioContext.state }`. Implement `createRmsMeter(source)` or equivalent helpers for separate mic/listening RMS and AI-output/speaking RMS. Implement `listAudioInputDevices()`, `selectAudioOutputIfSupported(audio, deviceId)`, and copy helpers that return the exact UI-SPEC unsupported picker copy.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run tests/unit/call-state.test.ts tests/unit/call-audio.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `store.svelte.ts` contains every state and method named in the action.
    - `audio.ts` contains `createBuffer(1, 1, audioContext.sampleRate)` or equivalent one-sample silent buffer logic.
    - `audio.ts` contains an RMS helper used separately for mic/listening and AI/speaking state.
    - `audio.ts` contains the exact strings `Input selection is not available in this browser. RayMe will use the current microphone.` and `Output selection is not available in this browser. RayMe will use the browser default output.`
    - The verification command exits 0.
  </acceptance_criteria>
  <done>Client call state and browser media helper tests pass.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | Denial of Service | Browser call bootstrap | mitigate | Audio unlock and mic acquisition expose explicit failed states rather than continuing half-connected. |
| T-03-07-02 | Tampering | Transcript streaming | mitigate | Forward-stable `appendAiToken` appends only and never rewrites previously visible text. |
| T-03-07-03 | Information Disclosure | Device picker errors | mitigate | Unsupported picker helpers return fixed UI copy, not raw DOMException details. |
</threat_model>

<verification>
Run `npm --prefix web-ui/client run test:unit -- --run tests/unit/call-state.test.ts tests/unit/call-audio.test.ts`.
</verification>

<success_criteria>
The client has reusable typed call APIs, WebRTC helper functions, audio unlock/device handling, and a tested call FSM ready for UI integration.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-working-call-mvp/03-07-SUMMARY.md`.
</output>
