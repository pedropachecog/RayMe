---
phase: 03-first-working-call-mvp
plan: "09"
type: execute
wave: 4
depends_on: ["03-04", "03-05", "03-06", "03-07", "03-08"]
files_modified:
  - ai-backend/app/api/webrtc.py
  - ai-backend/app/call/events.py
  - ai-backend/app/call/session.py
  - ai-backend/tests/test_call_session.py
  - ai-backend/tests/test_webrtc_signaling.py
  - web-ui/server/app/api/calls.py
  - web-ui/server/app/domain/call_service.py
  - web-ui/server/app/domain/ai_backend_client.py
  - web-ui/server/tests/test_calls.py
  - web-ui/client/src/lib/api/calls.ts
  - web-ui/client/src/lib/call/client.ts
  - web-ui/client/src/lib/call/store.svelte.ts
  - web-ui/client/src/routes/call/[threadId]/+page.svelte
  - web-ui/client/tests/e2e/call-start.spec.ts
  - .planning/phases/03-first-working-call-mvp/03-09-SUMMARY.md
autonomous: true
requirements: [REQ-40, REQ-47, REQ-49, REQ-50, REQ-63, REQ-A0]
must_haves:
  truths:
    - "A `user_final` event produced from inbound WebRTC audio triggers prompt hydration, LLM streaming text, AI-backend TTS playback, durable AI speech writeback, and return to listening."
    - "At least two user→AI cycles run inside the same call session before `call_end`."
    - "Button interrupt cancels both remaining AI text generation and AI audio playback."
    - "The visible AI text stream is forward-stable and becomes the durable `ai_speech` row."
    - "`call_id` and `session_id` correlation is read from the Web UI server active-call registry, never guessed by the browser."
  artifacts:
    - path: "web-ui/server/app/api/calls.py"
      provides: "Call turn orchestration endpoint"
    - path: "ai-backend/app/api/webrtc.py"
      provides: "Session speech playback and cancellation controls"
  key_links:
    - from: "AI backend `user_final` event"
      to: "POST /api/calls/{call_id}/turns"
      via: "browser rayme-events data-channel handler"
      pattern: "inbound RTP -> VAD/STT -> user_final -> server turn orchestration"
    - from: "CallService.session_for_call(call_id)"
      to: "POST /webrtc/sessions/{session_id}/speak"
      via: "AiBackendClient"
      pattern: "server-owned call/session mapping"
    - from: "SSE ai_token stream"
      to: "Thread ai_speech message"
      via: "accumulated visible text"
      pattern: "forward-stable transcript persistence"
---

<objective>
Wire the full Phase 3 MVP call loop across browser, Web UI server, and AI backend.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-first-working-call-mvp/03-CONTEXT.md
@.planning/phases/03-first-working-call-mvp/03-RESEARCH.md
@.planning/phases/03-first-working-call-mvp/03-PATTERNS.md
@ai-backend/app/api/stt.py
@ai-backend/app/api/tts.py
@web-ui/server/app/domain/llm_stream.py
@web-ui/server/app/domain/prompt_builder.py
@web-ui/client/src/lib/api/stream.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add backend speech playback and cancellation session hooks</name>
  <read_first>
    ai-backend/app/api/webrtc.py
    ai-backend/app/call/session.py
    ai-backend/app/call/events.py
    ai-backend/app/api/tts.py
    ai-backend/app/models/model_manager.py
    ai-backend/tests/test_call_session.py
    ai-backend/tests/test_webrtc_signaling.py
  </read_first>
  <files>ai-backend/app/api/webrtc.py, ai-backend/app/call/events.py, ai-backend/app/call/session.py, ai-backend/tests/test_call_session.py, ai-backend/tests/test_webrtc_signaling.py</files>
  <action>Add `POST /webrtc/sessions/{session_id}/speak` accepting JSON fields `turn_id`, `text`, `voice_id`, `engine_id`, and optional `final_chunk`. Add `CallSession.speak_text(turn_id, text, voice_id, engine_id, final_chunk=False)` that queues or plays TTS audio through the outbound call audio path and emits typed data-channel events `ai_audio_started` and `ai_done` on `rayme-events`. Add `CallSession.cancel_ai_turn(turn_id=None)` used by interrupt and end. Do not silently switch voices or engines; failures return fixed code `call_tts_failed` with `engine_id`. Keep tests model-download-free by injecting a fake TTS adapter/session track in tests.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_call_session.py ai-backend/tests/test_webrtc_signaling.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `ai-backend/app/api/webrtc.py` contains `/webrtc/sessions/{session_id}/speak`.
    - `session.py` contains `speak_text` and `cancel_ai_turn`.
    - Tests assert interrupt cancels queued or active speech before `ai_done`.
    - The verification command exits 0.
  </acceptance_criteria>
  <done>AI backend can receive AI speech text for playback and cancel it deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Add server turn orchestration stream</name>
  <read_first>
    web-ui/server/app/api/calls.py
    web-ui/server/app/domain/call_service.py
    web-ui/server/app/domain/ai_backend_client.py
    web-ui/server/app/domain/llm_stream.py
    web-ui/server/app/domain/prompt_builder.py
    web-ui/server/tests/test_calls.py
  </read_first>
  <files>web-ui/server/app/api/calls.py, web-ui/server/app/domain/call_service.py, web-ui/server/app/domain/ai_backend_client.py, web-ui/server/tests/test_calls.py</files>
  <action>Add `POST /api/calls/{call_id}/turns` that accepts finalized user speech JSON forwarded from the browser data-channel handler: `{ \"session_id\": \"...\", \"turn_id\": \"...\", \"text\": \"...\", \"source\": \"user_final\" }`. The route must verify `session_id` matches `CallService.session_for_call(call_id)` and reject mismatches with `call_session_not_found`. It must write `user_speech`, call `build_call_prompt_context(max_turns=24)`, stream the LLM response using the existing OpenAI-compatible chat streaming path, emit forward-stable `ai_token` SSE events to the browser, forward sentence or chunk text to AI backend `/webrtc/sessions/{session_id}/speak`, and write a final `ai_speech` message with exactly the visible accumulated text. Add interrupt handling so `POST /api/calls/{call_id}/interrupt` cancels the server LLM stream and calls AI backend interrupt for the mapped `session_id`. If LLM generation fails, return/send fixed code `call_generation_failed` and preserve already written user speech. Server tests must cover two sequential `/turns` calls in the same `call_id` producing two `user_speech` and two `ai_speech` rows before `call_end`.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py web-ui/server/tests/test_prompt_builder.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `web-ui/server/app/api/calls.py` contains `/api/calls/{call_id}/turns`.
    - Server tests assert two user turns in one call write two `user_speech` rows, streamed tokens accumulate in order, and two final `ai_speech.content_text` rows equal the visible accumulated text for each turn.
    - Server tests assert a mismatched `session_id` is rejected with `call_session_not_found`.
    - Server tests assert interrupt calls both the LLM cancellation path and AI backend `interrupt_call`.
    - The verification command exits 0.
  </acceptance_criteria>
  <done>Web UI server orchestrates repeated `user_final` turns into streamed AI text, TTS playback, and durable writeback within one call.</done>
</task>

<task type="auto">
  <name>Task 3: Wire browser turn stream, transcript, and controls end-to-end</name>
  <read_first>
    web-ui/client/src/lib/api/calls.ts
    web-ui/client/src/lib/api/stream.ts
    web-ui/client/src/lib/call/store.svelte.ts
    web-ui/client/src/routes/call/[threadId]/+page.svelte
    web-ui/client/tests/e2e/call-start.spec.ts
  </read_first>
  <files>web-ui/client/src/lib/api/calls.ts, web-ui/client/src/lib/call/store.svelte.ts, web-ui/client/src/routes/call/[threadId]/+page.svelte, web-ui/client/tests/e2e/call-start.spec.ts</files>
  <action>Add client turn submission after a `user_final` event arrives on the `rayme-events` data channel: post `{ session_id, turn_id, text, source: \"user_final\" }` to `/api/calls/{call_id}/turns`, consume SSE events with types `ai_token`, `ai_done`, and `error`, append tokens to the active AI transcript without rewriting previous text, set visual states `thinking` before first token and `speaking` when `ai_audio_started` arrives or token flow begins, and return to `listening` after `ai_done`. Support two sequential user_final events in the same call without recreating the peer. Wire `Interrupt` to call `/api/calls/{call_id}/interrupt`, cancel the local SSE reader, stop current remote playback if active, mark transcript chip `Interrupted`, and return to listening.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:e2e -- tests/e2e/call-start.spec.ts tests/e2e/call-toolbar.spec.ts tests/e2e/call-summary.spec.ts --project=desktop-chromium</automated>
  </verify>
  <acceptance_criteria>
    - The call route handles data-channel event types `user_final` and `ai_audio_started`, plus SSE event types `ai_token`, `ai_done`, and `error`.
    - `Interrupt` path calls the API wrapper and cancels the active stream reader.
    - `call-start.spec.ts` asserts two user→AI cycles in one call, visible AI text grows forward-stably for both cycles, and final thread `ai_speech` rows contain the same text.
    - The verification command exits 0.
  </acceptance_criteria>
  <done>Browser call flow runs a multi-turn MVP loop with streaming text and interrupt.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09-01 | Tampering | Streamed transcript | mitigate | Server writes final `ai_speech` from accumulated emitted tokens; client append-only rendering prevents rewrites. |
| T-03-09-02 | Denial of Service | Active AI generation/playback | mitigate | Interrupt and end cancel LLM stream, queued TTS playback, and active remote audio. |
| T-03-09-03 | Information Disclosure | Generation/TTS failures | mitigate | Fixed public error codes `call_generation_failed` and `call_tts_failed`; no raw stack traces. |
| T-03-09-04 | Repudiation | Partial calls | mitigate | User speech written before generation is preserved and end/failure writes truthful call end state. |
| T-03-09-05 | Spoofing | call/session correlation | mitigate | `/turns` validates posted `session_id` against server-owned `call_id` mapping before writeback or TTS playback. |
</threat_model>

<verification>
Run backend, server, and client call targets: `uv run --project ai-backend pytest ai-backend/tests/test_call_session.py ai-backend/tests/test_webrtc_signaling.py -q && uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py web-ui/server/tests/test_prompt_builder.py -q && npm --prefix web-ui/client run test:e2e -- tests/e2e/call-start.spec.ts tests/e2e/call-toolbar.spec.ts tests/e2e/call-summary.spec.ts --project=desktop-chromium`.
</verification>

<success_criteria>
Two finalized user speech turns produced from inbound WebRTC audio can each produce streamed AI text, AI speech playback, durable call rows, interrupt-safe cancellation, and return-to-listening behavior inside one call session.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-working-call-mvp/03-09-SUMMARY.md`.
</output>
