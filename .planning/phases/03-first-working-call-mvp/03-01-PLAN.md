---
phase: 03-first-working-call-mvp
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - ai-backend/tests/test_call_session.py
  - ai-backend/tests/test_webrtc_signaling.py
  - .planning/phases/03-first-working-call-mvp/03-01-SUMMARY.md
autonomous: true
requirements: [REQ-47, REQ-A0, REQ-A1]
must_haves:
  truths:
    - "AI backend live call behavior is specified by failing tests before implementation."
    - "Server-side mute, interrupt, teardown, and status contracts are named before feature work starts."
    - "Inbound WebRTC audio becomes a finalized `user_final` event through VAD/end-of-turn and STT, not a fabricated JSON input."
  artifacts:
    - path: "ai-backend/tests/test_call_session.py"
      provides: "RED call-session lifecycle, mute, interrupt, teardown, and stats contracts"
    - path: "ai-backend/tests/test_webrtc_signaling.py"
      provides: "RED WebRTC offer/status/control API contracts"
---

<objective>
Add AI backend Wave 0 tests for the Phase 3 call session and WebRTC signaling contracts.

Purpose: lock the backend transport/session surface before replacing the Phase 2 `/webrtc` skeleton.
Output: failing tests that define the session manager, offer/answer route, server-side mute, interrupt, teardown, and sanitized status behavior.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-first-working-call-mvp/03-CONTEXT.md
@.planning/phases/03-first-working-call-mvp/03-RESEARCH.md
@.planning/phases/03-first-working-call-mvp/03-VALIDATION.md
@.planning/phases/03-first-working-call-mvp/03-PATTERNS.md
@ai-backend/app/api/webrtc.py
@ai-backend/tests/test_webrtc_signaling.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add RED call session tests</name>
  <read_first>
    .planning/phases/03-first-working-call-mvp/03-VALIDATION.md
    .planning/phases/03-first-working-call-mvp/03-PATTERNS.md
    ai-backend/app/api/webrtc.py
    ai-backend/tests/test_webrtc_signaling.py
    ai-backend/tests/test_stt.py
    ai-backend/tests/test_tts_registry.py
  </read_first>
  <files>ai-backend/tests/test_call_session.py</files>
  <action>Create `ai-backend/tests/test_call_session.py` with tests for a future `app.call.session.CallSession` and `CallSessionManager`. Cover exact behaviors: `create_session()` returns a stable `session_id`; `set_muted(True)` changes `muted` to true and records incoming frame consumption as stopped; `interrupt()` sets `interrupted` and cancels any active AI turn; `end(reason=\"hangup\")` closes the peer/session once and records reason `hangup`; failed connection state records reason `connection_failed`; `stats()` returns `incoming_audio_frames`, `dropped_audio_frames`, `muted`, `state`, and `session_id`. Mark tests as ordinary pytest tests, not skipped.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_call_session.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `ai-backend/tests/test_call_session.py` imports `CallSession` or `CallSessionManager` from `app.call.session`.
    - The test file contains the exact strings `test_mute_stops_server_consumption`, `test_inbound_audio_emits_user_final_after_vad_end`, `test_interrupt_cancels_active_ai_turn`, and `test_end_closes_peer_once`.
    - Running `uv run --project ai-backend pytest ai-backend/tests/test_call_session.py -q` fails because implementation modules or methods are missing, not because of syntax errors.
  </acceptance_criteria>
  <done>Backend call-session behavior is captured as RED tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add RED inbound audio to user_final tests</name>
  <read_first>
    ai-backend/app/api/stt.py
    ai-backend/app/models/stt.py
    ai-backend/app/models/vad.py
    ai-backend/tests/test_stt.py
    ai-backend/tests/test_call_session.py
    .planning/phases/03-first-working-call-mvp/03-RESEARCH.md
  </read_first>
  <files>ai-backend/tests/test_call_session.py</files>
  <action>Extend `test_call_session.py` with tests for a future inbound-audio finalization path. Add a fake inbound audio frame source, fake VAD adapter, and fake STT adapter. Assert `CallSession.handle_inbound_audio_frame(...)` increments `incoming_audio_frames` while unmuted, buffers PCM until VAD end-of-turn, calls STT once, and emits exactly one typed event `{ \"type\": \"user_final\", \"session_id\": ..., \"turn_id\": ..., \"text\": \"hello from mic\" }`. Add a muted variant asserting frames are counted as dropped and no STT call occurs while muted.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_call_session.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_call_session.py` contains `handle_inbound_audio_frame`, `user_final`, and `hello from mic`.
    - The test asserts muted inbound frames do not call STT.
    - Running the test command fails only because implementation is missing.
  </acceptance_criteria>
  <done>Real mic-audio finalization is captured as a RED backend contract.</done>
</task>

<task type="auto">
  <name>Task 3: Extend WebRTC signaling tests for live call contracts</name>
  <read_first>
    ai-backend/app/api/webrtc.py
    ai-backend/tests/test_webrtc_signaling.py
    .planning/phases/03-first-working-call-mvp/03-RESEARCH.md
  </read_first>
  <files>ai-backend/tests/test_webrtc_signaling.py</files>
  <action>Replace the Phase 2-only expectation that `/webrtc/offer` always returns `501` with Phase 3 contract tests. Keep a test that `/webrtc/status` exposes `live_call_ready`, `media_transport_ready`, `active_sessions`, and `status`. Add tests that `POST /webrtc/offer` accepts JSON fields `session_id`, `thread_id`, `voice_id`, `engine_id`, `prompt_messages`, and `offer`, returning `session_id` plus `answer.sdp`, `answer.type`, and data-channel label `rayme-events`. Add tests for `POST /webrtc/sessions/{session_id}/mute`, `/interrupt`, and `/end`, each returning JSON with `session_id` and `state`. Add one malformed payload test expecting a sanitized `400` or `422` response that does not include traceback text.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_webrtc_signaling.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_webrtc_signaling.py` contains `/webrtc/sessions/{session_id}/mute`, `/interrupt`, and `/end`.
    - `test_webrtc_signaling.py` no longer asserts that every offer returns `501`.
    - The malformed payload test asserts response text does not contain `Traceback`.
    - Running `uv run --project ai-backend pytest ai-backend/tests/test_webrtc_signaling.py -q` reaches assertion/import failures for missing Phase 3 implementation, not Python syntax errors.
  </acceptance_criteria>
  <done>WebRTC signaling and control routes are captured as RED tests.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high
- execution_rule: any high-severity unmitigated issue blocks plan completion.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | WebRTC control payloads | mitigate | RED tests require malformed payload handling through FastAPI/Pydantic and sanitized 400/422 responses. |
| T-03-01-02 | Denial of Service | Orphaned peer sessions | mitigate | RED tests require `end()` to close peers exactly once and failure state to tear down sessions. |
| T-03-01-03 | Information Disclosure | Backend errors | mitigate | Tests assert no traceback text in malformed offer responses. |
</threat_model>

<verification>
Run `uv run --project ai-backend pytest ai-backend/tests/test_call_session.py ai-backend/tests/test_webrtc_signaling.py -q`. This plan is complete when tests are syntactically valid and fail only because Phase 3 implementation is not present yet.
</verification>

<success_criteria>
AI backend Wave 0 contracts are present, RED for the right reasons, and ready for implementation plans to satisfy them.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-working-call-mvp/03-01-SUMMARY.md`.
</output>
