---
phase: 03-first-working-call-mvp
plan: "06"
type: execute
wave: 1
depends_on: ["03-02"]
files_modified:
  - web-ui/server/app/domain/prompt_builder.py
  - web-ui/server/tests/test_prompt_builder.py
  - .planning/phases/03-first-working-call-mvp/03-06-SUMMARY.md
autonomous: true
requirements: [REQ-63, REQ-40, REQ-50]
must_haves:
  truths:
    - "Call turns use a bounded sliding window of recent text and speech turns."
    - "Call start/end event rows remain visible in the thread but are not sent as LLM conversational messages."
  artifacts:
    - path: "web-ui/server/app/domain/prompt_builder.py"
      provides: "Call-mode prompt context assembly"
---

<objective>
Implement sliding-window prompt context for Phase 3 calls.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-first-working-call-mvp/03-CONTEXT.md
@web-ui/server/app/domain/prompt_builder.py
@web-ui/server/app/domain/thread_service.py
@web-ui/server/tests/test_prompt_builder.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add call prompt windowing helper</name>
  <read_first>
    web-ui/server/app/domain/prompt_builder.py
    web-ui/server/tests/test_prompt_builder.py
    web-ui/server/app/storage/models.py
  </read_first>
  <files>web-ui/server/app/domain/prompt_builder.py, web-ui/server/tests/test_prompt_builder.py</files>
  <action>Implement `build_call_prompt_context(thread_id: str, *, repository=None, max_turns: int = 24)` or an equivalent public call-mode function exported from `prompt_builder.py`. It must reuse the existing selected non-stale branch logic and system/persona prompt logic. Include only message roles `user` and `assistant` from message kinds `user_text`, `ai_text`, `user_speech`, and `ai_speech`. Exclude `call_start` and `call_end` from LLM messages. Apply the window after filtering stale/event rows so at most the most recent 24 conversational turns plus the optional system message are returned. Preserve chronological order in the returned list.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_prompt_builder.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `prompt_builder.py` exports `build_call_prompt_context` or documents the equivalent call-mode helper in `__all__`.
    - Tests assert `call_start` and `call_end` are excluded.
    - Tests assert a 30-turn fixture returns no more than 25 messages when a system prompt exists.
    - The verification command exits 0.
  </acceptance_criteria>
  <done>Call prompt context uses a tested sliding window.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Denial of Service | Prompt growth | mitigate | Default call context is capped at 24 conversational turns. |
| T-03-06-02 | Tampering | Edited/stale branches | mitigate | Existing selected non-stale branch logic is reused for call context. |
| T-03-06-03 | Information Disclosure | Event metadata | mitigate | `call_start` and `call_end` event rows are excluded from LLM prompt messages. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests/test_prompt_builder.py -q`.
</verification>

<success_criteria>
Every AI turn in a Phase 3 call can hydrate bounded recent text and speech context from the unified thread.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-working-call-mvp/03-06-SUMMARY.md`.
</output>
