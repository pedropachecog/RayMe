---
phase: 03-first-working-call-mvp
plan: "02"
type: execute
wave: 0
depends_on: []
files_modified:
  - web-ui/server/tests/test_calls.py
  - web-ui/server/tests/test_prompt_builder.py
  - .planning/phases/03-first-working-call-mvp/03-02-SUMMARY.md
autonomous: true
requirements: [REQ-40, REQ-50, REQ-63, REQ-A1]
must_haves:
  truths:
    - "Web UI server call bootstrap, voice preflight, prompt windowing, and durable writeback are specified before implementation."
    - "Phase 3 avoids schema churn unless a later plan explicitly proves it is required."
    - "Every active call has an unguessable server-owned `call_id` mapped to exactly one AI-backend `session_id` before controls can run."
  artifacts:
    - path: "web-ui/server/tests/test_calls.py"
      provides: "RED call bootstrap, writeback, and sanitized error contracts"
    - path: "web-ui/server/tests/test_prompt_builder.py"
      provides: "RED sliding-window call context contract"
---

<objective>
Add Web UI server Wave 0 tests for call bootstrap, durable call rows, voice/readiness failures, and sliding-window call prompt context.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-first-working-call-mvp/03-CONTEXT.md
@.planning/phases/03-first-working-call-mvp/03-VALIDATION.md
@.planning/phases/03-first-working-call-mvp/03-PATTERNS.md
@web-ui/server/app/domain/thread_service.py
@web-ui/server/app/domain/voice_service.py
@web-ui/server/app/domain/ai_backend_client.py
@web-ui/server/app/domain/prompt_builder.py
@web-ui/server/app/storage/models.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add RED call API and writeback tests</name>
  <read_first>
    web-ui/server/tests/test_threads.py
    web-ui/server/tests/test_voices.py
    web-ui/server/tests/test_chat_stream.py
    web-ui/server/app/api/threads.py
    web-ui/server/app/api/voices.py
    web-ui/server/app/domain/thread_service.py
    web-ui/server/app/storage/models.py
  </read_first>
  <files>web-ui/server/tests/test_calls.py</files>
  <action>Create `web-ui/server/tests/test_calls.py` with RED tests for the foundational same-origin call API. Cover exact routes: `POST /api/calls/start`, `POST /api/calls/{call_id}/offer`, `POST /api/calls/{call_id}/mute`, `POST /api/calls/{call_id}/interrupt`, and `POST /api/calls/{call_id}/end`. Do not add `/api/calls/{call_id}/turns` tests in this plan; Plan 03-09 owns turn orchestration tests. Test thread-header start with body `{\"thread_id\":\"...\"}` and character-card start with body `{\"character_id\":\"...\"}` that creates or selects a thread. Test start returns unguessable `call_id` and server-owned `session_id`, and that mute/interrupt/end reject mismatched or unknown call/session pairs with public code `call_session_not_found`. Test no assigned voice returns public code `call_voice_required` and message `Assign a voice before calling this character.` Test unavailable voice returns `call_voice_unavailable`. Test backend not ready returns `call_backend_not_ready`. Test unsafe call-control requests with a foreign `Origin` header are rejected with public code `call_origin_not_allowed`. Test start/end writeback creates chronological `call_start` and `call_end` message kinds using existing `messages.content_text` with no migration requirement.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `web-ui/server/tests/test_calls.py` contains the exact route strings `/api/calls/start`, `/offer`, `/mute`, `/interrupt`, and `/end`, and does not contain `/turns` until Plan 03-09.
    - The test file asserts exact public error codes `call_voice_required`, `call_voice_unavailable`, `call_backend_not_ready`, `call_session_not_found`, and `call_origin_not_allowed`.
    - The test file asserts `call_id` and `session_id` are returned by start and used as a server-owned mapping for controls.
    - The test file asserts `call_start` and `call_end` appear in chronological order; Plan 03-09 adds `user_speech` and `ai_speech` turn tests.
    - Running the test command fails because `app.api.calls` or call service implementation is missing, not because of syntax errors.
  </acceptance_criteria>
  <done>Server call API and writeback behavior is captured as RED tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add RED sliding-window call prompt tests</name>
  <read_first>
    web-ui/server/app/domain/prompt_builder.py
    web-ui/server/tests/test_prompt_builder.py
    web-ui/server/app/storage/models.py
    .planning/phases/03-first-working-call-mvp/03-CONTEXT.md
  </read_first>
  <files>web-ui/server/tests/test_prompt_builder.py</files>
  <action>Extend `test_prompt_builder.py` with tests for a future `build_call_prompt_context(thread_id, max_turns=24, repository=...)` helper or equivalent `mode=\"call\"` option. Test that call context includes recent `user_text`, `ai_text`, `user_speech`, and `ai_speech` rows; excludes `call_start` and `call_end` event rows from LLM messages; preserves selected non-stale branch behavior; and limits to the most recent 24 conversational turns while preserving the system/persona prompt at index 0.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_prompt_builder.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_prompt_builder.py` contains `build_call_prompt_context` or `mode=\"call\"`.
    - The added test asserts `call_start` and `call_end` are excluded from LLM messages.
    - The added test asserts a 30-turn fixture yields no more than 24 user/assistant turn messages plus the system message.
    - The command reaches implementation failures, not syntax failures.
  </acceptance_criteria>
  <done>Call memory sliding-window behavior is captured as RED tests.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Spoofing | Call bootstrap identifiers | mitigate | Tests require server-side lookup of thread/character/voice IDs instead of trusting display names. |
| T-03-02-02 | Tampering | Call event writeback | mitigate | Tests require known message kinds and chronological sequence allocation through server services. |
| T-03-02-03 | Information Disclosure | AI backend readiness errors | mitigate | Tests require fixed public error codes and messages, not raw backend exceptions. |
| T-03-02-04 | Denial of Service | Prompt context growth | mitigate | Tests require a 24-turn sliding window for call context. |
| T-03-02-05 | Cross-Site Request Forgery | Call control routes | mitigate | Tests require unsafe call-control routes to reject foreign `Origin` values and unknown call/session mappings. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py web-ui/server/tests/test_prompt_builder.py -q`. This plan is complete when the new tests are valid RED contracts.
</verification>

<success_criteria>
Server-side call ownership, durable writeback, failure semantics, and call memory are specified before implementation begins.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-working-call-mvp/03-02-SUMMARY.md`.
</output>
