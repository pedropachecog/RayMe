---
phase: 03-first-working-call-mvp
plan: "10"
type: execute
wave: 5
depends_on: ["03-04", "03-05", "03-06", "03-07", "03-08", "03-09"]
files_modified:
  - web-ui/client/tests/e2e/call-start.spec.ts
  - web-ui/client/tests/e2e/call-toolbar.spec.ts
  - web-ui/client/tests/e2e/call-permissions.spec.ts
  - web-ui/client/tests/e2e/call-visualizer.spec.ts
  - web-ui/client/tests/e2e/call-summary.spec.ts
  - web-ui/client/tests/e2e/call-mobile.spec.ts
  - web-ui/client/tests/e2e/live-call.spec.ts
  - .planning/phases/03-first-working-call-mvp/PLAYWRIGHT-EVIDENCE.md
  - .planning/phases/03-first-working-call-mvp/03-10-SUMMARY.md
autonomous: true
requirements: [REQ-40, REQ-47, REQ-48, REQ-49, REQ-50, REQ-63, REQ-A0]
must_haves:
  truths:
    - "All local backend, server, unit, desktop Playwright, and mobile-emulation Playwright call checks pass before live deployment."
    - "Browser evidence is saved under the Phase 3 planning directory."
    - "The non-mocked live-call spec is present and intentionally skipped unless `RAYME_ENABLE_LIVE_E2E=1`."
  artifacts:
    - path: ".planning/phases/03-first-working-call-mvp/PLAYWRIGHT-EVIDENCE.md"
      provides: "Local browser and mobile-emulation evidence"
---

<objective>
Run and harden full local Phase 3 automated acceptance, then record Playwright evidence.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-first-working-call-mvp/03-VALIDATION.md
@.planning/OPERATING-NOTES.md
@web-ui/client/playwright.config.ts
@.planning/phases/02-ai-backend-skeleton-voice-lab/PLAYWRIGHT-EVIDENCE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Run full local automated suites and fix regressions</name>
  <read_first>
    .planning/phases/03-first-working-call-mvp/03-VALIDATION.md
    web-ui/client/playwright.config.ts
    web-ui/server/tests/test_calls.py
    ai-backend/tests/test_call_session.py
  </read_first>
  <files>web-ui/client/tests/e2e/call-start.spec.ts, web-ui/client/tests/e2e/call-toolbar.spec.ts, web-ui/client/tests/e2e/call-permissions.spec.ts, web-ui/client/tests/e2e/call-visualizer.spec.ts, web-ui/client/tests/e2e/call-summary.spec.ts, web-ui/client/tests/e2e/call-mobile.spec.ts</files>
  <action>Run full local acceptance: `uv run --project ai-backend pytest ai-backend/tests -q`, `uv run --project web-ui/server pytest web-ui/server/tests -q`, `npm --prefix web-ui/client run test:unit -- --run`, `npm --prefix web-ui/client run test:e2e -- tests/e2e/call-start.spec.ts tests/e2e/call-toolbar.spec.ts tests/e2e/call-permissions.spec.ts tests/e2e/call-visualizer.spec.ts tests/e2e/call-summary.spec.ts --project=desktop-chromium`, and `npm --prefix web-ui/client run test:e2e -- tests/e2e/call-mobile.spec.ts --project=mobile-chromium`. Verify `web-ui/client/tests/e2e/live-call.spec.ts` exists, contains no `/api/calls/*` or `/webrtc/*` route mocks, and skips unless `RAYME_ENABLE_LIVE_E2E=1`. Fix failures in the smallest owning source/test files. Do not weaken assertions for Start Call, mic denial, server mute, visualizer RMS states, chronological two-turn call rows, or mobile non-overlap.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests -q && uv run --project web-ui/server pytest web-ui/server/tests -q && npm --prefix web-ui/client run test:unit -- --run && npm --prefix web-ui/client run test:e2e -- tests/e2e/call-start.spec.ts tests/e2e/call-toolbar.spec.ts tests/e2e/call-permissions.spec.ts tests/e2e/call-visualizer.spec.ts tests/e2e/call-summary.spec.ts --project=desktop-chromium && npm --prefix web-ui/client run test:e2e -- tests/e2e/call-mobile.spec.ts --project=mobile-chromium</automated>
  </verify>
  <acceptance_criteria>
    - All commands in the automated verify block exit 0.
    - The call specs still contain exact UI copy strings from `03-UI-SPEC.md`.
    - `live-call.spec.ts` exists, contains `RAYME_ENABLE_LIVE_E2E`, and contains no route mock for `/api/calls` or `/webrtc`.
    - No local mocked Phase 3 test file contains `.skip(`, `test.only`, or weakened `TODO` assertions for Phase 3 call requirements; `live-call.spec.ts` may contain exactly one env-gated skip controlled by `RAYME_ENABLE_LIVE_E2E`.
  </acceptance_criteria>
  <done>Local automated Phase 3 checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Record local Playwright and mobile-emulation evidence</name>
  <read_first>
    .planning/phases/02-ai-backend-skeleton-voice-lab/PLAYWRIGHT-EVIDENCE.md
    .planning/phases/03-first-working-call-mvp/03-VALIDATION.md
    .planning/OPERATING-NOTES.md
  </read_first>
  <files>.planning/phases/03-first-working-call-mvp/PLAYWRIGHT-EVIDENCE.md</files>
  <action>Create `.planning/phases/03-first-working-call-mvp/PLAYWRIGHT-EVIDENCE.md` using the Phase 2 evidence style. Record date/time, commit SHA, exact commands run, pass/fail result, desktop Chromium call specs, mobile Chromium call spec, local mocked-vs-live split, browser console/page-error guard status, and screenshots/traces path if Playwright produced them. Include a section `Known caveats` with `None` if no caveats remain. Record `live-call.spec.ts: present, gated, not run locally without RAYME_ENABLE_LIVE_E2E=1`.</action>
  <verify>
    <automated>test -f .planning/phases/03-first-working-call-mvp/PLAYWRIGHT-EVIDENCE.md && rg -n "commit SHA|desktop-chromium|mobile-chromium|call-start|call-mobile|Known caveats" .planning/phases/03-first-working-call-mvp/PLAYWRIGHT-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Evidence file contains `commit SHA`, `desktop-chromium`, `mobile-chromium`, `call-start`, `call-mobile`, `live-call.spec.ts`, and `Known caveats`.
    - Evidence file lists the exact local commands run.
  </acceptance_criteria>
  <done>Local Phase 3 browser evidence is saved.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10-01 | Repudiation | Local acceptance evidence | mitigate | Evidence records commit SHA, commands, browser projects, and pass/fail status. |
| T-03-10-02 | Information Disclosure | Evidence logs | mitigate | Evidence must not include API keys, TLS private key contents, or raw user mic audio. |
| T-03-10-03 | Tampering | Test weakening | mitigate | Acceptance forbids `.skip(`, `test.only`, and TODO-weakened Phase 3 assertions. |
</threat_model>

<verification>
Run the full automated verify block and evidence grep command.
</verification>

<success_criteria>
Phase 3 has passing local automated validation and saved browser/mobile evidence before live OMEN-PC deployment.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-working-call-mvp/03-10-SUMMARY.md`.
</output>
