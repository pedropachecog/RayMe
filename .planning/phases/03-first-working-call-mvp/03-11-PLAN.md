---
phase: 03-first-working-call-mvp
plan: "11"
type: execute
wave: 6
depends_on: ["03-10"]
files_modified:
  - .planning/phases/03-first-working-call-mvp/OMEN-PC-LIVE-EVIDENCE.md
  - .planning/phases/03-first-working-call-mvp/03-11-SUMMARY.md
autonomous: false
requirements: [REQ-40, REQ-47, REQ-48, REQ-49, REQ-50, REQ-63, REQ-A0, REQ-A1]
user_setup:
  - service: OMEN-PC
    why: "Live RTX 3060 AI backend, LAN HTTPS, real browser/media/runtime acceptance"
    env_vars:
      - RAYME_ENABLE_LIVE_E2E=1
      - RAYME_LIVE_WEB_URL=https://192.168.1.199:8443
      - RAYME_LIVE_AI_HEALTH_URL=https://192.168.1.199:9443/health
    dashboard_config:
      - task: "Use canonical checkout at C:\\Users\\pmpg\\rayme\\RayMe and existing TLS material from C:\\Users\\pmpg\\rayme\\phase1-tls\\"
        location: "OMEN-PC / 192.168.1.199"
must_haves:
  truths:
    - "The live call flow works against the OMEN-PC GPU backend before Android product-owner acceptance."
    - "Live evidence records health, model residency, VRAM/headroom, server-side mute frame-rate drop, and call row writeback."
  artifacts:
    - path: ".planning/phases/03-first-working-call-mvp/OMEN-PC-LIVE-EVIDENCE.md"
      provides: "Live LAN/GPU/browser call evidence"
---

<objective>
Deploy or sync the Phase 3 implementation to OMEN-PC, run live LAN call acceptance, and record live evidence.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/OPERATING-NOTES.md
@ai-backend/docs/RUNTIME-EVIDENCE.md
@ai-backend/docs/STT-GPU-RUNTIME.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/OMEN-PC-LIVE-EVIDENCE.md
@.planning/phases/03-first-working-call-mvp/PLAYWRIGHT-EVIDENCE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Sync OMEN-PC and run live health/browser call checks</name>
  <read_first>
    .planning/OPERATING-NOTES.md
    ai-backend/docs/RUNTIME-EVIDENCE.md
    ai-backend/docs/STT-GPU-RUNTIME.md
    .planning/phases/03-first-working-call-mvp/PLAYWRIGHT-EVIDENCE.md
  </read_first>
  <files>.planning/phases/03-first-working-call-mvp/OMEN-PC-LIVE-EVIDENCE.md</files>
  <action>Using the established `ssh rayme-pmpg` path and canonical OMEN-PC checkout `C:\\Users\\pmpg\\rayme\\RayMe`, sync or pull the current commit. Reuse TLS material under `C:\\Users\\pmpg\\rayme\\phase1-tls\\`; do not generate throwaway certs. Start Web UI at `https://192.168.1.199:8443` and AI backend at `https://192.168.1.199:9443`. Verify AI backend `/health` reports CUDA STT, resident TTS engine, available engine metadata, `vram_used_mb`, `vram_headroom_mb`, and no CPU fallback. Verify browser `window.isSecureContext === true` against the live Web UI URL. Run non-mocked live desktop Playwright with `RAYME_ENABLE_LIVE_E2E=1 RAYME_LIVE_WEB_URL=https://192.168.1.199:8443 RAYME_LIVE_AI_HEALTH_URL=https://192.168.1.199:9443/health npm --prefix web-ui/client run test:e2e -- tests/e2e/live-call.spec.ts --project=desktop-chromium`. Do not use the mocked local `call-start`, `call-toolbar`, or `call-summary` specs as live evidence unless they explicitly bypass all route mocks under `RAYME_ENABLE_LIVE_E2E=1`.</action>
  <verify>
    <automated>RAYME_ENABLE_LIVE_E2E=1 RAYME_LIVE_WEB_URL=https://192.168.1.199:8443 RAYME_LIVE_AI_HEALTH_URL=https://192.168.1.199:9443/health npm --prefix web-ui/client run test:e2e -- tests/e2e/live-call.spec.ts --project=desktop-chromium</automated>
  </verify>
  <acceptance_criteria>
    - Live health records `stt_model`, `stt_compute_type`, `vad_ready`, `resident_tts_engine`, `vram_used_mb`, and `vram_headroom_mb`.
    - Non-mocked `live-call.spec.ts` exits 0 with browser console/page-error guard passing.
    - Evidence records `window.isSecureContext === true`.
    - Evidence records the exact commit SHA deployed on OMEN-PC.
  </acceptance_criteria>
  <done>Live OMEN-PC desktop call acceptance passes.</done>
</task>

<task type="auto">
  <name>Task 2: Record server-side mute and 5-minute desktop stability evidence</name>
  <read_first>
    .planning/OPERATING-NOTES.md
    .planning/phases/03-first-working-call-mvp/03-CONTEXT.md
    .planning/phases/03-first-working-call-mvp/03-VALIDATION.md
  </read_first>
  <files>.planning/phases/03-first-working-call-mvp/OMEN-PC-LIVE-EVIDENCE.md</files>
  <action>Run one live desktop call long enough to exercise Start Call, two finalized user turns produced from real mic audio, two streamed AI text responses, two AI speech playbacks, Mute, Unmute, Interrupt if AI is active, and End Call. Inspect AI backend logs or session stats before/during mute and record incoming frame-rate or `incoming_audio_frames`/`dropped_audio_frames` evidence that server-side audio consumption stops while muted. Run a 5-minute desktop speaker stability check with at least two user→AI cycles and record whether there are uncaught exceptions, catastrophic ping-pong, or runaway loopback. Write all results to `OMEN-PC-LIVE-EVIDENCE.md` using the Phase 2 evidence style.</action>
  <verify>
    <automated>test -f .planning/phases/03-first-working-call-mvp/OMEN-PC-LIVE-EVIDENCE.md && rg -n "commit SHA|/health|resident_tts_engine|vram_used_mb|window.isSecureContext|two user turns|two ai_speech|server-side mute|incoming_audio_frames|5-minute|Android" .planning/phases/03-first-working-call-mvp/OMEN-PC-LIVE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Evidence file contains `two user turns`, `two ai_speech`, `server-side mute`, `incoming_audio_frames` or `dropped_audio_frames`, and `5-minute`.
    - Evidence file records `no uncaught exceptions`, exact exceptions, or exact caveats from the stability check.
    - Evidence file includes an `Android Chrome product-owner acceptance: pending` line for the next plan.
  </acceptance_criteria>
  <done>Live mute and desktop stability evidence is recorded.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11-01 | Spoofing | Live target services | mitigate | Use exact LAN HTTPS URLs and record deployed commit SHA plus `/health`. |
| T-03-11-02 | Information Disclosure | Evidence | mitigate | Do not record API keys, TLS private key contents, raw local paths beyond approved project paths, or raw user mic audio. |
| T-03-11-03 | Denial of Service | GPU runtime | mitigate | Evidence records no CPU fallback, resident engine, and VRAM/headroom before Android handoff. |
| T-03-11-04 | Repudiation | Mute behavior | mitigate | Evidence records server-side frame/stat changes during mute. |
</threat_model>

<verification>
Non-mocked `live-call.spec.ts` passes and evidence grep finds required live evidence fields.
</verification>

<success_criteria>
Live OMEN-PC call acceptance passes with saved GPU/runtime/browser/mute/stability evidence before physical Android acceptance.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-working-call-mvp/03-11-SUMMARY.md`.
</output>
