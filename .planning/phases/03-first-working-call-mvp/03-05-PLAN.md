---
phase: 03-first-working-call-mvp
plan: "05"
type: execute
wave: 1
depends_on: ["03-02"]
files_modified:
  - web-ui/server/app/api/calls.py
  - web-ui/server/app/domain/call_service.py
  - web-ui/server/app/domain/ai_backend_client.py
  - web-ui/server/app/main.py
  - web-ui/server/tests/test_calls.py
  - .planning/phases/03-first-working-call-mvp/03-05-SUMMARY.md
autonomous: true
requirements: [REQ-40, REQ-47, REQ-50, REQ-A1]
must_haves:
  truths:
    - "The browser starts calls through same-origin Web UI server routes, not directly against backend internals."
    - "Call rows are persisted through the server into the existing unified messages chronology."
    - "The Web UI server owns the active `call_id` to AI-backend `session_id` mapping."
  artifacts:
    - path: "web-ui/server/app/api/calls.py"
      provides: "Same-origin call facade"
    - path: "web-ui/server/app/domain/call_service.py"
      provides: "Thread, voice, readiness, and durable writeback ownership"
  key_links:
    - from: "POST /api/calls/start"
      to: "POST /webrtc/offer"
      via: "CallService active call registry"
      pattern: "server-owned call_id maps to session_id"
    - from: "POST /api/calls/{call_id}/mute"
      to: "POST /webrtc/sessions/{session_id}/mute"
      via: "stored call_id/session_id mapping"
      pattern: "server-side mute enforcement"
---

<objective>
Implement the Web UI server call facade and durable call service.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-first-working-call-mvp/03-CONTEXT.md
@.planning/phases/03-first-working-call-mvp/03-PATTERNS.md
@web-ui/server/app/api/chat.py
@web-ui/server/app/api/threads.py
@web-ui/server/app/api/voices.py
@web-ui/server/app/domain/thread_service.py
@web-ui/server/app/domain/voice_service.py
@web-ui/server/app/domain/ai_backend_client.py
@web-ui/server/app/storage/models.py
@web-ui/server/tests/test_calls.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add CallService for preflight and writeback</name>
  <read_first>
    web-ui/server/app/domain/thread_service.py
    web-ui/server/app/domain/voice_service.py
    web-ui/server/app/domain/settings_service.py
    web-ui/server/app/storage/models.py
    web-ui/server/tests/test_calls.py
  </read_first>
  <files>web-ui/server/app/domain/call_service.py</files>
  <action>Create `CallService` with methods `start_call(thread_id=None, character_id=None)`, `attach_session(call_id, session_id)`, `session_for_call(call_id)`, `record_event(call_id, event)`, `set_muted(call_id, muted)`, `interrupt(call_id)`, and `end_call(call_id, reason)`. `start_call` must accept either a thread ID or character ID; if only `character_id` is supplied, call `ThreadService.create_thread()` immediately and return the new `thread_id`. Generate unguessable IDs with prefixes `call_` and `rtc_`; the server owns both and stores the in-memory active-call record `{call_id, session_id, thread_id, voice_id, engine_id, started_at, ended_at}`. Validate the character has a non-deleted default voice and return public failures `call_voice_required` or `call_voice_unavailable` when not. Write `call_start` as a `Message` with role `event` and `content_text` containing `Call started with {character_name} using {voice_name}`. `record_event` must write finalized `user_speech` and `ai_speech` rows with roles `user` and `assistant`. `end_call` must write `call_end` role `event` with content including `Call ended`, `Duration:`, `Voice:`, `Started:`, and `Ended:`. Allocate message sequence numbers through the same pattern as thread/chat services and update `Thread.last_message_at`.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `web-ui/server/app/domain/call_service.py` contains `class CallService`.
    - `call_service.py` contains `attach_session`, `session_for_call`, `call_`, and `rtc_`.
    - The file contains exact public codes `call_voice_required` and `call_voice_unavailable`.
    - The file writes exact message kinds `call_start`, `user_speech`, `ai_speech`, and `call_end`.
    - `uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py -q` passes its service-level tests.
  </acceptance_criteria>
  <done>Server call preflight and durable writeback service exists.</done>
</task>

<task type="auto">
  <name>Task 2: Add same-origin call API and AI backend proxy methods</name>
  <read_first>
    web-ui/server/app/api/chat.py
    web-ui/server/app/api/ai_backend.py
    web-ui/server/app/domain/ai_backend_client.py
    web-ui/server/app/domain/call_service.py
    web-ui/server/app/main.py
    web-ui/server/tests/test_calls.py
  </read_first>
  <files>web-ui/server/app/api/calls.py, web-ui/server/app/domain/ai_backend_client.py, web-ui/server/app/main.py</files>
  <action>Add router `app.api.calls` with prefix `/api/calls` and include it in `main.py`. Implement exact routes `POST /api/calls/start`, `POST /api/calls/{call_id}/offer`, `POST /api/calls/{call_id}/mute`, `POST /api/calls/{call_id}/interrupt`, and `POST /api/calls/{call_id}/end`; reserve `POST /api/calls/{call_id}/turns` for Plan 03-09 turn orchestration so user speech writeback has a single path. Add a shared unsafe-method origin guard for these call routes: allow missing Origin for non-browser tests and allow same-origin values matching configured host, but reject foreign origins with public code `call_origin_not_allowed`. Add `AiBackendClient` methods `get_webrtc_status()`, `create_webrtc_offer(payload)`, `mute_call(session_id, muted)`, `interrupt_call(session_id)`, and `end_call(session_id, reason)`. `POST /api/calls/start` must check `/webrtc/status`, create the server-owned `call_id`/`session_id`, and map not-ready states to code `call_backend_not_ready` and copy `RayMe voice backend is not ready. Check Settings, then try again.` `POST /api/calls/{call_id}/offer` must look up the stored `session_id`, proxy the browser SDP offer to the AI backend with that `session_id`, attach the returned session, and return `answer` plus `event_channel: \"rayme-events\"`. Control routes must look up `session_id` from `call_id`, proxy to AI backend, reject unknown mappings with `call_session_not_found`, and then update server call state where applicable.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py web-ui/server/tests/test_health_settings.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `web-ui/server/app/api/calls.py` contains all exact route decorators named in the action.
    - `web-ui/server/app/domain/ai_backend_client.py` contains `get_webrtc_status`, `create_webrtc_offer`, `mute_call`, `interrupt_call`, and `end_call`.
    - `web-ui/server/app/api/calls.py` contains `call_origin_not_allowed`, `call_session_not_found`, and `rayme-events`.
    - `web-ui/server/app/main.py` includes the calls router.
    - The verification command exits 0.
  </acceptance_criteria>
  <done>Browser-visible call API and backend proxy routes pass server tests.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Spoofing | Thread/character/voice IDs | mitigate | `CallService` resolves IDs from storage and rejects missing/deleted voices instead of trusting client-provided display data. |
| T-03-05-02 | Tampering | Durable call rows | mitigate | Server owns sequence allocation and allowed message kinds. |
| T-03-05-03 | Information Disclosure | AI backend failures | mitigate | `AiBackendClient` maps failures to fixed public codes and UI copy. |
| T-03-05-04 | Denial of Service | Backend not ready | mitigate | Start route blocks before creating half-connected active call UI state. |
| T-03-05-05 | Cross-Site Request Forgery | Call control routes | mitigate | Unsafe call routes reject foreign Origin values and controls can only use server-owned call/session mappings. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py web-ui/server/tests/test_prompt_builder.py web-ui/server/tests/test_health_settings.py -q`.
</verification>

<success_criteria>
The Web UI server owns call start, voice/readiness validation, AI backend proxying, and chronological call writeback.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-working-call-mvp/03-05-SUMMARY.md`.
</output>
