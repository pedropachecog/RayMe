---
phase: 09-integrate-faster-qwen3-tts-1-7b-into-live-calls
plan: 06
type: execute
wave: 6
depends_on: [09-05, 09-07]
files_modified:
  - ai-backend/app/models/tts_qwen3.py
  - ai-backend/app/models/model_manager.py
  - ai-backend/app/api/tts.py
  - ai-backend/tests/test_tts_qwen3.py
  - ai-backend/tests/test_model_manager.py
  - web-ui/server/app/domain/voice_service.py
  - web-ui/server/app/domain/ai_backend_client.py
  - web-ui/server/app/api/voices.py
  - web-ui/server/tests/test_voices.py
autonomous: true
requirements: [REQ-22]
estimate: {tokens: 44000, raw_tokens: 44000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-10: Deleting a saved Qwen voice invalidates matching AI-backend/worker prompt tensors and readiness by opaque key."
    - "D-17/D-19: Deleting an active prompt cancels its request safely; unrelated voices and services remain usable."
  artifacts:
    - {path: "web-ui/server/app/domain/voice_service.py", provides: "Delete-to-backend invalidation trigger"}
    - {path: "ai-backend/app/models/tts_qwen3.py", provides: "Opaque-key prompt eviction and active-request cancellation"}
  key_links:
    - {from: "web-ui/server/app/domain/voice_service.py", to: "ai-backend/app/api/tts.py", via: "internal invalidate request using opaque voice key", pattern: "invalidate"}
---

<objective>Close the saved-voice deletion lifecycle so private prompt state cannot outlive its owner.</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-05-SUMMARY.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-07-SUMMARY.md
@web-ui/server/app/domain/voice_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Evict matching worker prompt state by opaque voice key</name>
  <files>ai-backend/app/models/tts_qwen3.py, ai-backend/app/models/model_manager.py, ai-backend/app/api/tts.py, ai-backend/tests/test_tts_qwen3.py, ai-backend/tests/test_model_manager.py</files>
  <behavior>
    - Define `voice_key = sha256("rayme:qwen3_1_7b:" + saved_voice_id)` as an opaque owner key distinct from private content/cache hash; carry it through prepare/stream/invalidate.
    - Internal invalidate accepts only bounded opaque key, evicts matching worker prompt tensors/cache identity, resets prompt state to none, and reports idempotent matched/not-present result.
    - If matching voice is active, mark cancelled first, stop future output, request cancel/terminal drain, then invalidate. Deleting a non-owner key does not evict the current selected prompt.
    - Tests inspect fake worker state to prove no prompt tensor/cache object survives matching invalidation, while unrelated prepared voice remains ready and usable.
  </behavior>
  <action>Add the internal RayMe invalidate contract at the existing TTS API boundary and manager/adapter ownership seam. Do not expose content hash, tensors, transcript, or path. Test current-owner delete, unrelated-key delete, repeated delete, active generation, unload, and later prewarm/synthesis.</action>
  <verify><automated>uv run --project ai-backend pytest ai-backend/tests/test_tts_qwen3.py ai-backend/tests/test_model_manager.py -q -k 'invalidate or delete or cache'</automated></verify>
  <done>Matching prompt tensors and cache identity are gone after invalidate, and an unrelated voice still synthesizes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire saved-voice soft delete to prompt invalidation</name>
  <files>web-ui/server/app/domain/voice_service.py, web-ui/server/app/domain/ai_backend_client.py, web-ui/server/app/api/voices.py, web-ui/server/tests/test_voices.py</files>
  <behavior>
    - On confirmed Qwen soft delete, compute/send only the deterministic opaque voice key before returning deletion success; force/non-force referent rules remain unchanged.
    - Backend unavailable/invalidation failure returns a truthful sanitized retryable failure and does not falsely claim prompt removal; repeated delete/invalidate is safe.
    - Non-Qwen deletion does not invoke the Qwen invalidation path; unrelated Qwen voice preview/test-play remains usable.
  </behavior>
  <action>Inject the existing `AiBackendClient` into deletion flow, add its typed invalidate method, and preserve current referent/tombstone behavior. Add server tests with two Qwen voices proving matching eviction, unrelated usability, active-delete cancellation result, idempotency, non-Qwen isolation, and sanitized failure.</action>
  <verify><automated>uv run --project web-ui/server pytest web-ui/server/tests/test_voices.py -q -k 'delete or invalidate or qwen'</automated></verify>
  <done>Durable voice deletion and ephemeral GPU prompt deletion are one tested lifecycle.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Spoofing | Invalidate key | high | mitigate | Saved-voice-derived bounded opaque key and exact owner match. |
| T-09-07 | Information Disclosure | Prompt lifecycle | high | mitigate | No tensors/content/transcript/path crosses API; matching cache is evicted. |
| T-09-08 | DoS | Active delete | high | mitigate | Cancel-first, terminal drain, idempotent invalidate, unrelated voice recovery. |
</threat_model>

<verification>Run both focused commands and full voice/model-manager tests.</verification>
<success_criteria>A deleted saved voice leaves no reusable Qwen prompt state and does not break another voice.</success_criteria>
<output>Create `09-06-SUMMARY.md`.</output>
