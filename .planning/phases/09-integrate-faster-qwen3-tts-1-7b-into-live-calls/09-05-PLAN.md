---
phase: 09-integrate-faster-qwen3-tts-1-7b-into-live-calls
plan: 05
type: execute
wave: 5
depends_on: [09-04]
files_modified:
  - ai-backend/app/models/tts_qwen3_protocol.py
  - ai-backend/app/models/tts_qwen3_worker.py
  - ai-backend/app/models/tts_qwen3.py
  - ai-backend/app/models/model_manager.py
  - ai-backend/app/api/tts.py
  - ai-backend/app/api/webrtc.py
  - ai-backend/tests/test_tts_qwen3.py
  - ai-backend/tests/test_model_manager.py
  - ai-backend/tests/test_webrtc_signaling.py
autonomous: true
requirements: [REQ-22, REQ-45]
estimate: {tokens: 56000, raw_tokens: 56000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-05/D-06/D-07: Full ICL receives exact approved transcript; blank/gross mismatch stop before prompt extraction/generation while normal punctuation/case/accent variants pass."
    - "D-08: Text-relative token/audio/non-EOS ceilings fail the request and discard partial output."
    - "D-10/D-19: Capacity-one prompt state and malformed/crashed/stuck worker failures remain bounded, sanitized, and Qwen-scoped."
  artifacts:
    - {path: "ai-backend/app/models/tts_qwen3.py", provides: "Alignment, cache identity, ceilings, typed failures"}
    - {path: "ai-backend/app/api/tts.py", provides: "Final preview/synthesis trust boundary"}
  key_links:
    - from: "ai-backend/app/api/tts.py"
      to: "ai-backend/app/models/model_manager.py"
      via: "typed preview validation and sanitized mapping shared with app/api/webrtc.py for exact transcript/reference bytes and opaque request/voice ids"
      pattern: "qwen_transcript|prewarm|qwen_generation_ceiling"
    - from: "ai-backend/app/models/model_manager.py"
      to: "ai-backend/app/models/tts_qwen3.py"
      via: "capacity-one prompt cache, validated worker protocol, cancellation, and unload/reload containment into tts_qwen3_worker.py"
      pattern: "voice_key|cancel|invalidate"
    - from: "ai-backend/app/models/tts_qwen3.py"
      to: "ai-backend/app/models/tts_qwen3_worker.py"
      via: "pre-generation dual-threshold alignment and text-relative max token/audio bounds before native stream"
      pattern: "0.45|0.50|384"
---

<objective>Harden the real tracer against the known mismatch runaway, malformed output, cache drift, and runtime failure.</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-04-SUMMARY.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-AI-SPEC.md
@.planning/spikes/004-a-faster-qwen3-tts-06b-cuda/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce prompt identity, output ceilings, and protocol containment</name>
  <files>ai-backend/app/models/tts_qwen3_protocol.py, ai-backend/app/models/tts_qwen3_worker.py, ai-backend/app/models/tts_qwen3.py, ai-backend/tests/test_tts_qwen3.py</files>
  <behavior>
    - D-05/D-10: cache key covers reference-byte SHA-256, normalized comparison transcript, model revision, full-ICL mode, and append-silence policy; exact transcript still enters ICL unchanged.
    - D-08: reject target segments over 60 words and apply expected-seconds, 6–32 s hard audio, four-step rounding, and max 384 token formula; ceiling or non-natural EOS drains partial audio and yields failed terminal.
    - Validate wrong request, indexes/times, strict bytes/rate/finite/silence/cumulative limits, one terminal, one active generation, and two-second cancel acknowledgement.
    - Crash/hang/protocol failure terminates only the worker and allows clean reload; prompt state is not serialized or retained after unload/crash.
  </behavior>
  <action>Extend the fake process mutation matrix first. Implement bounded cache/ceiling/protocol state exactly from AI-SPEC, including a scripted non-ending stream and known bad-prompt behavior. Distinguish validation, prompt, ceiling/cancel, and runtime/protocol exceptions; only runtime/protocol identity failures mark Qwen unavailable.</action>
  <verify><automated>uv run --project ai-backend pytest ai-backend/tests/test_tts_qwen3.py -q</automated></verify>
  <done>Malformed input/output or runaway generation cannot become a successful or unbounded speech turn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply STT alignment and sanitized typed failures at backend boundaries</name>
  <files>ai-backend/app/models/model_manager.py, ai-backend/app/api/tts.py, ai-backend/app/api/webrtc.py, ai-backend/tests/test_model_manager.py, ai-backend/tests/test_webrtc_signaling.py</files>
  <behavior>
    - D-06/D-07: missing bytes, blank transcript, empty target, or both token coverage below 0.45 and normalized edit similarity below 0.50 block before prompt/generation; punctuation/case and accepted accented-English cases pass.
    - Reuse RayMe STT; cache scalar alignment result by immutable prompt key and never replace the approved exact transcript with STT output.
    - D-09/D-11/D-19: `/tts/synthesize`, prepare/status, and `/webrtc` return stable actionable codes; correctable voice errors keep model usable, prompt failure scopes to one key, and runtime failure scopes to Qwen.
    - Public/log data contains opaque ids/hashes/scores only, with no traceback, full transcript, audio, token, or private/model/cache path.
  </behavior>
  <action>Add typed adapter/manager errors and translate them at both APIs. Keep the AI backend as final trust boundary even though the Web server later provides earlier UX validation. Prove a bad voice leaves a valid Qwen voice, STT, WebRTC, backend health, and another TTS engine usable per D-19.</action>
  <verify><automated>uv run --project ai-backend pytest ai-backend/tests/test_tts_qwen3.py ai-backend/tests/test_model_manager.py ai-backend/tests/test_webrtc_signaling.py -q</automated></verify>
  <done>Every backend path blocks invalid clone context early and reports it without poisoning unrelated service state.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-04 | Tampering/DoS | Transcript/runaway | critical | mitigate | STT dual-threshold preflight and text-relative ceilings. |
| T-09-05 | DoS | Worker failure | high | mitigate | Supervision, bounded cancel/terminal, restart recovery. |
| T-09-07 | Information Disclosure | Errors/status | high | mitigate | Stable codes, opaque hashes/scalars, leak tests. |
</threat_model>

<verification>Run both task commands, VoxCPM2 focused regressions, and `git diff --check`.</verification>
<success_criteria>The deployed runtime cannot repeat the 81.92-second mismatch failure or leak/poison state on bad inputs.</success_criteria>
<output>Create `09-05-SUMMARY.md`.</output>
