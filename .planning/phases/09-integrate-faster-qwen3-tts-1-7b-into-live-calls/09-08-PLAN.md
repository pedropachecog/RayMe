---
phase: 09-integrate-faster-qwen3-tts-1-7b-into-live-calls
plan: 08
type: execute
wave: 6
depends_on: [09-07]
files_modified:
  - web-ui/client/src/lib/api/types.ts
  - web-ui/client/src/lib/components/EndpointSettingsPanel.svelte
  - web-ui/client/src/lib/components/voice/TtsEnginePicker.svelte
  - web-ui/client/src/lib/components/voice/VoiceAssignmentSelect.svelte
  - web-ui/client/src/lib/components/voice/VoiceLibraryRow.svelte
  - web-ui/client/src/lib/components/voice/VoiceLibraryList.svelte
  - web-ui/client/src/routes/voice-lab/+page.svelte
  - web-ui/client/src/routes/call/[threadId]/+page.svelte
  - web-ui/client/tests/unit/voice-lab.test.ts
  - web-ui/client/tests/unit/settings.test.ts
autonomous: true
requirements: [REQ-22, REQ-45, REQ-46]
estimate: {tokens: 56000, raw_tokens: 56000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-02/D-04: Existing RayMe controls render canonical 1.7B identity from server metadata without upstream internals."
    - "D-06/D-09/D-10/D-11: Fast component tests prove canonical Settings labels, distinct load/prewarm and row-scoped prepare/synthesis states, the three-field authorization block, retry/unrelated-row responsiveness, and no premature Listening."
    - "D-19: Fixed actionable failures preserve form state and render no private/raw details."
  artifacts:
    - {path: "web-ui/client/tests/unit/voice-lab.test.ts", provides: "Fast readiness/validation component contract"}
    - {path: "web-ui/client/src/routes/call/[threadId]/+page.svelte", provides: "Honest selected-voice preparation gate"}
    - {path: "web-ui/client/src/lib/components/voice/VoiceLibraryRow.svelte", provides: "Row-scoped preparing/testing/retry/status/error behavior"}
    - {path: "web-ui/client/src/lib/components/EndpointSettingsPanel.svelte", provides: "Canonical model resident/loading labels"}
  key_links:
    - from: "web-ui/client/src/lib/api/types.ts"
      to: "web-ui/client/src/lib/components/EndpointSettingsPanel.svelte"
      via: "AiBackendSettingsStatus/TtsEngineMetadata server id-to-label normalization for resident/loading/available engines, also consumed by TtsEnginePicker.svelte"
      pattern: "resident_tts_engine|loading_engine|available_engines"
    - from: "web-ui/client/src/routes/voice-lab/+page.svelte"
      to: "web-ui/client/src/routes/call/[threadId]/+page.svelte"
      via: "previewVoice/saveVoice authorization values and shared model/prompt readiness gate"
      pattern: "prewarming|ready|Preparing voice"
    - from: "web-ui/client/src/lib/components/voice/VoiceLibraryList.svelte"
      to: "web-ui/client/src/lib/components/voice/VoiceLibraryRow.svelte"
      via: "voice-id-scoped model/prompt/synthesis state and retry callback"
      pattern: "Preparing voice|Testing voice|Retry Preparation"
---

<objective>Make Qwen identity and readiness visible in existing Voice Lab and call UI with fast component feedback.</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-UI-SPEC.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-07-SUMMARY.md
@docs/stitch/DESIGN.md
@web-ui/client/src/routes/voice-lab/+page.svelte
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type canonical identity and render canonical Settings readiness labels</name>
  <files>web-ui/client/src/lib/api/types.ts, web-ui/client/src/lib/components/EndpointSettingsPanel.svelte, web-ui/client/src/lib/components/voice/TtsEnginePicker.svelte, web-ui/client/src/lib/components/voice/VoiceAssignmentSelect.svelte, web-ui/client/tests/unit/settings.test.ts</files>
  <behavior>
    - D-02: client union/fallback/labels use qwen3_1_7b; server-returned canonical engines not in fallback remain visible rather than being filtered out.
    - Type model loading/resident separately from selected-voice prompt prewarming/ready/failed, timestamps, and stable error codes; expose no model path/provider server controls.
    - `EndpointSettingsPanel.svelte` maps `resident_tts_engine`, `loading_engine`, and available engine ids through server metadata and renders canonical `Qwen3-TTS 1.7B-Base` labels; raw `qwen3_1_7b` is only a last-resort unknown-label fallback.
    - Canonical model copy is exactly `Loading Qwen3-TTS 1.7B…`, `Qwen3-TTS 1.7B loaded`, and `Qwen3-TTS 1.7B unavailable` with progress in `role=status`, fixed failures in `role=alert`, and no prompt/cache dashboard in Settings.
    - Assignment and Settings controls preserve canonical identity while load state does not disable unrelated library/engine controls; text, not color alone, communicates state.
  </behavior>
  <action>Write focused Vitest assertions first, then update types, metadata cards, assignment labels, fallback normalization, and `EndpointSettingsPanel.svelte` label/state derivation using existing design tokens/components. Preserve non-Qwen payloads, the compact Settings residency grid, fixed `role=status`/`role=alert` semantics, visible focus rings, and reduced-motion behavior.</action>
  <verify><automated>npm --prefix web-ui/client exec vitest -- run tests/unit/settings.test.ts</automated></verify>
  <done>Every engine-selection surface and all three Settings engine fields carry canonical 1.7B labels and honest loading/resident/unavailable semantics without exposing backend internals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate call Listening on selected Qwen prompt readiness</name>
  <files>web-ui/client/src/routes/call/[threadId]/+page.svelte, web-ui/client/src/routes/voice-lab/+page.svelte, web-ui/client/tests/unit/voice-lab.test.ts</files>
  <behavior>
    - D-09/D-10/D-11: Voice Lab renders Loading/Resident and Prewarming/Ready/Failed separately, remains responsive during delay, and retries without form loss.
    - D-06/D-07: the conditional `Reference authorization` block exposes exactly three required 44 px controls labeled `Reference source`, `Authorization basis`, and `Use scope`; values map to `voice_data_steward`, `authorization_basis`, and `use_scope`, begin unconfirmed, persist across failures/engine switches, and Qwen actions require them plus sample and editable nonblank transcript without altering transcript.
    - Selected Qwen call remains Connecting/Preparing until authoritative resident plus prompt-ready result, then preserves normal call FSM/toolbar/visualizer behavior.
    - D-19: transcript-required, mismatch, prewarm, ceiling, worker/runtime failures map to fixed actionable copy and never render raw backend/private material.
    - Fixed `role=status`/`role=alert` semantics announce only state changes; validation focuses the first invalid Qwen field, preparation failure focuses its heading/action, the initiating button retains focus during inline work, motion is static under reduced-motion, and existing mobile 44 px controls/unrelated-engine behavior remain intact.
  </behavior>
  <action>Add a focused component/readiness state-machine test for form validation, delayed prepare, failure/retry, call gating, and fixed error copy before updating route state derivation. Preserve preview-optional save behavior. Do not create a new dashboard or engine-specific browser endpoint.</action>
  <verify><automated>npm --prefix web-ui/client exec vitest -- run tests/unit/voice-lab.test.ts</automated></verify>
  <done>The visible call never claims Listening while Qwen is still loading or prewarming.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keep saved-voice preparation and synthesis row-scoped</name>
  <files>web-ui/client/src/lib/components/voice/VoiceLibraryRow.svelte, web-ui/client/src/lib/components/voice/VoiceLibraryList.svelte, web-ui/client/tests/unit/voice-lab.test.ts</files>
  <behavior>
    - `VoiceLibraryList.svelte` carries model/prompt/operation state and stable error/retry callbacks keyed by `voice_id`; changing one row never disables Test/Rename/Delete or readiness updates on another row.
    - `VoiceLibraryRow.svelte` renders separate model and selected-voice rows with the canonical loaded/loading/unavailable and `Voice not prepared|Preparing saved voice…|Saved voice ready|Voice preparation failed` copy.
    - Test-play button progression is exactly `Test Voice` → `Preparing voice…` during load/prewarm → `Testing voice…` during synthesis; retry uses `Retry Preparation`, stays inside the affected row, and does not create duplicate spinners/toasts.
    - Progress/success uses one polite `role=status` region per row; operation failure uses inline `role=alert`; unchanged polling is not re-announced. Keep focus on the initiating/retry button, preserve Rename/Delete on the affected row when safe, and preserve all controls on unrelated rows.
    - Controls remain at least 44 px, copy wraps at 320 px without horizontal page scroll, text conveys state without color, focus-visible rings remain, and `RefreshCw` becomes static under `prefers-reduced-motion: reduce`.
  </behavior>
  <action>Write focused source/component contracts first, then pass voice-id-scoped readiness/operation state from `VoiceLibraryList.svelte` into `VoiceLibraryRow.svelte`. Split preparation from synthesis state instead of overloading today's `testing` boolean. Reuse existing row layout/tokens and do not replace the whole library with Qwen progress or alter rename/delete/referent semantics.</action>
  <verify><automated>npm --prefix web-ui/client exec vitest -- run tests/unit/voice-lab.test.ts</automated></verify>
  <done>One saved Qwen row truthfully distinguishes preparation from testing, offers an accessible retry, and leaves every unrelated row responsive on desktop and mobile.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-02 | Elevation | Browser voice selection | high | mitigate | Opaque saved id; server authorizes asset. |
| T-09-07 | Information Disclosure | Rendered state/errors | high | mitigate | Typed fixed fields/copy only. |
| T-09-08 | DoS | Readiness UI | medium | mitigate | Bounded polling/retry and row-scoped progress. |
</threat_model>

<verification>Each task uses the focused fast component command; run full client unit/check at wave completion.</verification>
<success_criteria>The user sees truthful 1.7B load/prewarm/error state and never enters a hidden cold call.</success_criteria>
<output>Create `09-08-SUMMARY.md`.</output>
