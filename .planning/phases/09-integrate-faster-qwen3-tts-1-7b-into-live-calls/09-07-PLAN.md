---
phase: 09-integrate-faster-qwen3-tts-1-7b-into-live-calls
plan: 07
type: execute
wave: 5
depends_on: [09-04]
files_modified:
  - web-ui/server/alembic/versions/0003_qwen3_engine_identity.py
  - web-ui/server/app/domain/settings_service.py
  - web-ui/server/app/domain/voice_service.py
  - web-ui/server/app/api/voices.py
  - web-ui/server/app/domain/call_service.py
  - web-ui/server/app/domain/ai_backend_client.py
  - web-ui/server/tests/test_migrations.py
  - web-ui/server/tests/test_voices.py
  - web-ui/server/tests/test_calls.py
autonomous: true
requirements: [REQ-22, REQ-45]
estimate: {tokens: 60000, raw_tokens: 60000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-02: Exact persisted old Qwen ids translate idempotently to qwen3_1_7b without broad aliases or default changes."
    - "D-04/D-06/D-07: Save, preview, test-play, and call preparation accept only contained saved audio plus exact nonblank matching transcript through RayMe APIs."
    - "D-09/D-10/D-11/D-19: Shared load/prewarm readiness and sanitized failures reach server clients without hidden work or private diagnostics."
    - "No migration or autonomous fixture fabricates permission for a real person."
  artifacts:
    - {path: "web-ui/server/alembic/versions/0003_qwen3_engine_identity.py", provides: "Exact idempotent identity migration"}
    - {path: "web-ui/server/app/domain/voice_service.py", provides: "Qwen reference/provenance and operation validation"}
---

<objective>Make the hard-gated engine durable and safe across voice storage, server operations, and call preparation.</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-04-SUMMARY.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-CONTEXT.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate exact engine identity and record provenance without inventing consent</name>
  <files>web-ui/server/alembic/versions/0003_qwen3_engine_identity.py, web-ui/server/app/domain/settings_service.py, web-ui/server/app/domain/voice_service.py, web-ui/server/tests/test_migrations.py, web-ui/server/tests/test_voices.py</files>
  <behavior>
    - D-02: update only `voices.default_engine == qwen3_0_6b` and exact endpoint-settings `tts_default_engine`; preserve unknown values, unrelated JSON, and current non-Qwen default.
    - Zero-row, repeated upgrade/read normalization, populated, malformed/unrelated settings, and compatibility reads are idempotent; APIs return canonical id.
    - New real-person Qwen voices require explicit provenance source, authorization basis, and LAN-test scope supplied by the user. Migrated rows are marked `authorization_status=needs_confirmation`; no boolean permission is invented.
    - Synthetic mechanical fixtures use `authorization_basis=generated_non_person_fixture`, never pretend a person granted permission, and cannot be promoted as the user's likeness acceptance voice.
  </behavior>
  <action>Create revision 0003 from actual head, defensively rewrite exact durable values, and centralize narrow compatibility normalization. Extend metadata validation without adding a column unless the existing JSON cannot carry the locked record. Preserve downgrade ambiguity explicitly rather than rewriting arbitrary canonical values.</action>
  <verify><automated>uv run --project web-ui/server pytest web-ui/server/tests/test_migrations.py web-ui/server/tests/test_voices.py -q &amp;&amp; uv run --project web-ui/server alembic -c web-ui/server/alembic.ini upgrade head</automated></verify>
  <done>Durable identity is canonical and provenance is honest for migrated, real-person, and synthetic references.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate saved-voice operations on a contained authorized reference</name>
  <files>web-ui/server/app/domain/voice_service.py, web-ui/server/app/api/voices.py, web-ui/server/app/domain/ai_backend_client.py, web-ui/server/tests/test_voices.py</files>
  <behavior>
    - D-04/D-06: save, preview, test-play, and forwarding require active saved voice, contained basename asset, valid bytes, exact nonblank transcript, required provenance, and nonempty target where applicable.
    - D-07: reuse backend alignment/prewarm scalar/stable result; never replace exact transcript, downgrade to reference-only, accept a browser path, or call a hosted service.
    - D-09/D-10/D-11: preview/test-play trigger/poll shared model plus opaque-voice prompt state asynchronously and expose loading/resident separately from prewarming/ready/failed.
    - D-19: parse safe backend 4xx details and remove path-heavy diagnostics; errors/logs contain opaque ids/hashes/scores and fixed copy only.
  </behavior>
  <action>Reuse canonical validation/provenance helpers from every saved-voice route. Resolve only the selected saved asset under RayMe blob storage, hash bytes, compute opaque voice key, and pass bytes/exact transcript/key to backend preparation. Test blank/mismatch/path/provenance/empty-target/readiness/retry/leak cases and valid Phase 005 or synthetic scope.</action>
  <verify><automated>uv run --project web-ui/server pytest web-ui/server/tests/test_voices.py -q</automated></verify>
  <done>Every saved-voice Qwen operation is bound to an honestly authorized reference and shared visible prewarm path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Carry authorization and readiness into call preparation</name>
  <files>web-ui/server/app/domain/call_service.py, web-ui/server/app/domain/ai_backend_client.py, web-ui/server/tests/test_calls.py</files>
  <behavior>
    - D-04/D-06/D-07: call start resolves the active saved voice, contained audio, exact nonblank transcript, and provenance through the same canonical helpers before backend work.
    - D-09/D-10/D-11: call preparation polls shared model and opaque prompt state without duplicating load or prompt work.
    - D-19: invalid or failed Qwen preparation returns fixed safe details and leaves unrelated calls and voices usable.
  </behavior>
  <action>Wire call preparation to the saved-voice authorization and backend readiness interfaces. Test missing/deleted/mismatched references, pending/failed/retry readiness, safe errors, and a later valid call after Qwen failure.</action>
  <verify><automated>uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py -q</automated></verify>
  <done>Call start cannot bypass saved-reference authorization or honest selected-prompt readiness.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-02 | Elevation/Disclosure | Saved reference | high | mitigate | Active saved lookup, path containment, explicit provenance/authorization basis. |
| T-09-04 | Tampering/DoS | Transcript | critical | mitigate | Every-boundary nonblank gate plus backend alignment. |
| T-09-07 | Information Disclosure | Logs/errors | high | mitigate | Opaque ids/hashes/scalars only and leak assertions. |
</threat_model>

<verification>Run both commands, full Web server tests, and `git diff --check`.</verification>
<success_criteria>Saved Qwen voices remain truthful, authorized, contained, and visibly prepared across all server flows.</success_criteria>
<output>Create `09-07-SUMMARY.md`.</output>
