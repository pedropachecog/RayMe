---
phase: 09-integrate-faster-qwen3-tts-1-7b-into-live-calls
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - ai-backend/app/models/tts_qwen3_protocol.py
  - ai-backend/app/models/tts_qwen3_worker.py
  - ai-backend/app/models/tts_qwen3.py
  - ai-backend/tests/test_tts_qwen3.py
autonomous: true
requirements: [REQ-22, REQ-45]
estimate: {tokens: 50000, raw_tokens: 50000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-01/D-03/D-05: A spawned Windows worker is the sole CUDA/model/full-ICL prompt owner."
    - "D-09/D-10: Model load and capacity-one selected-voice prewarm are separate request-scoped operations."
    - "D-12/D-17/D-19: The adapter exposes native stream and prompt cancellation with one validated terminal and Qwen-scoped containment."
  artifacts:
    - {path: "ai-backend/app/models/tts_qwen3_protocol.py", provides: "Versioned Pydantic worker IPC"}
    - {path: "ai-backend/app/models/tts_qwen3_worker.py", provides: "Pinned CUDA load/prewarm/stream/cancel worker"}
    - {path: "ai-backend/app/models/tts_qwen3.py", provides: "Supervised streaming adapter"}
  key_links:
    - {from: "ai-backend/app/models/tts_qwen3.py", to: "ai-backend/app/models/tts_qwen3_worker.py", via: "spawned request-scoped IPC", pattern: "request_id|cancel"}
---

<objective>
Build the model-free tested worker/adapter preparation slice used by the real production tracer.

Purpose: Establish CUDA ownership, prompt preparation, native chunk emission, and request cancellation without mixing in manager/call/deployment work.
Output: Versioned protocol, worker, adapter, and fake-process contracts.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/LIVE-CALL-INVARIANTS.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-01-SUMMARY.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-AI-SPEC.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define validated request-scoped worker commands and events</name>
  <files>ai-backend/app/models/tts_qwen3_protocol.py, ai-backend/tests/test_tts_qwen3.py</files>
  <behavior>
    - Commands cover load, prewarm, generate, cancel, invalidate, and unload with schema version, bounded request id, opaque voice key, and no browser filesystem path.
    - Events cover loaded, prompt ready/failed, chunk, and exactly one `done|cancelled|error` terminal with monotonic indexes/times and sample rate 24000.
    - Wrong version/id, malformed/oversized base64, duplicate terminal, and late event fail validation before playout.
  </behavior>
  <action>Write RED Pydantic discriminated-union tests from AI-SPEC 4b.1, then create a CUDA-free protocol module. Bound every string/byte/count field and keep raw transcript/audio/model path outside event/log schemas. Include an invalidate acknowledgement keyed only by opaque voice key so saved-voice deletion can later evict prompt state.</action>
  <verify><automated>uv run --project ai-backend pytest ai-backend/tests/test_tts_qwen3.py -q -k 'protocol or schema or event'</automated></verify>
  <done>Malformed or cross-request process data cannot become audio, readiness, cache state, or completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Supervise the pinned CUDA worker and pull native full-ICL chunks</name>
  <files>ai-backend/app/models/tts_qwen3_worker.py, ai-backend/app/models/tts_qwen3.py, ai-backend/tests/test_tts_qwen3.py</files>
  <behavior>
    - D-01/D-03: worker-local import loads only the exact local 1.7B snapshot with CUDA, bfloat16, SDPA, Torch backend, max sequence 2048, and warmup prefill 100; parent imports no CUDA runtime before spawn.
    - D-05/D-10: prewarm builds one full-ICL prompt from reference bytes plus exact transcript and atomically owns one opaque voice key; no prompt tensors cross IPC.
    - D-12: stream calls only `generate_voice_clone_streaming` with chunk size 4, full ICL, append silence, and current-segment prefill; generator exhaustion is normal authority.
    - D-17/D-19: a stdin reader can receive exact-request cancel while generation pulls; adapter drains one terminal and terminate/kills a stuck/malformed worker without killing RayMe.
  </behavior>
  <action>Use the VoxCPM2 subprocess lifecycle only as an analog. Keep a dedicated command reader able to set cancellation during synchronous generation; validate every stdout event with Task 1 schemas. Implement load/prewarm/stream/cancel/invalidate/unload methods with injected process factory tests. Whole synthesis, x-vector-only, CPU, GGML, CustomVoice, VoiceDesign, generic `generate()`, and low-level cached-reference arguments remain excluded per D-05/D-12 and COVERAGE.</action>
  <verify><automated>uv run --project ai-backend pytest ai-backend/tests/test_tts_qwen3.py -q</automated></verify>
  <done>A fake worker proves exact load/prewarm/native-stream/cancel/invalidate lifecycle without local model import/download.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Spoofing/Tampering | Worker IPC | high | mitigate | Versioned schemas, exact request identity, monotonic chunks, one terminal. |
| T-09-05 | DoS | Worker hang/crash | high | mitigate | Request cancel plus bounded terminate/kill containment. |
| T-09-07 | Information Disclosure | IPC/logs | high | mitigate | Opaque keys/scalars only; no voice material or paths. |
</threat_model>

<verification>Run the focused suite and `git diff --check`; confirm tests import no real model.</verification>
<success_criteria>The manager can consume a production-shaped, CUDA-isolated, streaming-only Qwen adapter in the next plan.</success_criteria>
<output>Create `09-02-SUMMARY.md`.</output>
