---
phase: 09-integrate-faster-qwen3-tts-1-7b-into-live-calls
plan: 10
type: execute
wave: 7
depends_on: [09-06]
files_modified:
  - web-ui/server/app/domain/call_tts_segments.py
  - web-ui/server/app/api/calls.py
  - web-ui/server/app/domain/call_service.py
  - web-ui/server/app/domain/ai_backend_client.py
  - web-ui/server/tests/test_call_tts_segments.py
  - web-ui/server/tests/test_calls.py
autonomous: true
requirements: [REQ-22, REQ-45, REQ-46]
estimate: {tokens: 50000, raw_tokens: 50000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-12/D-16: First safe natural Qwen segment is submitted while a slow LLM stream remains open; only that bounded segment uses upstream current-text prefill."
    - "D-17/D-18: Turn terminal controls persistence: cancelled/error turns write no complete ai_speech and one normal multi-segment turn writes exactly one."
    - "D-19: Incremental errors/cancellation remain sanitized and do not break captions, SSE keepalive, or later calls."
  artifacts:
    - {path: "web-ui/server/app/domain/call_tts_segments.py", provides: "Deterministic incremental natural-boundary segmenter"}
    - {path: "web-ui/server/tests/test_call_tts_segments.py", provides: "Slow-LLM early-submit and 60-word contracts"}
---

<objective>Start Qwen speech from incremental LLM text and persist only what completed normally.</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/LIVE-CALL-INVARIANTS.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-06-SUMMARY.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Submit first safe Qwen text before LLM completion</name>
  <files>web-ui/server/app/domain/call_tts_segments.py, web-ui/server/app/api/calls.py, web-ui/server/app/domain/ai_backend_client.py, web-ui/server/tests/test_call_tts_segments.py, web-ui/server/tests/test_calls.py</files>
  <behavior>
    - Feed deterministic splitter per token; prefer `.?!`/safe newline, retain punctuation, avoid tiny fragments, force natural phrase before 60 words, and flush one nonempty final tail.
    - D-12/D-16: held-open LLM emits first non-final speech submission before release; each safe segment uses native Qwen streaming and current-segment prefill only.
    - Turn scheduler is bounded, non-final submit returns after bounded acceptance, finalization waits for one terminal/playout result, and one native generation owns CUDA.
    - ai_token captions/SSE keepalive stay live; blank assistant output creates no speech request or fake events/persistence.
  </behavior>
  <action>Write pure feed/finish table tests and slow-LLM RED test before implementation. Extend the existing `/turns` SSE loop and internal backend client submit/finalize seam, not browser/API topology. Preserve compatible non-Qwen behavior.</action>
  <verify><automated>uv run --project web-ui/server pytest web-ui/server/tests/test_call_tts_segments.py web-ui/server/tests/test_calls.py -q -k 'segment or slow_llm or qwen'</automated></verify>
  <done>First Qwen speech work begins from an early safe sentence rather than full response EOS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Authorize durable speech only from one normal final terminal</name>
  <files>web-ui/server/app/api/calls.py, web-ui/server/app/domain/call_service.py, web-ui/server/app/domain/ai_backend_client.py, web-ui/server/tests/test_calls.py</files>
  <behavior>
    - Backend client returns typed normal/cancelled/error terminal plus playout completion; only normal final plus playout authorizes one `record_ai_speech` using exact visible accumulated text.
    - Cancel before/after audio, barge-in, hangup, worker error, and ceiling write zero complete ai_speech and emit no normal ai_done; transient captions may remain.
    - Reject later segment submissions after cancelled terminal and permit a later turn/call; fixed errors contain no private/raw material.
  </behavior>
  <action>Move persistence after terminal success and test database rows/event order for blank, normal multi-segment, cancel-before, cancel-after, hangup, and error. Keep call boundary rows and existing thread behavior unchanged.</action>
  <verify><automated>uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py web-ui/server/tests/test_call_tts_segments.py -q</automated></verify>
  <done>Durable call history now matches exactly what reached normal completed playout.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-04 | DoS | LLM segment input | high | mitigate | Incremental natural segmentation, 60-word bound, bounded scheduler. |
| T-09-01 | Tampering | Terminal/persistence | high | mitigate | Exact turn terminal and single normal persistence authorization. |
| T-09-07 | Information Disclosure | SSE/errors/history | high | mitigate | Exact visible text only on success; fixed sanitized errors. |
</threat_model>

<verification>Run both task commands, full server suite, and `git diff --check`.</verification>
<success_criteria>Speech begins while the LLM is still streaming, and cancelled/unheard output never becomes durable complete history.</success_criteria>
<output>Create `09-10-SUMMARY.md`.</output>
