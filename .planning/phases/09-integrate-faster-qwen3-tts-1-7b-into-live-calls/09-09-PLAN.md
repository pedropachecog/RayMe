---
phase: 09-integrate-faster-qwen3-tts-1-7b-into-live-calls
plan: 09
type: execute
wave: 7
depends_on: [09-08]
files_modified:
  - web-ui/client/tests/e2e/qwen3-readiness.spec.ts
  - web-ui/client/tests/e2e/voice-lab.spec.ts
  - web-ui/client/tests/e2e/call-start.spec.ts
  - web-ui/client/tests/e2e/settings-connection.spec.ts
  - web-ui/client/tests/e2e/live-call.spec.ts
autonomous: true
requirements: [REQ-22, REQ-45, REQ-46]
estimate: {tokens: 36000, raw_tokens: 36000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-02/D-09/D-10/D-11: Saved mocked browser acceptance observes canonical identity and every separate readiness/failure/retry state."
    - "The opt-in production live-call spec permits qwen3_1_7b, requires a permitted fixture/transcript/provenance, and asserts canonical URLs plus deployed commit."
    - "Mocked UI evidence is labeled contract-only; release readiness requires the post-deploy live execution in Plan 09-14."
  artifacts:
    - {path: "web-ui/client/tests/e2e/qwen3-readiness.spec.ts", provides: "Saved mocked UI acceptance"}
    - {path: "web-ui/client/tests/e2e/live-call.spec.ts", provides: "Opt-in deployed Qwen live-call acceptance"}
---

<objective>Create distinct saved browser contracts for local readiness UI and deployed Qwen call acceptance.</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-UI-SPEC.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-08-SUMMARY.md
@web-ui/client/tests/e2e/live-call.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Save the mocked readiness/error/retry browser acceptance</name>
  <files>web-ui/client/tests/e2e/qwen3-readiness.spec.ts, web-ui/client/tests/e2e/voice-lab.spec.ts, web-ui/client/tests/e2e/call-start.spec.ts, web-ui/client/tests/e2e/settings-connection.spec.ts</files>
  <behavior>
    - Drive one canonical Qwen voice through loading, resident, prewarming, ready, failed, retry, and call Connecting-to-Listening while delayed RayMe responses remain controllable.
    - Assert transcript/provenance block, form preservation, fixed sanitized errors, dynamic engine metadata retention, no console/page errors, and existing mobile controls.
    - Mark saved artifact/result metadata `environment=mocked_contract`; it cannot satisfy deployed browser/live evidence gates.
  </behavior>
  <action>Mock only RayMe routes and use existing browser error guard. Update stale engine fixtures systematically. Use the fast Plan 09-08 component tests during edits; this task's Playwright run is the wave UI acceptance, not the sub-30-second feedback loop.</action>
  <verify><automated>npm --prefix web-ui/client exec playwright -- test --list qwen3-readiness.spec.ts voice-lab.spec.ts call-start.spec.ts</automated></verify>
  <done>The saved UI acceptance is structurally runnable and honestly labeled as mocked contract evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Permit Qwen in the canonical post-deploy live-call spec</name>
  <files>web-ui/client/tests/e2e/live-call.spec.ts</files>
  <behavior>
    - Allow `qwen3_1_7b` beside VoxCPM2/F5 and reject unknown ids; create the voice through production APIs with exact nonblank transcript and explicit provenance basis/scope.
    - Require canonical URLs, `RAYME_ENABLE_LIVE_E2E=1`, expected deployed commit, local permitted reference/fake-mic files, `RAYME_LIVE_REFERENCE_TRANSCRIPT_FILE`, `RAYME_LIVE_REFERENCE_PROVENANCE_FILE`, and Qwen engine selection.
    - Assert `/webrtc/status`/health deployed commit equals `RAYME_LIVE_EXPECTED_COMMIT`, selected voice reaches resident/prompt-ready, and two live user→AI cycles produce early-audio and normal completion.
    - Phase 005 input carries `phase005_product_owner_directed`; mechanical fallback carries `generated_non_person_fixture`. Do not create `permission_confirmed` for either automatically.
  </behavior>
  <action>Extend the existing serial live suite and fixture builder without adding a Qwen browser route. Read transcript and provenance JSON from the explicit local files so shell quoting cannot corrupt private or punctuation-rich content. Add required environment validation and commit/readiness assertions. Do not run against localhost mocks as release proof; Plan 09-14 executes it after canonical deployment with real OMEN URLs and the copied permitted fixture.</action>
  <verify><automated>npm --prefix web-ui/client exec playwright -- test --list live-call.spec.ts</automated></verify>
  <done>The opt-in live suite accepts only an explicit permitted Qwen fixture and commit-matched deployed RayMe.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-02 | Elevation/Disclosure | Live fixture | high | mitigate | Required provenance basis/scope, exact transcript, production API authorization. |
| T-09-06 | Tampering | Browser deployment target | high | mitigate | Canonical URL and deployed-commit assertions. |
| T-09-07 | Information Disclosure | Browser artifacts | high | mitigate | Error guard and sanitized scalar/result capture only. |
</threat_model>

<verification>Run Plan 09-08 focused unit tests during edits, then `npm --prefix web-ui/client run test:e2e -- qwen3-readiness.spec.ts voice-lab.spec.ts call-start.spec.ts` once as wave UI acceptance.</verification>
<success_criteria>RayMe has separate honest mocked UI and real deployed Qwen browser acceptance paths.</success_criteria>
<output>Create `09-09-SUMMARY.md`.</output>
