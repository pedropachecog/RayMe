---
phase: 09-integrate-faster-qwen3-tts-1-7b-into-live-calls
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - web-ui/server/alembic/versions/0003_qwen3_engine_identity.py
  - web-ui/server/app/domain/settings_service.py
  - web-ui/server/app/domain/voice_service.py
  - web-ui/server/app/api/voices.py
  - web-ui/server/app/domain/call_service.py
  - web-ui/server/app/domain/ai_backend_client.py
  - web-ui/server/tests/test_migrations.py
  - web-ui/server/tests/test_voices.py
  - web-ui/server/tests/test_calls.py
autonomous: true
requirements:
  - REQ-22
  - REQ-45
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-02: Persisted exact qwen3_0_6b voice/settings values translate idempotently to truthful qwen3_1_7b; unknown strings are not broadened into aliases."
    - "D-04: Browser/server callers continue using RayMe-owned voice and call APIs; no upstream TTS service or filesystem path crosses the public boundary."
    - "D-06/D-07: Save, preview, test-play, call start, and backend forwarding all require a nonblank matching Qwen reference transcript and valid RayMe-owned reference bytes."
    - "D-09/D-10/D-11: Preview, test-play, and call preparation use the shared load/prewarm path and return honest model/prompt readiness with sanitized actionable failures."
    - "Only a saved voice whose reference provenance/permission is confirmed for private-LAN use can enter Qwen cloning."
  artifacts:
    - path: "web-ui/server/alembic/versions/0003_qwen3_engine_identity.py"
      provides: "Exact, idempotent persisted engine-id compatibility migration"
    - path: "web-ui/server/app/domain/voice_service.py"
      provides: "Canonical Qwen voice/transcript/provenance validation and readiness bridge"
    - path: "web-ui/server/app/domain/call_service.py"
      provides: "Saved-asset containment, privacy-safe diagnostics, and call prewarm input"
  key_links:
    - from: "web-ui/server/app/domain/voice_service.py"
      to: "web-ui/server/app/domain/ai_backend_client.py"
      via: "preview/test-play prepare and stable validation/readiness response"
      pattern: "qwen3_1_7b|prewarm"
    - from: "web-ui/server/app/domain/call_service.py"
      to: "web-ui/server/app/api/calls.py"
      via: "opaque saved voice id resolved to contained asset bytes/exact transcript before call preparation"
      pattern: "reference_transcript|storage_path"
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Never clone an unapproved or arbitrary reference path, unsaved blob, missing transcript, or grossly mismatched audio/transcript pair."
    - "[FLAGGED-UNVERIFIED] Never send reference audio, full transcripts, generated private speech, access tokens, or absolute private paths to hosted services, logs, or committed evidence."
---

<objective>
Make Faster Qwen3-TTS a truthful, durable saved-voice engine across migration, Voice Lab/server operations, call preparation, and privacy/provenance boundaries.

Purpose: Ensure every production clone comes from the approved saved RayMe voice and exact transcript, with explicit compatibility and no identity/path/privacy ambiguity.
Output: Idempotent Alembic migration, canonical normalization, Qwen voice/provenance validation, safe reference resolution, and backend readiness/error forwarding.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-01-SUMMARY.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-CONTEXT.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-AI-SPEC.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-PATTERNS.md

<interfaces>
Persist identity through existing `Voice.default_engine`, `Voice.reference_transcript`, `Voice.metadata_json`, `VoiceAsset.storage_path`, and `endpoint_settings.tts_default_engine`. Use existing `VoiceService`, `/api/voices` routes, `CallService`, and `AiBackendClient`; add no new database column unless execution proves metadata_json cannot carry the required provenance record.
</interfaces>
</context>

<assumption_delta_decision>
primary_noun: generalized saved-voice `engine_id`
decision: no-change
rationale: The transport rule forbidding whole-synthesis fallback does not add a second identity abstraction; `qwen3_1_7b` truthfully replaces or translates the historical specific value.
</assumption_delta_decision>

<planner_assumptions>
- Legacy-id migration, read normalization, zero-row upgrade, repeated deployment, and same-key prewarm are idempotent.
- Exact old values migrate; unknown engine strings remain untouched/rejected. Existing voices without Qwen provenance confirmation cannot clone until the user confirms permission.
- SHA-256 always covers stored reference bytes; normalized lexical text is comparison/cache data only, and exact approved transcript remains durable ICL input.
</planner_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate and normalize truthful engine identity without corrupting saved state</name>
  <read_first>
    - web-ui/server/alembic/versions/0002_voice_storage.py
    - web-ui/server/app/domain/settings_service.py
    - web-ui/server/app/domain/voice_service.py
    - web-ui/server/tests/test_migrations.py
    - web-ui/server/tests/test_voices.py
  </read_first>
  <files>web-ui/server/alembic/versions/0003_qwen3_engine_identity.py, web-ui/server/app/domain/settings_service.py, web-ui/server/app/domain/voice_service.py, web-ui/server/tests/test_migrations.py, web-ui/server/tests/test_voices.py</files>
  <behavior>
    - D-02: migrate only `voices.default_engine == qwen3_0_6b` and exact `endpoint_settings.tts_default_engine` JSON values to `qwen3_1_7b`; update read/write normalization during the compatibility window.
    - Prove upgrade of populated fixtures, zero-row databases, and repeated/read-normalized operation are idempotent; preserve unknown strings for explicit rejection and do not change the user's current non-Qwen default.
    - Store Qwen reference provenance/permission/scope under existing metadata_json, marking migrated records without confirmation as needing confirmation instead of inventing consent.
    - Keep downgrade behavior narrowly documented/tested so it cannot rewrite arbitrary canonical/unknown strings or lose unrelated metadata.
  </behavior>
  <action>Create Alembic revision `0003_qwen3_engine_identity` from the actual `0002_voice_storage` head. Parse and update settings JSON defensively while preserving unrelated keys. Centralize exact legacy normalization in the voice/settings domain boundary so API output is canonical during rollout. Add migration fixtures for exact old voice/settings values, unknown strings, malformed/unrelated settings content, empty tables, and repeated canonical reads; use the repository's Alembic test helper and command rather than schema-push tooling.</action>
  <reversibility rating="costly">The data rewrite touches durable voice/settings values, but exact-value translation plus compatibility reads and a migration-tested downgrade keep it recoverable without inventing aliases.</reversibility>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_migrations.py web-ui/server/tests/test_voices.py -q && uv run --project web-ui/server alembic -c web-ui/server/alembic.ini upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - Exact old voice/settings ids become canonical once; zero-row and repeated runs remain clean.
    - Unknown strings and unrelated JSON fields are not mutated into Qwen identity.
    - No API response advertises the legacy id except explicit compatibility test fixtures.
    - Migrated records do not claim permission that was never confirmed.
  </acceptance_criteria>
  <done>Durable Qwen identity is truthful, explicit, idempotent, and compatible across voice and settings storage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate save, preview, test-play, and call preparation on authorized contained reference data</name>
  <read_first>
    - web-ui/server/app/domain/voice_service.py
    - web-ui/server/app/api/voices.py
    - web-ui/server/app/domain/call_service.py
    - web-ui/server/app/domain/ai_backend_client.py
    - web-ui/server/tests/test_voices.py
    - web-ui/server/tests/test_calls.py
  </read_first>
  <files>web-ui/server/app/domain/voice_service.py, web-ui/server/app/api/voices.py, web-ui/server/app/domain/call_service.py, web-ui/server/app/domain/ai_backend_client.py, web-ui/server/tests/test_voices.py, web-ui/server/tests/test_calls.py</files>
  <behavior>
    - D-04/D-06/D-07: require a saved Qwen voice, basename-contained RayMe asset, valid nonempty bytes, exact nonblank transcript, and confirmed private-LAN reference permission before save/preview/test-play/call preparation; empty target preview text cannot return success.
    - Reuse backend alignment/prewarm and preserve its scalar score/stable code; do not silently replace the transcript, downgrade clone mode, accept a browser path, or send voice material to a hosted service.
    - D-09/D-10/D-11: preview/test-play/call preparation trigger/poll the shared model plus selected-voice prewarm state and propagate fixed loading/prewarming/ready/failed semantics without freezing the server request loop.
    - D-19: parse safe backend 4xx processing details and render actionable Qwen-specific errors while stripping tracebacks, worker text, full transcript, base64, blob/model/cache paths, and directory listings from public payloads and logs.
  </behavior>
  <action>Add canonical Qwen validation/provenance helpers to `VoiceService` and reuse them from every voice route. In `CallService`, resolve only the selected active saved voice to its generated flat asset name under the configured blob root, hash the bytes, and pass bytes plus exact transcript and opaque voice identity to the backend prepare/prewarm seam. Replace current path-heavy diagnostics with opaque ids/hashes and scalar metrics. Extend `AiBackendClient` safe-detail parsing for stable validation/readiness codes, keeping all browser traffic on RayMe APIs.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_voices.py web-ui/server/tests/test_calls.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Blank transcript, invalid/missing asset, arbitrary/traversal path, unconfirmed permission, empty target, and backend mismatch all block before Qwen generation.
    - A valid authorized saved voice carries the exact transcript and reference-byte hash through preview, test-play, and call preparation.
    - Loading/prewarming/ready/failed remain distinct and retryable; user input stays intact after failure.
    - Rendered errors/log captures contain no voice bytes, full transcript, access token, absolute path, directory listing, raw worker message, or traceback.
  </acceptance_criteria>
  <done>Every server-side Qwen operation is bound to one approved saved voice and safely reaches the shared prewarm path with honest state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (ASVS L1; block on high)

| Boundary | Description |
|---|---|
| Browser voice payload → Web server/database | User-controlled identity, transcript, provenance, and target text become durable clone configuration. |
| Saved voice id → blob filesystem | Opaque database identity resolves private reference bytes. |
| Web server → AI backend | Private voice material and readiness/errors cross the internal LAN API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-02 | Elevation/Disclosure | Saved reference resolution | high | mitigate | Active saved voice lookup, generated basename containment, configured blob root, no browser filesystem path. |
| T-09-03 | Tampering/DoS | Uploaded/reference audio | high | mitigate | Existing upload/base64/byte bounds plus nonempty/format checks before forwarding. |
| T-09-04 | DoS | Reference transcript | critical | mitigate | Blank block at all routes, backend alignment preflight, exact transcript preservation, generation ceilings downstream. |
| T-09-07 | Information Disclosure | Voice metadata/log/error/evidence | high | mitigate | Permission/scope record, opaque ids/hashes, stable codes, leak scans, deletion/cache invalidation; no hosted service. |
| T-09-08 | DoS | Repeated migration/prewarm/voice operations | medium | mitigate | Exact idempotent migration/read normalization and same-key prewarm semantics. |
</threat_model>

## Artifacts this phase produces

- New file: `web-ui/server/alembic/versions/0003_qwen3_engine_identity.py`.
- New/changed symbols: exact legacy-engine normalizer; Qwen voice/provenance validator; contained saved-reference resolver; safe readiness/error mapping.
- New metadata fields: `reference_provenance.source`, `reference_provenance.permission_confirmed`, and `reference_provenance.scope` under existing voice `metadata_json`.
- Commands: repository Alembic upgrade-to-head command plus focused migration/voice/call test commands.

<verification>
- Run both focused commands above, the full Web server suite, and `git diff --check`.
- Leak-scan serialized test responses/log captures for reference bytes/transcript/private paths/tokens.
</verification>

<success_criteria>
Saved Qwen voices have canonical identity and explicit reference permission; invalid or private-path inputs stop safely; preview/test-play/call preparation expose truthful prewarm state through RayMe's existing APIs.
</success_criteria>

<output>
Create `.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-03-SUMMARY.md` when done.
</output>
