---
phase: 09-integrate-faster-qwen3-tts-1-7b-into-live-calls
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - ai-backend/app/models/tts_registry.py
  - ai-backend/app/models/model_manager.py
  - ai-backend/app/api/webrtc.py
  - ai-backend/app/call/session.py
  - ai-backend/tests/test_model_manager.py
  - ai-backend/tests/test_webrtc_signaling.py
  - ai-backend/tests/test_call_session.py
  - ai-backend/tests/test_tts_voxcpm2.py
autonomous: true
requirements: [REQ-22, REQ-45, REQ-46]
estimate: {tokens: 52000, raw_tokens: 52000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-09/D-10: Model residency and selected opaque-voice prompt readiness are separately observable through RayMe status."
    - "D-12/D-13/D-14/D-15: A fake slow Qwen stream starts call playback before completion through a capacity-two bridge and keeps immediate/final carriers separate."
    - "D-17/D-18/D-20: Cancel before/after audio rejects late chunks and normal completion while VoxCPM2 recurrence guards remain green."
  artifacts:
    - {path: "ai-backend/app/models/model_manager.py", provides: "Async one-hot model and prompt prepare state"}
    - {path: "ai-backend/app/call/session.py", provides: "Qwen streaming-only fake call tracer"}
  key_links:
    - {from: "ai-backend/app/api/webrtc.py", to: "ai-backend/app/models/model_manager.py", via: "prepare/status and strict speak boundary", pattern: "qwen3_1_7b|prewarm"}
    - {from: "ai-backend/app/call/session.py", to: "ai-backend/app/models/tts_qwen3.py", via: "native stream plus request cancel", pattern: "stream|cancel"}
---

<objective>
Wire the local fake contract slice from one-hot prepare through CallSession early playout and cancellation.

Purpose: Make the canonical OMEN deployment in Plan 09-04 a real hardware proof, not the first time the layers meet.
Output: Async readiness, strict WebRTC input, bounded bridge, truthful metrics, and recurrence tests.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/LIVE-CALL-INVARIANTS.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-02-SUMMARY.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-AI-SPEC.md
@ai-backend/tests/test_call_session.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose responsive one-hot model and prompt preparation</name>
  <files>ai-backend/app/models/tts_registry.py, ai-backend/app/models/model_manager.py, ai-backend/app/api/webrtc.py, ai-backend/tests/test_model_manager.py, ai-backend/tests/test_webrtc_signaling.py</files>
  <behavior>
    - D-03/D-09: unload prior TTS before Qwen load; fake slow load leaves event loop/status responsive and never reports more than one resident TTS.
    - D-09/D-10: model `idle|loading|resident|unavailable` is separate from opaque selected-voice `none|prewarming|ready|failed`; same-key prepare is idempotent.
    - Strict prepare/speak payloads accept opaque voice key, contained reference bytes, exact transcript, and target segment but no browser filesystem/model path.
    - Status includes deployed-commit carrier used by later live E2E and sanitized stable errors only.
  </behavior>
  <action>Write fake slow-load/prewarm tests first, then add async prepare/status seams around the supervised adapter without weakening unload-before-load. Extend the generic synthesis input with request/turn/opaque voice identity only. Keep the public boundary under existing RayMe `/webrtc`; do not add an upstream Qwen endpoint per D-04.</action>
  <verify><automated>uv run --project ai-backend pytest ai-backend/tests/test_model_manager.py ai-backend/tests/test_webrtc_signaling.py -q</automated></verify>
  <done>A caller can observe model and prompt readiness before speech without blocking the backend loop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove fake early playback, truthful carriers, and request cancellation</name>
  <files>ai-backend/app/call/session.py, ai-backend/tests/test_call_session.py, ai-backend/tests/test_webrtc_signaling.py, ai-backend/tests/test_tts_voxcpm2.py</files>
  <behavior>
    - D-12/D-13: qwen3_1_7b routes only through stream; a deliberately held-open producer emits outbound audio and `ai_audio_started` before completion.
    - D-14: the thread-to-async bridge is `maxsize=2` and a blocking put applies backpressure; later paced-track refinement remains Plan 09-11.
    - D-15: immediate event contains only first chunk/enqueue/start fields; final totals remain terminal-only.
    - D-17/D-18: cancel before/after first audio stops playout, invokes exact request cancel, discards late events, restores listening/ended, and emits no normal `ai_done`.
    - D-20: existing VoxCPM2 slow stream, no whole-synthesis fallback, timing separation, and interrupt tests remain unchanged and green.
  </behavior>
  <action>Add Qwen twins of the established live-stream invariant tests and route by explicit streaming capability. Convert the producer callback to a cancellation-aware blocking put. Do not collect a generator or call whole synthesis. Keep this deterministic slice narrow; real adapter/manager/saved voice/WebRTC behavior is the next plan's mandatory hardware tracer.</action>
  <verify><automated>uv run --project ai-backend pytest ai-backend/tests/test_call_session.py ai-backend/tests/test_webrtc_signaling.py ai-backend/tests/test_tts_voxcpm2.py -q</automated></verify>
  <done>The local contract proves first playback before completion and no normal terminal after cancellation for both protected streaming engines.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Spoofing/Tampering | Call/worker events | high | mitigate | Exact session/turn/request identity and one terminal. |
| T-09-05 | DoS | Hidden load/cancel | high | mitigate | Async readiness, bounded bridge, prompt cancel, late drain. |
| T-09-08 | DoS | Queue/residency | critical | mitigate | One resident engine and capacity-two bridge with blocking admission. |
</threat_model>

<verification>Run both focused commands and `git diff --check`.</verification>
<success_criteria>The next wave can deploy a production path whose local architecture already proves live early playback and cancel semantics.</success_criteria>
<output>Create `09-03-SUMMARY.md`.</output>
