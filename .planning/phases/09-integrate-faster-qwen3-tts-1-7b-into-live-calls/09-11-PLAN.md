---
phase: 09-integrate-faster-qwen3-tts-1-7b-into-live-calls
plan: 11
type: execute
wave: 6
depends_on: [09-05]
files_modified:
  - ai-backend/app/call/tracks.py
  - ai-backend/app/call/session.py
  - ai-backend/app/api/webrtc.py
  - ai-backend/tests/test_call_session.py
  - ai-backend/tests/test_webrtc_signaling.py
  - ai-backend/tests/test_tts_voxcpm2.py
autonomous: true
requirements: [REQ-45, REQ-46]
estimate: {tokens: 52000, raw_tokens: 52000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-13/D-14: Backpressure reaches paced 20 ms WebRTC consumption; neither bridge nor track can accumulate a whole turn."
    - "D-15: Immediate first-audio and final generation/playout/smoothness metrics remain physically separate and truthful."
    - "D-17/D-18/D-20: Every control cause cancels the matching worker/playout, rejects late completion, recovers, and keeps VoxCPM2 invariants green."
  artifacts:
    - {path: "ai-backend/app/call/tracks.py", provides: "Pending-sample playout credit and high-water metrics"}
    - {path: "ai-backend/tests/test_call_session.py", provides: "Slow producer/consumer/cancel recurrence suite"}
  key_links:
    - from: "ai-backend/app/call/session.py"
      to: "ai-backend/app/call/tracks.py"
      via: "CallSession._speak_streaming_speech capacity-two bridge awaits QueuedAudioOutputTrack admission credits released by paced 20 ms recv consumption"
      pattern: "maxsize=2|pending_samples|enqueue"
    - from: "ai-backend/app/call/tracks.py"
      to: "ai-backend/app/call/session.py"
      via: "QueuedAudioOutputTrack recv/stop_current exposes high-water, block, underflow, debt, join, and discard counters to tts_playback_final"
      pattern: "tts_playback_final|underflow|high_water"
    - from: "ai-backend/app/api/webrtc.py"
      to: "ai-backend/app/call/session.py"
      via: "interrupt/end/switch/close controls carry matching session/turn/request id through playout drain to Qwen3TtsAdapter.cancel and terminal watchdog"
      pattern: "request_id|cancel|stop_current"
---

<objective>Finish bounded realtime playout, truthful final metrics, and terminal-safe interruption in the AI backend.</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/LIVE-CALL-INVARIANTS.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-05-SUMMARY.md
@.planning/phases/08.1-live-call-streaming-invariant-incident-repair-and-prevention/
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry capacity-two backpressure through paced playout</name>
  <files>ai-backend/app/call/tracks.py, ai-backend/app/call/session.py, ai-backend/tests/test_call_session.py, ai-backend/tests/test_webrtc_signaling.py</files>
  <behavior>
    - D-13/D-14: deliberately fast producer plus slow consumer still starts first audio before completion; bridge high-water <=2 and producer blocks on actual put/admission.
    - Track pending samples includes queue plus internal buffer, blocks admission over fixed tested audio budget, releases/notifies as 20 ms frames consume, and wakes/resets on stop.
    - Record bridge/track high-water, block time, underflow, debt, order/drop, joins, and discard counts; sustained RTFx below 1.05 is explicit, never hidden by larger/full-turn buffering.
    - D-15: `ai_audio_started` contains only first-known/startup fields; terminal owns totals, EOS, RTF, generation/playout completion, underflow/join/high-water.
  </behavior>
  <action>Begin with the slow paced consumer regression that defeats today's unbounded track. Add pending-sample credit without changing 20 ms pacing. Preserve bounded startup upper limit and current VoxCPM2 first-before-completion/no-whole-fallback assertions.</action>
  <verify><automated>uv run --project ai-backend pytest ai-backend/tests/test_call_session.py ai-backend/tests/test_webrtc_signaling.py ai-backend/tests/test_tts_voxcpm2.py -q -k 'slow or stream or playback or metric or fallback'</automated></verify>
  <done>Memory/audio debt stays bounded by real playout consumption while first audio remains early.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make every interrupt/control path request-scoped and terminal-safe</name>
  <files>ai-backend/app/call/session.py, ai-backend/app/call/tracks.py, ai-backend/app/api/webrtc.py, ai-backend/tests/test_call_session.py, ai-backend/tests/test_webrtc_signaling.py</files>
  <behavior>
    - D-17: VAD barge-in, button interrupt, cancel before/after audio, hangup, engine switch, and session close mark cancelled first, reject later events, stop/drain playout, cancel exact request, drain terminal, and force terminate by two seconds.
    - D-18: no post-cancel enqueue/audio, normal ai_done, complete terminal, or stale next-turn audio; correct state becomes listening or ended and a later call succeeds.
    - D-19: forced failure is Qwen-scoped/sanitized and does not disable STT, WebRTC, backend, or other engines.
  </behavior>
  <action>Write parameterized control-cause tests and adapter spies before implementation. Make stop wake blocked producers and count late events only as discarded evidence. Preserve exactly one normal terminal for unaffected turns.</action>
  <verify><automated>uv run --project ai-backend pytest ai-backend/tests/test_call_session.py ai-backend/tests/test_webrtc_signaling.py -q -k 'cancel or interrupt or hangup or switch or close or late'</automated></verify>
  <done>All call controls promptly stop Qwen work/audio without false completion and recover cleanly.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-08 | DoS | Chunk/track queues | critical | mitigate | Capacity-two bridge plus paced pending-sample admission. |
| T-09-01 | Tampering | Late/cross-turn events | high | mitigate | Cancelled-first request identity and late rejection. |
| T-09-05 | DoS | Missed cancellation | high | mitigate | Two-second watchdog, worker termination, recovery test. |
</threat_model>

<verification>Run both focused commands, the full backend suite, and `git diff --check`.</verification>
<success_criteria>Qwen remains a live interruptible phone call under slow producer, slow consumer, and every control cause.</success_criteria>
<output>Create `09-11-SUMMARY.md`.</output>
