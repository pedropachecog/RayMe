---
phase: 09-integrate-faster-qwen3-tts-1-7b-into-live-calls
plan: 13
type: execute
wave: 9
depends_on: [09-06, 09-09, 09-10, 09-11, 09-12]
files_modified:
  - .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-run-omen-evidence.py
  - .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/test_phase09_evidence.py
autonomous: true
requirements: [REQ-22, REQ-45, REQ-46]
estimate: {tokens: 44000, raw_tokens: 44000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-20/D-21: The hardware runner executes every manifest scenario through production RayMe paths and emits raw data for independent verification."
    - "Reference selection/preflight uses the authorized saved Phase 005 voice when present or the mechanical non-person fallback; private inputs never enter committed evidence."
    - "Fifty hot turns capture intelligibility, validity, latency/realtime, acoustic/speaker drift, memory, anchors, early/middle/late, and reel metadata."
  artifacts:
    - {path: "09-run-omen-evidence.py", provides: "Production RayMe 20-scenario/50-turn OMEN runner"}
---

<objective>Construct the production-path OMEN evidence runner separately from final deployment and handoff.</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-12-SUMMARY.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-evidence-manifest.json
@.planning/spikes/005-faster-qwen3-tts-longitudinal-quality/soak_probe.py
@.planning/spikes/006-faster-qwen3-tts-live-stream-contract/live_contract_probe.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement production-path scenario and 50-turn acquisition</name>
  <files>.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-run-omen-evidence.py, .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/test_phase09_evidence.py</files>
  <behavior>
    - Invoke production voice APIs, manager, worker, prewarm, CallSession/track, WebRTC/public call flow, STT, and persistence; no parallel model-only implementation or direct whole synthesis.
    - Execute valid short/medium/long, message integrity/endings, tolerant/invalid alignment, ceiling, slow producer/consumer, cancel before/after, hangup/switch, identity-one-hot, worker failure, hot-50-turn, and canonical-deployed-call cases.
    - Reuse the hard-gate opaque saved voice/provenance. Resolve Phase 005 asset if present; otherwise mechanical fixture. Never invent a real person's permission or accept arbitrary path.
    - Record raw event/order/timing/RTF/queue/track/underflow/join/cancel/persistence/audio-validity/STT/WER/acoustic/memory/anchor samples and local WAV/reel pointers; sanitized outputs include ids/hashes/scalars only.
  </behavior>
  <action>Reuse measurement functions from Spikes 005/006 but route actions through production RayMe objects/APIs. Make runner schema match manifest/verifier and accept expected commit plus output directory. Include deterministic dependency injection dry-run tests for all scenario dispatch and forbidden direct generation imports.</action>
  <verify><automated>python3 -m pytest .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/test_phase09_evidence.py -q -k 'runner or scenario or production_path'</automated></verify>
  <done>The runner can acquire all raw release signals from the integrated product path without CUDA in local tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Coordinate pinned speaker scoring, service restoration, and sanitized result bundle</name>
  <files>.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-run-omen-evidence.py, .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/test_phase09_evidence.py</files>
  <behavior>
    - `--core-only` collects runtime/status/call-flow/soak/STT at one expected commit and leaves Qwen resident plus selected prompt ready; `--finish-acoustic-leak` later scores the same raw soak audio and scans same-commit logs.
    - After soak audio is captured locally, the finish mode cleanly unloads Qwen through manager, runs pinned WavLM scorer on OMEN CUDA against baseline/early/middle/late buckets, then reloads Qwen and prewarms the selected saved voice before browser readiness.
    - Assert scorer model/revision/transformers/Torch/CUDA/input hashes and late-vs-early/baseline deltas; local audio and embeddings remain uncommitted.
    - Emit separate runtime, status, call-flow, soak, STT, speaker, browser-placeholder, and leak-scan schema files for final deployment to fill/verify; never self-certify overall status.
    - On scorer/restore failure, Qwen is not ready and bundle remains failed; no CPU/remote judge fallback.
  </behavior>
  <action>Add dry-run tests for the core-only/finish split and state transitions proving unload→CUDA scorer→reload→prewarm plus failure containment. Let the independent verifier decide gates. Bind each mode to expected commit and ensure the finish mode leaves OMEN Qwen resident and selected prompt ready for browser/live acceptance.</action>
  <verify><automated>python3 -m pytest .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/test_phase09_evidence.py -q &amp;&amp; python3 .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-verify-evidence.py --contracts-only &amp;&amp; python3 .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-verify-evidence.py --self-test</automated></verify>
  <done>The hardware runner is contract-complete, privacy-local, independently verifiable, and restores call-ready Qwen state.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-02 | Elevation/Disclosure | Runner fixture | high | mitigate | Opaque authorized saved voice or generated non-person fallback. |
| T-09-07 | Information Disclosure | Result bundle | high | mitigate | Local audio/embeddings, committed ids/hashes/scalars, leak scan. |
| T-09-08 | DoS/Tampering | Runner/scorer lifecycle | critical | mitigate | Production paths, deterministic scenarios, unload/restore assertions, independent verifier. |
</threat_model>

<verification>Run both task commands and inspect imports to confirm no direct model-only generation path.</verification>
<success_criteria>The final deploy can invoke one complete, privacy-safe production evidence runner rather than build tools while releasing.</success_criteria>
<output>Create `09-13-SUMMARY.md`.</output>
