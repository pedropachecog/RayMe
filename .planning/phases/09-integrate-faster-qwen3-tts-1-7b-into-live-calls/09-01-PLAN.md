---
phase: 09-integrate-faster-qwen3-tts-1-7b-into-live-calls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-backend/pyproject.toml
  - ai-backend/uv.lock
  - ai-backend/app/models/tts_registry.py
  - ai-backend/app/models/engine_metadata.py
  - ai-backend/tests/test_tts_registry.py
  - ai-backend/tests/test_health.py
autonomous: true
requirements: [REQ-22]
estimate: {tokens: 36000, raw_tokens: 36000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-01/D-02: RayMe locks the approved source commit and advertises only the truthful qwen3_1_7b production identity."
    - "D-03: Registry/health metadata preserves one-hot CUDA-only residency and does not change the current global default."
    - "The resolved young-package decision is reproducible from exact registry, annotated-tag-object presence, peeled-tag commit, and lock assertions before install."
  artifacts:
    - {path: "ai-backend/uv.lock", provides: "Immutable faster-qwen3-tts source lock"}
    - {path: "ai-backend/app/models/tts_registry.py", provides: "Canonical 1.7B streaming/transcript metadata"}
  key_links:
    - from: "ai-backend/pyproject.toml"
      to: "ai-backend/uv.lock"
      via: "exact Git commit dependency"
      pattern: "a70afc0f81f7f5f8801c3227968f1102f43f211c"
---

<objective>
Create the narrow dependency and roster seam the real worker tracer will consume.

Purpose: Separate package/identity work from worker and call implementation so the first hardware gate has one truthful runtime.
Output: Exact source lock, canonical engine metadata, and package-provenance evidence.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-CONTEXT.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-RESEARCH.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/COVERAGE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the product-owner-approved runtime source</name>
  <files>ai-backend/pyproject.toml, ai-backend/uv.lock</files>
  <behavior>
    - D-01: lock `faster-qwen3-tts` tag v0.3.2 source commit `a70afc0f81f7f5f8801c3227968f1102f43f211c`; keep qwen-tts 0.1.1 and transformers 4.57.3 compatible.
    - Do not let the broad upstream Torch constraint replace the canonical OMEN CUDA wheel; deployment reasserts Torch later.
    - Treat any registry/repository/tag/lock mismatch as blocking, while preserving the already recorded product-owner approval instead of asking a new product question.
  </behavior>
  <action>Run `node /home/agent/.codex/gsd-core/bin/gsd-tools.cjs package-legitimacy check --ecosystem pypi faster-qwen3-tts`, `python3 -m pip index versions faster-qwen3-tts`, and the exact two-ref assertion in this task's automated verification. Compare the repository URL and version with the resolved audit, require the annotated tag-object ref to exist, and require only the peeled `refs/tags/v0.3.2^{}` ref—not the tag-object hash—to equal source commit `a70afc0f81f7f5f8801c3227968f1102f43f211c`. Add the immutable Git dependency to the optional TTS group and regenerate the lock. Do not add an upstream server, GGML path, second model, or new public API per D-01/D-04.</action>
  <verify><automated>tag_refs="$(git ls-remote --tags https://github.com/andimarafioti/faster-qwen3-tts 'refs/tags/v0.3.2' 'refs/tags/v0.3.2^{}')" &amp;&amp; printf '%s\n' "$tag_refs" | awk '$2 == "refs/tags/v0.3.2" { tag_object = $1 } $2 == "refs/tags/v0.3.2^{}" { peeled = $1 } END { exit !(tag_object != "" &amp;&amp; peeled == "a70afc0f81f7f5f8801c3227968f1102f43f211c") }' &amp;&amp; uv lock --project ai-backend --check &amp;&amp; rg -n 'a70afc0f81f7f5f8801c3227968f1102f43f211c' ai-backend/uv.lock</automated></verify>
  <done>The annotated tag-object ref exists, its peeled commit equals the approved immutable source commit, and the regenerated lock names that commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish only the truthful selectable 1.7B roster identity</name>
  <files>ai-backend/app/models/tts_registry.py, ai-backend/app/models/engine_metadata.py, ai-backend/tests/test_tts_registry.py, ai-backend/tests/test_health.py</files>
  <behavior>
    - D-02: selectable metadata contains `qwen3_1_7b`, label `Qwen3-TTS 1.7B-Base`, and no selectable old-id alias.
    - Set `requires_transcript=True` and `supports_streaming=True`; preserve F5 as current default and truthful caveat/runtime evidence.
    - D-03: health remains one-hot and CUDA-only; no private model path or raw exception enters metadata.
  </behavior>
  <action>Update both backend roster sources and focused tests. Historical `qwen3_0_6b` may appear only in later compatibility migration tests. Keep the metadata generic enough that the browser learns capabilities/readiness from RayMe, never upstream runtime arguments per D-04.</action>
  <verify><automated>uv run --project ai-backend pytest ai-backend/tests/test_tts_registry.py ai-backend/tests/test_health.py -q</automated></verify>
  <done>Every production roster response names the selected 1.7B engine truthfully without changing unrelated defaults.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-06 | Tampering | Runtime identity | critical | mitigate | Immutable source commit and exact lock assertion. |
| T-09-SC | Tampering | Package install | high | mitigate | Recorded human provenance plus exact package-legitimacy, PyPI, annotated-tag-object, peeled-commit, and lock checks. |
</threat_model>

<verification>Run both task commands and `git diff --check`.</verification>
<success_criteria>The executor can build the worker against one approved immutable dependency and one truthful public engine identity.</success_criteria>
<output>Create `09-01-SUMMARY.md`.</output>
