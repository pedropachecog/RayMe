---
phase: 09-integrate-faster-qwen3-tts-1-7b-into-live-calls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-backend/app/models/tts_qwen3_protocol.py
  - ai-backend/app/models/tts_qwen3_worker.py
  - ai-backend/app/models/tts_qwen3.py
  - ai-backend/app/models/tts_registry.py
  - ai-backend/app/models/engine_metadata.py
  - ai-backend/app/models/model_manager.py
  - ai-backend/app/call/session.py
  - ai-backend/app/api/webrtc.py
  - ai-backend/pyproject.toml
  - ai-backend/uv.lock
  - ai-backend/tests/test_tts_qwen3.py
  - ai-backend/tests/test_tts_registry.py
  - ai-backend/tests/test_model_manager.py
  - ai-backend/tests/test_call_session.py
  - ai-backend/tests/test_webrtc_signaling.py
  - ai-backend/tests/test_health.py
  - web-ui/server/app/domain/call_service.py
  - web-ui/server/app/domain/ai_backend_client.py
  - web-ui/server/tests/test_calls.py
autonomous: true
requirements:
  - REQ-22
  - REQ-45
  - REQ-46
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-03/D-05: One canonical qwen3_1_7b adapter supervises the pinned 1.7B full-ICL CUDA worker behind RayMe's one-hot manager."
    - "D-09/D-10: A saved voice can move through visible model loading/resident and prompt prewarming/ready states before speech."
    - "D-12/D-13/D-14/D-15: The tracer plays a native Qwen chunk before a slow producer completes through a capacity-two bridge, with immediate and final metrics kept separate."
    - "D-17/D-18: Cancelling the tracer after first audio stops the matching generation, rejects late chunks, and emits no normal ai_done."
    - "REQ-46 evidence fields retain hot native median <=500 ms, normal RayMe first playback <=1.25 s, sustained RTFx >=1.05 each and median >=1.25, plus user-end-to-audio <800 ms as a target rather than a buffering excuse."
  artifacts:
    - path: "ai-backend/app/models/tts_qwen3_protocol.py"
      provides: "CUDA-free versioned request-scoped worker command/event schemas"
    - path: "ai-backend/app/models/tts_qwen3_worker.py"
      provides: "Spawned Windows CUDA owner for load, prompt prewarm, streaming, and cancel"
    - path: "ai-backend/app/models/tts_qwen3.py"
      provides: "Supervised qwen3_1_7b streaming adapter"
    - path: "ai-backend/tests/test_tts_qwen3.py"
      provides: "Model-free worker/adapter tracer contracts"
  key_links:
    - from: "ai-backend/app/models/model_manager.py"
      to: "ai-backend/app/models/tts_qwen3.py"
      via: "one-hot async prepare/load and selected-voice prewarm"
      pattern: "qwen3_1_7b|prewarm"
    - from: "ai-backend/app/call/session.py"
      to: "ai-backend/app/models/tts_qwen3.py"
      via: "TtsStreamingAdapter stream plus request-scoped cancel"
      pattern: "stream|cancel"
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Never silently substitute another model, CPU path, x-vector mode, or whole-WAV path for the selected 1.7B full-ICL engine."
    - "[FLAGGED-UNVERIFIED] Never present hidden model loading, hidden prompt extraction, or whole-stream buffering as a ready live call."
---

<objective>
Land the leading production-quality tracer: one saved Faster Qwen3-TTS 1.7B voice loads and prewarms through RayMe, emits early native streamed call audio through the existing call surface, and cancels without late completion.

Purpose: Prove the hardest architectural constraint before broadening validation, migration, UI, and evidence work.
Output: Pinned dependency/identity, versioned worker protocol, supervised CUDA worker/adapter, one-hot readiness, and deterministic end-to-end slow-stream/cancel contracts.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/LIVE-CALL-INVARIANTS.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-CONTEXT.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-AI-SPEC.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-RESEARCH.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-PATTERNS.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/COVERAGE.md

<interfaces>
Use the existing `TtsSynthesisInput`, `TtsAudioChunk`, `TtsStreamingAdapter.stream()`, `EngineSwitchState`, `ModelManager.switch_tts_engine()`, `CallSession.speak_text()`, and strict `/webrtc/sessions/{session_id}/speak` boundary. Extend these contracts without adding an upstream/browser-facing Qwen service. The worker protocol source of truth is AI-SPEC section 4b.1.
</interfaces>
</context>

<planner_assumptions>
- Blank reference transcript, missing/invalid saved reference, and empty target text are rejected before prompt extraction/generation; empty target text cannot become fake successful speech.
- SHA-256 is computed over reference bytes. Lowercase lexical normalization that ignores punctuation/case is used only for alignment/cache comparison; the exact approved transcript is passed to full ICL.
- Exactly one active generation owns the worker. Every stream has request-scoped cancel and exactly one terminal event; queues are bounded.
- The locked live-call invariants and AI-SPEC streaming/cancellation/metrics tests are the REQ-45 acceptance definition.
</planner_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one saved 1.7B voice from load and prewarm through early call playback and cancel</name>
  <read_first>
    - ai-backend/app/models/tts_voxcpm2.py
    - ai-backend/app/models/tts_voxcpm2_worker.py
    - ai-backend/app/models/tts_registry.py
    - ai-backend/app/models/model_manager.py
    - ai-backend/app/call/session.py
    - ai-backend/app/api/webrtc.py
    - ai-backend/tests/test_tts_voxcpm2.py
    - ai-backend/tests/test_call_session.py
  </read_first>
  <files>web-ui/server/app/domain/call_service.py, web-ui/server/app/domain/ai_backend_client.py, web-ui/server/tests/test_calls.py, ai-backend/app/models/tts_qwen3_protocol.py, ai-backend/app/models/tts_qwen3_worker.py, ai-backend/app/models/tts_qwen3.py, ai-backend/app/models/tts_registry.py, ai-backend/app/models/model_manager.py, ai-backend/app/call/session.py, ai-backend/app/api/webrtc.py, ai-backend/tests/test_tts_qwen3.py, ai-backend/tests/test_model_manager.py, ai-backend/tests/test_call_session.py, ai-backend/tests/test_webrtc_signaling.py</files>
  <behavior>
    - D-01/D-03/D-05: RED-first fake-worker tests drive a production worker that is the only importer/owner of the pinned 1.7B Torch CUDA runtime and full-ICL prompt; the parent process stays CUDA-free before Windows spawn.
    - D-09/D-10: expose model `idle/loading/resident/unavailable` independently from selected-voice prompt `none/prewarming/ready/failed`; a fake slow load/prewarm leaves the event loop/status endpoint responsive.
    - D-12/D-13/D-14: route qwen3_1_7b by streaming capability, pull validated chunks without calling whole synthesis, and bridge the blocking stream through `asyncio.Queue(maxsize=2)` with a real blocking thread-to-loop put.
    - D-15: emit first-chunk/first-enqueue/ai_audio_started fields immediately and generation/playout totals only at terminal completion.
    - D-17/D-18: cancel by exact request id before/after first audio; stop playout, reject late chunks, accept one cancelled terminal or terminate after the hard deadline, restore listening/ended state, and suppress normal completion.
    - Resolve one canonical saved Qwen voice through the existing Web server call service/backend client into the load/prewarm/speak path so the tracer proves saved production data flow, not an adapter-only sample.
    - Preserve the existing VoxCPM2 slow-stream, no-whole-synthesis, immediate/final-carrier, and interrupt regressions unchanged.
  </behavior>
  <action>Write the RED fake-worker, saved-call fixture, and slow-stream/cancel assertions first, then implement the versioned IPC module, spawned CUDA worker, supervising adapter, asynchronous one-hot model/prompt readiness, and the smallest saved-voice Web server → `/webrtc` → CallSession integration that passes them. Use the existing Vox worker only as the lifecycle analog: the Qwen worker must keep a stdin reader able to set request-scoped cancellation while the generation thread pulls the native generator. Convert and enqueue every validated chunk immediately; generator exhaustion is the only normal completion authority. Keep the public RayMe API stable and keep compatibility migration, complete validation/provenance, UI, persistence expansion, and hardware evidence for later plans.</action>
  <reversibility rating="costly">This establishes a shared worker/readiness/cancel interface used by call, preview, status, and deployment code; later reversal would touch multiple boundaries, but the public RayMe API remains stable.</reversibility>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_tts_qwen3.py ai-backend/tests/test_model_manager.py ai-backend/tests/test_call_session.py ai-backend/tests/test_webrtc_signaling.py ai-backend/tests/test_tts_voxcpm2.py -q && uv run --project web-ui/server pytest web-ui/server/tests/test_calls.py -q</automated>
  </verify>
  <acceptance_criteria>
    - A fake saved voice reaches resident plus prompt-ready and begins outbound playback while the scripted producer is still incomplete.
    - The fake saved voice originates at the Web server's existing call service/client boundary and no Qwen-specific public browser route is added.
    - Queue high-water is at most two and the producer blocks under a slower consumer without dropped or reordered chunks.
    - Cancel-before-audio and cancel-after-audio enqueue no post-cancel chunks and emit no normal ai_done.
    - VoxCPM2 still rejects whole-synthesis fallback and begins slow-stream playback before completion.
  </acceptance_criteria>
  <done>The thin real architecture—saved voice input, model/prompt readiness, native chunks, call playback, and cancellation—works end to end under deterministic tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin truthful engine identity, dependency provenance, and one-hot runtime metadata</name>
  <read_first>
    - .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/COVERAGE.md
    - ai-backend/pyproject.toml
    - ai-backend/app/models/engine_metadata.py
    - ai-backend/tests/test_tts_registry.py
    - ai-backend/tests/test_health.py
    - scripts/deploy-omen.sh
  </read_first>
  <files>ai-backend/pyproject.toml, ai-backend/uv.lock, ai-backend/app/models/tts_registry.py, ai-backend/app/models/engine_metadata.py, ai-backend/tests/test_tts_registry.py, ai-backend/tests/test_health.py</files>
  <behavior>
    - D-01/D-02: replace the selectable `qwen3_0_6b` roster entry with truthful `qwen3_1_7b`, label `Qwen3-TTS 1.7B-Base`, `requires_transcript=True`, `supports_streaming=True`, and evidence-backed caveat/readiness metadata; retain the old value only in later compatibility code/tests.
    - Record the product-owner-supplied repository, tag, commit, PyPI source metadata, and young-package `[SUS]` reason; run the repository package/slop check and lock the immutable Git commit instead of a mutable version/source.
    - Keep the optional TTS dependency group compatible with qwen-tts 0.1.1 and transformers 4.57.3. Do not add an alternate server/backend or change the global TTS default.
    - D-03: health and registry contracts prove exactly one resident TTS engine and expose stable sanitized availability/readiness fields without raw model paths or exception text.
  </behavior>
  <action>Update the optional dependency group and lockfile with the immutable user-approved source commit after recording package legitimacy metadata and running the package/slop check. Replace the old selectable engine metadata across both backend rosters and their tests, without treating the historical identifier as a selectable alias or changing RayMe's current default engine. Add stable runtime/prompt readiness fields to health metadata only where the tracer consumes them; do not expose upstream server concepts or private local paths.</action>
  <verify>
    <automated>uv lock --project ai-backend --check && uv run --project ai-backend pytest ai-backend/tests/test_tts_registry.py ai-backend/tests/test_health.py ai-backend/tests/test_model_manager.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The lock resolves the exact Faster Qwen3-TTS Git commit and no selectable 0.6B adapter remains.
    - Registry/health return qwen3_1_7b with transcript and streaming capabilities while preserving one-hot residency and the existing default.
    - Dependency provenance evidence names repository owner, official source/tag/commit, and registry metadata before installation.
  </acceptance_criteria>
  <done>The selected runtime/model have an immutable dependency seam and a truthful, one-hot public RayMe identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (ASVS L1; block on high)

| Boundary | Description |
|---|---|
| FastAPI/model manager → worker stdin | Trusted application state becomes process commands carrying request and voice identities. |
| Worker stdout → adapter/call session | Untrusted/malformed process output becomes playable audio and terminal state. |
| Package/model cache → CUDA worker | External source artifacts become executable runtime/model state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Spoofing/Tampering | Worker events | high | mitigate | Versioned Pydantic events, exact request id, monotonic chunk index/time, one terminal, terminate on violation. |
| T-09-03 | Tampering/DoS | Base64 WAV chunks | high | mitigate | Strict base64, byte/sample-rate/duration/finite/silence/cumulative caps before `TtsAudioChunk`. |
| T-09-05 | DoS | Worker crash/hang | high | mitigate | Spawned-process isolation, bounded terminal/cancel deadlines, terminate/kill containment, Qwen-only health failure. |
| T-09-06 | Tampering | Runtime/model identity | critical | mitigate | Immutable source/model revisions, CUDA/device/version handshake, no CPU/model substitution, one-hot assertion. |
| T-09-08 | DoS | Chunk queue/prompt cache/output | critical | mitigate | Capacity-two bridge, capacity-one prompt ownership, one active generator, bounded output and terminal lifecycle. |
| T-09-SC | Tampering | Python dependency install | high | mitigate | Prior product-owner provenance approval plus immutable commit, metadata comparison, slopcheck, lock verification, and deployed attestation. |
</threat_model>

## Artifacts this phase produces

- New files: `tts_qwen3_protocol.py`, `tts_qwen3_worker.py`, `test_tts_qwen3.py`.
- New/changed symbols: canonical `qwen3_1_7b`; Qwen command/event models; supervised `Qwen3TtsAdapter`; model and prompt readiness fields; request-scoped `prewarm`/`stream`/`cancel` seams.
- New fields: model revision/runtime commit/CUDA attestation; prompt state/cache key timestamp; queue high-water and cancellation terminal metadata.
- Commands: focused model-free tracer pytest command and immutable `uv lock --project ai-backend --check` gate.

<verification>
- Run the focused tracer/registry/manager/call/WebRTC/VoxCPM2 regressions named above.
- Run `git diff --check`.
- Confirm no test imports/downloads the real model locally; hardware work remains Plan 09-06.
</verification>

<success_criteria>
One production-shaped saved Qwen 1.7B voice path is proven from visible load/prewarm through early live playback and prompt cancellation, with exact identity, bounded memory flow, and existing VoxCPM2 live-call guards intact.
</success_criteria>

<output>
Create `.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-01-SUMMARY.md` when done.
</output>
