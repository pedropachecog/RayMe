---
phase: 09-integrate-faster-qwen3-tts-1-7b-into-live-calls
plan: 04
type: execute
wave: 3
depends_on:
  - 09-03
files_modified:
  - web-ui/client/src/lib/api/types.ts
  - web-ui/client/src/lib/components/voice/TtsEnginePicker.svelte
  - web-ui/client/src/lib/components/voice/VoiceAssignmentSelect.svelte
  - web-ui/client/src/routes/voice-lab/+page.svelte
  - web-ui/client/src/routes/call/[threadId]/+page.svelte
  - web-ui/client/tests/unit/voice-lab.test.ts
  - web-ui/client/tests/unit/settings.test.ts
  - web-ui/client/tests/e2e/voice-lab.spec.ts
  - web-ui/client/tests/e2e/settings-connection.spec.ts
  - web-ui/client/tests/e2e/call-start.spec.ts
  - web-ui/client/tests/e2e/qwen3-readiness.spec.ts
autonomous: true
requirements:
  - REQ-22
  - REQ-45
  - REQ-46
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-02/D-04: Voice Lab, assignment controls, settings, and call UI show the canonical Qwen3-TTS 1.7B identity from RayMe metadata and never expose upstream runtime internals."
    - "D-06/D-07: The browser blocks Qwen save/preview/test-play until sample, exact editable transcript, and reference-permission confirmation exist, while server/backend remain the final trust boundaries."
    - "D-09/D-10/D-11: Users can see model Loading/Resident separately from voice Prewarming/Ready/Failed and can retry without losing their form or being told the call is ready early."
    - "D-19: Actionable fixed Qwen errors render without traceback, raw worker/model text, transcript, reference bytes, or private paths."
  artifacts:
    - path: "web-ui/client/tests/e2e/qwen3-readiness.spec.ts"
      provides: "Saved browser contract for visible model/prompt states, errors, and call startup gating"
    - path: "web-ui/client/src/routes/voice-lab/+page.svelte"
      provides: "First-class 1.7B voice save/preview/test-play and progress UI"
    - path: "web-ui/client/src/routes/call/[threadId]/+page.svelte"
      provides: "Honest selected-voice preparation state before Listening"
  key_links:
    - from: "web-ui/client/src/routes/voice-lab/+page.svelte"
      to: "web-ui/client/src/lib/api/types.ts"
      via: "server-driven qwen3_1_7b engine/model/prompt metadata"
      pattern: "qwen3_1_7b|prompt"
    - from: "web-ui/client/src/routes/call/[threadId]/+page.svelte"
      to: "RayMe call start/readiness response"
      via: "Connecting/preparing gate before Listening"
      pattern: "Connecting|prewarm|ready"
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Never present hidden loading, hidden prompt extraction, or whole buffering as a ready live call."
    - "[FLAGGED-UNVERIFIED] Never render or transmit a filesystem path, raw reference audio, full transcript, or upstream server control from browser UI."
---

<objective>
Make the selected 1.7B engine and its readiness honest and usable in RayMe's existing Voice Lab, saved-voice controls, Settings fixtures, and call startup surface.

Purpose: The user must see what is loading/prewarming, correct reference problems, and know when a saved Qwen voice is genuinely ready before a call.
Output: Canonical client contracts, narrow existing-design-system controls, progress/error states, and saved Playwright coverage.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-03-SUMMARY.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-CONTEXT.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-PATTERNS.md
@docs/stitch/DESIGN.md

<interfaces>
Extend existing `TtsEngineId`, `TtsEngineMetadata`, AI backend status, voice payload/summary, preview/test-play, and `startCall` response types. Keep `TtsEnginePicker` metadata-driven. Preserve the call route's current Connecting gate and explicit visualizer states; readiness augments those states rather than redesigning the call screen.
</interfaces>
</context>

<planner_assumptions>
- The server is authoritative; client validation improves the experience but cannot authorize a reference by itself.
- Server-returned canonical engines are preserved, not filtered to a hard-coded fallback order. Legacy ids may exist only in explicit compatibility fixtures.
- Physical call acceptance is not requested in this plan; saved Playwright behavior is the automated UI gate before OMEN deployment.
</planner_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace stale engine identity and type separate model/prompt readiness</name>
  <read_first>
    - web-ui/client/src/lib/api/types.ts
    - web-ui/client/src/lib/components/voice/TtsEnginePicker.svelte
    - web-ui/client/src/lib/components/voice/VoiceAssignmentSelect.svelte
    - web-ui/client/src/routes/voice-lab/+page.svelte
    - web-ui/client/tests/unit/voice-lab.test.ts
    - web-ui/client/tests/unit/settings.test.ts
  </read_first>
  <files>web-ui/client/src/lib/api/types.ts, web-ui/client/src/lib/components/voice/TtsEnginePicker.svelte, web-ui/client/src/lib/components/voice/VoiceAssignmentSelect.svelte, web-ui/client/src/routes/voice-lab/+page.svelte, web-ui/client/tests/unit/voice-lab.test.ts, web-ui/client/tests/unit/settings.test.ts, web-ui/client/tests/e2e/settings-connection.spec.ts</files>
  <behavior>
    - D-02: replace the selectable client union/labels/fallback fixtures with qwen3_1_7b and preserve additional canonical server-returned engines instead of dropping them during normalization.
    - Type/model the backend's separate model and selected-voice prompt states, timestamps, and stable sanitized error codes; do not collapse them to one `available`/boolean or expose model paths/runtime server details.
    - D-06/D-07: for Qwen only, require sample asset, nonblank editable transcript, and an explicit private-LAN permission confirmation before save/preview/test-play controls enable; do not silently alter the user's transcript.
    - Preserve row-scoped progress and unrelated Voice Library/engine controls while one Qwen operation is loading/prewarming.
  </behavior>
  <action>Write unit assertions for canonical identity, dynamic metadata preservation, Qwen transcript/provenance gates, and independent model/prompt states. Then update the shared types, engine cards/assignment labels, Voice Lab fallback roster, normalization, and operation-state derivation using existing True Dark components/tokens. Keep non-Qwen payloads free of Qwen-specific provenance/readiness fields except generic engine status metadata.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run</automated>
  </verify>
  <acceptance_criteria>
    - qwen3_1_7b renders as Qwen3-TTS 1.7B-Base and old identity does not appear outside compatibility/migration tests.
    - A canonical server-returned engine not present in local fallback metadata remains visible.
    - Missing transcript/sample/permission blocks only the invalid Qwen action and preserves the form.
    - Loading/Resident and Prewarming/Ready/Failed remain separately typed and rendered.
  </acceptance_criteria>
  <done>Client identity and readiness types reflect the real backend contract and Voice Lab can form an authorized saved 1.7B voice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render retryable Voice Lab progress and honest call preparation in saved Playwright flows</name>
  <read_first>
    - web-ui/client/src/routes/voice-lab/+page.svelte
    - web-ui/client/src/routes/call/[threadId]/+page.svelte
    - web-ui/client/tests/e2e/voice-lab.spec.ts
    - web-ui/client/tests/e2e/call-start.spec.ts
    - web-ui/client/tests/e2e/helpers/acceptance.ts
  </read_first>
  <files>web-ui/client/src/routes/voice-lab/+page.svelte, web-ui/client/src/routes/call/[threadId]/+page.svelte, web-ui/client/tests/e2e/voice-lab.spec.ts, web-ui/client/tests/e2e/call-start.spec.ts, web-ui/client/tests/e2e/qwen3-readiness.spec.ts</files>
  <behavior>
    - D-09/D-10/D-11: render model loading/resident and prompt prewarming/ready/failed in Voice Lab preview/test-play and selected-voice call startup; allow status polling/retry and keep the route responsive during delayed mocked operations.
    - Do not enter/show Listening for a selected Qwen voice until call preparation says model resident and prompt ready; keep Connecting/Preparing honest, then preserve normal call states and controls.
    - Render stable actionable transcript-required, mismatch, prewarm, ceiling, worker/runtime codes using fixed copy with no private/raw details.
    - Preserve 44px mobile controls, existing design tokens, browser error guard, preview-optional save policy, and unrelated engine behavior.
  </behavior>
  <action>Add a focused saved Playwright spec that drives one Qwen voice through loading, resident, prewarming, ready, failure/retry, and call Connecting-to-Listening. Mock only RayMe routes and assert no console/page errors. Update the route/components just enough to satisfy the locked visible-state contract; do not create a separate dashboard or upstream-specific browser endpoint. Keep delayed promises controllable so the test proves the UI stays responsive and does not claim readiness early.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:e2e -- qwen3-readiness.spec.ts voice-lab.spec.ts call-start.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The saved browser test visibly observes each model/prompt state and retry without form loss or console errors.
    - The call route remains Connecting/Preparing until selected Qwen prompt readiness, then enters Listening without changing other call controls.
    - Sanitized failures are readable and do not render raw backend/worker/private content.
    - Existing Voice Lab and call-start Playwright specs remain green on desktop/mobile-sensitive paths.
  </acceptance_criteria>
  <done>The browser accurately shows when Qwen is loading, prewarming, ready, or failed and never hands a hidden cold path to the caller.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (ASVS L1; block on high)

| Boundary | Description |
|---|---|
| RayMe API response → browser UI | Status/error/voice metadata becomes visible product state. |
| Browser form → RayMe voice/call API | User transcript, permission, voice id, and target text cross the public LAN boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-02 | Elevation | Browser voice selection | high | mitigate | Browser sends opaque saved voice id only; server resolves/authorizes contained asset. |
| T-09-04 | DoS | Transcript/target form | medium | mitigate | Qwen-only nonblank client gate backed by authoritative server/backend validation. |
| T-09-07 | Information Disclosure | Rendered status/errors | high | mitigate | Stable typed fields/fixed copy, no raw worker text, paths, transcript, base64, or provider URL. |
| T-09-08 | DoS | Readiness polling/UI state | medium | mitigate | Bounded polling/retry, row-scoped progress, no unbounded client cache or duplicate prewarm submission. |
</threat_model>

## Artifacts this phase produces

- New file: `web-ui/client/tests/e2e/qwen3-readiness.spec.ts`.
- New/changed symbols: canonical `TtsEngineId` member; model/prompt readiness types; Qwen readiness display/validation state; call preparation gate.
- New metadata fields: client shape for reference provenance/permission and model/prompt readiness timestamps/error codes.
- Commands: focused Qwen readiness/Voice Lab/call-start Playwright run and full client unit run.

<verification>
- Run both commands above, `npm --prefix web-ui/client run check`, and `git diff --check`.
- Save the actual deployed Playwright result in Plan 09-06; this plan creates the durable test itself.
</verification>

<success_criteria>
The user can select/save a truthful 1.7B voice, understand and retry load/prewarm failures, and see a call become ready only when the selected model and prompt truly are ready.
</success_criteria>

<output>
Create `.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-04-SUMMARY.md` when done.
</output>
