---
phase: 09-integrate-faster-qwen3-tts-1-7b-into-live-calls
plan: 04
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - scripts/deploy-omen.sh
  - .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-run-hardware-tracer.py
  - .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/results/qwen3-hardware-tracer.json
  - .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/results/.gitignore
autonomous: true
requirements: [REQ-22, REQ-45, REQ-46]
estimate: {tokens: 60000, raw_tokens: 60000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-01/D-03/D-22: Canonical deploy installs and attests the exact CUDA runtime/model on OMEN at the deployed commit."
    - "D-05/D-09/D-10: A hash-bound authorized saved Qwen reference, or the deterministic non-person fallback, created/resolved through production RayMe APIs reaches resident and prompt-ready."
    - "D-12/D-13/D-14/D-15: Real CallSession/WebRTC playout starts before the real native producer completes with bounded bridge and separate carriers."
    - "D-17/D-18: Request-scoped interrupt after first audio stops the real worker, rejects late audio and normal completion, and recovers."
    - "This plan is a hard gate: no migration, UI, broad call-pipeline, or release-evidence expansion may proceed if the real tracer fails."
  artifacts:
    - {path: "scripts/deploy-omen.sh", provides: "Canonical Qwen install/snapshot/launcher/deploy seam"}
    - {path: ".planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-run-hardware-tracer.py", provides: "Production API saved-voice/CallSession/WebRTC tracer"}
    - {path: ".planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/results/qwen3-hardware-tracer.json", provides: "Commit-matched hard-gate evidence and frozen integrated scorer baseline inputs"}
  key_links:
    - from: "scripts/deploy-omen.sh"
      to: ".planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-run-hardware-tracer.py"
      via: "RAYME_OMEN_VERIFY_QWEN3_TRACER=1"
      pattern: "QWEN3_TRACER"
    - from: ".planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-run-hardware-tracer.py"
      to: "web-ui/server/app/api/voices.py"
      via: "_resolve_authorized_reference validates steward/basis/scope plus reference/transcript SHA-256 sidecar or selects synthetic fallback before production voice upload/save"
      pattern: "reference_sha256|transcript_sha256|use_scope"
---

<objective>
Run the early production-path hardware tracer before broad implementation proceeds.

Purpose: Prove the real pinned adapter, one-hot manager, authorized saved voice, CallSession/WebRTC early playout, and request cancellation on OMEN while architectural changes are still narrow.
Output: Canonical deploy support, hash-bound authorization/provenance preflight with deterministic non-person fallback, real normal/cancel traces, and a commit-matched hard-gate artifact.
</objective>

<execution_context>
@/home/agent/.codex/gsd-core/workflows/execute-plan.md
@/home/agent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/LIVE-CALL-INVARIANTS.md
@.planning/OPERATING-NOTES.md
@.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-03-SUMMARY.md
@.planning/spikes/005-faster-qwen3-tts-longitudinal-quality/HUMAN-LISTENING-CHECKPOINT.md
@scripts/deploy-omen.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exact Qwen install, snapshot, launcher, and tracer support only to canonical deploy</name>
  <files>scripts/deploy-omen.sh, .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-run-hardware-tracer.py, .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/results/.gitignore</files>
  <behavior>
    - D-01/D-03: deploy flag installs source commit a70afc0f81f7f5f8801c3227968f1102f43f211c, reasserts Torch 2.10.0+cu126/CUDA 12.6, materializes model revision fd4b254389122332181a7c3db7f27e918eec64e3, and passes only its verified local path through the canonical AI launcher.
    - D-22: preserve canonical checkout/HEAD verification, start-ai-backend.cmd/start-web-ui.cmd, RayMePhase1AI/RayMePhase1Web, TLS, ports, visible launchers, and existing Vox verification; create no alternate deploy/launcher/task path.
    - A Phase 005 real-person reference is eligible only when a co-located hash-bound authorization/provenance sidecar explicitly contains `voice_data_steward` (speaker/data-steward name or opaque steward id), `authorization_basis`, `use_scope=rayme_lan_call_testing`, `reference_sha256`, and `transcript_sha256`, and both hashes match the exact local reference/transcript bytes. Product-owner direction or prior listening alone is never treated as speaker/data-steward authorization.
    - Missing, malformed, wrong-reference-hash, wrong-transcript-hash, or wrong-scope Phase 005 metadata automatically selects a deterministic Windows SAPI mechanical reference generated locally on OMEN with fixed transcript. The synthetic sidecar records `generated_non_person_fixture` and its exact hashes/scope; it does not claim real-person permission. Generate a separate deterministic SAPI fake-microphone WAV for the final live browser call.
    - Upload/save through production `/api/voices/assets` and `/api/voices`; record only opaque voice id, steward id, authorization basis, intended scope, reference SHA-256, and transcript SHA-256. Delete temporary audio/transcript after upload; local copied fixtures/sidecars/results remain gitignored and no private audio/transcript is committed.
    - Deterministic self-tests cover authorized Phase 005 selection plus missing, malformed, wrong-reference-hash, wrong-transcript-hash, and wrong-scope sidecars, proving every invalid real-person case selects SAPI automatically.
  </behavior>
  <action>Extend `scripts/deploy-omen.sh` under `RAYME_OMEN_VERIFY_QWEN3_TRACER=1` as the sole remote install/launcher/evidence path. Implement `_resolve_authorized_reference` and its self-test CLI in the committed Python tracer; validate the exact sidecar schema and file hashes before any Phase 005 bytes are opened for upload, and choose SAPI on every invalid case. The runner may invoke built-in Windows SAPI through PowerShell for the synthetic fallback but must not write a deploy script/launcher/task outside the repository. If SAPI generation itself fails, stop with an execution checkpoint rather than inventing permission or using arbitrary audio.</action>
  <verify><automated>bash -n scripts/deploy-omen.sh &amp;&amp; python3 -m py_compile .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-run-hardware-tracer.py &amp;&amp; python3 .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-run-hardware-tracer.py --self-test-reference-authorization</automated></verify>
  <done>The canonical path uses Phase 005 only with matching steward/basis/scope/hash authorization; all invalid metadata cases automatically provision the deterministic non-person reference and fake microphone.</done>
</task>

<task type="tracer" tdd="false">
  <name>Task 2: Deploy and prove the real saved-voice early-stream and cancel path</name>
  <precondition>The Plan 09-01 through 09-04 implementation is committed in a clean pushable checkout on the canonical deployment branch, origin is reachable, OMEN is reachable as `rayme-pmpg`, and the canonical deploy prerequisites are ready.</precondition>
  <files>.planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/results/qwen3-hardware-tracer.json</files>
  <behavior>
    - Verify deployed HEAD equals intended SHA; status attests exact runtime/model/CUDA/RTX 3060, one resident TTS, and separately observable loading/resident plus prewarming/ready.
    - Use the saved fixture voice through production RayMe API → ModelManager → Qwen adapter → CallSession/WebRTC; do not call a model-only spike entry point.
    - Normal real stream records first native chunk/enqueue/playout before producer completion, bridge high-water at most two, no whole synthesis/fallback, natural EOS, and separate immediate/final fields.
    - Interrupt a second request after first audio and require worker acknowledgement under two seconds, zero post-cancel enqueue/audio, no normal ai_done/complete speech, and a successful recovery request.
    - Produce three local-only baseline outputs (short/medium/long) from the same integrated path for later pinned speaker scoring; committed JSON stores only hashes, bucket ids, and scalar runtime/control data.
  </behavior>
  <action>Record `intended_sha=$(git rev-parse HEAD)` and `deployment_branch=${OMEN_BRANCH:-main}`, push explicitly with `git push origin "HEAD:refs/heads/$deployment_branch"`, then resolve `remote_sha` from `git ls-remote origin "refs/heads/$deployment_branch"` and assert it equals `intended_sha`. Only after that verified remote-SHA gate passes, run `RAYME_OMEN_VERIFY_QWEN3_TRACER=1 scripts/deploy-omen.sh` and let the canonical path invoke/copy back the tracer evidence; the deploy script's existing expected-HEAD check must also prove OMEN checked out the same commit. Independently assert commit identity, hash-bound sidecar/fallback decision, and raw event ordering. Stop phase execution immediately on any push/remote-SHA, identity, saved-voice authorization, readiness, first-before-completion, fallback, queue, cancellation, or recovery failure; Plans 09-05 onward all depend on this gate.</action>
  <verify><automated>intended_sha="$(git rev-parse HEAD)" &amp;&amp; deployment_branch="${OMEN_BRANCH:-main}" &amp;&amp; remote_sha="$(git ls-remote origin "refs/heads/$deployment_branch" | awk 'NR == 1 { print $1 }')" &amp;&amp; test "$remote_sha" = "$intended_sha" &amp;&amp; RAYME_OMEN_VERIFY_QWEN3_TRACER=1 scripts/deploy-omen.sh &amp;&amp; python3 .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/09-run-hardware-tracer.py --verify-results .planning/phases/09-integrate-faster-qwen3-tts-1-7b-into-live-calls/results/qwen3-hardware-tracer.json --expected-commit "$intended_sha"</automated></verify>
  <done>The real production saved-voice call path starts audio early, cancels cleanly, and recovers on exact pinned OMEN hardware.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-02 | Elevation/Disclosure | Reference fixture | critical | mitigate | Hash-bound speaker/data-steward authorization sidecar is mandatory for Phase 005; missing/malformed/hash/scope mismatch automatically selects generated non-person SAPI fallback. |
| T-09-06 | Tampering | Deployed runtime/model | critical | mitigate | Exact source/snapshot/CUDA/deployed-SHA attestation in canonical deploy. |
| T-09-08 | DoS | Real queues/worker | critical | mitigate | First-before-completion, high-water, cancel watchdog, and recovery hard gates. |
</threat_model>

<verification>Task 2 is the hard gate; no later plan runs unless its exact committed artifact passes.</verification>
<success_criteria>A real user-shaped saved voice reaches audible early WebRTC playout and request-scoped cancellation on OMEN before broad implementation.</success_criteria>
<output>Create `09-04-SUMMARY.md`.</output>
