---
phase: 02-ai-backend-skeleton-voice-lab
plan: "12"
type: execute
wave: 4
depends_on: ["02-03", "02-09", "02-08"]
files_modified:
  - web-ui/client/src/lib/api/types.ts
  - web-ui/client/src/lib/api/voices.ts
  - web-ui/client/src/lib/components/voice/AudioSampleDropzone.svelte
  - web-ui/client/src/lib/components/voice/TranscriptEditor.svelte
  - web-ui/client/src/lib/components/voice/TtsEnginePicker.svelte
  - web-ui/client/src/lib/components/voice/SynthPreviewPanel.svelte
  - web-ui/client/src/routes/voice-lab/+page.svelte
  - web-ui/client/tests/unit/voice-lab.test.ts
  - web-ui/client/tests/e2e/voice-lab.spec.ts
autonomous: true
requirements: [REQ-20, REQ-21, REQ-22, REQ-90]
user_setup: []
must_haves:
  truths:
    - "User can upload a sample, get or enter a transcript, choose any supported engine, optionally preview, and save."
    - "Save Voice is available without a successful preview when name, sample, transcript, and engine are valid."
  artifacts:
    - path: "web-ui/client/src/routes/voice-lab/+page.svelte"
      provides: "Voice Lab creation workflow"
    - path: "web-ui/client/src/lib/api/voices.ts"
      provides: "Client wrapper for voice APIs"
    - path: "web-ui/client/src/lib/components/voice/TtsEnginePicker.svelte"
      provides: "Full-roster metadata-driven engine picker"
  key_links:
    - from: "web-ui/client/src/routes/voice-lab/+page.svelte"
      to: "web-ui/client/src/lib/api/voices.ts"
      via: "upload/transcribe/preview/save calls"
      pattern: "saveVoice"
---

<objective>
Build the client Voice Lab creation workflow: upload, transcript, engine picker, optional preview, and save.

Purpose: Voice Lab must produce saved voices for Phase 3 without treating preview as a gate and without hard-coding a three-engine UI.
Output: Voice Lab route, client API wrappers, and creation-flow components.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-DECISION-COVERAGE.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-UI-SPEC.md
@web-ui/client/src/lib/api/client.ts
@web-ui/client/src/lib/api/types.ts
@web-ui/client/tests/unit/voice-lab.test.ts
@web-ui/client/tests/e2e/voice-lab.spec.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add voice client types and API wrappers</name>
  <read_first>
    web-ui/client/src/lib/api/types.ts
    web-ui/client/src/lib/api/client.ts
    web-ui/client/tests/unit/api.test.ts
    web-ui/client/tests/unit/voice-lab.test.ts
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
  </read_first>
  <files>web-ui/client/src/lib/api/types.ts, web-ui/client/src/lib/api/voices.ts, web-ui/client/tests/unit/voice-lab.test.ts</files>
  <action>Add types `TtsEngineId`, `TtsEngineMetadata`, `VoiceAsset`, `VoiceSummary`, `VoiceDetail`, `VoiceSavePayload`, `VoicePreviewPayload`, `VoiceTestPlayPayload`, `VoiceSynthesisResult`, and `VoiceDeleteResult`. Create `voices.ts` wrappers: `uploadVoiceAsset(file)`, `transcribeVoiceAsset(assetId)`, `previewVoice(payload)`, `saveVoice(payload)`, `listVoices()`, `getVoice(voiceId)`, `renameVoice(voiceId, name)`, `deleteVoice(voiceId, force)`, and `testPlayVoice(voiceId, payload)`. Use FormData for upload and `apiFetch` for all routes; reject absolute URLs through existing `apiFetch` behavior.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run tests/unit/voice-lab.test.ts tests/unit/api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "TtsEngineMetadata|uploadVoiceAsset|transcribeVoiceAsset|previewVoice|saveVoice|listVoices|renameVoice|deleteVoice|testPlayVoice|/voices/assets|/voices/preview|/test-play" web-ui/client/src/lib/api web-ui/client/tests/unit/voice-lab.test.ts` finds all strings.
    - Tests assert `deleteVoice("voice 1", true)` calls `/api/voices/voice%201?force=true`.
  </acceptance_criteria>
  <done>Client has typed wrappers for the Voice Lab and Voice Library server API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Voice Lab creation route and components</name>
  <read_first>
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-UI-SPEC.md
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
    web-ui/client/src/app.css
    web-ui/client/src/lib/components/StatusChip.svelte
    web-ui/client/src/lib/components/ToastStack.svelte
    web-ui/client/tests/unit/voice-lab.test.ts
    web-ui/client/tests/e2e/voice-lab.spec.ts
  </read_first>
  <files>web-ui/client/src/lib/components/voice/AudioSampleDropzone.svelte, web-ui/client/src/lib/components/voice/TranscriptEditor.svelte, web-ui/client/src/lib/components/voice/TtsEnginePicker.svelte, web-ui/client/src/lib/components/voice/SynthPreviewPanel.svelte, web-ui/client/src/routes/voice-lab/+page.svelte, web-ui/client/tests/unit/voice-lab.test.ts, web-ui/client/tests/e2e/voice-lab.spec.ts</files>
  <action>Create Voice Lab creation UI with exact step labels `1 Upload`, `2 Transcript`, `3 Engine`, `4 Preview`, `5 Save`. `AudioSampleDropzone` accepts `.wav,.mp3,.flac`, keeps stable dimensions, and shows 6-15 second guidance. `TranscriptEditor` supports pending, error, retry, and manual transcript entry without re-upload. `TtsEnginePicker` renders engine options from metadata returned by API/status, including all six labels and concise caveat chips. `SynthPreviewPanel` includes `Use default engine`, editable preview text, `Preview Voice`, audio play/pause, retry, and error state. The route enables `Save Voice` when sample asset, non-empty name, non-empty transcript, and selected engine exist; it must not require preview success and must preserve fields after preview failure.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run tests/unit/voice-lab.test.ts && npm --prefix web-ui/client run test:e2e -- voice-lab.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "1 Upload|2 Transcript|3 Engine|4 Preview|5 Save|Upload Sample|Transcribe Sample|Use default engine|Preview Voice|Save Voice|F5-TTS|XTTS v2|Qwen3-TTS 0.6B-Base|LuxTTS|Chatterbox Turbo|TADA 1B" web-ui/client/src/routes/voice-lab web-ui/client/src/lib/components/voice web-ui/client/tests` finds all labels.
    - Playwright proves save succeeds after a preview route returns HTTP 502.
    - No horizontal scroll appears at 320px viewport in `voice-lab.spec.ts`.
  </acceptance_criteria>
  <done>Voice Lab creation flow is usable, metadata-driven, and preview-optional.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high
- phase_controls: ASVS V5 input validation, V6 TLS, V8 data protection, V10 malicious code, V12 file/resource safety, V14 configuration
- execution_rule: any high-severity gap in this threat model or its listed mitigations blocks plan completion.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser file picker to Voice Lab | Audio file metadata and bytes are user-controlled. |
| Voice transcript/name to DOM | User-controlled text appears in the app UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12-01 | Elevation of Privilege | Transcript/name rendering | mitigate | Render values as Svelte text bindings, not raw HTML. |
| T-02-12-02 | Tampering | Engine picker | mitigate | Render from backend metadata and include all six engines. |
| T-02-12-03 | Denial of Service | Upload UX | mitigate | Client rejects unsupported extensions before upload; server remains authoritative. |
| T-02-12-04 | Information Disclosure | Preview errors | mitigate | Show operation-specific user copy, not backend tracebacks. |
</threat_model>

<verification>
Run `npm --prefix web-ui/client run test:unit -- --run tests/unit/voice-lab.test.ts && npm --prefix web-ui/client run test:e2e -- voice-lab.spec.ts`.
</verification>

<success_criteria>
The Voice Lab creation screen completes upload to save with all six engines visible and `Save Voice` independent from preview success.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-backend-skeleton-voice-lab/02-12-SUMMARY.md`.
</output>
