---
phase: 02-ai-backend-skeleton-voice-lab
plan: "14"
type: execute
wave: 3
depends_on: ["02-03", "02-10"]
files_modified:
  - web-ui/client/src/lib/api/types.ts
  - web-ui/client/src/lib/api/settings.ts
  - web-ui/client/src/lib/components/AppShell.svelte
  - web-ui/client/src/lib/components/EndpointSettingsPanel.svelte
  - web-ui/client/src/lib/components/settings/AudioSettingsPanel.svelte
  - web-ui/client/src/lib/components/settings/VadSettingsPanel.svelte
  - web-ui/client/src/routes/settings/+page.svelte
  - web-ui/client/tests/unit/settings.test.ts
  - web-ui/client/tests/unit/app-shell.test.ts
  - web-ui/client/tests/e2e/settings-connection.spec.ts
autonomous: true
requirements: [REQ-02, REQ-05, REQ-80, REQ-90, REQ-A3]
user_setup: []
must_haves:
  truths:
    - "Settings lets the user save audio defaults and VAD values while preserving save-before-test."
    - "Settings displays compact AI backend residency status."
    - "Voice Lab is a real top-level destination once implemented."
  artifacts:
    - path: "web-ui/client/src/routes/settings/+page.svelte"
      provides: "Phase 2 Settings UI"
    - path: "web-ui/client/src/lib/components/AppShell.svelte"
      provides: "Top-level Voice Lab navigation"
  key_links:
    - from: "web-ui/client/src/routes/settings/+page.svelte"
      to: "web-ui/client/src/lib/api/settings.ts"
      via: "get/update/test wrappers"
      pattern: "save_ai_audio"
---

<objective>
Extend the client Settings surface and app navigation for Phase 2.

Purpose: Settings must expose endpoint tests, AI backend residency, save-audio defaults, and VAD values without pretending call behavior is implemented in Phase 2.
Output: Settings UI panels, client types/wrappers, and Voice Lab navigation.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-DECISION-COVERAGE.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-UI-SPEC.md
@web-ui/client/src/routes/settings/+page.svelte
@web-ui/client/src/lib/components/AppShell.svelte
@web-ui/client/src/lib/components/EndpointSettingsPanel.svelte
@web-ui/client/tests/e2e/settings-connection.spec.ts
@web-ui/client/tests/unit/settings.test.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend Settings types, route, and panels</name>
  <read_first>
    web-ui/client/src/lib/api/types.ts
    web-ui/client/src/lib/api/settings.ts
    web-ui/client/src/routes/settings/+page.svelte
    web-ui/client/src/lib/components/EndpointSettingsPanel.svelte
    web-ui/client/tests/unit/settings.test.ts
    web-ui/client/tests/e2e/settings-connection.spec.ts
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-UI-SPEC.md
  </read_first>
  <files>web-ui/client/src/lib/api/types.ts, web-ui/client/src/lib/api/settings.ts, web-ui/client/src/lib/components/EndpointSettingsPanel.svelte, web-ui/client/src/lib/components/settings/AudioSettingsPanel.svelte, web-ui/client/src/lib/components/settings/VadSettingsPanel.svelte, web-ui/client/src/routes/settings/+page.svelte, web-ui/client/tests/unit/settings.test.ts, web-ui/client/tests/e2e/settings-connection.spec.ts</files>
  <action>Extend `SettingsPayload`/`SettingsUpdatePayload` with `save_ai_audio`, `save_mic_audio`, `vad_threshold`, `vad_end_silence_ms`, `stt_model`, `tts_default_engine`, and `ai_backend_status`. Add `AudioSettingsPanel` with toggles `Save AI audio` default on and `Save mic audio` default off; mic helper text exactly `Off by default; future calls will not store your microphone audio unless enabled.` Add `VadSettingsPanel` with labels `VAD threshold`, `End-of-utterance silence`, and status text `Coming in Call Feel`; controls persist values but copy states call behavior belongs to Call Feel. Extend `EndpointSettingsPanel` or Settings route to show `STT model`, `VAD ready`, `Resident TTS engine`, `Available engines`, `Loading engine`, and `VRAM headroom` from `ai_backend_status`.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run tests/unit/settings.test.ts && npm --prefix web-ui/client run test:e2e -- settings-connection.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "Save AI audio|Save mic audio|Off by default; future calls will not store your microphone audio unless enabled\\.|VAD threshold|End-of-utterance silence|Coming in Call Feel|Resident TTS engine|Available engines|VRAM headroom|save_ai_audio|save_mic_audio" web-ui/client/src web-ui/client/tests` finds all strings.
    - Playwright still proves PATCH `/api/settings` occurs immediately before every `/api/settings/test/*` POST.
  </acceptance_criteria>
  <done>Settings UI exposes Phase 2 audio/VAD/status fields and preserves save-before-test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Voice Lab navigation</name>
  <read_first>
    web-ui/client/src/lib/components/AppShell.svelte
    web-ui/client/tests/unit/app-shell.test.ts
    web-ui/client/tests/e2e/ui-contract.spec.ts
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-UI-SPEC.md
  </read_first>
  <files>web-ui/client/src/lib/components/AppShell.svelte, web-ui/client/tests/unit/app-shell.test.ts, web-ui/client/tests/e2e/ui-contract.spec.ts</files>
  <action>Add a real top-level `Voice Lab` navigation item with href `/voice-lab` between Gallery and Settings, using a lucide `FileAudio` or `Mic` icon. Update desktop rail and mobile bottom nav grid to four items with stable 64px bottom nav height. Do not add Call navigation or disabled call controls.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run tests/unit/app-shell.test.ts && npm --prefix web-ui/client run test:e2e -- ui-contract.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "Voice Lab|/voice-lab|FileAudio|Mic|repeat\\(4, minmax\\(0, 1fr\\)\\)" web-ui/client/src/lib/components/AppShell.svelte web-ui/client/tests/unit/app-shell.test.ts web-ui/client/tests/e2e/ui-contract.spec.ts` finds navigation terms.
    - `rg "Call" web-ui/client/src/lib/components/AppShell.svelte` returns no call navigation label.
  </acceptance_criteria>
  <done>Voice Lab is reachable from app navigation and Phase 3 call navigation is not introduced.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high
- phase_controls: ASVS V5 input validation, V6 TLS, V8 data protection, V10 malicious code, V12 file/resource safety, V14 configuration
- execution_rule: any high-severity gap in this threat model or its listed mitigations blocks plan completion.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Settings form to API | Endpoint URLs, save-audio toggles, VAD values, and LLM key input cross into server settings. |
| AI backend status to browser | Runtime status is displayed to the user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14-01 | Information Disclosure | Settings UI | mitigate | Continue masking API keys and never render raw backend exception text. |
| T-02-14-02 | Tampering | VAD controls | mitigate | Client controls use numeric bounds matching server validation. |
| T-02-14-03 | Information Disclosure | Audio defaults | mitigate | Mic audio default is visibly off with privacy helper text. |
| T-02-14-04 | Spoofing | Navigation | accept | Top-level navigation is local UI state; routes remain protected by server API validation. |
</threat_model>

<verification>
Run `npm --prefix web-ui/client run test:unit -- --run tests/unit/settings.test.ts tests/unit/app-shell.test.ts && npm --prefix web-ui/client run test:e2e -- settings-connection.spec.ts ui-contract.spec.ts`.
</verification>

<success_criteria>
Settings and navigation reflect Phase 2 controls, save-before-test remains intact, and call-phase UI remains out of scope.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-backend-skeleton-voice-lab/02-14-SUMMARY.md`.
</output>
