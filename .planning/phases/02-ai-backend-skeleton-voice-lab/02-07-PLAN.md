---
phase: 02-ai-backend-skeleton-voice-lab
plan: "07"
type: execute
wave: 2
depends_on: ["02-06"]
files_modified:
  - ai-backend/app/audio/__init__.py
  - ai-backend/app/audio/io.py
  - ai-backend/app/audio/filters.py
  - ai-backend/app/models/vad.py
  - ai-backend/app/models/stt.py
  - ai-backend/app/api/stt.py
  - ai-backend/app/main.py
  - ai-backend/tests/test_stt.py
autonomous: true
requirements: [REQ-02, REQ-21, REQ-A3]
user_setup: []
must_haves:
  truths:
    - "Uploaded samples can be transcribed through English-only STT after VAD gating."
    - "STT failure allows retry/manual transcript flow and does not require re-upload."
  artifacts:
    - path: "ai-backend/app/models/stt.py"
      provides: "faster-whisper transcription adapter"
    - path: "ai-backend/app/models/vad.py"
      provides: "Silero VAD readiness and speech gate"
    - path: "ai-backend/app/api/stt.py"
      provides: "Transient transcription endpoint"
  key_links:
    - from: "ai-backend/app/api/stt.py"
      to: "ai-backend/app/models/stt.py"
      via: "transcribe endpoint"
      pattern: "condition_on_previous_text=False"
---

<objective>
Implement AI backend STT/VAD processing for Voice Lab sample transcription.

Purpose: Voice Lab needs auto-transcripts while avoiding Whisper hallucination and preserving a retry/manual transcript fallback.
Output: Audio decode helpers, VAD adapter, STT adapter, and `/stt/transcribe` route.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-DECISION-COVERAGE.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-RESEARCH.md
@.planning/phases/00-measurement-gate/KEY_DECISIONS.md
@ai-backend/app/models/model_manager.py
@ai-backend/app/main.py
@ai-backend/tests/test_stt.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add audio IO, VAD, and STT adapters</name>
  <read_first>
    ai-backend/app/models/model_manager.py
    ai-backend/tests/test_stt.py
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-RESEARCH.md
    .planning/phases/00-measurement-gate/KEY_DECISIONS.md
    MISSING: ai-backend/app/models/stt.py
    MISSING: ai-backend/app/models/vad.py
  </read_first>
  <files>ai-backend/app/audio/__init__.py, ai-backend/app/audio/io.py, ai-backend/app/audio/filters.py, ai-backend/app/models/vad.py, ai-backend/app/models/stt.py, ai-backend/tests/test_stt.py</files>
  <action>Create audio helpers that normalize uploaded bytes into a temporary WAV path or in-memory array without trusting the original filename. Create `SileroVadAdapter` with `ready`, `threshold`, `end_silence_ms`, and `speech_timestamps(audio)` API. Create `WhisperSttAdapter` using faster-whisper `WhisperModel("distil-large-v3", device="cuda", compute_type="int8_float16")` when real models are enabled and fake adapters in unit tests. The transcription call must set `language="en"`, `task="transcribe"`, `condition_on_previous_text=False`, `beam_size=5`, and VAD parameters from settings. Add `HALLUCINATION_BLOCKLIST` with exact phrases `thank you for watching`, `thanks for watching`, and `subscribe to my channel`; filtered responses return `status: needs_manual_transcript` instead of accepted transcript text.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_stt.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "WhisperModel|distil-large-v3|int8_float16|language=.en|task=.transcribe|condition_on_previous_text=False|HALLUCINATION_BLOCKLIST|needs_manual_transcript|SileroVadAdapter" ai-backend/app ai-backend/tests/test_stt.py` finds all terms.
    - Tests prove silent/no-speech audio returns `needs_manual_transcript` and `retry_allowed: true`.
  </acceptance_criteria>
  <done>STT/VAD adapters satisfy Phase 0 default and hallucination mitigation contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add transient STT route</name>
  <read_first>
    ai-backend/app/main.py
    ai-backend/app/models/model_manager.py
    ai-backend/app/models/stt.py
    ai-backend/tests/test_stt.py
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
  </read_first>
  <files>ai-backend/app/api/stt.py, ai-backend/app/main.py, ai-backend/tests/test_stt.py</files>
  <action>Create router `ai-backend/app/api/stt.py` with `POST /stt/transcribe` accepting multipart `file` and optional form fields `vad_threshold` and `vad_end_silence_ms`. It returns JSON fields `status`, `transcript`, `segments`, `language`, `model`, `compute_type`, `speech_detected`, `retry_allowed`, and `manual_transcript_allowed`. Include the router in `create_app()`. On adapter errors return HTTP 502 with detail `{code: "stt_failed", message: "Transcription failed"}` and no raw exception text.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_stt.py ai-backend/tests/test_health.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "POST.*/stt/transcribe|manual_transcript_allowed|retry_allowed|stt_failed|Transcription failed|include_router\\(stt" ai-backend/app ai-backend/tests/test_stt.py` finds all terms.
    - Tests assert route returns `manual_transcript_allowed: true` when transcription fails or needs manual transcript.
  </acceptance_criteria>
  <done>AI backend exposes a transient STT route ready for Web UI server calls.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high
- phase_controls: ASVS V5 input validation, V6 TLS, V8 data protection, V10 malicious code, V12 file/resource safety, V14 configuration
- execution_rule: any high-severity gap in this threat model or its listed mitigations blocks plan completion.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web UI server to `/stt/transcribe` | Uploaded audio bytes enter decoder, VAD, and Whisper code. |
| STT output to Web UI server | Transcript text becomes user-editable stored content if saved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Denial of Service | Audio decode | mitigate | Decode transiently, reject invalid input through route validation, and rely on Web UI upload size checks. |
| T-02-07-02 | Information Disclosure | STT errors | mitigate | Return fixed `Transcription failed` error detail without raw exception text. |
| T-02-07-03 | Tampering | Transcript output | mitigate | Mark low-confidence/no-speech/hallucination results as manual transcript needed. |
| T-02-07-04 | Repudiation | STT quality | mitigate | Return model, compute type, language, and segment metadata in response. |
</threat_model>

<verification>
Run `uv run --project ai-backend pytest ai-backend/tests/test_stt.py ai-backend/tests/test_health.py -q`.
</verification>

<success_criteria>
AI backend can perform English-only, VAD-gated STT with Phase 0 defaults and safe failure/manual transcript semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-backend-skeleton-voice-lab/02-07-SUMMARY.md`.
</output>
