---
phase: 02-ai-backend-skeleton-voice-lab
plan: "09"
type: execute
wave: 2
depends_on: ["02-04", "02-05"]
files_modified:
  - web-ui/server/app/domain/voice_service.py
  - web-ui/server/app/api/voices.py
  - web-ui/server/app/main.py
  - web-ui/server/tests/test_voices.py
autonomous: true
requirements: [REQ-20, REQ-21, REQ-22, REQ-23, REQ-24]
user_setup: []
must_haves:
  truths:
    - "Voice Lab server API can upload, transcribe, preview, save, list, read, rename, force-delete, and test-play voices."
    - "Voice save does not require a successful preview."
  artifacts:
    - path: "web-ui/server/app/domain/voice_service.py"
      provides: "Durable voice business logic"
    - path: "web-ui/server/app/api/voices.py"
      provides: "Voice Lab and Voice Library API routes"
  key_links:
    - from: "web-ui/server/app/api/voices.py"
      to: "web-ui/server/app/domain/ai_backend_client.py"
      via: "transcribe/preview/test-play calls"
      pattern: "AiBackendClient"
---

<objective>
Implement Web UI server voice domain service and API routes.

Purpose: The browser needs RayMe-owned endpoints for Voice Lab and Voice Library while the Web UI server remains durable owner and the AI backend remains transient processor.
Output: Voice API and service satisfying Wave 0 contracts.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-DECISION-COVERAGE.md
@web-ui/server/app/domain/voice_assets.py
@web-ui/server/app/domain/ai_backend_client.py
@web-ui/server/app/storage/models.py
@web-ui/server/app/api/characters.py
@web-ui/server/app/main.py
@web-ui/server/tests/test_voices.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement voice service persistence and processing calls</name>
  <read_first>
    web-ui/server/app/storage/models.py
    web-ui/server/app/domain/voice_assets.py
    web-ui/server/app/domain/ai_backend_client.py
    web-ui/server/tests/test_voices.py
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
  </read_first>
  <files>web-ui/server/app/domain/voice_service.py, web-ui/server/tests/test_voices.py</files>
  <action>Create `VoiceService` with methods `upload_sample`, `transcribe_asset`, `preview_voice`, `save_voice`, `list_voices`, `get_voice`, `rename_voice`, `delete_voice`, `test_play_voice`, and `sample_blob`. Generate IDs `voice_{uuid4().hex}` and `voice_asset_{uuid4().hex}`. `upload_sample` stores original samples using `voice_assets.py`. `transcribe_asset`, `preview_voice`, and `test_play_voice` call `AiBackendClient` with bytes from Web UI storage. `save_voice` persists `name`, `default_engine`, `reference_transcript`, `metadata_json`, and sample asset link without any preview field. `delete_voice(force=True)` sets `voices.deleted_at` and leaves existing `characters.default_voice_id` intact for unavailable state rendering. `rename_voice` changes only `Voice.name`.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_voices.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "class VoiceService|upload_sample|transcribe_asset|preview_voice|save_voice|list_voices|get_voice|rename_voice|delete_voice|test_play_voice|voice_|voice_asset_|deleted_at" web-ui/server/app/domain/voice_service.py web-ui/server/tests/test_voices.py` finds all terms.
    - Tests prove saving a voice succeeds when preview returned `tts_failed`.
  </acceptance_criteria>
  <done>Voice service owns durable voice state and delegates only transient processing to AI backend client.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose voice API routes</name>
  <read_first>
    web-ui/server/app/domain/voice_service.py
    web-ui/server/app/api/characters.py
    web-ui/server/app/main.py
    web-ui/server/tests/test_voices.py
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
  </read_first>
  <files>web-ui/server/app/api/voices.py, web-ui/server/app/main.py, web-ui/server/tests/test_voices.py</files>
  <action>Create `web-ui/server/app/api/voices.py` router with prefix `/api/voices`. Implement exact routes `POST /assets`, `GET /assets/{asset_id}/sample`, `POST /assets/{asset_id}/transcribe`, `POST /preview`, `POST ""`, `GET ""`, `GET /{voice_id}`, `PATCH /{voice_id}`, `DELETE /{voice_id}`, and `POST /{voice_id}/test-play`. `DELETE /{voice_id}` must require query param `force=true` when references exist and return referents with readable `kind`, `id`, and `name`; with `force=true` it soft-deletes and returns `{voice_id, deleted_at, strategy: "soft_delete", referents}`. Include the router in `create_app()` before static mount.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_voices.py web-ui/server/tests/test_app.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "APIRouter\\(prefix=.*/api/voices|/assets|/transcribe|/preview|/test-play|force|soft_delete|include_router\\(voices" web-ui/server/app/api/voices.py web-ui/server/app/main.py web-ui/server/tests/test_voices.py` finds all route and wiring terms.
    - API tests assert unsupported files return HTTP 400 with no traceback text.
  </acceptance_criteria>
  <done>Voice API routes are reachable through the Web UI FastAPI app and satisfy upload/save/library/delete/test-play contracts.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high
- phase_controls: ASVS V5 input validation, V6 TLS, V8 data protection, V10 malicious code, V12 file/resource safety, V14 configuration
- execution_rule: any high-severity gap in this threat model or its listed mitigations blocks plan completion.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to `/api/voices` | Audio uploads, transcripts, names, preview text, and delete requests are untrusted. |
| Web UI server to AI backend | Stored sample bytes are sent for transient processing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09-01 | Tampering | Voice API | mitigate | Pydantic route models and stable internal IDs; user names never become IDs. |
| T-02-09-02 | Information Disclosure | API errors | mitigate | Convert validation/processing failures to typed public messages. |
| T-02-09-03 | Denial of Service | Preview/test-play | mitigate | Use server-side AI backend client timeouts and do not persist preview audio. |
| T-02-09-04 | Tampering | Force delete | mitigate | Require `force=true`, preserve tombstone, and return readable referents. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests/test_voices.py web-ui/server/tests/test_app.py -q`.
</verification>

<success_criteria>
Web UI server Voice Lab APIs are complete and durable state remains in Web UI storage, not AI backend storage.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-backend-skeleton-voice-lab/02-09-SUMMARY.md`.
</output>
