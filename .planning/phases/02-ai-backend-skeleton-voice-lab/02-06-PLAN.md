---
phase: 02-ai-backend-skeleton-voice-lab
plan: "06"
type: execute
wave: 1
depends_on: ["02-02"]
files_modified:
  - ai-backend/pyproject.toml
  - ai-backend/app/config.py
  - ai-backend/app/models/__init__.py
  - ai-backend/app/models/model_manager.py
  - ai-backend/app/models/engine_metadata.py
  - ai-backend/app/api/__init__.py
  - ai-backend/app/api/health.py
  - ai-backend/app/main.py
  - ai-backend/tests/test_model_manager.py
  - ai-backend/tests/test_health.py
autonomous: true
requirements: [REQ-02, REQ-22, REQ-80, REQ-A3]
user_setup: []
must_haves:
  truths:
    - "AI backend boots with a lifespan-owned model manager and reports residency health."
    - "Health reports STT default, VAD readiness, one resident TTS engine, engine availability, and VRAM/headroom."
  artifacts:
    - path: "ai-backend/app/models/model_manager.py"
      provides: "Model residency coordinator"
    - path: "ai-backend/app/api/health.py"
      provides: "Expanded `/health` payload"
    - path: "ai-backend/app/config.py"
      provides: "Runtime settings for STT/VAD/TTS defaults"
  key_links:
    - from: "ai-backend/app/main.py"
      to: "ai-backend/app/models/model_manager.py"
      via: "FastAPI lifespan app.state.model_manager"
      pattern: "lifespan"
---

<objective>
Implement AI backend configuration, lifespan model manager, engine metadata model, and expanded `/health`.

Purpose: Phase 2 needs resident model topology visible before STT/TTS endpoints are wired.
Output: AI backend health/status foundation.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-RESEARCH.md
@.planning/phases/00-measurement-gate/KEY_DECISIONS.md
@ai-backend/app/main.py
@ai-backend/pyproject.toml
@ai-backend/tests/test_model_manager.py
@ai-backend/tests/test_health.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add config and model manager foundation</name>
  <read_first>
    ai-backend/app/main.py
    ai-backend/pyproject.toml
    ai-backend/tests/test_model_manager.py
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
    .planning/phases/00-measurement-gate/KEY_DECISIONS.md
  </read_first>
  <files>ai-backend/pyproject.toml, ai-backend/app/config.py, ai-backend/app/models/__init__.py, ai-backend/app/models/model_manager.py, ai-backend/app/models/engine_metadata.py, ai-backend/tests/test_model_manager.py</files>
  <action>Add AI backend dependencies `pydantic==2.13.3`, `pynvml==13.0.1`, `soundfile==0.13.1`, and `numpy==2.2.6` if not already present. Create `AiBackendSettings` with defaults `stt_model="distil-large-v3"`, `stt_compute_type="int8_float16"`, `stt_language="en"`, `default_tts_engine="f5"`, `vram_budget_mb=11000`, `vad_threshold=0.5`, `vad_end_silence_ms=700`, and `load_models_on_startup=True`. Create `EngineMetadata`/`EngineStatus` dataclasses or Pydantic models with the six engine IDs and labels. Create `ModelManager` with `startup()`, `shutdown()`, `health()`, `switch_tts_engine(engine_id)`, and one-resident state fields. In local tests, use lightweight fake adapters; do not require GPU model downloads for unit tests.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_model_manager.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "distil-large-v3|int8_float16|default_tts_engine.*f5|vram_budget_mb.*11000|ModelManager|switch_tts_engine|EngineMetadata|EngineStatus" ai-backend/app ai-backend/tests/test_model_manager.py` finds all required strings.
    - Unit tests prove `switch_tts_engine("xtts_v2")` unloads the prior resident engine before marking `xtts_v2` resident.
  </acceptance_criteria>
  <done>AI backend has configurable model manager foundation with one-resident TTS state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire FastAPI lifespan and expanded health router</name>
  <read_first>
    ai-backend/app/main.py
    ai-backend/tests/test_health.py
    ai-backend/app/models/model_manager.py
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-VALIDATION.md
  </read_first>
  <files>ai-backend/app/api/__init__.py, ai-backend/app/api/health.py, ai-backend/app/main.py, ai-backend/tests/test_health.py</files>
  <action>Move health route into `ai-backend/app/api/health.py`. Update `create_app()` to build `FastAPI(title="RayMe AI Backend", version="0.2.0", lifespan=lifespan)` where lifespan stores `app.state.model_manager`. `/health` must serialize `ModelManager.health()` with `phase: "02"`, `capabilities` including `health`, `stt`, `vad`, and `tts`, plus `resident_tts_engine`, `available_engines`, `loading_engine`, `vram_used_mb`, and `vram_headroom_mb`. Keep HTTPS runner tests passing and keep wildcard bind rejection unchanged.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_health.py ai-backend/tests/test_model_manager.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "version=.0.2.0|lifespan|app.state.model_manager|phase.*02|capabilities|resident_tts_engine|available_engines|vram_headroom_mb" ai-backend/app ai-backend/tests/test_health.py` finds all terms.
    - `uv run --project ai-backend pytest ai-backend/tests/test_health.py -q` still passes runner tests.
  </acceptance_criteria>
  <done>AI backend exposes Phase 2 health with residency and VRAM fields through lifespan-managed state.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI backend process to GPU runtime | Model load/unload changes GPU memory state. |
| AI backend `/health` to Web UI | Health exposes operational details to the LAN browser through Web UI status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Denial of Service | `ModelManager.switch_tts_engine` | mitigate | Unload current engine before loading the target and keep failed target unavailable. |
| T-02-06-02 | Information Disclosure | `/health` | mitigate | Return structured statuses and reasons, not tracebacks or local model paths. |
| T-02-06-03 | Repudiation | VRAM reporting | mitigate | Include `vram_used_mb` and `vram_headroom_mb` in health for live evidence. |
| T-02-06-04 | Tampering | Runtime settings | mitigate | Centralize defaults in `AiBackendSettings` and test exact Phase 0 values. |
</threat_model>

<verification>
Run `uv run --project ai-backend pytest ai-backend/tests/test_model_manager.py ai-backend/tests/test_health.py -q`.
</verification>

<success_criteria>
AI backend starts through FastAPI lifespan, reports Phase 2 residency status, and preserves Phase 0 STT/TTS defaults without committing to split runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-backend-skeleton-voice-lab/02-06-SUMMARY.md`.
</output>
