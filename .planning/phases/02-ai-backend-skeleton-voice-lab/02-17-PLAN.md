---
phase: 02-ai-backend-skeleton-voice-lab
plan: "17"
type: execute
wave: 4
depends_on: ["02-06", "02-08"]
files_modified:
  - ai-backend/pyproject.toml
  - ai-backend/app/api/webrtc.py
  - ai-backend/app/main.py
  - ai-backend/tests/test_webrtc_signaling.py
autonomous: true
requirements: [REQ-02]
user_setup: []
must_haves:
  truths:
    - "AI backend exposes only a non-call aiortc signaling skeleton if implemented in Phase 2."
    - "The skeleton does not claim live media, call readiness, captions, or barge-in behavior."
  artifacts:
    - path: "ai-backend/app/api/webrtc.py"
      provides: "Non-call signaling contract for Phase 3"
    - path: "ai-backend/tests/test_webrtc_signaling.py"
      provides: "Tests preventing live-call readiness claims"
  key_links:
    - from: "ai-backend/app/main.py"
      to: "ai-backend/app/api/webrtc.py"
      via: "router include"
      pattern: "webrtc"
---

<objective>
Add an optional aiortc signaling skeleton that does not claim live call readiness.

Purpose: The roadmap mentions FastAPI plus aiortc signaling in Phase 2, but live call media belongs to Phase 3; this plan creates only a safe contract boundary.
Output: Minimal signaling route and tests.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-DECISION-COVERAGE.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-RESEARCH.md
@ai-backend/app/main.py
@ai-backend/pyproject.toml
@ai-backend/tests/test_health.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add non-call signaling contract tests</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
    ai-backend/app/main.py
    ai-backend/pyproject.toml
    ai-backend/tests/test_health.py
    MISSING: ai-backend/tests/test_webrtc_signaling.py
  </read_first>
  <files>ai-backend/tests/test_webrtc_signaling.py</files>
  <action>Create `ai-backend/tests/test_webrtc_signaling.py` requiring `GET /webrtc/status` to return `{status: "skeleton", phase: "02", live_call_ready: false, media_transport_ready: false, message: "Signaling skeleton only; Phase 3 owns live call media."}` and requiring `POST /webrtc/offer` to return HTTP 501 with detail `{code: "call_not_ready", message: "Live call media is Phase 3 scope."}`. Tests must also assert there is no `/call`, `/captions`, or `/barge-in` route in the app OpenAPI paths.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_webrtc_signaling.py ai-backend/tests/test_health.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "/webrtc/status|/webrtc/offer|live_call_ready.*false|media_transport_ready.*false|call_not_ready|Phase 3 owns live call media|/call|/captions|/barge-in" ai-backend/tests/test_webrtc_signaling.py` finds all contract terms.
    - Tests fail until the skeleton route exists.
  </acceptance_criteria>
  <done>aiortc skeleton contract tests define a non-call Phase 3 boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement non-call signaling skeleton</name>
  <read_first>
    ai-backend/pyproject.toml
    ai-backend/app/main.py
    ai-backend/tests/test_webrtc_signaling.py
    ai-backend/tests/test_health.py
    MISSING: ai-backend/app/api/webrtc.py
  </read_first>
  <files>ai-backend/pyproject.toml, ai-backend/app/api/webrtc.py, ai-backend/app/main.py, ai-backend/tests/test_webrtc_signaling.py</files>
  <action>Add `aiortc==1.14.0` to `ai-backend/pyproject.toml`. Create `ai-backend/app/api/webrtc.py` with `GET /webrtc/status` returning the contract from Task 1 and `POST /webrtc/offer` returning HTTP 501 with detail `{code: "call_not_ready", message: "Live call media is Phase 3 scope."}`. Include the router in `create_app()`. Do not create call screen, media tracks, captions, barge-in, or audio playback behavior.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_webrtc_signaling.py ai-backend/tests/test_health.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "aiortc==1.14.0|/webrtc/status|/webrtc/offer|live_call_ready.*false|media_transport_ready.*false|call_not_ready|Phase 3 owns live call media|include_router\\(webrtc" ai-backend/pyproject.toml ai-backend/app ai-backend/tests/test_webrtc_signaling.py` finds all terms.
    - `rg "Voice Visualizer|RTCPeerConnection" ai-backend/app/api/webrtc.py` returns no implementation terms.
  </acceptance_criteria>
  <done>aiortc skeleton exists only as a Phase 3 contract boundary and cannot be mistaken for call readiness.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high
- phase_controls: ASVS V5 input validation, V6 TLS, V8 data protection, V10 malicious code, V12 file/resource safety, V14 configuration
- execution_rule: any high-severity gap in this threat model or its listed mitigations blocks plan completion.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/Web UI to signaling endpoint | Future SDP offers will cross into AI backend, but Phase 2 rejects them. |
| AI backend status to client | Skeleton status could be misinterpreted as call readiness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17-01 | Spoofing | `/webrtc/status` | mitigate | Return explicit `live_call_ready: false` and `media_transport_ready: false`. |
| T-02-17-02 | Denial of Service | `/webrtc/offer` | mitigate | Return HTTP 501 without creating peer connections or media resources. |
| T-02-17-03 | Repudiation | Phase boundary | mitigate | Tests require Phase 3 scope message. |
| T-02-17-04 | Information Disclosure | Offer errors | mitigate | Fixed 501 detail only; no SDP parsing errors in Phase 2. |
</threat_model>

<verification>
Run `uv run --project ai-backend pytest ai-backend/tests/test_webrtc_signaling.py ai-backend/tests/test_health.py -q`.
</verification>

<success_criteria>
aiortc dependency and skeleton routes exist only as explicit non-call scaffolding for Phase 3.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-backend-skeleton-voice-lab/02-17-SUMMARY.md`.
</output>
