---
phase: 02-ai-backend-skeleton-voice-lab
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - web-ui/server/tests/test_voices.py
  - web-ui/server/tests/test_migrations.py
  - web-ui/server/tests/test_health_settings.py
autonomous: true
requirements: [REQ-05, REQ-15, REQ-20, REQ-21, REQ-22, REQ-23, REQ-24, REQ-80]
user_setup: []
must_haves:
  truths:
    - "Voice storage and API behavior is specified by failing or contract tests before implementation."
    - "Settings save-audio and AI backend status behavior is specified before implementation."
  artifacts:
    - path: "web-ui/server/tests/test_voices.py"
      provides: "Voice upload, save, library, delete, and default-voice API contracts"
    - path: "web-ui/server/tests/test_migrations.py"
      provides: "Schema assertions for voices, voice_assets, and character.default_voice_id"
    - path: "web-ui/server/tests/test_health_settings.py"
      provides: "Settings audio defaults and AI backend status bridge assertions"
  key_links:
    - from: "web-ui/server/tests/test_voices.py"
      to: "web-ui/server/app/api/voices.py"
      via: "FastAPI route expectations"
      pattern: "/api/voices"
---

<objective>
Create the Web UI server Wave 0 validation contracts for durable voice storage, voice APIs, character default voice references, and Settings extensions.

Purpose: Phase 2 must not implement voice behavior without tests that lock stable IDs, optional preview, tombstone delete semantics, and Web UI server ownership of durable voice state.
Output: Failing or contract tests that later plans must satisfy.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/OPERATING-NOTES.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-VALIDATION.md
@web-ui/server/tests/test_migrations.py
@web-ui/server/tests/test_health_settings.py
@web-ui/server/tests/conftest.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add voice API and storage contract tests</name>
  <read_first>
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-VALIDATION.md
    web-ui/server/tests/test_characters.py
    web-ui/server/tests/conftest.py
    MISSING: web-ui/server/tests/test_voices.py
  </read_first>
  <files>web-ui/server/tests/test_voices.py</files>
  <action>Create `web-ui/server/tests/test_voices.py` with pytest contracts for these exact routes: `POST /api/voices/assets`, `POST /api/voices/assets/{asset_id}/transcribe`, `POST /api/voices/preview`, `POST /api/voices`, `GET /api/voices`, `GET /api/voices/{voice_id}`, `PATCH /api/voices/{voice_id}`, `DELETE /api/voices/{voice_id}?force=true`, and `POST /api/voices/{voice_id}/test-play`. Cover WAV, MP3, and FLAC acceptance; `.txt` rejection; generated blob names that ignore uploaded filenames; stable internal `voice_id`; mutable `name`; `default_engine`; editable `reference_transcript`; save without any preview result per D-14; preview failure preserving payload state per D-16; force-delete tombstone behavior per D-07 through D-09; and `Voice unavailable` response state for a character that still references a deleted voice.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_voices.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "/api/voices/assets|/api/voices/preview|/api/voices/.*/test-play|force=true|Voice unavailable|save without preview|F5-TTS|XTTS v2|Qwen3-TTS 0.6B-Base|LuxTTS|Chatterbox Turbo|TADA 1B" web-ui/server/tests/test_voices.py` finds all listed contracts.
    - Tests assert `voice_id` is stable across rename and `name` changes without reference rewrites.
    - Tests assert no voice save payload requires `preview_id`, `preview_url`, or a successful synthesis result.
  </acceptance_criteria>
  <done>Voice API/storage tests exist and fail only because implementation files are not present yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend migration and Settings contracts</name>
  <read_first>
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-VALIDATION.md
    web-ui/server/tests/test_migrations.py
    web-ui/server/tests/test_health_settings.py
    web-ui/server/app/domain/settings_service.py
    web-ui/server/app/api/settings.py
  </read_first>
  <files>web-ui/server/tests/test_migrations.py, web-ui/server/tests/test_health_settings.py</files>
  <action>Extend `test_migrations.py` to require tables `voices` and `voice_assets`, columns `voices.id`, `voices.name`, `voices.default_engine`, `voices.reference_transcript`, `voices.metadata_json`, `voices.deleted_at`, `voice_assets.voice_id`, `voice_assets.asset_kind`, `voice_assets.storage_path`, and `characters.default_voice_id`. Extend `test_health_settings.py` to require Settings public payload fields `save_ai_audio`, `save_mic_audio`, `vad_threshold`, `vad_end_silence_ms`, and `ai_backend_status`; defaults must be `save_ai_audio: true`, `save_mic_audio: false`, `vad_threshold: 0.5`, `vad_end_silence_ms: 700`. Preserve the existing save-before-test route order for `/api/settings/test/web`, `/api/settings/test/ai-backend`, and `/api/settings/test/llm`.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_migrations.py web-ui/server/tests/test_health_settings.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "voices|voice_assets|default_voice_id|save_ai_audio|save_mic_audio|vad_threshold|vad_end_silence_ms|ai_backend_status" web-ui/server/tests/test_migrations.py web-ui/server/tests/test_health_settings.py` finds all strings.
    - Existing `PATCH /api/settings` before endpoint test assertions remain present.
  </acceptance_criteria>
  <done>Server-side Wave 0 tests specify schema, Settings defaults, and AI backend status bridge expectations.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to Web UI voice API | Multipart audio, voice names, transcripts, and delete requests are untrusted input. |
| Web UI settings API | Endpoint URLs and secret-bearing LLM settings cross from browser to server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `POST /api/voices/assets` | mitigate | Tests require generated blob names and server-side WAV/MP3/FLAC validation. |
| T-02-01-02 | Information Disclosure | Settings tests | mitigate | Tests preserve `llm_api_key_configured` only and forbid raw key echo. |
| T-02-01-03 | Denial of Service | Voice upload | mitigate | Tests require unsupported file rejection before AI backend processing. |
| T-02-01-04 | Tampering | Voice delete | mitigate | Tests require tombstone/`Voice unavailable` behavior instead of dangling reference crashes. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests/test_voices.py web-ui/server/tests/test_migrations.py web-ui/server/tests/test_health_settings.py -q`. In Wave 0, implementation failures are acceptable only where the contract intentionally precedes implementation.
</verification>

<success_criteria>
The Web UI server has explicit tests for all voice persistence/API and Settings behaviors listed in `02-VALIDATION.md`, including optional preview and six-engine roster coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-backend-skeleton-voice-lab/02-01-SUMMARY.md`.
</output>
