---
phase: 02-ai-backend-skeleton-voice-lab
plan: "13"
type: execute
wave: 5
depends_on: ["02-12"]
files_modified:
  - web-ui/client/src/lib/components/voice/VoiceLibraryList.svelte
  - web-ui/client/src/lib/components/voice/VoiceLibraryRow.svelte
  - web-ui/client/src/lib/components/voice/VoiceRenameDialog.svelte
  - web-ui/client/src/lib/components/voice/VoiceDeleteDialog.svelte
  - web-ui/client/src/routes/voice-lab/+page.svelte
  - web-ui/client/tests/unit/voice-lab.test.ts
  - web-ui/client/tests/e2e/voice-lab.spec.ts
autonomous: true
requirements: [REQ-22, REQ-23, REQ-24, REQ-90]
user_setup: []
must_haves:
  truths:
    - "Voice Library lists saved voices and supports rename, delete, and test-play."
    - "Deleting referenced voices uses explicit force confirmation and leads to visible unavailable state."
  artifacts:
    - path: "web-ui/client/src/lib/components/voice/VoiceLibraryList.svelte"
      provides: "Voice Library list/empty/loading/error states"
    - path: "web-ui/client/src/lib/components/voice/VoiceLibraryRow.svelte"
      provides: "Row-scoped test-play, rename, and delete actions"
  key_links:
    - from: "web-ui/client/src/lib/components/voice/VoiceLibraryRow.svelte"
      to: "web-ui/client/src/lib/api/voices.ts"
      via: "rename/delete/testPlay wrappers"
      pattern: "testPlayVoice"
---

<objective>
Build Voice Library management inside Voice Lab.

Purpose: Saved voices must be manageable before Phase 3 calls can use them, including safe delete semantics for referenced voices.
Output: Voice Library components and route integration.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-DECISION-COVERAGE.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-UI-SPEC.md
@web-ui/client/src/routes/voice-lab/+page.svelte
@web-ui/client/src/lib/api/voices.ts
@web-ui/client/src/lib/components/ConfirmDialog.svelte
@web-ui/client/tests/unit/voice-lab.test.ts
@web-ui/client/tests/e2e/voice-lab.spec.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add library list, rows, rename, and test-play</name>
  <read_first>
    web-ui/client/src/routes/voice-lab/+page.svelte
    web-ui/client/src/lib/api/voices.ts
    web-ui/client/tests/unit/voice-lab.test.ts
    web-ui/client/tests/e2e/voice-lab.spec.ts
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-UI-SPEC.md
  </read_first>
  <files>web-ui/client/src/lib/components/voice/VoiceLibraryList.svelte, web-ui/client/src/lib/components/voice/VoiceLibraryRow.svelte, web-ui/client/src/lib/components/voice/VoiceRenameDialog.svelte, web-ui/client/src/routes/voice-lab/+page.svelte, web-ui/client/tests/unit/voice-lab.test.ts, web-ui/client/tests/e2e/voice-lab.spec.ts</files>
  <action>Create Voice Library UI with empty copy `No voices yet` and `Upload a 6-15 second WAV, MP3, or FLAC sample to create the first voice.` Each row shows voice `name`, engine label, transcript presence, created/updated timestamp, and assignment status. Add row actions with exact labels `Test Voice`, `Rename Voice`, and `Delete Voice`. Test-play uses custom text input hint copy `Type a test phrase`, includes `Use default engine`, pauses current preview/test audio before playing another clip, and keeps loading row-scoped. Rename uses `PATCH /api/voices/{voice_id}` and changes display text only.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run tests/unit/voice-lab.test.ts && npm --prefix web-ui/client run test:e2e -- voice-lab.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "No voices yet|Upload a 6-15 second WAV, MP3, or FLAC sample|Test Voice|Rename Voice|Delete Voice|Type a test phrase|Use default engine|renameVoice|testPlayVoice" web-ui/client/src/lib/components/voice web-ui/client/src/routes/voice-lab web-ui/client/tests` finds all strings.
    - Playwright proves one row synthesizing does not disable another row rename button.
  </acceptance_criteria>
  <done>Voice Library lists voices and supports rename/test-play with stable row layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add delete and unavailable-state library UX</name>
  <read_first>
    web-ui/client/src/lib/components/ConfirmDialog.svelte
    web-ui/client/src/lib/api/voices.ts
    web-ui/client/src/lib/components/voice/VoiceLibraryRow.svelte
    web-ui/client/tests/unit/voice-lab.test.ts
    web-ui/client/tests/e2e/voice-lab.spec.ts
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
  </read_first>
  <files>web-ui/client/src/lib/components/voice/VoiceDeleteDialog.svelte, web-ui/client/src/lib/components/voice/VoiceLibraryRow.svelte, web-ui/client/src/routes/voice-lab/+page.svelte, web-ui/client/tests/unit/voice-lab.test.ts, web-ui/client/tests/e2e/voice-lab.spec.ts</files>
  <action>Create `VoiceDeleteDialog` using existing `ConfirmDialog` conventions. First delete call uses `deleteVoice(voiceId, false)`. If API returns referents, show dialog copy `Delete voice: Delete this voice?` plus each referent readable name and a destructive button `Force Delete Voice`; that button calls `deleteVoice(voiceId, true)`. After force-delete, remove the row from active library results and show a toast/status that referenced characters will show `Voice unavailable`. Do not silently clear character references in client state.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run tests/unit/voice-lab.test.ts && npm --prefix web-ui/client run test:e2e -- voice-lab.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "Delete voice: Delete this voice\\?|Force Delete Voice|Voice unavailable|deleteVoice\\(.*false|deleteVoice\\(.*true|referents" web-ui/client/src/lib/components/voice web-ui/client/src/routes/voice-lab web-ui/client/tests` finds all delete contract strings.
    - Playwright proves blocked delete lists at least one character referent by name before force delete.
  </acceptance_criteria>
  <done>Voice Library delete flow makes referenced deletes explicit and recoverable.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high
- phase_controls: ASVS V5 input validation, V6 TLS, V8 data protection, V10 malicious code, V12 file/resource safety, V14 configuration
- execution_rule: any high-severity gap in this threat model or its listed mitigations blocks plan completion.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to voice delete API | Destructive voice deletion request crosses into durable storage. |
| TTS test-play text to AI backend | User-entered test phrase is sent through Web UI server to TTS processing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13-01 | Tampering | Delete dialog | mitigate | Require explicit second `Force Delete Voice` action for referenced voices. |
| T-02-13-02 | Information Disclosure | Referent list | accept | Character/chat names are local user-owned app data needed to explain delete impact. |
| T-02-13-03 | Denial of Service | Test-play | mitigate | Row-scoped loading and single active audio playback. |
| T-02-13-04 | Elevation of Privilege | Voice names/test text | mitigate | Render names and text via Svelte text bindings only. |
</threat_model>

<verification>
Run `npm --prefix web-ui/client run test:unit -- --run tests/unit/voice-lab.test.ts && npm --prefix web-ui/client run test:e2e -- voice-lab.spec.ts`.
</verification>

<success_criteria>
Voice Library provides list, rename, delete, and test-play with explicit referenced delete handling and no layout instability.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-backend-skeleton-voice-lab/02-13-SUMMARY.md`.
</output>
