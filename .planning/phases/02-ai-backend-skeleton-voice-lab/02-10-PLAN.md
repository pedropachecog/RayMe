---
phase: 02-ai-backend-skeleton-voice-lab
plan: "10"
type: execute
wave: 2
depends_on: ["02-05", "02-06"]
files_modified:
  - web-ui/server/app/domain/settings_service.py
  - web-ui/server/app/api/settings.py
  - web-ui/server/tests/test_health_settings.py
autonomous: true
requirements: [REQ-02, REQ-05, REQ-80, REQ-A3]
user_setup: []
must_haves:
  truths:
    - "Settings persists save-audio defaults and Phase 2 VAD values server-side."
    - "Settings API returns compact AI backend residency status."
  artifacts:
    - path: "web-ui/server/app/domain/settings_service.py"
      provides: "Extended persisted settings model"
    - path: "web-ui/server/app/api/settings.py"
      provides: "Public Settings payload and endpoint tests"
  key_links:
    - from: "web-ui/server/app/api/settings.py"
      to: "web-ui/server/app/domain/ai_backend_client.py"
      via: "AI backend status mapping"
      pattern: "ai_backend_status"
---

<objective>
Extend Web UI server Settings service and API for audio defaults, VAD values, and compact AI backend status.

Purpose: Phase 2 must store settings that later call phases consume while preserving the proven save-before-test behavior.
Output: Settings server fields and tests.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-DECISION-COVERAGE.md
@web-ui/server/app/domain/settings_service.py
@web-ui/server/app/api/settings.py
@web-ui/server/app/domain/ai_backend_client.py
@web-ui/server/tests/test_health_settings.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend persisted Settings fields</name>
  <read_first>
    web-ui/server/app/domain/settings_service.py
    web-ui/server/tests/test_health_settings.py
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
  </read_first>
  <files>web-ui/server/app/domain/settings_service.py, web-ui/server/tests/test_health_settings.py</files>
  <action>Extend `SETTING_FIELDS` and `EndpointSettings` with `save_ai_audio: bool`, `save_mic_audio: bool`, `vad_threshold: float`, `vad_end_silence_ms: int`, `stt_model: str`, and `tts_default_engine: str`. Defaults must be `save_ai_audio=True`, `save_mic_audio=False`, `vad_threshold=0.5`, `vad_end_silence_ms=700`, `stt_model="distil-large-v3"`, and `tts_default_engine="f5"`. Update cleaning/coercion so booleans and numbers persist in `app_settings.value_json` as JSON booleans/numbers, while endpoint/key/model strings remain stripped strings.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_health_settings.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "save_ai_audio|save_mic_audio|vad_threshold|vad_end_silence_ms|stt_model|tts_default_engine|distil-large-v3|f5" web-ui/server/app/domain/settings_service.py web-ui/server/tests/test_health_settings.py` finds all settings.
    - Tests prove defaults are returned before any persisted settings row exists.
  </acceptance_criteria>
  <done>Settings service persists Phase 2 audio and VAD values with exact defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend Settings API response and AI backend status mapping</name>
  <read_first>
    web-ui/server/app/api/settings.py
    web-ui/server/app/domain/ai_backend_client.py
    web-ui/server/tests/test_health_settings.py
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
  </read_first>
  <files>web-ui/server/app/api/settings.py, web-ui/server/tests/test_health_settings.py</files>
  <action>Extend `SettingsPatch` and `PublicSettings` to include the new fields from Task 1 plus `ai_backend_status`. `ai_backend_status` must include `endpoint_status`, `status`, `stt_model`, `stt_compute_type`, `vad_ready`, `resident_tts_engine`, `available_engines`, `loading_engine`, `vram_used_mb`, and `vram_headroom_mb`. Preserve save-before-test semantics: each `/api/settings/test/*` route reads already-persisted settings; client save-before-test remains in the UI. Reject VAD threshold outside `0.0..1.0` and `vad_end_silence_ms` outside `100..3000` with Pydantic validation.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_health_settings.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "ai_backend_status|endpoint_status|resident_tts_engine|available_engines|vram_used_mb|vram_headroom_mb|ge=0|le=1|ge=100|le=3000" web-ui/server/app/api/settings.py web-ui/server/tests/test_health_settings.py` finds all validation/status fields.
    - Existing tests for `llm_api_key_configured` still prove raw key is never returned.
  </acceptance_criteria>
  <done>Settings API exposes audio/VAD defaults and compact AI backend residency status without weakening endpoint tests.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high
- phase_controls: ASVS V5 input validation, V6 TLS, V8 data protection, V10 malicious code, V12 file/resource safety, V14 configuration
- execution_rule: any high-severity gap in this threat model or its listed mitigations blocks plan completion.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to Settings API | Endpoint URLs, LLM key input, audio retention toggles, and VAD numeric values are untrusted. |
| AI backend status to Settings API | Runtime metadata crosses from AI backend to browser-visible Settings response. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10-01 | Information Disclosure | Settings response | mitigate | Continue returning `llm_api_key_configured` only and typed AI status. |
| T-02-10-02 | Tampering | VAD settings | mitigate | Pydantic numeric bounds `0.0..1.0` and `100..3000`. |
| T-02-10-03 | Information Disclosure | Audio retention | mitigate | Expose explicit `save_mic_audio` default false and `save_ai_audio` default true. |
| T-02-10-04 | Spoofing | Endpoint tests | mitigate | Preserve server-side configured endpoint probes and exact status enum. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests/test_health_settings.py -q`.
</verification>

<success_criteria>
Settings server API persists Phase 2 audio/VAD values, preserves endpoint tests, and surfaces compact AI backend model residency status.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-backend-skeleton-voice-lab/02-10-SUMMARY.md`.
</output>
