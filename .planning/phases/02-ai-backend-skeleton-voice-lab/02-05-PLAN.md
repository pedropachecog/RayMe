---
phase: 02-ai-backend-skeleton-voice-lab
plan: "05"
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - web-ui/server/app/domain/ai_backend_client.py
  - web-ui/server/app/api/ai_backend.py
  - web-ui/server/app/api/settings.py
  - web-ui/server/app/main.py
  - web-ui/server/tests/test_health_settings.py
autonomous: true
requirements: [REQ-02, REQ-05, REQ-21, REQ-23, REQ-80, REQ-A3]
user_setup: []
must_haves:
  truths:
    - "Web UI server can safely ask AI backend for status, STT, and TTS processing without owning model runtime."
    - "Browser-visible status never exposes raw tracebacks or secrets."
  artifacts:
    - path: "web-ui/server/app/domain/ai_backend_client.py"
      provides: "Typed AI backend HTTP client and error mapping"
    - path: "web-ui/server/app/api/ai_backend.py"
      provides: "RayMe-owned status bridge endpoint"
  key_links:
    - from: "web-ui/server/app/api/settings.py"
      to: "web-ui/server/app/domain/ai_backend_client.py"
      via: "AI backend status/test probe"
      pattern: "AiBackendClient"
---

<objective>
Implement the Web UI server AI backend client and status bridge.

Purpose: Voice APIs and Settings need transient STT/TTS/status calls while preserving the locked boundary that durable voice state lives in the Web UI server.
Output: Typed AI backend client, status bridge route, and Settings test mapping.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-DECISION-COVERAGE.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-RESEARCH.md
@web-ui/server/app/domain/llm_probe.py
@web-ui/server/app/api/settings.py
@web-ui/server/app/main.py
@web-ui/server/tests/test_health_settings.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add typed AI backend client</name>
  <read_first>
    web-ui/server/app/domain/llm_probe.py
    web-ui/server/app/api/settings.py
    web-ui/server/tests/test_health_settings.py
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
    MISSING: web-ui/server/app/domain/ai_backend_client.py
  </read_first>
  <files>web-ui/server/app/domain/ai_backend_client.py, web-ui/server/tests/test_health_settings.py</files>
  <action>Create `AiBackendClient` with methods `get_status(base_url)`, `transcribe_sample(base_url, audio_bytes, filename, content_type)`, and `synthesize(base_url, payload)`. Define Pydantic/dataclass response shapes `AiBackendStatus`, `EngineStatus`, `TranscriptionResult`, and `SynthesisResult`. Map HTTP timeouts, connection errors, non-JSON responses, and 5xx responses into `AiBackendUnavailable` or `AiBackendProcessingError` with public fields `code` and `message`; public messages must be one of `AI backend unreachable`, `AI backend returned an invalid response`, `Transcription failed`, or `Synthesis failed`. Never pass through `Traceback`, local file paths, exception class names, or adapter stack traces.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_health_settings.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "class AiBackendClient|AiBackendStatus|EngineStatus|TranscriptionResult|SynthesisResult|AI backend unreachable|AI backend returned an invalid response|Transcription failed|Synthesis failed" web-ui/server/app/domain/ai_backend_client.py web-ui/server/tests/test_health_settings.py` finds all strings.
    - Tests assert raw strings `Traceback`, `File \"`, and `RuntimeError` do not appear in public error payloads.
  </acceptance_criteria>
  <done>Web UI server has a typed, traceback-safe AI backend processing client.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RayMe status bridge endpoint and Settings probe integration</name>
  <read_first>
    web-ui/server/app/api/settings.py
    web-ui/server/app/main.py
    web-ui/server/tests/test_health_settings.py
    web-ui/server/app/domain/ai_backend_client.py
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
  </read_first>
  <files>web-ui/server/app/api/ai_backend.py, web-ui/server/app/api/settings.py, web-ui/server/app/main.py, web-ui/server/tests/test_health_settings.py</files>
  <action>Create router `web-ui/server/app/api/ai_backend.py` with `GET /api/ai-backend/status` returning compact status from the configured AI backend URL: `endpoint_status`, `stt_model`, `stt_compute_type`, `vad_ready`, `resident_tts_engine`, `available_engines`, `loading_engine`, `vram_used_mb`, and `vram_headroom_mb`. Include the router in `create_app()`. Update `/api/settings/test/ai-backend` to use the AI backend client health/status call while preserving exact status values `Connected`, `Unreachable`, `Unauthorized`, `Not configured`. If AI backend status is degraded but reachable, endpoint test returns `Connected` and the status bridge exposes `status: degraded`.</action>
  <verify>
    <automated>uv run --project web-ui/server pytest web-ui/server/tests/test_health_settings.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "/api/ai-backend/status|endpoint_status|stt_model|stt_compute_type|vad_ready|resident_tts_engine|vram_headroom_mb|include_router\\(ai_backend" web-ui/server/app web-ui/server/tests/test_health_settings.py` finds all strings.
    - Existing save-before-test Settings tests still pass.
  </acceptance_criteria>
  <done>Web UI exposes a RayMe-owned AI backend status bridge and keeps endpoint test semantics stable.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high
- phase_controls: ASVS V5 input validation, V6 TLS, V8 data protection, V10 malicious code, V12 file/resource safety, V14 configuration
- execution_rule: any high-severity gap in this threat model or its listed mitigations blocks plan completion.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web UI server to AI backend | Server-to-server transient processing/status requests cross LAN service boundary. |
| Web UI server to browser | AI backend errors and health metadata become browser-visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Information Disclosure | `AiBackendClient` | mitigate | Map raw exceptions to fixed public messages and log details server-side only. |
| T-02-05-02 | Denial of Service | AI backend calls | mitigate | Use bounded httpx timeouts and typed unavailable errors. |
| T-02-05-03 | Spoofing | Configured endpoint URL | mitigate | Reuse Settings absolute HTTP(S) validation and server-side calls. |
| T-02-05-04 | Repudiation | Status bridge | mitigate | Return structured engine/status fields so summaries can cite evidence. |
</threat_model>

<verification>
Run `uv run --project web-ui/server pytest web-ui/server/tests/test_health_settings.py -q`.
</verification>

<success_criteria>
The Web UI server can call AI backend processing/status APIs through typed server-side code, expose compact status for Settings, and avoid raw error leakage.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-backend-skeleton-voice-lab/02-05-SUMMARY.md`.
</output>
