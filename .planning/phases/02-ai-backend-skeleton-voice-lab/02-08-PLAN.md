---
phase: 02-ai-backend-skeleton-voice-lab
plan: "08"
type: execute
wave: 3
depends_on: ["02-06", "02-07"]
files_modified:
  - ai-backend/pyproject.toml
  - ai-backend/app/models/tts_registry.py
  - ai-backend/app/models/tts_f5.py
  - ai-backend/app/models/tts_xtts.py
  - ai-backend/app/models/tts_qwen3.py
  - ai-backend/app/models/tts_luxtts.py
  - ai-backend/app/models/tts_chatterbox.py
  - ai-backend/app/models/tts_tada.py
  - ai-backend/app/api/tts.py
  - ai-backend/app/main.py
  - ai-backend/tests/test_tts_registry.py
autonomous: true
requirements: [REQ-02, REQ-22, REQ-23]
user_setup: []
must_haves:
  truths:
    - "The AI backend has one public TTS API backed by a six-engine metadata registry."
    - "F5-TTS is the default resident adapter, while other engines degrade independently if unavailable."
  artifacts:
    - path: "ai-backend/app/models/tts_registry.py"
      provides: "Engine registry, metadata, switching, and adapter contract"
    - path: "ai-backend/app/api/tts.py"
      provides: "Transient synthesis endpoint"
    - path: "ai-backend/tests/test_tts_registry.py"
      provides: "Six-engine registry and degradation tests"
  key_links:
    - from: "ai-backend/app/api/tts.py"
      to: "ai-backend/app/models/tts_registry.py"
      via: "synthesize endpoint"
      pattern: "switch_tts_engine"
---

<objective>
Implement the AI backend TTS registry, first/default F5 adapter path, availability-gated adapters for the full roster, and transient synthesis API.

Purpose: Voice Lab preview/test-play needs a single stable AI backend API while runtime choices remain evidence-gated and per-engine failures do not take down the service.
Output: Metadata-driven TTS registry and `/tts/synthesize` route.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-DECISION-COVERAGE.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-RESEARCH.md
@.planning/phases/00-measurement-gate/KEY_DECISIONS.md
@.planning/phases/00-measurement-gate/results/phase0_summary.json
@ai-backend/app/models/model_manager.py
@ai-backend/tests/test_tts_registry.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build metadata-driven TTS registry contract</name>
  <read_first>
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-RESEARCH.md
    .planning/phases/00-measurement-gate/KEY_DECISIONS.md
    ai-backend/app/models/model_manager.py
    ai-backend/tests/test_tts_registry.py
  </read_first>
  <files>ai-backend/pyproject.toml, ai-backend/app/models/tts_registry.py, ai-backend/tests/test_tts_registry.py</files>
  <action>Add optional runtime dependencies in `ai-backend/pyproject.toml` only where package names are known from research: `f5-tts==1.1.17`, `coqui-tts==0.27.5`, `qwen-tts==0.1.1`, and `soundfile==0.13.1`. Create the registry/metadata contract before adapter/API wiring: `TtsEngineRegistry`, adapter protocol types, and registry metadata in `tts_registry.py` with engine IDs `f5`, `xtts_v2`, `qwen3_0_6b`, `luxtts`, `chatterbox_turbo`, `tada_1b`. Metadata must include exact labels, `is_default`, `code_license`, `model_license`, `caveat_chips`, `runtime_evidence`, `requires_transcript`, `supports_streaming`, `quality_notes`, and `availability`. F5 metadata: default, requires transcript, model license `CC-BY-NC`. XTTS v2 metadata: model license `CPML`, transcript stored for portability, native streaming inside safe chunks. Qwen metadata: `Apache-2.0`, opt-in, latency/accent caveats, variant `0.6B-Base`. LuxTTS, Chatterbox Turbo, and TADA 1B must be present with measured caveats from STATE and runtime evidence fields. Name the registry-only tests with `registry_metadata` so Task 2 can gate this slice before per-engine adapter modules or `/tts/synthesize` are implemented.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_tts_registry.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "f5|xtts_v2|qwen3_0_6b|luxtts|chatterbox_turbo|tada_1b|F5-TTS|XTTS v2|Qwen3-TTS 0.6B-Base|LuxTTS|Chatterbox Turbo|TADA 1B|CC-BY-NC|CPML|Apache-2.0|runtime_evidence" ai-backend/app ai-backend/tests/test_tts_registry.py` finds all terms.
    - Tests prove the registry fails if any engine is missing.
    - No task commits to Windows/WSL/Docker split runtime without a self-test evidence record.
  </acceptance_criteria>
  <done>Six-engine TTS registry metadata contract exists with F5 as default before adapter/API work proceeds.</done>
</task>

<task type="auto">
  <name>Task 2: Gate registry metadata before adapters and API</name>
  <read_first>
    ai-backend/app/models/tts_registry.py
    ai-backend/tests/test_tts_registry.py
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-DECISION-COVERAGE.md
  </read_first>
  <files>ai-backend/app/models/tts_registry.py, ai-backend/tests/test_tts_registry.py</files>
  <action>Run the registry-only metadata gate after Task 1 and before creating or filling `tts_f5.py`, `tts_xtts.py`, `tts_qwen3.py`, `tts_luxtts.py`, `tts_chatterbox.py`, `tts_tada.py`, `api/tts.py`, or `main.py` route wiring. If the gate fails, fix only registry metadata/tests until it passes. This gate exists to prove D-01 through D-03, D-11, D-24, and D-26 coverage before adapter breadth or API wiring can hide roster/metadata failures.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_tts_registry.py -q -k registry_metadata</automated>
  </verify>
  <done>Registry metadata tests pass independently before any per-engine adapter or synthesis API wiring proceeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add transient synthesis API, adapters, and switch status</name>
  <read_first>
    ai-backend/app/main.py
    ai-backend/app/models/model_manager.py
    ai-backend/app/models/tts_registry.py
    ai-backend/tests/test_tts_registry.py
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
  </read_first>
  <files>ai-backend/app/models/tts_f5.py, ai-backend/app/models/tts_xtts.py, ai-backend/app/models/tts_qwen3.py, ai-backend/app/models/tts_luxtts.py, ai-backend/app/models/tts_chatterbox.py, ai-backend/app/models/tts_tada.py, ai-backend/app/api/tts.py, ai-backend/app/main.py, ai-backend/tests/test_tts_registry.py</files>
  <action>Only after Task 2 passes, add per-engine adapter modules and `POST /tts/synthesize`. Adapter imports/load failures must mark only that engine unavailable with reason. The synthesis route accepts JSON fields `voice_id`, `engine_id`, `text`, `reference_audio_b64`, `reference_transcript`, and `use_default_engine`. If `use_default_engine` is true, use the caller-provided voice default engine; otherwise use `engine_id`. Before synthesis, call `ModelManager.switch_tts_engine(target_engine)` and expose `loading_engine` in health while switching. Return WAV bytes or JSON metadata according to tests; for failures return HTTP 502 detail `{code: "tts_failed", message: "Synthesis failed", engine_id: target_engine}`. Do not persist audio in the AI backend. Include `tts` router in `create_app()`.</action>
  <verify>
    <automated>uv run --project ai-backend pytest ai-backend/tests/test_tts_registry.py ai-backend/tests/test_health.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "POST.*/tts/synthesize|use_default_engine|switch_tts_engine|loading_engine|tts_failed|Synthesis failed|include_router\\(tts" ai-backend/app ai-backend/tests/test_tts_registry.py` finds all terms.
    - Tests prove AI backend has no voice library/list persistence endpoint.
  </acceptance_criteria>
  <done>AI backend exposes one transient TTS API with visible engine switching and no durable voice state.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high
- phase_controls: ASVS V5 input validation, V6 TLS, V8 data protection, V10 malicious code, V12 file/resource safety, V14 configuration
- execution_rule: any high-severity gap in this threat model or its listed mitigations blocks plan completion.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web UI server to `/tts/synthesize` | Reference audio, transcript, and synthesis text enter engine adapters. |
| TTS adapter to GPU runtime | Engine load/switch can consume VRAM and fail per adapter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Denial of Service | Engine switching | mitigate | One resident engine, unload-before-load, per-engine unavailable state. |
| T-02-08-02 | Information Disclosure | TTS errors | mitigate | Return fixed `Synthesis failed` public errors without local paths or tracebacks. |
| T-02-08-03 | Repudiation | Engine caveats/licenses | mitigate | Registry metadata includes license, caveats, and runtime evidence for all six engines. |
| T-02-08-04 | Tampering | Durable state boundary | mitigate | Tests forbid AI backend voice library/list persistence endpoints. |
</threat_model>

<verification>
Run `uv run --project ai-backend pytest ai-backend/tests/test_tts_registry.py ai-backend/tests/test_health.py -q`.
</verification>

<success_criteria>
The AI backend has a single public TTS API, metadata-driven full roster, visible switch/loading status, and F5 default behavior without premature split-runtime commitment.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-backend-skeleton-voice-lab/02-08-SUMMARY.md`.
</output>
