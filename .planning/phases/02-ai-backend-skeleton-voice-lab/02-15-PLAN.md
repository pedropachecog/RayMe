---
phase: 02-ai-backend-skeleton-voice-lab
plan: "15"
type: execute
wave: 6
depends_on: ["02-11", "02-13", "02-14"]
files_modified:
  - web-ui/client/src/lib/api/types.ts
  - web-ui/client/src/lib/api/characters.ts
  - web-ui/client/src/lib/components/voice/VoiceAssignmentSelect.svelte
  - web-ui/client/src/lib/components/voice/VoiceStateBadge.svelte
  - web-ui/client/src/routes/characters/[id]/+page.svelte
  - web-ui/client/src/lib/components/CharacterCard.svelte
  - web-ui/client/src/routes/gallery/+page.svelte
  - web-ui/client/tests/unit/character-editor.test.ts
  - web-ui/client/tests/unit/gallery.test.ts
  - web-ui/client/tests/e2e/voice-lab.spec.ts
autonomous: true
requirements: [REQ-15, REQ-24, REQ-90]
user_setup: []
must_haves:
  truths:
    - "User can assign a saved voice as a character default in the Character Editor."
    - "Gallery shows assigned, no voice, and unavailable voice states."
  artifacts:
    - path: "web-ui/client/src/lib/components/voice/VoiceAssignmentSelect.svelte"
      provides: "Editor default voice control"
    - path: "web-ui/client/src/lib/components/voice/VoiceStateBadge.svelte"
      provides: "Gallery voice state badge"
  key_links:
    - from: "web-ui/client/src/routes/characters/[id]/+page.svelte"
      to: "web-ui/client/src/lib/api/characters.ts"
      via: "default_voice_id in save payload"
      pattern: "default_voice_id"
---

<objective>
Wire Character Editor and Gallery default voice UI.

Purpose: Saved voices must be assignable to characters and visible in the Gallery before the first call phase.
Output: Default voice selector, badge states, and tests.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/execute-plan.md
@/home/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-CONTEXT.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-DECISION-COVERAGE.md
@.planning/phases/02-ai-backend-skeleton-voice-lab/02-UI-SPEC.md
@web-ui/client/src/routes/characters/[id]/+page.svelte
@web-ui/client/src/lib/components/CharacterCard.svelte
@web-ui/client/src/routes/gallery/+page.svelte
@web-ui/client/src/lib/api/voices.ts
@web-ui/client/tests/unit/character-editor.test.ts
@web-ui/client/tests/unit/gallery.test.ts
@web-ui/client/tests/e2e/voice-lab.spec.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add Character Editor default voice assignment</name>
  <read_first>
    web-ui/client/src/routes/characters/[id]/+page.svelte
    web-ui/client/src/lib/api/types.ts
    web-ui/client/src/lib/api/characters.ts
    web-ui/client/src/lib/api/voices.ts
    web-ui/client/tests/unit/character-editor.test.ts
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-UI-SPEC.md
  </read_first>
  <files>web-ui/client/src/lib/api/types.ts, web-ui/client/src/lib/api/characters.ts, web-ui/client/src/lib/components/voice/VoiceAssignmentSelect.svelte, web-ui/client/src/routes/characters/[id]/+page.svelte, web-ui/client/tests/unit/character-editor.test.ts</files>
  <action>Extend character types/payloads with `default_voice_id`, `default_voice_state`, `default_voice_label`, and `default_voice`. Create `VoiceAssignmentSelect` labeled `Default voice`. Options must include `No voice assigned` plus saved voices showing name, engine label, and caveat chip for `Qwen3-TTS 0.6B-Base`. If no voices exist, show secondary action `Create Voice`; if unsaved editor changes exist, confirm before navigating to `/voice-lab`. Persist assignment only through `Save Character` by including `default_voice_id` in create/update payloads. Do not auto-save character changes when selecting a voice.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run tests/unit/character-editor.test.ts tests/unit/voice-lab.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "Default voice|No voice assigned|Create Voice|default_voice_id|default_voice_state|Qwen3-TTS 0.6B-Base|Save Character" web-ui/client/src/routes/characters web-ui/client/src/lib/components/voice web-ui/client/tests/unit/character-editor.test.ts` finds all strings.
    - Tests prove `updateCharacter` payload contains `default_voice_id` only after Save Character.
  </acceptance_criteria>
  <done>Character Editor can assign default voice through normal save flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Gallery voice state badges</name>
  <read_first>
    web-ui/client/src/lib/components/CharacterCard.svelte
    web-ui/client/src/routes/gallery/+page.svelte
    web-ui/client/tests/unit/gallery.test.ts
    web-ui/client/tests/e2e/voice-lab.spec.ts
    .planning/phases/02-ai-backend-skeleton-voice-lab/02-UI-SPEC.md
  </read_first>
  <files>web-ui/client/src/lib/components/voice/VoiceStateBadge.svelte, web-ui/client/src/lib/components/CharacterCard.svelte, web-ui/client/src/routes/gallery/+page.svelte, web-ui/client/tests/unit/gallery.test.ts, web-ui/client/tests/e2e/voice-lab.spec.ts</files>
  <action>Create `VoiceStateBadge` for Gallery cards. Render exact states: assigned as `Voice: {voice name}` with accessible full text, none as `No voice`, and broken/deleted reference as `Voice unavailable` with `AlertTriangle` icon. Keep card dimensions stable and do not overlap portrait, card actions, or snippets. Update Gallery tests and Voice Lab E2E to assert the three states after assigning a voice and force-deleting it.</action>
  <verify>
    <automated>npm --prefix web-ui/client run test:unit -- --run tests/unit/gallery.test.ts && npm --prefix web-ui/client run test:e2e -- voice-lab.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "Voice:|No voice|Voice unavailable|AlertTriangle|VoiceStateBadge" web-ui/client/src/lib/components web-ui/client/src/routes/gallery web-ui/client/tests` finds all strings.
    - Playwright proves the `Voice unavailable` badge is visible after referenced voice force-delete.
  </acceptance_criteria>
  <done>Gallery surfaces character voice state with stable UI and recoverable unavailable references.</done>
</task>
</tasks>

<threat_model>
## Security Enforcement

- security_standard: OWASP ASVS L1
- block_threshold: high
- phase_controls: ASVS V5 input validation, V6 TLS, V8 data protection, V10 malicious code, V12 file/resource safety, V14 configuration
- execution_rule: any high-severity gap in this threat model or its listed mitigations blocks plan completion.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser character editor to API | User-selected voice IDs are submitted with character edits. |
| Voice names to Gallery DOM | User-controlled voice names render inside character cards. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15-01 | Tampering | `default_voice_id` payload | mitigate | Server validates ID; client sends only selected stable ID. |
| T-02-15-02 | Elevation of Privilege | Voice badges | mitigate | Render voice names as text, not raw HTML. |
| T-02-15-03 | Denial of Service | Deleted references | mitigate | Client supports `Voice unavailable` state without route crash. |
| T-02-15-04 | Spoofing | Engine caveats | mitigate | Voice options show backend metadata labels/caveats. |
</threat_model>

<verification>
Run `npm --prefix web-ui/client run test:unit -- --run tests/unit/character-editor.test.ts tests/unit/gallery.test.ts && npm --prefix web-ui/client run test:e2e -- voice-lab.spec.ts`.
</verification>

<success_criteria>
Character default voice assignment works through Save Character, and Gallery visibly distinguishes assigned, no voice, and unavailable voice states.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-backend-skeleton-voice-lab/02-15-SUMMARY.md`.
</output>
