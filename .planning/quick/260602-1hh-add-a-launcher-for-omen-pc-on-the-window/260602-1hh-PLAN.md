---
phase: quick-260602-1hh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/start-rayme-omen.ps1
  - scripts/deploy-omen.sh
  - web-ui/server/docs/HTTPS-LAN.md
autonomous: true
requirements: []
---

<objective>
Add a Windows Desktop launcher on OMEN-PC that lets the user start/open RayMe
from the desktop while keeping OMEN deployment and runtime launch behavior
inside the canonical repo-owned path.

Purpose: give the user a single desktop entry point for RayMe on OMEN-PC.

Output: a repo-owned PowerShell launcher, a Desktop `.lnk` created by
`scripts/deploy-omen.sh`, and updated runbook docs.

User-goal preservation: the launcher must preserve RayMe as a live phone-call
simulator by using the existing scheduled-task runtime; it must not change call,
TTS, STT, VAD, WebRTC, reconnect, or call UI behavior, and must not introduce a
generate-then-play or non-live mode.
</objective>

<execution_context>
@/home/agent/.codex/get-shit-done/workflows/quick.md
@AGENTS.md
@.planning/LIVE-CALL-INVARIANTS.md
@.planning/OPERATING-NOTES.md
@scripts/deploy-omen.sh
@web-ui/server/docs/HTTPS-LAN.md
</execution_context>

<context>
OMEN deployment constraints:
- Deploying to OMEN-PC must go through `scripts/deploy-omen.sh`.
- Do not manually create/delete `RayMePhase1AI` or `RayMePhase1Web` scheduled
  tasks.
- `RayMePhase1AI` and `RayMePhase1Web` must continue pointing only to
  `C:\Users\pmpg\rayme\start-ai-backend.cmd` and
  `C:\Users\pmpg\rayme\start-web-ui.cmd`.
- The deploy script writes those canonical launchers and verifies health.
- Push local commits before expecting OMEN-PC to pull them.

Implementation approach:
- Add a repo-owned script at `scripts/start-rayme-omen.ps1`. Because it lives
  in the canonical checkout, it is not an ad-hoc OMEN-side script.
- The script may query/run existing scheduled tasks and open
  `https://192.168.1.199:8443`; it must not create/delete/reconfigure tasks.
- Extend `scripts/deploy-omen.sh` to create/update a Desktop shortcut pointing
  to the repo-owned script after the checkout has been fast-forwarded.
- Do not add any new launcher files under `C:\Users\pmpg\rayme\` beyond the two
  canonical scheduled task launchers already owned by the deploy script.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repo-owned OMEN desktop launcher target</name>
  <files>scripts/start-rayme-omen.ps1</files>
  <action>
    Create a PowerShell launcher script that:
    - Uses `C:\Users\pmpg\rayme\RayMe` as the canonical repo root.
    - Confirms the canonical `RayMePhase1AI` and `RayMePhase1Web` scheduled
      tasks exist.
    - Starts the AI task if port 9443 is not already listening, waits for it,
      then starts the Web task if port 8443 is not already listening and waits
      for it.
    - Opens `https://192.168.1.199:8443` after the Web listener is ready.
    - Emits clear console output and points failures back to
      `scripts/deploy-omen.sh`.
    - Does not create, delete, or modify scheduled tasks.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$tokens = $null; $errors = $null; [System.Management.Automation.Language.Parser]::ParseFile('scripts/start-rayme-omen.ps1', [ref]$tokens, [ref]$errors) | Out-Null; if ($errors.Count) { $errors | ForEach-Object { $_.ToString() }; exit 1 }"</automated>
  </verify>
  <done>The launcher script parses, starts only the canonical scheduled tasks when their listeners are absent, and opens the LAN Web UI.</done>
</task>

<task type="auto">
  <name>Task 2: Have canonical OMEN deploy create the Desktop shortcut</name>
  <files>scripts/deploy-omen.sh, web-ui/server/docs/HTTPS-LAN.md</files>
  <action>
    Extend `scripts/deploy-omen.sh` so the remote PowerShell deployment creates
    or updates `Run RayMe.lnk` in the current Windows user's Desktop folder.
    The shortcut must target Windows PowerShell with `-File
    C:\Users\pmpg\rayme\RayMe\scripts\start-rayme-omen.ps1`, set the working
    directory to the repo checkout, and be created only by the canonical deploy
    script.

    Update `web-ui/server/docs/HTTPS-LAN.md` to document that the Desktop
    shortcut is created by `scripts/deploy-omen.sh`, starts/opens RayMe through
    existing scheduled tasks, and is not a replacement deployment path.
  </action>
  <verify>
    <automated>bash -n scripts/deploy-omen.sh</automated>
    <automated>rg -n "start-rayme-omen|Run RayMe|CreateShortcut|RayMePhase1AI|RayMePhase1Web" scripts/deploy-omen.sh scripts/start-rayme-omen.ps1 web-ui/server/docs/HTTPS-LAN.md</automated>
  </verify>
  <done>The deploy script creates the Desktop shortcut and documentation points users back to the canonical deploy/runtime path.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy and verify on OMEN-PC</name>
  <files>scripts/deploy-omen.sh, scripts/start-rayme-omen.ps1</files>
  <action>
    Commit and push the repo changes to `origin/main`, then run
    `OMEN_SSH_ALIAS=rayme-pmpg scripts/deploy-omen.sh`. After deployment,
    verify over SSH that the Desktop shortcut exists and that the OMEN checkout
    is at the expected commit. Do not manually create or edit the shortcut on
    OMEN-PC outside the deploy script.
  </action>
  <verify>
    <manual_or_live>OMEN deploy through `scripts/deploy-omen.sh` passes health checks and `ssh rayme-pmpg` confirms the Desktop `Run RayMe.lnk` exists.</manual_or_live>
  </verify>
  <done>OMEN-PC has the Desktop launcher at the deployed commit, and deploy health checks pass.</done>
</task>

</tasks>

<verification>
- `scripts/start-rayme-omen.ps1` parses as PowerShell.
- `bash -n scripts/deploy-omen.sh` passes.
- Static search confirms the deploy script creates `Run RayMe.lnk` and the
  launcher references only existing scheduled tasks.
- `OMEN_SSH_ALIAS=rayme-pmpg scripts/deploy-omen.sh` passes.
- `ssh rayme-pmpg` confirms the Desktop shortcut exists at the deployed commit.
</verification>

<success_criteria>
- OMEN-PC Desktop contains a `Run RayMe.lnk` shortcut after canonical deploy.
- The shortcut starts existing scheduled tasks if needed and opens
  `https://192.168.1.199:8443`.
- Scheduled task definitions and canonical `.cmd` launchers remain owned by
  `scripts/deploy-omen.sh`.
- No ad-hoc OMEN deployment scripts or manual scheduled-task edits are used.
</success_criteria>

<output>
After completion, create `.planning/quick/260602-1hh-add-a-launcher-for-omen-pc-on-the-window/260602-1hh-SUMMARY.md`
</output>
