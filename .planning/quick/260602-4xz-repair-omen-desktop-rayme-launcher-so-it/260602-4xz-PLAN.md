---
phase: quick-260602-4xz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/start-rayme-omen.ps1
  - scripts/deploy-omen.sh
  - web-ui/server/docs/HTTPS-LAN.md
autonomous: true
requirements: []
---

<objective>
Repair the OMEN-PC Desktop launcher UX so it no longer looks suspicious, no
longer opens the RayMe page automatically, and no longer leaves blank command
prompt windows for the AI/Web services.

Purpose: the Desktop launcher should be a quiet, normal local convenience entry
point for starting RayMe services, not a console transcript of scheduled-task
commands.

Output: updated repo-owned launcher script, updated canonical deploy script,
updated runbook docs, deployed and verified on OMEN-PC.

User-goal preservation: this repair must preserve RayMe's live phone-call
runtime behavior by continuing to use the existing scheduled-task service path;
it must not touch call, TTS, STT, VAD, WebRTC, reconnect, call UI, or introduce
any non-live generated-audio mode.
</objective>

<execution_context>
@AGENTS.md
@.planning/LIVE-CALL-INVARIANTS.md
@.planning/OPERATING-NOTES.md
@scripts/start-rayme-omen.ps1
@scripts/deploy-omen.sh
@web-ui/server/docs/HTTPS-LAN.md
</execution_context>

<context>
User feedback:
- Current shortcut looks weird and suspicious.
- It prints raw scheduled-task success output.
- It opens two blank command prompts.
- It should not open the web page automatically.

Constraints:
- OMEN deployment must still go through `scripts/deploy-omen.sh`.
- `RayMePhase1AI` and `RayMePhase1Web` scheduled tasks must continue pointing
  to `C:\Users\pmpg\rayme\start-ai-backend.cmd` and
  `C:\Users\pmpg\rayme\start-web-ui.cmd`.
- Do not add ad-hoc OMEN-side deployment scripts.
- Do not manually edit scheduled tasks outside the canonical deploy script.

Observed pre-check:
- OMEN CurrentUser PowerShell execution policy is `RemoteSigned`, so the
  Desktop shortcut does not need `-ExecutionPolicy Bypass` for the local repo
  script.
- Both AI and Web venvs include `pythonw.exe`.
- `pythonw.exe` can still redirect stdout to the RayMe log directory.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the Desktop launcher quiet and non-navigating</name>
  <files>scripts/start-rayme-omen.ps1, scripts/deploy-omen.sh</files>
  <action>
    Update `scripts/start-rayme-omen.ps1` so:
    - It has `-Quiet` and `-OpenBrowser` switches.
    - The shortcut path uses `-Quiet` and does not pass `-OpenBrowser`.
    - Raw `schtasks.exe /Run` success output is captured and suppressed unless
      there is a failure.
    - `Start-Process $WebUrl` only happens when `-OpenBrowser` is explicitly
      passed.
    - `scripts/deploy-omen.sh` creates the shortcut without
      `-ExecutionPolicy Bypass`, and with a description that says it starts
      services without opening a browser.
  </action>
  <verify>
    <automated>Windows PowerShell parser reports 0 errors for `scripts/start-rayme-omen.ps1`.</automated>
    <automated>`rg -n "ExecutionPolicy Bypass|Start-Process \\$WebUrl|Out-Host" scripts/start-rayme-omen.ps1 scripts/deploy-omen.sh` shows no shortcut bypass, no unconditional browser open, and no raw schtasks output path.</automated>
  </verify>
  <done>The launcher starts services quietly by default and does not open the browser unless explicitly requested.</done>
</task>

<task type="auto">
  <name>Task 2: Stop service scheduled tasks from owning visible command windows</name>
  <files>scripts/deploy-omen.sh, web-ui/server/docs/HTTPS-LAN.md</files>
  <action>
    Update the canonical `start-ai-backend.cmd` and `start-web-ui.cmd` content
    written by `scripts/deploy-omen.sh` to run the server scripts through
    `pythonw.exe` instead of `python.exe`, preserving the same working
    directory, environment variables, command arguments, and log redirection.

    Document that the Desktop shortcut starts services only and that the user
    opens `https://192.168.1.199:8443` manually after startup.
  </action>
  <verify>
    <automated>`bash -n scripts/deploy-omen.sh` passes.</automated>
    <manual_or_live>`OMEN_SSH_ALIAS=rayme-pmpg scripts/deploy-omen.sh` passes and recreates the Desktop shortcut.</manual_or_live>
    <manual_or_live>OMEN shortcut target does not include `ExecutionPolicy Bypass`; shortcut arguments include `-Quiet`; scheduled task `.cmd` files use `pythonw.exe`; live health checks pass.</manual_or_live>
  </verify>
  <done>The Desktop launcher no longer opens the page automatically, does not print raw scheduled-task success output, and the service task launchers no longer keep blank command prompts open.</done>
</task>

</tasks>

<verification>
- Local shell syntax and static searches pass.
- Windows PowerShell parser over SSH reports no launcher parse errors.
- Canonical deploy to OMEN-PC passes.
- Shortcut exists on `C:\Users\pmpg\Desktop\Run RayMe.lnk` and points to the
  repo script with `-Quiet`, without `ExecutionPolicy Bypass`.
- Deployed `start-ai-backend.cmd` and `start-web-ui.cmd` use `pythonw.exe`.
- AI/Web health remains reachable after deploy.
</verification>

<success_criteria>
- Clicking the Desktop shortcut starts RayMe services quietly.
- The shortcut does not open a browser automatically.
- The user no longer sees raw `schtasks` success output from the launcher.
- The service launchers no longer leave blank command prompt windows on the
  Desktop.
- The canonical OMEN deployment path remains the only deployment path used.
</success_criteria>

<output>
After completion, create `.planning/quick/260602-4xz-repair-omen-desktop-rayme-launcher-so-it/260602-4xz-SUMMARY.md`.
</output>
