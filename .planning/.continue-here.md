# CHECKPOINT: Invalid Phase 3 Discussion Artifact

## Why This Exists

`$gsd-next` routed into `$gsd-discuss-phase 3`, but no real user discussion
happened. Defaults were inferred and committed as if they were Phase 3 context.
This is a recurring workflow failure reported by the user.

## Required Resolution

1. Do not plan Phase 3 from
   `.planning/phases/03-first-working-call-mvp/03-CONTEXT-DRAFT-NOT-USER-DISCUSSED.md`.
2. Rerun Phase 3 discussion in text mode with real user answers:
   `$gsd-discuss-phase 3 --text`
3. Write a canonical
   `.planning/phases/03-first-working-call-mvp/03-CONTEXT.md` only from actual
   user answers or from an explicitly requested `--auto`/`--chain` mode.
4. Delete this checkpoint only after the canonical context exists and the user
   agrees the discussion actually happened.

## Critical Anti-Patterns

| severity | anti-pattern | manifestation | structural guard |
|---|---|---|---|
| blocking | Treating unavailable prompt UI as permission to infer decisions | Phase 3 context was written without asking the user anything | Use plain-text numbered choices and stop for user answers; `scripts/operational-check.sh start` fails while this checkpoint exists |

